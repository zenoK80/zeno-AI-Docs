# 학습 과목 문서 지침

독학사·시험·자격 학습 MDX를 작성할 때 따르는 공통 규칙이다. 상세 MDX 형식·파싱 규칙은 `CLAUDE.md`를 함께 따른다.

---

## 작업 흐름

1. `plan-study/reference/[과목명]/` 폴더가 있으면 먼저 읽어 시험 범위를 파악한다.
2. `plan-study/[과목명].md`의 목차와 작성 기준을 읽는다.
3. 해당 과목의 `_meta.js`를 실제 파일과 함께 갱신한다.
4. 과목 1개 완성 → 커밋 → push 순으로 진행한다.
5. 지정되지 않은 다른 과목은 건드리지 않는다.

---

## 폴더·파일 규칙

- 문서 위치: `content/<상위메뉴>/<과목폴더>/NN_slug.mdx`
- 파일명: `NN_kebab-case.mdx` (두 자리 숫자 + 언더스코어 + 소문자-케밥)
- frontmatter: `title`, `description` 두 필드만 사용
- 파일 추가·삭제·이름 변경 시 반드시 `_meta.js` 함께 갱신
- 아직 작성 안 된 파일은 `_meta.js`에 등록하지 않는다

---

## 콘텐츠 기준

- **분량**: 본문 10~15분, 마무리 복습 5~8분
- **범위**: 핵심 개념 2~4개. 긴 도입·반복 요약 제외
- **초보 친화·심화 기준**: `CLAUDE.md`의 `콘텐츠 작성 원칙` 규칙 전부 적용
  - 영단어 첫 등장 시 한국어 병기
  - 약어 첫 등장 시 풀어쓰기 (핵심 개념 약어는 어원 분해 표 사용)
  - 수학 기호 이름·읽는 법 설명
  - 공식 아래 변수 전체 설명
  - 비유·생활 예시 1개 이상
  - `쉽게 말하면` 요약 포함
  - `왜 필요한가` 먼저 설명
  - 작은 예시 데이터로 손으로 계산하는 과정을 단계별로 보여주기(중간 단계 생략 금지)

---

## 마무리 복습 규칙

- 같은 문서 끝에 `## 마무리 복습` 섹션 추가
- 전역 `<Quiz>` 컴포넌트 사용 (import 불필요)
- **일반 학습 파일**: Quiz **5~8개**
- **기출·모의 파일**: Quiz **15~25개**
- 각 Quiz: `questionNumber`, `question`, `options`, `correctAnswer`, `explanation`, `optionExplanations` 모두 작성
- 해설: 정답 근거 + 각 오답이 틀린 이유

---

## 시각화 우선순위

| 내용 유형 | 우선 도구 |
|-----------|-----------|
| 수식·공식 | KaTeX (`$...$`, `$$...$$`) |
| 단계·흐름·관계 | Table 또는 Steps (Mermaid는 이 프로젝트에서 작동하지 않음) |
| 분포·비교 차트 | Recharts |
| 함수·벡터·좌표 | JSXGraph |
| 노드 직접 조작 흐름도 | React Flow (필요할 때만) |
| JS 코드 실행 | CodePlayground (Sandpack 래퍼) |

- 시각화 도구는 이해에 실질적으로 도움될 때만 사용
- 한 문서에 모든 도구를 억지로 넣지 않는다
- 패키지 기반 시각화는 `app/components/`의 클라이언트 컴포넌트를 재사용

---

## 참고 자료

- 모든 MDX는 예외 없이 맨 아래에 `## 참고 자료` 섹션을 작성한다.
- `plan-study/reference/[과목]/` 파일이 있으면 그 폴더 안의 구체적인 파일을 참고 자료로 표기한다.
- 레퍼런스 폴더가 없으면 공식 출제 기준·공식 문서 URL을 표기한다.
- 기출 문제를 그대로 복제하지 않는다. 공식 출제 범위 기반 재구성 문제를 작성한다.

---

## 품질 기준

- 수학: 기호 읽는 법과 식 변환 이유 생략 금지
- 통계: 원자료(작은 숫자 데이터) 먼저 보여주고 결과를 일상어로 해석
- 시험 범위: `plan-study/reference/` 또는 공식 출제 기준을 우선
- 검증: 일반 작업은 `npm.cmd run lint`, 구조 변경 시 전체 빌드 1회 실행

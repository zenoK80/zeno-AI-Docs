---
title: '05-복습. WebGL과 3D 그래픽 개요'
description: 'Canvas 2D와 WebGL의 차이를 4지선다 퀴즈로 점검하고, 실제 Three.js 예제를 둘러보는 탐구 과제를 진행한다.'
---

## 05편 복습

이번 편은 코딩 실습 대신 **개념 정리와 탐구 과제**로 마무리한다.

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"Canvas 2D와 WebGL은 각각 무엇으로 그림을 계산하나?"}
  options={[
    "둘 다 CPU로 계산한다",
    "둘 다 GPU로 계산한다",
    "Canvas 2D는 CPU, WebGL은 GPU로 계산한다",
    "Canvas 2D는 GPU, WebGL은 CPU로 계산한다",
  ]}
  correctAnswer={3}
  explanation={"CPU(일반적인 연산을 순차적으로 처리)는 Canvas 2D의 계산을 담당하고, GPU(대량의 계산을 동시에 병렬로 처리)는 WebGL의 계산을 담당한다."}
  optionExplanations={[
    "Canvas 2D만 CPU를 쓴다. WebGL은 병렬 처리에 특화된 GPU를 쓴다.",
    "WebGL만 GPU를 쓴다. Canvas 2D는 CPU로 계산한다.",
    "정답이다. Canvas 2D는 CPU, WebGL은 GPU로 계산을 처리한다.",
    "반대로 설명됐다. Canvas 2D가 CPU를, WebGL이 GPU를 쓴다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"WebGL에서 그리기 로직(정점·색상 계산)을 짜는 데 쓰는 언어는?"}
  options={[
    "자바스크립트",
    "GLSL(OpenGL Shading Language)이라는 별도 언어",
    "HTML",
    "Python",
  ]}
  correctAnswer={2}
  explanation={"셰이더(GPU에서 실행되는 작은 프로그램)는 자바스크립트가 아니라 GLSL이라는 완전히 별도의 언어로 짠다. 이 코드를 GPU에 업로드해서 정점과 픽셀의 계산을 지시한다."}
  optionExplanations={[
    "자바스크립트는 WebGL API를 호출하고 셰이더 코드를 문자열로 전달하는 역할만 한다. 실제 계산 로직은 GLSL로 짠다.",
    "정답이다. 정점·색상 계산 로직은 GLSL이라는 별도 언어로 작성해서 GPU에 업로드한다.",
    "HTML은 마크업 언어라 그리기 계산 로직을 작성할 수 없다.",
    "Python은 WebGL과 관계없는 언어다. WebGL은 브라우저의 자바스크립트 환경에서 GLSL과 함께 쓰인다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"실무에서 WebGL을 다룰 때 보통 어떤 방식을 쓰나?"}
  options={[
    "항상 순수 WebGL 코드를 처음부터 직접 짠다",
    "Three.js, PixiJS 같은 라이브러리를 통해 간접적으로 사용한다",
    "WebGL은 실무에서 거의 쓰이지 않는다",
    "Photoshop 같은 이미지 편집 프로그램으로 대체한다",
  ]}
  correctAnswer={2}
  explanation={"순수 WebGL은 삼각형 하나를 그리는 데도 셰이더 작성, 버퍼 생성 등 수십 줄의 준비 코드가 필요할 만큼 복잡하다. 그래서 대부분의 실무 개발자는 이 복잡함을 감춰주는 라이브러리를 통해 WebGL의 능력을 간접적으로 활용한다."}
  optionExplanations={[
    "순수 WebGL은 준비 코드가 너무 많아서, 매번 처음부터 직접 짜는 방식은 실무에서 비효율적이다.",
    "정답이다. Three.js, PixiJS 같은 라이브러리로 복잡함을 감추고 간접적으로 WebGL을 활용하는 게 실무 방식이다.",
    "게임, 데이터 시각화, 3D 제품 뷰어 등 실무에서 WebGL(정확히는 그 위의 라이브러리)이 활발히 쓰인다.",
    "이미지 편집 프로그램은 정적인 이미지 편집 도구일 뿐, 실시간 3D 렌더링을 대체할 수 없다.",
  ]}
/>

## 정리 체크리스트

<Callout type="default">
- [ ] Canvas 2D와 WebGL의 근본적인 차이(CPU vs GPU)를 안다
- [ ] WebGL이 브라우저용 OpenGL ES라는 걸 안다
- [ ] 실무에서 왜 라이브러리를 거쳐 쓰는지 이해했다
</Callout>

이걸로 Canvas·그래픽 파트를 마쳤다. 다음 파트부터는 접근성·성능·SEO를 다룬다.

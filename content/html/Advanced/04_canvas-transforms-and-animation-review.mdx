---
title: '04-복습. Canvas 변형과 애니메이션'
description: 'save/restore와 requestAnimationFrame을 4지선다 퀴즈로 점검하고, 좌우로 튕기는 공 애니메이션을 직접 만들어 본다.'
---

## 04편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"`save()`와 `restore()`를 쓰지 않고 여러 도형에 변형(이동·회전 등)을 걸면 생기는 문제는?"}
  options={[
    "아무 문제 없다. 원래 안 써도 된다",
    "변형이 계속 누적되어, 다음에 그리는 도형들의 위치가 의도치 않게 어긋난다",
    "브라우저가 멈춘다",
    "색상이 자동으로 초기화된다",
  ]}
  correctAnswer={2}
  explanation={"`translate`, `rotate` 같은 변형은 한 번 걸면 그 뒤의 모든 그리기에 계속 영향을 준다. `save()`로 저장해두고 변형을 건 다음, `restore()`로 원상 복구하지 않으면 다음 도형에도 그 변형이 그대로 남는다."}
  optionExplanations={[
    "변형이 계속 누적되므로 실제로는 문제가 생긴다. `save()`/`restore()`는 이 문제를 막기 위한 안전장치다.",
    "정답이다. 변형을 되돌리지 않으면 그 뒤에 그리는 모든 도형의 위치가 계속 어긋나게 된다.",
    "브라우저가 멈추는 심각한 오류가 아니라, 도형이 의도한 자리에 그려지지 않는 시각적인 문제다.",
    "색상 설정과는 관계없는 기능이다. `save()`/`restore()`는 변형 상태를 다루는 것이다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"`setInterval` 대신 `requestAnimationFrame`을 애니메이션에 쓰는 이유는?"}
  options={[
    "`requestAnimationFrame`이 문법적으로 더 짧아서",
    "브라우저의 화면 주사율에 맞춰 정확한 타이밍에 실행되고, 탭이 백그라운드에 있으면 자동으로 멈춰 배터리를 아끼기 때문에",
    "`setInterval`은 더 이상 지원되지 않는 옛날 기능이라서",
    "`requestAnimationFrame`만 애니메이션에 쓸 수 있고 다른 용도로는 못 쓴다",
  ]}
  correctAnswer={2}
  explanation={"`requestAnimationFrame`은 브라우저가 실제 화면 갱신 주기에 맞춰 호출해주고, 탭이 안 보이는 상태면 자동으로 멈춰 자원을 아낀다."}
  optionExplanations={[
    "함수 이름 길이는 선택 이유와 관계없다. 핵심은 화면 갱신 타이밍과 자원 절약 동작이다.",
    "정답이다. 화면 주사율에 맞춘 정확한 타이밍과, 백그라운드 탭에서 자동으로 멈추는 자원 절약이 핵심 이유다.",
    "`setInterval`은 지금도 정상적으로 지원되는 함수다. 다만 애니메이션 용도로는 덜 적합할 뿐이다.",
    "`requestAnimationFrame`은 애니메이션 외에도 다음 리페인트 전에 실행할 작업이 필요한 다양한 곳에 쓸 수 있다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"`rotate()`로 회전시키면 항상 무엇을 기준으로 도는가?"}
  options={[
    "캔버스 전체의 정중앙",
    "현재 원점(translate로 옮겨둔 그 지점)",
    "도형 자신의 중심 (자동으로 계산해준다)",
    "마우스 커서 위치",
  ]}
  correctAnswer={2}
  explanation={"회전은 항상 현재 원점을 중심으로 일어난다. 특정 도형을 그 도형 중심으로 돌리고 싶다면, 먼저 `translate()`로 원점을 그 도형의 중심 위치로 옮긴 다음 `rotate()`를 호출해야 한다."}
  optionExplanations={[
    "캔버스 정중앙이 아니라, `translate()`로 옮겨둔 현재 원점을 기준으로 회전한다.",
    "정답이다. `rotate()`는 항상 현재 원점을 중심으로 돈다.",
    "도형 중심을 자동으로 계산해서 회전시켜주지 않는다. 원점을 직접 옮겨야 원하는 지점을 기준으로 돌릴 수 있다.",
    "마우스 위치와는 관계없이, 코드에서 `translate()`로 지정한 원점을 기준으로 회전한다.",
  ]}
/>

## 실습 — 좌우로 튕기는 공 만들기

아래 뼈대를 완성해서, 원이 캔버스 좌우 벽에 부딪히면 방향을 바꾸는 애니메이션을 만들어보자.

```html
<canvas id="stage" width="300" height="150"></canvas>
```

```js
const canvas = document.querySelector('#stage')
const ctx = canvas.getContext('2d')

let x = 50
let dx = 2   // 이동 방향과 속도

function draw() {
  ctx.clearRect(0, 0, canvas.width, canvas.height)

  ctx.beginPath()
  ctx.arc(x, 75, 20, 0, Math.PI * 2)
  ctx.fillStyle = 'crimson'
  ctx.fill()

  x += dx

  // 여기에: x가 캔버스 경계에 닿으면 dx의 부호를 바꾸는 코드를 추가해 보자

  requestAnimationFrame(draw)
}

requestAnimationFrame(draw)
```

<Collapse title="정답 보기">

```js
if (x + 20 > canvas.width || x - 20 < 0) {
  dx = -dx
}
```

`draw()` 함수 안, `x += dx` 다음 줄에 넣으면 된다.
</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] `save()`/`restore()`로 변형을 격리할 수 있다
- [ ] `requestAnimationFrame` 애니메이션 루프 패턴을 안다
- [ ] 경계에 닿으면 방향을 바꾸는 로직을 직접 짜봤다
</Callout>

다음 편에서는 Canvas 2D를 넘어 WebGL을 개념적으로 살펴본다.

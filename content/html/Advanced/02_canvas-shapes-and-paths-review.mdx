---
title: '02-복습. Canvas 도형과 경로'
description: '경로 그리기 순서를 4지선다 퀴즈로 점검하고, 삼각형과 원을 조합해 간단한 그림을 만들어 본다.'
---

## 02편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"`moveTo`/`lineTo`만 호출하고 `fill()`이나 `stroke()`를 호출하지 않으면 어떻게 되나?"}
  options={[
    "자동으로 검은색으로 그려진다",
    "아무것도 화면에 그려지지 않는다",
    "에러가 난다",
    "이전에 그린 도형이 지워진다",
  ]}
  correctAnswer={2}
  explanation={"`moveTo`와 `lineTo`는 경로를 설계할 뿐, 그 자체로는 화면에 아무것도 나타나지 않는다. 마지막에 `fill()`이나 `stroke()`를 호출해야 비로소 실제로 그려진다."}
  optionExplanations={[
    "자동으로 색이 칠해지지 않는다. 색을 칠하려면 `fill()`이나 `stroke()`를 직접 호출해야 한다.",
    "정답이다. 경로만 설계했을 뿐 `fill()`/`stroke()`를 부르지 않으면 아무것도 그려지지 않는다.",
    "`fill()`/`stroke()`를 호출하지 않는 것 자체는 문법 오류가 아니라서 에러가 나지 않는다.",
    "경로 설계는 그리기가 아니라서, 이전에 이미 그려진(픽셀로 칠해진) 도형에는 영향을 주지 않는다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"`arc()`로 원을 그릴 때 각도의 단위는?"}
  options={[
    "도(degree) — 360이 한 바퀴",
    "라디안(radian) — `Math.PI * 2`가 한 바퀴",
    "퍼센트 — 100이 한 바퀴",
    "픽셀",
  ]}
  correctAnswer={2}
  explanation={"`arc()`는 라디안을 쓴다. 완전한 원 하나는 `Math.PI * 2`(약 6.28)로 표현한다."}
  optionExplanations={[
    "우리에게 익숙한 도(degree) 단위가 아니라 라디안 단위를 쓰는 게 `arc()`의 헷갈리는 지점이다.",
    "정답이다. `arc()`는 라디안 단위를 쓰며, 한 바퀴는 `Math.PI * 2`다.",
    "각도를 퍼센트로 표현하는 방식은 `arc()`에 존재하지 않는다.",
    "픽셀은 길이의 단위이지 각도의 단위가 아니다. `arc()`의 각도 인자는 라디안 값이다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"새로운 도형을 그리기 전에 `beginPath()`를 호출하는 이유는?"}
  options={[
    "캔버스 전체를 지우기 위해서",
    "이전에 그리던 경로와 지금 그릴 경로를 분리하기 위해서",
    "색상을 초기화하기 위해서",
    "사실 없어도 결과는 똑같다",
  ]}
  correctAnswer={2}
  explanation={"`beginPath()`를 호출하지 않으면, 새로 그리는 경로가 이전 경로에 계속 이어 붙어서 의도하지 않은 모양이 만들어질 수 있다."}
  optionExplanations={[
    "캔버스 전체를 지우는 건 `clearRect()`의 역할이다. `beginPath()`는 지우기가 아니라 경로를 새로 시작하는 것이다.",
    "정답이다. `beginPath()`는 이전 경로와 새 경로를 분리해서 의도치 않게 이어 붙는 걸 막는다.",
    "색상 설정(`fillStyle`, `strokeStyle`)과는 별개의 기능이다. 색을 초기화하지 않는다.",
    "`beginPath()`를 생략하면 이전 경로에 새 경로가 이어 붙어 의도하지 않은 모양이 그려질 수 있다.",
  ]}
/>

## 실습 — 집 모양 그려보기

삼각형(지붕)과 사각형(몸통), 원(창문)을 조합해서 간단한 집 모양을 그려보자.

```js
const ctx = document.querySelector('#stage').getContext('2d')

// 1. 몸통: fillRect로 사각형 하나
// 2. 지붕: beginPath → moveTo/lineTo 세 번 → closePath → fill로 삼각형
// 3. 창문: beginPath → arc → fill로 원 하나
```

<Collapse title="정답 예시">

```js
// 몸통
ctx.fillStyle = 'peru'
ctx.fillRect(50, 100, 120, 100)

// 지붕
ctx.beginPath()
ctx.moveTo(40, 100)
ctx.lineTo(110, 40)
ctx.lineTo(180, 100)
ctx.closePath()
ctx.fillStyle = 'firebrick'
ctx.fill()

// 창문
ctx.beginPath()
ctx.arc(110, 150, 20, 0, Math.PI * 2)
ctx.fillStyle = 'skyblue'
ctx.fill()
```

</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] 경로 그리기 순서(beginPath → moveTo/lineTo/arc → fill/stroke)를 안다
- [ ] `arc()`로 원을 그릴 수 있다
- [ ] 도형 여러 개를 조합해 그림을 만들어봤다
</Callout>

다음 편에서는 텍스트와 이미지를 캔버스에 그린다.

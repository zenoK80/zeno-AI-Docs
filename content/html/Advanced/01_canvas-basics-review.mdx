---
title: '01-복습. Canvas 요소와 2D 렌더링 컨텍스트'
description: 'canvas와 getContext의 관계를 4지선다 퀴즈로 점검하고, 직접 사각형 두 개를 그려 배치해 본다.'
---

## 01편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"`<canvas>` 태그만 HTML에 써두면 화면에 무엇이 나타나나?"}
  options={[
    "회색 격자무늬",
    "아무것도 없는 빈 사각형(투명한 도화지)",
    "\"여기에 그림을 그리세요\"라는 안내 문구",
    "브라우저마다 기본 그림이 다르게 나타난다",
  ]}
  correctAnswer={2}
  explanation={"`<canvas>`는 그림을 그릴 수 있는 빈 도화지만 마련해줄 뿐, 무엇을 그릴지는 전부 JavaScript로 지시해야 한다."}
  optionExplanations={[
    "격자무늬 같은 기본 표시는 없다. `<canvas>`는 완전히 빈 상태로 시작한다.",
    "정답이다. `<canvas>`는 아무것도 그려지지 않은 투명한 빈 사각형으로 시작한다.",
    "안내 문구 같은 기본 텍스트는 표시되지 않는다. 화면에 뭔가 그리려면 JavaScript 코드가 필요하다.",
    "표준 HTML 요소이므로 브라우저마다 기본 모습이 달라지지 않는다. 항상 빈 사각형이다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"캔버스 크기를 CSS(`width`, `height` 속성이 아니라 CSS 스타일)로만 지정하면 안 되는 이유는?"}
  options={[
    "CSS로는 아예 크기 지정이 안 된다",
    "CSS로 크기를 주면 내부 좌표계(실제 해상도)는 그대로인 채 화면에서만 늘어나서 그림이 흐릿해진다",
    "CSS로 지정하면 캔버스가 사라진다",
    "속도가 느려질 뿐 화질에는 문제가 없다",
  ]}
  correctAnswer={2}
  explanation={"캔버스에는 두 가지 크기가 있다 — HTML `width`/`height` 속성이 정하는 \"실제 그리기 해상도\"와, CSS가 정하는 \"화면에 보이는 크기\"다. CSS로만 키우면 저해상도 이미지를 억지로 확대한 것처럼 흐려진다."}
  optionExplanations={[
    "CSS로 크기 지정 자체는 가능하다. 문제는 그 크기가 실제 해상도(좌표계)까지 바꿔주지는 않는다는 점이다.",
    "정답이다. CSS만으로 크기를 키우면 내부 해상도는 그대로인 채 화면에서만 확대되어 흐려진다.",
    "캔버스가 사라지는 게 아니라, 화면에 보이는 크기와 내부 해상도가 어긋나서 흐리게 보일 뿐이다.",
    "단순히 속도 문제가 아니라, 저해상도 그림을 억지로 늘린 것처럼 화질 자체가 흐려지는 문제다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"실제로 도형을 그리는 메서드(`fillRect` 등)는 어디에 있나?"}
  options={[
    "`canvas` 요소 자신에게 있다",
    "`canvas.getContext('2d')`로 받은 컨텍스트 객체(`ctx`)에 있다",
    "`document` 객체에 있다",
    "별도로 설치해야 하는 라이브러리에 있다",
  ]}
  correctAnswer={2}
  explanation={"`<canvas>` 요소 자체는 그림을 그릴 \"도화지\"일 뿐이고, 실제로 선을 긋고 색을 칠하는 도구들은 `getContext('2d')`로 요청해서 받은 렌더링 컨텍스트 객체 안에 들어있다."}
  optionExplanations={[
    "`canvas` 요소 자체에는 `fillRect` 같은 그리기 메서드가 없다. 먼저 컨텍스트를 받아야 한다.",
    "정답이다. `getContext('2d')`로 받은 컨텍스트 객체(`ctx`)가 실제 그리기 메서드를 갖고 있다.",
    "`document` 객체는 문서 전체를 다루는 객체이지, 캔버스 그리기 메서드를 갖고 있지 않다.",
    "`fillRect` 같은 2D 그리기 메서드는 브라우저에 내장된 표준 API라 별도 라이브러리 설치가 필요 없다.",
  ]}
/>

## 실습 — 사각형 두 개 나란히 그리기

아래 뼈대를 완성해서, 빨간 채워진 사각형과 파란 테두리 사각형을 나란히 그려보자.

```html
<canvas id="stage" width="300" height="150"></canvas>
```

```js
const canvas = document.querySelector('#stage')
const ctx = canvas.getContext('2d')

// 여기에 코드를 채워보자
// 1. fillStyle을 'crimson'으로, (20, 20) 위치에 80x60 채워진 사각형
// 2. strokeStyle을 'navy'로, (120, 20) 위치에 80x60 테두리 사각형
```

<Collapse title="정답 보기">

```js
ctx.fillStyle = 'crimson'
ctx.fillRect(20, 20, 80, 60)

ctx.strokeStyle = 'navy'
ctx.strokeRect(120, 20, 80, 60)
```

</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] canvas 요소와 컨텍스트의 관계를 설명할 수 있다
- [ ] 크기를 HTML 속성으로 줘야 하는 이유를 안다
- [ ] `fillRect`/`strokeRect`를 직접 써봤다
</Callout>

다음 편에서는 사각형을 넘어 자유로운 도형과 경로를 그린다.

---
title: '28. 기출 유형 집중 연습 2회 — SQL 기본'
description: '62·60·58회 기출을 분석해 만든 SQL 기본 집중 연습 15문제입니다. NULL 처리, 함수, 조인, 제약조건 핵심 함정 유형을 모두 담았습니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 2과목 SQL 기본 파트에서 반복 출제되는 함정 유형 15개를 기출 스타일 그대로 풀어봅니다. NULL 관련 함수와 집계 함수의 NULL 처리 방식을 특히 집중적으로 연습합니다.
</Callout>

## 풀이 전 체크

- NULL은 비교 연산자(=, !=)로 비교할 수 없고 IS NULL / IS NOT NULL만 사용 가능한가?
- NOT IN 서브쿼리에 NULL이 포함되면 결과가 공집합이 되는 이유를 설명할 수 있는가?
- COALESCE, NVL, NVL2, NULLIF의 차이를 구분할 수 있는가?
- GROUP BY와 HAVING의 처리 순서와 차이를 알고 있는가?

---

## 문제

<Quiz questionNumber={1} question="SQL의 특성에 대한 설명으로 옳지 않은 것은?" options={['집합적(Set-based) 언어이다.', '선언적(Declarative) 언어이다.', '절차적(Procedural) 언어이다.', '구조적(Structured) 언어이다.']} correctAnswer={3} explanation="SQL은 비절차적(선언적) 언어입니다. '어떻게 처리할지'가 아닌 '무엇을 원하는지'만 기술합니다. 절차적 언어는 실행 순서를 단계별로 명시해야 하며, PL/SQL이나 T-SQL 같은 SQL 확장 언어가 절차적 특성을 가집니다." optionExplanations={['SQL은 행 단위가 아닌 집합 단위로 데이터를 처리합니다. 맞는 설명입니다.', 'SQL은 결과만 지정하고 처리 방법은 옵티마이저가 결정합니다. 맞는 설명입니다.', 'SQL은 절차적 언어가 아닙니다. 이것이 틀린 설명입니다.', 'SQL(Structured Query Language)은 구조적 언어입니다. 맞는 설명입니다.']} />

<Quiz questionNumber={2} question="CTAS(CREATE TABLE ... AS SELECT)로 테이블을 복제할 때 자동으로 복제되는 것은?" options={['PRIMARY KEY 제약조건', 'NOT NULL 제약조건', 'UNIQUE 제약조건', 'FOREIGN KEY 제약조건']} correctAnswer={2} explanation="CTAS로 복제 시 PK, UNIQUE, CHECK, FK, DEFAULT 같은 제약조건은 복제되지 않습니다. 단, 컬럼의 NOT NULL 특성은 유지될 수 있습니다. 이는 NOT NULL이 컬럼 정의의 일부이기 때문입니다." optionExplanations={['PK는 CTAS로 복제되지 않습니다. 별도로 ALTER TABLE로 추가해야 합니다.', 'NOT NULL 특성은 CTAS 복제 시 유지될 수 있습니다. 정답입니다.', 'UNIQUE 제약조건은 CTAS로 복제되지 않습니다.', 'FK 제약조건은 CTAS로 복제되지 않습니다.']} />

<Quiz questionNumber={3} question="다음 CREATE TABLE 구문에서 NULL 값을 허용하는 컬럼은? (가: PRIMARY KEY, 나: NOT NULL, 다: UNIQUE, 라: CHECK(컬럼 IS NOT NULL))" options={['가', '나', '다', '라']} correctAnswer={3} explanation="UNIQUE 제약조건은 중복을 허용하지 않지만 NULL 값은 허용합니다(여러 행이 NULL을 가져도 UNIQUE 위반이 아닙니다). PK는 NOT NULL+UNIQUE이므로 NULL 불가, NOT NULL은 당연히 NULL 불가, CHECK(IS NOT NULL)는 명시적으로 NULL을 거부합니다." optionExplanations={['PK는 NOT NULL 특성이 내포되어 NULL을 허용하지 않습니다.', 'NOT NULL 제약이 있으므로 NULL 입력 시 오류가 발생합니다.', 'UNIQUE는 NULL을 허용합니다. 정답입니다.', 'CHECK(IS NOT NULL)는 NULL이 들어오면 CHECK 조건이 FALSE가 되어 입력이 거부됩니다.']} />

<Quiz questionNumber={4} question="컬럼 값이 100, 110, NULL, 50, 350, 200일 때 다음 SQL의 결과는? (WHERE 값 NOT BETWEEN 100 AND 200)" options={['1건', '2건', '3건', '4건']} correctAnswer={2} explanation="NOT BETWEEN 100 AND 200에 해당하는 값은 50과 350으로 2건입니다. NULL은 모든 비교 연산(BETWEEN 포함)에서 UNKNOWN을 반환하므로 결과에서 제외됩니다. 100, 110, 200은 BETWEEN 범위 내에 있어 NOT BETWEEN에서 제외됩니다." optionExplanations={['1건은 50 또는 350 중 하나만 선택한 오답입니다.', '50과 350, 두 건이 반환됩니다. 정답입니다.', '3건이 되려면 NULL이 포함되어야 하는데, NULL은 비교에서 UNKNOWN으로 처리되어 제외됩니다.', '4건이 되려면 100, 200도 포함되어야 하는데, 이들은 BETWEEN 범위 내에 있습니다.']} />

<Quiz questionNumber={5} question="PRICE 컬럼 값이 1000, 2000, NULL일 때 NULL을 0으로 출력하는 함수로 Oracle에서 올바른 것은?" options={['NULLIF(PRICE, 0)', 'NVL2(PRICE, PRICE, 0)', 'IFNULL(PRICE, 0)', 'NULLIF(0, PRICE)']} correctAnswer={2} explanation="NVL2(expr, not_null_val, null_val)에서 PRICE가 NULL이 아니면 PRICE 자체를, NULL이면 0을 반환합니다. COALESCE(PRICE, 0)도 같은 결과를 냅니다. NULLIF는 두 값이 같을 때 NULL을 반환하는 함수이고, IFNULL은 MySQL 함수입니다." optionExplanations={['NULLIF(PRICE, 0)은 PRICE가 0일 때 NULL을 반환하는 함수로 목적이 다릅니다.', 'NVL2(PRICE, PRICE, 0)은 PRICE가 NULL이 아니면 PRICE, NULL이면 0을 반환합니다. 정답입니다.', 'IFNULL은 MySQL 함수입니다. Oracle에서는 NVL 또는 COALESCE를 사용합니다.', 'NULLIF(0, PRICE)는 0과 PRICE가 같을 때 NULL을 반환합니다. NULL을 0으로 바꾸는 목적이 아닙니다.']} />

<Quiz questionNumber={6} question="Oracle에서 ROWNUM을 사용할 때 다음 중 2건의 결과가 나오지 않는 쿼리는? (테이블에 5건의 데이터 존재)" options={['WHERE ROWNUM <= 2', 'WHERE ROWNUM IN (1, 2)', 'WHERE ROWNUM BETWEEN 1 AND 2', 'WHERE ROWNUM >= 2']} correctAnswer={4} explanation="ROWNUM은 순차적으로 증가합니다. ROWNUM이 2가 되려면 먼저 1이 배정되어야 합니다. WHERE ROWNUM >= 2 조건은 ROWNUM=1인 행이 먼저 조건을 통과하지 못해 2가 부여되지 않아 결과적으로 공집합이 반환됩니다." optionExplanations={['ROWNUM <= 2는 1, 2번 행을 정상적으로 반환합니다.', 'ROWNUM IN (1, 2)도 1, 2번 행을 반환합니다.', 'ROWNUM BETWEEN 1 AND 2는 1, 2번 행을 반환합니다.', 'ROWNUM >= 2는 ROWNUM이 1부터 시작하므로 2 이상이 되는 행이 없어 공집합이 됩니다. 정답입니다.']} />

<Quiz questionNumber={7} question="AVG 집계 함수의 NULL 처리에 대한 설명으로 옳은 것은? (컬럼 값: 10, 20, NULL, 30)" options={['AVG 결과는 NULL이다.', 'AVG 결과는 15이다(NULL을 0으로 처리하여 60/4).', 'AVG 결과는 20이다(NULL 제외하여 60/3).', 'AVG 결과는 오류를 발생시킨다.']} correctAnswer={3} explanation="AVG를 포함한 집계 함수(SUM, MAX, MIN, COUNT(컬럼명))는 모두 NULL을 제외하고 계산합니다. 따라서 AVG(컬럼) = (10+20+30)/3 = 20입니다. COUNT(*)만 NULL을 포함하여 전체 행 수를 셉니다." optionExplanations={['집계 함수에 값이 없을 때 NULL을 반환하지만, 이 경우 NULL이 아닌 값이 있으므로 NULL이 아닙니다.', 'NULL을 0으로 처리하지 않습니다. AVG는 NULL을 제외합니다.', 'NULL을 제외한 (10+20+30)/3 = 20이 정확합니다. 정답입니다.', '집계 함수는 오류를 발생시키지 않고 NULL을 무시합니다.']} />

<Quiz questionNumber={8} question="GROUP BY의 기준 컬럼에 NULL 값이 있을 때의 처리 방식으로 옳은 것은?" options={['NULL은 GROUP BY에서 제외되어 결과에 나타나지 않는다.', 'NULL이 있으면 SQL 오류가 발생한다.', 'NULL 값을 가진 행들은 하나의 그룹으로 묶여 결과에 포함된다.', 'NULL은 자동으로 0 또는 공백으로 처리된다.']} correctAnswer={3} explanation="GROUP BY에서 NULL은 별도의 하나의 그룹으로 취급됩니다. NULL인 모든 행이 하나의 그룹을 이루어 집계 결과에 포함됩니다. DISTINCT에서도 마찬가지로 NULL은 하나의 고유 값처럼 처리됩니다." optionExplanations={['GROUP BY에서 NULL은 제외되지 않고 별도 그룹으로 포함됩니다.', 'NULL이 있어도 SQL 오류는 발생하지 않습니다.', 'NULL 행들은 하나의 그룹으로 묶입니다. 정답입니다.', 'NULL은 자동 변환되지 않고 NULL 그대로 그룹을 이룹니다.']} />

<Quiz questionNumber={9} question="NVL2(COL1, 'A', 'B')에서 COL1이 NULL일 때 반환하는 값은?" options={['COL1 원래 값', 'A', 'B', 'NULL']} correctAnswer={3} explanation="NVL2(expr, not_null_result, null_result)의 형식입니다. expr(COL1)이 NULL이 아닐 때는 두 번째 인수(A)를, NULL일 때는 세 번째 인수(B)를 반환합니다. COL1이 NULL이므로 'B'가 반환됩니다." optionExplanations={['NVL2는 NULL이 아닐 때의 반환값을 직접 지정합니다.', 'A는 COL1이 NULL이 아닐 때 반환합니다.', 'COL1이 NULL이면 세 번째 인수 B를 반환합니다. 정답입니다.', 'NVL2는 NULL을 반환하지 않고 항상 두 번째 또는 세 번째 인수를 반환합니다.']} />

<Quiz questionNumber={10} question="NOT IN 서브쿼리에서 서브쿼리 결과에 NULL이 포함될 때의 결과는?" options={['NULL 값은 무시되고 나머지 값으로 정상 필터링된다.', '결과가 공집합(0건)이 된다.', 'SQL 오류가 발생한다.', 'NULL은 자동으로 가장 큰 값으로 대체된다.']} correctAnswer={2} explanation="NOT IN 서브쿼리에 NULL이 포함되면 NULL과의 비교는 항상 UNKNOWN을 반환합니다. WHERE col NOT IN (1, NULL)은 col != 1 AND col != NULL이 되는데, col != NULL은 UNKNOWN이 되어 모든 행의 조건이 UNKNOWN이 됩니다. 따라서 결과가 공집합이 됩니다." optionExplanations={['NULL은 무시되지 않습니다. NULL과의 비교가 UNKNOWN을 반환하여 전체 결과에 영향을 줍니다.', 'NOT IN에 NULL이 포함되면 공집합이 됩니다. 정답입니다. 이를 방지하려면 IS NOT NULL 조건을 추가해야 합니다.', 'SQL 오류는 발생하지 않지만 예상치 못한 공집합이 됩니다.', 'NULL은 어떤 값으로도 자동 대체되지 않습니다.']} />

<Quiz questionNumber={11} question="WHERE 절과 HAVING 절의 차이로 옳은 것은?" options={['WHERE는 그룹화 후에, HAVING은 그룹화 전에 적용된다.', 'WHERE에서 집계함수를 사용할 수 있고 HAVING에서는 사용할 수 없다.', 'WHERE는 개별 행을 필터링하고 HAVING은 그룹을 필터링한다.', 'WHERE와 HAVING은 기능이 동일하므로 혼용해도 결과가 같다.']} correctAnswer={3} explanation="WHERE는 GROUP BY 이전에 개별 행을 필터링합니다. HAVING은 GROUP BY 이후에 집계된 그룹을 필터링합니다. 따라서 WHERE에서는 집계함수를 사용할 수 없고, HAVING에서 집계함수 조건을 사용합니다." optionExplanations={['반대입니다. WHERE가 그룹화 전, HAVING이 그룹화 후에 적용됩니다.', '집계함수는 WHERE에서 사용할 수 없고 HAVING에서 사용합니다.', 'WHERE는 행 필터링, HAVING은 그룹 필터링으로 역할이 다릅니다. 정답입니다.', 'WHERE와 HAVING은 역할과 실행 시점이 다릅니다.']} />

<Quiz questionNumber={12} question="COALESCE 함수에 대한 설명으로 옳은 것은?" options={['두 인수가 같으면 NULL을 반환한다.', '첫 번째 인수가 NULL이 아닐 때만 두 번째 인수를 반환한다.', '인수 목록에서 처음으로 NULL이 아닌 값을 반환한다.', '모든 인수가 NULL이어도 항상 0을 반환한다.']} correctAnswer={3} explanation="COALESCE(expr1, expr2, ..., exprN)는 인수를 왼쪽부터 순서대로 평가하여 처음 만나는 NULL이 아닌 값을 반환합니다. 모든 인수가 NULL이면 NULL을 반환합니다." optionExplanations={['두 인수가 같을 때 NULL을 반환하는 함수는 NULLIF입니다.', '두 번째 인수는 첫 번째가 NULL일 때 평가됩니다.', '처음으로 NULL이 아닌 값을 반환합니다. 정답입니다.', '모든 인수가 NULL이면 COALESCE도 NULL을 반환합니다.']} />

<Quiz questionNumber={13} question="사원 테이블에 데이터가 있을 때, 공집합이 아닌 결과(0)를 반환하는 것은? (WHERE 1=2 조건으로 행이 없음)" options={['SELECT SUM(급여) FROM 사원 WHERE 1=2', 'SELECT AVG(급여) FROM 사원 WHERE 1=2', 'SELECT MAX(급여) FROM 사원 WHERE 1=2', 'SELECT COUNT(*) FROM 사원 WHERE 1=2']} correctAnswer={4} explanation="WHERE 1=2로 대상 행이 없을 때 SUM, AVG, MAX, MIN 같은 집계함수는 NULL을 반환합니다. 반면 COUNT(*)는 대상 행이 없어도 0을 반환합니다. 이것이 COUNT(*)와 다른 집계함수의 중요한 차이점입니다." optionExplanations={['SUM은 집계할 행이 없으면 NULL을 반환합니다.', 'AVG도 집계할 행이 없으면 NULL을 반환합니다.', 'MAX도 집계할 행이 없으면 NULL을 반환합니다.', 'COUNT(*)만 행이 없어도 NULL이 아닌 0을 반환합니다. 정답입니다.']} />

<Quiz questionNumber={14} question="ORDER BY절에서 NULL의 정렬 순서에 대한 Oracle의 기본 동작은?" options={['ASC 정렬 시 NULL이 가장 처음에 출력된다.', 'ASC 정렬 시 NULL이 가장 마지막에 출력된다.', 'NULL은 ORDER BY에서 항상 제외된다.', 'NULL은 0으로 변환되어 정렬된다.']} correctAnswer={2} explanation="Oracle에서 ASC 정렬 시 NULL은 가장 마지막에, DESC 정렬 시 NULL은 가장 처음에 출력됩니다. 이를 명시적으로 제어하려면 NULLS FIRST 또는 NULLS LAST 옵션을 사용합니다." optionExplanations={['Oracle에서 ASC 정렬 시 NULL은 마지막에 옵니다. 반대입니다.', 'Oracle에서 ASC 정렬 시 NULL이 마지막에 출력됩니다. 정답입니다.', 'NULL은 ORDER BY에서 제외되지 않고 가장 끝(ASC) 또는 처음(DESC)에 출력됩니다.', 'NULL은 0으로 변환되지 않습니다.']} />

<Quiz questionNumber={15} question="ROLLUP과 CUBE에 대한 설명으로 옳지 않은 것은?" options={['ROLLUP(A, B)은 (A,B), (A), 전체 합계 순으로 집계를 생성한다.', 'CUBE는 가능한 모든 조합의 집계를 생성한다.', 'ROLLUP은 인수의 순서가 달라도 결과가 동일하다.', 'CUBE(A, B)는 (A,B), (A), (B), 전체 합계를 생성한다.']} correctAnswer={3} explanation="ROLLUP은 인수의 순서에 따라 결과가 달라집니다. ROLLUP(부서, 직급)과 ROLLUP(직급, 부서)는 서로 다른 소계를 생성합니다. 반면 CUBE는 모든 조합을 생성하므로 순서에 무관하게 동일한 결과를 생성합니다." optionExplanations={['ROLLUP(A,B)는 (A,B), (A), 전체 총합 세 수준의 집계를 생성합니다. 맞는 설명입니다.', 'CUBE는 지정한 컬럼들의 모든 조합 집계를 생성합니다. 맞는 설명입니다.', 'ROLLUP은 순서에 따라 결과가 달라집니다. 이것이 틀린 설명입니다. 정답입니다.', 'CUBE(A,B)는 (A,B), (A), (B), () 네 가지 조합을 생성합니다. 맞는 설명입니다.']} />

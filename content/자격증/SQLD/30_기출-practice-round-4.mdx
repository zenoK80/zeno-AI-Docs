---
title: '30. 최종 실전 모의 — 전범위 40문항'
description: '실제 SQLD 시험과 동일하게 1과목 10문제, 2과목 30문제로 구성한 최종 실전 모의고사입니다. 62·60·58·37회 기출 핵심 유형을 전범위 반영하였습니다.'
---

<Callout type="info">
  **시험 안내**
  - 실제 SQLD 시험 구성: 1과목 10문제 + 2과목 40문제 = 총 50문제 (90분)
  - 이 모의고사: 1과목 10문제 + 2과목 30문제 = 총 40문제 (목표 72분)
  - 합격 기준: 과목별 40점 이상, 총점 60점 이상
  - 풀이 후 반드시 오답을 확인하고 해설을 읽어보세요.
</Callout>

<Callout type="warning">
  **고난도 함정 문제 포함:** 21번, 28번, 33번, 37번, 40번은 기출에서 오답률이 높았던 함정 유형입니다. 특히 집중해서 풀어보세요.
</Callout>

---

## 1과목: 데이터 모델링의 이해

<Quiz questionNumber={1} question="발생 시점에 따라 엔티티를 분류할 때, 다른 엔티티로부터 업무 행위의 결과로 발생하며 데이터 변경이 자주 일어나고 인스턴스 수가 가장 많은 엔티티 유형은?" options={['기본 엔티티', '중심 엔티티', '개념 엔티티', '행위 엔티티']} correctAnswer={4} explanation="행위 엔티티(Activity Entity)는 두 개 이상의 부모 엔티티로부터 업무 행위를 통해 발생합니다. 주문, 계약, 입출금내역 등이 대표적입니다. 발생 빈도가 높아 인스턴스 수가 가장 많고 데이터 변경도 잦습니다." optionExplanations={['기본 엔티티는 업무에 처음부터 독립적으로 존재하는 실체입니다. 고객, 사원, 상품 등이 해당합니다.', '중심 엔티티는 기본 엔티티로부터 파생되며 행위 엔티티 생성의 중심 역할을 합니다.', '개념 엔티티는 발생 시점 분류의 공식 용어가 아닙니다.', '행위 엔티티는 다른 엔티티의 행위 결과로 발생하고 인스턴스 수가 가장 많습니다. 정답입니다.']} />

<Quiz questionNumber={2} question="다른 속성으로부터 계산하거나 가공하여 만든 속성으로, 원본 속성이 변경되면 정합성 관리가 필요하고 최소화하는 것이 바람직한 속성은?" options={['파생속성', '기본속성', '설계속성', '복합속성']} correctAnswer={1} explanation="파생속성(Derived Attribute)은 다른 속성을 계산하거나 가공하여 만들어지는 속성입니다. 총주문금액, 나이(생년월일에서 계산), 재고수량 등이 해당합니다. 원본 속성 변경 시 정합성 유지를 위해 최소화가 권장됩니다." optionExplanations={['파생속성은 다른 속성을 계산/가공하여 생성된 속성입니다. 정답입니다.', '기본속성은 업무 분석에서 도출된 원본 속성입니다.', '설계속성은 업무 규칙이나 시스템 설계에 의해 만들어진 속성입니다.', '복합속성은 여러 원자 속성의 조합으로 이루어진 속성을 의미합니다.']} />

<Quiz questionNumber={3} question="엔티티, 인스턴스, 속성, 속성값의 관계에 대한 설명으로 옳지 않은 것은?" options={['하나의 엔티티는 두 개 이상의 인스턴스를 가진다.', '하나의 엔티티는 두 개 이상의 속성을 가진다.', '하나의 인스턴스는 하나의 속성에 두 개 이상의 속성값을 가질 수 있다.', '하나의 속성은 하나의 속성값을 가진다.']} correctAnswer={3} explanation="관계형 모델에서 하나의 인스턴스(행)는 하나의 속성(컬럼)에 대해 원자값(Atomic Value) 하나만 가져야 합니다. 하나의 속성에 두 개 이상의 값을 가지면 제1정규형(1NF)을 위반하는 다중값 속성이 됩니다." optionExplanations={['엔티티는 의미 있는 집합이므로 두 개 이상의 인스턴스가 있어야 합니다.', '엔티티는 식별자 외에도 설명하는 속성이 있어야 하므로 두 개 이상이 일반적입니다.', '하나의 인스턴스는 하나의 속성에 단 하나의 값만 가집니다. 이것이 틀린 설명입니다.', '원자값 원칙에 따라 속성 하나에 값 하나를 가집니다.']} />

<Quiz questionNumber={4} question="피터 첸(Peter Chen) 표기법에서 관계(Relationship)를 나타내는 기호는?" options={['사각형', '타원', '마름모', '직선']} correctAnswer={3} explanation="Peter Chen 표기법에서 엔티티는 사각형(□), 속성은 타원(○), 관계는 마름모(◇)로 표현합니다. 직선은 엔티티-속성 또는 엔티티-관계를 연결하는 선입니다." optionExplanations={['사각형은 엔티티를 나타냅니다.', '타원은 속성을 나타냅니다.', '마름모가 관계를 나타냅니다. 정답입니다.', '직선은 연결선으로 관계 기호가 아닙니다.']} />

<Quiz questionNumber={5} question="한 명의 고객은 0건 이상의 주문을 하고, 모든 주문은 반드시 한 명의 고객에 속한다는 업무 규칙에 대한 설명으로 옳은 것은?" options={['고객 쪽 참여는 필수(mandatory)이다.', '주문 쪽 참여는 선택(optional)이다.', '고객:주문의 관계 차수(cardinality)는 1:M이다.', '주문이 없는 고객은 존재할 수 없다.']} correctAnswer={3} explanation="고객 하나가 여러 주문을 가질 수 있으므로 관계 차수는 1:M입니다. 고객은 0건 이상이므로 선택 참여(Optional)이고, 주문은 반드시 하나의 고객에 속하므로 필수 참여(Mandatory)입니다." optionExplanations={['고객 쪽은 주문이 0건일 수 있으므로 선택 참여입니다.', '주문 쪽은 반드시 한 명의 고객에 속해야 하므로 필수 참여입니다.', '한 고객이 여러 주문을 가질 수 있으므로 1:M 관계입니다. 정답입니다.', '0건 이상이므로 주문이 없는 고객도 존재할 수 있습니다.']} />

<Quiz questionNumber={6} question="비식별 관계(Non-Identifying Relationship)에 대한 설명으로 옳지 않은 것은?" options={['부모 엔티티의 주식별자가 자식 엔티티의 일반 속성(FK)으로 상속된다.', 'ERD에서 점선으로 표현한다.', '자식 엔티티는 자신만의 독립적인 주식별자를 구성한다.', '부모의 주식별자가 자식의 주식별자를 구성한다.']} correctAnswer={4} explanation="부모의 주식별자가 자식의 주식별자를 구성하는 것은 식별 관계(Identifying Relationship)의 특징입니다. 비식별 관계에서는 부모의 식별자가 자식의 일반 속성(FK)으로만 상속됩니다." optionExplanations={['부모 FK가 자식의 일반 속성으로 상속되는 것이 비식별 관계의 핵심입니다.', '점선 표기가 비식별 관계를 나타냅니다.', '자식이 독립 식별자를 가지는 것이 비식별 관계의 특징입니다.', '부모 식별자가 자식 PK를 구성하는 것은 식별 관계입니다. 이것이 틀린 설명입니다.']} />

<Quiz questionNumber={7} question="주식별자를 구성하는 속성 중 하나라도 제외되면 인스턴스를 유일하게 구분할 수 없게 되므로 더 이상 줄일 수 없는 최소 집합으로 구성해야 한다는 주식별자 특성은?" options={['대표성', '최소성', '불변성', '유일성']} correctAnswer={2} explanation="최소성(Minimality)은 주식별자를 구성하는 속성 중 하나라도 제거하면 유일성이 깨지는 최소 조합이어야 한다는 특성입니다. 유일성은 각 인스턴스를 고유하게 식별할 수 있는 특성이고, 최소성은 그 유일성을 만족하는 최소 집합을 의미합니다." optionExplanations={['대표성은 해당 엔티티를 대표할 수 있는 식별자를 선택하는 특성입니다.', '더 이상 줄일 수 없는 최소 조합이 최소성입니다. 정답입니다.', '불변성은 식별자 값이 변경되지 않아야 한다는 특성입니다.', '유일성은 각 인스턴스를 고유하게 구별할 수 있는 특성입니다.']} />

<Quiz questionNumber={8} question="정규화 단계 중 기본키에 대한 이행적 함수 종속(A→B→C)을 제거하는 정규화 단계는?" options={['제1정규형(1NF)', '제2정규형(2NF)', '제3정규형(3NF)', 'BCNF']} correctAnswer={3} explanation="제3정규형(3NF)은 기본키가 아닌 일반 속성 간의 이행적 함수 종속을 제거합니다. A→B→C 구조에서 A→C가 이행 종속이며 이를 분리해야 합니다. 1NF는 원자값, 2NF는 부분 함수 종속 제거가 목표입니다." optionExplanations={['1NF는 속성 원자값(다중값 속성 제거)을 목표로 합니다.', '2NF는 복합 기본키의 부분 함수 종속을 제거합니다.', '3NF가 이행적 함수 종속을 제거합니다. 정답입니다.', 'BCNF는 3NF를 강화하여 결정자가 후보키인 경우만 허용합니다.']} />

<Quiz questionNumber={9} question="인조 식별자(Artificial Identifier)를 사용하는 이유로 가장 적절한 것은?" options={['업무 담당자가 인식하기 쉬운 값을 사용하기 위해', '자연 식별자가 너무 짧아 확장이 필요한 경우', '복합 자연 식별자가 너무 길거나 변경 가능성이 있어 관리 편의를 위해', '반드시 문자형 데이터여야 하는 업무 규칙이 있어서']} correctAnswer={3} explanation="인조 식별자는 자연 식별자가 복잡하거나(복합키가 너무 긴 경우) 변경 가능성이 있을 때 관리 편의를 위해 인위적으로 만듭니다. 다만 과도한 인조 식별자 사용은 식별자의 업무 의미를 잃게 하므로 적절히 사용해야 합니다." optionExplanations={['인조 식별자는 업무 의미가 없으므로 담당자가 인식하기 어렵습니다.', '자연 식별자가 짧다는 이유로 인조 식별자를 사용하지 않습니다.', '복합키가 길거나 변경 가능성이 있을 때 인조 식별자를 사용합니다. 정답입니다.', '데이터 타입과 인조 식별자 사용 여부는 무관합니다.']} />

<Quiz questionNumber={10} question="트랜잭션의 ACID 특성 중 '작업이 모두 반영되거나 전혀 반영되지 않아야 한다'는 All or Nothing 원칙은?" options={['원자성(Atomicity)', '일관성(Consistency)', '고립성(Isolation)', '지속성(Durability)']} correctAnswer={1} explanation="원자성(Atomicity)은 트랜잭션의 모든 연산이 완전히 수행되거나 전혀 수행되지 않아야 한다는 특성입니다. COMMIT이 되면 전체 반영, ROLLBACK이 되면 전체 취소가 보장됩니다." optionExplanations={['원자성이 All or Nothing 원칙입니다. 정답입니다.', '일관성은 트랜잭션 전후에 데이터베이스가 일관된 상태를 유지하는 것입니다.', '고립성은 동시 실행 중인 트랜잭션이 서로 간섭하지 않는 것입니다.', '지속성은 완료된 트랜잭션의 결과가 영구적으로 반영되는 것입니다.']} />

---

## 2과목: SQL 기본 및 활용

<Quiz questionNumber={11} question="다음 Oracle CONNECT BY 의사컬럼에 대한 설명으로 모두 옳지 않은 것을 고르면? (가: LEVEL은 루트 노드에서 0을 반환. 나: CONNECT_BY_ISLEAF는 리프 노드일 때 1. 다: CONNECT_BY_ROOT는 최하위 노드 값을 반환.)" options={['가만 옳지 않다', '나만 옳지 않다', '가, 다가 옳지 않다', '가, 나, 다 모두 옳지 않다']} correctAnswer={3} explanation="가: LEVEL 루트는 0이 아닌 1입니다. 나: CONNECT_BY_ISLEAF는 리프 노드일 때 1을 반환합니다(이것은 맞는 설명). 다: CONNECT_BY_ROOT는 최상위(루트) 노드 값을 반환하며 최하위가 아닙니다. 따라서 가와 다가 틀린 설명입니다." optionExplanations={['가만 틀리면 다의 오류가 누락됩니다.', '나(CONNECT_BY_ISLEAF는 리프일 때 1)는 맞는 설명입니다.', '가(LEVEL 루트=0 → 실제는 1)와 다(최하위 → 실제는 최상위)가 모두 틀렸습니다. 정답입니다.', '나(CONNECT_BY_ISLEAF는 리프일 때 1)는 옳은 설명이므로 모두 틀린 것은 아닙니다.']} />

<Quiz questionNumber={12} question="부서 테이블의 부서번호 컬럼에 NULL이 포함될 때, SELECT 이름 FROM 사원 WHERE 부서번호 NOT IN (SELECT 부서번호 FROM 부서)의 결과는?" options={['사원 테이블 전체 행이 조회된다.', '부서번호가 NULL인 사원만 조회된다.', '한 건도 조회되지 않는다(공집합).', '문법 오류가 발생한다.']} correctAnswer={3} explanation="NOT IN 서브쿼리 결과에 NULL이 포함되면, 사원의 부서번호 값과 NULL을 비교할 때 결과가 UNKNOWN이 됩니다. 모든 비교가 UNKNOWN이 되어 WHERE 조건을 만족하는 행이 없어 공집합이 반환됩니다. 이를 방지하려면 WHERE 부서번호 IS NOT NULL 조건을 추가해야 합니다." optionExplanations={['NOT IN에 NULL이 포함되면 전체 조회가 아닌 공집합이 됩니다.', '부서번호 NULL 사원도 결과에 포함되지 않습니다.', 'NOT IN 서브쿼리에 NULL이 있으면 공집합입니다. 정답입니다.', '문법 오류는 발생하지 않습니다.']} />

<Quiz questionNumber={13} question="부서별·직급별 연봉 합계를 직급을 열로 전개하는 PIVOT 구문으로 올바른 것은? (직급: 사원, 대리, 팀장, 부장)" options={['PIVOT (직급 FOR SUM(연봉) IN (사원, 대리, 팀장, 부장))', 'PIVOT (SUM(연봉) FOR 부서 IN (사원, 대리, 팀장, 부장))', 'PIVOT (SUM(연봉) FOR 직급 IN (사원, 대리, 팀장, 부장))', 'UNPIVOT (SUM(연봉) FOR 직급 IN (사원, 대리, 팀장, 부장))']} correctAnswer={3} explanation="PIVOT(집계함수 FOR 기준컬럼 IN(전개할 값들)) 형식입니다. 직급을 열로 전개하므로 FOR 직급, 집계는 SUM(연봉), IN에 직급 값을 나열합니다. 집계함수가 FOR 앞에 오는 것에 주의하세요." optionExplanations={['집계함수와 FOR 기준컬럼의 순서가 바뀌었습니다.', '부서가 아닌 직급을 열로 전개해야 합니다.', 'SUM(연봉) FOR 직급 IN(값 목록)이 올바른 형식입니다. 정답입니다.', 'UNPIVOT은 열을 행으로 변환하는 반대 연산입니다.']} />

<Quiz questionNumber={14} question="급여 1000, 2000, 3000인 세 행에 대해 FIRST_VALUE(급여) OVER (ORDER BY 급여)를 실행했을 때 결과 세 행은?" options={['1000, 1000, 1000', '1000, 2000, 3000', '3000, 3000, 3000', '1000, 2000, 1000']} correctAnswer={1} explanation="FIRST_VALUE(급여) OVER(ORDER BY 급여)는 급여 오름차순 기준 기본 프레임(파티션 처음부터 현재 행까지)에서 첫 번째 값인 1000을 모든 행에 반환합니다. 오름차순이므로 첫 값이 가장 작은 1000이 됩니다." optionExplanations={['세 행 모두 FIRST_VALUE인 1000을 반환합니다. 정답입니다.', '이것은 단순 급여 컬럼 조회 결과입니다.', '3000이 첫 값이 되려면 ORDER BY 급여 DESC여야 합니다.', '행마다 다른 값이 반환되지 않습니다.']} />

<Quiz questionNumber={15} question="WHERE (사번, 회원번호) IN ((10005, 2003), (10007, 2005))와 논리적으로 동일한 조건은?" options={['(사번=10005 OR 회원번호=2003) AND (사번=10007 OR 회원번호=2005)', '(사번=10005 AND 회원번호=2003) OR (사번=10007 AND 회원번호=2005)', '사번 IN (10005,10007) OR 회원번호 IN (2003,2005)', '사번 IN (10005,10007) AND 회원번호 IN (2003,2005)']} correctAnswer={2} explanation="튜플 IN은 각 튜플에 대해 모든 컬럼 값이 동시에 일치해야 합니다. (사번=10005 AND 회원번호=2003)이 하나의 튜플 조건이며, 두 튜플 조건을 OR로 연결합니다. 컬럼 각각에 IN을 적용하면 실제 튜플 조합과 다른 결과가 나옵니다." optionExplanations={['AND/OR 구조가 복합 조건과 다릅니다.', '두 튜플 조건을 OR로 연결한 것이 정확합니다. 정답입니다.', '이 조건은 세 번째 보기(OR)로 각 컬럼을 따로 비교하여 의도한 조합이 아닐 수 있습니다.', '이 조건은 두 조합 외에 (10005,2005), (10007,2003)도 포함합니다.']} />

<Quiz questionNumber={16} question="8명의 사원을 급여 내림차순으로 정렬하고 NTILE(3)을 적용했을 때 각 그룹의 사원 수는?" options={['3,3,2 (앞 그룹이 더 많음)', '2,3,3 (뒷 그룹이 더 많음)', '2,2,4 (마지막 그룹이 더 많음)', '3,2,3 (중간 그룹이 더 적음)']} correctAnswer={1} explanation="NTILE(n)은 행을 n개의 그룹으로 최대한 균등하게 분배하며, 나머지가 생기면 앞 그룹부터 한 명씩 추가합니다. 8명을 3그룹으로 나누면 8÷3=2 나머지 2이므로 앞의 2그룹에 한 명씩 추가하여 3,3,2가 됩니다." optionExplanations={['3,3,2가 NTILE(3)의 올바른 8행 분배입니다. 정답입니다.', '나머지는 뒤가 아닌 앞 그룹부터 추가됩니다.', '2,2,4 분배는 NTILE 동작과 다릅니다.', '중간 그룹만 줄어드는 분배는 NTILE과 다릅니다.']} />

<Quiz questionNumber={17} question="부서와 사원 양쪽에서 매칭되지 않는 행을 모두 보존하는 조인 방식은?" options={['NVL 함수를 사용한 CROSS JOIN', 'NVL 함수를 사용한 INNER JOIN', 'FULL OUTER JOIN', 'UNION']} correctAnswer={3} explanation="FULL OUTER JOIN은 양쪽 테이블의 매칭되지 않는 행을 모두 NULL로 채워 결과에 포함합니다. LEFT OUTER JOIN은 왼쪽 보존, RIGHT OUTER JOIN은 오른쪽 보존이며, FULL은 양쪽 모두 보존합니다." optionExplanations={['CROSS JOIN은 카티션 곱으로 매칭과 무관하게 모든 조합을 반환합니다.', 'INNER JOIN은 매칭되는 행만 반환합니다.', 'FULL OUTER JOIN이 양쪽 미매칭 행을 모두 보존합니다. 정답입니다.', 'UNION은 두 SELECT 결과를 합치지만 조인과 다른 개념입니다.']} />

<Quiz questionNumber={18} question="사용자 R1이 소유한 테이블 주문에 대해 R2가 조회할 수 있도록 하는 올바른 방법은?" options={['R2가 CONNECT 롤을 다시 부여받는다.', 'R1이 GRANT SELECT ON 주문 TO R2를 실행한다.', 'R2가 자신의 스키마에 동일한 테이블을 새로 만든다.', 'R2가 COMMIT을 실행한 뒤 재조회한다.']} correctAnswer={2} explanation="객체 권한(Object Privilege)은 객체 소유자 또는 WITH GRANT OPTION을 받은 사용자가 부여할 수 있습니다. R1이 R1.주문 테이블의 소유자이므로 R1이 직접 GRANT SELECT ON 주문 TO R2를 실행해야 합니다." optionExplanations={['CONNECT 롤은 데이터베이스 접속 권한이며 특정 테이블 조회 권한과 다릅니다.', '객체 소유자 R1이 R2에게 SELECT 권한을 부여합니다. 정답입니다.', 'R2가 새 테이블을 만들면 R1.주문 테이블을 조회하는 것이 아닙니다.', 'COMMIT은 트랜잭션 확정이며 조회 권한과 무관합니다.']} />

<Quiz questionNumber={19} question="SELECT REGEXP_REPLACE('2026/02/25', '([0-9]{4})/([0-9]{2})/([0-9]{2})', '\\3/\\2/\\1') FROM DUAL의 결과는?" options={['2026/02/25', '25/02/2026', '02/25/2026', '2026/25/02']} correctAnswer={2} explanation="정규식 캡처 그룹에서 \\1=2026, \\2=02, \\3=25입니다. 치환 문자열 \\3/\\2/\\1은 그룹 3, 2, 1 순으로 재조합하므로 결과는 25/02/2026이 됩니다." optionExplanations={['원본 그대로 반환하려면 치환이 없어야 합니다.', '\\3/\\2/\\1이 25/02/2026을 만듭니다. 정답입니다.', '\\2/\\3/\\1이면 02/25/2026이 됩니다.', '\\1/\\3/\\2이면 2026/25/02가 됩니다.']} />

<Quiz questionNumber={20} question="1분기 컬럼 값이 4(A), NULL(B), NULL(C)이고 2분기 컬럼 값이 10(A), 8(B), 5(C)일 때 SELECT SUM(1분기), SUM(2분기)의 결과는?" options={['4, 23', 'NULL, 23', '4, NULL', 'NULL, NULL']} correctAnswer={1} explanation="SUM을 포함한 집계함수는 NULL을 제외하고 계산합니다. SUM(1분기)=4(NULL 제외), SUM(2분기)=10+8+5=23이 됩니다. 집계 대상 행 중 NULL이 아닌 값만 합산합니다." optionExplanations={['SUM(1분기)=4, SUM(2분기)=23이 정확합니다. 정답입니다.', 'NULL이 있어도 SUM은 나머지 값을 더합니다. NULL을 반환하지 않습니다.', 'SUM(2분기)는 NULL이 아닌 23입니다.', '집계 대상이 있으면 SUM은 NULL을 반환하지 않습니다.']} />

<Quiz questionNumber={21} question="Oracle에서 조회되는 행 수가 나머지 셋과 다른 것은? (사원 테이블: 10건)" options={['SELECT * FROM 사원 WHERE ROWNUM <= 1', 'SELECT * FROM 사원 WHERE ROWNUM = 1', 'SELECT * FROM 사원 WHERE ROWNUM < 2', 'SELECT * FROM 사원 WHERE ROWNUM >= 2']} correctAnswer={4} explanation="ROWNUM은 1부터 순차 증가합니다. ROWNUM >= 2 조건에서 첫 번째 행의 ROWNUM이 1이면 조건(>=2)을 만족하지 못해 ROWNUM 2가 부여되지 않습니다. 따라서 아무 행도 반환되지 않아 0건이 됩니다. 나머지 1, 2, 3번은 모두 1건을 반환합니다." optionExplanations={['ROWNUM <= 1은 첫 번째 행 1건을 반환합니다.', 'ROWNUM = 1은 첫 번째 행 1건을 반환합니다.', 'ROWNUM < 2는 ROWNUM=1인 행 1건을 반환합니다.', 'ROWNUM >= 2는 Oracle에서 0건을 반환합니다. 정답입니다.']} />

<Quiz questionNumber={22} question="사원 테이블에 데이터가 있어도 WHERE 1=2 조건으로 행이 없을 때 나머지 셋과 다른 값을 반환하는 것은?" options={['SELECT SUM(급여) FROM 사원 WHERE 1=2', 'SELECT AVG(급여) FROM 사원 WHERE 1=2', 'SELECT MAX(급여) FROM 사원 WHERE 1=2', 'SELECT COUNT(*) FROM 사원 WHERE 1=2']} correctAnswer={4} explanation="집계할 행이 없을 때 SUM, AVG, MAX, MIN은 NULL을 반환합니다. 반면 COUNT(*)는 행이 없어도 0을 반환합니다. 이것이 COUNT(*)와 다른 집계함수의 핵심 차이입니다." optionExplanations={['SUM은 행이 없으면 NULL을 반환합니다.', 'AVG도 행이 없으면 NULL을 반환합니다.', 'MAX도 행이 없으면 NULL을 반환합니다.', 'COUNT(*)만 행이 없어도 0을 반환합니다. 정답입니다.']} />

<Quiz questionNumber={23} question="부서 테이블의 모든 행을 보존하면서 사원 테이블과 조인하려 할 때, 결과가 나머지 셋과 다른 것은?" options={['SELECT * FROM 부서 D LEFT OUTER JOIN 사원 E ON D.부서번호 = E.부서번호', 'SELECT * FROM 부서 D, 사원 E WHERE D.부서번호 = E.부서번호(+)', 'SELECT * FROM 부서 D, 사원 E WHERE D.부서번호(+) = E.부서번호', 'SELECT * FROM 사원 E RIGHT OUTER JOIN 부서 D ON E.부서번호 = D.부서번호']} correctAnswer={3} explanation="Oracle의 (+) 연산자에서 (+)는 NULL로 채울 테이블 쪽에 붙입니다. 부서를 보존하려면 사원(E) 쪽에 (+)가 붙어야 합니다. ③은 D.부서번호(+)로 부서 쪽에 (+)가 붙어 있어 사원 테이블을 보존하는 RIGHT OUTER JOIN 방향이 반대가 됩니다." optionExplanations={['LEFT OUTER JOIN으로 부서(D) 전체를 보존합니다. 방향이 맞습니다.', 'E.부서번호(+)로 사원 쪽에 (+)를 붙여 부서를 보존합니다. 맞습니다.', 'D.부서번호(+)로 부서 쪽에 (+)를 붙이면 사원 방향이 바뀌어 결과가 다릅니다. 정답입니다.', '사원 E RIGHT OUTER JOIN 부서 D는 부서를 보존합니다. 맞습니다.']} />

<Quiz questionNumber={24} question="PIVOT 질의 SELECT * FROM (SELECT 부서, 직급 FROM 사원) PIVOT (COUNT(*) FOR 직급 IN ('사원', '대리', '과장'))에 대한 설명으로 옳은 것은?" options={['부서별로 행이 묶이고 직급 값이 열로 전개되어 각 직급 인원수가 표시된다.', 'PIVOT 절에는 집계함수 없이 전개할 컬럼만 나열한다.', 'IN 절에 명시하지 않은 직급도 자동으로 열에 추가된다.', '집계를 위해 반드시 GROUP BY 절을 함께 작성해야 한다.']} correctAnswer={1} explanation="PIVOT은 행 데이터를 열로 전환하면서 집계합니다. 부서가 그룹 기준이 되고 직급 값(사원, 대리, 과장)이 각각의 열로 전개됩니다. IN 절에 없는 값은 결과에 나타나지 않으며, GROUP BY는 PIVOT 내부에서 자동 처리됩니다." optionExplanations={['부서별로 집계되고 직급 값이 열로 전개됩니다. 정답입니다.', 'PIVOT은 반드시 집계함수가 필요합니다.', 'IN에 명시한 값만 열로 나타납니다.', 'PIVOT 내부에서 그룹화가 자동으로 이루어집니다.']} />

<Quiz questionNumber={25} question="SELECT 사원명 FROM 사원 WHERE 부서번호 = (SELECT 부서번호 FROM 부서 WHERE 부서명 = '영업')에서 ORA-01427 오류를 방지하려면 부서 테이블에 어떤 제약조건이 필요한가?" options={['CHECK 제약조건', 'UNIQUE 제약조건', 'NOT NULL 제약조건', 'FOREIGN KEY 제약조건']} correctAnswer={2} explanation="ORA-01427은 '단일 행 서브쿼리에서 둘 이상의 행이 반환됨' 오류입니다. 부서명이 유일해야 서브쿼리가 최대 1행만 반환합니다. 이를 보장하는 것이 UNIQUE 또는 PRIMARY KEY 제약조건입니다." optionExplanations={['CHECK는 값 범위나 조건을 제한하지만 유일성을 보장하지 않습니다.', 'UNIQUE 제약조건이 부서명의 유일성을 보장합니다. 정답입니다.', 'NOT NULL은 NULL 방지이며 중복을 허용합니다.', 'FOREIGN KEY는 다른 테이블 참조 무결성이며 유일성 보장이 아닙니다.']} />

<Quiz questionNumber={26} question="SUM(급여) OVER (ORDER BY 급여 ROWS UNBOUNDED PRECEDING)의 의미는?" options={['전체 합계를 모든 행에 반환', '첫 행부터 현재 행까지의 누적합 반환', '현재 행부터 마지막 행까지의 합 반환', '이전 행과 현재 행의 합 반환']} correctAnswer={2} explanation="ROWS UNBOUNDED PRECEDING은 파티션의 첫 번째 행(Unbounded Preceding)부터 현재 행(Current Row)까지를 프레임으로 정의합니다. 이것이 표준적인 누적합 계산 방식입니다." optionExplanations={['전체 합계는 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING입니다.', 'ROWS UNBOUNDED PRECEDING이 첫 행부터 현재 행까지의 누적합입니다. 정답입니다.', '현재부터 마지막까지는 ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING입니다.', '이전 행과 현재 행의 합은 ROWS BETWEEN 1 PRECEDING AND CURRENT ROW입니다.']} />

<Quiz questionNumber={27} question="트랜잭션의 고립성(Isolation)에 대한 설명으로 옳은 것은?" options={['실행 중인 트랜잭션은 다른 트랜잭션의 연산에 영향을 받지 않는다.', '트랜잭션의 연산은 전부 반영되거나 전혀 반영되지 않는다.', '트랜잭션이 완료되면 그 결과는 영구적으로 반영된다.', '트랜잭션 전후에 데이터베이스는 일관된 상태를 유지한다.']} correctAnswer={1} explanation="고립성(Isolation)은 동시에 실행되는 트랜잭션이 서로의 중간 상태에 영향을 주지 않는 특성입니다. 원자성(Atomicity)은 All or Nothing, 지속성(Durability)은 영구 반영, 일관성(Consistency)은 일관된 상태 유지입니다." optionExplanations={['다른 트랜잭션으로부터 영향을 받지 않는 것이 고립성입니다. 정답입니다.', '이것은 원자성(Atomicity)의 설명입니다.', '이것은 지속성(Durability)의 설명입니다.', '이것은 일관성(Consistency)의 설명입니다.']} />

<Quiz questionNumber={28} question="PRIMARY KEY 제약조건이 보장하는 특성의 조합으로 옳은 것은?" options={['NULL 허용 + 중복 허용', 'NULL 허용 + 중복 불허(UNIQUE)', 'NOT NULL + 중복 불허(UNIQUE)', 'NOT NULL + 중복 허용']} correctAnswer={3} explanation="PRIMARY KEY는 UNIQUE + NOT NULL의 특성을 동시에 가집니다. 각 행을 고유하게 식별(UNIQUE)해야 하고 식별자가 없어서는 안 되므로(NOT NULL) 두 특성이 모두 보장됩니다." optionExplanations={['PK는 NULL과 중복 모두 허용하지 않습니다.', 'PK는 NULL을 허용하지 않습니다.', 'NOT NULL + UNIQUE가 PK의 두 특성입니다. 정답입니다.', 'PK는 중복을 허용하지 않습니다.']} />

<Quiz questionNumber={29} question="CONNECT BY PRIOR 사원번호 = 관리자번호, START WITH 관리자번호 IS NULL로 전개할 때 올바른 설명은?" options={['관리자에서 사원 방향(하향)으로 계층이 전개된다.', '사원에서 관리자 방향(상향)으로 계층이 전개된다.', 'PRIOR가 없으면 동일한 결과가 나온다.', 'START WITH 조건은 전개 방향에 영향을 주지 않는다.']} correctAnswer={1} explanation="CONNECT BY PRIOR 사원번호 = 관리자번호는 부모(PRIOR 사원번호)의 사원번호가 자식의 관리자번호와 같은 방향, 즉 위에서 아래로 하향 전개입니다. START WITH 관리자번호 IS NULL은 최상위 관리자(루트)를 시작점으로 지정합니다." optionExplanations={['PRIOR 사원번호 = 관리자번호가 하향(부모→자식) 전개입니다. 정답입니다.', '상향 전개는 PRIOR 관리자번호 = 사원번호입니다.', 'PRIOR가 없으면 계층 전개가 되지 않습니다.', 'START WITH는 시작점을 결정하며 전개 방향은 CONNECT BY에서 정의됩니다.']} />

<Quiz questionNumber={30} question="계층 질의에서 CONNECT BY PRIOR가 없을 때의 결과는?" options={['루트 노드만 반환된다.', '전체 테이블이 무한 반복된다.', '계층 전개가 제대로 이루어지지 않아 오류가 발생하거나 의도하지 않은 결과가 나온다.', '역방향 전개가 자동으로 이루어진다.']} correctAnswer={3} explanation="CONNECT BY에서 PRIOR가 없으면 부모-자식 관계의 방향이 정의되지 않습니다. Oracle에서는 오류가 발생하거나 의도하지 않은 결과가 나올 수 있습니다. 계층 전개를 위해서는 반드시 PRIOR 키워드로 방향을 지정해야 합니다." optionExplanations={['PRIOR 없이는 루트만 반환되지 않습니다.', '무한 반복이 아닌 오류나 의도하지 않은 결과가 나옵니다.', 'PRIOR 없이는 정상적인 계층 전개가 이루어지지 않습니다. 정답입니다.', '역방향 자동 전개 기능은 없습니다.']} />

<Quiz questionNumber={31} question="KOR 국가 선수만 필터링하여 메달 종류별 집계 시 결과로 옳은 것은? (KOR: 골드 1명, 실버 2명 / USA: 골드 1명, 브론즈 1명)" options={['골드 1, 실버 2, 브론즈 1', '골드 1, 실버 2', '골드 1, 실버 1, 브론즈 1', '실버 2, 브론즈 1']} correctAnswer={2} explanation="WHERE 국가 = 'KOR'로 필터링하면 골드 1건, 실버 2건만 해당됩니다. USA의 브론즈는 KOR이 아니므로 포함되지 않습니다. GROUP BY 메달로 집계하면 골드 1, 실버 2 두 그룹이 나옵니다." optionExplanations={['브론즈는 USA 소속이므로 KOR 필터링 시 포함되지 않습니다.', 'KOR의 골드 1, 실버 2만 정확합니다. 정답입니다.', '실버가 2명이므로 실버 1은 틀렸습니다.', '골드가 빠져 있습니다.']} />

<Quiz questionNumber={32} question="이미 데이터가 있는 사원 테이블(급여 NUMBER(8,2), 이름 VARCHAR2(20), 최대 15자 저장)에서 오류 없이 실행되는 ALTER 문은?" options={['ALTER TABLE 사원 MODIFY 급여 NUMBER(4,2)', 'ALTER TABLE 사원 MODIFY 이름 VARCHAR2(10)', 'ALTER TABLE 사원 MODIFY 사번 DATE', 'ALTER TABLE 사원 MODIFY 급여 NUMBER(10,2)']} correctAnswer={4} explanation="기존 데이터보다 컬럼 크기를 줄이거나 호환되지 않는 타입으로 변경하면 기존 데이터 보존 불가로 오류가 발생합니다. 크기를 늘리는 ④(NUMBER(8,2) → NUMBER(10,2))는 기존 데이터에 영향을 주지 않아 오류가 발생하지 않습니다." optionExplanations={['NUMBER(4,2)로 줄이면 정수부 2자리뿐이라 기존 큰 값이 들어가지 않습니다.', '이름이 최대 15자인데 VARCHAR2(10)으로 줄이면 기존 데이터 저장 불가로 오류입니다.', '사번을 DATE 타입으로 변경하면 타입 호환성 문제로 오류입니다.', '범위를 넓히는 변경은 항상 안전합니다. 정답입니다.']} />

<Quiz questionNumber={33} question="상품 테이블(ID: 1,2,3)에서 주문이 없는 상품을 NOT EXISTS로 조회 시 결과는? (주문 테이블: 상품ID=3만 존재)" options={['ID=3', 'ID=1, ID=2', '공집합', '오류 발생']} correctAnswer={2} explanation="NOT EXISTS는 서브쿼리 결과가 없을 때 TRUE를 반환합니다. 상품ID=3은 주문에 있어 EXISTS가 TRUE, NOT EXISTS가 FALSE입니다. 상품ID=1과 2는 주문에 없어 NOT EXISTS가 TRUE가 되어 결과로 반환됩니다." optionExplanations={['ID=3은 주문에 존재하므로 NOT EXISTS가 FALSE, 결과에 포함되지 않습니다.', '주문에 없는 ID=1, ID=2가 반환됩니다. 정답입니다.', '모든 상품이 주문에 있어야 공집합이 됩니다.', 'NOT EXISTS는 유효한 구문이며 오류가 발생하지 않습니다.']} />

<Quiz questionNumber={34} question="집합 연산자 중 중복 행을 제거하지 않고 모든 행을 반환하는 것은?" options={['UNION', 'UNION ALL', 'INTERSECT', 'MINUS']} correctAnswer={2} explanation="UNION ALL은 두 SELECT 결과를 중복 제거 없이 단순 합산합니다. UNION은 중복 제거, INTERSECT는 교집합(공통 행), MINUS는 차집합입니다. UNION ALL이 성능상 가장 빠릅니다." optionExplanations={['UNION은 중복을 제거하여 반환합니다.', 'UNION ALL이 중복 제거 없이 모든 행을 반환합니다. 정답입니다.', 'INTERSECT는 두 집합의 공통된 행만 반환합니다.', 'MINUS는 첫 번째 집합에서 두 번째 집합 행을 뺀 차집합입니다.']} />

<Quiz questionNumber={35} question="날짜 컬럼이 2024-01-15, 2024-03-20, 2024-05-10인 테이블에서 EXTRACT(MONTH FROM 일자) UNION EXTRACT(YEAR FROM 일자)를 실행했을 때 반환되는 행 수는?" options={['1행', '2행', '3행', '4행']} correctAnswer={4} explanation="EXTRACT(MONTH)는 1, 3, 5 세 값을 반환합니다. EXTRACT(YEAR)는 2024 하나를 반환합니다. UNION은 중복을 제거하는데 이 두 집합 간 중복이 없으므로 3+1=4행이 반환됩니다." optionExplanations={['1행은 둘 다 같은 값이 하나뿐일 때 해당합니다.', '2행은 중복이 두 쌍인 경우입니다.', '3행은 연도 2024가 월 집합과 중복될 때입니다. 중복이 없으므로 4행입니다.', '월 1,3,5와 연도 2024를 합치면 4개의 고유 값입니다. 정답입니다.']} />

<Quiz questionNumber={36} question="SQL 명령어에 대한 설명으로 옳지 않은 것은?" options={['TRUNCATE TABLE은 WHERE 조건으로 특정 행만 지울 수 있는 DDL 명령어이다.', 'DELETE 문은 WHERE 절로 일부 행만 삭제할 수 있다.', 'TRUNCATE TABLE은 모든 행을 삭제하고 저장 공간을 초기화하며 일반적으로 롤백할 수 없다.', 'DROP TABLE은 테이블의 데이터와 구조 모두를 제거한다.']} correctAnswer={1} explanation="TRUNCATE는 DDL 명령어로 WHERE 절을 사용할 수 없습니다. 테이블의 모든 행을 한 번에 삭제하며 일반적으로 롤백이 불가합니다. WHERE로 특정 행만 삭제하려면 DML인 DELETE를 사용해야 합니다." optionExplanations={['TRUNCATE는 DDL이며 WHERE 절을 사용할 수 없습니다. 이것이 틀린 설명입니다. 정답입니다.', 'DELETE + WHERE는 정상적인 DML 사용 방법입니다.', 'TRUNCATE의 특성을 올바르게 설명합니다.', 'DROP TABLE은 데이터와 구조를 모두 삭제합니다.']} />

<Quiz questionNumber={37} question="WITH DeptPerf AS (영업1팀 평균=4000, 영업2팀 평균=1500) 조건에서 평균 3000 초과 팀 소속 사원 조회 결과는? (영업1팀: SMITH, WARD / 영업2팀: ALLEN, MARTIN)" options={['SMITH, WARD', 'ALLEN, MARTIN', 'SMITH, ALLEN, WARD, MARTIN 전원', '반환 행 없음']} correctAnswer={1} explanation="평균 달성액 3000 초과는 영업1팀(평균 4000)만 해당합니다. 영업2팀(평균 1500)은 3000 이하이므로 ALLEN, MARTIN은 포함되지 않습니다. WITH 절(CTE)로 계산된 평균을 JOIN 조건으로 사용합니다." optionExplanations={['영업1팀 소속 SMITH, WARD가 반환됩니다. 정답입니다.', '영업2팀 평균 1500은 3000 이하이므로 ALLEN, MARTIN은 제외됩니다.', '영업2팀은 조건을 만족하지 않아 전원이 반환되지 않습니다.', '영업1팀이 조건을 만족하므로 결과가 있습니다.']} />

<Quiz questionNumber={38} question="COALESCE(NULL, 1, 2)의 결과는?" options={['NULL', '1', '2', '오류']} correctAnswer={2} explanation="COALESCE는 인수를 왼쪽부터 평가하여 처음으로 NULL이 아닌 값을 반환합니다. 첫 번째 인수가 NULL이므로 넘어가고, 두 번째 인수인 1이 NULL이 아니므로 1을 반환합니다." optionExplanations={['NULL이 아닌 값이 있으므로 NULL을 반환하지 않습니다.', '두 번째 인수 1이 처음으로 만나는 NULL이 아닌 값입니다. 정답입니다.', '두 번째 인수에서 이미 NULL이 아닌 값을 찾았으므로 2는 반환되지 않습니다.', 'COALESCE는 유효한 표준 SQL 함수입니다.']} />

<Quiz questionNumber={39} question="PRIMARY KEY와 NOT NULL이 있는 테이블을 CTAS로 복제할 때 자동 적용되는 제약조건은?" options={['PRIMARY KEY만', 'NOT NULL만', 'PRIMARY KEY, NOT NULL 모두', '적용되는 제약조건 없음']} correctAnswer={2} explanation="Oracle에서 CTAS(CREATE TABLE AS SELECT)로 테이블을 복제할 때 PK, UNIQUE, FK, CHECK, DEFAULT 같은 명시적 제약조건은 복제되지 않습니다. 단, NOT NULL은 컬럼 정의의 속성으로 일반적으로 유지될 수 있습니다." optionExplanations={['PK는 CTAS로 복제되지 않습니다.', 'NOT NULL은 유지될 수 있습니다. 정답입니다.', 'PK는 복제되지 않으므로 둘 다 적용되지는 않습니다.', 'NOT NULL은 유지됩니다.']} />

<Quiz questionNumber={40} question="다음 SQL 중 실행 시 오류가 발생하는 것은? (판매 테이블: 제품명, 단가, 수량 컬럼 존재)" options={['SELECT 제품명, 단가 * 수량 AS 금액 FROM 판매 ORDER BY 금액', 'SELECT 제품명, 단가 FROM 판매 ORDER BY 2', 'SELECT 제품명, SUM(수량) AS 총수량 FROM 판매 GROUP BY 제품명', 'SELECT 제품명, 단가 * 수량 AS 금액 FROM 판매 WHERE 금액 > 1000']} correctAnswer={4} explanation="SELECT 절에서 정의한 별칭(Alias)은 WHERE 절에서 사용할 수 없습니다. SQL 처리 순서상 WHERE가 SELECT보다 먼저 실행되어 별칭이 아직 정의되지 않은 상태이기 때문입니다. 별칭을 조건으로 사용하려면 인라인 뷰로 감싸야 합니다." optionExplanations={['ORDER BY에서 SELECT 별칭 사용은 Oracle에서 허용됩니다.', 'ORDER BY 2는 두 번째 컬럼으로 정렬하는 위치 번호 사용이며 유효합니다.', 'GROUP BY 후 집계함수와 SELECT 그룹 기준 컬럼 조합은 유효합니다.', 'WHERE 절에서 SELECT 별칭 사용은 오류입니다. 정답입니다.']} />

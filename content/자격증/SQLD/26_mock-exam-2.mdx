---
title: '26. 실전 모의 2회'
description: '계층형 질의·ROLLUP·MERGE·상관 서브쿼리 등 고난도 유형을 포함한 두 번째 실전 모의고사입니다.'
---

<Callout type="info">
  **시험 안내**
  - 이 모의고사: 10문제 (1과목 3문제 + 2과목 7문제)
  - **목표 시간: 약 18분**
  - 1회 모의고사보다 난이도가 높습니다. 틀린 문제는 반드시 해설을 통해 이해하세요.
  - 취약 유형: 계층형 질의, ROLLUP, MERGE, 상관 서브쿼리
</Callout>

---

## 1과목: 데이터 모델링의 이해

> - 고객: 서비스를 이용하는 실체
> - 주문: 고객이 상품을 구매하는 행위로 생성되는 정보
> - 고객등급: 구매 금액에 따라 시스템이 자동 부여하는 등급

<Quiz
  questionNumber={1}
  question="위 설명에서 엔터티의 종류를 올바르게 분류한 것은?"
  options={['① 고객(기본 엔터티), 주문(중심 엔터티), 고객등급(행위 엔터티)', '② 고객(기본 엔터티), 주문(행위 엔터티), 고객등급(중심 엔터티)', '③ 고객(중심 엔터티), 주문(행위 엔터티), 고객등급(기본 엔터티)', '④ 고객(기본 엔터티), 주문(행위 엔터티), 고객등급(기본 엔터티)']}
  correctAnswer={2}
  explanation="기본 엔터티: 업무에 원래부터 존재하는 독립적 실체 → 고객. 행위 엔터티: 두 개 이상의 부모 엔터티로부터 행위를 통해 생성 → 주문(고객+상품의 구매 행위). 중심 엔터티: 기본 엔터티로부터 발생되며, 행위 엔터티 생성의 중심 → 고객등급(고객의 누적 구매에서 파생)."
  optionExplanations={['①은 주문과 고객등급의 엔터티 종류를 서로 바꿔서 잘못 분류했습니다.', '정답. 고객(기본), 주문(행위), 고객등급(중심)으로 올바르게 분류됩니다.', '③은 고객을 중심 엔터티, 고객등급을 기본 엔터티로 잘못 분류했습니다.', '④는 고객등급을 기본 엔터티로 잘못 분류했습니다. 고객등급은 고객 데이터로부터 파생되는 중심 엔터티입니다.']}
/>

---

배송정보(주문번호, 배송코드, 배송업체명, 수령인, 배송지주소) — 기본키: 주문번호

함수 종속:
- 주문번호 → 배송코드
- 배송코드 → 배송업체명
- 주문번호 → 수령인, 배송지주소

<Quiz
  questionNumber={2}
  question="위 테이블을 3NF로 만들기 위해 필요한 작업은?"
  options={['① 이미 3NF를 만족하므로 변경이 필요 없다.', '② 배송코드를 기본키에 추가하여 복합키로 만든다.', '③ 배송정보 테이블에서 (배송코드, 배송업체명)을 별도 테이블로 분리한다.', '④ 주문번호만으로 결정되는 모든 속성을 새 테이블로 분리한다.']}
  correctAnswer={3}
  explanation="이행 함수 종속: 주문번호 → 배송코드, 배송코드 → 배송업체명이므로 주문번호 → 배송업체명의 이행 종속이 존재합니다(3NF 위반). 해결: 배송업체(배송코드, 배송업체명) 테이블을 분리하고, 배송정보 테이블에는 배송코드만 남깁니다."
  optionExplanations={['①은 틀렸습니다. 이행 함수 종속(주문번호→배송코드→배송업체명)이 존재하므로 3NF를 위반합니다.', '②는 복합키 변경으로는 이행 함수 종속 문제를 해결할 수 없습니다.', '정답. (배송코드, 배송업체명)을 별도 테이블로 분리하면 이행 함수 종속이 제거됩니다.', '④는 주문번호로만 결정되는 속성을 분리하는 방법으로 2NF 해결 방법에 가깝습니다.']}
/>

---

<Quiz
  questionNumber={3}
  question="다음 중 식별 관계(Identifying Relationship)와 비식별 관계(Non-Identifying Relationship)의 차이로 올바른 것은?"
  options={['① 식별 관계: 자식 엔터티가 부모 엔터티의 기본키를 외래키로만 가짐 / 비식별 관계: 부모 기본키가 자식의 기본키에 포함됨', '② 식별 관계: 부모 기본키가 자식의 기본키를 구성함 / 비식별 관계: 부모 기본키가 자식의 일반 속성(외래키)으로만 존재함', '③ 식별 관계: 자식이 부모 없이 존재 가능 / 비식별 관계: 자식이 부모 없이 존재 불가', '④ 식별 관계와 비식별 관계는 외래키 여부로만 구분한다.']}
  correctAnswer={2}
  explanation="식별 관계: 부모 엔터티의 기본키가 자식 엔터티의 기본키의 일부 또는 전체를 구성합니다. 자식은 부모 없이 존재할 수 없습니다. ERD에서 실선으로 표현합니다. 비식별 관계: 부모 엔터티의 기본키가 자식 엔터티의 일반 속성(외래키)으로만 존재합니다. ERD에서 점선으로 표현합니다."
  optionExplanations={['①은 식별 관계와 비식별 관계의 설명을 반대로 바꿨습니다.', '정답. 식별 관계는 부모 기본키가 자식의 기본키를 구성하고, 비식별 관계는 일반 외래키로만 존재합니다.', '③은 식별/비식별 관계의 특성을 반대로 설명했습니다.', '④는 틀렸습니다. 두 관계 모두 외래키를 사용하지만, 자식의 기본키 포함 여부로 구분합니다.']}
/>

---

## 2과목: SQL 기본 및 활용

직원 테이블 데이터:

| 직원번호 | 부서코드 | 급여(만원) |
|----------|----------|------------|
| E001 | D01 | 300 |
| E002 | D01 | 400 |
| E003 | D02 | 250 |
| E004 | D02 | 350 |
| E005 | D02 | 200 |

쿼리:
```sql
SELECT 직원번호, 부서코드, 급여
FROM 직원 A
WHERE 급여 > (
    SELECT AVG(급여)
    FROM 직원 B
    WHERE B.부서코드 = A.부서코드
);
```

<Quiz
  questionNumber={4}
  question="위 상관 서브쿼리의 결과 행 수는?"
  options={['① 1행', '② 2행', '③ 3행', '④ 4행']}
  correctAnswer={2}
  explanation="각 직원마다 같은 부서의 평균 급여를 계산하여 비교합니다. D01 부서 평균: (300+400)/2=350. E001(300): 300>350? 거짓→제외. E002(400): 400>350? 참→포함. D02 부서 평균: (250+350+200)/3=266.67. E003(250): 거짓→제외. E004(350): 350>266.67? 참→포함. E005(200): 거짓→제외. 결과: E002, E004 = 2행."
  optionExplanations={['①은 부서 평균보다 높은 직원이 하나뿐이라고 잘못 계산했습니다.', '정답. E002(D01 부서 평균 초과)와 E004(D02 부서 평균 초과) = 2행입니다.', '③은 E003 또는 E005를 잘못 포함했습니다.', '④는 너무 많은 직원을 포함했습니다.']}
/>

---

ROLLUP 쿼리 결과 일부:

| 지역 | 상품 | 매출합계 |
|------|------|----------|
| 서울 | A | 100 |
| 서울 | B | 150 |
| 서울 | NULL | 250 |

<Quiz
  questionNumber={5}
  question="위 ROLLUP 결과에서 '서울 | NULL | 250' 행의 의미로 올바른 것은?"
  options={['① 지역이 서울이고 상품 데이터가 없는 행', '② 지역 서울의 모든 상품에 대한 소계', '③ 오류로 인해 상품 값이 누락된 행', '④ ROLLUP 없이 일반 GROUP BY를 사용한 결과와 동일']}
  correctAnswer={2}
  explanation="ROLLUP에서 NULL은 소계 또는 합계 행을 나타냅니다. 지역='서울', 상품=NULL, 매출합계=250은 서울 지역 전체 소계(A:100+B:150=250)입니다. 이 NULL은 실제 데이터가 NULL인 것이 아니라 ROLLUP이 소계 계산을 위해 생성한 집계 행의 표시입니다."
  optionExplanations={['①은 ROLLUP의 NULL을 실제 데이터 NULL로 잘못 해석했습니다.', '정답. ROLLUP의 NULL은 소계 행을 나타내며, 서울 지역 전체 합계(250)입니다.', '③은 틀렸습니다. ROLLUP이 의도적으로 생성하는 소계 행입니다.', '④는 틀렸습니다. 일반 GROUP BY는 소계 행을 생성하지 않습니다.']}
/>

---

조직도 테이블 데이터:

| 사원번호 | 이름 | 상위사원번호 |
|----------|------|-------------|
| 1 | 대표이사 | NULL |
| 2 | 부장A | 1 |
| 3 | 부장B | 1 |
| 4 | 과장A | 2 |
| 5 | 과장B | 2 |

쿼리:
```sql
SELECT LEVEL, 이름, 사원번호
FROM 조직도
START WITH 상위사원번호 IS NULL
CONNECT BY PRIOR 사원번호 = 상위사원번호;
```

<Quiz
  questionNumber={6}
  question="위 계층형 질의에 대한 설명으로 올바른 것은?"
  options={['① 결과는 대표이사 1명만 반환된다.', '② LEVEL 값: 대표이사=1, 부장A·B=2, 과장A·B=3', '③ CONNECT BY PRIOR는 현재 행의 사원번호가 다음 행의 사원번호와 같아야 연결된다.', '④ START WITH 조건을 생략하면 모든 행이 루트 노드가 된다.']}
  correctAnswer={2}
  explanation="START WITH 상위사원번호 IS NULL: 루트 노드(대표이사)에서 시작. CONNECT BY PRIOR 사원번호 = 상위사원번호: 현재 행의 사원번호가 자식 행의 상위사원번호가 되어 부모→자식 방향으로 탐색. LEVEL 결과: 대표이사(1)=1, 부장A·B(2,3)=2, 과장A·B(4,5)=3. 총 5행 반환."
  optionExplanations={['①은 틀렸습니다. START WITH 조건에 맞는 루트부터 하위 계층 전체가 반환됩니다.', '정답. 루트(대표이사)=LEVEL 1, 1단계 하위(부장A·B)=LEVEL 2, 2단계 하위(과장A·B)=LEVEL 3입니다.', '③은 틀렸습니다. PRIOR 사원번호는 부모의 사원번호를 의미하며, 자식의 상위사원번호와 매칭합니다.', '④는 일부 맞지만, 정확히는 루트 노드가 될 조건이 없어 모든 행이 독립적으로 시작점이 됩니다.']}
/>

---

TARGET(고객등급): C001-실버-5%, C002-골드-10%, C003-일반-0%

SOURCE(변경등급): C002-플래티넘-15%, C004-실버-5%

```sql
MERGE INTO 고객등급 T
USING 변경등급 S ON (T.고객번호 = S.고객번호)
WHEN MATCHED THEN
    UPDATE SET T.등급 = S.신등급, T.할인율 = S.신할인율
WHEN NOT MATCHED THEN
    INSERT (고객번호, 등급, 할인율) VALUES (S.고객번호, S.신등급, S.신할인율);
```

<Quiz
  questionNumber={7}
  question="위 MERGE 문 실행 후 TARGET 테이블의 최종 상태는?"
  options={['① C001(실버,5%), C002(플래티넘,15%), C003(일반,0%)', '② C001(실버,5%), C002(플래티넘,15%), C003(일반,0%), C004(실버,5%)', '③ C002(플래티넘,15%), C004(실버,5%)', '④ C001(실버,5%), C002(골드,10%), C003(일반,0%), C004(실버,5%)']}
  correctAnswer={2}
  explanation="SOURCE 행별 처리: C002는 TARGET에 존재 → MATCHED → UPDATE: 등급=플래티넘, 할인율=15%. C004는 TARGET에 없음 → NOT MATCHED → INSERT: (C004, 실버, 5%). 변경되지 않은 TARGET 행: C001, C003은 SOURCE에 없으므로 그대로 유지. 최종: C001(실버,5%), C002(플래티넘,15%), C003(일반,0%), C004(실버,5%)."
  optionExplanations={['①은 C004가 INSERT되지 않은 잘못된 결과입니다.', '정답. C002 UPDATE, C004 INSERT, C001·C003은 그대로 유지됩니다.', '③은 C001과 C003이 삭제된 잘못된 결과입니다. SOURCE에 없는 TARGET 행은 유지됩니다.', '④는 C002가 UPDATE되지 않고 원래 값으로 남은 잘못된 결과입니다.']}
/>

---

2024_수강 테이블: 학번 A001, A002, A002, A003 (A002 중복 포함, 총 4행)

2025_수강 테이블: 학번 A002, A003, A004 (총 3행)

<Quiz
  questionNumber={8}
  question="위 두 테이블에 대해 UNION ALL과 UNION을 사용했을 때 결과 행 수의 차이는?"
  options={['① UNION ALL: 7행, UNION: 4행 (3행 줄어듦)', '② UNION ALL: 7행, UNION: 5행 (2행 줄어듦)', '③ UNION ALL: 6행, UNION: 4행', '④ UNION ALL: 7행, UNION: 3행']}
  correctAnswer={1}
  explanation="UNION ALL(중복 제거 없음): 4+3=7행 (A001, A002, A002, A003, A002, A003, A004). UNION(중복 제거): 고유값 A001, A002, A003, A004 → 4행. 차이: 7-4=3행 줄어듦. A002가 3번, A003이 2번 등장하여 2+1=3행 감소."
  optionExplanations={['정답. UNION ALL은 7행, UNION은 중복 제거 후 4행으로 3행 줄어듭니다.', '②는 UNION 결과를 5행으로 잘못 계산했습니다. A002와 A003의 중복이 모두 제거됩니다.', '③은 UNION ALL을 6행으로 잘못 계산했습니다. 2024_수강에 A002가 중복 포함되어 4행입니다.', '④는 UNION 결과를 3행으로 잘못 계산했습니다. 4개의 고유값이 있습니다.']}
/>

---

월별매출 테이블 데이터:

| 영업사원 | 월 | 매출 |
|----------|-----|------|
| 박민수 | 1 | 100 |
| 박민수 | 2 | 150 |
| 박민수 | 3 | 200 |
| 김지수 | 1 | 120 |

쿼리:
```sql
SELECT 영업사원, 월, 매출,
       SUM(매출) OVER (
           PARTITION BY 영업사원
           ORDER BY 월
           ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW
       ) AS 누적매출
FROM 월별매출
WHERE 영업사원 = '박민수';
```

<Quiz
  questionNumber={9}
  question="위 쿼리 결과에서 박민수의 3월 누적매출 값은?"
  options={['① 200', '② 350', '③ 450', '④ 570']}
  correctAnswer={3}
  explanation="ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW는 '첫 번째 행부터 현재 행까지'를 의미합니다. 박민수 기준(PARTITION BY 영업사원): 1월 누적=100, 2월 누적=100+150=250, 3월 누적=100+150+200=450."
  optionExplanations={['①은 3월 매출만 반환한 값입니다. 누적이 아닙니다.', '②는 1월+2월 누적입니다. 3월 매출(200)을 더해야 합니다.', '정답. 1월(100)+2월(150)+3월(200)=450이 3월 누적매출입니다.', '④는 박민수와 김지수의 매출을 합산한 잘못된 결과입니다.']}
/>

---

> **상황**: 테스트 환경의 임시_주문내역 테이블에 100만 건의 데이터가 있다. 이 데이터를 모두 빠르게 삭제해야 하며, 삭제 후 되돌릴 필요가 없다. 테이블 구조는 유지해야 한다.

<Quiz
  questionNumber={10}
  question="위 상황에서 가장 적합한 선택은?"
  options={['① DELETE FROM 임시_주문내역; — DML이라 ROLLBACK 가능하므로 안전하다.', '② TRUNCATE TABLE 임시_주문내역; — DDL이라 빠르고 즉시 COMMIT되므로 적합하다.', '③ DROP TABLE 임시_주문내역; — 가장 빠르게 삭제할 수 있다.', '④ DELETE FROM 임시_주문내역 WHERE 1=1; — 조건을 명시해야 전체 삭제가 된다.']}
  correctAnswer={2}
  explanation="요구사항: 전체 삭제, 빠른 처리, 되돌릴 필요 없음, 테이블 구조 유지. TRUNCATE: 전체 삭제(WHERE 없음), 로그 최소화로 DELETE보다 훨씬 빠름, DDL이라 자동 COMMIT(되돌릴 필요 없으므로 문제없음), 테이블 구조 유지. DROP은 테이블 구조까지 삭제되므로 요구사항 불충족."
  optionExplanations={['①은 100만 건 처리 시 행별 로그로 속도가 느리고 빠른 처리 요구사항을 충족하지 못합니다.', '정답. TRUNCATE는 빠르고, 전체 삭제하며, 구조를 유지하고, 자동 COMMIT됩니다. 모든 요구사항 충족.', '③은 테이블 구조까지 삭제되므로 테이블 구조 유지 요구사항을 충족하지 못합니다.', '④는 ①과 동일하게 DELETE로 느립니다. WHERE 1=1 없이도 DELETE는 전체 행을 삭제합니다.']}
/>

---

## 최종 점검 체크리스트

시험 당일 확인할 핵심 개념을 최종 점검하세요.

### 데이터 모델링

- [ ] ERD 기호: `|`(필수,최솟값1), `O`(선택,최솟값0), `||`(최댓값1), `<>`(최댓값N) 구분
- [ ] 정규화 3단계: 1NF(원자값), 2NF(부분종속제거), 3NF(이행종속제거) 조건 암기
- [ ] 이상현상 3종: 삽입·삭제·갱신 이상의 발생 상황 구분
- [ ] 식별/비식별 관계: 기본키 포함 여부(식별=PK포함, 비식별=FK만)
- [ ] 반정규화 목적: 조회 성능 향상, 부작용: 무결성 관리 비용 증가

### SQL 기본

- [ ] NULL 규칙: NULL과의 산술=NULL, `=NULL` 불가, `IS NULL`만 가능
- [ ] COUNT(*) vs COUNT(컬럼): NULL 포함 여부 차이
- [ ] NOT IN + NULL: 항상 0건 반환 함정
- [ ] SQL 실행순서: FROM→WHERE→GROUP BY→HAVING→SELECT→ORDER BY
- [ ] HAVING vs WHERE: 집계 함수 사용 가능 위치
- [ ] JOIN 결과 행 수: INNER(교집합), LEFT(왼쪽전체), FULL(합집합)

### SQL 활용

- [ ] 단일행 서브쿼리: `=, >, <` 사용, 다중행이면 오류
- [ ] 다중행 서브쿼리: `IN, ANY, ALL, EXISTS` 사용
- [ ] UNION vs UNION ALL: 중복 제거 여부 차이
- [ ] RANK vs DENSE_RANK: 동점 시 건너뜀 여부 (RANK:건너뜀, DENSE_RANK:연속)
- [ ] ROLLUP 행 수: ROLLUP(A,B) → A종류×(B종류+1)+1
- [ ] TCL: COMMIT(확정), ROLLBACK(전체취소), ROLLBACK TO(부분취소)
- [ ] DDL 자동COMMIT: CREATE/ALTER/DROP/TRUNCATE는 자동 COMMIT
- [ ] MERGE: ON 조건 일치 시 MATCHED, 불일치 시 NOT MATCHED 실행
- [ ] CONNECT BY PRIOR: 계층형 쿼리에서 부모→자식 방향 탐색

<Callout type="warning">
  **시험 당일 주의사항**
  - 문제를 끝까지 읽고 선택지를 확인하세요. "올바른 것"과 "올바르지 않은 것"을 혼동하지 마세요.
  - SQL 결과를 구할 때는 데이터를 종이에 메모하며 단계별로 계산하세요.
  - 시간이 부족하면 확실한 문제를 먼저 풀고 어려운 문제는 나중에 검토하세요.
</Callout>

---

## 마무리 복습

<Quiz questionNumber={11} question="상관 서브쿼리(Correlated Subquery)에 대한 설명으로 올바른 것은?" options={['① 서브쿼리가 메인쿼리와 독립적으로 한 번만 실행된다.', '② 서브쿼리가 메인쿼리의 각 행마다 반복적으로 실행된다.', '③ 상관 서브쿼리는 항상 단일 값을 반환한다.', '④ 상관 서브쿼리는 SELECT 절에서만 사용할 수 있다.']} correctAnswer={2} explanation="상관 서브쿼리는 메인쿼리의 각 행을 처리할 때마다 해당 행의 값을 참조하여 서브쿼리를 실행합니다. 따라서 메인쿼리 결과 행 수만큼 서브쿼리가 반복 실행됩니다. 이는 비상관 서브쿼리(독립적으로 한 번만 실행)와의 핵심 차이입니다." optionExplanations={['비상관 서브쿼리(Non-Correlated Subquery)의 설명입니다. 메인쿼리와 독립적으로 한 번 실행됩니다.', '정답. 상관 서브쿼리는 메인쿼리의 각 행마다 서브쿼리가 실행됩니다.', '상관 서브쿼리도 다중행을 반환할 수 있으며, EXISTS와 함께 쓰일 때 특히 유용합니다.', '상관 서브쿼리는 WHERE, HAVING, SELECT 절 등 다양한 위치에서 사용 가능합니다.']} />

<Quiz questionNumber={12} question="다음 중 계층형 질의(CONNECT BY)에서 LEVEL 값에 대한 설명으로 올바른 것은?" options={['① LEVEL은 항상 0부터 시작한다.', '② 루트 노드의 LEVEL 값은 1이다.', '③ LEVEL 값이 클수록 루트에 가깝다.', '④ LEVEL은 CONNECT BY 없이도 사용 가능한 의사 컬럼이다.']} correctAnswer={2} explanation="계층형 질의에서 LEVEL은 계층 깊이를 나타내는 의사 컬럼(Pseudo Column)입니다. START WITH로 지정된 루트 노드의 LEVEL은 1이고, 하위 레벨로 내려갈수록 값이 증가합니다(2, 3, ...). LEVEL은 CONNECT BY 계층형 쿼리에서만 의미있는 의사 컬럼입니다." optionExplanations={['LEVEL은 0이 아닌 1부터 시작합니다.', '정답. 루트 노드(START WITH 조건에 해당하는 행)의 LEVEL은 1입니다.', 'LEVEL 값이 클수록 루트에서 멀리 떨어진(깊은) 노드입니다.', 'LEVEL은 CONNECT BY 계층형 쿼리 컨텍스트 안에서만 의미있는 값을 가집니다.']} />

<Quiz questionNumber={13} question="ROLLUP(A, B, C)을 사용했을 때 생성되는 소계/합계 행의 종류 수는 (기본 데이터 행 제외)?" options={['① 3종류 (A별, B별, 전체)', '② 4종류 (A별, B별, C별, 전체)', '③ 4종류 ((A,B)별 소계, A별 소계, 전체 합계, 빈 행 1개)', '④ 3종류 (C별 소계 포함 안 됨 → (A,B)별, A별, 전체)']} correctAnswer={4} explanation="ROLLUP(A, B, C)은 지정된 순서를 오른쪽부터 하나씩 제거하며 소계를 만듭니다. 생성 조합: (A,B,C) 원본 + (A,B,NULL) A,B별 소계 + (A,NULL,NULL) A별 소계 + (NULL,NULL,NULL) 전체 합계. 추가되는 소계/합계 행의 종류: (A,B)별, A별, 전체 = 3종류. C별 단독 소계는 생성하지 않습니다." optionExplanations={['A별 소계는 생성되지만 B별 단독 소계는 ROLLUP에서 생성되지 않습니다.', 'ROLLUP은 왼쪽에서 오른쪽으로 순차적 소계만 생성하므로 B별, C별 단독 소계는 없습니다.', '추가 소계 종류로 설명했지만 (A,B)별 소계는 원본 데이터 행에 이미 존재하는 경우가 많습니다.', '정답. ROLLUP(A,B,C)에서 추가 생성 소계: (A,B)별 소계, A별 소계, 전체 합계 = 3가지 종류입니다.']} />

## 참고 자료

- [KDATA 데이터자격시험 - SQL 개발자](https://www.dataq.or.kr/www/sub/a_04.do)

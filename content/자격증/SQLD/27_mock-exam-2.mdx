---
title: '27. SQLD 예상 모의고사 2회'
description: '조인, 서브쿼리, 집합 연산자 비중을 강화한 재구성 모의고사(데이터 모델링 10문항 + SQL 활용 40문항)'
---

<Callout type="warning">
  **이 모의고사는 실제 기출 문제가 아니라, 최근 회차(56~60회) 기출·복원 문제의 출제 유형과 함정 구성 방식을 분석해 새 테이블·데이터·상황으로 재구성한 연습 문제입니다.** 26편(1회)과 문항이 겹치지 않도록 전부 새로 작성했으며, 이번 회차는 조인·서브쿼리·집합 연산자 비중을 특히 높였습니다. 권장 풀이 시간은 **90분**입니다. 시간을 재고 한 번에 풀어본 뒤 채점하세요.
</Callout>

## 이 회차의 배점표

| 영역 | 문항 번호 | 문항 수 | 배점(1문항) |
|------|-----------|---------|-------------|
| 1과목 – 데이터 모델링의 이해 | 1–10 | 10문항 | 10점 |
| 2과목 – SQL 기본 및 활용 | 11–50 | 40문항 | 2.5점 |

2과목 40문항 중 조인·서브쿼리·집합 연산자 관련 문항이 약 24문항으로 절반 이상을 차지합니다. 조인 유형별 결과 건수 계산, NULL이 섞인 서브쿼리의 함정, 집합 연산자의 중복 제거 여부를 특히 집중적으로 점검하세요.

## 1과목 — 데이터 모델링의 이해 (1~10번)

<Quiz
  questionNumber={1}
  question="데이터 모델링을 구성하는 세 가지 관점에 대한 설명으로 옳지 않은 것은?"
  options={['데이터 관점은 업무에서 어떤 데이터가 존재하고 그 데이터 간에 어떤 관계가 있는지를 다룬다.', '프로세스 관점은 업무가 어떻게 처리되는지 절차와 흐름을 다룬다.', '상관 관점은 데이터와 프로세스가 서로 어떻게 영향을 주고받는지를 다룬다.', '기술 관점은 어떤 데이터베이스 제품과 하드웨어를 쓸지 결정하는 것으로 세 관점 중 하나다.']}
  correctAnswer={4}
  explanation="데이터 모델링의 세 관점은 데이터(무엇을), 프로세스(어떻게), 상관(데이터와 프로세스의 연관)이다. 특정 제품이나 하드웨어를 고르는 기술 관점은 이 세 관점에 포함되지 않는다."
  optionExplanations={['데이터가 무엇인지와 데이터 사이의 관계를 다루는 것이 데이터 관점이므로 옳은 설명이다.', '업무 처리 절차를 다루는 것이 프로세스 관점이므로 옳은 설명이다.', '데이터와 프로세스의 상호작용을 다루는 것이 상관 관점이므로 옳은 설명이다.', '정답. 기술 관점은 데이터 모델링의 세 관점에 속하지 않는 개념이다.']}
/>

<Quiz
  questionNumber={2}
  question="엔터티(Entity)의 특징에 대한 설명으로 옳지 않은 것은?"
  options={['엔터티는 두 개 이상의 인스턴스의 집합이어야 한다.', '엔터티는 하나의 속성만으로도 충분히 성립할 수 있다.', '엔터티의 각 인스턴스는 유일한 식별자로 구분할 수 있어야 한다.', '엔터티는 업무에서 사용되며 다른 엔터티와 관계를 가질 수 있다.']}
  correctAnswer={2}
  explanation="엔터티는 일반적으로 두 개 이상의 속성을 가져야 성립하는 것으로 본다. 속성이 하나뿐이면 그 값 자체가 식별자와 다를 것이 없어 엔터티로서의 의미가 성립하기 어렵다."
  optionExplanations={['엔터티가 두 개 이상의 인스턴스 집합이라는 설명은 정확하다.', '정답. 엔터티는 두 개 이상의 속성을 가져야 하며 하나의 속성만으로는 성립하기 어렵다.', '유일한 식별자로 인스턴스를 구분할 수 있어야 한다는 설명은 정확하다.', '업무에서 사용되고 다른 엔터티와 관계를 맺는다는 설명은 정확하다.']}
/>

<Quiz
  questionNumber={3}
  question="각 속성이 가질 수 있는 값의 데이터 타입, 길이, 허용되는 값의 범위를 정의한 것을 무엇이라 하는가?"
  options={['제약조건', '도메인', '식별자', '카디널리티']}
  correctAnswer={2}
  explanation="도메인은 어떤 속성이 가질 수 있는 값의 범위를 데이터 타입, 길이, 허용 값 목록 등으로 정의한 것이다. 예를 들어 성별 속성의 도메인은 남 또는 여로 제한된다."
  optionExplanations={['제약조건은 무결성을 지키기 위한 규칙 전반을 가리키는 더 넓은 개념으로, 값의 범위만을 뜻하는 도메인과는 층위가 다르다.', '정답. 값의 범위를 데이터 타입과 길이, 허용 값으로 정의한 것이 도메인이다.', '식별자는 인스턴스를 유일하게 구분하는 속성이나 속성의 조합을 의미한다.', '카디널리티는 관계의 차수(1대1, 1대다 등)를 나타내는 개념이다.']}
/>

<Quiz
  questionNumber={4}
  question="주식별자를 대표성 유무와 생성 방식에 따라 본질식별자와 인조식별자로 나눌 때의 설명으로 옳지 않은 것은?"
  options={['본질식별자는 업무에서 자연스럽게 발생해 그 자체로 업무적 의미를 가진다.', '인조식별자는 업무적 의미와 무관하게 관리 편의를 위해 일련번호처럼 인위적으로 부여한다.', '주식별자로 삼을 속성이 지나치게 많은 속성으로 구성될 때 인조식별자를 대안으로 쓸 수 있다.', '인조식별자는 여러 속성을 조합한 복합키의 형태로만 만들 수 있다.']}
  correctAnswer={4}
  explanation="인조식별자는 대개 단일 속성(주로 일련번호)으로 만들어지며, 복합키 형태로만 만들어야 한다는 제약은 없다. 오히려 인조식별자를 도입하는 대표적인 이유가 복잡한 복합키를 단순한 단일 속성으로 대체하기 위해서다."
  optionExplanations={['본질식별자가 업무에서 자연스럽게 생기고 업무적 의미를 가진다는 설명은 정확하다.', '인조식별자가 업무 의미와 무관하게 인위적으로 부여된다는 설명은 정확하다.', '복잡한 본질식별자를 대체하기 위해 인조식별자를 쓸 수 있다는 설명은 정확하다.', '정답. 인조식별자는 대개 단일 속성으로 만들어지며 복합키로만 만들 수 있다는 제한은 없다.']}
/>

<Quiz
  questionNumber={5}
  question="ERD에서 관계를 표기할 때 사용하는 표기 요소로 옳지 않은 것은?"
  options={['관계명(두 엔터티가 어떤 관계인지 이름을 붙이는 것)', '관계차수(1대1, 1대다, 다대다 같은 카디널리티)', '관계선택사양(참여가 필수인지 선택인지)', '관계빈도(그 관계가 발생하는 트랜잭션 횟수)']}
  correctAnswer={4}
  explanation="관계 표기의 표준 요소는 관계명, 관계차수, 관계선택사양 세 가지다. 트랜잭션 발생 횟수 같은 관계빈도는 성능 분석에서 다룰 수는 있어도 ERD의 표준 관계 표기 요소는 아니다."
  optionExplanations={['관계명은 관계를 표기하는 표준 요소이므로 옳은 설명이다.', '관계차수는 관계를 표기하는 표준 요소이므로 옳은 설명이다.', '관계선택사양은 관계를 표기하는 표준 요소이므로 옳은 설명이다.', '정답. 관계빈도는 표준 관계 표기 요소가 아니다.']}
/>

<Quiz
  questionNumber={6}
  question="택배사의 배송과 주문 엔터티가 한 건의 주문은 0건 이상의 배송을 가질 수 있고, 모든 배송은 반드시 하나의 주문에 속한다는 규칙으로 연결되어 있다. 이 관계에 대한 설명으로 옳은 것은?"
  options={['주문 쪽 참여는 필수(mandatory)다.', '배송 쪽 참여는 선택(optional)이다.', '주문과 배송의 관계 차수는 1대다(1:M)다.', '배송이 없는 주문은 존재할 수 없다.']}
  correctAnswer={3}
  explanation="한 건의 주문에 배송이 여러 건(0건 이상) 대응할 수 있다는 것은 주문 1에 배송 M이 대응하는 1대다 관계임을 의미한다."
  optionExplanations={['주문은 배송이 0건이어도 되므로 배송 쪽에서 볼 때 주문에 대한 참여는 필수가 아니라 선택이다.', '배송은 반드시 하나의 주문에 속해야 하므로 주문 쪽 참여가 필수이지 배송 쪽이 선택인 것이 아니다. 이 표현은 방향이 뒤바뀌었다.', '정답. 주문 1건에 배송 여러 건이 대응할 수 있으므로 1대다 관계다.', '주문은 배송이 0건일 수 있으므로 배송이 없는 주문도 존재할 수 있다.']}
/>

**수강료납부 테이블(정규화 이전 상태)**

| 납부번호(PK) | 학번 | 강좌코드 | 강좌명 | 강좌료 |
|--------------|------|----------|--------|--------|
| 9001 | S10 | L01 | 웹개발입문 | 150000 |
| 9002 | S11 | L01 | 웹개발입문 | 150000 |
| 9003 | S10 | L02 | 데이터분석 | 200000 |

납부번호(기본키)가 강좌코드를 정하고, 강좌코드가 다시 강좌명과 강좌료를 정하는 함수 종속이 있다.

<Quiz
  questionNumber={7}
  question="위 수강료납부 테이블에서 기본키를 거치지 않고 강좌코드가 강좌명, 강좌료를 직접 결정하는 이행적 함수 종속을 제거하려면 어느 정규화 단계가 필요한가?"
  options={['제1정규화', '제2정규화', '제3정규화', 'BCNF']}
  correctAnswer={3}
  explanation="납부번호에서 강좌코드로, 강좌코드에서 강좌명·강좌료로 이어지는 이행적 함수 종속을 제거하려면 강좌 엔터티(강좌코드, 강좌명, 강좌료)를 분리해내는 제3정규화가 필요하다."
  optionExplanations={['제1정규화는 다중값이나 반복 집단을 원자값으로 만드는 단계로, 이 테이블은 이미 각 칸이 원자값이라 해당하지 않는다.', '제2정규화는 복합키 일부에 대한 부분 함수 종속을 제거하는 단계인데, 이 테이블의 기본키는 납부번호 단일 속성이라 부분 종속이 성립하지 않는다.', '정답. 기본키가 아닌 속성 사이(강좌코드에서 강좌명, 강좌료로)의 이행적 종속을 제거하는 것이 제3정규화다.', 'BCNF는 후보키가 아닌 결정자를 제거하는 더 엄격한 단계로, 이 테이블에는 아직 3정규화 대상인 이행 종속이 남아 있어 우선 적용 대상이 아니다.']}
/>

<Quiz
  questionNumber={8}
  question="정규화와 반정규화의 관계에 대한 설명으로 옳지 않은 것은?"
  options={['정규화를 지나치게 많이 적용하면 조인이 늘어나 오히려 조회 성능이 떨어질 수 있다.', '반정규화는 데이터 중복을 감수하는 대신 조회 성능을 높이려는 기법이다.', '반정규화를 적용하면 값을 동기화해야 하는 관리 부담이 늘어날 수 있다.', '반정규화는 정규화를 거치지 않은 설계와 동일한 개념이다.']}
  correctAnswer={4}
  explanation="반정규화는 정규화를 이미 마친 뒤, 성능을 위해 의도적으로 일부를 되돌리는 기법이다. 처음부터 정규화를 하지 않은 설계와는 접근 순서와 목적이 다르므로 같은 개념으로 볼 수 없다."
  optionExplanations={['정규화가 과도하면 조인이 늘어 성능이 떨어질 수 있다는 설명은 정확하다.', '반정규화의 목적이 성능 향상이고 그 대가가 데이터 중복이라는 설명은 정확하다.', '중복 데이터가 생기면 값을 맞춰야 하는 관리 부담이 늘어난다는 설명은 정확하다.', '정답. 반정규화는 정규화를 거친 뒤 의도적으로 되돌리는 것이라, 애초에 정규화를 하지 않은 것과는 다른 개념이다.']}
/>

<Quiz
  questionNumber={9}
  question="식별자 관계와 비식별자 관계의 생명주기 차이에 대한 설명으로 옳지 않은 것은?"
  options={['식별자 관계는 부모의 기본키가 자식의 기본키 일부로 상속되어 두 엔터티가 강하게 결합된다.', '비식별자 관계는 부모의 기본키가 자식의 일반 속성(외래키)으로만 상속된다.', '비식별자 관계로 설계하면 상위 엔터티와 조인하지 않고도 조회 성능이 항상 향상된다.', '식별자 관계가 여러 단계로 이어지면 하위로 갈수록 기본키를 구성하는 속성 수가 늘어난다.']}
  correctAnswer={3}
  explanation="비식별자 관계는 부모 키가 자식의 일반 속성으로만 내려가므로 오히려 상위 정보를 조회하려면 조인이 더 필요해질 수 있다. 조회 성능이 항상 향상된다는 인과관계는 성립하지 않는다."
  optionExplanations={['식별자 관계에서 부모 키가 자식 기본키의 일부가 된다는 설명은 정확하다.', '비식별자 관계에서 부모 키가 자식의 일반 속성으로 상속된다는 설명은 정확하다.', '정답. 비식별자 관계는 모델을 단순화할 수는 있지만 조회 성능이 항상 향상된다고 단정할 수 없다.', '식별자 관계가 연쇄되면 하위 엔터티일수록 기본키 구성 속성이 늘어난다는 설명은 정확하다.']}
/>

<Quiz
  questionNumber={10}
  question="트랜잭션의 특성(ACID) 중 고립성(Isolation)에 대한 설명으로 가장 적절한 것은?"
  options={['트랜잭션의 작업은 모두 반영되거나 전혀 반영되지 않아야 한다.', '동시에 실행되는 트랜잭션들은 서로의 중간 연산 결과에 영향을 받지 않아야 한다.', '트랜잭션이 완료되면 그 결과는 영구히 보존되어야 한다.', '트랜잭션 수행 전후로 데이터베이스는 정해진 무결성 규칙을 계속 만족해야 한다.']}
  correctAnswer={2}
  explanation="고립성은 여러 트랜잭션이 동시에 실행되더라도 각 트랜잭션이 마치 혼자 실행되는 것처럼 서로의 중간 상태에 간섭받지 않아야 한다는 특성이다."
  optionExplanations={['이는 원자성(All or Nothing)에 대한 설명이다.', '정답. 동시 실행 트랜잭션 간의 비간섭이 고립성의 정의다.', '이는 지속성에 대한 설명이다.', '이는 일관성에 대한 설명이다.']}
/>

## 2과목 — SQL 기본 및 활용 (11~50번)

<Quiz
  questionNumber={11}
  question="UNIQUE 제약조건과 PRIMARY KEY 제약조건의 차이에 대한 설명으로 옳은 것은?"
  options={['UNIQUE 제약조건이 걸린 컬럼은 한 테이블에 여러 개 둘 수 있지만 기본키는 한 테이블에 하나만 지정할 수 있다.', 'UNIQUE 제약조건은 NULL을 전혀 허용하지 않는다.', '기본키는 NULL을 허용하지만 값의 중복은 허용하지 않는다.', 'UNIQUE와 PRIMARY KEY는 완전히 동일한 제약조건이라 차이가 없다.']}
  correctAnswer={1}
  explanation="한 테이블에는 기본키를 하나만 지정할 수 있지만, UNIQUE 제약조건은 여러 컬럼(또는 컬럼 조합)에 각각 걸 수 있다. 이 점이 두 제약조건의 대표적인 차이다."
  optionExplanations={['정답. 기본키는 테이블당 하나뿐이지만 UNIQUE는 여러 개 지정할 수 있다.', 'UNIQUE 제약조건은 NULL을 허용하며, 여러 행에 NULL이 있어도 서로 다른 값으로 취급되어 중복 위반이 되지 않는다.', '기본키는 NOT NULL과 UNIQUE를 모두 만족해야 하므로 NULL을 허용하지 않는다.', 'UNIQUE는 NULL을 허용하고 여러 개 지정 가능하지만 기본키는 NULL을 허용하지 않고 하나만 지정 가능하다는 분명한 차이가 있다.']}
/>

<Quiz
  questionNumber={12}
  question="다음 UPDATE 문의 동작에 대한 설명으로 가장 적절한 것은? 쿼리: UPDATE 사원 A SET 연봉 = (SELECT MAX(B.연봉) FROM 사원 B WHERE B.부서 = A.부서 AND B.직급 = 대리) WHERE A.직급 = 대리;"
  options={['모든 사원의 연봉을 회사 전체에서 가장 높은 대리의 연봉으로 바꾼다.', '직급이 대리인 사원의 연봉을 같은 부서 내 대리 중 최고 연봉으로 갱신한다.', '같은 부서에 대리가 한 명도 없으면 해당 행의 연봉이 0으로 바뀐다.', '서브쿼리가 상관관계 없이 실행되어 전체 사원에 대해 단 한 번만 평가된다.']}
  correctAnswer={2}
  explanation="바깥 WHERE 절이 직급이 대리인 행만 갱신 대상으로 선택하고, 서브쿼리는 A.부서로 메인쿼리와 연결된 상관 서브쿼리라 대리 행 하나마다 같은 부서의 대리 중 최고 연봉을 다시 계산해 대입한다."
  optionExplanations={['WHERE 절이 직급이 대리인 행만 갱신 대상으로 한정하므로 모든 사원이 아니라 대리만 갱신된다.', '정답. 상관 서브쿼리가 A.부서를 기준으로 같은 부서의 대리 중 최고 연봉을 찾아 그 행에 대입한다.', '같은 부서에 대리가 없으면 서브쿼리 결과가 NULL이 되어 해당 행의 연봉이 NULL로 바뀔 뿐 0이 되지는 않는다.', 'A.부서로 메인쿼리 값을 참조하는 상관 서브쿼리이므로 갱신 대상 행마다 반복 평가된다.']}
/>

빈 결제 테이블(결제번호 NUMBER, 승인상태 VARCHAR2, 금액 NUMBER)에 대해 다음을 순서대로 실행한다.

```sql
INSERT INTO 결제 VALUES (1, 대기, 1000);
INSERT INTO 결제 VALUES (2, 대기, 2000);
SAVEPOINT SP_A;
UPDATE 결제 SET 승인상태 = 완료 WHERE 결제번호 = 1;
SAVEPOINT SP_B;
INSERT INTO 결제 VALUES (3, 완료, 3000);
ROLLBACK TO SP_A;
UPDATE 결제 SET 승인상태 = 완료 WHERE 결제번호 = 2;
COMMIT;

SELECT COUNT(*) FROM 결제 WHERE 승인상태 = 완료;
```

<Quiz
  questionNumber={13}
  question="위 명령을 모두 실행한 뒤 마지막 SELECT 문의 결과(COUNT)로 옳은 것은?"
  options={['0', '1', '2', '3']}
  correctAnswer={2}
  explanation="결제번호 1, 2를 넣고 SP_A를 찍는다. 결제번호 1을 완료로 바꾸고 SP_B를 찍은 뒤 결제번호 3을 삽입하지만, ROLLBACK TO SP_A는 SP_A 시점 이후의 모든 변경(결제번호 1의 UPDATE와 결제번호 3의 INSERT 모두)을 취소한다. 그 결과 결제번호 1, 2만 남고 둘 다 승인상태는 대기로 되돌아간다. 이후 결제번호 2를 완료로 바꾸고 COMMIT하면 최종적으로 완료 상태인 행은 결제번호 2 하나뿐이다."
  optionExplanations={['결제번호 2에 대한 UPDATE가 ROLLBACK 이후 남아 있다는 점을 놓친 오답이다.', '정답. SP_A로 되돌리면 결제번호 1의 UPDATE와 결제번호 3의 INSERT가 모두 취소되고, 이후 결제번호 2만 완료로 바뀐다.', 'ROLLBACK TO SP_A가 SP_B 이후만 취소한다고 착각해 결제번호 1도 완료 상태로 남아 있다고 본 오답이다.', 'ROLLBACK TO SAVEPOINT를 전체 롤백으로 착각하지 않았다면 나오지 않을 값으로, 결제번호 3의 INSERT까지 살아 있다고 잘못 판단한 오답이다.']}
/>

<Quiz
  questionNumber={14}
  question="조건 NOT (급여가 3000보다 작음 OR 급여가 5000보다 큼)과 논리적으로 동일한 조건은?"
  options={['급여가 3000보다 크거나 같고 5000보다 작거나 같다.', '급여가 3000보다 작거나 5000보다 크다.', '급여가 3000보다 크고 5000보다 작다.', '급여가 3000과 같거나 5000과 같다.']}
  correctAnswer={1}
  explanation="드모르간 법칙에 따라 NOT(A OR B)는 (NOT A) AND (NOT B)로 바뀐다. NOT(급여가 3000보다 작음)은 급여가 3000보다 크거나 같음이 되고, NOT(급여가 5000보다 큼)은 급여가 5000보다 작거나 같음이 되어, 두 조건을 AND로 묶으면 급여가 3000 이상 5000 이하라는 범위 조건과 같아진다."
  optionExplanations={['정답. 드모르간 법칙을 적용하면 3000 이상 5000 이하라는 범위 조건과 정확히 같아진다.', '원래 부정하기 전의 조건과 같아 NOT을 적용하지 않은 것과 같은 오답이다.', '경계값인 3000과 5000을 포함하지 않는 조건이라 원래 조건과 다르다.', '드모르간 법칙을 잘못 적용해 AND를 OR로 바꾼 오답이다.']}
/>

<Quiz
  questionNumber={15}
  question="문자열 abcdefgh에 대해 SUBSTR(문자열, -3)을 실행하면 어떤 값이 반환되는가?"
  options={['abc', 'fgh', 'abcdefgh', '오류가 발생한다']}
  correctAnswer={2}
  explanation="SUBSTR의 시작 위치가 음수이면 문자열의 끝에서부터 위치를 센다. 시작 위치 -3은 끝에서 세 번째 문자인 f부터 시작한다는 뜻이므로 f, g, h를 반환한다."
  optionExplanations={['음수 위치는 문자열의 끝에서부터 세므로 앞쪽 abc가 아니다.', '정답. 끝에서 세 번째 위치인 f부터 끝까지 fgh가 반환된다.', '음수 위치를 지정했더라도 전체 문자열이 그대로 반환되는 것은 아니다.', 'SUBSTR은 음수 시작 위치를 정상적으로 지원하는 표준 문법이라 오류가 발생하지 않는다.']}
/>

<Quiz
  questionNumber={16}
  question="정규표현식 캐럿(^)과 대괄호 안의 캐럿의 의미 차이에 대한 설명으로 옳은 것은?"
  options={['대괄호 밖에 있는 캐럿과 대괄호 안에 있는 캐럿은 항상 같은 의미다.', '대괄호 밖의 캐럿은 문자열의 시작 위치를 나타내고, 대괄호 안 첫 위치의 캐럿은 그 안에 나열된 문자를 제외한다는 의미다.', '대괄호 안의 캐럿은 항상 문자열의 끝을 나타낸다.', '캐럿은 정규표현식에서 아무 의미도 갖지 않는 일반 문자다.']}
  correctAnswer={2}
  explanation="정규표현식에서 대괄호 밖의 캐럿(^)은 문자열이 시작하는 위치를 지정하는 앵커이고, 대괄호 안 맨 앞의 캐럿은 그 안에 나열된 문자들을 제외한 나머지 문자와 매칭한다는 부정의 의미로 완전히 다르게 쓰인다."
  optionExplanations={['위치와 부정이라는 서로 다른 역할을 하므로 항상 같은 의미라는 설명은 틀렸다.', '정답. 대괄호 밖은 시작 앵커, 대괄호 안 첫 위치는 부정(제외)이라는 서로 다른 의미다.', '문자열의 끝을 나타내는 것은 캐럿이 아니라 달러 기호다.', '캐럿은 위치나 부정을 나타내는 특수한 의미를 가진 메타문자다.']}
/>

수강 테이블에서 학점이 2.0 이상인 학생만 대상으로 학과별 평균 학점을 큰 순서로 정렬하려 한다.

**수강 테이블**

| 학번 | 학과 | 학점 |
|------|------|------|
| S1 | 컴공 | 3.5 |
| S2 | 컴공 | 1.5 |
| S3 | 전자 | 4.0 |
| S4 | 전자 | 2.5 |
| S5 | 국문 | 3.0 |

```sql
SELECT 학과, AVG(학점)     -- (가)
FROM 수강                  -- (나)
WHERE 학점 >= 2.0          -- (다)
GROUP BY 학과              -- (라)
HAVING AVG(학점) >= 2.0    -- (마)
ORDER BY AVG(학점);
```

<Quiz
  questionNumber={17}
  question="위 쿼리로 원하는 결과(전자 3.25, 국문 3.0)를 얻으려면 어느 부분을 고쳐야 하는가?"
  options={['(다) WHERE 절을 삭제한다.', '(마) HAVING 절을 삭제한다.', 'ORDER BY에 DESC를 추가한다.', 'GROUP BY를 SELECT 절보다 앞에 둔다.']}
  correctAnswer={3}
  explanation="학점 2.0 이상인 행만 남기면 컴공은 3.5 한 건만 남아 평균 3.5가 되고, 전자는 4.0과 2.5 평균 3.25, 국문은 3.0이 된다. HAVING 조건도 모두 통과해 컴공 3.5, 전자 3.25, 국문 3.0 세 그룹이 남는데, 이를 큰 순서(내림차순)로 보려면 ORDER BY AVG(학점) DESC가 필요하다. 지금은 DESC가 없어 오름차순(국문, 전자, 컴공 순)으로 나온다."
  optionExplanations={['WHERE 절은 학점 2.0 미만인 행을 걸러내는 정상적인 필터이므로 삭제하면 원치 않는 행까지 포함된다.', 'HAVING 절은 평균이 2.0 이상인 그룹만 남기는 정상적인 필터이므로 삭제할 필요가 없다.', '정답. ORDER BY에 DESC가 없어 오름차순으로 나오므로 내림차순으로 만들려면 DESC를 추가해야 한다.', 'GROUP BY의 위치는 SELECT 문법상 이미 올바른 절 순서에 있으므로 옮길 필요가 없다.']}
/>

**주문상세 테이블**

| 주문번호 | 상품코드 |
|----------|----------|
| 1001 | P1 |
| 1001 | P1 |
| 1001 | P2 |
| 1002 | P1 |

<Quiz
  questionNumber={18}
  question="위 주문상세 테이블에 대해 SELECT COUNT(DISTINCT 상품코드) FROM 주문상세 WHERE 주문번호 = 1001; 을 실행한 결과는?"
  options={['1', '2', '3', '4']}
  correctAnswer={2}
  explanation="주문번호가 1001인 행은 P1, P1, P2 세 건이다. DISTINCT로 중복을 제거하면 서로 다른 상품코드는 P1과 P2 두 개뿐이므로 COUNT(DISTINCT 상품코드)는 2다."
  optionExplanations={['서로 다른 상품이 P1과 P2 두 개이므로 1은 너무 적게 센 값이다.', '정답. 중복을 제거한 서로 다른 상품코드는 P1, P2 두 개다.', '중복 제거를 하지 않고 주문번호 1001의 행 수(3건)를 그대로 센 값이다.', '전체 테이블의 행 수를 그대로 센 값으로, 주문번호 조건과 중복 제거를 모두 반영하지 않았다.']}
/>

**학생 테이블(4행)**

| 학번 |
|------|
| S1 |
| S2 |
| S3 |
| S4 |

**수강신청 테이블(학번은 외래키, 6행)**

| 신청번호 | 학번 |
|----------|------|
| 1 | S1 |
| 2 | S1 |
| 3 | S2 |
| 4 | S3 |
| 5 | S3 |
| 6 | S3 |

<Quiz
  questionNumber={19}
  question="학생과 수강신청을 학번으로 INNER JOIN했을 때 결과 행 수는?"
  options={['4', '5', '6', '10']}
  correctAnswer={3}
  explanation="수강신청 테이블의 모든 행이 학생 테이블에 존재하는 학번을 참조하므로, INNER JOIN 결과는 수강신청의 행 수와 같다. 다만 학번 S4는 수강신청이 없어 결과에서 빠지고, 나머지 6건의 수강신청은 각각 정확히 하나의 학생과 매칭되어 총 6행이 나온다."
  optionExplanations={['학생 테이블의 행 수만 그대로 가져온 값으로, 수강신청이 없는 S4가 매칭되지 않는다는 점과 조인 결과가 수강신청 기준이라는 점을 반영하지 않았다.', '수강신청이 있는 서로 다른 학번의 수는 3개(S1, S2, S3)이지만, INNER JOIN의 결과 행 수는 이 값이 아니라 수강신청 테이블의 행 수를 따른다.', '정답. 수강신청 6건 모두 유효한 학번을 참조하므로 INNER JOIN 결과는 6행이다.', '두 테이블의 행 수를 곱한 카티전 곱 값으로, 조인 조건이 있는 INNER JOIN에는 해당하지 않는다.']}
/>

**부서 테이블(3행)**

| 부서ID |
|--------|
| D1 |
| D2 |
| D3 |

**사원 테이블(부서ID는 외래키, D3에 소속된 사원 없음)**

| 사원ID | 부서ID |
|--------|--------|
| E1 | D1 |
| E2 | D1 |
| E3 | D2 |

<Quiz
  questionNumber={20}
  question="부서를 기준으로 LEFT OUTER JOIN 사원을 부서ID로 실행했을 때, D3에 대응하는 결과 행의 사원ID 값과 전체 결과 행 수로 옳은 것은?"
  options={['사원ID는 NULL, 전체 4행', '사원ID는 공백 문자열, 전체 4행', '사원ID는 NULL, 전체 3행', 'D3는 결과에서 아예 제외되어 전체 3행']}
  correctAnswer={1}
  explanation="LEFT OUTER JOIN은 왼쪽 테이블인 부서의 모든 행을 보존한다. D3는 매칭되는 사원이 없으므로 사원 쪽 컬럼이 모두 NULL로 채워진 채 결과에 포함된다. D1은 사원 2명과 매칭되어 2행, D2는 1명과 매칭되어 1행, D3는 NULL과 매칭되어 1행이므로 전체는 2+1+1로 4행이다."
  optionExplanations={['정답. 매칭되는 사원이 없으면 NULL로 채워지고, 부서 D1의 2행과 D2의 1행에 D3의 1행을 더해 전체 4행이 된다.', 'SQL에서 매칭되지 않는 값은 빈 문자열이 아니라 NULL로 채워진다.', 'D3가 결과에 포함된다는 점은 맞지만 전체 행 수 계산에서 D1의 사원이 2명이라는 점을 반영하지 못해 3행으로 잘못 센 오답이다.', 'LEFT OUTER JOIN은 왼쪽 테이블의 행을 모두 보존하므로 매칭되는 사원이 없어도 D3가 제외되지 않고 NULL로 채워져 남는다.']}
/>

<Quiz
  questionNumber={21}
  question="RIGHT OUTER JOIN에 대한 설명으로 가장 적절한 것은?"
  options={['오른쪽 테이블의 행 중 왼쪽과 매칭되는 행만 결과에 남긴다.', '오른쪽 테이블의 모든 행을 보존하고, 왼쪽에 매칭되는 행이 없으면 해당 컬럼을 NULL로 채운다.', '왼쪽과 오른쪽 테이블의 모든 행을 항상 보존한다.', '조인 조건 없이 두 테이블의 모든 조합을 만든다.']}
  correctAnswer={2}
  explanation="RIGHT OUTER JOIN은 오른쪽(우측) 테이블의 모든 행을 결과에 포함시키고, 왼쪽 테이블에 매칭되는 행이 없으면 왼쪽 컬럼들을 NULL로 채운다."
  optionExplanations={['매칭되는 행만 남기는 것은 INNER JOIN의 동작이다.', '정답. 오른쪽 테이블 전체를 보존하는 것이 RIGHT OUTER JOIN의 정의다.', '양쪽 모두를 보존하는 것은 FULL OUTER JOIN의 동작이다.', '조인 조건 없이 모든 조합을 만드는 것은 CROSS JOIN의 동작이다.']}
/>

**A 테이블**

| 값 |
|----|
| 100 |
| 250 |
| 400 |

**구간 테이블**

| 등급 | 하한 | 상한 |
|------|------|------|
| 상 | 300 | 500 |
| 중 | 100 | 299 |
| 하 | 0 | 99 |

<Quiz
  questionNumber={22}
  question="A와 구간 테이블에 대해 다음 세 쿼리를 실행한 결과 행 수의 총합은? (ㄱ) A CROSS JOIN 구간, (ㄴ) A INNER JOIN 구간 ON A.값이 하한과 상한 사이, (ㄷ) A FULL OUTER JOIN 구간 ON A.값이 하한과 상한 사이"
  options={['13', '14', '15', '16']}
  correctAnswer={4}
  explanation="(ㄱ) CROSS JOIN은 3행 곱하기 3행으로 9행이다. (ㄴ) INNER JOIN은 100과 250이 중(100~299)에, 400이 상(300~500)에 매칭되어 3행이다. (ㄷ) FULL OUTER JOIN은 매칭된 3행에 더해, A의 값 중에는 매칭되지 않은 것이 없지만 구간 쪽에서는 하(0~99)에 매칭되는 A 값이 하나도 없어 미매칭 행 1개가 추가된다. 따라서 9+3+4는 16이다."
  optionExplanations={['어느 한 항목의 계산이 어긋난 값으로, 특히 FULL OUTER JOIN의 미매칭 행을 과소 계산한 경우다.', 'FULL OUTER JOIN에서 하 등급의 미매칭 행 1개를 아예 반영하지 않아 3행으로 계산한 값이다.', 'FULL OUTER JOIN을 매칭된 3행만으로 계산해 하 등급의 미매칭 행 1개를 빠뜨린 값이다.', '정답. CROSS 9행, INNER 3행, FULL OUTER 4행(매칭 3행에 미매칭 하 등급 1행 추가)을 더한 16행이다.']}
/>

<Quiz
  questionNumber={23}
  question="NATURAL JOIN과 USING 절에 대한 설명으로 옳지 않은 것은?"
  options={['NATURAL JOIN은 두 테이블에서 이름이 같은 모든 컬럼을 자동으로 조인 조건에 사용한다.', 'USING 절은 지정한 컬럼만 조인 조건으로 사용하며 나머지 동일 이름 컬럼은 무시한다.', 'NATURAL JOIN과 ON 절은 함께 사용할 수 없으며 함께 쓰면 오류가 발생한다.', 'NATURAL JOIN 결과에서는 조인에 사용된 동일 이름 컬럼이 양쪽에서 각각 중복 출력된다.']}
  correctAnswer={4}
  explanation="NATURAL JOIN은 이름이 같은 컬럼을 조인 조건으로 자동 사용하면서, 결과에서는 그 컬럼을 하나로 합쳐 한 번만 출력한다. 양쪽에서 중복으로 출력된다는 설명은 틀렸다."
  optionExplanations={['NATURAL JOIN이 동일 이름 컬럼을 자동으로 조인 조건에 사용한다는 설명은 정확하다.', 'USING이 지정한 컬럼만 조인에 사용한다는 설명은 정확하다.', 'NATURAL JOIN과 ON을 함께 쓰면 문법이 충돌해 오류가 난다는 설명은 정확하다.', '정답. NATURAL JOIN은 조인에 사용된 동일 이름 컬럼을 결과에서 하나로 합쳐 출력하며 중복 출력하지 않는다.']}
/>

<Quiz
  questionNumber={24}
  question="사원 테이블은 사번과 상사사번을 가진다. 어떤 사원의 이름과 그 상사, 그리고 그 상사의 상사(차상위 관리자)까지 세 단계를 한 행에 출력하려고 한다. 별칭을 E(사원), M(상사), G(차상위 관리자)로 둘 때 옳은 조인 조건은?"
  options={['E.사번 = M.사번, M.사번 = G.사번', 'E.상사사번 = M.사번, M.상사사번 = G.사번', 'E.사번 = M.상사사번, M.사번 = G.상사사번', 'E.상사사번 = M.상사사번, M.상사사번 = G.상사사번']}
  correctAnswer={2}
  explanation="사원(E)의 상사사번이 상사(M)의 사번과 같아야 그 상사를 찾을 수 있고, 다시 상사(M)의 상사사번이 차상위 관리자(G)의 사번과 같아야 차상위 관리자를 찾을 수 있다. 이 두 조건을 이어 붙이면 3단계 셀프 조인이 완성된다."
  optionExplanations={['사번끼리 매칭하면 각자 자기 자신이 매칭되어 상사 정보를 전혀 얻을 수 없다.', '정답. 상사사번과 사번을 순서대로 이어 붙이는 조건이 3단계 관리자 체인을 정확히 만든다.', '이 조건은 반대로 부하 직원을 찾는 방향이 되어 상사를 찾는 의도와 어긋난다.', '상사사번끼리만 비교하면 같은 상사를 둔 사람들이 묶일 뿐 실제 상사·차상위 관리자 체인이 만들어지지 않는다.']}
/>

<Quiz
  questionNumber={25}
  question="테이블 색상(4행)과 사이즈(3행)에 대해 CROSS JOIN을 실행하면 몇 개의 조합이 만들어지는가?"
  options={['7', '10', '12', '81']}
  correctAnswer={3}
  explanation="CROSS JOIN은 조인 조건 없이 두 테이블의 모든 조합을 만드는 카티전 곱이므로, 결과 행 수는 두 테이블 행 수의 곱인 4 곱하기 3, 즉 12다."
  optionExplanations={['두 테이블의 행 수를 곱하지 않고 더한 값이다.', '곱셈이 아니라 다른 계산으로 잘못 나온 값이다.', '정답. 4×3으로 12개의 조합이 만들어진다.', '4의 3제곱처럼 잘못된 지수 계산을 한 값이다.']}
/>

<Quiz
  questionNumber={26}
  question="다음 오라클 전용 표기(+)로 작성된 조인과 동일한 결과를 내는 ANSI 표준 조인은? 쿼리: SELECT * FROM 사원 E, 부서 D WHERE E.부서번호(+) = D.부서번호;"
  options={['사원 LEFT OUTER JOIN 부서 ON 사원.부서번호 = 부서.부서번호', '사원 RIGHT OUTER JOIN 부서 ON 사원.부서번호 = 부서.부서번호', '사원 INNER JOIN 부서 ON 사원.부서번호 = 부서.부서번호', '사원 FULL OUTER JOIN 부서 ON 사원.부서번호 = 부서.부서번호']}
  correctAnswer={2}
  explanation="오라클의 (+) 표기는 부족한(NULL로 채워질) 쪽에 붙는다. 여기서는 사원 쪽에 (+)가 붙어 있으므로 사원이 없어도 부서는 모두 보존된다. 즉 부서가 기준으로 남는 RIGHT OUTER JOIN과 같다."
  optionExplanations={['LEFT OUTER JOIN은 사원을 모두 보존하는 조인이라 (+)가 부서 쪽에 붙은 경우와 같아 방향이 반대다.', '정답. (+)가 사원 쪽에 붙어 있으므로 부서를 모두 보존하는 RIGHT OUTER JOIN과 동일하다.', 'INNER JOIN은 양쪽 모두 매칭되는 행만 남겨 (+) 표기의 의미(한쪽을 NULL로 채워 보존)를 반영하지 못한다.', 'FULL OUTER JOIN은 양쪽을 모두 보존해 (+)가 한쪽에만 붙은 경우와 결과가 다르다.']}
/>

<Quiz
  questionNumber={27}
  question="단일행 서브쿼리와 다중행 서브쿼리에 대한 설명으로 옳지 않은 것은?"
  options={['단일행 서브쿼리는 등호나 부등호 같은 단일행 비교 연산자와 함께 쓸 수 있다.', '다중행 서브쿼리는 IN, ANY, ALL 같은 다중행 비교 연산자와 함께 사용해야 한다.', '서브쿼리의 반환 건수는 서브쿼리 자체가 아니라 메인쿼리의 WHERE 절 형태에 의해 결정된다.', '서브쿼리가 실제로 두 건 이상을 반환했는데 등호를 사용하면 오류가 발생한다.']}
  correctAnswer={3}
  explanation="서브쿼리가 몇 건을 반환하는지는 서브쿼리 자체의 조건(WHERE, GROUP BY 등)에 의해 결정되는 것이지, 메인쿼리의 WHERE 절 형태가 서브쿼리의 반환 건수를 바꾸지 않는다."
  optionExplanations={['단일행 서브쿼리가 단일행 연산자와 쓰인다는 설명은 정확하다.', '다중행 서브쿼리가 다중행 연산자와 쓰인다는 설명은 정확하다.', '정답. 반환 건수는 서브쿼리 자체의 조건으로 결정되며 메인쿼리의 WHERE 절 형태와는 무관하다.', '다중행 결과에 등호를 쓰면 단일 행 하위 질의 오류가 발생한다는 설명은 정확하다.']}
/>

<Quiz
  questionNumber={28}
  question="서브쿼리의 결과로 여러 개의 컬럼이 함께 반환되고, 이 값들이 메인쿼리의 조건절에서 동시에 짝을 이루어 비교되는 서브쿼리의 유형은 무엇인가?"
  options={['단일행 서브쿼리', '다중행 서브쿼리', '다중컬럼 서브쿼리', '스칼라 서브쿼리']}
  correctAnswer={3}
  explanation="여러 컬럼을 하나의 튜플처럼 묶어 비교하는 서브쿼리를 다중컬럼 서브쿼리라 한다. 예를 들어 (사번, 부서번호)를 함께 비교하는 형태가 이에 해당한다."
  optionExplanations={['단일행 서브쿼리는 반환 행 수가 하나인 경우를 가리키는 분류로 컬럼 개수와는 다른 기준이다.', '다중행 서브쿼리는 반환 행 수가 여러 개인 경우를 가리키는 분류로 컬럼 개수 기준이 아니다.', '정답. 여러 컬럼을 튜플 단위로 묶어 비교하는 서브쿼리가 다중컬럼 서브쿼리다.', '스칼라 서브쿼리는 하나의 행과 하나의 컬럼(단일 값)만 반환하는 서브쿼리다.']}
/>

**회원 테이블**

| 회원ID | 등급 |
|--------|------|
| M1 | VIP |
| M2 | 일반 |
| M3 | VIP |

**주문 테이블(회원ID는 외래키)**

| 주문번호 | 회원ID |
|----------|--------|
| O1 | M1 |
| O2 | M1 |
| O3 | M3 |

<Quiz
  questionNumber={29}
  question="위 두 테이블에 대해 다음 SQL을 실행했을 때 결과로 옳은 것은? 쿼리: SELECT 회원ID FROM 회원 A WHERE EXISTS (SELECT 1 FROM 주문 B WHERE B.회원ID = A.회원ID);"
  options={['M1', 'M1, M2', 'M1, M3', 'M1, M2, M3']}
  correctAnswer={3}
  explanation="EXISTS는 상관 서브쿼리가 조건을 만족하는 행을 하나라도 찾으면 참이 된다. M1은 주문이 2건 있어 참, M3는 주문이 1건 있어 참, M2는 주문이 하나도 없어 거짓이다. 따라서 결과는 M1과 M3다."
  optionExplanations={['M3에게도 주문이 있다는 사실을 빠뜨린 오답이다.', 'M2는 주문이 없는데도 포함시킨 오답으로, EXISTS 조건을 반대로 해석했다.', '정답. 주문이 있는 회원은 M1과 M3이며, 주문이 없는 M2는 제외된다.', 'M2는 주문 기록이 전혀 없어 EXISTS 조건을 만족하지 못하므로 결과에 포함되면 안 된다.']}
/>

**사원 테이블**

| 사번 | 부서 |
|------|------|
| 1 | 개발 |
| 2 | NULL |

**휴직 테이블(휴직 중인 사번 목록)**

| 사번 |
|------|
| 2 |
| NULL |

<Quiz
  questionNumber={30}
  question="위 두 테이블에 대해 SELECT 사번 FROM 사원 WHERE 사번 NOT IN (SELECT 사번 FROM 휴직); 을 실행한 결과로 옳은 것은?"
  options={['사번 1만 조회된다.', '사번 1과 2가 조회된다.', '한 건도 조회되지 않는다(공집합).', '오류가 발생한다.']}
  correctAnswer={3}
  explanation="휴직 서브쿼리의 결과 집합에 NULL이 포함되어 있다. NOT IN은 모든 값과 다르다는 조건을 AND로 연결하는데, NULL과의 비교는 항상 UNKNOWN이 되어 전체 AND 결과가 참이 될 수 없다. 그 결과 어떤 사원도 조건을 통과하지 못해 공집합이 된다."
  optionExplanations={['NULL 함정이 없다면 나올 수 있는 결과이지만, 서브쿼리에 NULL이 섞여 있어 실제로는 이렇게 나오지 않는다.', '서브쿼리의 NULL이 결과에 영향을 주지 않는다고 잘못 가정한 오답이다.', '정답. 서브쿼리 결과에 NULL이 있으면 NOT IN 비교가 모두 UNKNOWN이 되어 어떤 행도 반환되지 않는다.', 'NOT IN은 문법적으로 정상 실행되며, NULL이 있다고 오류가 나는 것이 아니라 결과가 조용히 공집합이 되는 것이 함정이다.']}
/>

**사원 테이블**

| 사번 | 부서 | 급여 |
|------|------|------|
| 1 | 영업 | 3000 |
| 2 | 영업 | 5000 |
| 3 | 개발 | 4500 |
| 4 | 개발 | 4000 |

<Quiz
  questionNumber={31}
  question="다음 SQL을 실행했을 때 결과로 옳은 것은? 쿼리: SELECT Y.사번, Y.부서, Y.급여 FROM (SELECT 사번, MAX(급여) OVER(PARTITION BY 부서) AS 최고급여 FROM 사원) X, 사원 Y WHERE X.사번 = Y.사번 AND X.최고급여 = Y.급여;"
  options={['사번 2(영업, 5000), 사번 3(개발, 4500)', '사번 1(영업, 3000), 사번 4(개발, 4000)', '사번 2(영업, 5000)만 조회된다.', '전체 4명이 모두 조회된다.']}
  correctAnswer={1}
  explanation="인라인 뷰 X는 각 사원 행에 자신이 속한 부서의 최고 급여를 윈도우 함수로 붙인다. 영업 부서의 최고 급여는 5000(사번 2), 개발 부서의 최고 급여는 4500(사번 3)이다. 바깥 쿼리에서 실제 급여가 그 부서 최고 급여와 같은 행만 남기면 사번 2와 사번 3만 남는다."
  optionExplanations={['정답. 영업의 최고 급여는 5000(사번 2), 개발의 최고 급여는 4500(사번 3)이므로 이 둘만 조건을 만족한다.', '각 부서의 최저 급여를 가진 사원을 고른 결과로, MAX가 아니라 MIN을 사용했을 때 나올 결과와 혼동한 오답이다.', '개발 부서에서 최고 급여인 사번 3을 빠뜨린 오답이다.', '급여가 부서 최고 급여와 같은 행만 남기는 조건을 무시하고 전체 사원을 그대로 반환한다고 본 오답이다.']}
/>

<Quiz
  questionNumber={32}
  question="스칼라 서브쿼리의 반환 건수와 관련된 설명으로 옳은 것은?"
  options={['스칼라 서브쿼리는 반드시 하나 이상의 행을 반환해야 하며 0건이면 오류가 발생한다.', '스칼라 서브쿼리가 0건을 반환하면 NULL로 처리된다.', '스칼라 서브쿼리가 2건 이상을 반환해도 첫 번째 행만 자동으로 사용된다.', '스칼라 서브쿼리는 항상 정확히 한 건을 반환하도록 옵티마이저가 자동으로 보장한다.']}
  correctAnswer={2}
  explanation="스칼라 서브쿼리는 결과가 0건이면 오류 없이 NULL로 처리된다. 오류가 발생하는 경우는 오직 2건 이상을 반환할 때뿐이다."
  optionExplanations={['0건이어도 오류가 아니라 NULL로 처리되므로 이 설명은 틀렸다.', '정답. 0건일 때는 조용히 NULL이 반환된다.', '2건 이상 반환되면 첫 번째 행이 자동으로 선택되는 것이 아니라 단일 행 하위 질의 오류가 발생한다.', '옵티마이저는 실제 행 수를 보장하지 않으며, 개발자가 서브쿼리 조건을 정확히 설계해 한 건만 나오도록 해야 한다.']}
/>

**A 테이블**

| 값 |
|----|
| 1 |
| 1 |
| 2 |
| 3 |

**B 테이블**

| 값 |
|----|
| 2 |
| 3 |
| 3 |
| 4 |

<Quiz
  questionNumber={33}
  question="A와 B 테이블에 대해 다음 SQL의 결과 행 수를 순서대로 나열한 것은? (ㄱ) SELECT 값 FROM A UNION SELECT 값 FROM B, (ㄴ) SELECT 값 FROM A UNION ALL SELECT 값 FROM B"
  options={['(ㄱ) 4, (ㄴ) 8', '(ㄱ) 5, (ㄴ) 8', '(ㄱ) 4, (ㄴ) 6', '(ㄱ) 8, (ㄴ) 4']}
  correctAnswer={1}
  explanation="A는 1, 1, 2, 3(4행) B는 2, 3, 3, 4(4행)다. UNION은 중복을 제거한 서로 다른 값만 남기므로 1, 2, 3, 4 네 개가 남아 4행이다. UNION ALL은 중복 제거 없이 두 결과를 그대로 이어 붙이므로 4행과 4행을 더한 8행이 된다."
  optionExplanations={['정답. UNION은 서로 다른 값 4개(1,2,3,4)만 남고, UNION ALL은 8행 그대로 합쳐진다.', 'UNION 결과를 5행으로 잘못 계산한 값으로, 중복 제거 후 남는 값이 4개뿐이라는 점을 놓쳤다.', 'UNION ALL을 8행이 아니라 중복 제거가 일부 반영된 것처럼 6행으로 잘못 계산한 값이다.', 'UNION과 UNION ALL의 결과를 서로 바꿔 쓴 오답이다.']}
/>

**재고보유 테이블**

| 상품코드 |
|----------|
| P1 |
| P2 |
| P3 |

**주문이력 테이블**

| 상품코드 |
|----------|
| P2 |
| P3 |
| P4 |

<Quiz
  questionNumber={34}
  question="재고보유와 주문이력 테이블에 대해 SELECT 상품코드 FROM 재고보유 INTERSECT SELECT 상품코드 FROM 주문이력; 을 실행한 결과로 옳은 것은?"
  options={['P1', 'P2, P3', 'P1, P2, P3, P4', 'P4']}
  correctAnswer={2}
  explanation="INTERSECT는 두 결과 집합에 공통으로 존재하는 값만 반환하는 교집합 연산이다. 재고보유(P1, P2, P3)와 주문이력(P2, P3, P4)에 공통으로 있는 값은 P2와 P3다."
  optionExplanations={['P1은 재고보유에만 있고 주문이력에는 없어 교집합에 포함되지 않는다.', '정답. 양쪽 모두에 존재하는 P2와 P3가 교집합이다.', '이는 두 집합의 합집합(UNION)에 해당하는 결과이지 교집합이 아니다.', 'P4는 주문이력에만 있고 재고보유에는 없어 교집합에 포함되지 않는다.']}
/>

<Quiz
  questionNumber={35}
  question="같은 재고보유와 주문이력 테이블에 대해 SELECT 상품코드 FROM 재고보유 MINUS SELECT 상품코드 FROM 주문이력; 을 실행한 결과로 옳은 것은?"
  options={['P1', 'P4', 'P1, P4', '한 건도 없다']}
  correctAnswer={1}
  explanation="MINUS(EXCEPT)는 앞 결과에서 뒤 결과에 있는 값을 뺀 차집합이다. 재고보유(P1, P2, P3)에서 주문이력(P2, P3, P4)에 있는 값을 빼면 P2와 P3가 제거되고 P1만 남는다."
  optionExplanations={['정답. 재고보유에만 있고 주문이력에는 없는 값은 P1뿐이다.', 'P4는 주문이력에만 있는 값이라 재고보유 기준의 MINUS 결과에는 나타나지 않는다.', 'P4를 포함시킨 것은 반대 방향(주문이력 MINUS 재고보유)의 결과와 혼동한 오답이다.', '재고보유에 있고 주문이력에 없는 값인 P1이 분명히 존재하므로 공집합이 아니다.']}
/>

<Quiz
  questionNumber={36}
  question="집합 연산자 사용 조건에 대한 설명으로 옳지 않은 것은?"
  options={['집합 연산에 참여하는 각 SELECT 문의 컬럼 개수가 서로 같아야 한다.', '대응되는 컬럼끼리는 서로 호환되는 데이터 타입이어야 한다.', '집합 연산에 참여하는 개별 SELECT 문에는 각각 별도의 ORDER BY를 자유롭게 붙일 수 있다.', 'UNION, INTERSECT, MINUS는 기본적으로 중복 제거 과정에서 정렬이 수반될 수 있다.']}
  correctAnswer={3}
  explanation="집합 연산에서 ORDER BY는 전체 결과의 맨 마지막에 한 번만 붙일 수 있으며, 중간의 개별 SELECT 문마다 각각 ORDER BY를 붙이는 것은 허용되지 않는다."
  optionExplanations={['컬럼 개수 일치는 집합 연산의 필수 조건이므로 옳은 설명이다.', '데이터 타입이 호환되어야 한다는 것은 옳은 설명이다.', '정답. ORDER BY는 전체 결과의 맨 끝에 한 번만 쓸 수 있으며 개별 SELECT 문마다 붙일 수 없다.', 'UNION 계열 연산이 중복 제거를 위해 내부적으로 정렬을 수반할 수 있다는 설명은 옳다.']}
/>

**A 테이블(3행)**

| 값 |
|----|
| 1 |
| 2 |
| 3 |

**B 테이블(2행)**

| 값 |
|----|
| 2 |
| 4 |

**C 테이블(2행)**

| 값 |
|----|
| 2 |
| 5 |

<Quiz
  questionNumber={37}
  question="다음 SQL의 실행 결과 행 수는? 쿼리: SELECT 값 FROM A MINUS SELECT 값 FROM B UNION ALL SELECT 값 FROM C;"
  options={['3', '4', '5', '6']}
  correctAnswer={2}
  explanation="MINUS와 UNION ALL이 함께 쓰이면 왼쪽부터 순서대로 계산된다. 먼저 A MINUS B를 계산하면 A(1,2,3)에서 B(2,4)에 있는 2를 빼 1과 3이 남는다(2행). 이 결과에 UNION ALL로 C(2,5)를 그대로 이어 붙이면 1, 3, 2, 5 총 4행이 된다."
  optionExplanations={['MINUS 결과가 2행이라는 것은 맞지만 UNION ALL로 이어 붙일 때 C의 2행을 더하지 않은 값이다.', '정답. A MINUS B의 결과 2행에 C의 2행을 UNION ALL로 더해 4행이 된다.', 'MINUS를 잘못 계산해 3행이 그대로 남는다고 본 뒤 C의 일부만 더한 값이다.', 'UNION ALL을 UNION처럼 중복 제거한다고 착각하거나 계산 순서를 다르게 적용한 값이다.']}
/>

**시험 테이블**

| 이름 | 점수 |
|------|------|
| 가영 | 95 |
| 나영 | 95 |
| 다영 | 88 |

<Quiz
  questionNumber={38}
  question="점수를 내림차순 정렬해 RANK와 ROW_NUMBER를 각각 계산할 때, 두 번째로 정렬된 행(나영)의 RANK 값과 ROW_NUMBER 값을 순서대로 나열한 것은?"
  options={['1, 2', '2, 2', '1, 1', '2, 1']}
  correctAnswer={1}
  explanation="가영과 나영이 95점으로 동점이라 RANK는 둘 다 1위를 받는다. 반면 ROW_NUMBER는 동점이어도 정렬된 순서에 따라 유일한 번호를 매기므로 나영은 2번이 된다."
  optionExplanations={['정답. 동점인 두 행 모두 RANK는 1이지만, ROW_NUMBER는 순서대로 유일한 번호인 2를 받는다.', 'RANK가 동점을 무시하고 순서대로 2를 받는다고 본 오답이다.', 'ROW_NUMBER가 동점 처리를 한다고 착각해 RANK와 같은 값을 준 오답이다.', 'RANK와 ROW_NUMBER의 값을 서로 바꿔 쓴 오답이다.']}
/>

**주문 테이블(주문일자 오름차순 정렬됨)**

| 주문일자 | 부서 | 금액 |
|----------|------|------|
| 1월 3일 | 영업 | 1000 |
| 1월 8일 | 영업 | 3000 |
| 1월 15일 | 개발 | 2000 |

<Quiz
  questionNumber={39}
  question="PARTITION BY 부서 ORDER BY 주문일자로 LEAD(금액)을 계산할 때, 영업 부서의 1월 3일 행에서 LEAD(금액) 값은?"
  options={['1000', '3000', '2000', 'NULL']}
  correctAnswer={2}
  explanation="LEAD는 같은 파티션(부서) 안에서 정렬 순서상 다음 행의 값을 가져온다. 영업 부서 안에서 1월 3일 다음 행은 1월 8일(금액 3000)이므로 LEAD 값은 3000이다."
  optionExplanations={['자기 자신의 값을 그대로 가져왔다고 착각한 오답이다.', '정답. 같은 영업 부서 안에서 다음 행인 1월 8일의 금액이 3000이다.', '개발 부서는 다른 파티션이라 영업 부서의 LEAD 계산에 영향을 주지 않는다.', 'PARTITION BY로 부서를 나누었으므로 영업 부서 안에는 다음 행이 분명히 존재해 NULL이 아니다.']}
/>

<Quiz
  questionNumber={40}
  question="지역이 3개이고 각 지역마다 상품군이 2개씩 있어 (지역, 상품군) 조합이 총 6가지일 때, CUBE(지역, 상품군)를 적용한 결과의 총 행 수는?"
  options={['6', '9', '11', '12']}
  correctAnswer={4}
  explanation="CUBE(지역, 상품군)는 가능한 모든 조합의 소계를 만든다. (지역, 상품군) 상세 6행, 지역별 소계 3행(지역이 3개이므로), 상품군별 소계 2행(상품군이 2개이므로), 전체 합계 1행을 모두 더하면 6+3+2+1로 12행이다."
  optionExplanations={['상세 집계 6행만 계산하고 나머지 소계와 총계를 모두 빠뜨린 값이다.', '지역별 소계와 전체 합계만 더하고 상품군별 소계를 빠뜨린 값이다(ROLLUP 결과에 해당).', '상품군별 소계 일부를 빠뜨리는 등 조합 하나를 놓친 값이다.', '정답. 상세 6행, 지역별 소계 3행, 상품군별 소계 2행, 총계 1행을 모두 더한 12행이다.']}
/>

<Quiz
  questionNumber={41}
  question="FETCH FIRST n ROWS ONLY 구문에 대한 설명으로 옳은 것은?"
  options={['오라클 12c 이상에서 지원하는 표준 SQL 방식의 Top N 추출 구문이다.', 'ROWNUM과 달리 정렬 이전에 먼저 적용되어 정렬 결과와 무관한 임의의 N개를 가져온다.', 'OFFSET 절과 함께 사용할 수 없다.', 'FETCH FIRST는 정확히 1개의 행만 가져올 때만 사용할 수 있다.']}
  correctAnswer={1}
  explanation="FETCH FIRST n ROWS ONLY는 표준 SQL의 행 제한 구문으로 오라클 12c 이상에서 지원하며, ORDER BY로 정렬을 마친 뒤의 결과에서 상위 N개를 정확히 가져온다."
  optionExplanations={['정답. 오라클 12c 이상에서 지원하는 표준 SQL 방식의 Top N 구문이라는 설명이 정확하다.', 'FETCH FIRST는 ORDER BY로 정렬된 이후의 결과에 적용되므로 ROWNUM과 달리 정렬 결과를 정확히 반영한다.', 'OFFSET n ROWS와 함께 사용해 특정 구간의 행을 건너뛰고 가져올 수 있다.', 'n에 원하는 개수를 자유롭게 지정할 수 있으며 1개로 제한되지 않는다.']}
/>

<Quiz
  questionNumber={42}
  question="사원 테이블(사번, 관리자사번)에서 관리자사번이 없는 사람을 최상위로 하여 하위 방향으로 조직도를 전개하려고 한다. 빈칸에 들어갈 조건으로 옳은 것은? 쿼리: START WITH 관리자사번 IS NULL CONNECT BY 빈칸"
  options={['PRIOR 사번 = 관리자사번', 'PRIOR 관리자사번 = 사번', '사번 = 관리자사번', 'PRIOR 사번 = PRIOR 관리자사번']}
  correctAnswer={1}
  explanation="상위에서 하위로 전개하려면 부모 행의 사번이 자식 행의 관리자사번과 같아야 하므로, PRIOR를 부모 쪽 컬럼인 사번에 붙여야 한다."
  optionExplanations={['정답. 부모의 사번이 자식의 관리자사번과 연결되어 상위에서 하위로 정상 전개된다.', 'PRIOR의 위치가 반대라 하위에서 상위로 거슬러 올라가는 역방향 전개가 된다.', 'PRIOR가 없어 부모-자식 관계가 정의되지 않아 계층이 전개되지 않는다.', '양쪽 모두에 PRIOR를 붙이는 것은 의미가 성립하지 않는 잘못된 조건이다.']}
/>

<Quiz
  questionNumber={43}
  question="PIVOT 구문에 대한 설명으로 옳지 않은 것은?"
  options={['PIVOT은 행에 나열된 값을 열로 펼쳐 교차표 형태를 만드는 구문이다.', 'PIVOT 절에는 집계함수와 회전 기준이 되는 컬럼을 함께 지정해야 한다.', 'IN 절에 나열하지 않은 값도 결과 열에 자동으로 나타난다.', 'UNPIVOT은 PIVOT과 반대로 열을 행으로 되돌리는 구문이다.']}
  correctAnswer={3}
  explanation="PIVOT의 IN 절에 나열한 값만 결과 열이 되며, 나열하지 않은 값은 결과에 나타나지 않는다. 자동으로 모든 값이 열이 되지는 않는다."
  optionExplanations={['PIVOT이 행 값을 열로 펼치는 구문이라는 설명은 정확하다.', 'PIVOT 절에 집계함수와 회전 기준 컬럼을 지정해야 한다는 설명은 정확하다.', '정답. IN 절에 명시한 값만 열로 만들어지며, 나열하지 않은 값은 결과에서 빠진다.', 'UNPIVOT이 PIVOT의 반대 방향(열을 행으로) 연산이라는 설명은 정확하다.']}
/>

<Quiz
  questionNumber={44}
  question="다음 GRANT, REVOKE 명령이 순서대로 실행되었다. 순서: 1. DBA가 R1에게 테이블 T에 대한 SELECT 권한을 재위임 가능하도록 부여, 2. R1이 R2에게 SELECT 권한을 부여, 3. R2가 R3에게 SELECT 권한을 부여, 4. DBA가 R1의 SELECT 권한을 회수. 이후 테이블 T에 대해 SELECT 권한을 가진 사용자로 가장 적절한 것은? (참고: 오라클에서 REVOKE는 그 사용자가 재위임한 권한까지 연쇄적으로 회수한다.)"
  options={['DBA만', 'DBA, R1', 'DBA, R2, R3', 'DBA, R1, R2, R3']}
  correctAnswer={1}
  explanation="오라클에서 WITH GRANT OPTION으로 위임된 권한을 회수하면, 그 사용자가 재위임한 권한까지 연쇄적으로 함께 회수된다. R1의 SELECT를 회수하면 R1이 R2에게 준 권한, R2가 R3에게 준 권한까지 모두 연쇄적으로 사라져 최종적으로 SELECT 권한은 소유자인 DBA만 가진다."
  optionExplanations={['정답. 연쇄 회수(CASCADE REVOKE) 때문에 R1, R2, R3의 권한이 모두 사라지고 소유자인 DBA만 남는다.', 'R1은 이미 회수 대상이라 권한을 잃으므로 DBA와 함께 남는다고 볼 수 없다.', 'R1의 권한이 회수되면 그로부터 파생된 R2, R3의 권한도 함께 사라진다는 연쇄 회수 규칙을 놓친 오답이다.', 'REVOKE가 연쇄적으로 하위 위임까지 회수한다는 점을 전혀 반영하지 않은 오답이다.']}
/>

**주문 테이블**

| 주문번호 | 고객ID |
|----------|--------|
| 1 | C1 |
| 2 | C2 |
| 3 | C3 |

**배송완료 테이블**

| 고객ID |
|--------|
| C1 |
| C3 |

<Quiz
  questionNumber={45}
  question="다음 두 방식이 항상 같은 결과를 내는지에 대한 설명으로 가장 적절한 것은? 방식1: SELECT 고객ID FROM 주문 A WHERE EXISTS (SELECT 1 FROM 배송완료 B WHERE B.고객ID = A.고객ID). 방식2: SELECT A.고객ID FROM 주문 A INNER JOIN 배송완료 B ON A.고객ID = B.고객ID."
  options={['배송완료 쪽에 같은 고객ID가 중복되어 있어도 두 방식은 항상 같은 결과를 낸다.', '배송완료 쪽에 같은 고객ID가 중복되어 있으면 INNER JOIN 방식은 행이 중복될 수 있지만 EXISTS 방식은 중복되지 않는다.', 'EXISTS 방식은 주문 테이블에 없는 고객까지 결과에 포함시킨다.', 'INNER JOIN 방식은 배송완료에 없는 고객ID도 결과에 포함시킨다.']}
  correctAnswer={2}
  explanation="EXISTS는 조건을 만족하는 행이 존재하는지 여부만 확인하고 값을 가져오지 않으므로, 배송완료 쪽에 같은 고객ID가 여러 번 있어도 주문 쪽 행 하나당 결과는 한 번만 나온다. 반면 INNER JOIN은 실제로 값을 결합하므로 배송완료 쪽에 중복된 고객ID가 있으면 그 수만큼 조인 결과가 늘어나 중복 행이 생길 수 있다."
  optionExplanations={['배송완료 쪽에 중복이 있으면 INNER JOIN 결과는 늘어나지만 EXISTS 결과는 그대로라 두 방식이 달라질 수 있다.', '정답. EXISTS는 존재 여부만 판단해 중복의 영향을 받지 않지만, INNER JOIN은 값을 결합하므로 중복만큼 행이 늘어난다.', 'EXISTS의 조건은 주문 테이블 각 행에 대해 서브쿼리를 평가하는 것이라, 배송완료에 대응이 없는 고객(C2)은 오히려 결과에서 제외된다.', 'INNER JOIN은 양쪽 모두에 값이 있는 경우만 결합하므로 배송완료에 없는 고객ID는 결과에 포함되지 않는다.']}
/>

<Quiz
  questionNumber={46}
  question="NOT IN을 NOT EXISTS로 안전하게 대체할 수 있는 조건으로 가장 적절한 것은?"
  options={['서브쿼리 결과와 비교 대상 컬럼 모두에 NULL이 전혀 없을 때만 안전하게 대체할 수 있다.', 'NULL이 섞여 있어도 NOT IN과 NOT EXISTS는 항상 같은 결과를 낸다.', 'NOT IN은 서브쿼리 결과가 1건일 때만 NOT EXISTS로 바꿀 수 있다.', '비교 대상 컬럼에 NULL이 있어도 서브쿼리 결과에만 NULL이 없으면 항상 안전하다.']}
  correctAnswer={1}
  explanation="NOT IN은 서브쿼리 결과 집합에 NULL이 하나라도 있으면 전체 결과가 공집합이 되어버리는 함정이 있다. 서브쿼리 결과와 비교 대상 컬럼 양쪽 모두에 NULL이 없다는 것이 보장될 때만 NOT IN을 NOT EXISTS로 바꿔도 안전하게 같은 결과를 얻을 수 있다."
  optionExplanations={['정답. 양쪽 모두 NULL이 없어야 두 방식의 결과가 일치한다는 것이 정확한 조건이다.', 'NULL이 섞이면 NOT IN은 공집합이 되지만 NOT EXISTS는 정상적으로 값을 반환해 결과가 달라진다.', '서브쿼리의 반환 건수는 NOT IN과 NOT EXISTS의 동등성 여부와 직접적인 관련이 없다.', '비교 대상 컬럼에 NULL이 있으면 그 행 자체가 NOT IN 비교에서 UNKNOWN이 될 수 있어 안전하다고 단정할 수 없다.']}
/>

**사원 테이블(3행)**

| 사번 | 부서ID |
|------|--------|
| 1 | 10 |
| 2 | 20 |
| 3 | 20 |

**부서 테이블(2행)**

| 부서ID |
|--------|
| 10 |
| 20 |

<Quiz
  questionNumber={47}
  question="다음 SQL의 실행 결과 행 수는? 쿼리: SELECT * FROM 사원, 부서 WHERE 사원.부서ID = 부서.부서ID OR 1=1;"
  options={['3', '5', '6', '오류가 발생한다']}
  correctAnswer={3}
  explanation="OR 1=1은 항상 참인 조건이라 WHERE 절 전체가 항상 참이 되어, 사실상 조인 조건이 없는 것과 같아진다. 그 결과 사원 3행과 부서 2행의 모든 조합인 카티전 곱, 즉 3 곱하기 2로 6행이 나온다."
  optionExplanations={['사원 테이블의 행 수만 그대로 가져온 값으로, OR 1=1이 조건을 무력화한다는 점을 반영하지 못했다.', '정상적인 조인 조건(부서ID가 같음)만 적용됐을 때 나오는 3행에 부서 하나를 더한 값으로, OR 1=1의 효과를 절반만 반영한 오답이다.', '정답. OR 1=1이 조건을 항상 참으로 만들어 사실상 카티전 곱이 되어 3×2인 6행이 나온다.', 'OR 1=1은 문법적으로 정상적인 조건식이라 오류가 발생하지 않는다.']}
/>

**사원 테이블**

| 사번 | 급여 |
|------|------|
| 1 | 3000 |
| 2 | 4500 |
| 3 | 5000 |

<Quiz
  questionNumber={48}
  question="다음 두 조건의 실행 결과 행 수를 순서대로 나열한 것은? (ㄱ) 급여가 ANY(사번이 1 또는 2인 사원의 급여 집합)보다 크다, 즉 급여가 3000 또는 4500 중 하나보다 크면 참이다. (ㄴ) 급여가 ALL(사번이 1 또는 2인 사원의 급여 집합)보다 크다, 즉 급여가 3000과 4500 모두보다 커야 참이다."
  options={['(ㄱ) 1, (ㄴ) 1', '(ㄱ) 2, (ㄴ) 1', '(ㄱ) 1, (ㄴ) 2', '(ㄱ) 2, (ㄴ) 2']}
  correctAnswer={2}
  explanation="비교 대상 집합은 3000과 4500이다. ANY는 그중 하나보다만 크면 참이므로, 3000보다 큰 4500과 5000 두 행이 조건을 만족해 2건이다. ALL은 두 값 모두보다 커야 하므로 4500보다도 큰 5000 한 행만 만족해 1건이다."
  optionExplanations={['ANY 조건이 더 느슨해 2건이 나와야 하는데 1건으로 계산한 오답이다.', '정답. ANY는 더 느슨한 조건이라 2건, ALL은 더 엄격한 조건이라 1건이 나온다.', 'ANY와 ALL의 결과를 서로 바꿔 쓴 오답이다.', 'ALL 조건이 더 엄격해 1건만 나와야 하는데 2건으로 계산한 오답이다.']}
/>

<Quiz
  questionNumber={49}
  question="집합 연산자의 성능 특징에 대한 설명으로 옳지 않은 것은?"
  options={['UNION은 중복 제거를 위한 내부 정렬 비용이 있어 UNION ALL보다 일반적으로 느리다.', 'UNION ALL은 중복 제거와 정렬을 하지 않아 중복이 없다는 것이 확실할 때 UNION보다 유리하다.', 'INTERSECT와 MINUS도 내부적으로 정렬이나 비교 작업이 필요해 단순 UNION ALL보다 비용이 더 든다.', 'UNION 계열 연산자는 모두 항상 동일한 성능을 낸다.']}
  correctAnswer={4}
  explanation="UNION, UNION ALL, INTERSECT, MINUS는 각각 내부 동작(중복 제거 여부, 비교 방식)이 달라 성능 특성도 서로 다르다. 모두 항상 동일한 성능을 낸다는 서술은 틀렸다."
  optionExplanations={['UNION이 중복 제거를 위한 정렬 비용 때문에 UNION ALL보다 느리다는 설명은 정확하다.', 'UNION ALL이 정렬·중복 제거가 없어 상대적으로 빠르다는 설명은 정확하다.', 'INTERSECT와 MINUS도 비교 작업이 필요해 UNION ALL보다 비용이 든다는 설명은 정확하다.', '정답. 네 연산자는 내부 동작 방식이 달라 성능 특성도 다르므로 항상 동일하다는 설명은 틀렸다.']}
/>

**A 테이블**

| 값 |
|----|
| 1 |
| 2 |

**B 테이블**

| A값 | B값 |
|-----|-----|
| 1 | 10 |
| 1 | 20 |
| 2 | 30 |

<Quiz
  questionNumber={50}
  question="다음 SQL의 실행 결과 행 수는? 쿼리: SELECT * FROM A INNER JOIN B ON A.값 = B.A값 WHERE B.B값이 (SELECT AVG(B값) FROM B) 보다 크다;"
  options={['1', '2', '3', '4']}
  correctAnswer={1}
  explanation="B값의 평균은 (10+20+30)을 3으로 나눈 20이다. INNER JOIN 결과는 A값 1과 매칭되는 B값 10, 20 두 행, A값 2와 매칭되는 B값 30 한 행으로 총 3행이다. 이 중 B값이 20보다 큰 행은 20은 크지 않아 제외되고 30만 남아, 최종 결과는 1행이다."
  optionExplanations={['정답. 평균 20보다 큰 B값은 30 하나뿐이라 최종 결과는 1행이다.', '평균 계산을 잘못해 20도 초과로 포함시킨 오답이다.', '평균 조건을 적용하기 전 INNER JOIN 결과 3행을 그대로 답으로 삼은 오답이다.', '조인 조건과 평균 조건을 모두 적용하지 않고 원본 B 테이블의 행 수를 그대로 가져온 오답이다.']}
/>

## 채점 후 복습 동선

이번 회차는 조인·서브쿼리·집합 연산자에 집중되어 있으므로, 이 세 영역에서 틀린 문항이 많다면 반드시 해당 편을 다시 읽고 데이터를 직접 손으로 따라가며 결과를 재구성해 보세요.

| 틀린 문항 번호대 | 취약 영역 | 돌아갈 편 |
|------------------|-----------|-----------|
| 1–3 | 데이터 모델링 관점, 엔터티, 도메인 | 01편, 03편 |
| 4–5 | 식별자, 관계 표기 | 04편, 05편 |
| 6 | ERD 해석 | 05편, 08편 |
| 7–8 | 정규화, 반정규화 | 06편, 07편 |
| 9–10 | 식별자 관계, 트랜잭션 ACID | 08편, 09편 |
| 11–13 | 제약조건, DML, TCL | 10편, 11편, 12편 |
| 14–16 | WHERE, 단일행 함수 | 13편, 14편 |
| 17–18 | 그룹 함수, HAVING | 15편 |
| 19–26 | 조인 전 유형 | 16편, 08편 |
| 27–32 | 서브쿼리 전 유형 | 17편 |
| 33–37 | 집합 연산자 | 18편 |
| 38–39 | 윈도우 함수 기본 | 19편, 20편 |
| 40 | GROUPING SETS, ROLLUP, CUBE | 21편 |
| 41–42 | Top N, 계층형 질의 | 22편 |
| 43 | PIVOT | 23편 |
| 44 | DCL | 24편 |
| 45–48 | 서브쿼리·조인 심화 | 17편, 16편 |
| 49–50 | 집합 연산자 성능, 조인·서브쿼리 복합 | 18편, 17편 |

## 합격 기준 안내

SQLD는 두 과목 합산 100점 만점에 60점 이상이면서, 각 과목에서 40점(과목별 40퍼센트) 미만이 없어야 합격입니다. 이번 모의고사 기준으로는 1과목(1~10번)에서 4문항 이상, 2과목(11~50번)에서 16문항 이상을 맞혀야 각 과목의 과락을 면하고, 전체 50문항 중 30문항(60퍼센트) 이상을 맞혀야 합니다. 특히 이번 회차에서 조인·서브쿼리·집합 연산자(19~37, 45~50번)의 정답률이 낮다면, 26편(1회)으로 돌아가 같은 영역을 다시 점검한 뒤 이 회차를 재도전하는 것을 권장합니다.

## 참고 자료

- [데이터자격시험 - SQLD 출제기준](https://www.dataq.or.kr/www/sub/a_04.do)
- [Oracle Database SQL Language Reference](https://docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/)
- [PostgreSQL - GROUPING SETS 문서](https://www.postgresql.org/docs/current/queries-table-expressions.html#QUERIES-GROUPING-SETS)

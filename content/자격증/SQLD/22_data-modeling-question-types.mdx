---
title: '22. 데이터 모델링 기출 유형'
description: 'ERD 읽기부터 반정규화까지 시험에 자주 나오는 유형을 예제로 정복합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 데이터 모델링 파트의 6가지 출제 유형을 예제 문제와 해설로 익혀서 실전에서 빠르게 판단할 수 있게 됩니다.
</Callout>

## 유형 1. ERD 읽기 — 차수와 선택성 판단

ERD 문제는 엔터티 사이의 관계선을 읽고 "몇 개와 연결될 수 있는가(차수)"와 "반드시 존재해야 하는가(선택성)"를 판단합니다.

**기호 해석 요약**

| 기호 | 의미 |
|------|------|
| `\|` (단선) | 최솟값 1 (필수) |
| `O` (원) | 최솟값 0 (선택) |
| `\|\|` (이중선) | 최댓값 1 |
| `<` 또는 `>` (까마귀 발) | 최댓값 N |

---

> **업무 규칙**
> - 부서는 반드시 1명 이상의 직원을 보유한다.
> - 직원은 반드시 하나의 부서에 소속된다.
> - 하나의 부서에는 여러 명의 직원이 소속될 수 있다.

<Quiz
  questionNumber={1}
  question="위 업무 규칙을 보고 ERD 관계의 차수와 선택성이 올바르게 설명된 것을 고르세요."
  options={['① 부서(1, 필수) : 직원(1, 필수) — 1:1 관계', '② 부서(1, 필수) : 직원(N, 필수) — 1:N 관계, 양쪽 필수', '③ 부서(1, 선택) : 직원(N, 선택) — 1:N 관계, 양쪽 선택', '④ 부서(N, 필수) : 직원(1, 선택) — N:1 관계']}
  correctAnswer={2}
  explanation="부서 입장: 직원을 1명 이상 보유 → 최솟값 1(필수), 최댓값 N(다수)으로 연결됩니다. 직원 입장: 반드시 하나의 부서에 소속 → 최솟값 1(필수), 최댓값 1(하나만)으로 연결됩니다. 전체 관계: 부서 1 : 직원 N, 양쪽 모두 필수(Mandatory) 관계입니다."
  optionExplanations={['①은 차수 오류입니다. 직원이 여럿일 수 있으므로 1:1이 아닌 1:N 관계입니다.', '정답. 부서 1(필수) : 직원 N(필수)으로 양쪽 모두 필수인 1:N 관계입니다.', '③은 선택성 오류입니다. 업무 규칙에 양쪽 모두 필수라고 명시되어 있습니다.', '④는 차수가 반대입니다. 부서가 N이 아닌 1쪽입니다.']}
/>

---

<Quiz
  questionNumber={2}
  question="다음 중 ERD에서 '선택적(Optional) 관계'가 필요한 업무 상황으로 가장 적합한 것은?"
  options={['① 주문은 반드시 고객 정보를 포함해야 한다.', '② 사원은 반드시 소속 부서가 있어야 한다.', '③ 학생은 동아리에 가입하지 않을 수도 있다.', '④ 제품은 반드시 하나의 카테고리에 속해야 한다.']}
  correctAnswer={3}
  explanation="선택적 관계(O 기호)는 최솟값이 0인 경우, 즉 반드시 연결되지 않아도 되는 경우에 사용합니다. '가입하지 않을 수도 있다'는 0개와 연결 가능 → 선택적 관계입니다. 나머지 ①②④는 모두 '반드시'가 포함되어 필수(Mandatory) 관계입니다."
  optionExplanations={['①은 \"반드시\"가 포함되어 필수(Mandatory) 관계입니다.', '②는 \"반드시\"가 포함되어 필수(Mandatory) 관계입니다.', '정답. 가입하지 않을 수도 있으므로 최솟값 0의 선택적 관계입니다.', '④는 \"반드시\"가 포함되어 필수(Mandatory) 관계입니다.']}
/>

---

## 유형 2. 정규화 판단 — 정규형 식별과 이상현상

정규화 문제는 "이 테이블은 몇 정규형인가?" 또는 "어떤 이상현상이 발생하는가?"를 묻습니다.

**핵심 요약**

| 정규형 | 조건 |
|--------|------|
| 1NF | 모든 속성이 원자값(반복 그룹 없음) |
| 2NF | 1NF + 부분 함수 종속 제거(복합 키일 때 일부 키에만 종속된 속성 제거) |
| 3NF | 2NF + 이행 함수 종속 제거(비키 속성이 다른 비키 속성을 결정) |

---

수강신청(학번, 과목코드, 학생이름, 과목명, 성적) — 기본키: (학번, 과목코드)

| 학번 | 과목코드 | 학생이름 | 과목명 | 성적 |
|------|----------|----------|--------|------|
| S001 | C101 | 김철수 | 데이터베이스 | A |
| S001 | C102 | 김철수 | 알고리즘 | B |
| S002 | C101 | 이영희 | 데이터베이스 | A+ |

<Quiz
  questionNumber={3}
  question="위 수강신청 테이블에 대한 설명으로 올바른 것은?"
  options={['① 1NF를 만족하지 않는다.', '② 2NF를 만족한다.', '③ 학생이름은 학번에만 종속되므로 부분 함수 종속이 존재한다.', '④ 성적은 학번만으로 결정되므로 이행 함수 종속이 존재한다.']}
  correctAnswer={3}
  explanation="기본키가 (학번, 과목코드)인 복합키인데, 학생이름은 학번만 알면 결정됩니다(학번 → 학생이름). 이처럼 복합 기본키의 일부에만 종속된 경우를 부분 함수 종속이라 합니다. 이 테이블은 1NF는 만족하지만 2NF를 위반합니다."
  optionExplanations={['①은 틀렸습니다. 모든 속성이 원자값이므로 1NF는 만족합니다.', '②는 틀렸습니다. 부분 함수 종속이 존재하므로 2NF를 위반합니다.', '정답. 학생이름은 학번만으로 결정되는 부분 함수 종속입니다.', '④는 틀렸습니다. 성적은 (학번, 과목코드) 전체로 결정되는 완전 함수 종속입니다.']}
/>

---

<Quiz
  questionNumber={4}
  question="다음 중 삭제 이상(Deletion Anomaly)이 발생하는 상황으로 가장 적절한 것은?"
  options={['① 동일한 데이터가 여러 행에 중복 저장되어 수정 시 일부만 바뀐다.', '② 새 학생을 등록하려면 수강 과목이 없어도 반드시 과목코드를 입력해야 한다.', '③ 특정 학생의 수강 기록을 삭제했더니 그 학생 정보 자체가 사라졌다.', '④ 같은 학번의 학생에 대해 학생이름이 서로 다르게 저장되어 있다.']}
  correctAnswer={3}
  explanation="삭제 이상은 하나의 사실을 삭제하려 할 때 의도치 않게 다른 사실도 삭제되는 현상입니다. ①④는 갱신 이상, ②는 삽입 이상에 해당합니다."
  optionExplanations={['①은 갱신 이상입니다. 중복 데이터를 모두 수정하지 않아 불일치가 발생합니다.', '②는 삽입 이상입니다. 원하지 않는 데이터를 함께 입력해야만 삽입 가능한 경우입니다.', '정답. 삭제 이상입니다. 수강 기록 삭제 시 학생 정보까지 함께 삭제됩니다.', '④는 갱신 이상입니다. 동일 학번에 다른 이름이 저장되는 불일치 상황입니다.']}
/>

---

## 유형 3. 엔터티·속성·관계 분류

주어진 업무 설명에서 무엇이 엔터티이고 무엇이 속성인지를 판단하는 유형입니다.

**핵심 기준**

- **엔터티**: 독립적으로 관리할 필요가 있는 대상. 인스턴스(행)가 여러 개 존재할 수 있음.
- **속성**: 엔터티의 특징이나 상태를 나타내는 값. 인스턴스를 추가로 가지지 않음.
- **관계**: 두 엔터티 사이의 업무적 연관성.

---

> 온라인 서점에서는 회원이 도서를 주문할 수 있다. 각 도서는 출판사가 발행하며, 주문 시 배송지 주소를 기록한다. 도서에는 ISBN, 제목, 정가 정보가 있다.

<Quiz
  questionNumber={5}
  question="위 업무 설명에서 엔터티로 관리해야 할 대상만 모두 고른 것은?"
  options={['① 회원, 도서, 주문', '② 회원, 도서, 주문, 출판사', '③ 회원, 도서, 주문, 출판사, ISBN, 배송지 주소', '④ 회원, 도서, 주문, ISBN, 정가']}
  correctAnswer={2}
  explanation="엔터티는 독립적으로 관리가 필요한 대상입니다. 회원, 도서, 주문, 출판사는 각각 여러 인스턴스를 가지며 독립 관리합니다. ISBN, 정가, 배송지 주소는 각각 도서/주문의 속성입니다."
  optionExplanations={['①은 출판사를 빠뜨렸습니다. 출판사는 별도 정보(출판사명, 연락처 등)를 관리하므로 엔터티입니다.', '정답. 회원, 도서, 주문, 출판사가 독립적으로 관리되는 엔터티입니다.', '③은 ISBN, 배송지 주소를 엔터티로 잘못 분류했습니다. 이들은 속성입니다.', '④는 ISBN, 정가를 엔터티로 잘못 분류했습니다. 이들은 도서의 속성입니다.']}
/>

---

## 유형 4. 식별자 선택 — 후보키와 기본키

**핵심 개념**

| 용어 | 설명 |
|------|------|
| 후보키(Candidate Key) | 유일성 + 최소성을 만족하는 속성(집합) |
| 기본키(Primary Key) | 후보키 중 대표로 선택된 키, NULL 불가 |
| 대체키(Alternate Key) | 후보키 중 기본키로 선택되지 않은 나머지 |
| 외래키(Foreign Key) | 다른 테이블의 기본키를 참조하는 속성 |

---

직원(직원번호, 주민등록번호, 이름, 부서코드, 이메일) — 직원번호·주민등록번호·이메일은 각각 고유, 이름·부서코드는 중복 가능

<Quiz
  questionNumber={6}
  question="위 직원 테이블에서 후보키가 될 수 있는 것을 모두 고른 것은?"
  options={['① 직원번호', '② 직원번호, 주민등록번호', '③ 직원번호, 주민등록번호, 이메일', '④ 직원번호, 이름']}
  correctAnswer={3}
  explanation="후보키는 유일성과 최소성을 모두 만족해야 합니다. 직원번호, 주민등록번호, 이메일 각각이 단독으로 유일성과 최소성을 만족하므로 모두 후보키입니다. (직원번호, 주민등록번호) 조합은 각각이 이미 후보키이므로 최소성 위반으로 후보키가 아닙니다."
  optionExplanations={['①은 직원번호만 나열했습니다. 주민등록번호와 이메일도 후보키입니다.', '②는 (직원번호, 주민등록번호) 조합을 후보키로 포함했으나, 조합은 최소성 위반입니다.', '정답. 직원번호, 주민등록번호, 이메일이 각각 단독으로 후보키 조건을 만족합니다.', '④는 이름이 중복 가능하므로 (직원번호, 이름) 조합에서 이름 단독으로는 유일성을 만족하지 않아 최소성 위반입니다.']}
/>

---

<Quiz
  questionNumber={7}
  question="주민등록번호가 후보키인데도 기본키로 선택하지 않는 이유로 가장 적절한 것은?"
  options={['① 주민등록번호는 유일성을 만족하지 않기 때문이다.', '② 주민등록번호는 개인정보 보호 관점에서 노출 위험이 있고 변경 가능성이 있기 때문이다.', '③ 주민등록번호는 숫자가 너무 길어 인덱스 성능이 떨어지기 때문이다.', '④ 주민등록번호는 외래키로 사용할 수 없기 때문이다.']}
  correctAnswer={2}
  explanation="기본키 선택 기준에서 주민등록번호는 유일하지만 개인정보 보호법상 수집 제한, 외부 기관 부여로 인한 변경 가능성, 시스템 간 공유 시 보안 위험 등의 이유로 실무에서 기본키로 잘 사용하지 않습니다. 대신 내부 대리키(Surrogate Key)를 사용합니다."
  optionExplanations={['①은 틀렸습니다. 주민등록번호는 개인마다 고유하므로 유일성은 만족합니다.', '정답. 개인정보 보호 및 변경 가능성이 주민등록번호를 기본키로 쓰지 않는 주요 이유입니다.', '③은 성능도 고려 요소이지만 가장 적절한 이유는 아닙니다.', '④는 틀렸습니다. 후보키는 외래키로도 참조될 수 있습니다.']}
/>

---

## 유형 5. 함수 종속 — 부분 vs 완전 함수 종속

**핵심 구분**

| 구분 | 설명 | 예시 |
|------|------|------|
| 완전 함수 종속 | 기본키 전체가 있어야 결정 | (학번, 과목코드) → 성적 |
| 부분 함수 종속 | 기본키 일부만으로 결정 가능 | 학번 → 학생이름 |
| 이행 함수 종속 | A→B, B→C이면 A→C | 학번→부서코드, 부서코드→부서명 → 학번→부서명 |

---

주문(주문번호, 고객번호, 고객등급, 상품코드, 수량) 테이블의 함수 종속:
- 주문번호 → 고객번호
- 고객번호 → 고객등급
- 주문번호 → 상품코드
- (주문번호, 상품코드) → 수량

<Quiz
  questionNumber={8}
  question="위 주문 테이블에서 이행 함수 종속(Transitive Functional Dependency)에 해당하는 것은?"
  options={['① 주문번호 → 상품코드', '② 주문번호 → 고객등급', '③ (주문번호, 상품코드) → 수량', '④ 고객번호 → 고객등급']}
  correctAnswer={2}
  explanation="이행 함수 종속은 A → B이고 B → C일 때 A → C가 성립하는 경우입니다. 주문번호 → 고객번호, 고객번호 → 고객등급이므로 주문번호 → 고객등급은 이행 함수 종속입니다. 이를 해결하려면 고객 테이블(고객번호, 고객등급)을 분리해야 합니다(3NF 변환)."
  optionExplanations={['①은 주문번호에서 상품코드로의 직접 종속입니다. 이행 종속이 아닙니다.', '정답. 주문번호 → 고객번호 → 고객등급의 이행 함수 종속입니다.', '③은 완전 함수 종속입니다. (주문번호, 상품코드) 전체로 수량이 결정됩니다.', '④는 고객번호에서 고객등급으로의 직접 종속입니다. 이행 종속이 아닙니다.']}
/>

---

## 유형 6. 반정규화 — 필요 상황 판단

반정규화는 성능 향상을 위해 의도적으로 정규화 규칙을 어기는 것입니다. 조회 성능은 올라가지만 데이터 무결성 관리 비용이 증가합니다.

**반정규화가 필요한 대표 상황**

1. 조회 빈도가 매우 높고 JOIN이 복잡한 경우
2. 집계(합계, 평균) 값을 매번 계산하는 비용이 높은 경우
3. 이력 데이터를 별도로 관리해야 하는 경우

---

<Quiz
  questionNumber={9}
  question="다음 상황에서 반정규화가 가장 적합한 경우는?"
  options={['① 신규 회원가입 처리 시스템에서 INSERT 성능이 중요한 상황', '② 일별 매출 합계를 실시간으로 제공해야 하는데 주문 테이블에 수백만 건이 있어 매번 SUM을 계산하면 응답이 5초를 초과하는 상황', '③ 개인정보 보호를 위해 민감 정보를 별도 테이블로 분리해야 하는 상황', '④ 여러 테이블에 분산된 데이터를 정확하게 관리하기 위해 트랜잭션 무결성이 중요한 상황']}
  correctAnswer={2}
  explanation="반정규화의 핵심 목적은 조회 성능 향상입니다. ②처럼 집계 연산이 매우 느리고 조회 빈도가 높다면, 매출 합계 컬럼을 별도로 관리(컬럼 반정규화 또는 집계 테이블 추가)하는 것이 적합합니다."
  optionExplanations={['①은 INSERT 성능 문제로 반정규화로 해결하지 않습니다. 인덱스 조정 등으로 접근합니다.', '정답. 집계 조회 성능이 문제인 상황으로 반정규화(집계 테이블 추가)가 가장 적합합니다.', '③은 분리 방향으로 정규화에 해당합니다. 반정규화의 반대입니다.', '④는 무결성 중시 시스템에서는 반정규화를 피해야 합니다.']}
/>

---

<Quiz
  questionNumber={10}
  question="다음 반정규화 기법 중 '테이블 반정규화'에 해당하는 것은?"
  options={['① 자주 사용되는 계산 값을 컬럼으로 추가한다.', '② 조회 조건으로 자주 사용되는 컬럼을 다른 테이블에 중복 저장한다.', '③ 두 개의 테이블을 하나로 합친다.', '④ 긴 문자열 컬럼을 별도 테이블로 분리한다.']}
  correctAnswer={3}
  explanation="테이블 반정규화는 테이블 병합(두 테이블을 하나로), 중복 테이블 추가 등을 포함합니다. ③이 테이블 병합에 해당합니다. ①②는 컬럼 반정규화, ④는 수직 분할(정규화 방향)입니다."
  optionExplanations={['①은 파생 컬럼 추가로 컬럼 반정규화에 해당합니다.', '②는 중복 컬럼 추가로 컬럼 반정규화에 해당합니다.', '정답. 두 테이블을 하나로 합치는 테이블 병합은 테이블 반정규화입니다.', '④는 수직 분할로 정규화 방향에 가까우며, 테이블 반정규화의 반대입니다.']}
/>

---

## 마무리 복습

<Quiz questionNumber={11} question="다음 중 2NF(제2정규형)를 위반하는 상황은?" options={['① 기본키가 단일 속성이고, 모든 비키 속성이 기본키에 종속된다.', '② 복합 기본키(A, B)에서 비키 속성 C가 A만으로도 결정된다.', '③ 비키 속성 X가 다른 비키 속성 Y를 결정하는 이행 종속이 존재한다.', '④ 모든 속성이 원자값이고 반복 그룹이 없다.']} correctAnswer={2} explanation="2NF 위반은 복합 기본키 상황에서 비키 속성이 기본키 전체가 아닌 일부에만 종속(부분 함수 종속)될 때 발생합니다. ②번이 정확히 이 상황입니다. ③은 이행 함수 종속으로 3NF 위반이고, ①④는 정규형을 만족하는 상황입니다." optionExplanations={['단일 기본키이면 부분 함수 종속이 존재할 수 없으므로 2NF는 자동으로 만족합니다.', '정답. 복합 기본키(A,B)에서 C가 A만으로 결정된다면 부분 함수 종속이므로 2NF 위반입니다.', '이행 함수 종속(A→B, B→C이면 A→C)은 3NF 위반입니다. 2NF와는 별개입니다.', '1NF의 조건을 설명합니다. 2NF와는 별개입니다.']} />

<Quiz questionNumber={12} question="ERD에서 '선택적 관계(Optional Relationship)'를 표현할 때 관계선에 사용하는 기호는?" options={['① | (단선, 실선)', '② O (원)', '③ < 또는 > (까마귀 발)', '④ || (이중 실선)']} correctAnswer={2} explanation="선택적 관계는 최솟값이 0, 즉 연결이 없어도 된다는 의미입니다. ERD에서 최솟값 0을 나타내는 기호는 O(원)입니다. |(단선)은 최솟값 1(필수), 까마귀 발은 최댓값 N, 이중 실선은 최댓값 1을 나타냅니다." optionExplanations={['단선(|)은 최솟값 1, 즉 필수(Mandatory)를 의미합니다.', '정답. O(원)은 최솟값 0으로 선택적(Optional) 관계를 나타냅니다.', '까마귀 발(Crow\'s Foot)은 최댓값 N으로 다(多) 측을 나타냅니다.', '이중 실선(||)은 최댓값 1, 즉 단 하나와만 연결됨을 나타냅니다.']} />

<Quiz questionNumber={13} question="다음 중 반정규화 적용 시 발생하는 부작용으로 가장 적절한 것은?" options={['① 조회 쿼리 성능이 저하된다.', '② 테이블 구조가 단순해져 이해하기 어려워진다.', '③ 데이터 중복으로 인해 갱신 시 무결성 유지 비용이 증가한다.', '④ 인덱스를 사용할 수 없게 된다.']} correctAnswer={3} explanation="반정규화는 조회 성능을 높이기 위해 데이터를 중복 저장하거나 테이블을 합치는 기법입니다. 이 과정에서 동일한 데이터가 여러 곳에 존재하게 되어, 갱신(UPDATE/DELETE) 시 모든 중복 데이터를 일관되게 유지하는 비용이 증가합니다. 이것이 핵심 부작용입니다." optionExplanations={['반정규화는 오히려 조회 성능을 향상시키기 위한 기법입니다.', '반정규화는 테이블을 합치거나 컬럼을 추가하므로 오히려 구조가 복잡해질 수 있습니다.', '정답. 중복 데이터 발생으로 인한 갱신 이상 가능성 증가가 반정규화의 대표적 부작용입니다.', '반정규화는 인덱스 사용과 직접적인 관계가 없습니다.']} />

## 참고 자료

- [KDATA 데이터자격시험 - SQL 개발자](https://www.dataq.or.kr/www/sub/a_04.do)

---
title: '30. SQLD 예상 모의고사 5회'
description: '시험 직전 최종 점검용으로 전 범위를 균형 있게 배분한 재구성 모의고사 50문항, 데이터 모델링 10문항과 SQL 활용 40문항'
---

<Callout type="warning">
  **이 모의고사는 실제 기출문제를 그대로 옮긴 것이 아닙니다.** 공개된 회차의 출제 범위·문항 형식·오답 구성 방식을 분석해, 테이블·컬럼명·숫자를 모두 새로 바꿔 재구성한 문항입니다. **권장 풀이 시간은 90분**입니다(실제 SQLD 시험 시간과 동일하게 맞췄습니다). 이 회차는 특정 주제에 치우치지 않고 **데이터 모델링부터 DBMS 문법 차이까지 전 범위를 표준 시험 비중대로 균형 있게 배분**했습니다. 시험 직전 마지막 실력 점검용으로 활용하십시오.
</Callout>

실제 SQLD는 1과목 데이터 모델링 10문항(20점), 2과목 SQL 기본 및 활용 40문항(80점), 총 100점 만점이며 과목별 40퍼센트(40%) 미만이면 과락으로 총점과 무관하게 불합격합니다. 이 회차는 영화관 예매 시스템(영화·상영관·좌석·회원·예매·결제)을 소재로 삼아 전 단원을 고르게 담았습니다. 실전처럼 시간을 재고 끝까지 푼 뒤 채점하고, 마지막 절의 합격 기준과 비교해 지금 상태를 냉정하게 점검하십시오.

## 문항 배분

| 영역 | 문항 수 | 문항 번호 | 배점 |
| --- | --- | --- | --- |
| 데이터 모델링(엔터티·식별자·정규화·트랜잭션·DCL) | 10 | 1–10 | 20점 |
| DDL·DML·TCL·WHERE·함수 | 10 | 11–20 | 20점 |
| 조인·서브쿼리·집합연산자 | 10 | 21–30 | 20점 |
| 윈도우 함수·GROUP 확장 함수·계층형 질의 | 10 | 31–40 | 20점 |
| PIVOT·CASE·정규표현식·절차형 SQL·DBMS 차이·종합 실행 결과 예측 | 10 | 41–50 | 20점 |
| 합계 | 50 | 1–50 | 100점 |

## 데이터 모델링 (1–10번)

<Quiz
  questionNumber={1}
  question="다음 중 행위 엔터티(트랜잭션 엔터티)에 해당하는 것은?"
  options={['회원', '영화', '예매', '상영관']}
  correctAnswer={3}
  explanation="행위 엔터티는 다른 엔터티들의 활동 결과로 발생하는 엔터티다. 예매는 회원이 특정 영화와 상영관을 대상으로 행동한 결과로 만들어지므로 행위 엔터티에 해당한다. 회원, 영화, 상영관은 그 자체로 독립적으로 존재하는 기본(키) 엔터티다."
  optionExplanations={['다른 엔터티 없이 독립적으로 존재하는 기본 엔터티다.', '다른 엔터티 없이 독립적으로 존재하는 기본 엔터티다.', '정답이다. 회원·영화·상영관의 활동 결과로 발생하는 행위 엔터티다.', '다른 엔터티 없이 독립적으로 존재하는 기본 엔터티다.']}
/>

<Quiz
  questionNumber={2}
  question="예매 엔터티는 좌석단가, 예매수량, 총결제금액을 가지며 총결제금액은 좌석단가와 예매수량을 곱해 산출된다. 이 상황에 대한 설명으로 옳지 않은 것은?"
  options={['좌석단가와 예매수량은 기본속성이다', '총결제금액은 파생속성이다', '총결제금액도 예매 시점에 직접 입력받는 기본속성으로 관리해야 정확하다', '파생속성은 원본 속성 값이 바뀌면 함께 갱신해야 값의 정합성이 유지된다']}
  correctAnswer={3}
  explanation="계산으로 얻을 수 있는 값을 굳이 별도로 직접 입력받게 하면, 원본 값(좌석단가·예매수량)이 바뀌었을 때 총결제금액과 값이 어긋나는 정합성 문제가 생길 위험이 커진다. 파생속성은 계산식으로 관리하는 것이 원칙이다."
  optionExplanations={['업무에서 직접 정해지는 값이므로 기본속성이라는 설명은 옳다.', '다른 속성의 곱으로 산출되므로 파생속성이라는 설명은 옳다.', '정답이다. 계산 가능한 값을 직접 입력받게 하면 오히려 정합성이 깨질 위험이 커진다.', '파생속성의 일반적 특징으로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 후보키(Candidate Key)의 조건으로 옳지 않은 것은?"
  options={['각 인스턴스를 유일하게 식별해야 한다', '키를 구성하는 속성 중 하나라도 제거하면 유일성이 깨져야 한다', '후보키는 반드시 두 개 이상의 속성으로 구성되어야 한다', '한 엔터티에는 후보키가 여러 개 존재할 수 있다']}
  correctAnswer={3}
  explanation="후보키의 조건은 유일성과 최소성이다. 속성 개수에 대한 제약은 없으며, 하나의 속성만으로도 유일성과 최소성을 만족하면 후보키가 될 수 있다. 반드시 두 개 이상이어야 한다는 설명은 옳지 않다."
  optionExplanations={['유일성에 대한 옳은 설명이다.', '최소성에 대한 옳은 설명이다.', '정답이다. 후보키는 단일 속성으로도 구성될 수 있어 이 설명은 옳지 않다.', '한 엔터티에 여러 후보키가 있을 수 있고 그중 하나를 기본키로 선정하는 것이 옳은 설명이다.']}
/>

<Quiz
  questionNumber={4}
  question="한 회원은 여러 번 예매할 수 있고, 하나의 예매는 반드시 한 명의 회원에게만 속한다. 회원과 예매 사이 관계의 카디널리티로 옳은 것은?"
  options={['1대1', '회원 1 대 예매 N', '회원 N 대 예매 1', 'N대M']}
  correctAnswer={2}
  explanation="회원 한 명이 여러 예매를 할 수 있고 하나의 예매는 한 명의 회원에만 속하므로, 회원 쪽이 1, 예매 쪽이 N인 1대N 관계다."
  optionExplanations={['회원 한 명이 여러 번 예매할 수 있으므로 1대1이 아니다.', '정답이다. 회원이 1, 예매가 N인 1대N 관계다.', '회원과 예매의 방향을 서로 바꿔 표현한 오답이다.', '예매가 여러 회원에 동시에 속할 수 있어야 N대M이 되는데, 하나의 예매는 한 회원에만 속하므로 N대M이 아니다.']}
/>

<Quiz
  questionNumber={5}
  question="[좌석] 엔터티의 기본키가 (상영관번호, 좌석번호) 복합키로 구성되고 상영관번호는 [상영관] 엔터티의 기본키를 그대로 물려받았다. 이 관계에 대한 설명으로 가장 적절한 것은?"
  options={['이는 식별관계로, 상영관 없이는 좌석이 존재할 수 없다', '이는 비식별관계로, 좌석은 상영관과 무관하게 독립적으로 존재할 수 있다', '상영관번호는 좌석의 외래키가 아니다', '이 구조에서는 좌석이 부모 엔터티가 된다']}
  correctAnswer={1}
  explanation="부모(상영관)의 기본키가 자식(좌석)의 기본키 일부로 상속된 구조는 식별관계다. 식별관계에서 자식은 부모의 존재를 전제로만 존재할 수 있다."
  optionExplanations={['정답이다. 부모 키가 자식 기본키의 일부가 되는 전형적인 식별관계다.', '식별관계는 부모 존재에 종속되므로 독립적으로 존재할 수 있다는 설명은 옳지 않다.', '상영관번호는 기본키의 일부이면서 동시에 부모를 참조하는 외래키 역할도 한다.', '기본키를 물려주는 쪽인 상영관이 부모 엔터티다.']}
/>

<Quiz
  questionNumber={6}
  question="[예매] 테이블에 동반관람인이라는 컬럼이 있고, 한 행에 여러 명의 동반관람인 이름이 하나의 값으로 함께 저장되어 있다. 이 상태를 해결하기 위해 수행해야 하는 정규화 단계는?"
  options={['제1정규화', '제2정규화', '제3정규화', '반정규화']}
  correctAnswer={1}
  explanation="한 컬럼에 여러 값이 함께 들어 있는 것은 원자값 조건을 위반한 것으로, 이를 해결하는 단계는 제1정규화다. 동반관람인을 별도 행이나 별도 엔터티로 분리해 각 값이 하나의 컬럼에 하나씩만 담기도록 만들어야 한다."
  optionExplanations={['정답이다. 원자값 위반을 해결하는 단계는 제1정규화다.', '제2정규화는 부분 함수 종속 제거로, 이 문제는 원자값 위반이라 대상이 다르다.', '제3정규화는 이행적 함수 종속 제거로, 이 문제와는 무관하다.', '반정규화는 정규화를 되돌리는 것으로 이 문제 해결과는 방향이 반대다.']}
/>

<Quiz
  questionNumber={7}
  question="[예매] 테이블에서 예매번호(기본키)가 회원번호를 결정하고, 회원번호가 다시 회원등급을 결정한다. 이 이행적 종속을 제거하려면 수행해야 하는 정규화 단계는?"
  options={['제1정규화', '제2정규화', '제3정규화', '반정규화']}
  correctAnswer={3}
  explanation="기본키를 거치지 않고 간접적으로 이어지는 예매번호에서 회원번호를 거쳐 회원등급까지 이어지는 종속은 이행적 함수 종속이다. 이를 제거하는 단계는 제3정규화이며, 회원 엔터티를 별도로 분리해야 한다."
  optionExplanations={['제1정규화는 원자값 위반을 다루는 단계로 이 문제와 무관하다.', '제2정규화는 복합키 일부에 대한 부분 종속을 다루는데, 이 테이블은 단일 기본키라 해당하지 않는다.', '정답이다. 이행적 함수 종속을 제거하는 단계는 제3정규화다.', '반정규화는 성능을 위해 정규화를 되돌리는 것으로 종속 제거와 방향이 반대다.']}
/>

<Quiz
  questionNumber={8}
  question="다음 중 트랜잭션의 고립성(Isolation)에 대한 설명으로 가장 적절한 것은?"
  options={['트랜잭션의 연산은 모두 반영되거나 모두 취소되어야 한다', '완료된 트랜잭션의 결과는 장애가 발생해도 영구적으로 보존되어야 한다', '동시에 실행되는 트랜잭션들은 서로의 중간 상태에 영향을 주지 않아야 한다', '트랜잭션 실행 전후로 데이터의 무결성 제약조건이 항상 유지되어야 한다']}
  correctAnswer={3}
  explanation="고립성은 여러 트랜잭션이 동시에 실행되더라도 서로의 완료되지 않은 중간 상태를 보거나 간섭받지 않아야 한다는 특성이다."
  optionExplanations={['이는 원자성(Atomicity)에 대한 설명이다.', '이는 영속성(Durability)에 대한 설명이다.', '정답이다. 동시 실행 트랜잭션 간 간섭을 차단하는 것이 고립성이다.', '이는 일관성(Consistency)에 대한 설명이다.']}
/>

<Quiz
  questionNumber={9}
  question="[예매] 테이블의 취소일자 컬럼이 NULL인 행에 대한 설명으로 가장 적절한 것은?"
  options={['취소되지 않은 예매라는 의미로 흔히 사용된다', '반드시 데이터 오류를 의미한다', '취소일자가 0으로 자동 저장된 것과 같다', '다른 행의 취소일자와 항상 같다고 비교된다']}
  correctAnswer={1}
  explanation="NULL은 값이 존재하지 않거나 아직 정해지지 않았음을 나타내는 표시일 뿐, 그 자체로 오류를 의미하지 않는다. 취소일자 컬럼에서는 흔히 아직 취소되지 않은 예매를 의미하는 용도로 쓰인다."
  optionExplanations={['정답이다. 취소되지 않았다는 업무적 의미로 NULL을 사용하는 것은 흔한 설계 방식이다.', 'NULL은 값이 없거나 아직 정해지지 않았다는 뜻이지 오류를 뜻하지 않는다.', 'NULL은 0이라는 값과 다르며 자동으로 0으로 저장되지 않는다.', 'NULL은 다른 NULL과 비교해도 같다고 판정되지 않고 알 수 없음이 된다.']}
/>

<Quiz
  questionNumber={10}
  question="다음 중 ROLE에 대한 설명으로 가장 적절한 것은?"
  options={['여러 권한을 하나로 묶어 관리하는 권한의 집합으로, 사용자에게 부여하면 묶인 권한을 한 번에 부여할 수 있다', 'ROLE은 테이블 구조를 정의하는 DDL 명령어이다', 'ROLE은 트랜잭션을 커밋하는 명령어이다', 'ROLE은 한 명의 사용자에게만 부여할 수 있고 여러 사용자에게 동시에 부여할 수 없다']}
  correctAnswer={1}
  explanation="ROLE은 여러 시스템·객체 권한을 하나로 묶은 권한의 집합이다. 사용자에게 ROLE 하나를 부여하면 그 안에 묶인 여러 권한을 한 번에 부여하는 효과를 낼 수 있어 권한 관리가 단순해진다."
  optionExplanations={['정답이다. 여러 권한을 묶어 한 번에 관리하는 것이 ROLE의 역할이다.', '테이블 구조 정의는 DDL의 역할이며 ROLE과 무관하다.', '트랜잭션 커밋은 TCL의 COMMIT 명령어의 역할이다.', 'ROLE은 여러 사용자에게 동시에 부여할 수 있다.']}
/>

## DDL·DML·TCL·WHERE·함수 (11–20번)

<Quiz
  questionNumber={11}
  question="다음 중 외래키(FOREIGN KEY) 제약조건에 대한 설명으로 옳은 것은?"
  options={['자식 테이블의 외래키 값은 반드시 부모 테이블에 존재하는 값이거나 NULL이어야 한다', '외래키 컬럼은 항상 NOT NULL이어야 한다', '외래키는 반드시 부모 테이블의 기본키만 참조할 수 있고 UNIQUE 컬럼은 참조할 수 없다', '외래키 제약조건이 있으면 부모 테이블의 행을 자유롭게 삭제할 수 있다']}
  correctAnswer={1}
  explanation="외래키 제약조건은 그 값이 부모 테이블에 실제로 존재하는 값이거나, 값이 없음을 뜻하는 NULL이어야 한다는 참조 무결성을 강제한다."
  optionExplanations={['정답이다. 외래키는 부모 테이블의 값이거나 NULL이어야 한다는 참조 무결성을 지킨다.', '외래키 컬럼도 별도로 NOT NULL을 지정하지 않으면 NULL을 허용할 수 있다.', '외래키는 부모 테이블의 UNIQUE 제약조건이 걸린 컬럼도 참조할 수 있다.', '자식이 참조하고 있는 부모 행을 그냥 삭제하면 참조 무결성 위반으로 오류가 발생하며, CASCADE 옵션 등을 지정해야 함께 처리된다.']}
/>

[회원] 테이블에는 회원ID 1(등급 일반), 2(등급 VIP)가 있다.

```sql
UPDATE 예매 E
SET 할인율 = 0.1
WHERE E.회원ID IN (SELECT 회원ID FROM 회원 WHERE 등급 = 'VIP');
```

<Quiz
  questionNumber={12}
  question="위 UPDATE 문의 동작으로 가장 적절한 것은?"
  options={['등급이 VIP인 회원의 예매 건에 대해서만 할인율을 0.1로 변경한다', '모든 예매 건의 할인율을 0.1로 변경한다', '등급이 VIP가 아닌 회원의 예매 건만 변경한다', '회원 테이블의 등급 컬럼 값을 변경한다']}
  correctAnswer={1}
  explanation="서브쿼리는 등급이 VIP인 회원의 회원ID만 반환한다. WHERE 절의 IN 조건으로 그 회원ID에 해당하는 예매 건만 걸러 할인율을 변경하므로, VIP 회원의 예매 건에만 UPDATE가 적용된다."
  optionExplanations={['정답이다. IN 서브쿼리로 VIP 회원의 예매 건만 걸러 변경한다.', 'WHERE 조건이 있으므로 조건에 맞는 일부 행만 변경되며 전체가 변경되지 않는다.', 'IN 조건은 VIP 회원의 예매만 포함하며 그 반대가 아니다.', 'UPDATE 대상 테이블은 예매이며 회원 테이블은 조회만 될 뿐 값이 바뀌지 않는다.']}
/>

<Quiz
  questionNumber={13}
  question="SAVEPOINT에 대한 설명으로 옳지 않은 것은?"
  options={['트랜잭션 중간에 저장점을 만들어 그 지점까지만 부분적으로 롤백할 수 있게 한다', 'SAVEPOINT는 COMMIT과 같아서 지정하는 즉시 데이터가 영구 저장된다', '하나의 트랜잭션 안에 여러 개의 SAVEPOINT를 지정할 수 있다', 'ROLLBACK TO 저장점명을 실행하면 그 저장점 이후의 변경만 취소된다']}
  correctAnswer={2}
  explanation="SAVEPOINT는 트랜잭션이 아직 끝나지 않은 상태에서 나중에 되돌아올 지점을 표시만 할 뿐, 데이터를 영구 저장하지 않는다. 데이터를 영구 저장하는 것은 COMMIT의 역할이다."
  optionExplanations={['SAVEPOINT의 정의로 옳은 설명이다.', '정답이다. SAVEPOINT는 COMMIT과 달리 데이터를 영구 저장하지 않는다.', '하나의 트랜잭션에 여러 저장점을 둘 수 있다는 것은 옳은 설명이다.', 'ROLLBACK TO의 동작 방식으로 옳은 설명이다.']}
/>

```sql
SELECT 영화ID FROM 영화 WHERE 관람등급 IN ('12세', '15세');
```

<Quiz
  questionNumber={14}
  question="[영화] 테이블에 영화ID 1(전체), 2(12세), 3(15세), 4(청불)이 있을 때, 위 SQL의 실행 결과로 옳은 것은?"
  options={['영화ID 1, 4', '영화ID 2, 3', '영화ID 1, 2, 3, 4', '결과가 없다']}
  correctAnswer={2}
  explanation="IN 연산자는 목록에 포함된 값과 일치하는 행을 찾는다. 관람등급이 12세 또는 15세인 영화는 영화ID 2와 3이다."
  optionExplanations={['이 목록에 없는 관람등급(전체, 청불)을 가진 영화ID다.', '정답이다. 12세인 영화ID 2와 15세인 영화ID 3이 조건에 맞는다.', '조건에 맞지 않는 영화까지 모두 포함한 오답이다.', '조건을 만족하는 영화가 실제로 존재하므로 결과가 없을 수 없다.']}
/>

```sql
SELECT ROUND(15750, -3) AS 결과 FROM DUAL;
```

<Quiz
  questionNumber={15}
  question="위 SQL의 실행 결과로 옳은 것은?"
  options={['15000', '16000', '15800', '15700']}
  correctAnswer={2}
  explanation="ROUND(숫자, n)에서 n이 음수이면 소수점 왼쪽 자릿수를 반올림한다. n이 -3이면 천의 자리에서 반올림하는데, 15750을 천 단위로 보면 15.75(천)이므로 반올림하면 16(천), 즉 16000이 된다."
  optionExplanations={['버림(TRUNC)으로 계산했을 때 나올 법한 값이다.', '정답이다. 천의 자리 기준으로 반올림하면 16000이다.', '반올림 기준 자릿수를 잘못 적용했을 때 나오는 값이다.', '원래 값의 백의 자리만 0으로 바꿨을 때 나오는 값으로 반올림 규칙을 반영하지 않았다.']}
/>

```sql
SELECT TO_NUMBER('1,250', '9,999') + 100 AS 결과 FROM DUAL;
```

<Quiz
  questionNumber={16}
  question="위 SQL의 실행 결과로 옳은 것은?"
  options={['1250', '1350', '1100', '오류가 발생한다']}
  correctAnswer={2}
  explanation="TO_NUMBER는 지정한 포맷에 맞춰 문자열을 숫자로 변환한다. 콤마 포함 포맷 9,999에 맞춰 1,250이라는 문자열은 숫자 1250으로 변환되고, 여기에 100을 더하면 1350이 된다."
  optionExplanations={['100을 더하는 계산을 빠뜨렸을 때 나오는 값이다.', '정답이다. 1250에 100을 더하면 1350이다.', '변환된 숫자를 잘못 계산했을 때 나오는 값이다.', '포맷 문자열이 콤마가 포함된 값을 정확히 처리하므로 오류 없이 정상 실행된다.']}
/>

[결제] 테이블의 금액 컬럼에는 1000, NULL, 3000이 있다.

```sql
SELECT MAX(금액), MIN(금액) FROM 결제;
```

<Quiz
  questionNumber={17}
  question="위 SQL의 실행 결과로 옳은 것은?"
  options={['MAX 3000, MIN 1000', 'MAX NULL, MIN NULL', 'MAX 3000, MIN NULL', 'MAX 0, MIN 0']}
  correctAnswer={1}
  explanation="MAX, MIN 같은 그룹(집계) 함수는 NULL 값을 계산 대상에서 제외한다. NULL을 제외한 1000과 3000 중 최댓값은 3000, 최솟값은 1000이다."
  optionExplanations={['정답이다. NULL을 제외한 값 중 최댓값 3000, 최솟값 1000이 반환된다.', 'MAX, MIN은 NULL이 섞여도 NULL을 제외하고 정상적으로 값을 계산한다.', 'MIN도 MAX와 마찬가지로 NULL을 제외하고 정상적으로 값을 계산한다.', 'NULL이 있다고 해서 0으로 취급되지 않는다.']}
/>

[예매] 테이블은 (영화ID 1, 결제방법 카드, 수량 2), (영화ID 1, 결제방법 현금, 수량 1), (영화ID 2, 결제방법 카드, 수량 3)으로 구성된다.

```sql
SELECT 영화ID, 결제방법, SUM(수량)
FROM 예매
GROUP BY 영화ID, 결제방법;
```

<Quiz
  questionNumber={18}
  question="위 SQL을 실행했을 때 반환되는 행의 개수는?"
  options={['1개', '2개', '3개', '4개']}
  correctAnswer={3}
  explanation="GROUP BY에 두 컬럼을 지정하면 두 컬럼 값의 조합이 같은 행끼리 묶인다. (영화ID, 결제방법) 조합은 (1,카드), (1,현금), (2,카드) 세 가지뿐이므로 결과는 3행이다."
  optionExplanations={['모든 행을 하나로 묶었을 때 나올 법한 개수로 GROUP BY 컬럼을 잘못 적용한 오답이다.', '영화ID만으로 묶었을 때 나올 법한 개수다.', '정답이다. 서로 다른 조합이 3가지이므로 3행이다.', '실제로 존재하지 않는 조합까지 포함했을 때 나올 수 있는 개수다.']}
/>

<Quiz
  questionNumber={19}
  question="GROUP BY 뒤에 오는 HAVING 절에 대한 설명으로 옳지 않은 것은?"
  options={['HAVING 절에는 집계 함수를 사용한 조건을 쓸 수 있다', 'HAVING 절은 GROUP BY 없이는 절대 사용할 수 없다', 'HAVING 절은 그룹으로 묶인 결과에 대해 조건을 검사한다', 'HAVING 절의 조건에는 그룹화 기준 컬럼도 사용할 수 있다']}
  correctAnswer={2}
  explanation="HAVING은 일반적으로 GROUP BY와 함께 쓰이지만, GROUP BY 없이도 문법적으로 사용할 수 있다. 이 경우 테이블 전체를 하나의 그룹으로 보고 집계 함수 조건을 검사한다. 따라서 절대 사용할 수 없다는 설명은 옳지 않다."
  optionExplanations={['HAVING의 기본 용도로 옳은 설명이다.', '정답이다. GROUP BY 없이도 전체를 한 그룹으로 보고 HAVING을 사용할 수 있어 이 설명은 옳지 않다.', 'HAVING의 기본 동작으로 옳은 설명이다.', '그룹화 기준 컬럼도 HAVING 조건에 사용할 수 있으므로 옳은 설명이다.']}
/>

[예매] 테이블은 (영화ID 1, 수량 5), (영화ID 1, 수량 3), (영화ID 2, 수량 10), (영화ID 2, 수량 1), (영화ID 3, 수량 2)로 구성된다.

```sql
SELECT 영화ID, SUM(수량) AS 총수량
FROM 예매
WHERE 수량 >= 3
GROUP BY 영화ID
HAVING SUM(수량) >= 8;
```

<Quiz
  questionNumber={20}
  question="위 SQL을 실행했을 때 반환되는 결과로 옳은 것은?"
  options={['영화1 8, 영화2 10', '영화1 8만 나온다', '영화2 10만 나온다', '결과가 없다']}
  correctAnswer={1}
  explanation="WHERE 수량이 3 이상인 조건은 그룹을 만들기 전에 개별 행을 먼저 걸러낸다. 남는 행은 (영화1,5), (영화1,3), (영화2,10)이고 영화2의 수량1 행과 영화3의 수량2 행은 이 시점에 제외된다. 남은 행을 영화ID로 묶으면 영화1은 5+3=8, 영화2는 10이 되고, HAVING 절의 수량 합 8 이상 조건을 둘 다 만족해 두 그룹 모두 결과에 남는다."
  optionExplanations={['정답이다. 영화1은 8, 영화2는 10으로 두 그룹 모두 조건을 만족한다.', '영화2도 10으로 조건을 만족하므로 결과에서 빠지면 안 된다.', '영화1도 8로 조건을 만족하므로 결과에서 빠지면 안 된다.', 'WHERE 필터를 적용하지 않았거나 HAVING 조건 계산을 잘못했을 때 나올 법한 오답이다.']}
/>

## 조인·서브쿼리·집합연산자 (21–30번)

[영화] 테이블에는 영화ID 1, 2, 3이 있다. [예매] 테이블에는 영화ID 1, 1, 2에 대한 예매 기록이 있고 영화3은 예매가 없다.

```sql
SELECT M.영화ID, COUNT(E.예매ID) AS 예매건수
FROM 영화 M
LEFT JOIN 예매 E ON M.영화ID = E.영화ID
GROUP BY M.영화ID;
```

<Quiz
  questionNumber={21}
  question="위 SQL을 실행했을 때 영화3의 예매건수 값은?"
  options={['0', 'NULL', '1', '오류가 발생한다']}
  correctAnswer={1}
  explanation="LEFT JOIN으로 영화3은 대응하는 예매가 없어도 예매ID가 NULL인 행 하나로 남는다. COUNT(예매ID)는 NULL을 세지 않으므로, 대응하는 예매가 하나도 없으면 0을 반환한다."
  optionExplanations={['정답이다. COUNT는 대응 행이 없어도 NULL이 아니라 0을 반환한다.', 'COUNT는 세는 대상이 없다고 해서 NULL을 반환하지 않고 0을 반환한다.', '영화3은 예매 기록이 전혀 없으므로 1이 될 수 없다.', 'LEFT JOIN과 COUNT의 조합은 정상적으로 실행되며 오류가 나지 않는다.']}
/>

<Quiz
  questionNumber={22}
  question="같은 [영화], [예매] 테이블에 대해 LEFT JOIN 대신 INNER JOIN으로 그룹화하면, 결과에 포함되는 영화 수는?"
  options={['1개', '2개', '3개', '0개']}
  correctAnswer={2}
  explanation="INNER JOIN은 대응하는 예매가 있는 영화만 결과에 남긴다. 예매 기록이 있는 영화1, 영화2만 남고, 예매가 전혀 없는 영화3은 행 자체가 결과에서 사라진다."
  optionExplanations={['영화1만 남기고 영화2를 빠뜨렸을 때 나올 법한 개수다.', '정답이다. 예매 기록이 있는 영화1, 영화2 두 편만 남는다.', 'LEFT JOIN 결과처럼 예매가 없는 영화3까지 포함했을 때 나올 법한 개수다.', 'INNER JOIN에서도 조건을 만족하는 영화가 존재하므로 0개가 될 수 없다.']}
/>

<Quiz
  questionNumber={23}
  question="[좌석] 4개 행과 [상영시간] 3개 행을 CROSS JOIN하면 반환되는 행의 개수는?"
  options={['7개', '12개', '4개', '3개']}
  correctAnswer={2}
  explanation="CROSS JOIN(카티전 곱)은 조인 조건 없이 한쪽의 모든 행과 다른쪽의 모든 행을 짝짓는다. 결과 행 수는 두 테이블 행 수의 곱인 4곱하기3으로 12개다."
  optionExplanations={['두 테이블의 행 수를 곱하지 않고 더한 값이다.', '정답이다. 4곱하기3은 12다.', '한쪽 테이블의 행 수와 같은 값으로 곱셈을 하지 않은 오답이다.', '다른 한쪽 테이블의 행 수와 같은 값으로 곱셈을 하지 않은 오답이다.']}
/>

<Quiz
  questionNumber={24}
  question="[영화] 테이블에 영화ID, 제목, 전편ID 컬럼이 있고 전편ID는 그 영화의 전편에 해당하는 영화ID를 가리킨다. 다음 SELF JOIN SQL이 조회하는 것으로 가장 적절한 것은? SELECT A.제목 AS 현재작, B.제목 AS 전편 FROM 영화 A JOIN 영화 B ON A.전편ID = B.영화ID"
  options={['각 영화와 그 영화의 전편 제목을 나란히 조회한다', '각 영화와 그 영화의 후속작 제목을 나란히 조회한다', '전편이 없는 영화만 조회한다', '같은 제목을 가진 영화 쌍을 조회한다']}
  correctAnswer={1}
  explanation="A는 현재 영화, B는 A의 전편ID와 영화ID가 일치하는 행이므로 B가 A의 전편에 해당한다. 이 SELF JOIN은 각 영화와 그 영화의 전편 제목을 나란히 보여준다."
  optionExplanations={['정답이다. A.전편ID와 B.영화ID를 연결한 것은 현재작과 전편을 짝짓는 것이다.', '조인 조건이 반대라면 이렇게 해석되지만, 이 SQL은 전편을 B로 두었으므로 방향이 다르다.', 'JOIN(INNER)은 전편ID가 있어야 매칭되므로 전편이 없는 영화는 오히려 결과에서 빠진다.', '제목이 아니라 전편ID와 영화ID를 기준으로 연결했으므로 제목이 같은지는 무관하다.']}
/>

<Quiz
  questionNumber={25}
  question="INNER JOIN, LEFT OUTER JOIN, RIGHT OUTER JOIN, FULL OUTER JOIN에 대한 설명으로 옳지 않은 것은?"
  options={['INNER JOIN은 양쪽에 대응하는 행이 있는 경우만 결과에 포함한다', 'LEFT OUTER JOIN은 왼쪽 테이블의 행을 모두 보존한다', 'RIGHT OUTER JOIN의 결과는 항상 LEFT OUTER JOIN의 결과보다 행 수가 많다', 'FULL OUTER JOIN은 양쪽 테이블에서 대응하지 못한 행까지 모두 포함한다']}
  correctAnswer={3}
  explanation="RIGHT OUTER JOIN과 LEFT OUTER JOIN 중 어느 쪽 행 수가 더 많은지는 데이터에 따라 달라진다. 왼쪽 테이블에 미매칭 행이 더 많으면 LEFT OUTER JOIN 결과가 더 많을 수도 있어, 항상 RIGHT가 많다는 절대적 서술은 옳지 않다."
  optionExplanations={['INNER JOIN의 정의로 옳은 설명이다.', 'LEFT OUTER JOIN의 정의로 옳은 설명이다.', '정답이다. 어느 쪽이 더 많은지는 데이터에 따라 달라지므로 항상 많다는 설명은 옳지 않다.', 'FULL OUTER JOIN의 정의로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={26}
  question="다음 중 다중행 서브쿼리에 사용할 수 없는 연산자는?"
  options={['IN', 'ANY', 'ALL', '단독으로 사용하는 등호(=)']}
  correctAnswer={4}
  explanation="등호(=)는 서브쿼리가 정확히 하나의 값만 반환할 때 사용하는 단일행 비교 연산자다. 서브쿼리가 여러 값을 반환하는 다중행 상황에서는 등호를 그대로 쓸 수 없고, IN이나 등호 ANY처럼 다중행 전용 연산자로 바꿔야 한다."
  optionExplanations={['IN은 다중행 서브쿼리에 사용할 수 있는 대표적인 연산자다.', 'ANY는 다중행 서브쿼리에 사용할 수 있는 연산자다.', 'ALL도 다중행 서브쿼리에 사용할 수 있는 연산자다.', '정답이다. 단독 등호는 서브쿼리가 여러 값을 반환하면 오류가 발생하는 단일행 전용 연산자다.']}
/>

[영화] 테이블에는 영화ID 1, 2, 3이 있다. [예매] 테이블에는 영화ID 1, 1, 3에 대한 예매 기록이 있다.

```sql
SELECT 영화ID FROM 영화 A
WHERE EXISTS (SELECT 1 FROM 예매 B WHERE B.영화ID = A.영화ID);
```

<Quiz
  questionNumber={27}
  question="위 SQL을 실행했을 때 결과로 옳은 것은?"
  options={['영화ID 1, 3', '영화ID 2', '영화ID 1, 2, 3', '결과가 없다']}
  correctAnswer={1}
  explanation="EXISTS는 상관 서브쿼리가 한 건이라도 행을 반환하면 참이 된다. 예매 기록이 있는 영화1과 영화3은 조건을 만족해 결과에 남고, 예매 기록이 없는 영화2는 서브쿼리가 공집합이 되어 제외된다."
  optionExplanations={['정답이다. 예매 기록이 있는 영화1, 영화3이 결과에 남는다.', '영화2는 오히려 예매 기록이 없어 EXISTS 조건을 만족하지 못해 결과에서 제외된다.', '예매 기록이 없는 영화2까지 포함한 오답이다.', '조건을 만족하는 영화가 실제로 존재하므로 결과가 없을 수 없다.']}
/>

[A] 테이블의 영화ID 컬럼에는 1, 2, 3이 있다. [B] 테이블의 영화ID 컬럼에는 2, 3, 4가 있다.

```sql
SELECT 영화ID FROM A
INTERSECT
SELECT 영화ID FROM B;
```

<Quiz
  questionNumber={28}
  question="위 INTERSECT SQL을 실행했을 때 결과로 옳은 것은?"
  options={['영화ID 1', '영화ID 2, 3', '영화ID 4', '영화ID 1, 2, 3, 4']}
  correctAnswer={2}
  explanation="INTERSECT는 두 결과 집합에 공통으로 존재하는 행만 반환하는 교집합 연산자다. A와 B에 모두 있는 값은 2와 3이다."
  optionExplanations={['A에만 있고 B에는 없는 값으로 이는 A MINUS B의 결과다.', '정답이다. 두 집합에 공통으로 존재하는 값은 2와 3이다.', 'B에만 있고 A에는 없는 값으로 이는 B MINUS A의 결과다.', '두 집합을 합친 UNION의 결과와 혼동한 오답이다.']}
/>

[A] 테이블의 좌석번호 컬럼에는 1, 1, 2가 있다. [B] 테이블의 좌석번호 컬럼에는 1, 3이 있다.

```sql
SELECT 좌석번호 FROM A
UNION
SELECT 좌석번호 FROM B;
```

<Quiz
  questionNumber={29}
  question="위 UNION SQL을 실행했을 때 결과로 나오는 값의 개수는?"
  options={['2개', '3개', '4개', '5개']}
  correctAnswer={2}
  explanation="UNION은 두 결과를 합치면서 중복을 제거한다. A의 1, 1, 2와 B의 1, 3을 모두 합쳐도 서로 다른 값은 1, 2, 3 세 가지뿐이다."
  optionExplanations={['서로 다른 값 중 하나(3)를 빠뜨렸을 때 나오는 개수다.', '정답이다. 중복을 모두 제거하면 1, 2, 3 세 개의 값만 남는다.', 'A 안의 중복(1이 두 번)까지 서로 다른 값으로 잘못 셌을 때 나오는 개수다.', 'UNION ALL의 전체 행 개수(3+2=5)와 혼동한 오답이다.']}
/>

<Quiz
  questionNumber={30}
  question="같은 [A], [B] 테이블에 UNION 대신 UNION ALL을 사용하면 반환되는 행의 개수는?"
  options={['3개', '4개', '5개', '6개']}
  correctAnswer={3}
  explanation="UNION ALL은 중복 제거 없이 두 결과를 그대로 이어 붙인다. A의 3행(1, 1, 2)과 B의 2행(1, 3)을 그대로 합치면 3+2=5행이 된다."
  optionExplanations={['UNION의 결과 개수(중복 제거 후)와 혼동한 오답이다.', 'A 안의 중복을 하나로 줄여 계산했을 때 나오는 개수다.', '정답이다. 중복 제거 없이 두 테이블의 행 수를 그대로 더하면 5행이다.', '실제 행 수보다 많게 계산한 오답이다.']}
/>

## 윈도우 함수·GROUP 확장 함수·계층형 질의 (31–40번)

[매출4] 테이블은 영화1(매출 1000), 영화2(매출 3000), 영화3(매출 2000)으로 구성된다.

```sql
SELECT 영화ID, 매출, RANK() OVER (ORDER BY 매출 DESC) AS 순위 FROM 매출4;
```

<Quiz
  questionNumber={31}
  question="위 SQL을 실행했을 때 영화2(매출 3000)의 순위 값은?"
  options={['1', '2', '3', '0']}
  correctAnswer={1}
  explanation="매출을 내림차순으로 정렬하면 3000, 2000, 1000 순서가 된다. 매출이 가장 높은 영화2가 1위가 된다."
  optionExplanations={['정답이다. 매출이 가장 높으므로 순위는 1이다.', '매출 2000인 영화3에 해당하는 순위다.', '매출 1000인 영화1에 해당하는 순위다.', 'RANK는 1부터 시작하며 0이라는 순위는 존재하지 않는다.']}
/>

[월매출] 테이블은 1월 100, 2월 150, 3월 130으로 구성된다.

```sql
SELECT 월, 매출, LAG(매출) OVER (ORDER BY 월) AS 전월매출 FROM 월매출;
```

<Quiz
  questionNumber={32}
  question="위 SQL을 실행했을 때 3월 행의 전월매출 값은?"
  options={['100', '130', '150', 'NULL']}
  correctAnswer={3}
  explanation="LAG(매출)은 정렬 기준으로 바로 이전 행의 값을 가져온다. 3월의 바로 이전 행은 2월이므로 2월의 매출 값인 150이 반환된다."
  optionExplanations={['1월 매출 값으로, 몇 행 앞을 봐야 하는지 잘못 세었을 때 나올 수 있는 오답이다.', '3월 자신의 매출 값으로, LAG는 이전 행 값을 가져오므로 자기 자신의 값이 나올 수 없다.', '정답이다. 3월 바로 이전 행인 2월의 매출 값이다.', '첫 행에서 이전 행이 없을 때 나오는 값과 혼동한 오답이다.']}
/>

```sql
SELECT 월, SUM(매출) OVER (ORDER BY 월 ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS 누적 FROM 월매출;
```

<Quiz
  questionNumber={33}
  question="같은 [월매출] 테이블에 위 SQL을 실행했을 때 3월 행의 누적 값은?"
  options={['130', '250', '380', '280']}
  correctAnswer={3}
  explanation="ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW는 첫 행부터 현재 행까지 더하는 누적 합계 프레임이다. 1월 100, 2월 150, 3월 130을 모두 더하면 380이다."
  optionExplanations={['3월 자신의 매출 값 하나만 반영한 오답이다.', '1월과 2월만 더하고 3월 자신의 값을 빠뜨린 오답이다.', '정답이다. 100+150+130=380이다.', '1월을 빠뜨리고 2월과 3월만 더했을 때 나오는 값이다.']}
/>

[지점매출] 테이블은 (강남, 1분기, 500), (강남, 2분기, 700), (홍대, 1분기, 300)으로 구성된다.

```sql
SELECT 지점, 분기, SUM(매출) OVER (PARTITION BY 지점) AS 지점합계 FROM 지점매출;
```

<Quiz
  questionNumber={34}
  question="위 SQL을 실행했을 때 강남 2분기 행의 지점합계 값은?"
  options={['700', '1200', '500', '1500']}
  correctAnswer={2}
  explanation="PARTITION BY 지점은 지점별로 파티션을 나눠 그 파티션 전체에 대한 집계를 각 행에 표시한다. ORDER BY가 없어 프레임이 파티션 전체이므로, 강남 지점의 모든 행(500과 700)을 더한 1200이 강남에 속한 모든 행에 동일하게 표시된다."
  optionExplanations={['2분기 자신의 매출 값만 반영한 오답으로, PARTITION BY는 파티션 전체를 집계한다는 점을 놓쳤다.', '정답이다. 강남 지점 전체 매출인 500+700=1200이 모든 강남 행에 표시된다.', '1분기 매출 값만 반영한 오답이다.', '홍대 지점 매출까지 포함해 전체를 더했을 때 나오는 값으로 PARTITION BY가 지점별로 나눈다는 점을 반영하지 않았다.']}
/>

```sql
SELECT * FROM (
  SELECT 영화ID, 매출, ROW_NUMBER() OVER (ORDER BY 매출 DESC) AS RN
  FROM 매출4
) WHERE RN <= 1;
```

<Quiz
  questionNumber={35}
  question="위 서브쿼리 기반 SQL이 하는 일로 가장 적절한 것은? ([매출4]는 영화1 1000, 영화2 3000, 영화3 2000이다)"
  options={['매출이 가장 높은 영화 한 편만 조회한다', '매출이 가장 낮은 영화 한 편만 조회한다', '전체 영화를 매출 순으로 정렬만 하고 모두 반환한다', '매출이 동일한 영화가 있으면 아무것도 반환하지 않는다']}
  correctAnswer={1}
  explanation="안쪽 서브쿼리에서 매출 내림차순으로 ROW_NUMBER를 매기면 매출이 가장 높은 행이 1번을 받는다. 바깥 쿼리에서 RN이 1 이하인 행만 걸러내므로 매출이 가장 높은 영화 한 편만 남는다."
  optionExplanations={['정답이다. 내림차순 정렬에서 1번은 매출이 가장 높은 행이다.', '오름차순으로 정렬했다면 이렇게 되지만 이 SQL은 DESC로 정렬했다.', 'WHERE RN <= 1 조건이 있어 한 행만 남고 전체가 반환되지 않는다.', 'ROW_NUMBER는 동점이어도 서로 다른 번호를 매기므로 항상 정확히 한 행이 RN 1을 받는다.']}
/>

[매출5] 테이블은 강남 500, 홍대 300으로 구성된다.

```sql
SELECT 지점, SUM(매출) FROM 매출5 GROUP BY ROLLUP(지점);
```

<Quiz
  questionNumber={36}
  question="위 SQL을 실행했을 때 반환되는 행의 개수는?"
  options={['2개', '3개', '4개', '1개']}
  correctAnswer={2}
  explanation="ROLLUP(지점)은 지점별 소계 각각과 전체 총계 한 행을 만든다. 지점은 강남, 홍대 두 종류이므로 소계 2행에 총계 1행을 더해 총 3행이다."
  optionExplanations={['총계 행을 빠뜨렸을 때 나오는 개수다.', '정답이다. 지점별 소계 2행과 총계 1행을 합쳐 3행이다.', '실제로 만들어지지 않는 조합까지 포함했을 때 나올 법한 개수다.', '총계 하나만 남기고 지점별 소계를 모두 빠뜨렸을 때 나오는 개수다.']}
/>

<Quiz
  questionNumber={37}
  question="ROLLUP과 CUBE의 차이에 대한 설명으로 가장 적절한 것은?"
  options={['ROLLUP은 지정한 컬럼 순서대로 계층적 소계만 만들고, CUBE는 지정한 컬럼들의 모든 조합에 대해 소계를 만든다', 'ROLLUP과 CUBE는 완전히 동일한 결과를 만든다', 'CUBE는 오직 하나의 컬럼에만 사용할 수 있다', 'ROLLUP은 총계 행을 만들지 않지만 CUBE는 총계 행을 만든다']}
  correctAnswer={1}
  explanation="ROLLUP(A, B)은 (A,B), (A), 전체 총계처럼 지정한 순서를 따르는 계층적 소계만 만든다. CUBE(A, B)는 이에 더해 (B)만의 소계까지, 즉 A와 B의 가능한 모든 조합에 대한 소계를 만든다."
  optionExplanations={['정답이다. ROLLUP은 계층적 소계만, CUBE는 모든 조합의 소계를 만든다는 차이를 정확히 설명한다.', 'CUBE는 ROLLUP보다 더 많은 조합의 소계를 만들어 결과가 다르다.', 'CUBE는 여러 컬럼을 지정해 모든 조합을 구할 때 주로 사용한다.', 'ROLLUP과 CUBE 모두 총계 행을 만든다는 점은 같다.']}
/>

[매출5확장] 테이블은 (강남, 1분기, 500), (강남, 2분기, 300), (홍대, 1분기, 200)으로 구성된다.

```sql
SELECT 지점, 분기, SUM(매출) FROM 매출5확장 GROUP BY GROUPING SETS((지점, 분기), ());
```

<Quiz
  questionNumber={38}
  question="위 SQL을 실행했을 때 반환되는 행의 개수는?"
  options={['3개', '4개', '5개', '6개']}
  correctAnswer={2}
  explanation="GROUPING SETS((지점, 분기), ())는 (지점, 분기) 조합별 상세 집계와 전체 총계만 만든다. 실제 존재하는 (지점, 분기) 조합은 3개(강남-1분기, 강남-2분기, 홍대-1분기)이고 총계가 1개이므로 3+1=4행이다."
  optionExplanations={['총계 행을 빠뜨렸을 때 나오는 개수다.', '정답이다. 상세 조합 3행과 총계 1행을 더하면 4행이다.', '실제로 존재하지 않는 조합까지 상세에 포함했을 때 나오는 개수다.', '지점별·분기별 소계까지 잘못 추가했을 때 나오는 개수로, 이 GROUPING SETS는 그 조합을 지정하지 않았다.']}
/>

[장르] 테이블은 장르ID 1(상위장르ID 없음, 드라마), 2(상위장르ID 1, 멜로), 3(상위장르ID 1, 가족), 4(상위장르ID 2, 로맨틱코미디)로 구성된다.

<Quiz
  questionNumber={39}
  question="최상위 장르부터 하위 장르 방향으로 전개하는 계층 질의에서 START WITH 상위장르ID IS NULL을 사용할 때, 옳은 CONNECT BY 절은?"
  options={['CONNECT BY 장르ID = PRIOR 상위장르ID', 'CONNECT BY PRIOR 장르ID = 상위장르ID', 'CONNECT BY PRIOR 상위장르ID = 장르ID', 'CONNECT BY 상위장르ID = 장르ID']}
  correctAnswer={2}
  explanation="하향 전개에서는 PRIOR가 부모 행의 컬럼에 붙는다. 부모의 장르ID(PRIOR 장르ID)가 자식의 상위장르ID와 같은 행을 연결해야 하므로 CONNECT BY PRIOR 장르ID = 상위장르ID가 옳다."
  optionExplanations={['PRIOR의 위치가 반대라 상향 전개가 되어 원하는 하향 전개와 어긋난다.', '정답이다. 부모의 장르ID를 자식의 상위장르ID와 연결하는 하향 전개 조건이다.', 'PRIOR가 상위장르ID에 붙어 상향 전개가 되므로 하향 전개 목적과 맞지 않는다.', 'PRIOR가 없어 부모-자식 관계 자체가 지정되지 않아 계층이 전개되지 않는다.']}
/>

<Quiz
  questionNumber={40}
  question="위 [장르] 테이블 계층 질의에서 로맨틱코미디(장르ID 4)의 LEVEL 값은?"
  options={['1', '2', '3', '4']}
  correctAnswer={3}
  explanation="드라마(LEVEL 1)에서 멜로(LEVEL 2)로, 멜로에서 로맨틱코미디(LEVEL 3)로 이어지는 3단계 계층이다. 루트인 드라마가 1이고 한 단계씩 내려갈 때마다 1씩 증가하므로 로맨틱코미디는 3이다."
  optionExplanations={['루트인 드라마의 LEVEL 값이다.', '멜로의 LEVEL 값이다.', '정답이다. 드라마 1, 멜로 2를 거쳐 로맨틱코미디는 3이다.', '이 계층 구조에는 4단계까지 내려가는 노드가 없다.']}
/>

## PIVOT·CASE·정규표현식·절차형 SQL·DBMS 차이·종합 (41–50번)

<Quiz
  questionNumber={41}
  question="[예매] 테이블의 결제방법별 매출을 카드, 현금 두 컬럼으로 펼쳐 보여주려 할 때 사용할 구문으로 가장 적절한 것은?"
  options={['PIVOT', 'UNPIVOT', 'ROLLUP', 'CONNECT BY']}
  correctAnswer={1}
  explanation="PIVOT은 행으로 저장된 값(결제방법)의 각 항목을 열로 펼쳐 요약 표 형태로 재구성하는 구문이다."
  optionExplanations={['정답이다. 행을 열로 펼치는 것이 PIVOT의 역할이다.', 'UNPIVOT은 반대로 열을 행으로 되돌리는 구문이다.', 'ROLLUP은 소계·총계를 만드는 그룹 확장 함수로 열 펼치기와는 다르다.', 'CONNECT BY는 계층형 질의를 위한 절로 열 펼치기와는 무관하다.']}
/>

[예매] 테이블은 예매ID 1(수량 1), 2(수량 5), 3(수량 10)으로 구성된다.

```sql
SELECT 예매ID,
       CASE WHEN 수량 >= 10 THEN '대량'
            WHEN 수량 >= 5 THEN '보통'
            ELSE '소량'
       END AS 구분
FROM 예매;
```

<Quiz
  questionNumber={42}
  question="위 SQL을 실행했을 때 예매ID 2(수량 5)의 구분 값은?"
  options={['대량', '보통', '소량', 'NULL']}
  correctAnswer={2}
  explanation="CASE WHEN은 위에서부터 순서대로 조건을 검사해 처음 참이 되는 절의 값을 반환한다. 수량 5는 10 이상 조건을 만족하지 못하지만 5 이상 조건은 만족하므로 보통이 반환된다."
  optionExplanations={['수량이 10 이상일 때 나오는 값으로 이 예매(수량 5)와는 조건이 다르다.', '정답이다. 5 이상 10 미만 구간에 해당해 보통이 반환된다.', 'ELSE 절은 앞선 조건을 모두 만족하지 못할 때만 적용되는데, 이 예매는 5 이상 조건을 만족해 ELSE까지 가지 않는다.', 'CASE WHEN에 ELSE 절이 있으면 조건을 만족하는 절이 있는 한 NULL이 반환되지 않는다.']}
/>

<Quiz
  questionNumber={43}
  question="REGEXP_LIKE(이메일, 골뱅이 기호를 포함하고 그 뒤에 도메인이 이어지는 일반적인 이메일 형식 패턴)에 매칭되지 않는 값으로 가장 적절한 것은?"
  options={['user@example.com', 'user.name@example.co.kr', 'userexample.com', 'user@sub.example.com']}
  correctAnswer={3}
  explanation="이메일 형식 패턴은 골뱅이(@) 기호로 아이디와 도메인이 구분되어야 한다는 것을 전제로 한다. userexample.com에는 골뱅이 기호 자체가 없어 이 패턴에 매칭되지 않는다."
  optionExplanations={['골뱅이 기호와 도메인을 모두 갖춘 정상적인 이메일 형식이라 패턴에 매칭된다.', '아이디 부분에 마침표가 있어도 골뱅이 기호와 도메인을 갖췄으므로 패턴에 매칭된다.', '정답이다. 골뱅이 기호가 없어 이메일 형식 패턴에 매칭되지 않는다.', '서브도메인이 있어도 골뱅이 기호와 도메인 형식을 갖췄으므로 패턴에 매칭된다.']}
/>

<Quiz
  questionNumber={44}
  question="프로시저에 대한 설명으로 옳지 않은 것은?"
  options={['IN, OUT, INOUT 파라미터를 사용해 값을 주고받을 수 있다', '프로시저 내부에서 여러 SQL문과 절차적 로직(IF, LOOP 등)을 조합할 수 있다', '프로시저는 SELECT 절 안에서 값처럼 바로 호출해 사용할 수 있다', 'CALL이나 EXECUTE 문으로 명시적으로 실행한다']}
  correctAnswer={3}
  explanation="SELECT 절 안에서 값처럼 바로 호출할 수 있는 것은 반드시 하나의 값을 RETURN하는 사용자 정의 함수다. 프로시저는 반환값이 없어도 되고 CALL·EXECUTE로 별도 실행해야 하므로 SELECT 절 안에서 값처럼 쓸 수 없다."
  optionExplanations={['프로시저 파라미터 방식으로 옳은 설명이다.', '프로시저의 일반적인 특징으로 옳은 설명이다.', '정답이다. SELECT 절 안에서 값처럼 호출하는 것은 함수의 특징이지 프로시저의 특징이 아니다.', '프로시저의 실행 방식으로 옳은 설명이다.']}
/>

<Quiz
  questionNumber={45}
  question="다음 중 기본키 자동 채번 방식에 대한 설명으로 옳은 것은?"
  options={['Oracle은 SEQUENCE 객체를 만들어 NEXTVAL로 다음 값을 가져오고, MySQL은 컬럼에 AUTO_INCREMENT 속성을 지정한다', 'Oracle과 MySQL 모두 AUTO_INCREMENT만 지원한다', 'SEQUENCE는 MySQL 전용 기능이다', 'AUTO_INCREMENT는 한 테이블의 여러 컬럼에 동시에 지정할 수 있다']}
  correctAnswer={1}
  explanation="Oracle은 SEQUENCE 객체를 별도로 만들고 NEXTVAL로 다음 값을 가져와 채번하는 방식을 쓰고, MySQL은 컬럼 자체에 AUTO_INCREMENT 속성을 지정해 자동 증가시키는 방식을 쓴다."
  optionExplanations={['정답이다. Oracle은 SEQUENCE, MySQL은 AUTO_INCREMENT를 사용하는 짝이 맞다.', 'Oracle은 AUTO_INCREMENT가 아니라 SEQUENCE 방식을 사용한다.', 'SEQUENCE는 Oracle에서 널리 쓰이는 기능으로 MySQL 전용이 아니다.', 'AUTO_INCREMENT는 보통 한 테이블에 하나의 컬럼에만 지정할 수 있다.']}
/>

<Quiz
  questionNumber={46}
  question="Oracle의 DUAL 테이블에 대한 설명으로 옳은 것은?"
  options={['SELECT 절에서 테이블 없이 상수나 함수 결과만 조회할 때 사용하는 더미 테이블이다', '사용자가 반드시 데이터를 입력해야 사용할 수 있는 일반 테이블이다', 'MySQL에서도 반드시 FROM DUAL을 써야 SELECT 문이 실행된다', 'DUAL 테이블은 여러 행을 저장하도록 설계되었다']}
  correctAnswer={1}
  explanation="DUAL은 실제 데이터를 담기 위한 테이블이 아니라, 특정 테이블을 조회할 필요 없이 상수나 함수 결과만 확인하고 싶을 때 형식상 FROM 절을 채우기 위해 사용하는 더미(dummy) 테이블이다."
  optionExplanations={['정답이다. DUAL은 상수·함수 결과 확인용 더미 테이블이다.', 'DUAL은 사용자가 데이터를 입력하지 않아도 이미 한 행이 존재하는 상태로 제공된다.', 'MySQL은 FROM 절 없이도 SELECT 1처럼 바로 실행할 수 있어 DUAL이 필수가 아니다.', 'DUAL 테이블은 오직 한 행만 갖도록 설계되었다.']}
/>

[예매] 테이블은 예매ID 1(회원ID 1), 2(회원ID 9, 회원 테이블에 없는 값)로 구성된다. [회원] 테이블에는 회원ID 1, 2가 있다.

```sql
SELECT E.예매ID, M.회원ID
FROM 예매 E, 회원 M
WHERE E.회원ID = M.회원ID;
```

<Quiz
  questionNumber={47}
  question="위 SQL(Oracle 전통 콤마 조인)을 실행했을 때 반환되는 행의 개수는?"
  options={['0개', '1개', '2개', '3개']}
  correctAnswer={2}
  explanation="콤마로 나열한 뒤 WHERE 절에 조인 조건을 쓰는 것은 암묵적 INNER JOIN이다. 예매1(회원ID 1)은 회원 테이블의 회원1과 매칭되지만, 예매2(회원ID 9)는 회원 테이블에 대응하는 값이 없어 결과에서 제외된다. 따라서 결과는 1행이다."
  optionExplanations={['예매1은 회원1과 정상적으로 매칭되므로 결과가 없을 수 없다.', '정답이다. 매칭되는 예매1만 결과에 남는다.', 'INNER JOIN 방식이라 대응하는 값이 없는 예매2는 결과에 포함될 수 없다.', '실제 테이블에 존재하는 행 수보다 많은 개수로 계산한 오답이다.']}
/>

[회원] 테이블은 회원ID 1(가입일 2024년 1월 10일), 2(가입일 2024년 6월 15일)로 구성된다.

```sql
SELECT 회원ID, EXTRACT(MONTH FROM 가입일) AS 가입월
FROM 회원
WHERE EXTRACT(MONTH FROM 가입일) <= 3;
```

<Quiz
  questionNumber={48}
  question="위 SQL을 실행했을 때 결과로 옳은 것은?"
  options={['회원ID 1만 나온다', '회원ID 2만 나온다', '회원ID 1, 2 모두 나온다', '결과가 없다']}
  correctAnswer={1}
  explanation="EXTRACT(MONTH FROM 가입일)은 가입일에서 월만 뽑아낸다. 회원1은 1월 가입이라 3 이하 조건을 만족하지만, 회원2는 6월 가입이라 조건을 만족하지 못해 제외된다."
  optionExplanations={['정답이다. 1월 가입인 회원1만 조건을 만족한다.', '6월 가입인 회원2는 3 이하 조건을 만족하지 못해 결과에서 제외되어야 한다.', '회원2는 조건을 만족하지 못하므로 결과에 포함될 수 없다.', '회원1은 조건을 만족하는 데이터가 실제로 존재하므로 결과가 없을 수 없다.']}
/>

[A] 테이블의 값 컬럼에는 1, NULL, 3이 있다. [B] 테이블의 값 컬럼에는 3, 4가 있다.

```sql
SELECT 값 FROM A
MINUS
SELECT 값 FROM B;
```

<Quiz
  questionNumber={49}
  question="위 MINUS SQL을 실행했을 때 결과로 옳은 것은?"
  options={['1', '1, NULL', 'NULL만', '결과가 없다']}
  correctAnswer={2}
  explanation="MINUS는 A의 값 중에서 B에도 있는 값을 제외한 나머지를 반환한다. A의 3은 B에도 있어 제외되고, 1은 B에 없는 값이라 남는다. B에는 NULL이 없으므로 A의 NULL 값은 어떤 값과도 제외 대상으로 매칭되지 않아 그대로 결과에 남는다."
  optionExplanations={['NULL 행이 결과에서 사라진다고 착각한 오답으로, 실제로는 B에 NULL이 없어 A의 NULL이 그대로 남는다.', '정답이다. B에 없는 1과, B에 대응 값이 없는 NULL이 함께 남는다.', '1을 결과에서 빠뜨린 오답이다.', '실제로 B에 없는 값이 존재하므로 결과가 없을 수 없다.']}
/>

[매출4] 테이블은 영화1(매출 1000), 영화2(매출 3000), 영화3(매출 2000)으로 구성된다.

```sql
SELECT * FROM (
  SELECT 영화ID, 매출, RANK() OVER (ORDER BY 매출 DESC) AS 순위
  FROM 매출4
) WHERE 순위 <= 2;
```

<Quiz
  questionNumber={50}
  question="위 SQL을 실행했을 때 결과에 포함되는 영화로 옳은 것은?"
  options={['영화2, 영화3', '영화1, 영화2', '영화1, 영화3', '영화1, 영화2, 영화3 모두']}
  correctAnswer={1}
  explanation="매출 내림차순으로 순위를 매기면 영화2(3000)가 1위, 영화3(2000)이 2위, 영화1(1000)이 3위가 된다. 바깥 쿼리에서 순위가 2 이하인 행만 남기므로 영화2와 영화3이 결과에 남고, 3위인 영화1은 제외된다."
  optionExplanations={['정답이다. 1위 영화2와 2위 영화3이 순위 2 이하 조건을 만족한다.', '영화1은 매출이 가장 낮아 3위이므로 순위 2 이하 조건을 만족하지 못해 제외되어야 한다.', '영화2가 아니라 영화1을 포함시킨 오답으로, 순위 계산을 잘못했다.', '영화1은 3위라 조건을 만족하지 못해 전체가 아니라 상위 2편만 남아야 한다.']}
/>

## 채점 후 복습 동선

50문항을 모두 푼 뒤 아래 표로 틀린 문항 번호대를 확인하고, 해당 범위에서 5문항 이상 틀렸다면 지정된 편으로 돌아가 개념부터 다시 확인하십시오.

| 틀린 문항 범위 | 다루는 개념 | 복습할 편 |
| --- | --- | --- |
| 1–10 | 엔터티·속성·식별자·정규화·트랜잭션·DCL | 03–09편 |
| 11–20 | DDL·DML·TCL·WHERE·함수·HAVING과 WHERE 차이 | 10–15편 |
| 21–30 | 조인 종류·서브쿼리·집합연산자 | 16–18편 |
| 31–40 | 윈도우 함수·GROUPING SETS·ROLLUP·CUBE·계층형 질의 | 19–22편 |
| 41–50 | PIVOT·CASE WHEN·정규표현식·절차형 SQL·DBMS 문법 차이 | 23–25편 |

## 합격 기준 안내

SQLD는 데이터 모델링 10문항(20점)과 SQL 기본 및 활용 40문항(80점), 총 100점 만점이며 두 조건을 동시에 만족해야 합격합니다.

- **과락 방지**: 데이터 모델링에서 4문항(8점) 미만, SQL에서 16문항(32점) 미만으로 맞히면 과목별 40퍼센트(40%) 기준 미달로 과락 처리되어 총점과 무관하게 불합격합니다.
- **평균 합격선**: 두 과목 점수를 합쳐 60점(전체 30문항) 이상이어야 합니다.

이 회차는 전 범위를 균형 있게 배분한 마지막 점검용입니다. 특정 영역에서만 틀렸다면 그 영역이 실제 시험에서도 취약 지점일 가능성이 높으므로, 위 복습 동선표를 따라 시험 직전까지 해당 편을 다시 훑으십시오. 전 영역에서 고르게 틀렸다면 시간 배분 문제일 수 있으니, 90분 동안 자신 있는 영역부터 먼저 풀고 계산이 오래 걸리는 문항은 표시만 해두고 넘어가는 순서 전략을 다시 점검하십시오.

## 참고 자료

- [데이터자격시험 - SQLD 출제기준](https://www.dataq.or.kr/www/sub/a_04.do)
- [Oracle Database SQL Language Reference](https://docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/)
- [Oracle - Analytic Functions](https://docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/Analytic-Functions.html)
- [PostgreSQL - GROUPING SETS 문서](https://www.postgresql.org/docs/current/queries-table-expressions.html#QUERIES-GROUPING-SETS)

---
title: '27. 기출 유형 집중 연습 1회 — 데이터 모델링'
description: '62·60·58·37회 기출을 분석해 만든 1과목 집중 연습 15문제입니다. 엔티티 분류, ERD 표기법, 식별자, 정규화 핵심 유형을 모두 담았습니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 1과목(데이터 모델링의 이해)에서 반복 출제되는 핵심 유형 15개를 기출 스타일 그대로 풀어봅니다. 풀이 후 틀린 문제는 해설을 통해 개념을 완전히 정리하세요.
</Callout>

## 풀이 전 체크

- 엔티티 3분류(기본/중심/행위)와 대표 예시를 암기했는가?
- IE 표기법 기호(단선, 원, 까마귀 발)의 의미를 설명할 수 있는가?
- 식별 관계(실선)와 비식별 관계(점선)의 차이를 말할 수 있는가?
- 1NF/2NF/3NF 각 단계에서 제거하는 종속성 유형을 구분할 수 있는가?

---

## 문제

<Quiz questionNumber={1} question="발생 시점에 따른 엔티티 분류에서 나머지 셋과 다른 하나는?" options={['주문내역과 고객 사이에서 발생한 주문 엔티티', '기업 간에 맺은 계약 내용을 기록한 계약 엔티티', '인적자원을 관리하기 위해 존재하는 사원 엔티티', '일별 매출을 집계하여 생성되는 매출집계 엔티티']} correctAnswer={3} explanation="사원 엔티티는 업무에 원래부터 독립적으로 존재하는 기본(Key) 엔티티입니다. 나머지 주문, 계약, 매출집계는 모두 다른 엔티티 간의 행위나 결과로 발생하는 행위 엔티티 또는 중심 엔티티에 해당합니다." optionExplanations={['주문은 고객과 상품 사이의 행위 결과로 발생하는 행위 엔티티입니다.', '계약은 두 기업 사이의 행위 결과로 발생하므로 행위 엔티티에 해당합니다.', '사원은 업무에 처음부터 독립적으로 존재하는 기본 엔티티입니다. 발생 시점 분류에서 이질적입니다.', '매출집계는 주문 등의 행위 결과를 집계한 것으로 중심 또는 행위 엔티티입니다.']} />

<Quiz questionNumber={2} question="엔티티의 특징에 대한 설명으로 옳지 않은 것은?" options={['업무 프로세스에서 반드시 사용되어야 한다.', '두 개 이상의 인스턴스가 있어야 한다.', '반드시 주식별자를 가져야 한다.', '속성이 하나도 없어도 엔티티가 될 수 있다.']} correctAnswer={4} explanation="엔티티는 최소 하나 이상의 속성을 가져야 한다는 요건이 있습니다. 속성이 전혀 없으면 엔티티의 정의를 충족하지 못합니다. 다만 일반 속성이 없더라도 주식별자만 있으면 엔티티가 성립할 수 있으나, 속성 자체가 '하나도 없다'면 식별자도 없는 것과 같으므로 엔티티가 될 수 없습니다." optionExplanations={['업무에서 사용되지 않는 엔티티는 불필요한 엔티티이므로 이 설명은 맞습니다.', '엔티티는 두 개 이상의 인스턴스가 있어야 의미가 있으므로 맞는 설명입니다.', '주식별자가 없으면 각 인스턴스를 구분할 수 없어 엔티티가 될 수 없습니다. 맞는 설명입니다.', '속성이 하나도 없으면 주식별자도 없는 상태이므로 엔티티가 될 수 없습니다. 이것이 틀린 설명입니다.']} />

<Quiz questionNumber={3} question="속성명 부여 원칙으로 옳지 않은 것은?" options={['가능한 약어를 사용하지 않는다.', '전체 데이터 모델에서 가급적 중복되지 않게 유일성을 확보한다.', '업무에서 실제 사용하는 용어를 사용한다.', '가급적 서술적이고 길게 속성명을 부여한다.']} correctAnswer={4} explanation="속성명은 서술적이고 길게 작성하는 것이 아니라 단순하고 명확한 명사형으로 부여해야 합니다. 서술적 표현은 가독성을 떨어뜨리고 모델 관리를 어렵게 합니다." optionExplanations={['약어는 이해도를 낮추므로 가급적 사용하지 않는 것이 원칙입니다. 맞는 설명입니다.', '유일성을 확보해야 속성 간 혼동을 막을 수 있습니다. 맞는 설명입니다.', '업무 용어를 사용해야 현장과의 소통이 원활해집니다. 맞는 설명입니다.', '속성명은 서술적이 아닌 단순하고 명확한 명사형이어야 합니다. 이것이 틀린 설명입니다.']} />

<Quiz questionNumber={4} question="다음 ERD 표기를 올바르게 해석한 것은? (IE 표기법: 고객 ──|────O◁ 주문)" options={['고객과 주문은 일대일 관계이며 양쪽 모두 필수 참여이다.', '일대다 관계이며 고객은 필수 참여, 주문은 선택 참여이다.', '일대다 관계이며 고객은 선택 참여, 주문은 필수 참여이다.', '다대다 관계이며 양쪽 모두 선택 참여이다.']} correctAnswer={3} explanation="IE 표기법에서 고객 쪽의 O(원)는 선택 참여(최솟값 0), 단선(|)은 최댓값 1을 의미합니다. 주문 쪽의 단선(|)은 필수 참여(최솟값 1), 까마귀 발(◁)은 최댓값 N을 의미합니다. 따라서 고객은 주문이 0건일 수 있고, 주문은 반드시 하나의 고객에 귀속됩니다." optionExplanations={['까마귀 발 기호가 있으므로 일대일이 아닌 일대다 관계입니다.', '고객 쪽의 원(O) 기호는 선택 참여를 의미합니다. 고객과 주문의 선택성이 반대입니다.', '고객 쪽: 원(O)=선택, 주문 쪽: 단선(|)=필수이므로 이 해석이 정확합니다.', '까마귀 발이 한쪽에만 있으므로 일대다 관계입니다. 다대다가 아닙니다.']} />

<Quiz questionNumber={5} question="IE 표기법에서 식별 관계와 비식별 관계를 표현하는 방법으로 올바른 것은?" options={['식별 관계는 점선, 비식별 관계는 실선으로 표현한다.', '식별 관계는 실선, 비식별 관계는 점선으로 표현한다.', '둘 다 실선으로 표현하며 기호로 구분한다.', '둘 다 점선으로 표현하며 화살표로 구분한다.']} correctAnswer={2} explanation="IE 표기법에서 식별 관계(Identifying Relationship)는 실선으로, 비식별 관계(Non-Identifying Relationship)는 점선으로 표현합니다. 식별 관계는 부모의 주식별자가 자식의 주식별자 구성에 포함되는 경우입니다." optionExplanations={['반대입니다. 식별 관계가 실선, 비식별 관계가 점선입니다.', '식별 관계=실선, 비식별 관계=점선이 맞는 설명입니다.', '선의 종류(실선/점선) 자체가 식별/비식별을 구분합니다.', '점선은 비식별 관계에만 사용합니다.']} />

<Quiz questionNumber={6} question="두 개 이상의 부모 엔티티 중 어느 한 시점에는 오직 하나의 부모와만 관계를 가져야 하는 제약 조건을 무엇이라 하는가?" options={['식별 관계', '비식별 관계', '순환 관계', '상호배타적 관계']} correctAnswer={4} explanation="상호배타적 관계(Exclusive Relationship, ARC 관계)는 두 관계 중 하나만 선택적으로 성립할 수 있는 구조입니다. 예를 들어 '법인 차량'이거나 '개인 차량' 둘 중 하나에만 속하는 관계가 이에 해당합니다. A 관계를 선택하면 B 관계의 외래키는 반드시 NULL이어야 합니다." optionExplanations={['식별 관계는 부모 식별자가 자식의 주식별자에 포함되는 관계입니다.', '비식별 관계는 부모 식별자가 자식의 일반 속성(FK)으로 상속되는 관계입니다.', '순환 관계는 하나의 엔티티가 자기 자신과 관계를 맺는 구조입니다.', '상호배타적 관계는 여러 관계 중 동시에 하나만 성립할 수 있는 배타 구조입니다.']} />

<Quiz questionNumber={7} question="주식별자의 특성에 대한 설명으로 옳지 않은 것은?" options={['모든 인스턴스를 유일하게 구분할 수 있어야 한다(유일성).', '가능한 한 최소한의 속성 수로 구성되어야 한다(최소성).', 'NULL 값을 가질 수 없다(존재성).', '업무 변화에 따라 유연하게 변경할 수 있다(가변성).']} correctAnswer={4} explanation="주식별자는 한 번 정의되면 업무가 변해도 변경되어서는 안 됩니다. 이것을 불변성(Immutability)이라 합니다. 주식별자가 자주 변경되면 참조 무결성이 깨지고 시스템 전반에 영향을 미칩니다." optionExplanations={['유일성은 주식별자의 핵심 특성입니다. 맞는 설명입니다.', '최소성은 불필요한 속성을 제거하여 최소한의 조합으로 유일성을 만족하는 특성입니다.', '존재성(NOT NULL)은 주식별자가 반드시 값을 가져야 함을 의미합니다.', '주식별자는 불변성을 가져야 합니다. 가변성이 있다는 설명은 틀렸습니다.']} />

<Quiz questionNumber={8} question="다음 중 제3정규형(3NF)을 위반하는 상황은?" options={['주문번호와 상품코드의 조합으로 상품명이 결정된다.', '학번으로 학생명과 학과코드가 결정되고, 학과코드로 학과명이 결정된다.', '사원번호로 사원명이 결정되고, 부서번호로 부서명이 결정된다.', '주문번호로만 고객번호와 배송지가 결정된다.']} correctAnswer={2} explanation="3NF 위반은 이행적 함수 종속이 있는 경우입니다. 학번 → 학과코드 → 학과명 구조에서 학번에서 학과코드를 거쳐 학과명이 결정되는 이행 종속이 발생합니다. 이를 해소하려면 학과 테이블을 별도로 분리해야 합니다." optionExplanations={['복합 기본키 중 일부에만 종속되는 경우로 2NF 위반에 해당합니다.', '학번→학과코드→학과명의 이행 종속이 있어 3NF를 위반합니다. 정답입니다.', '기본키(사원번호, 부서번호)와의 직접 종속 관계이므로 정규화 위반이 아닙니다.', '기본키(주문번호)에 직접 종속하는 관계로 정규화 위반이 아닙니다.']} />

<Quiz questionNumber={9} question="다음 중 파생속성(Derived Attribute)에 해당하는 것은?" options={['사원번호 — 시스템이 자동 부여하는 일련번호', '고객명 — 고객 등록 시 직접 입력하는 이름', '주문합계금액 — 주문 건별 단가×수량을 합산한 값', '생년월일 — 고객이 직접 입력하는 출생 연월일']} correctAnswer={3} explanation="파생속성은 다른 속성의 값을 계산하거나 가공하여 저장하는 속성입니다. 주문합계금액은 단가와 수량이라는 다른 속성으로부터 계산되므로 대표적인 파생속성입니다. 파생속성은 원본 속성이 변경되면 정합성 관리가 필요하므로 최소화하는 것이 바람직합니다." optionExplanations={['시스템이 자동 부여하는 번호는 설계속성(인조식별자)에 해당합니다.', '사용자가 직접 입력하는 이름은 기본속성입니다.', '다른 속성(단가, 수량)을 계산하여 만들어지는 값으로 파생속성입니다. 정답입니다.', '직접 입력하는 날짜는 기본속성입니다.']} />

<Quiz questionNumber={10} question="인조 식별자(Artificial Identifier)에 대한 설명으로 옳은 것은?" options={['업무에서 자연스럽게 사용되어 사용자가 쉽게 인지할 수 있는 식별자이다.', '여러 속성을 조합하여 만든 복합 식별자만을 의미한다.', '업무적 의미와 관계없이 관리 편의를 위해 인위적으로 부여한 식별자이다.', '반드시 숫자형 일련번호여야 한다.']} correctAnswer={3} explanation="인조 식별자는 업무 의미와 무관하게 시스템·관리 편의를 위해 생성한 식별자입니다. 예를 들어 주문번호를 ORDER_0001처럼 임의로 부여하는 경우가 해당합니다. 자연 식별자는 업무에서 실제로 사용하는 고객주민번호, 상품바코드 등입니다." optionExplanations={['업무에서 자연스럽게 사용되는 식별자는 자연 식별자(Natural Identifier)입니다.', '인조 식별자는 단일 속성인 경우가 많으며 복합 식별자를 의미하지 않습니다.', '업무 의미 없이 관리 목적으로 인위 부여하는 것이 인조 식별자입니다. 정답입니다.', '인조 식별자가 꼭 숫자형일 필요는 없습니다.']} />

<Quiz questionNumber={11} question="피터 첸(Peter Chen) 표기법에서 엔티티, 속성, 관계를 나타내는 기호를 순서대로 올바르게 나열한 것은?" options={['타원 — 사각형 — 마름모', '사각형 — 타원 — 마름모', '마름모 — 사각형 — 타원', '사각형 — 마름모 — 타원']} correctAnswer={2} explanation="Peter Chen 표기법에서 엔티티는 사각형(□), 속성은 타원(○), 관계는 마름모(◇)로 표현합니다. 이 표기법은 ER 다이어그램의 원형으로 현재는 IE 표기법이나 바커(Barker) 표기법이 실무에서 더 많이 사용됩니다." optionExplanations={['타원이 먼저 나오면 속성이 먼저가 되므로 순서가 틀렸습니다.', '사각형=엔티티, 타원=속성, 마름모=관계 순으로 정확합니다.', '마름모가 먼저면 관계가 먼저가 되므로 순서가 틀렸습니다.', '사각형과 마름모 사이에 타원(속성)이 와야 하는 순서로 틀렸습니다.']} />

<Quiz questionNumber={12} question="고객과 상품 사이에 다대다(M:N) 관계가 존재할 때 이를 관계형 데이터베이스에서 처리하는 올바른 방법은?" options={['고객 테이블에 상품 컬럼을 여러 개 추가한다.', '상품 테이블에 고객 컬럼을 여러 개 추가한다.', '고객과 상품 사이에 교차(관계) 엔티티를 생성하여 분해한다.', '다대다 관계는 데이터베이스에서 직접 지원하므로 별도 처리가 필요 없다.']} correctAnswer={3} explanation="관계형 데이터베이스는 다대다(M:N) 관계를 직접 지원하지 않습니다. 주문 또는 구매이력 같은 교차 엔티티(Intersection Entity, 연결 엔티티)를 생성하여 두 개의 일대다(1:N) 관계로 분해해야 합니다." optionExplanations={['컬럼을 여러 개 추가하면 1NF를 위반하는 반복 그룹이 됩니다.', '상품 테이블에 고객 컬럼을 여러 개 추가하는 것도 1NF 위반입니다.', '교차 엔티티를 생성하여 M:N을 두 개의 1:N으로 분해합니다. 정답입니다.', '관계형 DB는 M:N을 직접 지원하지 않아 반드시 분해가 필요합니다.']} />

<Quiz questionNumber={13} question="한 명의 직원이 여러 개의 자격증을 보유할 수 있는 상황에서 자격증을 다중값 속성으로 저장할 때의 문제점과 올바른 해결 방법은?" options={['구분자로 이어 한 컬럼에 저장하면 조회가 편리하므로 권장된다.', 'NULL 값으로 처리하면 다중값을 효율적으로 관리할 수 있다.', '컬럼을 여러 개 추가하여 최대 개수만큼 준비한다.', '별도의 자격증 엔티티를 생성하고 직원과 1:N 관계로 분리한다.']} correctAnswer={4} explanation="다중값 속성(Multi-Valued Attribute)은 제1정규형(1NF)을 위반합니다. 올바른 처리는 별도 엔티티로 분리하여 정규화하는 것입니다. 직원-자격증 관계에서 자격증 엔티티를 만들고 직원 1:N 자격증 관계로 표현합니다." optionExplanations={['구분자로 이어 저장하면 원자값 원칙(1NF)을 위반하고 검색이 어려워집니다.', 'NULL로 처리하면 여러 값을 표현할 수 없고 데이터 손실이 발생합니다.', '최대 개수만큼 컬럼을 준비하는 것도 1NF 위반이며 유연성이 없습니다.', '별도 엔티티 분리가 정규화의 정석적인 해결 방법입니다. 정답입니다.']} />

<Quiz questionNumber={14} question="비식별 관계(Non-Identifying Relationship)에 대한 설명으로 옳은 것을 모두 고른 것은? (가: 부모의 주식별자가 자식의 일반 속성으로 상속된다. 나: ERD에서 점선으로 표현한다. 다: 자식 엔티티는 독립적인 주식별자를 가진다.)" options={['가, 나', '가, 나, 다', '나, 다', '가만 옳다']} correctAnswer={2} explanation="비식별 관계의 세 가지 특성이 모두 옳습니다. 부모의 식별자가 자식의 FK(일반 속성)로만 상속되고, ERD에서 점선으로 표현하며, 자식은 자신만의 주식별자를 별도로 구성합니다. 이와 대비하여 식별 관계는 부모의 식별자가 자식의 주식별자에 포함되고 실선으로 표현합니다." optionExplanations={['가와 나만 고르면 다(독립 식별자)가 빠집니다. 셋 모두 맞습니다.', '가, 나, 다 모두 비식별 관계의 정확한 특성입니다. 정답입니다.', '나와 다만 고르면 가(일반 속성으로 상속)가 빠집니다.', '가만 선택하면 나, 다의 중요한 특성이 빠집니다.']} />

<Quiz questionNumber={15} question="다음 중 제2정규형(2NF)에서 제거하는 종속성 유형은?" options={['이행적 함수 종속(A→B→C)', '완전 함수 종속(기본키 전체가 결정)', '부분 함수 종속(기본키 일부로 결정)', '다중값 종속(1NF 위반)']} correctAnswer={3} explanation="2NF는 완전 함수 종속을 만족시키기 위해 부분 함수 종속을 제거합니다. 복합 기본키에서 키의 일부분만으로 다른 속성이 결정되는 경우가 부분 함수 종속에 해당합니다. 이행적 함수 종속 제거는 3NF이며, 다중값 종속 제거는 1NF에 해당합니다." optionExplanations={['이행 함수 종속 제거는 3NF의 목표입니다.', '완전 함수 종속은 2NF가 달성하려는 목표 상태이지, 제거 대상이 아닙니다.', '부분 함수 종속을 제거하는 것이 2NF의 목표입니다. 정답입니다.', '다중값 종속(반복 그룹)을 제거하는 것은 1NF의 목표입니다.']} />

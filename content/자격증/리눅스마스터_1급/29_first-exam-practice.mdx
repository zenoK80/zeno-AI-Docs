---
title: '29. 1차 객관식 실전 점검'
description: '리눅스마스터 1급 1차 시험 형식의 모의 10문제로 핵심 개념을 최종 점검합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 1차 시험(객관식 4지선다, 60문제, 60분, 60점 이상 합격) 형식으로 10문제를 풀며 핵심 개념을 최종 점검합니다. 각 문제의 해설을 통해 오개념도 바로잡습니다.
</Callout>

## 1차 시험 안내

| 항목 | 내용 |
|------|------|
| 문제 형식 | 객관식 4지선다 |
| 문제 수 | 60문제 |
| 시험 시간 | 60분 |
| 합격 기준 | 60점 이상 (36문제 이상 정답) |
| 출제 범위 | 리눅스 일반, 리눅스 시스템 관리, 네트워크 및 서비스 |

<Callout type="warning">
  **전략:** 모르는 문제는 과감하게 넘기고, 아는 문제를 확실히 맞추는 것이 중요합니다. 문제당 평균 1분 배정이므로 시간 관리에 주의하세요.
</Callout>

## 모의 10문제

---

### 문제 1

<Quiz
  questionNumber={1}
  question="chmod 754 파일명 명령을 실행했을 때 설정되는 권한으로 올바른 것은?"
  options={['① 소유자: rwx, 그룹: r-x, 기타: r--', '② 소유자: rwx, 그룹: r--, 기타: r-x', '③ 소유자: r-x, 그룹: rwx, 기타: r--', '④ 소유자: rwx, 그룹: r-x, 기타: ---']}
  correctAnswer={1}
  explanation="8진수 권한 표기법에서 각 자리는 순서대로 소유자(user), 그룹(group), 기타(others)입니다. 숫자별 권한: 7=rwx, 5=r-x, 4=r-- 이므로 754는 소유자(7=rwx), 그룹(5=r-x), 기타(4=r--)입니다. ls -l 결과: -rwxr-xr-- 로 표시됩니다."
  optionExplanations={['① 정답입니다. 7=rwx, 5=r-x, 4=r--이므로 소유자:rwx, 그룹:r-x, 기타:r--가 됩니다.', '② 그룹(5=r-x)과 기타(4=r--)의 순서가 뒤바뀐 오답입니다.', '③ 소유자(7=rwx)와 그룹(5=r-x)의 순서가 뒤바뀐 오답입니다.', '④ 기타 권한이 ---이 되려면 4가 아닌 0이어야 합니다.']}
/>

---

### 문제 2

<Quiz
  questionNumber={2}
  question="/etc/fstab의 각 필드 순서로 올바른 것은?"
  options={['① 장치명 - 마운트포인트 - 파일시스템 - 옵션 - dump - pass', '② 마운트포인트 - 장치명 - 파일시스템 - pass - dump - 옵션', '③ 파일시스템 - 장치명 - 마운트포인트 - 옵션 - dump - pass', '④ 장치명 - 파일시스템 - 마운트포인트 - 옵션 - dump - pass']}
  correctAnswer={1}
  explanation="/etc/fstab의 6개 필드 순서는 '장치명(또는UUID) 마운트포인트 파일시스템 옵션 dump pass'입니다. 장치명=/dev/sdXN 또는 UUID=..., LABEL=... 형식 사용. dump=0(덤프안함)/1(덤프). pass=0(검사안함)/1(루트먼저)/2(이후검사)."
  optionExplanations={['① 정답입니다. 장치명-마운트포인트-파일시스템-옵션-dump-pass 순서가 올바릅니다.', '② 마운트포인트와 장치명의 순서가 바뀌었고 dump와 pass 순서도 잘못되었습니다.', '③ 파일시스템이 맨 앞에 오는 것은 잘못된 순서입니다.', '④ 파일시스템과 마운트포인트의 순서가 바뀐 오답입니다.']}
/>

---

### 문제 3

<Quiz
  questionNumber={3}
  question="crontab 항목 '0 3 * * 0 /usr/bin/backup.sh'의 의미로 올바른 것은?"
  options={['① 매일 오전 3시 0분에 실행', '② 매주 일요일 오전 3시 0분에 실행', '③ 매월 3일 자정에 실행', '④ 매시간 0분과 3분에 실행']}
  correctAnswer={2}
  explanation="crontab 필드 순서는 '분 시 일 월 요일'입니다. 항목 분석: 0(분)=0분에, 3(시)=오전3시에, *(일)=매일, *(월)=매월, 0(요일)=일요일(0과 7 모두 일요일을 의미). 따라서 매주 일요일 오전 3시 0분에 실행됩니다. 자주 출제되는 예: '0 0 * * *'=매일자정, '0 9 * * 1-5'=평일오전9시, '0 0 1 * *'=매월1일자정."
  optionExplanations={['① 매일 실행되려면 요일 필드가 *이어야 하는데, 마지막 필드가 0(일요일)이므로 틀렸습니다.', '② 정답입니다. 분=0, 시=3, 요일=0(일요일)이므로 매주 일요일 오전 3시 0분에 실행됩니다.', '③ 매월 3일 자정이려면 "0 0 3 * *" 형식이어야 합니다.', '④ 매시간 여러 분에 실행하려면 분 필드에 콤마나 범위를 사용해야 합니다.']}
/>

---

### 문제 4

<Quiz
  questionNumber={4}
  question="RAID 5를 구성하기 위해 필요한 최소 디스크 수는?"
  options={['① 1개', '② 2개', '③ 3개', '④ 4개']}
  correctAnswer={3}
  explanation="RAID 유형별 최소 디스크 수: RAID 0=2개(스트라이핑, 장애허용없음), RAID 1=2개(미러링, 1개장애허용), RAID 5=3개(분산패리티, 1개장애허용), RAID 6=4개(이중패리티, 2개장애허용), RAID 10=4개(미러스트라이핑). RAID 5 실제 사용 가능 용량=(n-1)×디스크1개용량. 예: 3개×1TB=총3TB, 사용가능 2TB."
  optionExplanations={['① RAID는 복수 디스크를 사용하는 기술이므로 1개로는 구성할 수 없습니다.', '② 2개 디스크로 구성 가능한 것은 RAID 0과 RAID 1입니다.', '③ 정답입니다. RAID 5는 데이터 스트라이핑과 분산 패리티를 위해 최소 3개 디스크가 필요합니다.', '④ 4개는 RAID 6이나 RAID 10에 필요한 최소 디스크 수입니다.']}
/>

---

### 문제 5

<Quiz
  questionNumber={5}
  question="ss -tuln 명령 출력에서 LISTEN 상태의 의미로 올바른 것은?"
  options={['① 현재 데이터 전송 중인 활성 연결', '② 원격 측의 연결 종료를 기다리는 상태', '③ 특정 포트에서 들어오는 연결 요청을 기다리는 상태', '④ 연결이 완전히 종료된 상태']}
  correctAnswer={3}
  explanation="소켓 상태 설명: LISTEN=포트를 열고 연결 요청 대기 중(서버 소켓), ESTABLISHED=연결이 수립되어 데이터 통신 중, TIME_WAIT=연결 종료 후 남은 패킷 처리를 위한 대기, CLOSE_WAIT=상대방이 종료했고 내 측 종료 대기, SYN_RECV=SYN 패킷 수신 후 ACK 대기 중. ss 옵션: -t(TCP), -u(UDP), -l(LISTEN 상태만), -n(숫자로 표시)."
  optionExplanations={['① 데이터 전송 중인 활성 연결은 ESTABLISHED 상태입니다.', '② 원격 측의 연결 종료를 기다리는 상태는 CLOSE_WAIT입니다.', '③ 정답입니다. LISTEN은 서버 소켓이 특정 포트에서 클라이언트의 연결 요청을 기다리는 상태입니다.', '④ 연결 종료 후 대기 상태는 TIME_WAIT이며, 완전 종료는 CLOSED 상태입니다.']}
/>

---

### 문제 6

<Quiz
  questionNumber={6}
  question="SELinux Enforcing 모드와 Permissive 모드의 차이로 올바른 것은?"
  options={['① Enforcing은 SELinux를 완전히 비활성화하고, Permissive는 로그만 기록한다', '② Enforcing은 정책 위반을 차단하고 로그를 기록하며, Permissive는 차단 없이 로그만 기록한다', '③ Enforcing과 Permissive 모두 정책을 강제 적용하지만, Permissive는 경고만 표시한다', '④ Permissive는 정책을 강제 적용하고, Enforcing은 로그만 기록한다']}
  correctAnswer={2}
  explanation="SELinux 세 가지 모드: Enforcing(정책 위반 차단+로그 기록, 운영환경), Permissive(차단 없이 로그만 기록, 디버깅/정책개발), Disabled(완전 비활성화). 핵심 포인트: Permissive 모드는 SELinux를 비활성화한 것이 아닙니다. 정책 위반이 일어나더라도 차단하지 않고 /var/log/audit/audit.log에 기록만 합니다."
  optionExplanations={['① SELinux를 완전히 비활성화하는 것은 Disabled 모드입니다. Enforcing은 가장 강력한 보안 모드입니다.', '② 정답입니다. Enforcing은 정책 위반을 차단+로그, Permissive는 차단 없이 로그만 기록합니다.', '③ Permissive는 강제 적용하지 않습니다. 위반을 허용하고 로그만 남깁니다.', '④ Permissive와 Enforcing의 역할이 뒤바뀐 설명입니다.']}
/>

---

### 문제 7

<Quiz
  questionNumber={7}
  question="tar -cjvf archive.tar.bz2 /data 명령에서 j 옵션의 의미는?"
  options={['① gzip으로 압축', '② bzip2로 압축', '③ xz로 압축', '④ 압축 없이 아카이브만 생성']}
  correctAnswer={2}
  explanation="tar 압축 옵션 비교: -z=gzip(.tar.gz/.tgz, 빠름/중간 압축률), -j=bzip2(.tar.bz2, 느림/높은 압축률), -J=xz(.tar.xz, 가장 느림/가장 높은 압축률), 옵션없음=압축없음(.tar). tar 주요 옵션: -c(create, 새 아카이브 생성), -x(extract, 압축해제), -t(list, 목록확인), -v(verbose, 파일목록 출력), -f(file, 아카이브 파일 지정)."
  optionExplanations={['① gzip 압축은 소문자 -z 옵션을 사용합니다.', '② 정답입니다. 소문자 -j 옵션은 bzip2 압축 방식을 지정합니다. 확장자는 .tar.bz2입니다.', '③ xz 압축은 대문자 -J 옵션을 사용합니다.', '④ 압축 없이 아카이브만 생성할 때는 -z, -j, -J 옵션 없이 -cvf만 사용합니다.']}
/>

---

### 문제 8

<Quiz
  questionNumber={8}
  question="좀비(Zombie, Z) 프로세스에 대한 설명으로 올바른 것은?"
  options={['① CPU를 100% 사용하며 시스템을 느리게 만드는 프로세스', '② 실행이 완료됐지만 부모 프로세스가 exit 상태를 수거하지 않아 PCB가 남아있는 프로세스', '③ 백그라운드에서 실행 중인 데몬 프로세스', '④ 메모리 누수를 일으키는 비정상적인 프로세스']}
  correctAnswer={2}
  explanation="좀비 프로세스(Zombie Process): 프로세스 실행이 완료됐지만 부모 프로세스가 wait() 시스템 콜로 종료 상태를 수거하지 않은 상태입니다. 실제 CPU나 메모리는 거의 사용하지 않지만 PID와 PCB(Process Control Block)는 차지합니다. ps 출력의 STAT 컬럼에 Z로 표시되며, 좀비가 많이 쌓이면 PID가 고갈될 수 있습니다. 좀비는 직접 kill로 종료할 수 없으며 부모 프로세스를 종료하거나 SIGCHLD 신호를 전송해야 합니다."
  optionExplanations={['① 좀비 프로세스는 CPU나 메모리를 거의 사용하지 않습니다. CPU 100% 사용은 다른 문제입니다.', '② 정답입니다. 실행이 완료됐으나 부모가 종료 상태를 수거하지 않아 PID/PCB만 남아있는 상태입니다.', '③ 백그라운드 데몬 프로세스는 정상적으로 실행 중인 프로세스입니다. 좀비와는 다릅니다.', '④ 메모리 누수는 메모리를 해제하지 않는 현상으로, 좀비 프로세스와는 다른 개념입니다.']}
/>

---

### 문제 9

<Quiz
  questionNumber={9}
  question="rsync --delete 옵션의 동작으로 올바른 것은?"
  options={['① 동기화 완료 후 원본 파일을 삭제', '② 대상 디렉토리에만 있고 원본에 없는 파일을 대상에서 삭제', '③ 대상 디렉토리를 완전히 삭제하고 원본으로 교체', '④ 삭제된 파일을 복구하여 원본과 동일하게 만듦']}
  correctAnswer={2}
  explanation="rsync --delete는 대상(destination)에만 있고 원본(source)에는 없는 파일을 삭제하여 완전한 미러링을 수행합니다. 주의: 원본 경로 끝의 슬래시(/) 여부에 따라 동작이 달라집니다. rsync 주요 옵션: -a(아카이브 모드), -v(상세 출력), -z(전송 중 압축), --delete(원본에 없는 파일 삭제), --dry-run(테스트 실행), -e ssh(SSH로 전송)."
  optionExplanations={['① --delete는 원본 파일을 삭제하지 않습니다. 대상에만 있는 파일을 삭제합니다.', '② 정답입니다. 대상 디렉토리에만 있고 원본에는 없는 파일을 삭제하여 완전한 미러링을 합니다.', '③ 대상 디렉토리를 완전히 삭제하는 것이 아니라 원본에 없는 파일만 선택적으로 삭제합니다.', '④ rsync는 삭제된 파일을 복구하는 기능이 없습니다. --delete는 삭제하는 옵션입니다.']}
/>

---

### 문제 10

<Quiz
  questionNumber={10}
  question="useradd -G wheel,developers user1 명령에서 -G 옵션의 의미는?"
  options={['① user1의 기본 그룹(primary group)을 wheel로 설정', '② user1을 wheel과 developers 그룹에 보조 그룹(supplementary group)으로 추가', '③ wheel과 developers 그룹을 새로 생성하면서 user1을 추가', '④ user1의 GID를 wheel 그룹의 GID로 변경']}
  correctAnswer={2}
  explanation="useradd 그룹 옵션 구분: -g 그룹명은 기본 그룹(primary group) 지정으로 /etc/passwd에 GID로 기록되고, -G 그룹명[,그룹명]은 보조 그룹(supplementary group) 지정으로 /etc/group에 기록됩니다. 시험 포인트: usermod -G로 보조 그룹을 설정할 때 -a(append) 없이 사용하면 기존 보조 그룹이 모두 제거됩니다. 기존 그룹을 유지하면서 추가하려면 반드시 -aG를 함께 써야 합니다."
  optionExplanations={['① 기본 그룹(primary group)을 설정하는 옵션은 소문자 -g입니다. 대문자 -G는 보조 그룹입니다.', '② 정답입니다. 대문자 -G는 보조 그룹(supplementary group)을 지정하며, 쉼표로 여러 그룹을 지정할 수 있습니다.', '③ useradd -G 옵션은 그룹을 새로 생성하지 않습니다. 이미 존재하는 그룹에 사용자를 추가합니다.', '④ GID를 변경하는 옵션은 -g이며, -G는 보조 그룹 목록을 지정하는 옵션입니다.']}
/>

---

## 마무리 복습

<Quiz questionNumber={1} question="chmod 755 파일 명령 후 ls -l로 확인했을 때 표시되는 권한 문자열은?" options={["-rwxr-xr-x", "-rwxrwxrwx", "-r-xr-xr-x", "-rwxrwxr-x"]} correctAnswer={1} explanation="755를 2진수로 변환하면 7=111(rwx), 5=101(r-x), 5=101(r-x)입니다. 소유자는 rwx(읽기+쓰기+실행), 그룹과 기타는 r-x(읽기+실행)만 허용됩니다. 일반 파일은 맨 앞에 -가 붙어 -rwxr-xr-x가 됩니다." optionExplanations={["정답입니다. 7=rwx, 5=r-x, 5=r-x 이므로 -rwxr-xr-x입니다.", "777에 해당하는 권한입니다. 모든 사용자에게 rwx가 부여된 상태입니다.", "555에 해당하는 권한입니다. 소유자에게도 쓰기 권한이 없습니다.", "775에 해당합니다. 그룹에 쓰기 권한이 있고 기타에는 없는 설정입니다."]} />

<Quiz questionNumber={2} question="매월 마지막 날은 지정하기 어렵지만, 매월 1일 오전 2시 30분에 실행하는 crontab 항목으로 올바른 것은?" options={["30 2 1 * * /path/to/script", "2 30 1 * * /path/to/script", "* * 1 2 30 /path/to/script", "30 * 2 1 * /path/to/script"]} correctAnswer={1} explanation="crontab 필드 순서는 '분 시 일 월 요일'입니다. 매월 1일 오전 2시 30분은 분=30, 시=2, 일=1, 월=*(매월), 요일=*(무관)이므로 '30 2 1 * *'입니다." optionExplanations={["정답입니다. 30(분) 2(시) 1(매월1일) *(매월) *(요일무관)의 순서입니다.", "2를 분으로, 30을 시로 읽으면 오전 30시는 존재하지 않는 시각입니다.", "필드 순서가 완전히 잘못되었습니다.", "30을 분이 아닌 다른 위치에 두면 의미가 달라집니다."]} />

<Quiz questionNumber={3} question="RAID 1과 RAID 5를 비교했을 때 올바른 설명은?" options={["RAID 1은 최소 3개, RAID 5는 최소 2개 디스크가 필요하다", "RAID 5는 2개 디스크 동시 장애를 허용하지만 RAID 1은 1개만 허용한다", "RAID 1은 미러링으로 1개 장애를 허용하고, RAID 5는 분산 패리티로 1개 장애를 허용한다", "RAID 1과 RAID 5 모두 성능 향상을 주목적으로 한다"]} correctAnswer={3} explanation="RAID 1은 모든 디스크에 동일한 데이터를 복제(미러링)하여 최소 2개 디스크로 1개 장애를 허용합니다. RAID 5는 최소 3개 디스크에 데이터와 패리티를 분산 저장하여 1개 장애를 허용합니다. 두 방식 모두 1개 디스크 장애에 내성이 있습니다." optionExplanations={["RAID 1 최소 2개, RAID 5 최소 3개입니다. 설명이 반대입니다.", "2개 동시 장애를 허용하는 것은 RAID 6입니다.", "정답입니다. 허용 장애 수는 같지만 방식이 다릅니다. RAID 1=미러링, RAID 5=분산 패리티.", "RAID 0이 성능(스트라이핑) 목적입니다. RAID 1은 안정성, RAID 5는 안정성+효율성을 목적으로 합니다."]} />

## 참고 자료

- [KAIT 리눅스마스터 안내](https://kait.or.kr/user/MainMenuList.do?cateSeq=5&menuSeq=119)
- [Linux man-pages project](https://man7.org/linux/man-pages/)

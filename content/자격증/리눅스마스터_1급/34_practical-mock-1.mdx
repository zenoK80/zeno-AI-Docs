---
title: '34. 2차 실기 모의고사 1회'
description: '리눅스마스터 1급 2차 실기 모의고사 1회 (단답·서술 25문항)'
---

<Callout type="info">
  리눅스마스터 1급 2차 실기는 단답형과 서술형으로 구성됩니다. 실제 시험 환경처럼 직접 답을 작성해 보세요.
</Callout>

<Quiz
  questionNumber={1}
  question="리눅스에서 파일의 소유자, 그룹, 기타 사용자의 읽기·쓰기·실행 권한을 8진수로 표현할 때 rwxr-xr--에 해당하는 값으로 알맞은 것은?"
  options={['① 754', '② 755', '③ 644', '④ 764']}
  correctAnswer={1}
  explanation="rwxr-xr-- 변환: rwx=7(4+2+1), r-x=5(4+0+1), r--=4(4+0+0) → 754. 각 3비트를 8진수로 변환합니다."
  optionExplanations={['정답입니다. rwx=7, r-x=5, r--=4 → 754입니다.', '755는 rwxr-xr-x입니다.', '644는 rw-r--r--입니다.', '764는 rwxrw-r--입니다.']}
/>

<Quiz
  questionNumber={2}
  question="ls -al 명령어 출력에서 파일 유형이 'd'를 나타내는 것으로 알맞은 것은?"
  options={['① 심볼릭 링크', '② 디렉터리', '③ 일반 파일', '④ 블록 디바이스']}
  correctAnswer={2}
  explanation="ls -al 출력에서 첫 번째 문자가 파일 유형입니다: -(일반 파일), d(디렉터리), l(심볼릭 링크), b(블록 디바이스), c(문자 디바이스), p(파이프), s(소켓)"
  optionExplanations={['심볼릭 링크는 l입니다.', '정답입니다. d는 디렉터리를 나타냅니다.', '일반 파일은 -입니다.', '블록 디바이스는 b입니다.']}
/>

<Quiz
  questionNumber={3}
  question="find 명령어로 현재 디렉터리에서 확장자가 .log인 파일을 찾아 삭제하는 명령으로 알맞은 것은?"
  options={['① find . -name *.log -delete', '② find . -name "*.log" -exec rm {} \\;', '③ ls *.log | rm', '④ grep -r .log . | xargs rm']}
  correctAnswer={2}
  explanation="find . -name '*.log' -exec rm {} \; 명령은 현재 디렉터리에서 .log 파일을 찾아 rm으로 삭제합니다. -delete 옵션도 사용 가능하지만 안전을 위해 -exec rm {} \; 방식이 더 일반적입니다."
  optionExplanations={['*.log는 따옴표 없이 쉘 글로브로 해석될 수 있습니다.', '정답입니다. find와 -exec rm으로 파일을 찾아 삭제합니다.', 'ls 출력을 rm에 파이프로 넘기는 것은 동작하지 않습니다.', 'grep은 파일 내용 검색용입니다.']}
/>

<Quiz
  questionNumber={4}
  question="ps aux 명령어 출력에서 'Z' 상태를 나타내는 프로세스로 알맞은 것은?"
  options={['① 실행 대기 중인 프로세스', '② 좀비(Zombie) 프로세스', '③ 슬립(Sleep) 중인 프로세스', '④ 정지(Stopped) 프로세스']}
  correctAnswer={2}
  explanation="ps 명령의 프로세스 상태: R(Running/Runnable), S(Sleeping), D(Uninterruptible Sleep), Z(Zombie), T(Stopped), I(Idle). Z는 실행이 종료되었으나 부모 프로세스가 wait()를 호출하지 않아 프로세스 테이블에 남아있는 좀비 프로세스입니다."
  optionExplanations={['대기 중인 프로세스는 R 또는 S 상태입니다.', '정답입니다. Z는 좀비 프로세스입니다.', '슬립 중인 프로세스는 S 또는 D 상태입니다.', '정지 프로세스는 T 상태입니다.']}
/>

<Quiz
  questionNumber={5}
  question="crontab에서 매일 오전 3시 30분에 실행되도록 설정하는 형식으로 알맞은 것은?"
  options={['① 30 3 * * * command', '② 3 30 * * * command', '③ * * 3 30 * command', '④ 0 3 30 * * command']}
  correctAnswer={1}
  explanation="crontab 형식: 분 시 일 월 요일 명령어. 오전 3시 30분 = 분:30 시:3. 따라서 30 3 * * * command가 됩니다."
  optionExplanations={['정답입니다. 분(30) 시(3) 일(*) 월(*) 요일(*) 명령어 순서입니다.', '3 30은 3분 30시로 잘못된 형식입니다.', 'crontab 순서가 잘못되었습니다.', '0 3 30은 0분 3시 30일로 다른 의미입니다.']}
/>

<Quiz
  questionNumber={6}
  question="리눅스에서 네트워크 인터페이스의 IP 주소를 확인하는 명령으로 알맞은 것은?"
  options={['① ifconfig 또는 ip addr show', '② netstat -r', '③ route -n', '④ arp -n']}
  correctAnswer={1}
  explanation="인터페이스 IP 주소 확인: ifconfig(구형) 또는 ip addr show(최신). netstat -r과 route -n은 라우팅 테이블을 표시하고 arp -n은 ARP 테이블을 표시합니다."
  optionExplanations={['정답입니다. ifconfig 또는 ip addr show로 IP 주소를 확인합니다.', 'netstat -r은 라우팅 테이블을 표시합니다.', 'route -n은 라우팅 테이블을 표시합니다.', 'arp -n은 ARP 캐시를 표시합니다.']}
/>

<Quiz
  questionNumber={7}
  question="리눅스에서 프로세스 간 통신(IPC) 방법이 아닌 것으로 알맞은 것은?"
  options={['① 파이프(Pipe)', '② 공유 메모리(Shared Memory)', '③ 소켓(Socket)', '④ 스왑(Swap)']}
  correctAnswer={4}
  explanation="리눅스 IPC 방법: 파이프(Pipe), 명명된 파이프(FIFO), 메시지 큐(Message Queue), 공유 메모리(Shared Memory), 세마포어(Semaphore), 소켓(Socket). 스왑(Swap)은 가상 메모리를 위한 디스크 공간으로 IPC 방법이 아닙니다."
  optionExplanations={['파이프는 IPC 방법입니다.', '공유 메모리는 IPC 방법입니다.', '소켓은 IPC 방법입니다.', '정답입니다. 스왑은 가상 메모리 공간이며 IPC 방법이 아닙니다.']}
/>

<Quiz
  questionNumber={8}
  question="Apache 웹 서버에서 VirtualHost를 이용해 IP 하나로 여러 도메인을 서비스하는 방식으로 알맞은 것은?"
  options={['① IP 기반 가상 호스트', '② 이름 기반 가상 호스트(Name-based Virtual Host)', '③ 포트 기반 가상 호스트만 가능', '④ Apache는 가상 호스트를 지원하지 않음']}
  correctAnswer={2}
  explanation="이름 기반 가상 호스트(Name-based Virtual Host)는 하나의 IP 주소에서 HTTP 요청의 Host 헤더를 보고 여러 도메인을 구분하는 방식입니다. 같은 IP로 여러 웹사이트를 운영할 때 사용합니다."
  optionExplanations={['IP 기반은 IP마다 다른 도메인을 운영합니다.', '정답입니다. 이름 기반 가상 호스트로 하나의 IP로 여러 도메인을 서비스합니다.', '포트 기반도 가능하지만 가장 일반적인 방식은 이름 기반입니다.', 'Apache는 가상 호스트를 지원합니다.']}
/>

<Quiz
  questionNumber={9}
  question="리눅스에서 /etc/fstab 파일의 역할로 알맞은 것은?"
  options={['① 네트워크 인터페이스 설정', '② 부팅 시 자동으로 마운트할 파일 시스템 목록 설정', '③ 사용자 계정 정보 저장', '④ 패키지 저장소 설정']}
  correctAnswer={2}
  explanation="/etc/fstab은 부팅 시 자동으로 마운트할 파일 시스템 정보를 담고 있습니다. 장치, 마운트 포인트, 파일 시스템 유형, 옵션, 덤프, fsck 순서를 6개 필드로 설정합니다."
  optionExplanations={['네트워크 인터페이스는 /etc/network/interfaces 또는 /etc/sysconfig/network-scripts/에 설정합니다.', '정답입니다. /etc/fstab은 부팅 시 자동 마운트 설정 파일입니다.', '사용자 계정은 /etc/passwd에 저장합니다.', '패키지 저장소는 /etc/apt/sources.list 또는 /etc/yum.repos.d/에 설정합니다.']}
/>

<Quiz
  questionNumber={10}
  question="iptables에서 외부에서 80번 포트(HTTP)로 들어오는 패킷을 허용하는 명령으로 알맞은 것은?"
  options={['① iptables -A INPUT -p tcp --dport 80 -j ACCEPT', '② iptables -D INPUT -p tcp --dport 80 -j ACCEPT', '③ iptables -A OUTPUT -p tcp --dport 80 -j ACCEPT', '④ iptables -F INPUT']}
  correctAnswer={1}
  explanation="iptables -A INPUT -p tcp --dport 80 -j ACCEPT: -A(추가), INPUT(수신 체인), -p tcp(TCP 프로토콜), --dport 80(목적지 포트 80), -j ACCEPT(허용). 외부에서 들어오는 HTTP 패킷을 허용합니다."
  optionExplanations={['정답입니다. -A INPUT으로 인바운드 80포트를 허용합니다.', '-D는 규칙 삭제 명령입니다.', 'OUTPUT은 나가는 패킷 체인입니다.', '-F INPUT은 INPUT 체인의 모든 규칙을 초기화합니다.']}
/>

<Quiz
  questionNumber={11}
  question="리눅스에서 디스크 사용량을 확인하는 명령으로 알맞은 것은?"
  options={['① df -h', '② du -sh', '③ fdisk -l', '④ lsblk']}
  correctAnswer={1}
  explanation="df -h는 마운트된 파일 시스템별 디스크 사용량을 사람이 읽기 좋은 형식(human-readable)으로 표시합니다. du -sh는 특정 디렉터리의 사용량, fdisk -l은 파티션 정보, lsblk는 블록 장치 목록을 표시합니다."
  optionExplanations={['정답입니다. df -h는 파일 시스템별 전체 디스크 사용량을 확인합니다.', 'du -sh는 특정 디렉터리나 파일의 사용량을 확인합니다.', 'fdisk -l은 파티션 정보를 표시합니다.', 'lsblk는 블록 장치 목록을 표시합니다.']}
/>

<Quiz
  questionNumber={12}
  question="SSH 서버(sshd) 설정 파일 위치로 알맞은 것은?"
  options={['① /etc/ssh/sshd_config', '② /etc/ssh/ssh_config', '③ /home/user/.ssh/config', '④ /etc/sshd.conf']}
  correctAnswer={1}
  explanation="/etc/ssh/sshd_config는 SSH 서버 데몬의 설정 파일입니다. /etc/ssh/ssh_config는 SSH 클라이언트 설정, ~/.ssh/config는 사용자별 SSH 클라이언트 설정입니다."
  optionExplanations={['정답입니다. /etc/ssh/sshd_config는 SSH 서버 설정 파일입니다.', '/etc/ssh/ssh_config는 SSH 클라이언트 설정 파일입니다.', '~/.ssh/config는 사용자별 SSH 클라이언트 설정입니다.', '/etc/sshd.conf는 일반적인 SSH 설정 파일 경로가 아닙니다.']}
/>

<Quiz
  questionNumber={13}
  question="리눅스에서 특정 포트가 열려있는지 확인하는 명령으로 알맞은 것은?"
  options={['① netstat -tuln 또는 ss -tuln', '② ping 포트번호', '③ traceroute 포트번호', '④ ifconfig 포트번호']}
  correctAnswer={1}
  explanation="netstat -tuln 또는 ss -tuln으로 현재 열려있는 포트를 확인합니다. 옵션: -t(TCP), -u(UDP), -l(Listening), -n(숫자로 표시). ss는 netstat의 현대적 대안입니다."
  optionExplanations={['정답입니다. netstat -tuln 또는 ss -tuln으로 열린 포트를 확인합니다.', 'ping은 포트 번호를 인수로 받지 않습니다.', 'traceroute는 경로 추적 명령입니다.', 'ifconfig는 네트워크 인터페이스 설정 명령입니다.']}
/>

<Quiz
  questionNumber={14}
  question="리눅스에서 RPM 패키지를 설치하는 명령으로 알맞은 것은?"
  options={['① rpm -ivh package.rpm', '② rpm -e package', '③ rpm -qa package', '④ rpm -V package']}
  correctAnswer={1}
  explanation="rpm -ivh package.rpm: -i(install), -v(verbose), -h(hash mark 진행 표시). -e는 제거(erase), -qa는 설치된 패키지 조회(query all), -V는 검증(verify)입니다."
  optionExplanations={['정답입니다. rpm -ivh로 RPM 패키지를 설치합니다.', 'rpm -e는 패키지를 제거합니다.', 'rpm -qa는 설치된 패키지를 조회합니다.', 'rpm -V는 패키지 파일 무결성을 검증합니다.']}
/>

<Quiz
  questionNumber={15}
  question="리눅스 커널 파라미터를 런타임에 변경하는 명령으로 알맞은 것은?"
  options={['① sysctl -w 파라미터=값', '② modprobe 파라미터', '③ insmod 파라미터', '④ lsmod 파라미터']}
  correctAnswer={1}
  explanation="sysctl -w key=value는 커널 파라미터를 런타임에 변경합니다(재부팅 후 초기화). 영구 변경은 /etc/sysctl.conf에 작성합니다. modprobe는 커널 모듈 로드, insmod는 모듈 직접 삽입, lsmod는 로드된 모듈 목록입니다."
  optionExplanations={['정답입니다. sysctl -w로 커널 파라미터를 런타임에 변경합니다.', 'modprobe는 커널 모듈을 로드합니다.', 'insmod는 커널 모듈을 직접 삽입합니다.', 'lsmod는 로드된 커널 모듈 목록을 표시합니다.']}
/>

<Quiz
  questionNumber={16}
  question="Nginx 설정 파일에서 리버스 프록시를 설정할 때 사용하는 지시어로 알맞은 것은?"
  options={['① proxy_pass', '② rewrite', '③ alias', '④ root']}
  correctAnswer={1}
  explanation="Nginx에서 proxy_pass 지시어는 요청을 다른 서버로 전달하는 리버스 프록시를 설정합니다. 예: proxy_pass http://backend_server;. rewrite는 URL 재작성, alias와 root는 파일 경로 매핑에 사용됩니다."
  optionExplanations={['정답입니다. proxy_pass는 리버스 프록시 설정 지시어입니다.', 'rewrite는 URL 재작성에 사용됩니다.', 'alias는 URL 경로를 파일 시스템 경로로 매핑합니다.', 'root는 문서 루트 디렉터리를 설정합니다.']}
/>

<Quiz
  questionNumber={17}
  question="리눅스에서 tar.gz 파일을 압축 해제하는 명령으로 알맞은 것은?"
  options={['① tar -xzf archive.tar.gz', '② tar -czf archive.tar.gz', '③ gunzip archive.tar.gz', '④ unzip archive.tar.gz']}
  correctAnswer={1}
  explanation="tar -xzf archive.tar.gz: -x(extract), -z(gzip), -f(파일). 생성은 -c(create), -z(gzip), -f(파일). gunzip은 .gz만 해제(tar 제외), unzip은 .zip 파일용입니다."
  optionExplanations={['정답입니다. tar -xzf로 tar.gz 파일을 압축 해제합니다.', 'tar -czf는 tar.gz 파일을 생성합니다.', 'gunzip은 .gz 압축만 해제하며 tar은 별도 처리가 필요합니다.', 'unzip은 .zip 형식에 사용합니다.']}
/>

<Quiz
  questionNumber={18}
  question="리눅스에서 실행 중인 서비스(데몬)를 관리하는 systemd 명령으로 알맞은 것은?"
  options={['① service 명령어', '② systemctl start/stop/restart 서비스명', '③ init 서비스명', '④ daemon 서비스명']}
  correctAnswer={2}
  explanation="systemd를 사용하는 최신 리눅스에서 서비스 관리는 systemctl 명령을 사용합니다. systemctl start/stop/restart/status/enable/disable 서비스명 형식입니다."
  optionExplanations={['service 명령은 구형 init 기반 시스템에서 사용하는 방식이며 최신에서는 systemctl을 권장합니다.', '정답입니다. systemctl이 systemd 기반 서비스 관리의 표준 명령입니다.', 'init은 이전 방식의 초기화 시스템입니다.', 'daemon은 서비스 관리 명령이 아닙니다.']}
/>

<Quiz
  questionNumber={19}
  question="리눅스에서 SELinux의 현재 모드를 확인하는 명령으로 알맞은 것은?"
  options={['① getenforce', '② setenforce', '③ sestatus', '④ semanage']}
  correctAnswer={1}
  explanation="getenforce 명령은 SELinux의 현재 모드(Enforcing/Permissive/Disabled)를 출력합니다. setenforce는 모드 변경, sestatus는 상세 상태 정보, semanage는 SELinux 정책 관리에 사용됩니다."
  optionExplanations={['정답입니다. getenforce는 SELinux 현재 모드를 확인합니다.', 'setenforce는 SELinux 모드를 변경합니다.', 'sestatus는 SELinux 상세 상태 정보를 표시합니다.', 'semanage는 SELinux 정책을 관리합니다.']}
/>

<Quiz
  questionNumber={20}
  question="리눅스에서 LVM(Logical Volume Manager)을 사용할 때 볼륨 그룹(VG)을 생성하는 명령으로 알맞은 것은?"
  options={['① vgcreate VG명 /dev/sdb1', '② lvcreate -L 10G -n LV명 VG명', '③ pvcreate /dev/sdb1', '④ vgextend VG명 /dev/sdb2']}
  correctAnswer={1}
  explanation="LVM 설정 순서: pvcreate(물리 볼륨 생성) → vgcreate(볼륨 그룹 생성) → lvcreate(논리 볼륨 생성). vgcreate VG명 /dev/sdb1로 볼륨 그룹을 생성합니다. vgextend는 기존 VG에 PV를 추가합니다."
  optionExplanations={['정답입니다. vgcreate는 볼륨 그룹을 생성합니다.', 'lvcreate는 논리 볼륨을 생성합니다.', 'pvcreate는 물리 볼륨을 초기화합니다.', 'vgextend는 기존 볼륨 그룹을 확장합니다.']}
/>

<Quiz
  questionNumber={21}
  question="리눅스에서 DNS 조회를 수행하는 명령으로 알맞은 것은?"
  options={['① nslookup 또는 dig', '② traceroute', '③ netstat', '④ arp']}
  correctAnswer={1}
  explanation="DNS 조회에는 nslookup 또는 dig 명령을 사용합니다. dig(Domain Information Groper)는 더 상세한 정보를 제공하며 DNS 문제 해결에 자주 사용됩니다. traceroute는 경로 추적, netstat는 네트워크 연결 상태, arp는 ARP 캐시 조회입니다."
  optionExplanations={['정답입니다. nslookup과 dig는 DNS 조회 명령입니다.', 'traceroute는 목적지까지의 라우터 경로를 추적합니다.', 'netstat는 네트워크 연결 상태를 조회합니다.', 'arp는 ARP 캐시를 조회합니다.']}
/>

<Quiz
  questionNumber={22}
  question="리눅스 프로세스에서 SIGKILL 신호의 번호로 알맞은 것은?"
  options={['① 9', '② 15', '③ 1', '④ 2']}
  correctAnswer={1}
  explanation="주요 시그널 번호: 1(SIGHUP-재시작), 2(SIGINT-Ctrl+C), 9(SIGKILL-강제 종료), 15(SIGTERM-정상 종료 요청), 18(SIGCONT), 19(SIGSTOP). SIGKILL(9)은 프로세스가 무시할 수 없는 강제 종료 신호입니다."
  optionExplanations={['정답입니다. SIGKILL의 번호는 9입니다.', '15는 SIGTERM(정상 종료 요청)입니다.', '1은 SIGHUP(재시작)입니다.', '2는 SIGINT(Ctrl+C, 인터럽트)입니다.']}
/>

<Quiz
  questionNumber={23}
  question="리눅스에서 /etc/hosts 파일의 역할로 알맞은 것은?"
  options={['① 사용자 비밀번호를 저장', '② 호스트명과 IP 주소를 로컬에서 매핑', '③ DNS 서버 주소를 설정', '④ 네트워크 인터페이스를 설정']}
  correctAnswer={2}
  explanation="/etc/hosts 파일은 DNS 조회 이전에 참조하는 로컬 호스트명-IP 주소 매핑 파일입니다. 여기에 설정된 항목은 DNS 조회 없이 바로 IP로 해석됩니다. DNS 서버 주소는 /etc/resolv.conf에 설정합니다."
  optionExplanations={['비밀번호는 /etc/shadow에 저장합니다.', '정답입니다. /etc/hosts는 로컬 호스트명-IP 매핑 파일입니다.', 'DNS 서버 주소는 /etc/resolv.conf에 설정합니다.', '네트워크 인터페이스는 /etc/sysconfig/network-scripts/나 /etc/network/interfaces에 설정합니다.']}
/>

<Quiz
  questionNumber={24}
  question="rsync 명령을 사용하여 /source/ 디렉터리를 원격 서버의 /dest/에 동기화하는 명령으로 알맞은 것은?"
  options={['① rsync -avz /source/ user@remote:/dest/', '② cp -r /source/ user@remote:/dest/', '③ scp /source/ user@remote:/dest/', '④ ftp /source/ user@remote:/dest/']}
  correctAnswer={1}
  explanation="rsync -avz /source/ user@remote:/dest/: -a(아카이브 모드, 권한·소유자 등 보존), -v(자세한 출력), -z(전송 중 압축). rsync는 변경된 파일만 전송하여 효율적인 동기화를 제공합니다."
  optionExplanations={['정답입니다. rsync -avz로 원격 서버와 효율적으로 동기화합니다.', 'cp는 로컬 복사 명령으로 원격 서버를 직접 지정할 수 없습니다.', 'scp는 단순 복사이며 증분 동기화를 지원하지 않습니다.', 'ftp는 이 형식을 지원하지 않습니다.']}
/>

<Quiz
  questionNumber={25}
  question="리눅스에서 RAID 구성 시 RAID 5의 특징으로 알맞은 것은?"
  options={['① 최소 2개 디스크로 구성, 디스크 1개 고장 허용 불가', '② 최소 3개 디스크로 구성, 패리티를 분산 저장하여 디스크 1개 고장 허용', '③ 최소 2개 디스크로 구성, 미러링으로 디스크 1개 고장 허용', '④ 최소 4개 디스크로 구성, 패리티 2개로 디스크 2개 고장 허용']}
  correctAnswer={2}
  explanation="RAID 종류: RAID 0(스트라이핑, 최소 2개, 속도 향상, 내결함성 없음), RAID 1(미러링, 최소 2개, 1개 고장 허용), RAID 5(패리티 분산, 최소 3개, 1개 고장 허용, 공간 효율 양호), RAID 6(패리티 2개, 최소 4개, 2개 고장 허용)."
  optionExplanations={['RAID 0의 특징입니다.', '정답입니다. RAID 5는 최소 3개 디스크, 패리티 분산, 1개 고장 허용입니다.', 'RAID 1(미러링)의 특징입니다.', 'RAID 6의 특징입니다.']}
/>

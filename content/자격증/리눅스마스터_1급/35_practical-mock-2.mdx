---
title: '35. 2차 실기 모의고사 2회'
description: '리눅스마스터 1급 2차 실기 모의고사 2회 (단답·서술 25문항)'
---

<Callout type="info">
  1회와 중복되지 않는 새로운 주제로 구성된 2차 실기 모의고사 2회입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="리눅스에서 파일의 하드 링크와 심볼릭 링크의 차이로 옳지 않은 것으로 알맞은 것은?"
  options={['① 하드 링크는 원본과 동일한 inode를 공유한다.', '② 심볼릭 링크는 원본 파일이 삭제되면 링크가 깨진다.', '③ 하드 링크는 다른 파일 시스템에도 생성 가능하다.', '④ 심볼릭 링크는 디렉터리를 가리킬 수 있다.']}
  correctAnswer={3}
  explanation="하드 링크는 동일한 파일 시스템 내에서만 생성 가능합니다. 다른 파티션이나 파일 시스템 간에는 하드 링크를 만들 수 없습니다. 심볼릭 링크는 다른 파일 시스템에도 가리킬 수 있습니다."
  optionExplanations={['하드 링크는 동일 inode를 공유합니다. 올바른 설명.', '심볼릭 링크는 원본 삭제 시 깨집니다. 올바른 설명.', '정답입니다(옳지 않은 것). 하드 링크는 다른 파일 시스템에는 생성 불가입니다.', '심볼릭 링크는 디렉터리를 가리킬 수 있습니다. 올바른 설명.']}
/>

<Quiz
  questionNumber={2}
  question="grep 명령어로 /var/log/syslog에서 'error'를 포함하는 줄을 대소문자 구분 없이 검색하는 명령으로 알맞은 것은?"
  options={['① grep -i error /var/log/syslog', '② grep -v error /var/log/syslog', '③ grep -n error /var/log/syslog', '④ grep -c error /var/log/syslog']}
  correctAnswer={1}
  explanation="grep 주요 옵션: -i(ignore case, 대소문자 무시), -v(invert, 패턴 미포함 줄 출력), -n(줄 번호 표시), -c(매칭 줄 수 출력), -r(재귀 검색), -l(파일명만 출력)."
  optionExplanations={['정답입니다. grep -i는 대소문자 구분 없이 검색합니다.', 'grep -v는 패턴을 포함하지 않는 줄을 출력합니다.', 'grep -n은 줄 번호와 함께 출력합니다.', 'grep -c는 매칭된 줄의 수를 출력합니다.']}
/>

<Quiz
  questionNumber={3}
  question="리눅스에서 useradd 명령으로 홈 디렉터리를 /home/newuser로, 기본 쉘을 /bin/bash로 설정하여 사용자 newuser를 생성하는 명령으로 알맞은 것은?"
  options={['① useradd -d /home/newuser -s /bin/bash newuser', '② useradd -h /home/newuser -b /bin/bash newuser', '③ adduser /home/newuser /bin/bash newuser', '④ usermod -d /home/newuser -s /bin/bash newuser']}
  correctAnswer={1}
  explanation="useradd 주요 옵션: -d(홈 디렉터리), -s(기본 쉘), -u(UID), -g(기본 그룹), -G(추가 그룹), -m(홈 디렉터리 생성), -c(주석). usermod는 기존 사용자 수정 명령입니다."
  optionExplanations={['정답입니다. useradd -d -s 옵션으로 홈 디렉터리와 쉘을 설정합니다.', '-h는 useradd에서 도움말, -b는 기본 홈 디렉터리 베이스 경로입니다.', 'adduser는 대화형이며 이 형식은 유효하지 않습니다.', 'usermod는 기존 사용자 수정에 사용합니다.']}
/>

<Quiz
  questionNumber={4}
  question="리눅스 파일 시스템 중 저널링(Journaling)을 지원하여 시스템 비정상 종료 후 복구가 빠른 파일 시스템으로 알맞은 것은?"
  options={['① ext2', '② ext3 또는 ext4', '③ FAT32', '④ NFS']}
  correctAnswer={2}
  explanation="ext3와 ext4는 저널링 파일 시스템으로 파일 시스템 변경 내용을 먼저 저널에 기록하여 비정상 종료 후 fsck로 전체 검사 없이 빠르게 복구할 수 있습니다. ext2는 저널링을 지원하지 않습니다."
  optionExplanations={['ext2는 저널링을 지원하지 않습니다.', '정답입니다. ext3/ext4는 저널링을 지원하는 파일 시스템입니다.', 'FAT32는 저널링을 지원하지 않습니다.', 'NFS는 네트워크 파일 시스템 프로토콜입니다.']}
/>

<Quiz
  questionNumber={5}
  question="vi 편집기에서 현재 커서가 있는 줄을 삭제하는 명령으로 알맞은 것은?"
  options={['① dd', '② yy', '③ p', '④ dw']}
  correctAnswer={1}
  explanation="vi 주요 명령: dd(현재 줄 삭제), yy(현재 줄 복사), p(붙여넣기), dw(단어 삭제), u(실행 취소), /패턴(검색), :w(저장), :q(종료), :%s/old/new/g(전체 치환)."
  optionExplanations={['정답입니다. dd는 현재 줄을 삭제합니다.', 'yy는 현재 줄을 복사(yank)합니다.', 'p는 복사한 내용을 붙여넣습니다.', 'dw는 커서 위치부터 단어 끝까지 삭제합니다.']}
/>

<Quiz
  questionNumber={6}
  question="리눅스에서 메모리 사용량을 확인하는 명령으로 알맞은 것은?"
  options={['① free -h', '② top (M 키)', '③ cat /proc/meminfo', '④ 위 모두 가능']}
  correctAnswer={4}
  explanation="리눅스에서 메모리 사용량을 확인하는 방법은 다양합니다: free -h(사람 읽기 쉬운 형식), top(실시간 모니터링, M 키로 메모리 정렬), cat /proc/meminfo(커널에서 제공하는 상세 메모리 정보) 모두 가능합니다."
  optionExplanations={['free -h만이 아닌 여러 방법이 있습니다.', 'top만이 아닌 여러 방법이 있습니다.', '/proc/meminfo만이 아닌 여러 방법이 있습니다.', '정답입니다. 위의 세 가지 방법 모두 메모리 사용량을 확인할 수 있습니다.']}
/>

<Quiz
  questionNumber={7}
  question="BIND DNS 서버에서 정방향 조회 영역(Forward Zone) 파일에서 호스트명을 IP 주소로 매핑하는 레코드 유형으로 알맞은 것은?"
  options={['① A 레코드', '② PTR 레코드', '③ MX 레코드', '④ CNAME 레코드']}
  correctAnswer={1}
  explanation="DNS 레코드 유형: A(IPv4 주소 매핑), AAAA(IPv6 주소 매핑), PTR(역방향, IP→도메인), MX(메일 서버), CNAME(별칭), NS(네임서버), SOA(Start of Authority). A 레코드가 정방향(호스트명→IP) 매핑에 사용됩니다."
  optionExplanations={['정답입니다. A 레코드는 호스트명을 IPv4 주소로 매핑합니다.', 'PTR 레코드는 역방향(IP→도메인) 조회에 사용됩니다.', 'MX 레코드는 메일 서버를 지정합니다.', 'CNAME 레코드는 도메인 별칭을 설정합니다.']}
/>

<Quiz
  questionNumber={8}
  question="리눅스에서 로그 파일을 실시간으로 모니터링하는 명령으로 알맞은 것은?"
  options={['① tail -f /var/log/syslog', '② head /var/log/syslog', '③ cat /var/log/syslog', '④ less /var/log/syslog']}
  correctAnswer={1}
  explanation="tail -f 명령은 파일의 끝 부분을 실시간으로 표시합니다. -f(follow) 옵션으로 파일이 업데이트될 때 자동으로 새 내용을 출력합니다. 로그 파일 실시간 모니터링에 가장 많이 사용됩니다."
  optionExplanations={['정답입니다. tail -f는 파일을 실시간으로 모니터링합니다.', 'head는 파일 처음 부분을 출력합니다.', 'cat은 파일 전체를 출력하지만 실시간 업데이트를 지원하지 않습니다.', 'less는 파일을 페이지 단위로 탐색하지만 실시간 업데이트를 기본 지원하지 않습니다.']}
/>

<Quiz
  questionNumber={9}
  question="리눅스에서 setuid(SUID) 비트가 설정된 파일의 특징으로 알맞은 것은?"
  options={['① 파일 소유자만 실행 가능', '② 실행 시 파일 소유자의 권한으로 실행됨', '③ 모든 사용자가 쓰기 가능', '④ 파일을 숨겨서 ls에 표시되지 않음']}
  correctAnswer={2}
  explanation="SUID(Set User ID) 비트가 설정된 실행 파일은 실행하는 사용자가 아닌 파일 소유자의 권한으로 실행됩니다. /usr/bin/passwd가 대표적인 예로 일반 사용자가 실행해도 root 권한으로 /etc/shadow를 수정합니다."
  optionExplanations={['SUID는 실행 권한과 별도 개념입니다.', '정답입니다. SUID 비트는 파일 소유자의 권한으로 실행됩니다.', '모든 사용자 쓰기는 chmod o+w로 설정합니다.', 'SUID와 파일 숨김은 관련 없습니다.']}
/>

<Quiz
  questionNumber={10}
  question="리눅스 부트로더 GRUB2의 설정 파일 위치로 알맞은 것은?"
  options={['① /boot/grub2/grub.cfg', '② /etc/grub.conf', '③ /boot/grub/menu.lst', '④ /etc/default/grub.cfg']}
  correctAnswer={1}
  explanation="GRUB2의 주요 파일: /boot/grub2/grub.cfg(자동 생성되는 실제 설정 파일), /etc/default/grub(커스텀 설정), /etc/grub.d/(설정 스크립트). grub2-mkconfig 명령으로 grub.cfg를 재생성합니다."
  optionExplanations={['정답입니다. /boot/grub2/grub.cfg가 GRUB2의 실제 설정 파일입니다.', '/etc/grub.conf는 GRUB Legacy(구버전)의 파일입니다.', '/boot/grub/menu.lst도 GRUB Legacy의 파일입니다.', '/etc/default/grub.cfg는 잘못된 경로입니다.']}
/>

<Quiz
  questionNumber={11}
  question="Postfix 메일 서버에서 메일 큐를 확인하는 명령으로 알맞은 것은?"
  options={['① mailq 또는 postqueue -p', '② postfix status', '③ mail -l', '④ sendmail -bp']}
  correctAnswer={1}
  explanation="mailq 또는 postqueue -p 명령으로 Postfix 메일 큐에 있는 대기 중인 메일을 확인합니다. postfix status는 Postfix 데몬 상태, sendmail -bp는 Sendmail의 큐 확인 명령입니다."
  optionExplanations={['정답입니다. mailq 또는 postqueue -p로 Postfix 메일 큐를 확인합니다.', 'postfix status는 데몬 상태를 확인합니다.', 'mail -l은 유효한 옵션이 아닙니다.', 'sendmail -bp는 Sendmail의 큐 확인 명령입니다.']}
/>

<Quiz
  questionNumber={12}
  question="Samba 서버에서 삼바 설정 파일의 위치로 알맞은 것은?"
  options={['① /etc/samba/smb.conf', '② /etc/smb/samba.conf', '③ /etc/samba.d/main.conf', '④ /usr/local/samba/etc/smb.conf']}
  correctAnswer={1}
  explanation="Samba 서버의 주 설정 파일은 /etc/samba/smb.conf입니다. [global] 섹션에 전역 설정, [homes] 섹션에 홈 디렉터리 공유, 사용자 정의 섹션에 개별 공유를 설정합니다."
  optionExplanations={['정답입니다. /etc/samba/smb.conf가 Samba 기본 설정 파일입니다.', '잘못된 경로입니다.', '잘못된 경로입니다.', '소스 컴파일 설치 시 경로이며 표준 경로가 아닙니다.']}
/>

<Quiz
  questionNumber={13}
  question="리눅스에서 특정 프로세스가 사용하는 파일과 소켓을 확인하는 명령으로 알맞은 것은?"
  options={['① lsof -p PID', '② ps aux PID', '③ top PID', '④ strace PID']}
  correctAnswer={1}
  explanation="lsof(list open files)는 특정 프로세스(-p PID) 또는 파일, 네트워크 연결 등이 어떤 프로세스에 의해 사용 중인지 확인합니다. lsof -i :80은 80포트를 사용하는 프로세스를 확인합니다."
  optionExplanations={['정답입니다. lsof -p PID로 특정 프로세스가 사용하는 파일과 소켓을 확인합니다.', 'ps aux는 프로세스 목록을 표시합니다.', 'top은 실시간 시스템 모니터링 도구입니다.', 'strace는 시스템 콜을 추적합니다.']}
/>

<Quiz
  questionNumber={14}
  question="리눅스에서 swap 공간을 추가하기 위해 swap 파일을 생성하는 순서로 알맞은 것은?"
  options={['① dd → mkswap → swapon', '② mkswap → dd → swapon', '③ swapon → dd → mkswap', '④ dd → swapon → mkswap']}
  correctAnswer={1}
  explanation="swap 파일 생성 순서: 1)dd if=/dev/zero of=/swapfile bs=1M count=1024(파일 생성) → 2)mkswap /swapfile(스왑 포맷) → 3)swapon /swapfile(활성화). /etc/fstab에 추가하면 부팅 시 자동 활성화됩니다."
  optionExplanations={['정답입니다. dd(생성) → mkswap(포맷) → swapon(활성화) 순서입니다.', 'mkswap을 dd 전에 실행하면 안 됩니다.', 'swapon을 먼저 실행하면 안 됩니다.', 'swapon은 마지막 단계입니다.']}
/>

<Quiz
  questionNumber={15}
  question="NFS(Network File System) 서버에서 /exports 디렉터리를 192.168.1.0/24 네트워크에 읽기/쓰기 권한으로 공유하는 /etc/exports 설정으로 알맞은 것은?"
  options={['① /exports 192.168.1.0/24(rw,sync,no_subtree_check)', '② share /exports 192.168.1.0/24 rw', '③ /exports * rw', '④ nfs /exports 192.168.1.0/24(rw)']}
  correctAnswer={1}
  explanation="/etc/exports 형식: 디렉터리 클라이언트(옵션). rw(읽기쓰기), ro(읽기전용), sync(동기 쓰기), async(비동기), no_root_squash(원격 root를 로컬 root로 매핑), no_subtree_check(하위 트리 검사 비활성화)."
  optionExplanations={['정답입니다. /exports 192.168.1.0/24(rw,sync,no_subtree_check)가 올바른 형식입니다.', 'share 키워드는 /etc/exports 형식이 아닙니다.', '* 와일드카드로 모든 호스트에 공유하지만 네트워크를 지정해야 합니다.', 'nfs 키워드는 /etc/exports 형식이 아닙니다.']}
/>

<Quiz
  questionNumber={16}
  question="리눅스에서 커널 모듈을 로드하는 명령으로 알맞은 것은?"
  options={['① modprobe 모듈명', '② lsmod 모듈명', '③ rmmod 모듈명', '④ depmod 모듈명']}
  correctAnswer={1}
  explanation="커널 모듈 관련 명령: modprobe(모듈 로드, 의존성 자동 처리), insmod(모듈 직접 삽입, 의존성 수동 처리), lsmod(로드된 모듈 목록), rmmod(모듈 제거), depmod(모듈 의존성 파일 생성)."
  optionExplanations={['정답입니다. modprobe는 의존성을 고려하여 커널 모듈을 로드합니다.', 'lsmod는 로드된 모듈 목록을 표시합니다.', 'rmmod는 커널 모듈을 제거합니다.', 'depmod는 모듈 의존성 파일을 업데이트합니다.']}
/>

<Quiz
  questionNumber={17}
  question="리눅스에서 시스템 부팅 메시지를 확인하는 명령으로 알맞은 것은?"
  options={['① dmesg', '② journalctl -b', '③ cat /var/log/boot.log', '④ 위 모두 가능']}
  correctAnswer={4}
  explanation="부팅 메시지 확인 방법: dmesg(커널 링 버퍼 메시지), journalctl -b(systemd 저널에서 현재 부팅 이후 메시지), /var/log/boot.log 또는 /var/log/dmesg(파일로 저장된 부팅 메시지). 모두 부팅 관련 메시지 확인에 사용됩니다."
  optionExplanations={['dmesg만이 아닌 여러 방법이 있습니다.', 'journalctl -b만이 아닌 여러 방법이 있습니다.', '/var/log/boot.log만이 아닌 여러 방법이 있습니다.', '정답입니다. 세 가지 방법 모두 부팅 메시지를 확인하는 데 사용됩니다.']}
/>

<Quiz
  questionNumber={18}
  question="Apache 설정에서 특정 디렉터리의 Directory Listing(파일 목록 표시)을 비활성화하는 설정으로 알맞은 것은?"
  options={['① Options -Indexes', '② AllowOverride None', '③ Deny from all', '④ Order deny,allow']}
  correctAnswer={1}
  explanation="Options -Indexes는 Apache에서 디렉터리 인덱스(파일 목록 표시)를 비활성화합니다. Options +Indexes는 활성화합니다. AllowOverride는 .htaccess 파일 허용 여부, Deny from all은 접근 거부 설정입니다."
  optionExplanations={['정답입니다. Options -Indexes로 디렉터리 목록 표시를 비활성화합니다.', 'AllowOverride None은 .htaccess 파일 사용을 비활성화합니다.', 'Deny from all은 모든 접근을 거부합니다.', 'Order deny,allow는 접근 제어 순서를 설정합니다.']}
/>

<Quiz
  questionNumber={19}
  question="리눅스에서 /etc/sudoers 파일을 안전하게 편집하는 명령으로 알맞은 것은?"
  options={['① visudo', '② vi /etc/sudoers', '③ nano /etc/sudoers', '④ chmod와 vi']}
  correctAnswer={1}
  explanation="visudo 명령은 /etc/sudoers 파일을 안전하게 편집합니다. 저장 전 문법 검사를 수행하여 오류가 있는 설정 저장을 방지합니다. 직접 vi로 편집 시 문법 오류가 있어도 저장되어 sudo가 동작하지 않을 수 있습니다."
  optionExplanations={['정답입니다. visudo는 문법 검사와 함께 /etc/sudoers를 안전하게 편집합니다.', 'vi로 직접 편집 시 문법 오류를 방지할 수 없습니다.', 'nano로 직접 편집 시 문법 오류를 방지할 수 없습니다.', 'chmod 변경 후 편집도 안전하지 않습니다.']}
/>

<Quiz
  questionNumber={20}
  question="리눅스에서 현재 실행 중인 모든 프로세스의 CPU 사용률을 실시간으로 모니터링하는 명령으로 알맞은 것은?"
  options={['① top 또는 htop', '② ps aux', '③ vmstat', '④ iostat']}
  correctAnswer={1}
  explanation="top과 htop은 실시간 프로세스 모니터링 도구입니다. top은 기본 설치, htop은 컬러와 마우스 지원의 향상된 버전입니다. ps aux는 순간 스냅샷, vmstat는 가상 메모리 통계, iostat는 I/O 통계를 표시합니다."
  optionExplanations={['정답입니다. top/htop은 실시간 CPU 및 프로세스 모니터링 도구입니다.', 'ps aux는 실행 시점의 프로세스 스냅샷을 출력합니다.', 'vmstat는 가상 메모리와 시스템 통계를 표시합니다.', 'iostat는 디스크 I/O 통계를 표시합니다.']}
/>

<Quiz
  questionNumber={21}
  question="Keepalived를 사용한 VRRP(Virtual Router Redundancy Protocol)에서 Master 서버 장애 시 Backup 서버가 VIP를 인계받는 메커니즘으로 알맞은 것은?"
  options={['① Master가 VRRP 광고(Advertisement)를 중단하면 Backup이 VIP를 인수함', '② 관리자가 수동으로 VIP를 이전함', '③ DNS 업데이트로 VIP를 변경함', '④ 클라이언트가 자동으로 다른 서버를 선택함']}
  correctAnswer={1}
  explanation="VRRP에서 Master는 주기적으로 Advertisement 패킷을 Backup에 전송합니다. Master 장애 시 Advertisement가 중단되고, Backup은 지정된 시간(deadInterval) 후 Master로 승격되어 VIP(Virtual IP)를 인수합니다."
  optionExplanations={['정답입니다. Master의 광고 중단 시 Backup이 자동으로 VIP를 인수합니다.', '자동으로 처리됩니다.', 'DNS 업데이트는 VRRP 메커니즘이 아닙니다.', '클라이언트는 VIP 변경을 인식하지 못합니다.']}
/>

<Quiz
  questionNumber={22}
  question="리눅스에서 네트워크 패킷을 캡처하는 명령으로 알맞은 것은?"
  options={['① tcpdump', '② netstat', '③ ifconfig', '④ ss']}
  correctAnswer={1}
  explanation="tcpdump는 네트워크 패킷을 실시간으로 캡처하고 분석하는 도구입니다. tcpdump -i eth0 port 80으로 eth0 인터페이스의 80포트 패킷을 캡처할 수 있습니다. Wireshark는 GUI 버전입니다."
  optionExplanations={['정답입니다. tcpdump는 네트워크 패킷 캡처 도구입니다.', 'netstat는 네트워크 연결 상태를 표시합니다.', 'ifconfig는 인터페이스 설정 도구입니다.', 'ss는 소켓 통계를 표시합니다.']}
/>

<Quiz
  questionNumber={23}
  question="리눅스 시스템의 평균 부하(Load Average)를 확인하는 방법으로 알맞은 것은?"
  options={['① uptime 또는 top', '② df -h', '③ free -h', '④ netstat -i']}
  correctAnswer={1}
  explanation="평균 부하(Load Average)는 uptime 명령 또는 top 명령의 상단에서 확인할 수 있습니다. 1분/5분/15분 평균 부하가 표시됩니다. 숫자가 CPU 코어 수보다 높으면 시스템 부하가 높은 상태입니다."
  optionExplanations={['정답입니다. uptime과 top으로 평균 부하를 확인합니다.', 'df -h는 디스크 사용량을 확인합니다.', 'free -h는 메모리 사용량을 확인합니다.', 'netstat -i는 네트워크 인터페이스 통계를 확인합니다.']}
/>

<Quiz
  questionNumber={24}
  question="리눅스에서 PAM(Pluggable Authentication Modules)의 역할로 알맞은 것은?"
  options={['① 패키지 관리', '② 인증 메커니즘을 모듈화하여 유연한 인증 정책 적용 가능', '③ 파일 시스템 마운트', '④ 네트워크 방화벽 설정']}
  correctAnswer={2}
  explanation="PAM(Pluggable Authentication Modules)은 리눅스에서 애플리케이션이 다양한 인증 메커니즘(패스워드, LDAP, Kerberos 등)을 유연하게 사용할 수 있도록 하는 프레임워크입니다. /etc/pam.d/에 서비스별 PAM 설정이 있습니다."
  optionExplanations={['패키지 관리는 apt, yum 등이 담당합니다.', '정답입니다. PAM은 인증 메커니즘을 모듈화합니다.', '파일 시스템 마운트는 mount 명령과 /etc/fstab이 담당합니다.', '방화벽 설정은 iptables/firewalld가 담당합니다.']}
/>

<Quiz
  questionNumber={25}
  question="리눅스에서 MariaDB/MySQL 서비스를 시작하고 부팅 시 자동 시작되도록 설정하는 systemctl 명령으로 알맞은 것은?"
  options={['① systemctl start mariadb; systemctl enable mariadb', '② service mysql start; chkconfig mysql on', '③ mysqld_safe &; rc.local에 추가', '④ init.d/mysql start; update-rc.d mysql defaults']}
  correctAnswer={1}
  explanation="systemd 기반 시스템에서: systemctl start mariadb(즉시 시작), systemctl enable mariadb(부팅 시 자동 시작 설정). systemctl enable과 start를 합친 --now 옵션도 있습니다: systemctl enable --now mariadb."
  optionExplanations={['정답입니다. systemctl start와 enable로 서비스를 시작하고 자동 시작을 설정합니다.', 'service와 chkconfig는 구형 SysV init 방식입니다.', 'mysqld_safe와 rc.local은 구형 방식입니다.', 'init.d와 update-rc.d는 Debian 구형 방식입니다.']}
/>

---
title: '33. 1차 최종 실전 — 전범위 50문항'
description: '1과목(20문) + 2과목(30문) 구성의 리눅스마스터 1급 1차 실전 모의고사입니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 실제 시험과 동일한 유형으로 1과목 20문제, 2과목 30문제를 풀며 합격 실력을 점검합니다.
</Callout>

## 1과목. 리눅스 실무의 이해 (문제 1~20)

<Quiz
  questionNumber={1}
  question="다음 괄호 안에 들어갈 내용으로 알맞은 것은? 리눅스 커널은 ( ㉠ )이(가) 만들었는데, 이것은 ( ㉡ )이(가) 개발한 교육용 유닉스인 미닉스를 참고해서 만들었다."
  options={['① ㉠ 리처드 스톨먼, ㉡ 리누스 토발즈', '② ㉠ 리처드 스톨먼, ㉡ 켄 톰슨', '③ ㉠ 리누스 토발즈, ㉡ 앤드루 S. 타넨바움', '④ ㉠ 켄 톰슨, ㉡ 데니스 리치']}
  correctAnswer={3}
  explanation="리눅스 커널은 리누스 토발즈(Linus Torvalds)가 1991년 만들었습니다. 그는 앤드루 S. 타넨바움(Andrew S. Tanenbaum)이 교육용으로 개발한 유닉스 계열 OS 미닉스(MINIX)를 참고하였습니다. 리처드 스톨먼은 GNU 프로젝트를 이끈 인물이고, 켄 톰슨과 데니스 리치는 유닉스(UNIX)를 개발한 인물입니다."
  optionExplanations={['리처드 스톨먼은 GNU 프로젝트 창시자이며 커널 제작자가 아닙니다.', '리처드 스톨먼이 커널을 만든 것이 아니고, 켄 톰슨은 UNIX 개발자입니다.', '정답입니다. 리누스 토발즈가 커널을 만들었고 앤드루 타넨바움의 MINIX를 참고했습니다.', '켄 톰슨과 데니스 리치는 Bell Labs에서 유닉스를 개발한 인물입니다.']}
/>

<Quiz
  questionNumber={2}
  question="다음 설명에 해당하는 리눅스의 기술적인 특징으로 알맞은 것은? 특정 프로세스의 표준 출력을 다른 프로세스의 표준 입력으로 쓰이게 하는 것으로 대표적인 프로세스 간 통신 기술이다."
  options={['① 리다이렉션', '② 파이프', '③ 라이브러리', '④ 가상 콘솔']}
  correctAnswer={2}
  explanation="파이프(Pipe)는 한 프로세스의 표준 출력(stdout)을 다른 프로세스의 표준 입력(stdin)으로 연결하는 IPC(프로세스 간 통신) 기술입니다. 셸에서 | 기호로 표현합니다. 리다이렉션은 파일과 표준 스트림을 연결하는 것이고, 파이프는 프로세스 간 연결입니다."
  optionExplanations={['리다이렉션(>. >>. <)은 파일과 표준 스트림을 연결하지만 프로세스 간 통신이 아닙니다.', '정답입니다. 파이프(|)는 프로세스의 표준 출력을 다른 프로세스의 표준 입력으로 연결합니다.', '라이브러리는 함수 집합으로 프로세스 간 통신 기술이 아닙니다.', '가상 콘솔(Virtual Console)은 여러 터미널 세션을 제공하는 기능으로 IPC와 무관합니다.']}
/>

<Quiz
  questionNumber={3}
  question="다음 설명에 해당하는 라이선스로 알맞은 것은? 이 라이선스가 부여된 소프트웨어는 누구든 자유롭게 개인적 또는 상업적 목적으로 이용할 수 있다. 재배포 시에도 소스 코드를 반드시 공개할 필요가 없다. 대표적인 프로그램에는 Hadoop, Tomcat 등이 있다."
  options={['① Apache License', '② LGPL', '③ BSD', '④ MPL']}
  correctAnswer={1}
  explanation="Apache License는 소스 코드 공개 의무가 없고 상업적 이용이 자유롭습니다. 수정된 소스를 공개하지 않아도 되며 Hadoop, Tomcat, Kafka 등이 대표적입니다. GPL과 달리 카피레프트 의무가 없어 기업에서 많이 채택합니다."
  optionExplanations={['정답입니다. Apache License는 소스 공개 의무 없이 자유로운 상업적 이용을 허용합니다.', 'LGPL은 라이브러리 링크 시 소스 공개 의무를 완화한 GPL 변형 라이선스입니다.', 'BSD 라이선스도 소스 공개 의무는 없지만 Apache License와는 별개의 라이선스입니다.', 'MPL(Mozilla Public License)은 수정한 파일의 소스를 공개해야 하는 파일 단위 카피레프트 라이선스입니다.']}
/>

<Quiz
  questionNumber={4}
  question="다음 설명에 해당하는 리눅스 배포판으로 가장 알맞은 것은? 패키지 설치 및 업데이트할 때 dpkg 또는 apt-get 명령을 사용하고, 칼리 리눅스도 이 리눅스 배포판을 기반으로 만들었다."
  options={['① Rocky Linux', '② CentOS', '③ Debian', '④ Ubuntu']}
  correctAnswer={3}
  explanation="Debian은 dpkg와 apt-get을 사용하는 대표 배포판으로, Kali Linux, Ubuntu 등이 Debian을 기반으로 파생되었습니다. Rocky Linux와 CentOS는 Red Hat 계열로 rpm, yum, dnf를 사용합니다. Ubuntu도 Debian 기반이지만 문제의 '이 배포판'은 기반이 된 Debian을 묻고 있습니다."
  optionExplanations={['Rocky Linux는 Red Hat 계열로 rpm/dnf 기반입니다.', 'CentOS도 Red Hat 계열로 rpm/yum/dnf를 사용합니다.', '정답입니다. Debian이 dpkg/apt-get의 원조이며 칼리 리눅스의 기반 배포판입니다.', 'Ubuntu는 Debian을 기반으로 만들어졌으므로 dpkg/apt-get을 사용하지만 문제에서 기반이 된 배포판을 묻습니다.']}
/>

<Quiz
  questionNumber={5}
  question="다수의 웹 서버 앞단에 로드 밸런서를 운영 중이다. 로드 밸런서 역할을 수행하는 시스템의 오류에 대비하려고 할 때 구성해야 할 인프라 기술로 알맞은 것은?"
  options={['① 고가용성 클러스터', '② 고계산용 클러스터', '③ 베어울프 클러스터', '④ HPC 클러스터']}
  correctAnswer={1}
  explanation="고가용성(HA, High Availability) 클러스터는 주 노드에 장애가 발생했을 때 예비 노드가 서비스를 이어받아 중단 없이 운영할 수 있도록 구성합니다. 베어울프·HPC 클러스터는 대규모 계산 성능 향상이 목적입니다."
  optionExplanations={['정답입니다. 고가용성 클러스터는 장애 발생 시 서비스 연속성을 보장합니다.', '고계산용 클러스터는 대규모 연산·시뮬레이션을 위한 클러스터입니다.', '베어울프 클러스터는 일반 PC를 병렬로 연결해 계산 성능을 높이는 고계산용 클러스터의 일종입니다.', 'HPC(High Performance Computing) 클러스터는 고계산용 클러스터의 다른 표현입니다.']}
/>

<Quiz
  questionNumber={6}
  question="10GB 용량의 하드디스크 6개가 장착된 시스템에서 스패어(Spare) 디스크 1개를 제외하고 나머지로 RAID-5를 구성할 때 사용 가능한 실제 디스크 용량으로 알맞은 것은?"
  options={['① 30GB', '② 40GB', '③ 50GB', '④ 60GB']}
  correctAnswer={2}
  explanation="스패어 1개를 제외하면 5개 디스크로 RAID-5를 구성합니다. RAID-5는 패리티 1개 분량이 사용되므로 사용 가능 용량은 (디스크 수 - 1) x 디스크 용량입니다. (5 - 1) x 10GB = 40GB입니다."
  optionExplanations={['30GB는 4개 디스크 RAID-5 구성 시의 용량입니다. (4-1) x 10 = 30.', '정답입니다. 스패어 제외 5개 디스크로 RAID-5 구성 시 (5-1) x 10 = 40GB입니다.', '50GB는 스패어 없이 6개 전부로 RAID-5를 구성할 때의 용량입니다. (6-1) x 10 = 50.', '60GB는 RAID-0처럼 패리티 없이 전체 용량을 사용할 때의 합계입니다.']}
/>

<Quiz
  questionNumber={7}
  question="리눅스에서 사용 가능한 파일시스템 중 B-트리 기반의 Copy-on-Write 구조를 채택하여 스냅샷, 압축, 서브볼륨을 지원하는 차세대 파일시스템으로 알맞은 것은?"
  options={['① ext4', '② xfs', '③ btrfs', '④ vfat']}
  correctAnswer={3}
  explanation="btrfs(B-Tree File System)는 Copy-on-Write 방식을 사용하며 스냅샷, 서브볼륨, 온라인 압축, 내장 RAID, 투명한 압축 등의 고급 기능을 제공합니다. ext4는 저널링 기반의 범용 파일시스템, xfs는 대용량 파일 처리에 강한 64비트 파일시스템입니다."
  optionExplanations={['ext4는 리눅스 전통적인 저널링 파일시스템으로 안정적이지만 Copy-on-Write와 스냅샷은 지원하지 않습니다.', 'xfs는 대용량 파일·고성능 I/O에 강하지만 Copy-on-Write 기반이 아닙니다.', '정답입니다. btrfs는 Copy-on-Write, 스냅샷, 서브볼륨, 내장 RAID를 제공하는 차세대 파일시스템입니다.', 'vfat는 Windows와의 호환성을 위한 FAT 기반 파일시스템으로 리눅스 고급 기능을 지원하지 않습니다.']}
/>

<Quiz
  questionNumber={8}
  question="inode에 저장되는 정보로 가장 거리가 먼 것은?"
  options={['① 파일 크기', '② 파일 소유자 UID', '③ 파일의 실제 데이터 블록 위치', '④ 파일 이름']}
  correctAnswer={4}
  explanation="inode에는 파일 크기, 소유자(UID/GID), 권한(퍼미션), 타임스탬프(생성·수정·접근), 링크 수, 데이터 블록 포인터 등이 저장됩니다. 파일 이름은 inode가 아닌 디렉토리 엔트리에 inode 번호와 함께 저장됩니다."
  optionExplanations={['파일 크기(byte 단위)는 inode에 저장됩니다.', '파일 소유자 UID와 GID는 inode에 저장됩니다.', '데이터 블록의 물리적 위치(포인터)는 inode의 핵심 정보입니다.', '정답입니다. 파일 이름은 inode가 아닌 디렉토리 엔트리에 기록됩니다. inode 번호와 파일 이름이 디렉토리에 함께 저장됩니다.']}
/>

<Quiz
  questionNumber={9}
  question="하드링크와 심볼릭링크에 대한 비교 설명으로 틀린 것은?"
  options={['① 하드링크는 원본 파일과 동일한 inode 번호를 공유한다.', '② 심볼릭링크는 원본 파일이 삭제되면 링크가 깨진다.', '③ 하드링크는 파티션을 초월하여 다른 파일시스템에도 생성할 수 있다.', '④ ln -s 명령으로 심볼릭링크를 생성한다.']}
  correctAnswer={3}
  explanation="하드링크는 동일한 파일시스템 내에서만 생성할 수 있으며, 다른 파티션이나 파일시스템을 초월할 수 없습니다. 심볼릭링크는 다른 파일시스템의 경로를 참조할 수 있어 파티션을 초월한 링크가 가능합니다."
  optionExplanations={['하드링크는 원본과 동일한 inode를 가리키므로 inode 번호가 같습니다. 올바른 설명입니다.', '심볼릭링크는 경로를 저장하므로 원본이 삭제되면 dangling link(깨진 링크)가 됩니다. 올바른 설명입니다.', '정답입니다(틀린 설명). 하드링크는 같은 파일시스템 내에서만 생성 가능하며 파티션을 초월할 수 없습니다.', 'ln -s 원본경로 링크명 으로 심볼릭링크를 생성합니다. 올바른 설명입니다.']}
/>

<Quiz
  questionNumber={10}
  question="chmod 754 파일명 명령으로 설정되는 파일 권한을 ls -l로 출력했을 때 알맞은 것은?"
  options={['① -rwxr-xr--', '② -rwxr--r-x', '③ -rwxrw-r--', '④ -rwxr-xrw-']}
  correctAnswer={1}
  explanation="8진수 권한 754는 소유자=7(rwx), 그룹=5(r-x), 기타=4(r--)로 변환됩니다. 소유자는 읽기·쓰기·실행(rwx), 그룹은 읽기·실행(r-x), 기타는 읽기만(r--)이므로 -rwxr-xr-- 입니다."
  optionExplanations={['정답입니다. 7=rwx, 5=r-x, 4=r-- 이므로 -rwxr-xr-- 입니다.', '이것은 755(rwxr-xr-x)가 아닌 잘못된 조합입니다. 기타 부분이 r-x이면 5이어야 합니다.', '이것은 764(rwxrw-r--)에 해당합니다.', '이것은 756(rwxr-xrw-)에 해당합니다.']}
/>

<Quiz
  questionNumber={11}
  question="다음과 같이 허가권이 설정된 경우에 관련 설명으로 알맞은 것은? ls -l /usr/bin/passwd 결과: -rwsr-xr-x. 1 root root ... /usr/bin/passwd"
  options={['① 이 파일은 root 사용자만 실행할 수 있다.', '② ihduser 사용자가 실행하면 ihduser 사용자 권한으로 실행된다.', '③ ihduser 사용자가 실행하면 실행하는 동안 root 사용자 권한으로 인정된다.', '④ ihduser 사용자가 실행하면 실행하는 동안 root 그룹 권한으로 인정된다.']}
  correctAnswer={3}
  explanation="소유자 실행 권한 자리(x)에 s가 있으면 SetUID(SUID)입니다. SUID가 설정된 파일을 일반 사용자가 실행하면 파일 소유자(root)의 권한으로 동작합니다. /usr/bin/passwd가 대표적인 예로, 일반 사용자가 자신의 패스워드를 변경할 수 있는 이유가 SUID 덕분입니다."
  optionExplanations={['기타(others) 실행 권한(r-x)이 있으므로 모든 사용자가 실행할 수 있습니다.', 'SUID가 설정되어 있으므로 ihduser가 실행해도 파일 소유자인 root 권한으로 실행됩니다.', '정답입니다. SUID 설정으로 인해 실행하는 동안 파일 소유자(root)의 UID로 동작합니다.', 'SUID는 소유자 UID 권한을 부여하며 그룹 권한이 아닙니다. root 그룹 권한은 SGID에 해당합니다.']}
/>

<Quiz
  questionNumber={12}
  question="현재 로그인한 사용자의 홈 디렉토리 경로를 저장하는 환경변수로 알맞은 것은?"
  options={['① $PATH', '② $HOME', '③ $SHELL', '④ $PWD']}
  correctAnswer={2}
  explanation="$HOME은 현재 사용자의 홈 디렉토리 경로(예: /home/user1)를 저장합니다. $PATH는 명령어 검색 경로 목록, $SHELL은 기본 로그인 셸 경로, $PWD는 현재 작업 디렉토리 경로를 저장합니다."
  optionExplanations={['$PATH는 콜론(:)으로 구분된 명령어 탐색 경로 목록을 저장합니다.', '정답입니다. $HOME은 현재 사용자의 홈 디렉토리 절대 경로를 저장합니다.', '$SHELL은 현재 사용자의 기본 로그인 셸 경로(예: /bin/bash)를 저장합니다.', '$PWD는 현재 작업 중인 디렉토리(Present Working Directory)의 절대 경로를 저장합니다.']}
/>

<Quiz
  questionNumber={13}
  question="다음 명령의 결과값으로 알맞은 것은? test 20 -gt 30 ; echo $?"
  options={['① 0', '② 1', '③ 10', '④ -10']}
  correctAnswer={2}
  explanation="test 20 -gt 30은 20이 30보다 크다는 조건을 검사합니다. 20은 30보다 크지 않으므로 조건이 거짓(false)이고, test 명령의 종료 상태($?)는 1(실패)이 됩니다. 조건이 참이면 0을 반환합니다."
  optionExplanations={['0은 명령이 성공했을 때 반환됩니다. 20 -gt 30은 거짓이므로 0이 아닙니다.', '정답입니다. 20은 30보다 크지 않으므로 조건이 거짓이고 $?는 1입니다.', 'test 명령의 종료 상태는 0(참) 또는 1(거짓)만 반환합니다.', 'test 명령은 음수 종료 상태를 반환하지 않습니다.']}
/>

<Quiz
  questionNumber={14}
  question="command 1 &gt; file.txt 2&gt;&amp;1 명령에 대한 설명으로 알맞은 것은?"
  options={['① 표준 출력을 file.txt에 저장하고 표준 오류는 화면에 출력한다.', '② 표준 출력과 표준 오류 모두 file.txt에 저장한다.', '③ 표준 오류를 file.txt에 저장하고 표준 출력은 화면에 출력한다.', '④ 표준 출력을 표준 오류로 리다이렉션한다.']}
  correctAnswer={2}
  explanation="1 &gt; file.txt는 표준 출력(fd 1)을 file.txt로 리다이렉션합니다. 2&gt;&amp;1은 표준 오류(fd 2)를 현재 fd 1이 가리키는 곳(file.txt)으로 리다이렉션합니다. 따라서 표준 출력과 표준 오류 모두 file.txt에 저장됩니다."
  optionExplanations={['2>&1로 인해 표준 오류도 file.txt로 리다이렉션되므로 화면에 출력되지 않습니다.', '정답입니다. 표준 출력(1)을 file.txt에 저장하고, 표준 오류(2)를 fd 1(file.txt)로 리다이렉션합니다.', '표준 오류가 아닌 표준 출력이 먼저 file.txt로 리다이렉션됩니다.', '2>&1은 fd 2를 fd 1로 리다이렉션하는 것이지 그 반대가 아닙니다.']}
/>

<Quiz
  questionNumber={15}
  question="표준 입력(stdin), 표준 출력(stdout), 표준 오류(stderr)의 파일 디스크립터 번호를 순서대로 나열한 것으로 알맞은 것은?"
  options={['① 0, 1, 2', '② 1, 0, 2', '③ 0, 2, 1', '④ 1, 2, 0']}
  correctAnswer={1}
  explanation="유닉스/리눅스에서 표준 입력(stdin)은 파일 디스크립터 0, 표준 출력(stdout)은 1, 표준 오류(stderr)는 2입니다. 이 번호는 리다이렉션 시에 사용되며 2&gt;&amp;1처럼 명시적으로 지정하거나 생략하면 기본값으로 적용됩니다."
  optionExplanations={['정답입니다. stdin=0, stdout=1, stderr=2 순서입니다.', 'stdin이 0이고 stdout이 1입니다. 순서가 바뀌었습니다.', 'stderr는 2이고 stdout은 1입니다. 순서가 잘못되었습니다.', 'stdout이 1이고 stdin이 0입니다. 순서가 잘못되었습니다.']}
/>

<Quiz
  questionNumber={16}
  question="grep 명령에서 숫자로만 시작하는 행을 찾는 정규표현식으로 알맞은 것은?"
  options={['① grep [0-9] 파일명', '② grep ^[0-9] 파일명', '③ grep [0-9]$ 파일명', '④ grep .[0-9] 파일명']}
  correctAnswer={2}
  explanation="정규표현식에서 ^는 행의 시작을 의미하고 [0-9]는 0~9 사이의 숫자 한 글자를 의미합니다. ^[0-9]는 숫자로 시작하는 행을 매칭합니다. $는 행의 끝, .은 임의의 한 문자를 의미합니다."
  optionExplanations={['[0-9]만 사용하면 행 어디에든 숫자가 있는 행을 찾습니다. 시작 위치를 제한하지 않습니다.', '정답입니다. ^[0-9]는 행의 시작(^)이 숫자([0-9])인 행을 찾습니다.', '[0-9]$는 숫자로 끝나는 행을 찾습니다.', '.[0-9]는 임의의 한 문자 다음에 숫자가 오는 패턴을 찾습니다.']}
/>

<Quiz
  questionNumber={17}
  question="다음 설명에 해당하는 가상화 방식으로 알맞은 것은? VMM(Virtual Machine Monitor)과 원활한 통신을 위해 게스트 운영체제의 커널 일부분을 수정해 적용한다. 이를 통해 게스트 운영체제에서 물리적 자원에 대해 직접 접근이 가능해진다."
  options={['① 완전 가상화(전가상화)', '② 반가상화(파라가상화)', '③ OS 수준 가상화', '④ 에뮬레이션']}
  correctAnswer={2}
  explanation="반가상화(Para-virtualization)는 게스트 OS 커널을 수정하여 하이퍼바이저와 직접 통신(Hypercall)하는 방식으로 Xen이 대표적입니다. 전가상화는 게스트 OS를 수정하지 않고 하드웨어를 완전히 에뮬레이션하며, OS 수준 가상화는 Docker/LXC 같은 컨테이너 방식입니다."
  optionExplanations={['완전 가상화는 게스트 OS를 수정하지 않으며 하드웨어를 소프트웨어로 에뮬레이션합니다.', '정답입니다. 반가상화는 게스트 OS 커널을 수정해 하이퍼바이저와 직접 통신하여 성능을 향상시킵니다.', 'OS 수준 가상화(컨테이너)는 호스트 OS 커널을 공유하며 별도 게스트 OS가 없습니다.', '에뮬레이션은 하드웨어 자체를 소프트웨어로 흉내 내는 방식으로 성능 오버헤드가 큽니다.']}
/>

<Quiz
  questionNumber={18}
  question="다음 설명에 해당하는 프로그램으로 알맞은 것은? 구글이 개발을 시작한 컨테이너 관리 프로그램으로 컨테이너화된 애플리케이션의 배포, 확장, 관리를 자동화해준다."
  options={['① Docker', '② CoreOS', '③ Kubernetes', '④ OpenStack']}
  correctAnswer={3}
  explanation="Kubernetes(쿠버네티스)는 구글이 개발을 시작하여 CNCF(Cloud Native Computing Foundation)에 기부한 오픈소스 컨테이너 오케스트레이션 플랫폼입니다. Docker는 컨테이너 실행 엔진이며, Kubernetes는 다수의 컨테이너를 클러스터 환경에서 관리합니다."
  optionExplanations={['Docker는 컨테이너 이미지 빌드와 실행을 담당하는 컨테이너 런타임입니다.', 'CoreOS(Container Linux)는 컨테이너 실행에 최적화된 경량 리눅스 배포판입니다.', '정답입니다. Kubernetes는 구글이 개발한 컨테이너 오케스트레이션 플랫폼입니다.', 'OpenStack은 IaaS 기반 클라우드 인프라 플랫폼으로 컨테이너 관리가 주 목적이 아닙니다.']}
/>

<Quiz
  questionNumber={19}
  question="커널에 적재된 모듈을 의존성을 포함하여 제거하는 명령으로 알맞은 것은?"
  options={['① insmod -r 모듈명', '② depmod -r 모듈명', '③ rmmod 모듈명', '④ modprobe -r 모듈명']}
  correctAnswer={4}
  explanation="modprobe -r은 지정한 모듈과 그 모듈에 의존하는 다른 모듈까지 함께 제거합니다. rmmod는 단일 모듈을 제거하지만 의존성을 자동으로 처리하지 않습니다. insmod는 모듈 적재, depmod는 의존성 데이터베이스 갱신 명령입니다."
  optionExplanations={['insmod는 모듈 적재 명령이며 -r 옵션이 없습니다.', 'depmod는 커널 모듈 의존성 데이터베이스(modules.dep)를 갱신하는 명령입니다.', 'rmmod는 단일 모듈을 제거하지만 의존 모듈을 자동으로 처리하지 않습니다.', '정답입니다. modprobe -r은 의존성을 고려하여 모듈과 관련 모듈을 함께 제거합니다.']}
/>

<Quiz
  questionNumber={20}
  question="systemd를 사용하는 시스템에서 부팅 시 X 윈도우 그래픽 모드로 시작하도록 설정하는 명령으로 알맞은 것은?"
  options={['① systemctl get-default multi-user.target', '② systemctl set-default multi-user.target', '③ systemctl get-default graphical.target', '④ systemctl set-default graphical.target']}
  correctAnswer={4}
  explanation="systemctl set-default는 기본 부팅 타겟을 변경합니다. graphical.target은 X 윈도우 그래픽 환경(구 런레벨 5)이고, multi-user.target은 텍스트 다중 사용자 환경(구 런레벨 3)입니다. get-default는 현재 기본 타겟 확인 명령입니다."
  optionExplanations={['get-default는 현재 설정된 기본 타겟을 확인하는 명령이며 변경이 아닙니다.', 'multi-user.target은 텍스트 모드(CLI) 부팅 설정입니다.', 'get-default는 확인용 명령이며 graphical.target을 지정해도 변경되지 않습니다.', '정답입니다. set-default graphical.target으로 X 윈도우 그래픽 모드를 기본 부팅 타겟으로 설정합니다.']}
/>

---

## 2과목. 리눅스 시스템 관리 (문제 21~50)

<Quiz
  questionNumber={21}
  question="ssh 데몬이 리눅스 부팅 시에 자동으로 실행되도록 설정하는 명령으로 알맞은 것은?"
  options={['① systemctl enable sshd', '② systemctl status sshd', '③ systemctl active sshd', '④ systemctl start sshd']}
  correctAnswer={1}
  explanation="systemctl enable은 서비스 유닛 파일의 심볼릭 링크를 wants 디렉토리에 생성하여 부팅 시 자동 시작을 등록합니다. start는 지금 즉시 시작(재부팅 후 유지 안 됨), status는 현재 상태 확인, active는 유효한 서브명령이 아닙니다(is-active가 맞습니다)."
  optionExplanations={['정답입니다. systemctl enable sshd는 부팅 시 sshd가 자동으로 시작되도록 등록합니다.', 'status는 서비스의 현재 실행 상태와 최근 로그를 확인합니다.', 'active는 유효한 systemctl 서브명령이 아닙니다. 실행 여부 확인은 is-active입니다.', 'start는 서비스를 지금 즉시 시작하지만 재부팅 후에는 유지되지 않습니다.']}
/>

<Quiz
  questionNumber={22}
  question="cron을 이용해서 매주 수요일과 금요일 오전 1시 2분에 /etc/check.sh를 실행하려고 한다. 다음 중 crontab 항목으로 알맞은 것은?"
  options={['① 1 2 * * 3,5 /etc/check.sh', '② 2 1 * * 3,5 /etc/check.sh', '③ 1 2 * * 3-5 /etc/check.sh', '④ 2 1 * * 3-5 /etc/check.sh']}
  correctAnswer={2}
  explanation="crontab 필드 순서는 '분(0-59) 시(0-23) 일 월 요일'입니다. 오전 1시 2분은 분=2, 시=1입니다. 수요일=3, 금요일=5이며, 콤마(,)는 비연속 요일 나열이고 하이픈(-)은 범위(3-5=수,목,금)를 의미합니다."
  optionExplanations={['1 2는 오전 2시 1분을 의미합니다. 오전 1시 2분은 2 1이어야 합니다.', '정답입니다. 분=2, 시=1, 요일=3,5(수,금)으로 오전 1시 2분 수·금요일 실행입니다.', '3-5는 수,목,금 범위를 의미하여 목요일도 포함됩니다.', '시간은 맞지만 3-5 범위 지정으로 목요일도 포함됩니다.']}
/>

<Quiz
  questionNumber={23}
  question="useradd 명령으로 사용자를 생성할 때 홈 디렉토리를 /data/joon으로 지정하는 옵션으로 알맞은 것은?"
  options={['① -d /data/joon', '② -D /data/joon', '③ -m /data/joon', '④ -h /data/joon']}
  correctAnswer={1}
  explanation="useradd -d 옵션은 사용자의 홈 디렉토리 경로를 지정합니다. -D는 useradd 기본값을 표시하거나 변경, -m은 홈 디렉토리가 없을 때 생성, -h는 도움말 출력 옵션입니다. 실제로 디렉토리를 생성하려면 -m 옵션을 함께 사용해야 합니다."
  optionExplanations={['정답입니다. -d 옵션으로 사용자의 홈 디렉토리 경로를 지정합니다.', '-D 옵션은 useradd의 기본 설정값을 표시하거나 변경하는 옵션입니다.', '-m 옵션은 홈 디렉토리를 실제로 생성하는 옵션이며 경로 지정은 -d로 합니다.', '-h 옵션은 useradd 도움말을 출력합니다.']}
/>

<Quiz
  questionNumber={24}
  question="다음 결과에 해당하는 명령어로 알맞은 것은? 출력 예: Last password change: never / Password expires: never / Minimum number of days between password change: 0 / Maximum number of days between password change: 99999"
  options={['① passwd -l 사용자명', '② chage -l 사용자명', '③ usermod -l 사용자명', '④ chpasswd -l 사용자명']}
  correctAnswer={2}
  explanation="chage -l은 사용자의 패스워드 에이징(aging) 정보를 출력합니다. 마지막 패스워드 변경일, 만료일, 비활성화 기간, 계정 만료일, 경고 일수, 패스워드 변경 최소/최대 일수 등을 확인할 수 있습니다."
  optionExplanations={['passwd -l(소문자 L)은 계정 패스워드를 잠금(lock)하는 명령입니다.', '정답입니다. chage -l은 패스워드 및 계정 만료 정책 정보를 상세하게 출력합니다.', 'usermod -l(소문자 L)은 사용자의 로그인 이름(username)을 변경합니다.', 'chpasswd는 파일에서 패스워드를 일괄 변경하는 명령으로 -l 옵션이 없습니다.']}
/>

<Quiz
  questionNumber={25}
  question="그룹 관리자 정보를 확인할 수 있는 파일과 필드로 알맞은 것은?"
  options={['① /etc/group 파일의 3번째 필드', '② /etc/group 파일의 4번째 필드', '③ /etc/gshadow 파일의 3번째 필드', '④ /etc/gshadow 파일의 4번째 필드']}
  correctAnswer={3}
  explanation="/etc/gshadow 파일의 구조는 '그룹명:암호화된 패스워드:그룹관리자:그룹멤버'입니다. 3번째 필드가 그룹 관리자(group administrator), 4번째 필드가 그룹 멤버 목록입니다. /etc/group의 3번째 필드는 GID입니다."
  optionExplanations={['/etc/group의 3번째 필드는 GID(Group ID)입니다.', '/etc/group의 4번째 필드는 그룹 멤버 목록입니다.', '정답입니다. /etc/gshadow의 3번째 필드가 그룹 관리자입니다.', '/etc/gshadow의 4번째 필드는 그룹 멤버 목록입니다.']}
/>

<Quiz
  questionNumber={26}
  question="설치된 httpd 패키지의 환경 설정 파일 목록만 빠르게 확인하는 rpm 명령으로 알맞은 것은?"
  options={['① rpm -qa httpd', '② rpm -qf httpd', '③ rpm -qc httpd', '④ rpm -ql httpd']}
  correctAnswer={3}
  explanation="rpm -qc(query config)는 패키지에 포함된 설정 파일(%config)만 출력합니다. -qa는 설치된 전체 패키지 목록, -qf는 파일이 속한 패키지 확인, -ql은 패키지의 전체 파일 목록을 출력합니다."
  optionExplanations={['rpm -qa는 설치된 모든 패키지 목록을 출력합니다. 특정 패키지 지정 시 설치 여부를 확인합니다.', 'rpm -qf는 특정 파일이 어느 패키지에 속하는지 확인합니다(예: rpm -qf /etc/httpd/conf/httpd.conf).', '정답입니다. rpm -qc는 설정 파일(config file) 목록만 추출합니다.', 'rpm -ql은 패키지에 포함된 모든 파일 경로를 출력합니다.']}
/>

<Quiz
  questionNumber={27}
  question="ps aux 명령 출력에서 프로세스 상태(STAT) 필드가 'Z'를 나타내는 프로세스 상태로 알맞은 것은?"
  options={['① 실행 대기 중인 프로세스', '② 슬립(sleep) 상태의 프로세스', '③ 좀비(zombie) 프로세스', '④ 백그라운드에서 실행 중인 프로세스']}
  correctAnswer={3}
  explanation="ps aux의 STAT 필드에서 Z는 좀비(Zombie) 프로세스를 의미합니다. 좀비 프로세스는 실행이 종료되었지만 부모 프로세스가 wait()를 호출하지 않아 프로세스 테이블에서 제거되지 않은 상태입니다. R은 실행 중, S는 인터럽트 가능한 슬립, D는 인터럽트 불가능한 슬립입니다."
  optionExplanations={['실행 대기 중(Runnable)은 R 상태입니다.', '슬립 상태는 S(인터럽트 가능) 또는 D(인터럽트 불가능)입니다.', '정답입니다. Z는 좀비(Zombie) 프로세스로 종료되었지만 부모가 회수하지 않은 상태입니다.', '백그라운드 실행은 별도의 STAT 코드가 아니며 일반적으로 S나 R 상태일 수 있습니다.']}
/>

<Quiz
  questionNumber={28}
  question="실행 중인 PID 513번 bash 프로세스의 NI 값을 높여(우선순위를 낮춰) 10으로 변경하는 명령으로 알맞은 것은?"
  options={['① nice 10 513', '② nice 10 bash', '③ renice 10 513', '④ renice 10 bash']}
  correctAnswer={3}
  explanation="renice는 이미 실행 중인 프로세스의 NI 값을 변경합니다. PID로 지정하는 것이 기본이며 renice 10 513 형식으로 사용합니다. nice는 새 명령을 실행할 때 NI 값을 지정합니다. renice로 프로세스 이름을 직접 지정할 수 없습니다."
  optionExplanations={['nice는 새 프로세스를 시작할 때 사용하며 PID를 인수로 받지 않습니다.', 'nice는 명령어명을 인수로 받아 새 프로세스를 실행합니다.', '정답입니다. renice NI값 PID 형식으로 실행 중인 프로세스의 우선순위를 변경합니다.', 'renice는 프로세스 이름이 아닌 PID나 -u 사용자명, -g 그룹명으로 대상을 지정합니다.']}
/>

<Quiz
  questionNumber={29}
  question="다음 중 시그널 번호가 가장 큰 것으로 알맞은 것은?"
  options={['① SIGTSTP', '② SIGKILL', '③ SIGTERM', '④ SIGQUIT']}
  correctAnswer={1}
  explanation="주요 시그널 번호: SIGHUP=1, SIGINT=2, SIGQUIT=3, SIGKILL=9, SIGTERM=15, SIGTSTP=20입니다. SIGTSTP(20)가 가장 큰 번호입니다. SIGKILL(9)은 강제 종료, SIGTERM(15)은 정상 종료 요청, SIGQUIT(3)은 코어 덤프 후 종료입니다."
  optionExplanations={['정답입니다. SIGTSTP는 번호 20으로 이 중 가장 큽니다. Ctrl+Z를 눌렀을 때 발생합니다.', 'SIGKILL은 번호 9입니다.', 'SIGTERM은 번호 15입니다.', 'SIGQUIT는 번호 3입니다.']}
/>

<Quiz
  questionNumber={30}
  question="현재 마운트된 파일시스템별 디스크 사용량을 사람이 읽기 쉬운 단위(KB, MB, GB)로 출력하는 명령으로 알맞은 것은?"
  options={['① df -h', '② du -sh', '③ du -h /', '④ df -i']}
  correctAnswer={1}
  explanation="df -h는 마운트된 파일시스템 전체의 사용량·여유 공간을 사람이 읽기 쉬운 단위(human-readable)로 출력합니다. du는 특정 디렉토리의 사용량을 조회하며, df -i는 inode 사용률을 확인합니다."
  optionExplanations={['정답입니다. df -h는 파일시스템별 전체/사용/여유 용량을 읽기 쉬운 단위로 표시합니다.', 'du -sh는 특정 디렉토리의 전체 사용량을 요약(s)해서 사람이 읽기 쉽게(h) 출력합니다.', 'du -h /는 루트부터 모든 하위 디렉토리별 사용량을 나열합니다.', 'df -i는 파일시스템의 inode 사용률을 확인합니다.']}
/>

<Quiz
  questionNumber={31}
  question="fdisk 명령으로 RAID 구성을 위한 파티션 타입(Partition ID)을 설정할 때 사용하는 코드값으로 알맞은 것은?"
  options={['① 82', '② 83', '③ 8e', '④ fd']}
  correctAnswer={4}
  explanation="fdisk에서 파티션 타입 코드 fd는 Linux RAID autodetect에 해당합니다. 82는 Linux swap, 83은 Linux 일반 파티션, 8e는 Linux LVM입니다. RAID 파티션 구성 시에는 fd로 설정합니다."
  optionExplanations={['82는 Linux swap 파티션 코드입니다.', '83은 일반 Linux 파티션(ext4, xfs 등) 코드입니다.', '8e는 Linux LVM 파티션 코드입니다.', '정답입니다. fd는 Linux RAID autodetect 파티션 코드로 mdadm으로 RAID 구성 시 사용합니다.']}
/>

<Quiz
  questionNumber={32}
  question="/etc/fstab 파일에서 UUID=abc123 /data ext4 defaults,noatime 0 2 항목의 마지막 숫자 '2'가 의미하는 것으로 알맞은 것은?"
  options={['① dump 백업을 2회 수행', '② fsck 검사를 루트 파일시스템 이후에 수행', '③ 파티션 우선순위 2번', '④ 마운트 순서 2번']}
  correctAnswer={2}
  explanation="/etc/fstab의 6번째 필드(pass)는 fsck 검사 순서를 지정합니다. 0=검사 안 함, 1=루트 파일시스템(/ 파티션에 사용), 2=루트 이후 나머지 파일시스템에 사용합니다. 5번째 필드(dump)는 dump 백업 여부입니다."
  optionExplanations={['5번째 필드(dump)가 0이므로 dump 백업은 수행하지 않습니다. 6번째 필드는 fsck 관련입니다.', '정답입니다. pass 필드 2는 루트(/)보다 나중에 fsck 검사를 수행함을 의미합니다.', 'fstab에는 파티션 우선순위 개념이 없습니다.', 'fstab의 숫자 필드는 마운트 순서가 아닙니다.']}
/>

<Quiz
  questionNumber={33}
  question="LVM을 구성하는 올바른 순서로 알맞은 것은?"
  options={['① vgcreate → pvcreate → lvcreate', '② pvcreate → lvcreate → vgcreate', '③ pvcreate → vgcreate → lvcreate', '④ lvcreate → vgcreate → pvcreate']}
  correctAnswer={3}
  explanation="LVM 구성 순서는 물리 볼륨(PV) → 볼륨 그룹(VG) → 논리 볼륨(LV) 순입니다. pvcreate로 물리 디스크/파티션을 PV로 초기화하고, vgcreate로 PV를 묶어 VG를 생성한 다음, lvcreate로 VG에서 LV를 할당합니다."
  optionExplanations={['VG를 생성하기 전에 PV가 먼저 존재해야 합니다. 순서가 잘못되었습니다.', 'LV 생성은 VG가 있어야 가능합니다. lvcreate가 vgcreate 앞에 올 수 없습니다.', '정답입니다. PV → VG → LV 순으로 구성합니다.', '역순으로 LV를 먼저 만들 수 없습니다.']}
/>

<Quiz
  questionNumber={34}
  question="다음 그림과 같이 네트워크 인터페이스의 IP 주소, MAC 주소, 상태 등을 출력하는 명령으로 알맞은 것은? 출력 예: 2: enp0s3: BROADCAST,MULTICAST,UP / link/ether 08:00:27:ab:b0:cb / inet 10.0.2.15/24"
  options={['① ip addr', '② ifconfig', '③ route', '④ ss']}
  correctAnswer={1}
  explanation="ip addr 명령은 네트워크 인터페이스의 IP 주소, MAC 주소, 상태, MTU 등 상세 정보를 출력합니다. 표시 형식에서 link/ether, inet, inet6 항목이 보이는 것이 ip 명령의 특징입니다. ifconfig는 기존 명령이지만 최근 배포판에서는 net-tools 패키지를 별도 설치해야 합니다."
  optionExplanations={['정답입니다. ip addr (또는 ip a)는 인터페이스 정보를 출력하는 iproute2 패키지 명령입니다.', 'ifconfig도 유사한 정보를 출력하지만 표시 형식이 다릅니다. 최신 배포판에서는 기본 포함 안 됩니다.', 'route는 라우팅 테이블을 출력하는 명령입니다.', 'ss는 소켓 통계를 출력하는 명령입니다(netstat의 현대적 대안).']}
/>

<Quiz
  questionNumber={35}
  question="현재 시스템의 라우팅 테이블을 확인하는 명령으로 알맞은 것은?"
  options={['① ip route', '② ip addr', '③ ip link', '④ ip neigh']}
  correctAnswer={1}
  explanation="ip route (또는 ip r) 명령으로 라우팅 테이블을 확인합니다. 기존에는 route -n 명령을 사용했습니다. ip addr는 인터페이스 주소, ip link는 링크 계층 정보, ip neigh는 ARP 테이블(인접 호스트)을 확인합니다."
  optionExplanations={['정답입니다. ip route는 라우팅 테이블을 확인하는 명령입니다.', 'ip addr는 네트워크 인터페이스의 IP 주소 정보를 확인합니다.', 'ip link는 네트워크 인터페이스의 링크 계층 상태를 확인합니다.', 'ip neigh는 ARP/NDP 이웃 테이블(MAC 주소 매핑)을 확인합니다.']}
/>

<Quiz
  questionNumber={36}
  question="특정 도메인의 DNS 정보를 조회할 때 사용하는 명령어 조합으로 알맞은 것은?"
  options={['① dig, nslookup', '② dig, netstat', '③ nslookup, ss', '④ host, traceroute']}
  correctAnswer={1}
  explanation="dig(Domain Information Groper)와 nslookup은 DNS 조회 명령어입니다. dig는 상세한 DNS 응답을 출력하며, nslookup은 대화형/비대화형으로 DNS 조회를 수행합니다. host 명령도 DNS 조회에 사용됩니다. netstat, ss, traceroute는 네트워크 상태 및 경로 추적 명령입니다."
  optionExplanations={['정답입니다. dig와 nslookup 모두 DNS 조회 명령입니다.', 'netstat는 네트워크 연결 상태를 확인하는 명령으로 DNS 조회와 무관합니다.', 'ss는 소켓 통계를 확인하는 명령으로 DNS 조회와 무관합니다.', 'traceroute는 네트워크 경로 추적 명령입니다. host도 DNS 조회에 사용되지만 이 보기의 traceroute는 해당하지 않습니다.']}
/>

<Quiz
  questionNumber={37}
  question="firewall-cmd를 사용하여 현재 시스템에 적용된 기본 존(zone)을 확인하는 명령으로 알맞은 것은?"
  options={['① firewall-cmd --get-zones', '② firewall-cmd --get-default-zone', '③ firewall-cmd --list-all', '④ firewall-cmd --get-active-zones']}
  correctAnswer={2}
  explanation="firewall-cmd --get-default-zone은 현재 시스템의 기본 존(zone)을 출력합니다. --get-zones는 사용 가능한 전체 존 목록, --list-all은 현재 존의 상세 규칙, --get-active-zones는 활성화된 존과 연결된 인터페이스를 출력합니다."
  optionExplanations={['--get-zones는 설정 가능한 모든 존 이름을 나열합니다.', '정답입니다. --get-default-zone은 현재 기본으로 설정된 존 이름을 출력합니다.', '--list-all은 현재 기본 존의 서비스, 포트, 규칙 등 전체 설정을 출력합니다.', '--get-active-zones는 현재 활성화된 존과 각 존에 연결된 인터페이스를 출력합니다.']}
/>

<Quiz
  questionNumber={38}
  question="SELinux의 현재 동작 모드를 확인하는 명령으로 알맞은 것은?"
  options={['① setenforce', '② getenforce', '③ sestatus', '④ seinfo']}
  correctAnswer={2}
  explanation="getenforce 명령은 SELinux의 현재 동작 모드를 출력합니다. 출력값은 Enforcing(강제 적용), Permissive(허용 모드), Disabled(비활성) 중 하나입니다. setenforce는 모드를 변경(0=Permissive, 1=Enforcing), sestatus(또는 sestatus)는 더 상세한 SELinux 상태를 출력합니다."
  optionExplanations={['setenforce 0 또는 setenforce 1로 SELinux 동작 모드를 변경합니다.', '정답입니다. getenforce는 현재 SELinux 모드(Enforcing/Permissive/Disabled)를 출력합니다.', 'sestatus는 SELinux의 전반적인 상태를 더 자세히 출력합니다. 단순 모드 확인에는 getenforce가 더 간단합니다.', 'seinfo는 SELinux 정책 정보를 조회하는 도구입니다.']}
/>

<Quiz
  questionNumber={39}
  question="SSH 서버 설정 파일(/etc/ssh/sshd_config)에서 root 계정의 직접 SSH 로그인을 금지하기 위해 수정할 항목과 값으로 알맞은 것은?"
  options={['① PermitRootLogin no', '② AllowRootLogin no', '③ RootLogin disable', '④ DenyRoot yes']}
  correctAnswer={1}
  explanation="/etc/ssh/sshd_config에서 PermitRootLogin 항목을 no로 설정하면 root 계정으로의 직접 SSH 로그인이 금지됩니다. 기본값은 prohibit-password(패스워드 인증만 금지)이며, no는 키 기반 인증도 포함하여 완전히 금지합니다."
  optionExplanations={['정답입니다. PermitRootLogin no로 설정하면 root의 SSH 직접 로그인이 완전히 차단됩니다.', 'AllowRootLogin은 sshd_config에 없는 항목입니다.', 'RootLogin은 sshd_config에 없는 항목입니다.', 'DenyRoot는 sshd_config에 없는 항목입니다.']}
/>

<Quiz
  questionNumber={40}
  question="아파치 웹 서버 환경 설정 파일에서 웹 문서가 저장되는 위치를 설정하는 항목으로 알맞은 것은?"
  options={['① ServerRoot', '② DocumentRoot', '③ DirectoryRoot', '④ IndexRoot']}
  correctAnswer={2}
  explanation="DocumentRoot는 아파치가 클라이언트에게 제공하는 웹 문서의 루트 디렉토리를 지정합니다. 예: DocumentRoot /var/www/html. ServerRoot는 아파치 서버 프로그램의 루트 디렉토리(설정, 로그 등이 위치)를 지정합니다."
  optionExplanations={['ServerRoot는 아파치 서버 자체의 설치 경로(설정·로그 파일 위치)를 지정합니다.', '정답입니다. DocumentRoot는 웹 문서(HTML, 이미지 등)가 저장된 루트 디렉토리를 지정합니다.', 'DirectoryRoot는 아파치 설정에 없는 항목입니다.', 'IndexRoot는 아파치 설정에 없는 항목입니다.']}
/>

<Quiz
  questionNumber={41}
  question="DNS Zone 파일에서 도메인의 IP 주소를 역방향(IP→도메인)으로 매핑할 때 사용하는 레코드 타입으로 알맞은 것은?"
  options={['① A', '② MX', '③ PTR', '④ CNAME']}
  correctAnswer={3}
  explanation="PTR(Pointer) 레코드는 IP 주소를 도메인 이름으로 역방향 조회(Reverse DNS Lookup)할 때 사용합니다. 역방향 존 파일에서만 사용됩니다. A 레코드는 도메인→IPv4, MX는 메일 서버, CNAME은 도메인 별칭을 설정합니다."
  optionExplanations={['A 레코드는 도메인 이름을 IPv4 주소로 정방향 매핑합니다.', 'MX 레코드는 도메인의 메일 서버를 지정합니다.', '정답입니다. PTR 레코드는 IP 주소를 도메인 이름으로 역방향 조회할 때 사용합니다.', 'CNAME 레코드는 도메인의 별칭(Canonical Name)을 설정합니다.']}
/>

<Quiz
  questionNumber={42}
  question="NFS 서버 /etc/exports 파일 설정에서 root를 포함한 모든 사용자의 권한을 nfsnobody로 매핑하는 옵션으로 알맞은 것은?"
  options={['① root_squash', '② no_root_squash', '③ all_squash', '④ no_all_squash']}
  correctAnswer={3}
  explanation="all_squash는 root를 포함하여 접속하는 모든 사용자의 권한을 nfsnobody(또는 nobody)로 매핑합니다. root_squash는 root만 nfsnobody로 매핑(기본값), no_root_squash는 root 권한을 그대로 허용, no_all_squash는 모든 사용자 권한을 유지합니다."
  optionExplanations={['root_squash는 root 사용자만 nfsnobody로 매핑하는 기본 옵션입니다.', 'no_root_squash는 NFS 클라이언트의 root를 서버의 root로 허용합니다(보안 위험).', '정답입니다. all_squash는 모든 사용자(root 포함)를 nfsnobody로 매핑합니다.', 'no_all_squash는 모든 사용자 권한을 원래대로 유지합니다.']}
/>

<Quiz
  questionNumber={43}
  question="Samba 설정 파일(smb.conf)에서 공유 디렉토리의 실제 경로를 지정하는 항목으로 알맞은 것은?"
  options={['① data = /usr/local/share', '② path = /usr/local/share', '③ share = /usr/local/share', '④ root = /usr/local/share']}
  correctAnswer={2}
  explanation="smb.conf에서 path 항목은 Samba로 공유할 디렉토리의 실제 경로를 지정합니다. 예시: path = /home/share. workgroup은 작업 그룹명, comment는 공유 설명, valid users는 접근 허용 사용자를 지정합니다."
  optionExplanations={['data는 smb.conf의 유효한 항목이 아닙니다.', '정답입니다. path 항목으로 Samba 공유 디렉토리의 경로를 지정합니다.', 'share는 smb.conf의 유효한 항목이 아닙니다.', 'root는 smb.conf의 유효한 항목이 아닙니다.']}
/>

<Quiz
  questionNumber={44}
  question="iptables를 이용해서 외부에서 들어오는 TCP 80번 포트(HTTP) 접속을 허용하는 규칙으로 알맞은 것은?"
  options={['① iptables -A INPUT -p tcp --dport 80 -j ACCEPT', '② iptables -A OUTPUT -p tcp --dport 80 -j ACCEPT', '③ iptables -A INPUT -p udp --dport 80 -j ACCEPT', '④ iptables -D INPUT -p tcp --dport 80 -j ACCEPT']}
  correctAnswer={1}
  explanation="외부에서 들어오는 트래픽은 INPUT 체인에서 처리합니다. -p tcp로 TCP 프로토콜을 지정하고 --dport 80으로 목적지 포트 80을 지정, -j ACCEPT로 허용합니다. -A는 체인에 규칙 추가, -D는 규칙 삭제입니다."
  optionExplanations={['정답입니다. INPUT 체인에 TCP 목적지 포트 80 허용 규칙을 추가합니다.', 'OUTPUT은 서버에서 나가는 트래픽 체인입니다. 외부 접속 허용은 INPUT 체인입니다.', 'HTTP는 TCP 프로토콜을 사용합니다. UDP 포트 80 규칙은 일반적으로 사용하지 않습니다.', '-D는 규칙을 추가(-A)가 아닌 삭제하는 옵션입니다.']}
/>

<Quiz
  questionNumber={45}
  question="일반 텍스트 형식으로 저장되어 편집기로 내용을 확인할 수 있는 로그 파일로 알맞은 것은?"
  options={['① /var/log/btmp', '② /var/log/secure', '③ /var/log/wtmp', '④ /var/log/lastlog']}
  correctAnswer={2}
  explanation="/var/log/secure는 인증, sudo, SSH 로그인 시도 등의 보안 관련 로그를 텍스트 형식으로 저장합니다. /var/log/btmp(실패한 로그인), /var/log/wtmp(로그인/로그아웃 기록), /var/log/lastlog(최근 로그인 정보)는 바이너리 형식으로 last, lastb, lastlog 명령으로 확인합니다."
  optionExplanations={['/var/log/btmp는 바이너리 형식으로 lastb 명령으로 확인합니다.', '정답입니다. /var/log/secure는 텍스트 형식으로 cat이나 편집기로 직접 확인할 수 있습니다.', '/var/log/wtmp는 바이너리 형식으로 last 명령으로 확인합니다.', '/var/log/lastlog는 바이너리 형식으로 lastlog 명령으로 확인합니다.']}
/>

<Quiz
  questionNumber={46}
  question="tar 명령을 이용해서 증분 백업을 수행할 때 사용하는 옵션으로 알맞은 것은?"
  options={['① -C', '② -g', '③ -L', '④ -N']}
  correctAnswer={2}
  explanation="tar -g(--listed-incremental)는 스냅샷 파일을 이용한 증분 백업을 수행합니다. 사용 예: tar -g snapshot.file -cvfp home1.tar /home. -C는 디렉토리 변경, -L는 테이프 용량 제한, -N는 특정 날짜 이후 수정된 파일만 백업합니다."
  optionExplanations={['-C는 tar 실행 전 지정한 디렉토리로 이동하는 옵션입니다.', '정답입니다. -g(--listed-incremental)는 스냅샷 파일 기반의 증분 백업 옵션입니다.', '-L는 테이프 백업 시 볼륨 크기를 제한하는 옵션입니다.', '-N는 지정한 날짜 이후 수정된 파일만 포함하는 옵션입니다.']}
/>

<Quiz
  questionNumber={47}
  question="rsync 명령으로 /home 디렉토리를 원격 서버 backup.example.com의 /backup/home 디렉토리로 동기화하되 원본에 없는 파일은 대상에서 삭제하는 명령으로 알맞은 것은?"
  options={['① rsync -avz /home/ backup.example.com:/backup/home/', '② rsync -avz --delete /home/ backup.example.com:/backup/home/', '③ rsync -avz --delete backup.example.com:/backup/home/ /home/', '④ rsync -av /home/ backup.example.com:/backup/home/ --remove-source-files']}
  correctAnswer={2}
  explanation="rsync -avz는 아카이브 모드(-a: 소유권, 권한, 심볼릭링크 등 보존), 자세한 출력(-v), gzip 압축(-z)을 사용합니다. --delete는 원본에 없는 파일을 대상에서 삭제하여 완전한 미러링을 수행합니다."
  optionExplanations={['--delete 없이는 원본에서 삭제된 파일이 대상에 그대로 남습니다.', '정답입니다. -avz --delete 옵션으로 아카이브 동기화와 원본 없는 파일 삭제를 함께 수행합니다.', '원본과 대상이 반대로 되어 원격에서 로컬로 복사됩니다.', '--remove-source-files는 전송 후 원본 파일을 삭제하는 옵션으로 미러링과 다릅니다.']}
/>

<Quiz
  questionNumber={48}
  question="실시간으로 시스템의 CPU, 메모리, 프로세스 사용 현황을 모니터링하는 명령으로 알맞은 것은?"
  options={['① vmstat', '② iostat', '③ top', '④ sar']}
  correctAnswer={3}
  explanation="top은 실시간으로 CPU 사용률, 메모리, 스왑, 프로세스 목록을 갱신하며 대화형으로 표시합니다. vmstat는 가상 메모리, 블록 I/O, CPU 통계를 출력하고, iostat는 디스크 I/O 통계, sar는 시스템 활동 리포트를 제공합니다."
  optionExplanations={['vmstat는 가상 메모리 통계, 블록 I/O, CPU 활동을 주기적으로 출력합니다.', 'iostat는 디스크와 CPU I/O 통계를 출력합니다.', '정답입니다. top은 프로세스별 CPU/메모리 사용률을 실시간으로 대화형 화면에 표시합니다.', 'sar는 시스템 활동 데이터를 수집하고 과거 데이터 리포트를 생성합니다.']}
/>

<Quiz
  questionNumber={49}
  question="서버에서 netstat 명령을 실행했을 때 State 항목에 SYN_RECV 상태가 다수 나타났다. 이때 의심할 수 있는 공격으로 알맞은 것은?"
  options={['① UDP Flooding', '② SYN Flooding', '③ Ping of Death', '④ Smurf Attack']}
  correctAnswer={2}
  explanation="SYN Flooding 공격은 TCP 3-way handshake의 SYN 패킷만 대량으로 보내고 ACK를 보내지 않아 서버가 SYN_RECV 상태의 연결을 대량으로 유지하게 만드는 DoS 공격입니다. 서버의 연결 큐가 가득 차 정상적인 연결이 불가능해집니다."
  optionExplanations={['UDP Flooding은 UDP 패킷을 대량으로 전송하여 대역폭을 소모하는 공격입니다.', '정답입니다. SYN Flooding은 SYN 패킷만 보내 SYN_RECV 상태를 대량 생성하는 공격입니다.', 'Ping of Death는 최대 크기를 초과하는 ICMP 패킷으로 시스템을 다운시키는 공격입니다.', 'Smurf Attack은 피해자 IP로 변조된 ICMP 브로드캐스트를 보내 증폭 공격을 유발합니다.']}
/>

<Quiz
  questionNumber={50}
  question="로그인한 사용자, TTY, 접속 원격지, 로그인 시간, 유휴 시간(IDLE), JCPU, PCPU 및 현재 실행 중인 명령까지 함께 출력하는 명령으로 알맞은 것은?"
  options={['① w', '② who', '③ users', '④ lslogins']}
  correctAnswer={1}
  explanation="w 명령은 현재 로그인한 사용자 목록과 함께 각 사용자의 TTY, 접속 원격지, 로그인 시간, 유휴 시간(IDLE), 현재 실행 중인 프로세스(WHAT)까지 출력합니다. who는 기본 로그인 정보만 출력하고, users는 로그인 사용자명만 출력합니다."
  optionExplanations={['정답입니다. w는 로그인 사용자의 세부 정보(TTY, 원격지, 시간, JCPU, PCPU, 명령)를 모두 출력합니다.', 'who는 사용자명, TTY, 로그인 시간, 원격 호스트만 출력하며 유휴 시간과 실행 명령은 포함되지 않습니다.', 'users는 현재 로그인한 사용자명 목록만 간략히 출력합니다.', 'lslogins는 사용자 계정 전반의 로그인 통계 정보를 출력합니다.']}
/>

---

## 참고 자료

- [리눅스마스터 자격증 - 한국정보통신진흥협회(KAIT)](https://www.ihd.or.kr/introducesubject1.do)

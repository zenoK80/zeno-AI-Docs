---
title: '27. 리눅스마스터 1급 1차 예상기출문제 A (리눅스 실무·시스템 관리)'
description: '실제 기출과 같은 개념·같은 난이도로 재구성한 리눅스 실무 및 시스템 관리 영역 20문항. 정답 근거와 오답 이유, 출제자가 노린 함정, 변형 문제 대처법까지 해설한다'
---

<Callout type="warning">
  **이 문서의 문제는 실제 기출문제의 복제가 아닙니다.** 공개된 기출의 출제 개념과 난이도를 분석해 **같은 개념을 같은 수준으로 다시 출제한 재구성 문항**입니다. 원 기출 중 조건이 모호하거나 정답 근거가 약했던 문항은 조건을 명확히 다듬어 새로 만들었습니다. 따라서 답을 외우는 것이 아니라 **해설에 적힌 판단 근거를 익히는 것**이 이 문서의 목적입니다.
</Callout>

<Callout type="info">
  **이번 문서의 목표:** 리눅스 실무(배포판·라이선스·커널·부팅·파일시스템·프로세스)와 시스템 관리(사용자·권한·서비스·스케줄링·로그·백업·패키지) 영역에서 자주 나오는 판단 기준을 문제로 확인하고, 틀린 이유를 개념 단위로 되돌릴 수 있게 된다.
</Callout>

## 이 문제집을 푸는 법

한 문제를 풀고 바로 답을 확인하지 말고, **각 보기가 왜 아닌지를 먼저 말로 설명해 보세요.** 1급 1차는 네 개 보기가 모두 실존하는 명령·파일·개념으로 채워지기 때문에, 정답만 아는 것으로는 변형 문제에서 무너집니다.

해설은 네 겹으로 구성했습니다.

<Steps>
1. **정답 근거** — 왜 그것이 답인지
2. **오답 각각의 정체** — 나머지 보기는 실제로 무엇을 하는 것인지
3. **출제 의도와 함정** — 이 문제가 무엇을 착각시키려 했는지
4. **변형 대처** — 같은 개념을 다른 각도에서 물어볼 때의 판단 기준
</Steps>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="다음 설명에 해당하는 오픈소스 라이선스로 알맞은 것은? — 이 라이선스가 적용된 소스 코드를 수정해 배포할 경우, 수정된 부분을 포함한 전체 소스 코드를 동일한 라이선스로 공개해야 한다. 리눅스 커널이 채택하고 있다."
  options={['MIT License', 'Apache License 2.0', 'GPL', 'BSD License']}
  correctAnswer={3}
  explanation="정답 근거: 파생 저작물까지 같은 라이선스로 공개하도록 강제하는 성질을 카피레프트라고 하며, 이를 대표하는 것이 GPL 이다. 리눅스 커널은 GPL v2 를 채택하고 있다. 오답 정리: MIT 와 BSD 는 저작권 표시만 유지하면 소스 공개 없이 상용 폐쇄 제품에 넣을 수 있는 허용적 라이선스이고, Apache 2.0 은 여기에 특허 조항이 추가된 형태로 역시 소스 공개 의무가 없다. 함정: 문제는 대표 프로그램 이름이나 배포 주체를 힌트로 주는 방식으로도 나온다. Hadoop 과 Tomcat 이 나오면 Apache, 리눅스 커널이나 GCC 가 나오면 GPL 로 연결하면 된다. 변형 대처: 라이선스 문제는 소스 공개 의무가 있는가와 정적 링크한 프로그램까지 전염되는가 두 축으로 정리하면 대부분 풀린다. LGPL 은 라이브러리를 동적으로 링크한 프로그램에는 공개 의무를 전파하지 않는다는 점에서 GPL 과 갈린다."
  optionExplanations={['저작권 고지만 유지하면 되는 대표적 허용적 라이선스로 소스 공개 의무가 없다.', '허용적 라이선스에 특허권 관련 조항이 보강된 형태이며 수정본 공개를 요구하지 않는다.', '정답. 파생물까지 동일 라이선스로 공개하도록 요구하는 카피레프트 라이선스다.', '허용적 라이선스이며 초기 판본의 광고 조항 문제로 수정판이 나왔을 뿐 공개 의무는 없다.']}
/>

<Quiz
  questionNumber={2}
  question="다음 설명에 해당하는 리눅스 배포판 계열로 알맞은 것은? — 패키지 설치와 갱신에 dpkg 와 apt 계열 도구를 사용하며, 우분투와 칼리 리눅스가 이 배포판을 기반으로 파생되었다."
  options={['Debian', 'Rocky Linux', 'openSUSE', 'Fedora']}
  correctAnswer={1}
  explanation="정답 근거: dpkg 와 apt 를 사용하고 우분투 및 칼리 리눅스의 모체가 되는 배포판은 데비안이다. 오답 정리: Rocky Linux 는 CentOS 를 대체하기 위해 등장한 RHEL 호환 배포판으로 rpm 과 dnf 를 쓰고, openSUSE 는 rpm 기반이지만 zypper 라는 별도 도구를 쓰며, Fedora 는 레드햇의 선행 개발 배포판으로 역시 rpm 과 dnf 계열이다. 함정: 우분투와 데비안 중 무엇이 모체인지 묻는 방식으로 자주 뒤집는다. 칼리 리눅스는 데비안 기반이지 우분투 기반이 아니다. 변형 대처: 배포판 문제는 패키지 도구 이름 하나로 계열을 먼저 가른 뒤, 그 계열 안에서 상용과 커뮤니티, 상류와 하류를 구분하면 된다. rpm 이 나오면 레드햇 또는 수세 계열, deb 가 나오면 데비안 계열이다."
  optionExplanations={['정답. dpkg 와 apt 를 사용하며 우분투와 칼리 리눅스의 기반이다.', 'RHEL 호환을 목표로 하는 rpm 기반 배포판이다.', 'rpm 기반이지만 zypper 를 사용하는 수세 계열 배포판이다.', '레드햇 계열의 선행 개발 배포판으로 dnf 를 사용한다.']}
/>

<Quiz
  questionNumber={3}
  question="10GB 하드디스크 7개가 장착된 시스템에서 1개를 스페어 디스크로 남기고 나머지로 RAID-6 을 구성하였다. 실제로 사용 가능한 용량으로 알맞은 것은?"
  options={['30GB', '40GB', '50GB', '60GB']}
  correctAnswer={2}
  explanation="정답 근거: 스페어 1개를 제외하면 RAID 구성에 참여하는 디스크는 6개다. RAID-6 은 패리티를 두 벌 두므로 사용 가능 용량 계산식이 디스크 개수에서 2를 뺀 값에 디스크 용량을 곱한 것이 된다. 6 빼기 2 는 4, 여기에 10GB 를 곱해 40GB 다. 오답 정리: 30GB 는 디스크 3개분으로 스페어를 두 개로 잘못 세었을 때 나오고, 50GB 는 RAID-5 로 착각해 1개만 뺐을 때 나오며, 60GB 는 패리티를 전혀 고려하지 않은 값이다. 함정: 스페어 디스크는 평소에 데이터를 담지 않고 대기만 하므로 용량 계산에서 먼저 빼야 한다. 이 한 단계를 건너뛰게 만드는 것이 출제 의도다. 변형 대처: RAID 용량 문제는 스페어 제외 후 레벨별 공식을 적용하는 두 단계로 고정한다. RAID-0 은 전체 합, RAID-1 은 절반, RAID-5 는 하나 빼기, RAID-6 은 둘 빼기, RAID-10 은 절반이다."
  optionExplanations={['참여 디스크를 5개로 잘못 계산했을 때 나오는 값이다.', '정답. 참여 디스크 6개에서 패리티 2개분을 뺀 4개분 용량이다.', 'RAID-5 로 계산해 패리티를 하나만 뺐을 때의 값이다.', '패리티 용량을 전혀 빼지 않은 전체 합계다.']}
/>

<Quiz
  questionNumber={4}
  question="root 패스워드를 분실한 CentOS 7 시스템에서 GRUB2 편집 화면의 커널 인자를 수정해 셸로 직접 부팅하려고 한다. 기존의 ro 를 대체할 인자로 알맞은 것은?"
  options={['rw rescue', 'rw init=/bin/sh', 'rw systemd=/bin/bash', 'ro single']}
  correctAnswer={2}
  explanation="정답 근거: 커널은 초기화를 마친 뒤 init= 인자가 가리키는 프로그램을 사용자 공간의 첫 프로세스로 실행한다. 이를 셸로 지정하면 systemd 도 로그인 절차도 거치지 않고 곧바로 root 셸이 열린다. 이때 패스워드 파일을 수정해야 하므로 루트 파일시스템을 읽기 쓰기로 붙이도록 ro 를 rw 로 함께 바꾼다. 오답 정리: rescue 는 단독 인자로는 의미가 없고 systemd.unit=rescue.target 형태여야 하며 그마저도 root 패스워드를 요구한다. systemd= 라는 커널 인자는 존재하지 않는다. ro single 은 읽기 전용이라 패스워드 변경 자체가 불가능하고 systemd 환경에서는 인증을 요구할 수 있다. 함정: rw 로 바꾸는 부분을 빠뜨리게 만드는 것이 첫 번째 함정이고, 존재하지 않는 인자 이름을 그럴듯하게 섞어 놓는 것이 두 번째다. 변형 대처: 부팅 인자 문제는 첫째 PID 1 을 무엇으로 바꾸는가, 둘째 루트를 읽기 쓰기로 붙였는가 두 가지를 확인하면 된다. SELinux 가 켜진 시스템에서는 작업 후 최상위에 autorelabel 파일을 만들어야 재부팅 후 로그인이 된다는 조건이 붙는 변형도 자주 나온다."
  optionExplanations={['rescue 는 단독 커널 인자로 쓰이지 않으며 target 지정 형식이어야 한다.', '정답. 첫 프로세스를 셸로 대체하고 루트를 읽기 쓰기로 마운트한다.', 'systemd= 라는 커널 인자는 존재하지 않는다.', '읽기 전용 상태라 패스워드 파일을 수정할 수 없다.']}
/>

<Quiz
  questionNumber={5}
  question="echo 명령으로 현재 디렉터리 경로를 문장에 섞어 출력하려고 한다. 명령 치환(command substitution)이 실제로 일어나는 표기로 알맞은 것은?"
  options={['큰따옴표로 감싼 문장 안에 달러 기호와 소괄호로 pwd 를 감싸 넣는다', '따옴표 없이 문장 안에 pwd 라는 글자를 그대로 적는다', '큰따옴표로 감싼 문장 안에 pwd 라는 글자를 그대로 적는다', '작은따옴표로 감싼 문장 안에 달러 기호와 소괄호로 pwd 를 감싸 넣는다']}
  correctAnswer={1}
  explanation="정답 근거: 달러 기호와 소괄호로 감싼 형태 또는 역따옴표로 감싼 형태는 명령 치환이며, 안의 명령을 실행한 결과 문자열로 바뀐다. 큰따옴표 안에서는 변수 확장과 명령 치환이 그대로 동작하므로 현재 디렉터리 경로가 출력된다. 오답 정리: 두 번째와 세 번째는 pwd 를 그냥 글자로 취급해 문자 그대로 출력되고, 네 번째는 작은따옴표가 모든 확장을 막으므로 치환 구문이 문자 그대로 노출된다. 함정: 큰따옴표와 작은따옴표의 차이를 묻는 것이 핵심이다. 큰따옴표는 일부 확장을 허용하고 작은따옴표는 전부 막는다. 변형 대처: 따옴표 문제는 세 가지로 정리한다. 작은따옴표는 모든 확장 차단, 큰따옴표는 변수와 명령 치환과 역슬래시 허용, 역따옴표는 명령 치환이다. 명령 치환을 중첩할 때는 역따옴표보다 달러 소괄호 형식이 안전하다는 점도 함께 나온다."
  optionExplanations={['정답. 큰따옴표 안의 명령 치환이 실행되어 경로가 출력된다.', '따옴표가 없어도 치환 구문이 아니므로 pwd 라는 글자가 그대로 출력된다.', '큰따옴표 안이지만 치환 구문이 아니어서 글자 그대로 출력된다.', '작은따옴표가 모든 확장을 막아 치환 구문이 그대로 보인다.']}
/>

<Quiz
  questionNumber={6}
  question="다음 중 시그널의 번호가 큰 것부터 순서대로 올바르게 나열한 것은?"
  options={['SIGKILL, SIGSTOP, SIGQUIT, SIGINT', 'SIGSTOP, SIGINT, SIGQUIT, SIGKILL', 'SIGINT, SIGQUIT, SIGKILL, SIGSTOP', 'SIGSTOP, SIGKILL, SIGQUIT, SIGINT']}
  correctAnswer={4}
  explanation="정답 근거: 주요 시그널 번호는 SIGINT 가 2, SIGQUIT 가 3, SIGKILL 이 9, SIGSTOP 이 19 다. 큰 것부터 나열하면 SIGSTOP, SIGKILL, SIGQUIT, SIGINT 순이 된다. 오답 정리: 첫 번째는 SIGKILL 을 가장 크다고 본 것으로 강제 종료의 강력함과 번호 크기를 혼동한 배열이고, 두 번째와 세 번째는 중간 순서가 어긋난다. 함정: SIGKILL 이 가장 강력한 시그널이라는 인식 때문에 번호도 가장 클 것이라 착각하게 만드는 것이 정확한 출제 의도다. 시그널의 강제성과 번호 크기는 아무 관련이 없다. 변형 대처: 자주 나오는 번호는 1 SIGHUP, 2 SIGINT, 3 SIGQUIT, 9 SIGKILL, 15 SIGTERM, 18 SIGCONT, 19 SIGSTOP, 20 SIGTSTP 다. 특히 kill 명령의 기본 시그널이 15 번 SIGTERM 이라는 점과, 9 번과 19 번은 프로세스가 무시하거나 가로챌 수 없다는 점이 함께 출제된다."
  optionExplanations={['SIGKILL 이 9 번이라 19 번인 SIGSTOP 보다 작으므로 순서가 어긋난다.', 'SIGINT 와 SIGQUIT 가 SIGKILL 보다 앞에 와 번호 순서가 맞지 않는다.', '작은 번호부터 나열한 배열이라 조건과 반대다.', '정답. 19, 9, 3, 2 순으로 올바르게 내림차순 정렬되어 있다.']}
/>

<Quiz
  questionNumber={7}
  question="/etc/passwd 파일에서 콜론으로 구분된 필드 중 사용자의 로그인 셸이 기록되는 위치로 알맞은 것은?"
  options={['6번째 필드', '7번째 필드', '5번째 필드', '4번째 필드']}
  correctAnswer={2}
  explanation="정답 근거: passwd 파일의 필드는 사용자명, 패스워드 자리표시자, UID, GID, 주석(GECOS), 홈 디렉터리, 로그인 셸 순서다. 따라서 로그인 셸은 마지막인 7번째다. 오답 정리: 6번째는 홈 디렉터리, 5번째는 주석 필드, 4번째는 기본 그룹의 GID 다. 함정: 홈 디렉터리와 로그인 셸의 위치를 서로 바꿔 묻는 것이 이 유형의 전형적인 변형이다. 두 필드가 나란히 붙어 있어 헷갈리기 쉽다. 변형 대처: 필드 순서를 외울 때는 계정을 만들 때 결정되는 순서로 기억하면 좋다. 이름 다음 비밀번호 자리, 그다음 숫자 두 개가 이어지고, 사람이 읽는 설명, 마지막에 위치와 실행 환경이다. 함께 출제되는 shadow 파일은 1번째가 사용자명, 2번째가 암호화된 패스워드, 3번째부터 날짜 관련 필드이며, gshadow 파일의 3번째 필드가 그룹 관리자라는 점도 세트로 나온다."
  optionExplanations={['6번째 필드는 홈 디렉터리 경로다.', '정답. 마지막 7번째 필드가 로그인 셸이다.', '5번째 필드는 사용자 설명을 담는 주석 필드다.', '4번째 필드는 기본 그룹의 GID 다.']}
/>

<Quiz
  questionNumber={8}
  question="휴직 중인 사용자 ihduser 의 계정을 삭제하지 않고 로그인만 일시적으로 차단하려고 한다. 사용할 명령으로 알맞은 것은?"
  options={['userdel ihduser', 'passwd -d ihduser', 'usermod -L ihduser', 'usermod -l ihduser']}
  correctAnswer={3}
  explanation="정답 근거: usermod 의 대문자 L 옵션은 shadow 파일의 암호화된 패스워드 앞에 느낌표를 붙여 어떤 입력과도 일치하지 않게 만든다. 계정과 데이터는 그대로 두고 로그인만 막는 정확한 수단이며 해제는 대문자 U 로 한다. 오답 정리: userdel 은 계정 자체를 삭제하므로 조건과 정반대이고, passwd 의 소문자 d 는 패스워드를 지워 오히려 빈 패스워드로 로그인 가능한 상태를 만들 수 있어 매우 위험하다. usermod 의 소문자 l 은 로그인 이름을 변경하는 옵션이다. 함정: 대소문자 하나로 의미가 완전히 갈리는 점이 핵심이다. 잠금은 대문자 L, 이름 변경은 소문자 l 이다. 변형 대처: 같은 목적을 passwd 명령으로 물으면 소문자 l 이 잠금, 소문자 u 가 해제로 대소문자 규칙이 반대라는 점을 반드시 기억해야 한다. 완전 차단을 원하면 로그인 셸을 nologin 으로 바꾸는 방법도 함께 출제된다."
  optionExplanations={['계정과 관련 정보를 삭제하므로 일시 정지라는 조건에 맞지 않는다.', '패스워드를 제거하는 옵션으로 오히려 보안상 위험한 상태를 만든다.', '정답. 패스워드를 잠가 로그인만 차단한다.', '소문자 l 은 로그인 이름을 변경하는 옵션이다.']}
/>

<Quiz
  questionNumber={9}
  question="점검 스크립트를 매월 1일과 15일 오전 3시 30분에 실행하도록 crontab 에 등록하려고 한다. 알맞은 설정은?"
  options={['30 3 1,15 * * /etc/check.sh', '3 30 1,15 * * /etc/check.sh', '30 3 * * 1,15 /etc/check.sh', '30 3 1-15 * * /etc/check.sh']}
  correctAnswer={1}
  explanation="정답 근거: crontab 필드는 분, 시, 일, 월, 요일 순이다. 30분 3시이므로 앞의 두 필드는 30 과 3 이고, 1일과 15일이라는 특정 날짜 목록은 세 번째 일 필드에 쉼표로 나열한다. 월과 요일은 제한이 없으므로 별표다. 오답 정리: 두 번째는 분과 시가 뒤바뀌어 30시 3분이라는 불가능한 값이 되고, 세 번째는 1,15 를 요일 필드에 넣었는데 요일은 0에서 7 범위라 15 가 유효하지 않다. 네 번째는 하이픈이 범위를 뜻하므로 1일부터 15일까지 매일 실행된다. 함정: 쉼표는 목록, 하이픈은 범위, 슬래시는 간격이라는 기호 의미를 뒤섞는 것이 핵심 함정이다. 변형 대처: 필드 순서를 분시일월요일로 외우고, 요일은 0 과 7 이 모두 일요일이라는 점, 일 필드와 요일 필드를 동시에 지정하면 둘 중 하나만 맞아도 실행된다는 OR 동작을 함께 기억하면 대부분의 변형에 대응된다."
  optionExplanations={['정답. 30분 3시에 매월 1일과 15일 실행된다.', '분과 시 필드가 뒤바뀌어 유효하지 않은 값이 된다.', '요일 필드에 15 를 넣어 범위를 벗어난다.', '하이픈은 범위를 뜻하므로 1일부터 15일까지 매일 실행된다.']}
/>

<Quiz
  questionNumber={10}
  question="이미 실행 중인 PID 2481 프로세스의 우선순위를 낮추려고 한다. 알맞은 명령은?"
  options={['nice -n 5 2481', 'renice 5 -p 2481', 'renice -5 -p 2481', 'nice --5 2481']}
  correctAnswer={2}
  explanation="정답 근거: 이미 실행 중인 프로세스의 NI 값을 바꾸는 명령은 renice 이고, 우선순위를 낮추려면 NI 값을 양수 방향으로 올려야 한다. 따라서 5 라는 양수 값을 지정하고 대상은 p 옵션으로 PID 를 준다. 오답 정리: nice 는 아직 실행하지 않은 명령을 새로 시작할 때 우선순위를 지정하는 명령이라 PID 를 인자로 받지 않는다. 세 번째는 음수 5 를 지정한 것으로 우선순위를 오히려 높이며 이는 root 권한이 필요하다. 네 번째는 nice 의 잘못된 문법이다. 함정: nice 와 renice 의 역할 구분, 그리고 값이 커질수록 우선순위가 낮아진다는 방향성이 동시에 걸려 있다. 변형 대처: NI 값의 범위는 마이너스 20 부터 19 까지이며 마이너스 20 이 가장 높은 우선순위다. 사용자가 직접 조절할 수 있는 값은 PRI 가 아니라 NI 이고, 일반 사용자는 자기 프로세스의 NI 값을 올릴 수만 있고 다시 낮출 수 없다는 제약도 자주 출제된다."
  optionExplanations={['nice 는 새로 실행할 명령의 우선순위를 지정하며 PID 를 대상으로 하지 않는다.', '정답. 실행 중 프로세스의 NI 값을 양수로 올려 우선순위를 낮춘다.', '음수 값은 우선순위를 높이는 방향이라 조건과 반대다.', 'nice 명령의 문법에 맞지 않으며 실행 중 프로세스에도 쓸 수 없다.']}
/>

<Quiz
  questionNumber={11}
  question="httpd 패키지에 포함된 파일 중 환경 설정 파일 목록만 확인하려고 한다. 알맞은 rpm 명령 옵션은?"
  options={['rpm -qa httpd', 'rpm -ql httpd', 'rpm -qf httpd', 'rpm -qc httpd']}
  correctAnswer={4}
  explanation="정답 근거: rpm 의 q 는 질의이고 여기에 붙는 c 는 설정 파일(configuration files)만 추려 보여 준다. 오답 정리: a 는 설치된 전체 패키지 목록, l 은 해당 패키지가 설치한 파일 전부, f 는 지정한 파일이 어느 패키지에 속하는지 역으로 찾는 옵션이다. 함정: l 과 c 를 혼동시키는 것이 주된 의도다. l 은 전부 나열하고 c 는 설정 파일만 추리므로 빠르고 간편하게 설정 파일을 찾는다는 조건이 c 를 가리킨다. 변형 대처: rpm 질의 옵션은 q 뒤에 붙는 글자로 정리한다. i 는 정보, l 은 파일 목록, c 는 설정 파일, d 는 문서 파일, R 은 의존성, f 는 파일에서 패키지 역추적, p 는 설치되지 않은 패키지 파일 대상이다. 설치하지 않은 rpm 파일의 내용을 보려면 p 를 함께 붙여야 한다는 조건이 자주 추가된다."
  optionExplanations={['설치된 모든 패키지를 나열하는 옵션이다.', '패키지가 설치한 파일 전부를 나열하므로 설정 파일만 추리지 못한다.', '특정 파일이 속한 패키지를 역으로 찾는 옵션이다.', '정답. 설정 파일 목록만 출력한다.']}
/>

<Quiz
  questionNumber={12}
  question="기존에 만들어 둔 data.tar 아카이브에 새로 생성된 로그 파일들을 추가로 묶으려고 한다. tar 명령의 옵션으로 알맞은 것은?"
  options={['tar cvf data.tar *.log', 'tar xvf data.tar *.log', 'tar rvf data.tar *.log', 'tar tvf data.tar *.log']}
  correctAnswer={3}
  explanation="정답 근거: tar 의 r 옵션은 기존 아카이브 끝에 파일을 덧붙이는 append 동작이다. 오답 정리: c 는 새 아카이브를 만드는 옵션이라 기존 내용을 덮어써 버리고, x 는 추출, t 는 내용 목록 확인이다. 함정: 조건에 이미 생성되어 있던이라는 표현을 넣어 두고 c 를 고르게 만드는 것이 노림수다. c 를 쓰면 기존 백업이 사라지는 사고가 실제로 발생한다. 변형 대처: tar 는 동작 옵션 하나와 보조 옵션 여럿의 조합으로 이해한다. 동작은 c 생성, x 추출, t 목록, r 추가, u 갱신이고 보조는 v 진행 표시, f 파일 지정, z 와 j 와 J 압축 방식, p 권한 보존, g 증분 스냅샷이다. 압축된 아카이브에는 r 로 추가할 수 없다는 제약이 조건으로 붙는 변형도 있다."
  optionExplanations={['새 아카이브를 생성하므로 기존 내용이 사라진다.', '아카이브에서 파일을 꺼내는 추출 옵션이다.', '정답. 기존 아카이브에 파일을 덧붙인다.', '아카이브에 든 파일 목록만 확인하는 옵션이다.']}
/>

<Quiz
  questionNumber={13}
  question="커널 모듈을 적재할 때 해당 모듈이 의존하는 다른 모듈까지 자동으로 함께 올려 주는 명령으로 알맞은 것은?"
  options={['insmod', 'modprobe', 'depmod', 'lsmod']}
  correctAnswer={2}
  explanation="정답 근거: modprobe 는 modules.dep 에 기록된 의존성 정보를 참고해 필요한 모듈을 순서대로 함께 적재한다. 제거할 때도 r 옵션으로 의존 모듈까지 정리한다. 오답 정리: insmod 는 지정한 모듈 파일 하나만 그대로 올리므로 의존 모듈이 없으면 실패한다. depmod 는 모듈 간 의존성 정보를 새로 계산해 modules.dep 파일을 갱신하는 명령이고, lsmod 는 현재 적재된 모듈 목록을 보여 준다. 함정: 적재라는 단어만 보고 insmod 를 고르게 만드는 것이 의도다. 의존성 자동 처리라는 조건이 붙으면 언제나 modprobe 다. 변형 대처: 네 명령의 역할을 한 줄로 고정한다. insmod 는 단순 적재, rmmod 는 단순 제거, modprobe 는 의존성까지 처리하는 적재와 제거, depmod 는 의존성 정보 갱신, lsmod 는 조회이며 그 실체는 proc 아래 modules 파일이다. 모듈 정보 확인은 modinfo 가 담당한다."
  optionExplanations={['모듈 하나만 그대로 적재하며 의존성을 처리하지 않는다.', '정답. 의존 모듈까지 자동으로 함께 적재한다.', '의존성 정보 파일을 갱신하는 명령이다.', '적재된 모듈 목록을 조회하는 명령이다.']}
/>

<Quiz
  questionNumber={14}
  question="fdisk 로 파티션을 만든 뒤 LVM 의 물리 볼륨으로 사용하기 위해 지정해야 할 파티션 타입 코드로 알맞은 것은?"
  options={['8e', 'fd', '83', '82']}
  correctAnswer={1}
  explanation="정답 근거: 파티션 타입 코드 8e 가 Linux LVM 이다. 오답 정리: fd 는 소프트웨어 RAID 자동 감지를 뜻하는 Linux raid autodetect, 83 은 일반 리눅스 파일시스템, 82 는 스왑 영역이다. 함정: 8e 와 fd 를 서로 바꿔 묻는 문제가 반복 출제된다. LVM 과 RAID 를 한 문제에서 함께 언급해 혼동을 유도한다. 변형 대처: 네 코드를 짝으로 외운다. 82 스왑, 83 일반, 8e LVM, fd RAID 다. 타입 코드는 커널과 도구에 주는 힌트일 뿐 강제력은 없으므로 83 인 파티션도 물리적으로는 LVM 에 넣을 수 있다는 점이 서술형 판단 문제로 나오기도 한다. GPT 를 사용하는 환경에서는 gdisk 나 parted 를 쓰며 코드 체계가 다르다는 조건이 붙는 변형도 있다."
  optionExplanations={['정답. Linux LVM 을 뜻하는 코드다.', '소프트웨어 RAID 자동 감지를 뜻하는 코드다.', '일반적인 리눅스 파일시스템 파티션 코드다.', '스왑 영역을 뜻하는 코드다.']}
/>

<Quiz
  questionNumber={15}
  question="다음 중 소프트웨어 RAID 장치의 현재 동작 상태와 레벨 정보를 실시간으로 확인할 수 있는 파일로 알맞은 것은?"
  options={['/etc/mdadm.conf', '/proc/partitions', '/proc/mdstat', '/proc/raid']}
  correctAnswer={3}
  explanation="정답 근거: proc 파일시스템 아래 mdstat 파일에는 현재 활성화된 md 장치의 레벨, 참여 디스크, 동기화 진행 상황이 실시간으로 반영된다. 오답 정리: mdadm.conf 는 부팅 시 배열을 조립하기 위한 설정 파일이지 현재 상태가 아니고, partitions 는 커널이 인식한 블록 장치와 파티션 목록만 보여 주며 RAID 레벨 정보는 없다. proc 아래 raid 라는 파일은 존재하지 않는다. 함정: 설정 파일과 상태 파일을 구분하지 못하게 만드는 것이 의도다. 설정은 etc 아래, 실시간 상태는 proc 아래라는 원칙으로 가른다. 변형 대처: 같은 원리로 커널 파라미터의 실시간 값은 proc 아래 sys 경로에 있고 영구 설정은 etc 아래 sysctl 파일에 있다. 명령으로 확인하는 방법을 묻는 변형에서는 mdadm 에 detail 옵션을 붙이는 형태가 정답이 된다."
  optionExplanations={['부팅 시 배열 조립에 쓰이는 설정 파일이며 실시간 상태는 담지 않는다.', '커널이 인식한 블록 장치 목록으로 RAID 레벨 정보가 없다.', '정답. RAID 장치의 상태와 레벨을 실시간으로 보여 준다.', '이런 이름의 파일은 존재하지 않는다.']}
/>

<Quiz
  questionNumber={16}
  question="다음 로그 우선순위 중 가장 심각한 수준에 해당하며, 지정할 경우 기록되는 메시지의 양이 가장 적은 것은?"
  options={['warning', 'err', 'crit', 'emerg']}
  correctAnswer={4}
  explanation="정답 근거: rsyslog 우선순위는 심각한 쪽부터 emerg, alert, crit, err, warning, notice, info, debug 순이며 emerg 가 가장 심각하다. 특정 우선순위를 지정하면 그 이상만 기록되므로 가장 심각한 값을 지정할수록 걸러지는 메시지가 많아 기록량이 가장 적다. 오답 정리: warning 은 중간 수준이라 상당량이 기록되고, err 와 crit 도 emerg 보다 아래이므로 더 많은 메시지를 포함한다. 함정: 가장 많은 기록이 남는 낮은 수준을 묻는 방식으로 뒤집어 출제하는 경우가 더 흔하다. 이때는 보기 중 가장 아래 단계를 골라야 한다. 심각도가 높을수록 기록량이 적다는 반비례 관계가 핵심이다. 변형 대처: 순서를 외울 때 emerg 와 panic 이 같은 뜻이고 err 와 error, warning 과 warn 이 같은 뜻이라는 별칭까지 함께 정리해야 한다. 설정 표기에서 점 뒤에 등호가 없으면 이상 전부, 등호가 있으면 해당 등급만, none 은 제외라는 규칙도 세트로 출제된다."
  optionExplanations={['중간 수준이라 더 아래 단계까지 포함해 기록량이 많다.', 'warning 보다는 심각하지만 최상위는 아니다.', 'err 보다 심각하지만 emerg 와 alert 아래 단계다.', '정답. 가장 심각한 단계이며 기록되는 메시지가 가장 적다.']}
/>

<Quiz
  questionNumber={17}
  question="명령행이나 셸 스크립트에서 시스템 로그 데몬으로 임의의 메시지를 보내고자 할 때 사용하는 명령으로 알맞은 것은?"
  options={['logger', 'logrotate', 'dmesg', 'lastlog']}
  correctAnswer={1}
  explanation="정답 근거: logger 는 표준 syslog 인터페이스를 통해 메시지를 전달하는 명령으로, 태그나 우선순위를 지정하지 않으면 기본 설정에 따라 messages 파일 등에 기록된다. 오답 정리: logrotate 는 로그 파일이 무한정 커지지 않도록 주기적으로 회전하고 압축하는 도구이며, dmesg 는 커널 링 버퍼의 메시지를 읽는 명령이고, lastlog 는 각 사용자의 마지막 로그인 시각을 보여 주는 명령이다. 함정: 로그와 관련된 네 명령을 나란히 놓아 역할을 뒤섞는 것이 의도다. 쓰기는 logger, 읽기는 dmesg 와 journalctl, 관리는 logrotate 로 축을 나누면 헷갈리지 않는다. 변형 대처: 스크립트에서 로그를 남기는 실무 관례를 묻는 변형이 자주 나오며, 이때 태그를 지정하는 t 옵션과 우선순위를 지정하는 p 옵션이 함께 제시된다. 백업 스크립트를 cron 에 등록하고 결과를 logger 로 남기는 형태의 통합 문제로도 확장된다."
  optionExplanations={['정답. 명령행에서 로그 시스템에 메시지를 전송한다.', '로그 파일을 주기적으로 회전하고 압축하는 관리 도구다.', '커널 링 버퍼의 메시지를 읽는 명령이다.', '사용자별 최종 로그인 기록을 조회하는 명령이다.']}
/>

<Quiz
  questionNumber={18}
  question="파일의 세 가지 타임스탬프 중 touch 명령의 옵션으로 직접 지정해 변경할 수 없는 것은?"
  options={['Access Time', 'Modify Time', '세 가지 모두 변경 가능하다', 'Change Time']}
  correctAnswer={4}
  explanation="정답 근거: touch 는 a 옵션으로 접근 시각을, m 옵션으로 수정 시각을 원하는 값으로 지정할 수 있다. 그러나 ctime 은 아이노드의 메타데이터가 바뀔 때 커널이 자동으로 현재 시각으로 갱신하는 값이라 사용자가 임의로 지정할 수 없다. 오히려 touch 로 다른 시각을 바꾸는 행위 자체가 메타데이터 변경이므로 ctime 은 현재 시각으로 올라간다. 오답 정리: 접근 시각과 수정 시각은 옵션으로 지정 가능하고, 네 번째 보기는 ctime 이 예외라는 사실을 부정하므로 틀렸다. 함정: Change Time 을 파일 내용이 바뀐 시각으로 오해하게 만드는 것이 핵심이다. 내용 변경은 Modify Time 이고 Change Time 은 소유자나 권한 같은 아이노드 정보가 바뀐 시각이다. 변형 대처: 세 시각의 정의를 정확히 구분해 둔다. atime 은 읽힌 시각, mtime 은 내용이 바뀐 시각, ctime 은 아이노드 정보가 바뀐 시각이다. chmod 를 실행하면 ctime 만 바뀌고 mtime 은 그대로라는 점이 판단 문제로 자주 나오며, ls 의 정렬 옵션과 stat 명령이 함께 제시되기도 한다."
  optionExplanations={['a 옵션으로 원하는 값으로 지정할 수 있다.', 'm 옵션으로 원하는 값으로 지정할 수 있다.', '세 가지 중 ctime 은 예외이므로 옳지 않다.', '정답. 아이노드 변경 시각은 커널이 자동 갱신하며 임의 지정이 불가능하다.']}
/>

<Quiz
  questionNumber={19}
  question="웹 문서의 실제 위치는 /srv/www 인데 관리자들이 익숙한 /var/www/html 경로로도 접근할 수 있도록 심볼릭 링크를 만들려고 한다. 알맞은 명령은?"
  options={['ln -s /var/www/html /srv/www', 'ln /srv/www /var/www/html', 'ln -s /srv/www /var/www/html', 'ln -s /var/www/html /srv/www/']}
  correctAnswer={3}
  explanation="정답 근거: ln 의 인자 순서는 원본이 먼저이고 새로 만들 링크 이름이 나중이다. 원본이 /srv/www 이고 새 이름이 /var/www/html 이므로 원본을 앞에 쓴다. 오답 정리: 첫 번째와 네 번째는 순서가 뒤바뀌어 /srv/www 라는 이름의 링크를 만들려 하며, 두 번째는 s 옵션이 없어 하드 링크를 시도하는데 디렉터리에는 하드 링크를 만들 수 없어 실패한다. 함정: 인자 순서를 뒤집어 놓는 것이 이 유형의 유일하다시피 한 함정이다. 원본이 앞이라는 규칙 하나로 절반이 걸러진다. 변형 대처: 심볼릭 링크와 하드 링크의 차이를 묻는 방향으로도 확장된다. 하드 링크는 같은 아이노드를 가리키므로 파일시스템을 넘을 수 없고 디렉터리에는 만들 수 없으며 원본을 지워도 데이터가 남는다. 심볼릭 링크는 경로를 담은 별도 파일이라 파일시스템을 넘을 수 있고 원본이 사라지면 깨진 링크가 된다."
  optionExplanations={['원본과 링크 이름의 순서가 뒤바뀌어 있다.', 's 옵션이 없어 하드 링크가 되며 디렉터리에는 만들 수 없다.', '정답. 원본을 앞에, 새로 만들 링크 이름을 뒤에 지정했다.', '순서가 뒤바뀐 데다 뒤에 슬래시까지 붙어 있다.']}
/>

<Quiz
  questionNumber={20}
  question="locate 명령에 대한 설명으로 알맞은 것은?"
  options={['파일 내용에서 특정 패턴을 검색해 해당 줄을 출력한다', '소유자나 권한 같은 다양한 조건을 지정해 검색할 수 있다', '방금 생성한 파일도 별도 조치 없이 즉시 검색된다', '미리 구축한 데이터베이스를 조회하므로 find 보다 검색 속도가 빠르다']}
  correctAnswer={4}
  explanation="정답 근거: locate 는 디스크를 직접 훑지 않고 updatedb 가 주기적으로 만들어 둔 색인 데이터베이스를 조회하므로 응답이 매우 빠르다. 오답 정리: 파일 내용에서 패턴을 찾는 것은 grep 이고, 소유자나 권한 같은 조건 검색은 find 의 영역이다. 세 번째는 정확히 반대로, 데이터베이스가 갱신되기 전에 만들어진 파일은 검색되지 않으므로 updatedb 를 실행해야 한다. 함정: 빠르다는 장점만 알고 데이터베이스 기반이라는 원인을 모르면 세 번째 보기를 고르게 된다. 속도의 대가가 최신성이라는 점이 이 문제의 핵심이다. 변형 대처: 검색 도구 세 가지를 목적별로 정리한다. 이름으로 빠르게 찾으면 locate, 조건을 세밀하게 걸어 실시간으로 찾으면 find, 내용에서 패턴을 찾으면 grep, 실행 파일의 경로만 찾으면 which 나 whereis 다. find 의 exec 옵션으로 찾은 파일에 명령을 실행하는 형태가 함께 출제되는 단골 조합이다."
  optionExplanations={['파일 내용에서 패턴을 찾는 것은 grep 의 역할이다.', '소유자와 권한 등 조건 기반 검색은 find 의 기능이다.', '데이터베이스가 갱신되기 전의 새 파일은 검색되지 않는다.', '정답. 색인 데이터베이스를 조회하므로 실시간 탐색보다 훨씬 빠르다.']}
/>

## 참고 자료

- [한국정보통신진흥협회 자격검정 안내 (ihd.or.kr)](https://www.ihd.or.kr/)
- [rpm(8) 매뉴얼 페이지 (man7.org)](https://man7.org/linux/man-pages/man8/rpm.8.html)
- [crontab(5) 매뉴얼 페이지 (man7.org)](https://man7.org/linux/man-pages/man5/crontab.5.html)
- [signal(7) 매뉴얼 페이지 (man7.org)](https://man7.org/linux/man-pages/man7/signal.7.html)
- [usermod(8) 매뉴얼 페이지 (man7.org)](https://man7.org/linux/man-pages/man8/usermod.8.html)
- [modprobe(8) 매뉴얼 페이지 (man7.org)](https://man7.org/linux/man-pages/man8/modprobe.8.html)
- [GNU tar 공식 매뉴얼 (gnu.org)](https://www.gnu.org/software/tar/manual/tar.html)

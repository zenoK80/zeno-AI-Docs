---
title: '20. 파이썬 기초'
description: '파이썬 문법, 리스트/딕셔너리, 함수, 클래스를 학습합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 파이썬의 핵심 자료형과 문법, 함수·클래스 정의 방법을 이해하고 코드를 해석할 수 있습니다.
</Callout>

## 파이썬 기본 자료형

| 자료형 | 예시 |
|--------|------|
| int | 10, -5, 0 |
| float | 3.14, -0.5 |
| str | 'hello', "world" |
| bool | True, False |
| NoneType | None |

---

## 리스트 (List)

```python
lst = [1, 2, 3, 4, 5]
lst.append(6)         # [1, 2, 3, 4, 5, 6]
lst.pop()             # 6 제거
lst.insert(0, 0)      # 인덱스 0에 0 삽입
lst.remove(3)         # 값 3 첫 번째 제거
lst.sort()            # 오름차순 정렬
lst.reverse()         # 역순

# 슬라이싱
print(lst[1:3])       # 인덱스 1부터 2까지
print(lst[::-1])      # 역순
```

### 리스트 컴프리헨션

```python
squares = [x**2 for x in range(1, 6)]
# [1, 4, 9, 16, 25]

evens = [x for x in range(10) if x % 2 == 0]
# [0, 2, 4, 6, 8]
```

---

## 딕셔너리 (Dictionary)

```python
person = {'name': '홍길동', 'age': 25}
person['email'] = 'hong@test.com'  # 추가
del person['age']                   # 삭제

# 주요 메서드
person.keys()    # dict_keys(['name', 'email'])
person.values()  # dict_values(['홍길동', 'hong@test.com'])
person.items()   # dict_items([...])
person.get('age', 0)  # 없으면 기본값 0 반환
```

---

## 튜플과 세트

```python
# 튜플: 불변(immutable)
t = (1, 2, 3)

# 세트: 중복 없음, 순서 없음
s = {1, 2, 3, 2, 1}  # {1, 2, 3}
s.add(4)
s.remove(2)
```

---

## 함수 (Function)

```python
def greet(name, greeting='안녕하세요'):
    return f'{greeting}, {name}!'

print(greet('홍길동'))        # 안녕하세요, 홍길동!
print(greet('김철수', '반갑습니다'))  # 반갑습니다, 김철수!

# 람다 함수
square = lambda x: x**2
print(square(5))  # 25

# 가변 인수
def sum_all(*args):
    return sum(args)
print(sum_all(1, 2, 3, 4))  # 10
```

---

## 클래스 (Class)

```python
class Animal:
    def __init__(self, name):
        self.name = name
    
    def speak(self):
        return f'{self.name}이(가) 소리를 냅니다.'

class Dog(Animal):
    def speak(self):        # 오버라이딩
        return f'{self.name}이(가) 왈왈!'

dog = Dog('강아지')
print(dog.speak())   # 강아지이(가) 왈왈!
```

---

## 예외 처리

```python
try:
    x = int(input('숫자 입력: '))
    result = 10 / x
except ValueError:
    print('숫자가 아닙니다.')
except ZeroDivisionError:
    print('0으로 나눌 수 없습니다.')
finally:
    print('항상 실행됩니다.')
```

---

## 핵심 정리

- 리스트: 순서 있음, 변경 가능 / 튜플: 불변
- 딕셔너리: 키-값 쌍 / 세트: 중복 없음
- 람다: lambda 매개변수: 표현식
- 상속: class 자식(부모): / `super()`로 부모 접근

---

## 마무리 복습

<Quiz
  questionNumber={1}
  question="파이썬에서 리스트의 끝에 요소를 추가하는 메서드로 알맞은 것은?"
  options={['① insert()', '② add()', '③ append()', '④ push()']}
  correctAnswer={3}
  explanation="파이썬 리스트에서 append()는 리스트의 끝에 요소를 추가합니다. insert(index, value)는 특정 위치에 삽입, add()는 세트(set)에서 사용, push()는 파이썬 리스트에 없는 메서드입니다."
  optionExplanations={['insert()는 특정 인덱스에 요소를 삽입합니다.', 'add()는 세트(set)에서 요소를 추가하는 메서드입니다.', '정답입니다. append()는 리스트 끝에 요소를 추가합니다.', 'push()는 파이썬 리스트에 존재하지 않는 메서드입니다.']}
/>

<Quiz
  questionNumber={2}
  question="파이썬 리스트 컴프리헨션 [x**2 for x in range(1, 4)]의 결과로 알맞은 것은?"
  options={['① [1, 2, 3]', '② [1, 4, 9]', '③ [2, 4, 6]', '④ [1, 4, 9, 16]']}
  correctAnswer={2}
  explanation="range(1, 4)는 1, 2, 3을 생성합니다. 각 x에 대해 x**2를 계산하면 1**2=1, 2**2=4, 3**2=9이므로 결과는 [1, 4, 9]입니다."
  optionExplanations={['[1, 2, 3]은 x**2가 아닌 x 자체의 값입니다.', '정답입니다. 1의 제곱=1, 2의 제곱=4, 3의 제곱=9입니다.', '[2, 4, 6]은 x*2의 결과입니다.', 'range(1, 4)는 1, 2, 3만 포함하므로 4**2=16은 없습니다.']}
/>

<Quiz
  questionNumber={3}
  question="파이썬에서 딕셔너리의 키가 없을 때 기본값을 반환하는 메서드로 알맞은 것은?"
  options={['① dict[key]', '② dict.keys()', '③ dict.get(key, default)', '④ dict.values()']}
  correctAnswer={3}
  explanation="dict.get(key, default)는 키가 존재하면 해당 값을 반환하고, 키가 없으면 default 값을 반환합니다. dict[key]는 키가 없으면 KeyError가 발생합니다."
  optionExplanations={['dict[key]는 키가 없으면 KeyError 예외가 발생합니다.', 'dict.keys()는 모든 키를 반환합니다.', '정답입니다. dict.get(key, default)는 키가 없을 때 기본값을 반환합니다.', 'dict.values()는 모든 값을 반환합니다.']}
/>

<Quiz
  questionNumber={4}
  question="파이썬에서 lambda x: x**2를 이용해 [1, 2, 3, 4]의 각 요소를 제곱하는 코드로 알맞은 것은?"
  options={['① list(filter(lambda x: x**2, [1,2,3,4]))', '② list(map(lambda x: x**2, [1,2,3,4]))', '③ list(reduce(lambda x: x**2, [1,2,3,4]))', '④ list(sort(lambda x: x**2, [1,2,3,4]))']}
  correctAnswer={2}
  explanation="map(함수, 이터러블)은 이터러블의 각 요소에 함수를 적용한 결과를 반환합니다. filter()는 조건에 맞는 요소만 필터링, reduce()는 누적 계산에 사용합니다."
  optionExplanations={['filter()는 조건이 True인 요소만 반환합니다. x**2는 항상 양수라 모두 반환되지만 제곱값이 아닌 원본 값을 반환합니다.', '정답입니다. map()은 각 요소에 함수를 적용합니다.', 'reduce()는 누적 계산에 사용하며 두 인수를 받는 함수가 필요합니다.', 'sort()는 파이썬 내장 함수에 없는 형태입니다.']}
/>

<Quiz
  questionNumber={5}
  question="파이썬 클래스에서 __init__ 메서드의 역할로 알맞은 것은?"
  options={['① 클래스를 삭제할 때 호출되는 소멸자', '② 클래스의 인스턴스가 생성될 때 초기화하는 생성자', '③ 클래스 메서드를 외부에서 호출할 때 사용하는 메서드', '④ 상속 관계에서 부모 메서드를 호출하는 메서드']}
  correctAnswer={2}
  explanation="__init__ 메서드는 파이썬의 생성자(Constructor)로 인스턴스가 생성될 때 자동으로 호출됩니다. 인스턴스 속성을 초기화하는 데 사용합니다. __del__은 소멸자입니다."
  optionExplanations={['소멸자는 __del__ 메서드입니다.', '정답입니다. __init__은 인스턴스 생성 시 자동 호출되는 생성자입니다.', '__init__은 외부 호출 목적이 아닌 인스턴스 초기화를 위한 메서드입니다.', '부모 메서드 호출은 super() 함수를 사용합니다.']}
/>

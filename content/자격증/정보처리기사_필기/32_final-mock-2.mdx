---
title: '32. 최종 모의고사 2회'
description: '5개 과목 전체 범위의 최종 모의고사 2회입니다. (40문항)'
---

<Callout type="info">
  **최종 모의고사 2회** — 5개 과목에서 각 8문항씩 총 40문항입니다. 1회와 중복되지 않는 새로운 유형의 문제들로 구성됩니다.
</Callout>

## 1과목: 소프트웨어 설계

<Quiz
  questionNumber={1}
  question="소프트웨어 개발 방법론 중 위험 분석을 중심으로 계획, 개발, 평가 단계를 반복하는 모델로 알맞은 것은?"
  options={['① 폭포수 모델(Waterfall)', '② 나선형 모델(Spiral)', '③ 프로토타이핑 모델', '④ RAD 모델']}
  correctAnswer={2}
  explanation="나선형 모델(Spiral Model)은 Boehm이 제안한 모델로 계획-위험분석-개발-평가 단계를 반복적으로 수행합니다. 대규모 프로젝트에서 위험을 관리하는 데 적합합니다."
  optionExplanations={['폭포수 모델은 순차적 단계를 따르며 위험 분석 단계가 없습니다.', '정답입니다. 나선형 모델은 위험 분석을 중심으로 반복 진행합니다.', '프로토타이핑 모델은 원형 시스템을 만들어 요구사항을 파악합니다.', 'RAD는 빠른 개발을 위한 모델입니다.']}
/>

<Quiz
  questionNumber={2}
  question="UML 다이어그램 중 객체들 간의 메시지 교환을 시간 순서에 따라 나타내는 행위 다이어그램으로 알맞은 것은?"
  options={['① 클래스 다이어그램', '② 컴포넌트 다이어그램', '③ 시퀀스 다이어그램', '④ 배포 다이어그램']}
  correctAnswer={3}
  explanation="시퀀스 다이어그램은 UML 행위 다이어그램으로 객체 간의 메시지 교환을 수직 시간축에 따라 나타냅니다. 시스템의 동적 행위를 표현하는 데 사용됩니다."
  optionExplanations={['클래스 다이어그램은 정적 구조 다이어그램입니다.', '컴포넌트 다이어그램은 컴포넌트와 인터페이스를 나타냅니다.', '정답입니다. 시퀀스 다이어그램은 시간 순서에 따른 메시지 흐름을 나타냅니다.', '배포 다이어그램은 하드웨어와 소프트웨어 배치를 나타냅니다.']}
/>

<Quiz
  questionNumber={3}
  question="GoF 디자인 패턴 중 객체 생성을 서브클래스에 위임하여 어떤 클래스의 인스턴스를 생성할지 서브클래스가 결정하게 하는 패턴으로 알맞은 것은?"
  options={['① 팩토리 메서드(Factory Method)', '② 추상 팩토리(Abstract Factory)', '③ 빌더(Builder)', '④ 프로토타입(Prototype)']}
  correctAnswer={1}
  explanation="팩토리 메서드(Factory Method) 패턴은 객체 생성을 서브클래스에 위임합니다. 부모 클래스에서 생성 인터페이스를 정의하고 서브클래스에서 구체적인 객체를 생성합니다."
  optionExplanations={['정답입니다. 팩토리 메서드는 객체 생성을 서브클래스에 위임합니다.', '추상 팩토리는 관련된 객체 그룹을 생성하는 인터페이스를 제공합니다.', '빌더는 복잡한 객체 생성을 단계적으로 분리합니다.', '프로토타입은 기존 객체를 복제하여 새 객체를 생성합니다.']}
/>

<Quiz
  questionNumber={4}
  question="요구사항의 유형 중 시스템이 무엇을 해야 하는지 기능적 요구와 구별되는 품질 속성(성능, 보안, 가용성 등)을 설명하는 요구사항으로 알맞은 것은?"
  options={['① 기능적 요구사항(Functional Requirements)', '② 비기능적 요구사항(Non-Functional Requirements)', '③ 도메인 요구사항(Domain Requirements)', '④ 제약 요구사항(Constraint Requirements)']}
  correctAnswer={2}
  explanation="비기능적 요구사항(Non-Functional Requirements)은 시스템이 어떻게 동작해야 하는지에 대한 요구사항입니다. 성능, 보안, 가용성, 확장성, 유지보수성 등이 포함됩니다."
  optionExplanations={['기능적 요구사항은 시스템이 제공해야 하는 기능을 명세합니다.', '정답입니다. 비기능적 요구사항은 품질 속성에 관한 요구사항입니다.', '도메인 요구사항은 특정 도메인의 제약사항을 명세합니다.', '제약 요구사항은 보통 비기능적 요구사항의 하위 범주입니다.']}
/>

<Quiz
  questionNumber={5}
  question="소프트웨어 모듈화에서 한 모듈의 내부 요소들이 동일한 시간에 실행된다는 이유로 묶인 응집도 유형으로 알맞은 것은?"
  options={['① 기능적 응집(Functional Cohesion)', '② 교환적 응집(Communication Cohesion)', '③ 시간적 응집(Temporal Cohesion)', '④ 절차적 응집(Procedural Cohesion)']}
  correctAnswer={3}
  explanation="시간적 응집(Temporal Cohesion)은 특정 시간대에 처리되어야 할 요소들이 모인 응집 유형입니다. 초기화 루틴, 종료 루틴 등이 예입니다. 응집도 강도: 기능 > 순차 > 교환 > 절차 > 시간 > 논리 > 우연"
  optionExplanations={['기능적 응집은 단일 기능 수행으로 가장 강한 응집도입니다.', '교환적 응집은 동일 자료를 사용하는 요소들이 모인 유형입니다.', '정답입니다. 시간적 응집은 동일 시간에 실행되어야 하는 요소들의 묶음입니다.', '절차적 응집은 특정 순서로 실행되어야 하는 요소들의 묶음입니다.']}
/>

<Quiz
  questionNumber={6}
  question="마이크로서비스 아키텍처(MSA)의 특징으로 옳지 않은 것은?"
  options={['① 각 서비스는 독립적으로 배포 가능하다.', '② 서비스 간 통신은 주로 API를 통해 이루어진다.', '③ 하나의 단일 데이터베이스를 모든 서비스가 공유한다.', '④ 서비스별로 적합한 기술 스택을 선택할 수 있다.']}
  correctAnswer={3}
  explanation="마이크로서비스 아키텍처에서는 각 서비스가 자체 데이터베이스를 가집니다(Database per Service 패턴). 단일 데이터베이스 공유는 모놀리식 아키텍처의 특성입니다."
  optionExplanations={['MSA는 각 서비스를 독립적으로 배포합니다. 올바른 설명.', 'MSA에서 서비스 간 통신은 REST API, gRPC 등을 사용합니다. 올바른 설명.', '정답입니다(틀린 설명). MSA는 서비스별 독립 데이터베이스를 사용합니다.', 'MSA는 폴리글랏 퍼시스턴스와 폴리글랏 프로그래밍을 지원합니다. 올바른 설명.']}
/>

<Quiz
  questionNumber={7}
  question="인터페이스 프로토콜 중 XML 기반의 메시지 형식을 사용하며 엄격한 표준을 따르는 웹 서비스 프로토콜로 알맞은 것은?"
  options={['① REST', '② SOAP', '③ GraphQL', '④ gRPC']}
  correctAnswer={2}
  explanation="SOAP(Simple Object Access Protocol)는 XML 기반의 메시지 형식을 사용하는 웹 서비스 프로토콜입니다. 엄격한 WSDL 스키마를 따르며 WS-Security 같은 보안 표준을 지원합니다."
  optionExplanations={['REST는 HTTP 메서드와 JSON/XML을 사용하는 아키텍처 스타일입니다.', '정답입니다. SOAP는 XML 기반의 엄격한 웹 서비스 프로토콜입니다.', 'GraphQL은 클라이언트가 필요한 데이터를 쿼리 형식으로 요청하는 언어입니다.', 'gRPC는 Protocol Buffers를 사용하는 고성능 RPC 프레임워크입니다.']}
/>

<Quiz
  questionNumber={8}
  question="Scrum 방법론에서 제품 백로그를 우선순위에 따라 관리하고 팀의 장애물을 제거하는 역할로 알맞은 것은?"
  options={['① 개발팀(Development Team)', '② 스크럼 마스터(Scrum Master)', '③ 제품 책임자(Product Owner)', '④ 스테이크홀더(Stakeholder)']}
  correctAnswer={3}
  explanation="제품 책임자(Product Owner)는 제품 백로그를 관리하고 우선순위를 설정합니다. 비즈니스 요구사항을 이해하고 팀이 올바른 것을 만들도록 방향을 제시합니다. 스크럼 마스터는 팀의 장애물을 제거하고 Scrum 프로세스를 지원합니다."
  optionExplanations={['개발팀은 스프린트 내에서 실제 개발을 수행합니다.', '스크럼 마스터는 팀의 장애물 제거와 Scrum 프로세스 지원을 합니다.', '정답입니다. 제품 책임자는 제품 백로그를 관리하고 우선순위를 정합니다.', '스테이크홀더는 프로젝트 이해관계자로 Scrum 팀이 아닙니다.']}
/>

## 2과목: 소프트웨어 개발

<Quiz
  questionNumber={9}
  question="소프트웨어 품질 속성 중 소프트웨어 제품이 명시된 성능 수준을 특정 기간 동안 유지하는 능력으로 알맞은 것은?"
  options={['① 기능성(Functionality)', '② 신뢰성(Reliability)', '③ 사용성(Usability)', '④ 이식성(Portability)']}
  correctAnswer={2}
  explanation="신뢰성(Reliability)은 소프트웨어가 지정된 조건과 기간 동안 요구된 기능을 수행하는 능력입니다. MTBF(평균 고장 간격)가 높을수록 신뢰성이 높습니다."
  optionExplanations={['기능성은 요구된 기능을 제공하는 능력입니다.', '정답입니다. 신뢰성은 지정 기간 동안 기능을 유지하는 능력입니다.', '사용성은 이해하고 사용하기 쉬운 정도입니다.', '이식성은 다른 환경으로 전환하는 능력입니다.']}
/>

<Quiz
  questionNumber={10}
  question="형상 관리 도구 Git에서 로컬 저장소의 변경사항을 원격 저장소에 업로드하는 명령어로 알맞은 것은?"
  options={['① git pull', '② git fetch', '③ git push', '④ git merge']}
  correctAnswer={3}
  explanation="git push는 로컬 저장소의 커밋을 원격 저장소(remote repository)에 업로드합니다. git pull은 원격 저장소의 변경사항을 로컬에 가져와 병합합니다."
  optionExplanations={['git pull은 원격 저장소 변경사항을 로컬에 가져와 병합합니다.', 'git fetch는 원격 저장소 변경사항을 가져오지만 병합하지 않습니다.', '정답입니다. git push는 로컬 변경사항을 원격 저장소에 업로드합니다.', 'git merge는 두 브랜치를 병합합니다.']}
/>

<Quiz
  questionNumber={11}
  question="소프트웨어 테스트에서 경계값 분석(Boundary Value Analysis)을 사용하는 테스트 기법 유형으로 알맞은 것은?"
  options={['① 화이트박스 테스트', '② 블랙박스 테스트', '③ 구조적 테스트', '④ 단위 테스트']}
  correctAnswer={2}
  explanation="경계값 분석은 입력 도메인의 경계에서 발생하는 오류를 찾는 블랙박스 테스트 기법입니다. 내부 구조를 모르고 입력/출력만으로 테스트합니다. 동치 분할도 블랙박스 기법입니다."
  optionExplanations={['화이트박스 테스트는 내부 코드 구조를 기반으로 합니다.', '정답입니다. 경계값 분석은 블랙박스 테스트 기법입니다.', '구조적 테스트는 화이트박스 테스트와 동의어입니다.', '단위 테스트는 테스트 레벨이며 기법 유형이 아닙니다.']}
/>

<Quiz
  questionNumber={12}
  question="빅오 표기법에서 입력 크기에 관계없이 일정한 시간이 걸리는 알고리즘의 시간 복잡도로 알맞은 것은?"
  options={['① O(n)', '② O(log n)', '③ O(1)', '④ O(n²)']}
  correctAnswer={3}
  explanation="O(1)은 상수 시간 복잡도로 입력 크기에 관계없이 항상 일정한 시간이 걸립니다. 배열의 인덱스 접근, 해시 테이블 검색 등이 O(1)입니다."
  optionExplanations={['O(n)은 선형 시간으로 입력 크기에 비례합니다.', 'O(log n)은 로그 시간으로 이진 탐색 등이 해당됩니다.', '정답입니다. O(1)은 입력 크기와 무관한 상수 시간입니다.', 'O(n²)은 이중 루프처럼 입력 크기의 제곱에 비례합니다.']}
/>

<Quiz
  questionNumber={13}
  question="소프트웨어 유지보수 유형 중 소프트웨어가 정확하게 실행되도록 오류를 수정하는 유지보수로 알맞은 것은?"
  options={['① 수정 유지보수(Corrective Maintenance)', '② 적응 유지보수(Adaptive Maintenance)', '③ 완전 유지보수(Perfective Maintenance)', '④ 예방 유지보수(Preventive Maintenance)']}
  correctAnswer={1}
  explanation="수정 유지보수(Corrective Maintenance)는 소프트웨어의 버그나 결함을 발견하고 수정하는 유지보수입니다. 가장 일반적인 유지보수 유형입니다."
  optionExplanations={['정답입니다. 수정 유지보수는 오류 발견 및 수정입니다.', '적응 유지보수는 환경 변화(OS 업그레이드 등)에 적응하는 유지보수입니다.', '완전 유지보수는 기능 추가나 성능 개선을 위한 유지보수입니다.', '예방 유지보수는 미래 문제 예방을 위한 유지보수입니다.']}
/>

<Quiz
  questionNumber={14}
  question="코드 리뷰 기법 중 작성자가 다른 개발자에게 코드를 설명하면서 오류를 발견하는 기법으로 알맞은 것은?"
  options={['① 인스펙션(Inspection)', '② 워크스루(Walkthrough)', '③ 페어 프로그래밍(Pair Programming)', '④ 동적 분석']}
  correctAnswer={2}
  explanation="워크스루(Walkthrough)는 코드 작성자가 동료들에게 코드를 설명하면서 오류를 찾는 비공식적 검토 기법입니다. 인스펙션은 공식적인 절차와 역할이 있는 검토입니다."
  optionExplanations={['인스펙션은 공식적인 역할(사회자, 검토자, 기록자 등)과 절차가 있습니다.', '정답입니다. 워크스루는 작성자가 동료에게 설명하는 비공식 검토입니다.', '페어 프로그래밍은 두 명이 한 컴퓨터에서 함께 코딩하는 방식입니다.', '동적 분석은 코드를 실행하면서 분석하는 방법입니다.']}
/>

<Quiz
  questionNumber={15}
  question="소프트웨어 아키텍처 패턴 중 데이터를 순차적으로 처리하는 컴포넌트(필터)들을 파이프로 연결하는 패턴으로 알맞은 것은?"
  options={['① MVC 패턴', '② 파이프-필터 패턴(Pipe-Filter)', '③ 이벤트 기반 패턴', '④ 레이어 패턴']}
  correctAnswer={2}
  explanation="파이프-필터(Pipe-Filter) 패턴은 데이터를 처리하는 컴포넌트(필터)들이 파이프로 연결되어 순차적으로 데이터를 변환합니다. Unix 파이프 명령이 대표적인 예입니다."
  optionExplanations={['MVC는 Model-View-Controller로 UI 아키텍처 패턴입니다.', '정답입니다. 파이프-필터는 컴포넌트(필터)를 파이프로 연결합니다.', '이벤트 기반은 이벤트 발생-처리 중심의 아키텍처입니다.', '레이어는 시스템을 계층으로 나누는 아키텍처입니다.']}
/>

<Quiz
  questionNumber={16}
  question="소프트웨어 형상 관리에서 여러 개발자가 같은 파일을 동시에 수정할 때 발생하는 문제로 알맞은 것은?"
  options={['① 브랜치(Branch)', '② 태그(Tag)', '③ 충돌(Conflict)', '④ 리베이스(Rebase)']}
  correctAnswer={3}
  explanation="충돌(Conflict)은 여러 개발자가 같은 파일의 같은 부분을 동시에 수정하고 병합할 때 발생합니다. Git에서는 충돌 발생 시 개발자가 수동으로 해결해야 합니다."
  optionExplanations={['브랜치는 별도의 개발 흐름을 생성하는 기능입니다.', '태그는 특정 버전에 이름을 붙이는 기능입니다.', '정답입니다. 충돌은 동시 수정 시 발생하는 충돌 상황입니다.', '리베이스는 브랜치의 기반 커밋을 변경하는 작업입니다.']}
/>

## 3과목: 데이터베이스 구축

<Quiz
  questionNumber={17}
  question="관계형 데이터베이스에서 외래키(Foreign Key)의 역할로 알맞은 것은?"
  options={['① 테이블 내 행의 고유성을 보장한다.', '② 테이블 간의 참조 무결성을 보장한다.', '③ 데이터의 기밀성을 보장한다.', '④ 쿼리 성능을 향상시킨다.']}
  correctAnswer={2}
  explanation="외래키(Foreign Key)는 다른 테이블의 기본키를 참조하여 테이블 간의 관계를 정의하고 참조 무결성을 보장합니다. 참조되는 행이 없으면 외래키 값을 삽입하거나 기준 행을 삭제할 수 없습니다."
  optionExplanations={['행의 고유성 보장은 기본키(Primary Key)의 역할입니다.', '정답입니다. 외래키는 참조 무결성을 보장합니다.', '데이터 기밀성은 보안 기능으로 외래키와 관련 없습니다.', '인덱스가 쿼리 성능 향상에 사용됩니다.']}
/>

<Quiz
  questionNumber={18}
  question="SQL에서 서브쿼리가 반환하는 결과 중 하나라도 조건을 만족하면 참이 되는 연산자로 알맞은 것은?"
  options={['① ALL', '② ANY/SOME', '③ EXISTS', '④ IN']}
  correctAnswer={2}
  explanation="ANY(또는 SOME) 연산자는 서브쿼리 결과 중 하나라도 조건을 만족하면 참을 반환합니다. ALL은 모든 값이 조건을 만족해야 합니다. IN은 서브쿼리 결과 집합에 포함되면 참입니다."
  optionExplanations={['ALL은 서브쿼리의 모든 결과가 조건을 만족해야 참입니다.', '정답입니다. ANY/SOME은 하나라도 조건을 만족하면 참입니다.', 'EXISTS는 서브쿼리 결과가 하나 이상 존재하면 참입니다.', 'IN은 값이 서브쿼리 결과 집합 내에 있으면 참입니다.']}
/>

<Quiz
  questionNumber={19}
  question="데이터베이스 동시성 제어에서 Lock의 유형 중 읽기는 허용하지만 쓰기는 금지하는 잠금 유형으로 알맞은 것은?"
  options={['① 공유 잠금(Shared Lock)', '② 배타 잠금(Exclusive Lock)', '③ 의도 잠금(Intent Lock)', '④ 갱신 잠금(Update Lock)']}
  correctAnswer={1}
  explanation="공유 잠금(Shared Lock, S Lock)은 읽기 연산(SELECT)에 사용되며 다른 트랜잭션의 읽기는 허용하지만 쓰기는 금지합니다. 배타 잠금(Exclusive Lock, X Lock)은 읽기와 쓰기 모두 금지합니다."
  optionExplanations={['정답입니다. 공유 잠금은 읽기는 허용하고 쓰기는 금지합니다.', '배타 잠금은 다른 트랜잭션의 읽기와 쓰기 모두 금지합니다.', '의도 잠금은 계층적 잠금 관리에서 사용합니다.', '갱신 잠금은 공유 잠금과 배타 잠금의 중간 단계입니다.']}
/>

<Quiz
  questionNumber={20}
  question="데이터 웨어하우스의 특징으로 옳지 않은 것은?"
  options={['① 주제 지향적(Subject-Oriented)이다.', '② 통합적(Integrated)이다.', '③ 시간 가변적(Time-Variant)이다.', '④ 실시간 트랜잭션 처리에 최적화되어 있다.']}
  correctAnswer={4}
  explanation="데이터 웨어하우스는 OLAP(분석)에 최적화되어 있으며 실시간 트랜잭션 처리(OLTP)에는 적합하지 않습니다. 주제 지향, 통합, 시간 가변, 비휘발성이 데이터 웨어하우스의 4대 특성입니다."
  optionExplanations={['주제 지향적은 데이터 웨어하우스의 특성입니다.', '통합적은 데이터 웨어하우스의 특성입니다.', '시간 가변적은 데이터 웨어하우스의 특성입니다.', '정답입니다(틀린 설명). 데이터 웨어하우스는 분석(OLAP)에 최적화됩니다.']}
/>

<Quiz
  questionNumber={21}
  question="SQL 집합 연산자 중 두 SELECT의 결과를 합치되 중복을 제거하는 연산자로 알맞은 것은?"
  options={['① UNION', '② UNION ALL', '③ INTERSECT', '④ EXCEPT']}
  correctAnswer={1}
  explanation="UNION은 두 SELECT 결과의 합집합을 반환하며 중복 행을 제거합니다. UNION ALL은 중복을 제거하지 않습니다. INTERSECT는 교집합, EXCEPT(MINUS)는 차집합입니다."
  optionExplanations={['정답입니다. UNION은 중복을 제거한 합집합입니다.', 'UNION ALL은 중복을 제거하지 않는 합집합입니다.', 'INTERSECT는 두 결과의 교집합입니다.', 'EXCEPT는 첫 번째 결과에서 두 번째를 뺀 차집합입니다.']}
/>

<Quiz
  questionNumber={22}
  question="관계형 데이터베이스에서 뷰(View)의 특징으로 옳지 않은 것은?"
  options={['① 실제 데이터를 별도로 저장하지 않는다.', '② 복잡한 쿼리를 단순화할 수 있다.', '③ 보안 목적으로 특정 컬럼만 노출할 수 있다.', '④ 뷰를 통한 UPDATE는 항상 허용된다.']}
  correctAnswer={4}
  explanation="뷰(View)를 통한 UPDATE는 제한적입니다. 집계 함수, GROUP BY, DISTINCT, 조인이 포함된 뷰는 일반적으로 수정이 불가능합니다. 단순한 뷰는 수정이 가능하지만 항상 허용되는 것은 아닙니다."
  optionExplanations={['뷰는 가상 테이블로 실제 데이터를 저장하지 않습니다. 올바른 설명.', '뷰는 복잡한 쿼리를 단순화하는 데 유용합니다. 올바른 설명.', '뷰는 특정 컬럼만 노출하여 보안을 강화할 수 있습니다. 올바른 설명.', '정답입니다(틀린 설명). 뷰를 통한 UPDATE는 항상 허용되지 않습니다.']}
/>

<Quiz
  questionNumber={23}
  question="데이터베이스 스키마의 3단계 중 실제 물리적 저장 방법을 정의하는 단계로 알맞은 것은?"
  options={['① 외부 스키마(External Schema)', '② 개념 스키마(Conceptual Schema)', '③ 내부 스키마(Internal Schema)', '④ 논리 스키마(Logical Schema)']}
  correctAnswer={3}
  explanation="내부 스키마(Internal Schema)는 데이터의 물리적 저장 구조와 접근 방법을 정의합니다. 인덱스, 파일 구조, 레코드 크기 등이 포함됩니다. 외부 스키마는 사용자 뷰, 개념 스키마는 전체 논리적 구조입니다."
  optionExplanations={['외부 스키마는 개별 사용자의 뷰(사용자 관점)를 정의합니다.', '개념 스키마는 전체 데이터베이스의 논리적 구조를 정의합니다.', '정답입니다. 내부 스키마는 물리적 저장 방법을 정의합니다.', '논리 스키마는 개념 스키마와 유사한 표현입니다.']}
/>

<Quiz
  questionNumber={24}
  question="Hadoop의 주요 구성 요소 중 분산 파일 시스템으로 알맞은 것은?"
  options={['① MapReduce', '② HDFS(Hadoop Distributed File System)', '③ YARN', '④ Hive']}
  correctAnswer={2}
  explanation="HDFS(Hadoop Distributed File System)는 Hadoop의 분산 파일 시스템으로 대용량 데이터를 여러 노드에 분산 저장합니다. MapReduce는 분산 데이터 처리 프레임워크, YARN은 자원 관리 시스템입니다."
  optionExplanations={['MapReduce는 분산 데이터 처리를 위한 프로그래밍 모델입니다.', '정답입니다. HDFS는 Hadoop의 분산 파일 시스템입니다.', 'YARN은 Hadoop의 자원 관리 및 작업 스케줄링 시스템입니다.', 'Hive는 SQL과 유사한 인터페이스를 제공하는 Hadoop 도구입니다.']}
/>

## 4과목: 프로그래밍 언어 활용

<Quiz
  questionNumber={25}
  question="다음 파이썬 코드의 출력 결과로 알맞은 것은? nums = [1, 2, 3, 4, 5]; print(sum(filter(lambda x: x % 2 == 0, nums)))"
  options={['① 9', '② 6', '③ 15', '④ 10']}
  correctAnswer={2}
  explanation="filter(lambda x: x % 2 == 0, nums)는 리스트에서 짝수만 필터링합니다. [1,2,3,4,5]에서 짝수는 2, 4이고 sum([2, 4]) = 6입니다."
  optionExplanations={['홀수의 합: 1+3+5 = 9입니다.', '정답입니다. 짝수 2와 4의 합은 6입니다.', '전체 합: 1+2+3+4+5 = 15입니다.', '짝수 2와 4, 짝수 관련 수들의 합이 아닙니다.']}
/>

<Quiz
  questionNumber={26}
  question="자바에서 ArrayList 클래스를 사용할 때 필요한 import 패키지로 알맞은 것은?"
  options={['① import java.io.ArrayList;', '② import java.util.ArrayList;', '③ import java.lang.ArrayList;', '④ import java.collection.ArrayList;']}
  correctAnswer={2}
  explanation="ArrayList는 java.util 패키지에 포함되어 있습니다. 따라서 import java.util.ArrayList; 또는 import java.util.*;를 선언해야 합니다."
  optionExplanations={['java.io는 입출력 관련 클래스 패키지입니다.', '정답입니다. ArrayList는 java.util 패키지에 있습니다.', 'java.lang은 기본 클래스(String, Integer 등)를 포함하며 자동 import됩니다.', 'java.collection은 존재하지 않는 패키지입니다.']}
/>

<Quiz
  questionNumber={27}
  question="C 언어에서 구조체(struct)를 사용하는 목적으로 알맞은 것은?"
  options={['① 동적 메모리를 할당하기 위해', '② 여러 타입의 데이터를 하나의 단위로 묶기 위해', '③ 함수 포인터를 선언하기 위해', '④ 배열의 크기를 동적으로 조절하기 위해']}
  correctAnswer={2}
  explanation="구조체(struct)는 서로 다른 타입의 변수들을 하나의 논리적 단위로 묶는 데 사용합니다. 예: 이름(char), 나이(int), 키(float)를 하나의 Person 구조체로 묶을 수 있습니다."
  optionExplanations={['동적 메모리 할당은 malloc(), calloc() 함수로 합니다.', '정답입니다. 구조체는 다양한 타입의 데이터를 하나로 묶습니다.', '함수 포인터는 별도의 포인터 선언으로 사용합니다.', '배열 크기 조절은 realloc() 함수로 합니다.']}
/>

<Quiz
  questionNumber={28}
  question="파이썬에서 예외 발생 시 처리하고 정상 완료 시 실행하는 블록으로 알맞은 것은?"
  options={['① try-except-finally', '② try-except-else', '③ try-catch-finally', '④ try-except-then']}
  correctAnswer={2}
  explanation="파이썬의 try-except-else에서 else 블록은 try 블록이 예외 없이 정상적으로 완료됐을 때 실행됩니다. 자바의 try-catch-finally와 달리 파이썬은 catch 대신 except를 사용합니다."
  optionExplanations={['try-except-finally에서 finally는 예외 여부와 관계없이 실행됩니다.', '정답입니다. else는 예외 없이 정상 완료 시 실행됩니다.', '파이썬은 catch 대신 except를 사용합니다.', 'then은 파이썬 예외 처리 키워드가 아닙니다.']}
/>

<Quiz
  questionNumber={29}
  question="클라우드 네이티브 애플리케이션의 12-Factor App 방법론에 포함되지 않는 원칙으로 알맞은 것은?"
  options={['① 코드베이스(Codebase): 버전 관리로 추적되는 하나의 코드베이스', '② 의존성(Dependencies): 모든 의존성을 명시적으로 선언', '③ 설정(Config): 코드에 설정을 하드코딩', '④ 로그(Logs): 로그를 이벤트 스트림으로 처리']}
  correctAnswer={3}
  explanation="12-Factor App의 설정 원칙은 설정을 환경 변수에 저장하고 코드와 분리해야 합니다. 설정을 코드에 하드코딩하는 것은 안티패턴입니다."
  optionExplanations={['코드베이스: 하나의 코드베이스로 여러 배포. 올바른 원칙.', '의존성: 모든 의존성을 명시적 선언. 올바른 원칙.', '정답입니다(틀린 원칙). 설정은 환경 변수에 저장, 코드에 하드코딩 금지.', '로그: 이벤트 스트림으로 처리. 올바른 원칙.']}
/>

<Quiz
  questionNumber={30}
  question="웹 서비스 보안에서 사용자 인증에 널리 사용되는 토큰 기반 인증 표준으로 알맞은 것은?"
  options={['① 쿠키(Cookie)', '② 세션(Session)', '③ JWT(JSON Web Token)', '④ CSRF 토큰']}
  correctAnswer={3}
  explanation="JWT(JSON Web Token)는 JSON 형식으로 사용자 정보를 담아 서명한 토큰입니다. 헤더.페이로드.서명의 3부분으로 구성되며 서버에 상태를 저장하지 않는 Stateless 인증에 사용됩니다."
  optionExplanations={['쿠키는 브라우저에 저장되는 소규모 데이터입니다.', '세션은 서버에 상태를 저장하는 방식입니다.', '정답입니다. JWT는 토큰 기반 인증 표준으로 널리 사용됩니다.', 'CSRF 토큰은 CSRF 공격 방어를 위한 토큰입니다.']}
/>

<Quiz
  questionNumber={31}
  question="셸 스크립트에서 배열의 모든 요소 개수를 출력하는 방법으로 알맞은 것은?"
  options={['① echo $arr', '② echo ${arr[*]}', '③ echo ${#arr[@]}', '④ echo arr.length']}
  correctAnswer={3}
  explanation="${#arr[@]}는 배열 arr의 요소 개수를 반환합니다. ${arr[@]}는 모든 요소를 반환하고 ${#arr[@]}는 요소의 수를 반환합니다."
  optionExplanations={['$arr는 배열이 아닌 일반 변수처럼 첫 번째 요소나 변수명으로 처리됩니다.', '${arr[*]}는 모든 요소를 출력하지만 개수는 아닙니다.', '정답입니다. ${#arr[@]}는 배열 요소 개수를 반환합니다.', 'arr.length는 파이썬/자바의 문법이며 셸에서 사용하지 않습니다.']}
/>

<Quiz
  questionNumber={32}
  question="Java 람다 표현식이 가능하기 위한 조건으로 알맞은 것은?"
  options={['① 클래스에 static 메서드가 있어야 한다.', '② 함수형 인터페이스(추상 메서드가 1개)를 구현해야 한다.', '③ 인터페이스는 최소 3개의 추상 메서드가 있어야 한다.', '④ 클래스는 final로 선언되어야 한다.']}
  correctAnswer={2}
  explanation="Java 람다 표현식은 함수형 인터페이스(Functional Interface)를 구현할 때 사용합니다. 함수형 인터페이스는 추상 메서드가 정확히 1개인 인터페이스입니다. @FunctionalInterface 애너테이션으로 명시합니다."
  optionExplanations={['static 메서드 유무는 람다 사용과 관련 없습니다.', '정답입니다. 람다는 추상 메서드가 1개인 함수형 인터페이스를 구현합니다.', '추상 메서드가 2개 이상이면 람다를 사용할 수 없습니다.', '클래스의 final 선언은 람다와 관련 없습니다.']}
/>

## 5과목: 정보시스템 구축관리

<Quiz
  questionNumber={33}
  question="암호화 방식 중 공개키와 개인키를 사용하여 전자서명과 키 교환에 주로 사용되는 알고리즘으로 알맞은 것은?"
  options={['① AES', '② DES', '③ RSA', '④ SHA-256']}
  correctAnswer={3}
  explanation="RSA(Rivest-Shamir-Adleman)는 비대칭 키 알고리즘으로 공개키와 개인키를 사용합니다. 전자서명과 키 교환에 사용됩니다. AES, DES는 대칭키 알고리즘이고 SHA-256은 해시 함수입니다."
  optionExplanations={['AES는 대칭키 암호화 알고리즘으로 동일한 키를 사용합니다.', 'DES는 오래된 대칭키 알고리즘으로 현재는 안전하지 않습니다.', '정답입니다. RSA는 공개키/개인키 쌍을 사용하는 비대칭키 알고리즘입니다.', 'SHA-256은 해시 함수로 암호화/복호화가 아닌 단방향 변환입니다.']}
/>

<Quiz
  questionNumber={34}
  question="소프트웨어 개발 보안 지침 중 SQL 인젝션을 방지하는 방법으로 가장 효과적인 것은?"
  options={['① 입력 데이터를 모두 대문자로 변환', '② 파라미터화된 쿼리(Parameterized Query) 사용', '③ 데이터베이스 포트를 변경', '④ 로그인 시도 횟수 제한']}
  correctAnswer={2}
  explanation="파라미터화된 쿼리(Prepared Statement)는 SQL 구조와 데이터를 분리하여 사용자 입력이 SQL 명령으로 해석되지 않게 합니다. SQL 인젝션을 가장 효과적으로 방지합니다."
  optionExplanations={['입력 대문자 변환은 SQL 인젝션을 막지 못합니다.', '정답입니다. 파라미터화된 쿼리는 SQL 인젝션의 가장 효과적인 방어책입니다.', 'DB 포트 변경은 보안에 도움이 되지만 SQL 인젝션을 막지 못합니다.', '로그인 시도 제한은 무차별 대입 공격 방어책입니다.']}
/>

<Quiz
  questionNumber={35}
  question="ITIL에서 변경 관리(Change Management)의 주요 목적으로 알맞은 것은?"
  options={['① 장애 발생 시 신속한 서비스 복구', '② IT 환경 변경으로 인한 위험 최소화와 변경의 표준화', '③ 사용자 요청 처리', '④ 서비스 수준 협약 관리']}
  correctAnswer={2}
  explanation="ITIL 변경 관리(Change Management)의 주요 목적은 IT 인프라 변경으로 인한 위험을 최소화하고 변경을 표준화된 방법으로 통제하는 것입니다. 긴급 변경, 일반 변경, 표준 변경으로 분류합니다."
  optionExplanations={['장애 복구는 인시던트 관리의 목적입니다.', '정답입니다. 변경 관리는 변경 위험 최소화와 표준화를 목적으로 합니다.', '사용자 요청 처리는 서비스 요청 관리의 역할입니다.', 'SLA 관리는 서비스 수준 관리의 역할입니다.']}
/>

<Quiz
  questionNumber={36}
  question="소프트웨어 개발 계획에서 기능 점수(FP) 방법으로 규모를 산정하는 장점으로 알맞은 것은?"
  options={['① 정확한 LOC 수를 알아야 한다.', '② 개발 언어나 기술에 독립적이다.', '③ 개발 완료 후에만 측정 가능하다.', '④ 하드웨어 스펙에 따라 달라진다.']}
  correctAnswer={2}
  explanation="기능 점수(FP) 방법은 사용자 관점의 기능을 기반으로 측정하므로 구현 언어, 기술, 플랫폼에 독립적입니다. LOC 기반 방법은 언어에 따라 값이 달라집니다."
  optionExplanations={['LOC를 알 필요가 없습니다. 오히려 FP의 장점입니다.', '정답입니다. FP는 개발 언어나 기술에 독립적입니다.', 'FP는 개발 전 요구사항 단계에서도 측정 가능합니다.', 'FP는 하드웨어와 관련 없습니다.']}
/>

<Quiz
  questionNumber={37}
  question="네트워크 보안에서 DMZ(Demilitarized Zone)를 사용하는 주요 목적으로 알맞은 것은?"
  options={['① 내부 네트워크의 성능을 향상시키기 위해', '② 외부에서 접근 가능한 서버를 내부 네트워크와 분리하기 위해', '③ IP 주소를 자동으로 할당하기 위해', '④ 데이터를 암호화하기 위해']}
  correctAnswer={2}
  explanation="DMZ(비무장 지대)는 외부(인터넷)와 내부 네트워크 사이에 위치하는 중간 영역입니다. 웹 서버, 메일 서버 등 외부 접근이 필요한 서버를 DMZ에 배치하여 내부 네트워크를 보호합니다."
  optionExplanations={['DMZ는 성능 향상이 목적이 아닙니다.', '정답입니다. DMZ는 외부 접근 서버를 내부와 분리하여 보호합니다.', 'IP 자동 할당은 DHCP의 기능입니다.', '암호화는 VPN, SSL/TLS의 역할입니다.']}
/>

<Quiz
  questionNumber={38}
  question="프로젝트 관리에서 EVM(Earned Value Management)의 CPI(Cost Performance Index)가 1 미만일 때 의미하는 것으로 알맞은 것은?"
  options={['① 예산보다 적은 비용으로 수행 중이다.', '② 예산을 초과하여 비용을 사용하고 있다.', '③ 일정이 지연되고 있다.', '④ 일정이 예정보다 빠르다.']}
  correctAnswer={2}
  explanation="CPI(Cost Performance Index) = EV(획득가치) / AC(실제비용). CPI < 1이면 실제 비용이 획득 가치보다 크다는 의미로 예산 초과 상태입니다. CPI > 1은 예산 내 수행, SPI > 1은 일정 앞서 진행을 의미합니다."
  optionExplanations={['CPI > 1일 때 예산보다 적은 비용으로 수행 중입니다.', '정답입니다. CPI < 1은 예산 초과를 의미합니다.', '일정 지연은 SPI(Schedule Performance Index) < 1로 확인합니다.', '일정이 앞서는 것은 SPI > 1로 확인합니다.']}
/>

<Quiz
  questionNumber={39}
  question="정보통신망법에서 개인정보 침해사고 발생 시 신고 기관으로 알맞은 것은?"
  options={['① 경찰청', '② 한국인터넷진흥원(KISA)', '③ 국가정보원', '④ 방송통신위원회']}
  correctAnswer={2}
  explanation="정보통신망법에 따라 침해사고 발생 시 한국인터넷진흥원(KISA)에 신고해야 합니다. KISA는 인터넷 보안 사고에 대응하는 기관으로 침해사고대응팀(KrCERT/CC)을 운영합니다."
  optionExplanations={['경찰청은 일반 범죄 수사를 담당합니다.', '정답입니다. 정보통신망법 침해사고는 KISA에 신고합니다.', '국가정보원은 국가 안보 관련 업무를 담당합니다.', '방송통신위원회는 방송 통신 분야 규제 기관입니다.']}
/>

<Quiz
  questionNumber={40}
  question="서비스 지향 아키텍처(SOA)에서 서비스들의 위치와 인터페이스 정보를 등록하고 검색하는 컴포넌트로 알맞은 것은?"
  options={['① ESB(Enterprise Service Bus)', '② 서비스 레지스트리(Service Registry)', '③ SOAP', '④ API 게이트웨이']}
  correctAnswer={2}
  explanation="서비스 레지스트리(Service Registry)는 SOA에서 서비스의 위치, 인터페이스, 메타데이터를 등록하고 서비스 소비자가 필요한 서비스를 검색할 수 있게 합니다. UDDI가 대표적인 서비스 레지스트리 표준입니다."
  optionExplanations={['ESB는 서비스 간 메시지를 중계하는 미들웨어입니다.', '정답입니다. 서비스 레지스트리는 서비스 위치와 인터페이스를 등록·검색합니다.', 'SOAP는 웹 서비스 통신 프로토콜입니다.', 'API 게이트웨이는 마이크로서비스 아키텍처에서 단일 진입점 역할을 합니다.']}
/>

---
title: '12. 2과목 예상 기출문제 Quiz'
description: '소프트웨어 개발 과목의 예상 기출문제 15문항을 풀며 실력을 점검합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 2과목(소프트웨어 개발) 핵심 개념을 문제로 점검하고 취약 부분을 파악합니다.
</Callout>

<Quiz
  questionNumber={1}
  question="큐(Queue) 자료구조에서 먼저 삽입된 데이터가 먼저 삭제되는 방식을 나타내는 용어로 알맞은 것은?"
  options={['① LIFO(Last In First Out)', '② FIFO(First In First Out)', '③ LILO(Last In Last Out)', '④ FILO(First In Last Out)']}
  correctAnswer={2}
  explanation="큐(Queue)는 FIFO(First In First Out, 선입선출) 구조로 먼저 삽입된 데이터가 먼저 삭제됩니다. 줄 서기와 같은 원리로 프린터 스풀링, BFS 탐색 등에 활용됩니다."
  optionExplanations={['LIFO는 스택(Stack)의 동작 방식입니다.', '정답입니다. FIFO는 큐의 선입선출 방식입니다.', 'LILO는 FIFO와 같은 의미의 다른 표현입니다.', 'FILO는 LIFO와 같은 의미로 스택의 방식입니다.']}
/>

<Quiz
  questionNumber={2}
  question="정렬 알고리즘 중 피벗(Pivot)을 기준으로 분할하여 정렬하는 방법으로 평균 시간 복잡도 O(n log n)을 가지는 것으로 알맞은 것은?"
  options={['① 버블 정렬', '② 선택 정렬', '③ 퀵 정렬', '④ 삽입 정렬']}
  correctAnswer={3}
  explanation="퀵 정렬(Quick Sort)은 피벗을 기준으로 작은 값과 큰 값을 분할하는 분할 정복 방식입니다. 평균 O(n log n)의 시간 복잡도를 가지며 실용적으로 가장 빠릅니다. 단, 최악의 경우 O(n²)입니다."
  optionExplanations={['버블 정렬은 인접 요소 비교·교환으로 평균 O(n²)입니다.', '선택 정렬은 최솟값을 선택하여 배치하는 방식으로 O(n²)입니다.', '정답입니다. 퀵 정렬은 피벗 분할 방식으로 평균 O(n log n)입니다.', '삽입 정렬은 정렬된 부분에 요소를 삽입하는 방식으로 O(n²)입니다.']}
/>

<Quiz
  questionNumber={3}
  question="객체지향 프로그래밍에서 부모 클래스의 메서드를 자식 클래스에서 동일한 이름·매개변수로 재정의하는 것으로 알맞은 것은?"
  options={['① 오버로딩(Overloading)', '② 오버라이딩(Overriding)', '③ 캡슐화(Encapsulation)', '④ 추상화(Abstraction)']}
  correctAnswer={2}
  explanation="오버라이딩(Overriding)은 상속 관계에서 부모 클래스의 메서드를 자식 클래스에서 동일한 메서드명과 매개변수로 재정의하는 것입니다. 런타임 다형성을 구현하며 @Override 어노테이션으로 명시합니다."
  optionExplanations={['오버로딩은 같은 클래스 내에서 메서드명이 같고 매개변수가 다른 것입니다.', '정답입니다. 오버라이딩은 상속 관계에서 부모 메서드를 동일 시그니처로 재정의합니다.', '캡슐화는 데이터와 메서드를 묶고 내부를 숨기는 특성입니다.', '추상화는 공통 특성만 추출하는 특성입니다.']}
/>

<Quiz
  questionNumber={4}
  question="화이트박스 테스트 커버리지 기준 중 코드의 모든 조건식이 참(T)과 거짓(F) 결과를 모두 가지도록 테스트하는 기준으로 알맞은 것은?"
  options={['① 구문 커버리지(Statement Coverage)', '② 결정 커버리지(Decision/Branch Coverage)', '③ 경로 커버리지(Path Coverage)', '④ 조건 커버리지(Condition Coverage)']}
  correctAnswer={2}
  explanation="결정 커버리지(Decision Coverage, 분기 커버리지)는 각 조건식(if, while 등)의 결과가 참(T)과 거짓(F) 모두를 가지도록 테스트 케이스를 작성하는 기준입니다."
  optionExplanations={['구문 커버리지는 모든 구문을 최소 1번 실행하는 기준입니다.', '정답입니다. 결정 커버리지는 조건식의 T/F 분기를 모두 실행하는 기준입니다.', '경로 커버리지는 모든 가능한 실행 경로를 테스트하는 기준입니다.', '조건 커버리지는 각 개별 조건의 T/F를 모두 실행하는 기준입니다.']}
/>

<Quiz
  questionNumber={5}
  question="Git에서 현재 작업 중인 변경 사항을 임시로 저장하고 Working Directory를 깨끗하게 만드는 명령으로 알맞은 것은?"
  options={['① git commit', '② git add', '③ git stash', '④ git reset']}
  correctAnswer={3}
  explanation="git stash는 현재 변경 사항을 임시 저장소에 저장하고 Working Directory를 최근 커밋 상태로 되돌립니다. 긴급한 작업으로 브랜치를 변경해야 할 때 유용합니다. git stash pop으로 저장한 내용을 복원합니다."
  optionExplanations={['git commit은 스테이징된 변경 사항을 로컬 저장소에 저장합니다.', 'git add는 파일을 스테이징 영역에 추가합니다.', '정답입니다. git stash는 변경 사항을 임시로 저장합니다.', 'git reset은 특정 커밋 상태로 되돌리는 명령입니다.']}
/>

<Quiz
  questionNumber={6}
  question="GoF 디자인 패턴 중 호환되지 않는 인터페이스를 가진 클래스들이 함께 동작할 수 있도록 변환해주는 구조 패턴으로 알맞은 것은?"
  options={['① 데코레이터(Decorator) 패턴', '② 파사드(Facade) 패턴', '③ 어댑터(Adapter) 패턴', '④ 프록시(Proxy) 패턴']}
  correctAnswer={3}
  explanation="어댑터(Adapter) 패턴은 기존 클래스의 인터페이스를 클라이언트가 원하는 인터페이스로 변환하는 구조 패턴입니다. 마치 여행용 콘센트 어댑터처럼 호환되지 않는 인터페이스를 연결합니다."
  optionExplanations={['데코레이터 패턴은 객체에 동적으로 새로운 기능을 추가합니다.', '파사드 패턴은 복잡한 서브시스템에 단순한 인터페이스를 제공합니다.', '정답입니다. 어댑터 패턴은 호환되지 않는 인터페이스를 변환·연결합니다.', '프록시 패턴은 대상 객체에 대한 접근을 제어하는 대리 객체를 제공합니다.']}
/>

<Quiz
  questionNumber={7}
  question="트리 구조에서 자식이 없는 노드(Terminal Node)를 가리키는 용어로 알맞은 것은?"
  options={['① 루트 노드(Root Node)', '② 단말 노드(Leaf Node)', '③ 형제 노드(Sibling Node)', '④ 조상 노드(Ancestor Node)']}
  correctAnswer={2}
  explanation="단말 노드(Leaf Node 또는 Terminal Node)는 자식 노드가 없는 트리의 끝 노드입니다. 루트 노드는 최상위 노드, 형제 노드는 같은 부모를 가진 노드, 조상 노드는 특정 노드의 상위 노드들입니다."
  optionExplanations={['루트 노드는 트리의 최상위 노드입니다.', '정답입니다. 단말(리프) 노드는 자식 노드가 없는 트리의 끝 노드입니다.', '형제 노드는 같은 부모 노드를 가진 노드들입니다.', '조상 노드는 특정 노드에서 루트까지의 경로상 모든 상위 노드들입니다.']}
/>

<Quiz
  questionNumber={8}
  question="소프트웨어 테스트에서 상향식(Bottom-up) 통합 테스트에 필요한 임시 모듈로 알맞은 것은?"
  options={['① 스텁(Stub)', '② 드라이버(Driver)', '③ 목(Mock)', '④ 스파이(Spy)']}
  correctAnswer={2}
  explanation="상향식 통합 테스트는 하위 모듈부터 통합합니다. 아직 개발되지 않은 상위 모듈을 대신하여 테스트 대상 모듈을 호출하는 임시 모듈인 드라이버(Driver)가 필요합니다."
  optionExplanations={['스텁은 하향식 통합 테스트에서 하위 모듈을 대신하는 임시 모듈입니다.', '정답입니다. 드라이버는 상향식 통합 테스트에서 상위 모듈을 대신하는 임시 모듈입니다.', '목(Mock)은 예상 동작을 미리 정의한 테스트 대체 객체입니다.', '스파이(Spy)는 실제 객체의 일부 동작을 감시하는 테스트 더블입니다.']}
/>

<Quiz
  questionNumber={9}
  question="SOLID 원칙 중 자식 클래스는 부모 클래스를 대체할 수 있어야 한다는 원칙으로 알맞은 것은?"
  options={['① 단일 책임 원칙(SRP)', '② 개방-폐쇄 원칙(OCP)', '③ 리스코프 치환 원칙(LSP)', '④ 인터페이스 분리 원칙(ISP)']}
  correctAnswer={3}
  explanation="리스코프 치환 원칙(LSP, Liskov Substitution Principle)은 자식 클래스는 언제나 부모 클래스를 대체할 수 있어야 한다는 원칙입니다. 바바라 리스코프(Barbara Liskov)가 제안했습니다."
  optionExplanations={['SRP는 클래스가 단 하나의 책임만 가져야 한다는 원칙입니다.', 'OCP는 확장에는 열려있고 수정에는 닫혀있어야 한다는 원칙입니다.', '정답입니다. LSP는 자식 클래스가 부모 클래스를 대체할 수 있어야 한다는 원칙입니다.', 'ISP는 클라이언트에 필요한 인터페이스만 제공해야 한다는 원칙입니다.']}
/>

<Quiz
  questionNumber={10}
  question="블랙박스 테스트 기법 중 입력 데이터의 경계 값(최솟값, 최댓값, 경계±1)을 중점적으로 테스트하는 기법으로 알맞은 것은?"
  options={['① 동등 분류(Equivalence Partitioning)', '② 경계값 분석(Boundary Value Analysis)', '③ 결정 테이블(Decision Table)', '④ 상태 전이 테스트']}
  correctAnswer={2}
  explanation="경계값 분석(Boundary Value Analysis)은 오류가 경계 부근에서 많이 발생한다는 경험에 근거하여 경계값(최솟값, 최댓값)과 그 직전·직후 값을 테스트 케이스로 선택하는 기법입니다."
  optionExplanations={['동등 분류는 입력을 동등 클래스로 나누어 대표값으로 테스트합니다.', '정답입니다. 경계값 분석은 경계값과 경계±1을 테스트하는 기법입니다.', '결정 테이블은 조건 조합에 따른 동작을 표로 명세합니다.', '상태 전이 테스트는 상태 변화에 따른 시스템 동작을 검증합니다.']}
/>

<Quiz
  questionNumber={11}
  question="CI/CD 파이프라인에서 코드가 병합될 때마다 자동으로 빌드하고 단위 테스트를 실행하는 단계로 알맞은 것은?"
  options={['① Continuous Deployment', '② Continuous Delivery', '③ Continuous Integration', '④ Continuous Monitoring']}
  correctAnswer={3}
  explanation="CI(Continuous Integration, 지속적 통합)는 개발자가 코드를 공유 저장소에 자주 병합하고 자동으로 빌드·테스트를 실행하여 통합 오류를 조기에 발견하는 관행입니다."
  optionExplanations={['Continuous Deployment는 변경 사항을 자동으로 프로덕션에 배포합니다.', 'Continuous Delivery는 변경 사항을 스테이징 환경까지 자동 배포합니다.', '정답입니다. Continuous Integration은 코드 병합 시 자동 빌드·테스트를 수행합니다.', 'Continuous Monitoring은 배포된 시스템을 지속적으로 모니터링합니다.']}
/>

<Quiz
  questionNumber={12}
  question="해시 탐색(Hash Search)의 평균 시간 복잡도로 알맞은 것은?"
  options={['① O(n)', '② O(log n)', '③ O(n²)', '④ O(1)']}
  correctAnswer={4}
  explanation="해시 탐색은 해시 함수를 통해 키를 해시 테이블의 인덱스로 변환하여 O(1) 평균 시간에 데이터를 검색합니다. 해시 충돌이 없는 경우 상수 시간에 접근 가능합니다."
  optionExplanations={['O(n)은 순차 탐색의 시간 복잡도입니다.', 'O(log n)은 이진 탐색의 시간 복잡도입니다.', 'O(n²)는 버블·선택·삽입 정렬의 시간 복잡도입니다.', '정답입니다. 해시 탐색의 평균 시간 복잡도는 O(1)입니다.']}
/>

<Quiz
  questionNumber={13}
  question="소프트웨어 테스트 원칙 중 파레토 법칙이 적용되어 결함의 약 80%가 전체 모듈의 약 20%에 집중된다는 원칙으로 알맞은 것은?"
  options={['① 살충제 패러독스', '② 결함 집중(Defect Clustering)', '③ 테스팅의 맥락 의존성', '④ 완벽한 테스팅 불가']}
  correctAnswer={2}
  explanation="결함 집중(Defect Clustering) 원칙은 소프트웨어 결함의 대다수가 소수의 모듈에 집중되는 경향이 있다는 원칙으로 파레토 법칙(80:20 법칙)이 적용됩니다. 테스터는 결함이 집중된 영역을 집중적으로 테스트해야 합니다."
  optionExplanations={['살충제 패러독스는 반복된 동일 테스트의 효과가 감소한다는 원칙입니다.', '정답입니다. 결함 집중은 결함이 특정 모듈에 집중되는 경향을 나타냅니다.', '테스팅의 맥락 의존성은 상황에 따라 테스트 방법이 달라야 한다는 원칙입니다.', '완벽한 테스팅 불가는 모든 입력 조합을 테스트할 수 없다는 원칙입니다.']}
/>

<Quiz
  questionNumber={14}
  question="Docker 컨테이너와 가상 머신(VM)의 차이점에 대한 설명으로 알맞은 것은?"
  options={['① 컨테이너는 독립된 OS 커널을 가지고, VM은 호스트 OS를 공유한다.', '② 컨테이너는 호스트 OS 커널을 공유하여 VM보다 가볍고 빠르게 시작한다.', '③ VM이 컨테이너보다 적은 리소스를 사용한다.', '④ 컨테이너와 VM은 동일한 아키텍처를 사용한다.']}
  correctAnswer={2}
  explanation="Docker 컨테이너는 호스트 OS의 커널을 공유하므로 각각 완전한 OS를 포함하는 VM보다 훨씬 가볍고 시작 속도가 빠릅니다. VM은 하이퍼바이저 위에서 독립된 OS를 실행하여 더 강한 격리를 제공하지만 오버헤드가 큽니다."
  optionExplanations={['컨테이너는 호스트 OS 커널을 공유하고, VM이 독립된 OS를 가집니다. 설명이 반대입니다.', '정답입니다. 컨테이너는 호스트 OS 커널을 공유하여 VM보다 가볍고 빠릅니다.', 'VM이 더 많은 리소스를 사용합니다. 컨테이너가 더 가볍습니다.', '컨테이너와 VM은 다른 아키텍처를 사용합니다.']}
/>

<Quiz
  questionNumber={15}
  question="Git Flow 브랜치 전략에서 기능 개발을 위해 사용하는 브랜치로 알맞은 것은?"
  options={['① main 브랜치', '② develop 브랜치', '③ feature 브랜치', '④ hotfix 브랜치']}
  correctAnswer={3}
  explanation="Git Flow에서 새로운 기능 개발은 develop 브랜치에서 파생된 feature 브랜치에서 수행합니다. 기능 완성 후 develop 브랜치로 병합됩니다. hotfix는 프로덕션 버그 수정, release는 배포 준비에 사용합니다."
  optionExplanations={['main(master) 브랜치는 배포된 안정 버전을 유지합니다.', 'develop 브랜치는 기능 개발의 통합 브랜치입니다.', '정답입니다. feature 브랜치는 개별 기능 개발을 위해 develop에서 분기합니다.', 'hotfix 브랜치는 프로덕션의 긴급 버그를 수정하기 위해 main에서 분기합니다.']}
/>

---
title: '20. 통합 예상 기출 2회'
description: '정보처리기사 실기 전 범위 통합 예상 기출문제 2회 (20문항)'
---

<Callout type="info">
  1회와 중복되지 않는 새로운 유형의 문제들로 구성된 통합 예상 기출 2회입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="UML 다이어그램 중 시스템의 정적 구조를 나타내며 클래스 간의 속성, 메서드, 관계를 표현하는 다이어그램으로 알맞은 것은?"
  options={['① 시퀀스 다이어그램', '② 클래스 다이어그램', '③ 활동 다이어그램', '④ 상태 다이어그램']}
  correctAnswer={2}
  explanation="클래스 다이어그램은 UML의 구조 다이어그램으로 클래스의 속성, 메서드, 클래스 간의 관계(상속, 연관, 집합, 합성 등)를 나타냅니다. 정적 구조 설계에 사용됩니다."
  optionExplanations={['시퀀스 다이어그램은 시간 순서에 따른 메시지 흐름을 나타냅니다.', '정답입니다. 클래스 다이어그램은 정적 구조를 표현합니다.', '활동 다이어그램은 작업 흐름과 병렬 처리를 나타냅니다.', '상태 다이어그램은 객체의 상태 변화를 나타냅니다.']}
/>

<Quiz
  questionNumber={2}
  question="SQL에서 부서별 직원 수를 구하되 직원 수가 5명 이상인 부서만 조회하는 쿼리로 알맞은 것은?"
  options={['① SELECT dept_id, COUNT(*) FROM employees WHERE COUNT(*) >= 5 GROUP BY dept_id', '② SELECT dept_id, COUNT(*) FROM employees GROUP BY dept_id HAVING COUNT(*) >= 5', '③ SELECT dept_id, COUNT(*) FROM employees HAVING COUNT(*) >= 5 GROUP BY dept_id', '④ SELECT dept_id, COUNT(*) WHERE COUNT(*) >= 5 FROM employees GROUP BY dept_id']}
  correctAnswer={2}
  explanation="집계 결과 필터링은 WHERE가 아닌 HAVING을 사용합니다. 순서는 GROUP BY → HAVING입니다. WHERE는 GROUP BY 이전에 개별 행을 필터링합니다."
  optionExplanations={['WHERE 절에서 집계 함수를 사용할 수 없습니다.', '정답입니다. HAVING으로 그룹화된 결과를 필터링합니다.', 'HAVING은 GROUP BY 이후에 위치해야 합니다.', 'SQL 구조가 잘못되었습니다.']}
/>

<Quiz
  questionNumber={3}
  question="GoF 디자인 패턴 중 기존 코드를 수정하지 않고 호환되지 않는 두 인터페이스를 연결하는 패턴으로 알맞은 것은?"
  options={['① Facade(파사드)', '② Proxy(프록시)', '③ Adapter(어댑터)', '④ Bridge(브리지)']}
  correctAnswer={3}
  explanation="Adapter(어댑터) 패턴은 기존 코드를 수정하지 않고 래퍼(Wrapper)를 통해 호환되지 않는 인터페이스를 연결합니다. 레거시 코드 통합에 유용합니다."
  optionExplanations={['Facade는 복잡한 서브시스템에 단순한 인터페이스를 제공합니다.', 'Proxy는 다른 객체에 대한 대리자를 제공합니다.', '정답입니다. Adapter는 호환되지 않는 인터페이스를 연결합니다.', 'Bridge는 추상과 구현을 분리합니다.']}
/>

<Quiz
  questionNumber={4}
  question="데이터베이스 이상현상 중 특정 행을 삭제할 때 다른 필요한 정보도 함께 삭제되는 현상으로 알맞은 것은?"
  options={['① 삽입 이상(Insertion Anomaly)', '② 삭제 이상(Deletion Anomaly)', '③ 갱신 이상(Update Anomaly)', '④ 참조 이상(Reference Anomaly)']}
  correctAnswer={2}
  explanation="삭제 이상(Deletion Anomaly)은 어떤 행을 삭제할 때 그 행에 포함된 다른 유용한 정보도 함께 삭제되는 이상현상입니다. 정규화로 해결할 수 있습니다."
  optionExplanations={['삽입 이상은 데이터 삽입 시 불필요한 데이터도 함께 삽입해야 하는 현상입니다.', '정답입니다. 삭제 이상은 필요한 다른 정보도 함께 삭제되는 현상입니다.', '갱신 이상은 중복 데이터 일부만 수정 시 불일치가 생기는 현상입니다.', '참조 이상은 이상현상의 표준 분류에 없습니다.']}
/>

<Quiz
  questionNumber={5}
  question="C 언어에서 다음 코드의 출력으로 알맞은 것은? int x = 10; int y = x++; printf('%d %d', x, y);"
  options={['① 10 10', '② 11 10', '③ 10 11', '④ 11 11']}
  correctAnswer={2}
  explanation="x++는 후위 증가 연산자로 현재 값을 먼저 사용한 후 증가합니다. y = x++(=10)으로 y는 10이 되고 그 후 x가 11이 됩니다. 따라서 x=11, y=10이 출력됩니다."
  optionExplanations={['x는 증가하여 11이 됩니다.', '정답입니다. 후위 증가: y=10 대입 후 x=11.', 'y는 x++의 원래 값인 10이 됩니다.', 'y는 x++ 시점의 10이므로 11이 아닙니다.']}
/>

<Quiz
  questionNumber={6}
  question="CPU 스케줄링 알고리즘 중 실행 중인 프로세스보다 더 짧은 실행 시간을 가진 새 프로세스가 도착하면 CPU를 빼앗는 선점형 알고리즘으로 알맞은 것은?"
  options={['① FCFS', '② SJF', '③ SRTF', '④ Round Robin']}
  correctAnswer={3}
  explanation="SRTF(Shortest Remaining Time First)는 SJF의 선점형 버전입니다. 현재 실행 중인 프로세스의 남은 시간보다 더 짧은 실행 시간을 가진 새 프로세스가 도착하면 즉시 CPU를 빼앗습니다."
  optionExplanations={['FCFS는 비선점형 알고리즘입니다.', 'SJF는 비선점형 기본 버전입니다.', '정답입니다. SRTF는 SJF의 선점형 버전입니다.', 'Round Robin은 타임 퀀텀을 사용하는 선점형 알고리즘입니다.']}
/>

<Quiz
  questionNumber={7}
  question="HTTP 프로토콜에서 서버의 내부 오류를 나타내는 상태 코드 범위로 알맞은 것은?"
  options={['① 1xx', '② 3xx', '③ 4xx', '④ 5xx']}
  correctAnswer={4}
  explanation="HTTP 상태 코드 5xx는 서버 측 오류를 나타냅니다. 500 Internal Server Error, 502 Bad Gateway, 503 Service Unavailable 등이 있습니다. 4xx는 클라이언트 오류입니다."
  optionExplanations={['1xx는 정보성 응답입니다.', '3xx는 리다이렉션을 나타냅니다.', '4xx는 클라이언트 측 오류입니다.', '정답입니다. 5xx는 서버 측 오류를 나타냅니다.']}
/>

<Quiz
  questionNumber={8}
  question="소프트웨어 형상 관리(SCM)에서 변경 통제 없이 수정할 수 없는 공식 검토·승인된 소프트웨어 구성 항목으로 알맞은 것은?"
  options={['① 브랜치(Branch)', '② 태그(Tag)', '③ 기준선(Baseline)', '④ 커밋(Commit)']}
  correctAnswer={3}
  explanation="기준선(Baseline)은 형상 관리에서 공식적으로 검토되고 승인된 소프트웨어 구성 항목의 집합입니다. 기준선이 설정되면 변경 통제 절차 없이는 수정할 수 없습니다."
  optionExplanations={['브랜치는 독립적인 개발 흐름을 생성합니다.', '태그는 특정 버전에 이름을 붙입니다.', '정답입니다. 기준선은 공식 승인된 소프트웨어 구성 항목 집합입니다.', '커밋은 변경사항을 저장소에 기록하는 행위입니다.']}
/>

<Quiz
  questionNumber={9}
  question="접근 제어 모델 중 군사·정부 시스템에서 주로 사용하며 시스템이 보안 레벨에 따라 강제로 접근을 통제하는 모델로 알맞은 것은?"
  options={['① DAC(임의적 접근 제어)', '② MAC(강제적 접근 제어)', '③ RBAC(역할 기반 접근 제어)', '④ ABAC(속성 기반 접근 제어)']}
  correctAnswer={2}
  explanation="MAC(Mandatory Access Control, 강제적 접근 제어)은 보안 레벨(등급)에 따라 시스템이 자원에 대한 접근을 강제로 통제합니다. 군사 정보 시스템에서 주로 사용됩니다."
  optionExplanations={['DAC는 자원 소유자가 접근 권한을 결정합니다.', '정답입니다. MAC는 보안 레벨 기반으로 시스템이 강제로 통제합니다.', 'RBAC는 역할에 따라 권한을 부여합니다.', 'ABAC는 다양한 속성을 기반으로 결정합니다.']}
/>

<Quiz
  questionNumber={10}
  question="파이썬에서 딕셔너리 d = {'a':1, 'b':2, 'c':3}에서 d.get('d', 0)의 결과로 알맞은 것은?"
  options={['① None', '② KeyError', '③ 0', '④ 3']}
  correctAnswer={3}
  explanation="d.get(key, default)는 키가 존재하면 해당 값을 반환하고 존재하지 않으면 default 값을 반환합니다. 'd' 키가 없으므로 기본값 0이 반환됩니다."
  optionExplanations={['get() 메서드는 키 없을 때 KeyError 대신 None 또는 기본값을 반환합니다.', 'get() 메서드는 KeyError를 발생시키지 않습니다.', '정답입니다. 키 없을 때 지정한 기본값 0이 반환됩니다.', '3은 딕셔너리의 마지막 값이지만 'd' 키에 해당하지 않습니다.']}
/>

<Quiz
  questionNumber={11}
  question="이진 탐색 알고리즘의 시간 복잡도로 알맞은 것은?"
  options={['① O(1)', '② O(log n)', '③ O(n)', '④ O(n²)']}
  correctAnswer={2}
  explanation="이진 탐색(Binary Search)은 매 단계마다 탐색 범위를 절반으로 줄이므로 O(log n)의 시간 복잡도를 가집니다. 정렬된 배열에서만 사용 가능합니다."
  optionExplanations={['O(1)은 해시 탐색의 시간 복잡도입니다.', '정답입니다. 이진 탐색의 시간 복잡도는 O(log n)입니다.', 'O(n)은 순차 탐색의 시간 복잡도입니다.', 'O(n²)은 버블 정렬 등의 시간 복잡도입니다.']}
/>

<Quiz
  questionNumber={12}
  question="Java에서 다음 코드의 출력으로 알맞은 것은? String s1 = 'hello'; String s2 = 'hello'; System.out.println(s1 == s2); System.out.println(s1.equals(s2));"
  options={['① false false', '② true true', '③ false true', '④ true false']}
  correctAnswer={2}
  explanation="Java에서 문자열 리터럴은 String Pool에서 관리됩니다. 동일한 리터럴 'hello'를 할당하면 같은 객체를 참조합니다. ==은 참조 비교, equals()는 내용 비교입니다. 리터럴이므로 ==도 true입니다."
  optionExplanations={['문자열 리터럴은 String Pool에서 동일 객체를 재사용합니다.', '정답입니다. 리터럴은 String Pool에서 같은 객체이므로 ==도 true, equals()도 true입니다.', 'new String()으로 생성 시에는 ==는 false이지만 리터럴은 다릅니다.', 'equals()는 내용 비교로 true이며 ==도 리터럴에서 true입니다.']}
/>

<Quiz
  questionNumber={13}
  question="소프트웨어 유지보수 유형 중 새로운 운영체제나 하드웨어 변경에 소프트웨어를 적응시키는 유지보수로 알맞은 것은?"
  options={['① 수정 유지보수(Corrective)', '② 적응 유지보수(Adaptive)', '③ 완전 유지보수(Perfective)', '④ 예방 유지보수(Preventive)']}
  correctAnswer={2}
  explanation="적응 유지보수(Adaptive Maintenance)는 운영 환경 변화(OS 업그레이드, 하드웨어 교체, 법규 변경 등)에 소프트웨어를 맞추는 유지보수입니다."
  optionExplanations={['수정 유지보수는 버그·결함을 수정합니다.', '정답입니다. 적응 유지보수는 환경 변화에 소프트웨어를 적응시킵니다.', '완전 유지보수는 성능 향상이나 기능 추가를 합니다.', '예방 유지보수는 미래 문제 예방을 위한 수정입니다.']}
/>

<Quiz
  questionNumber={14}
  question="위험 관리에서 위험을 보험이나 외주 계약을 통해 다른 주체에게 넘기는 대응 전략으로 알맞은 것은?"
  options={['① 회피(Avoidance)', '② 전가(Transfer)', '③ 완화(Mitigation)', '④ 수용(Acceptance)']}
  correctAnswer={2}
  explanation="전가(Transfer) 전략은 위험을 보험 가입, 외주 계약, SLA 등을 통해 제3자에게 이전합니다. 위험 자체가 사라지는 것이 아니라 책임이 이전됩니다."
  optionExplanations={['회피는 위험을 유발하는 활동 자체를 제거합니다.', '정답입니다. 전가는 보험/외주로 위험 책임을 제3자에게 넘깁니다.', '완화는 위험 발생 확률이나 영향을 줄입니다.', '수용은 위험 발생을 허용하고 결과를 받아들입니다.']}
/>

<Quiz
  questionNumber={15}
  question="클라우드 서비스 모델 중 가상 서버, 네트워크, 스토리지 등 인프라를 제공하며 사용자가 OS와 애플리케이션을 직접 관리하는 모델로 알맞은 것은?"
  options={['① SaaS(Software as a Service)', '② PaaS(Platform as a Service)', '③ IaaS(Infrastructure as a Service)', '④ FaaS(Function as a Service)']}
  correctAnswer={3}
  explanation="IaaS(Infrastructure as a Service)는 가상 서버, 네트워크, 스토리지 등 기반 인프라를 제공합니다. 사용자는 OS, 미들웨어, 애플리케이션을 직접 설치하고 관리합니다. AWS EC2, Azure VM이 대표적입니다."
  optionExplanations={['SaaS는 완성된 소프트웨어를 서비스로 제공합니다.', 'PaaS는 플랫폼(OS, 미들웨어, 런타임)을 제공합니다.', '정답입니다. IaaS는 가상 인프라(서버, 네트워크, 스토리지)를 제공합니다.', 'FaaS는 함수 단위로 실행하는 서버리스 컴퓨팅입니다.']}
/>

<Quiz
  questionNumber={16}
  question="정규화 단계 중 제3정규형(3NF)으로 만들기 위해 제거해야 하는 것으로 알맞은 것은?"
  options={['① 다중값 속성', '② 부분 함수 종속', '③ 이행 함수 종속', '④ 순환 종속']}
  correctAnswer={3}
  explanation="제3정규형(3NF)은 제2정규형을 만족하면서 이행 함수 종속(A→B→C 형태로 A가 B를 통해 C에 종속)을 제거한 정규형입니다."
  optionExplanations={['다중값 속성 제거는 제1정규형(1NF)의 조건입니다.', '부분 함수 종속 제거는 제2정규형(2NF)의 조건입니다.', '정답입니다. 이행 함수 종속 제거는 3NF의 조건입니다.', '순환 종속은 정규화의 표준 용어가 아닙니다.']}
/>

<Quiz
  questionNumber={17}
  question="스택(Stack)을 활용하는 알고리즘으로 옳지 않은 것으로 알맞은 것은?"
  options={['① 후위 표기식(Postfix) 계산', '② 괄호 검사', '③ DFS(깊이 우선 탐색)', '④ BFS(너비 우선 탐색)']}
  correctAnswer={4}
  explanation="BFS(너비 우선 탐색)는 큐(Queue)를 사용합니다. 스택(Stack)은 후위 표기식 계산, 괄호 검사, DFS, 재귀 함수 호출, 되돌리기(undo) 등에 사용됩니다."
  optionExplanations={['후위 표기식 계산에 스택을 사용합니다.', '괄호 검사에 스택을 사용합니다.', 'DFS는 스택 또는 재귀(내부 스택)를 사용합니다.', '정답입니다(틀린 것). BFS는 큐(Queue)를 사용합니다.']}
/>

<Quiz
  questionNumber={18}
  question="SOLID 원칙 중 하위 클래스는 상위 클래스를 대체할 수 있어야 한다는 원칙으로 알맞은 것은?"
  options={['① 단일 책임 원칙(SRP)', '② 개방-폐쇄 원칙(OCP)', '③ 리스코프 치환 원칙(LSP)', '④ 인터페이스 분리 원칙(ISP)']}
  correctAnswer={3}
  explanation="리스코프 치환 원칙(LSP: Liskov Substitution Principle)은 자식 클래스는 부모 클래스를 대체할 수 있어야 한다는 원칙입니다. 부모 타입의 참조 변수에 자식 객체를 할당해도 동작이 보장되어야 합니다."
  optionExplanations={['단일 책임 원칙은 클래스가 하나의 책임만 가져야 합니다.', '개방-폐쇄 원칙은 확장에 열리고 수정에 닫혀있어야 합니다.', '정답입니다. 리스코프 치환 원칙은 하위 클래스가 상위 클래스를 대체 가능해야 합니다.', '인터페이스 분리 원칙은 사용하지 않는 인터페이스에 의존하지 않아야 합니다.']}
/>

<Quiz
  questionNumber={19}
  question="개인정보보호법에서 정보주체가 자신의 개인정보에 대해 열람, 정정·삭제, 처리 정지를 요구할 수 있는 권리를 통칭하는 용어로 알맞은 것은?"
  options={['① 개인정보보호 의무', '② 정보주체의 권리', '③ 개인정보 처리방침', '④ 개인정보 영향평가']}
  correctAnswer={2}
  explanation="개인정보보호법에서 정보주체의 권리는 열람권(자신의 정보 열람 요구), 정정·삭제권(잘못된 정보 수정·삭제 요구), 처리 정지권(처리 중단 요구), 동의 철회권 등을 포함합니다."
  optionExplanations={['개인정보보호 의무는 처리자의 의무를 나타냅니다.', '정답입니다. 정보주체의 권리는 열람, 정정·삭제, 처리 정지 등을 포함합니다.', '개인정보 처리방침은 처리 목적, 기간 등을 공개하는 문서입니다.', '개인정보 영향평가는 대량 개인정보 처리 시스템 도입 전 평가입니다.']}
/>

<Quiz
  questionNumber={20}
  question="ITIL 인시던트 관리의 주요 목적으로 알맞은 것은?"
  options={['① 인시던트의 근본 원인 파악', '② IT 서비스를 신속하게 정상 상태로 복구', '③ IT 환경 변경을 통제', '④ SLA를 협상']}
  correctAnswer={2}
  explanation="인시던트 관리(Incident Management)의 주요 목적은 IT 서비스 중단을 최소화하고 서비스를 신속하게 정상 상태로 복구하는 것입니다. 근본 원인 파악은 문제 관리(Problem Management)의 목적입니다."
  optionExplanations={['근본 원인 파악은 문제 관리(Problem Management)의 목적입니다.', '정답입니다. 인시던트 관리는 신속한 서비스 복구가 목적입니다.', 'IT 환경 변경 통제는 변경 관리(Change Management)의 역할입니다.', 'SLA 협상은 서비스 수준 관리의 역할입니다.']}
/>

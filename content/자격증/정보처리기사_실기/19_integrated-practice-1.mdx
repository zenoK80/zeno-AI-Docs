---
title: '19. 통합 예상 기출 1회'
description: '정보처리기사 실기 전 범위 통합 예상 기출문제 1회 (20문항)'
---

<Callout type="info">
  실기 시험 형식으로 구성한 통합 예상 기출문제입니다. 단답형 형식으로 답을 쓰는 연습을 하세요.
</Callout>

<Quiz
  questionNumber={1}
  question="소프트웨어 생명주기 모델 중 요구사항 변화에 유연하게 대응하고 2~4주 단위 스프린트로 반복 개발하는 방법론으로 알맞은 것은?"
  options={['① 폭포수 모델', '② 나선형 모델', '③ 애자일 방법론', '④ V 모델']}
  correctAnswer={3}
  explanation="애자일(Agile) 방법론은 짧은 반복 주기(스프린트)로 동작하는 소프트웨어를 지속적으로 제공하며 변화에 유연하게 대응합니다. Scrum과 XP가 대표적인 애자일 프레임워크입니다."
  optionExplanations={['폭포수는 순차적 모델로 요구사항 변경이 어렵습니다.', '나선형은 위험 분석 중심 반복 모델입니다.', '정답입니다. 애자일은 짧은 스프린트로 반복 개발합니다.', 'V 모델은 개발-테스트 대칭 순차 모델입니다.']}
/>

<Quiz
  questionNumber={2}
  question="SQL에서 사원 테이블(employees)에서 부서ID가 10인 직원의 이름과 급여를 이름 오름차순으로 조회하는 쿼리로 알맞은 것은?"
  options={['① SELECT name, salary FROM employees WHERE dept_id = 10 ORDER BY name ASC', '② SELECT name, salary FROM employees ORDER BY name WHERE dept_id = 10', '③ SELECT name, salary WHERE dept_id = 10 FROM employees ORDER BY name', '④ FROM employees SELECT name, salary WHERE dept_id = 10 ORDER BY name']}
  correctAnswer={1}
  explanation="올바른 SELECT 문 순서: SELECT → FROM → WHERE → ORDER BY. WHERE 절이 FROM 뒤에, ORDER BY가 마지막에 위치해야 합니다."
  optionExplanations={['정답입니다. SELECT-FROM-WHERE-ORDER BY 순서가 올바릅니다.', 'ORDER BY는 WHERE 뒤에 위치해야 합니다.', 'WHERE는 FROM 뒤에 위치해야 합니다.', 'SQL 문은 SELECT로 시작해야 합니다.']}
/>

<Quiz
  questionNumber={3}
  question="GoF 디자인 패턴 중 한 객체의 상태 변화를 관찰하고 있는 모든 객체에 자동으로 알려주는 패턴으로 알맞은 것은?"
  options={['① Strategy 패턴', '② Observer 패턴', '③ Singleton 패턴', '④ Decorator 패턴']}
  correctAnswer={2}
  explanation="Observer(옵서버) 패턴은 객체의 상태 변화를 구독(subscribe)하는 모든 관찰자에게 자동으로 알림을 보내는 행위 패턴입니다. 이벤트 처리와 MVC 구현에 주로 사용됩니다."
  optionExplanations={['Strategy는 알고리즘을 캡슐화하여 교환 가능하게 합니다.', '정답입니다. Observer는 상태 변화 시 모든 관찰자에게 자동 알림합니다.', 'Singleton은 인스턴스를 하나만 생성하도록 보장합니다.', 'Decorator는 객체에 동적으로 기능을 추가합니다.']}
/>

<Quiz
  questionNumber={4}
  question="정규화에서 1NF, 2NF, 3NF 중 부분 함수 종속을 제거하는 단계로 알맞은 것은?"
  options={['① 1NF', '② 2NF', '③ 3NF', '④ BCNF']}
  correctAnswer={2}
  explanation="제2정규형(2NF)은 1NF를 만족하면서 부분 함수 종속(복합키의 일부에만 의존하는 속성)을 제거합니다. 제3정규형은 이행 함수 종속을 제거합니다."
  optionExplanations={['1NF는 반복 그룹과 다중값 속성을 제거합니다.', '정답입니다. 2NF는 부분 함수 종속을 제거합니다.', '3NF는 이행 함수 종속을 제거합니다.', 'BCNF는 결정자가 후보키가 아닌 종속을 제거합니다.']}
/>

<Quiz
  questionNumber={5}
  question="다음 Java 코드에서 출력되는 값으로 알맞은 것은? int[] arr = {5, 3, 8, 1, 6}; int max = arr[0]; for(int i=1; i<arr.length; i++) { if(arr[i] > max) max = arr[i]; } System.out.println(max);"
  options={['① 1', '② 5', '③ 6', '④ 8']}
  correctAnswer={4}
  explanation="코드는 배열에서 최대값을 찾습니다. max=5로 시작, 3(작음), 8(큼)→max=8, 1(작음), 6(작음). 최종 max=8이 출력됩니다."
  optionExplanations={['1은 최소값입니다.', '5는 초기 max 값입니다.', '6은 마지막 요소이지만 8보다 작습니다.', '정답입니다. 배열에서 가장 큰 값은 8입니다.']}
/>

<Quiz
  questionNumber={6}
  question="교착상태(Deadlock) 4대 발생 조건이 아닌 것으로 알맞은 것은?"
  options={['① 상호 배제', '② 점유와 대기', '③ 비선점', '④ 우선순위']}
  correctAnswer={4}
  explanation="교착상태 4대 조건은 상호 배제(Mutual Exclusion), 점유와 대기(Hold and Wait), 비선점(No Preemption), 순환 대기(Circular Wait)입니다. 우선순위는 교착상태 조건이 아닙니다."
  optionExplanations={['상호 배제는 교착상태 4대 조건 중 하나입니다.', '점유와 대기는 교착상태 4대 조건 중 하나입니다.', '비선점은 교착상태 4대 조건 중 하나입니다.', '정답입니다. 우선순위는 교착상태 4대 조건이 아닙니다.']}
/>

<Quiz
  questionNumber={7}
  question="네트워크 OSI 7계층 중 IP 주소를 기반으로 라우팅을 수행하며 라우터가 동작하는 계층으로 알맞은 것은?"
  options={['① 2계층 (데이터링크)', '② 3계층 (네트워크)', '③ 4계층 (전송)', '④ 5계층 (세션)']}
  correctAnswer={2}
  explanation="네트워크 계층(3계층)은 IP 주소를 기반으로 패킷의 최적 경로를 선택하여 라우팅합니다. 라우터가 이 계층에서 동작하며 IP, ICMP 프로토콜이 해당합니다."
  optionExplanations={['데이터링크 계층은 MAC 주소 기반으로 스위치가 동작합니다.', '정답입니다. 네트워크 계층은 IP 주소 기반 라우팅을 담당합니다.', '전송 계층은 TCP/UDP로 포트 기반 통신을 합니다.', '세션 계층은 통신 세션을 관리합니다.']}
/>

<Quiz
  questionNumber={8}
  question="소프트웨어 테스트 기법 중 소스코드의 내부 논리 구조를 알고 수행하는 테스트로 알맞은 것은?"
  options={['① 블랙박스 테스트', '② 화이트박스 테스트', '③ 인수 테스트', '④ 시스템 테스트']}
  correctAnswer={2}
  explanation="화이트박스 테스트(White-box Test)는 소스코드의 내부 구조(로직, 조건, 루프)를 알고 테스트합니다. 구문 커버리지, 결정 커버리지, 경로 커버리지 등이 포함됩니다."
  optionExplanations={['블랙박스 테스트는 내부 구조를 모르고 입출력으로만 테스트합니다.', '정답입니다. 화이트박스 테스트는 내부 코드 구조를 기반으로 합니다.', '인수 테스트는 사용자가 요구사항 부합을 확인합니다.', '시스템 테스트는 전체 시스템이 요구사항에 맞는지 검증합니다.']}
/>

<Quiz
  questionNumber={9}
  question="정보보안 CIA 트리아드에서 DDoS 공격이 주로 침해하는 요소로 알맞은 것은?"
  options={['① 기밀성(Confidentiality)', '② 무결성(Integrity)', '③ 가용성(Availability)', '④ 인증성(Authenticity)']}
  correctAnswer={3}
  explanation="DDoS(분산 서비스 거부) 공격은 대량의 트래픽으로 서버를 마비시켜 정상적인 사용자가 서비스에 접근하지 못하게 합니다. 이는 가용성(Availability)을 침해하는 공격입니다."
  optionExplanations={['기밀성은 도청, 스니핑 등으로 침해됩니다.', '무결성은 데이터 위변조로 침해됩니다.', '정답입니다. DDoS는 서비스 가용성을 침해합니다.', '인증성은 CIA 트리아드에 포함되지 않습니다.']}
/>

<Quiz
  questionNumber={10}
  question="파이썬에서 list(map(lambda x: x*2, [1, 2, 3]))의 결과로 알맞은 것은?"
  options={['① [1, 2, 3]', '② [2, 4, 6]', '③ [1, 4, 9]', '④ [2, 3, 4]']}
  correctAnswer={2}
  explanation="map(lambda x: x*2, [1, 2, 3])은 리스트의 각 요소에 함수(x*2)를 적용합니다. 1*2=2, 2*2=4, 3*2=6이므로 결과는 [2, 4, 6]입니다."
  optionExplanations={['[1, 2, 3]은 원본 리스트입니다.', '정답입니다. 각 요소에 2를 곱한 [2, 4, 6]입니다.', '[1, 4, 9]는 제곱한 결과입니다.', '[2, 3, 4]는 각 요소에 1을 더한 결과입니다.']}
/>

<Quiz
  questionNumber={11}
  question="PERT 기대 시간 공식에서 최빈값(M)에 곱하는 가중치로 알맞은 것은?"
  options={['① 1', '② 2', '③ 4', '④ 6']}
  correctAnswer={3}
  explanation="PERT 기대 시간 = (O + 4M + P) / 6에서 최빈값(Most likely, M)에 가중치 4를 곱합니다. 낙관치와 비관치는 각각 가중치 1을 사용합니다."
  optionExplanations={['낙관치와 비관치의 가중치가 1입니다.', '2는 PERT 공식에서 사용하지 않는 가중치입니다.', '정답입니다. 최빈값에 가중치 4를 곱합니다.', '6은 나누는 값이며 최빈값의 가중치가 아닙니다.']}
/>

<Quiz
  questionNumber={12}
  question="SQL에서 NULL 값의 처리에 대한 설명으로 옳지 않은 것은?"
  options={['① NULL과 어떤 값을 비교해도 결과는 NULL(UNKNOWN)이다.', '② NULL 값 확인은 IS NULL을 사용한다.', '③ SUM(컬럼)은 NULL을 0으로 처리한다.', '④ COUNT(*)는 NULL을 포함하여 모든 행을 센다.']}
  correctAnswer={3}
  explanation="SUM, AVG 등 집계 함수는 NULL을 무시하고 계산합니다. NULL을 0으로 처리하지 않습니다. COALESCE(컬럼, 0)으로 NULL을 0으로 변환할 수 있습니다."
  optionExplanations={['NULL과 비교 시 UNKNOWN(NULL)이 반환됩니다. 올바른 설명.', 'IS NULL로 NULL 값을 확인합니다. 올바른 설명.', '정답입니다(틀린 것). SUM은 NULL을 무시하고 계산합니다.', 'COUNT(*)는 NULL 포함 모든 행을 셉니다. 올바른 설명.']}
/>

<Quiz
  questionNumber={13}
  question="소프트웨어 품질 특성 중 소프트웨어를 다른 운영체제나 하드웨어 환경으로 이전할 수 있는 능력으로 알맞은 것은?"
  options={['① 신뢰성(Reliability)', '② 유지보수성(Maintainability)', '③ 이식성(Portability)', '④ 효율성(Efficiency)']}
  correctAnswer={3}
  explanation="이식성(Portability)은 소프트웨어를 다른 환경(하드웨어, 운영체제, 플랫폼)으로 이전할 수 있는 능력입니다. Java의 'Write Once, Run Anywhere'가 높은 이식성의 예입니다."
  optionExplanations={['신뢰성은 지정 기간 동안 결함 없이 동작하는 능력입니다.', '유지보수성은 수정 및 개선의 용이성입니다.', '정답입니다. 이식성은 다른 환경으로의 이전 능력입니다.', '효율성은 자원 사용 대비 성능입니다.']}
/>

<Quiz
  questionNumber={14}
  question="Scrum의 5가지 이벤트 중 매 스프린트 종료 시 팀이 작업 방식을 돌아보고 개선사항을 도출하는 이벤트로 알맞은 것은?"
  options={['① 스프린트 계획(Sprint Planning)', '② 일일 스크럼(Daily Scrum)', '③ 스프린트 검토(Sprint Review)', '④ 스프린트 회고(Sprint Retrospective)']}
  correctAnswer={4}
  explanation="스프린트 회고(Sprint Retrospective)는 스프린트 종료 후 팀의 작업 방식, 협업, 프로세스를 돌아보고 다음 스프린트에서 개선할 사항을 논의하는 이벤트입니다."
  optionExplanations={['스프린트 계획은 스프린트 시작 시 목표와 작업을 선정합니다.', '일일 스크럼은 매일 15분 진행 상황을 공유합니다.', '스프린트 검토는 스프린트 종료 시 완성된 기능을 시연합니다.', '정답입니다. 스프린트 회고는 작업 방식 개선사항을 도출합니다.']}
/>

<Quiz
  questionNumber={15}
  question="C 언어에서 구조체 포인터를 사용하여 멤버에 접근할 때 사용하는 연산자로 알맞은 것은?"
  options={['① .(점 연산자)', '② ->(화살표 연산자)', '③ *(역참조 연산자)', '④ &(주소 연산자)']}
  correctAnswer={2}
  explanation="C 언어에서 구조체 포인터로 멤버에 접근할 때 -> 연산자를 사용합니다. p->name은 (*p).name과 동일합니다. 일반 구조체 변수의 멤버 접근에는 . 연산자를 사용합니다."
  optionExplanations={['.(점) 연산자는 일반 구조체 변수에서 멤버 접근에 사용합니다.', '정답입니다. -> 연산자는 구조체 포인터에서 멤버 접근에 사용합니다.', '* 연산자는 포인터 역참조에 사용합니다.', '& 연산자는 변수의 주소를 얻는 데 사용합니다.']}
/>

<Quiz
  questionNumber={16}
  question="암호화에서 공개키로 암호화하고 개인키로 복호화하는 방식으로 알맞은 것은?"
  options={['① 대칭키 암호화', '② 비대칭키(공개키) 암호화', '③ 해시 함수', '④ MAC(메시지 인증 코드)']}
  correctAnswer={2}
  explanation="비대칭키(공개키) 암호화는 수신자의 공개키로 암호화하고 수신자의 개인키로 복호화합니다. RSA가 대표적입니다. 전자서명은 반대로 개인키로 서명하고 공개키로 검증합니다."
  optionExplanations={['대칭키는 동일한 키로 암호화와 복호화를 합니다.', '정답입니다. 비대칭키는 공개키로 암호화, 개인키로 복호화합니다.', '해시 함수는 단방향으로 복호화가 불가능합니다.', 'MAC은 메시지 무결성 검증에 사용됩니다.']}
/>

<Quiz
  questionNumber={17}
  question="소프트웨어 통합 테스트에서 상향식(Bottom-up) 방법에서 사용하는 더미 모듈로 알맞은 것은?"
  options={['① 스텁(Stub)', '② 드라이버(Driver)', '③ 목(Mock)', '④ 페이크(Fake)']}
  correctAnswer={2}
  explanation="상향식 통합 테스트(Bottom-up)는 하위 모듈부터 테스트하며 아직 완성되지 않은 상위 모듈을 대신하는 드라이버(Driver)를 사용합니다. 스텁은 하향식 테스트에서 사용됩니다."
  optionExplanations={['스텁은 하향식(Top-down) 테스트에서 하위 모듈을 대체합니다.', '정답입니다. 드라이버는 상향식(Bottom-up) 테스트에서 상위 모듈을 대체합니다.', '목(Mock)은 행위 검증을 위한 테스트 더블입니다.', '페이크는 간단한 구현을 가진 테스트 더블입니다.']}
/>

<Quiz
  questionNumber={18}
  question="REST API에서 기존 리소스의 일부 필드만 수정할 때 사용하는 HTTP 메서드로 알맞은 것은?"
  options={['① GET', '② POST', '③ PUT', '④ PATCH']}
  correctAnswer={4}
  explanation="PATCH는 리소스의 일부만 부분적으로 수정하는 메서드입니다. PUT은 리소스 전체를 대체합니다. 예: 사용자의 이름만 변경할 때는 PATCH /users/1을 사용합니다."
  optionExplanations={['GET은 리소스 조회에 사용합니다.', 'POST는 새 리소스 생성에 사용합니다.', 'PUT은 리소스 전체를 대체합니다.', '정답입니다. PATCH는 리소스의 일부를 부분 수정합니다.']}
/>

<Quiz
  questionNumber={19}
  question="COCOMO 비용 산정 모델에서 비용 산정의 기준이 되는 단위로 알맞은 것은?"
  options={['① FP(기능 점수)', '② LOC(코드 라인 수)', '③ 테스트 케이스 수', '④ 개발 인원 수']}
  correctAnswer={2}
  explanation="COCOMO(Constructive Cost Model)는 LOC(Lines of Code, 코드 라인 수)를 기반으로 개발 노력, 일정, 비용을 산정합니다. 기능 점수(FP)는 COCOMO와 다른 별도의 산정 방법입니다."
  optionExplanations={['FP는 기능 점수 방법으로 COCOMO와는 다른 산정 방식입니다.', '정답입니다. COCOMO는 LOC를 기반으로 비용을 산정합니다.', '테스트 케이스 수는 비용 산정 기준이 아닙니다.', '개발 인원 수는 COCOMO의 출력 값 중 하나입니다.']}
/>

<Quiz
  questionNumber={20}
  question="파이썬 코드 [x for x in range(10) if x % 3 == 0]의 결과로 알맞은 것은?"
  options={['① [0, 3, 6, 9]', '② [3, 6, 9]', '③ [1, 4, 7]', '④ [0, 3, 6]']}
  correctAnswer={1}
  explanation="range(10)은 0~9를 생성합니다. x % 3 == 0인 값: 0(True), 1(False), 2(False), 3(True), 4(False), 5(False), 6(True), 7(False), 8(False), 9(True). 결과: [0, 3, 6, 9]"
  optionExplanations={['정답입니다. 0부터 9 중 3의 배수: 0, 3, 6, 9입니다.', '0도 3의 배수(0÷3=0)이므로 포함됩니다.', '나머지가 1인 값들입니다.', '9도 3의 배수이므로 포함됩니다.']}
/>

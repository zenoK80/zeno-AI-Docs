---
title: '09. Java 프로그래밍'
description: 'Java OOP, 상속, 다형성, 예외처리, 람다, 컬렉션 코드를 학습합니다.'
---

<Callout type="info">
  실기 시험에서 Java 코드 추적 문제가 자주 출제됩니다. 상속, 오버라이딩, 예외처리, 형변환 코드를 직접 추적해보세요.
</Callout>

## 클래스와 상속

```java
class Animal {
    String name;
    Animal(String name) { this.name = name; }
    void sound() { System.out.println(name + " 소리"); }
}

class Dog extends Animal {
    Dog(String name) { super(name); }
    @Override
    void sound() { System.out.println(name + " 멍멍!"); }
}

Animal a = new Dog("바둑이");
a.sound();  // 바둑이 멍멍! (동적 바인딩)
```

---

## 다형성과 형변환

```java
// 업캐스팅 (자동) - 자식을 부모 타입으로
Animal a = new Dog("멍이");

// 다운캐스팅 (명시적) - 부모를 자식 타입으로
Dog d = (Dog) a;

// instanceof 확인
if (a instanceof Dog) {
    Dog dog = (Dog) a;
    // ...
}
```

---

## 인터페이스

```java
interface Flyable {
    void fly();  // 추상 메서드 (자동으로 public abstract)
    default void land() {
        System.out.println("착륙");
    }
}

class Bird implements Flyable {
    @Override
    public void fly() {
        System.out.println("날아갑니다");
    }
}
```

---

## 예외 처리

```java
try {
    int result = 10 / 0;          // ArithmeticException 발생
    System.out.println(result);
} catch (ArithmeticException e) {
    System.out.println("0으로 나눌 수 없습니다");
} finally {
    System.out.println("항상 실행");  // 예외 여부 관계없이 실행
}
// 출력:
// 0으로 나눌 수 없습니다
// 항상 실행
```

---

## 람다 표현식

```java
// 함수형 인터페이스 사용
List<String> names = Arrays.asList("Charlie", "Alice", "Bob");

// 람다로 정렬
names.sort((a, b) -> a.compareTo(b));
// 결과: [Alice, Bob, Charlie]

// forEach
names.forEach(name -> System.out.println(name));
```

---

## 코드 추적 예제

```java
public class Main {
    static int count = 0;

    static void increment() {
        count++;
    }

    public static void main(String[] args) {
        for (int i = 0; i < 3; i++) {
            increment();
        }
        System.out.println(count);  // 3
    }
}
```

```java
// 정적 메서드와 인스턴스 메서드
class Counter {
    int n = 0;
    void add(int x) { n += x; }
    int get() { return n; }
}

Counter c = new Counter();
c.add(3);
c.add(5);
System.out.println(c.get());  // 8
```

---

## 마무리 복습

<Quiz
  questionNumber={1}
  question="다음 Java 코드의 출력으로 알맞은 것은? class A { void print() { System.out.println('A'); } } class B extends A { @Override void print() { System.out.println('B'); } } A obj = new B(); obj.print();"
  options={['① A', '② B', '③ AB', '④ 컴파일 오류']}
  correctAnswer={2}
  explanation="Java의 동적 바인딩(Dynamic Binding)으로 실제 객체 타입(B)의 메서드가 호출됩니다. A obj = new B()에서 참조 타입은 A이지만 실제 객체는 B이므로 B의 print()가 호출됩니다."
  optionExplanations={['참조 타입이 A이지만 실제 객체가 B이므로 B의 메서드가 호출됩니다.', '정답입니다. 동적 바인딩으로 실제 객체 B의 print()가 호출됩니다.', 'AB가 아닌 B만 출력됩니다.', '문법적으로 올바른 코드입니다.']}
/>

<Quiz
  questionNumber={2}
  question="Java에서 예외가 발생하지 않아도 항상 실행되는 블록으로 알맞은 것은?"
  options={['① try', '② catch', '③ throw', '④ finally']}
  correctAnswer={4}
  explanation="finally 블록은 예외 발생 여부와 관계없이 항상 실행됩니다. 자원 해제(파일 닫기, DB 연결 해제 등)에 주로 사용됩니다. try 블록 내에서 return이 있어도 finally는 실행됩니다."
  optionExplanations={['try는 예외가 발생할 수 있는 코드를 포함합니다.', 'catch는 예외 발생 시에만 실행됩니다.', 'throw는 예외를 명시적으로 던집니다.', '정답입니다. finally는 예외 여부와 관계없이 항상 실행됩니다.']}
/>

<Quiz
  questionNumber={3}
  question="Java에서 인터페이스를 구현하는 클래스 작성 시 반드시 해야 하는 것으로 알맞은 것은?"
  options={['① 인터페이스의 모든 추상 메서드를 구현', '② 인터페이스를 상속(extends)으로 선언', '③ 생성자를 반드시 정의', '④ static 메서드를 포함']}
  correctAnswer={1}
  explanation="인터페이스를 구현하는 클래스는 인터페이스의 모든 추상 메서드를 구현해야 합니다(Java 8의 default 메서드 제외). 미구현 시 해당 클래스도 추상 클래스로 선언해야 합니다."
  optionExplanations={['정답입니다. 인터페이스 구현 시 모든 추상 메서드를 구현해야 합니다.', '인터페이스 사용은 extends가 아닌 implements입니다.', '생성자 정의는 필수가 아닙니다.', 'static 메서드 포함은 필수가 아닙니다.']}
/>

<Quiz
  questionNumber={4}
  question="다음 Java 코드의 출력으로 알맞은 것은? int[] arr = {3, 1, 4, 1, 5}; int sum = 0; for (int x : arr) { sum += x; } System.out.println(sum);"
  options={['① 10', '② 13', '③ 14', '④ 15']}
  correctAnswer={3}
  explanation="배열 {3, 1, 4, 1, 5}의 모든 요소를 더하면 3 + 1 + 4 + 1 + 5 = 14입니다. 향상된 for 루프(for-each)로 배열의 모든 요소를 순회하며 더합니다."
  optionExplanations={['10이 아닌 14가 맞습니다.', '13이 아닌 14가 맞습니다.', '정답입니다. 3+1+4+1+5 = 14입니다.', '15가 아닌 14가 맞습니다. (1이 두 번 나옵니다)']}
/>

<Quiz
  questionNumber={5}
  question="Java에서 부모 클래스의 생성자를 호출하는 키워드로 알맞은 것은?"
  options={['① parent()', '② base()', '③ super()', '④ this()']}
  correctAnswer={3}
  explanation="Java에서 super()는 부모 클래스의 생성자를 호출하는 키워드입니다. 자식 클래스 생성자의 첫 줄에 작성해야 하며 생략하면 컴파일러가 자동으로 super()를 추가합니다."
  optionExplanations={['parent()는 Java에서 부모 생성자 호출 키워드가 아닙니다.', 'base()는 C#에서 사용하는 키워드입니다.', '정답입니다. super()는 Java에서 부모 생성자를 호출합니다.', 'this()는 같은 클래스 내 다른 생성자를 호출합니다.']}
/>

---
title: '29. 실전 모의고사 1회'
description: '빅데이터분석기사 필기 4과목 20문제 실전 모의고사로 실력을 점검합니다.'
---

<Callout type="info">
  **시험 안내:** 빅데이터분석기사 필기는 4과목 각 20문제, 총 80문제, 100분입니다. 합격 기준은 과목당 40점 이상, 전 과목 평균 60점 이상입니다. 이 모의고사는 과목당 5문제, 총 20문제로 구성됩니다.
</Callout>

## 1과목: 빅데이터 분석 기획 (문제 1~5)

---

<Quiz
  questionNumber={1}
  question="분석 과제 우선순위 결정 시 '가치-난이도 매트릭스'를 사용한다. 다음 중 가장 먼저 추진해야 할 과제는?"
  options={['① 비즈니스 가치가 낮고 분석 난이도가 낮은 과제', '② 비즈니스 가치가 높고 분석 난이도가 낮은 과제', '③ 비즈니스 가치가 높고 분석 난이도가 높은 과제', '④ 비즈니스 가치가 낮고 분석 난이도가 높은 과제']}
  correctAnswer={2}
  explanation="가치-난이도 매트릭스에서 우선순위는 가치 高 + 난이도 低인 과제입니다. 즉각적인 성과를 내면서 투입 리소스가 적게 드는 과제를 먼저 추진합니다. 가치 高 + 난이도 高는 장기 프로젝트로 분류하고, 가치 低는 중단 또는 보류를 검토합니다."
  optionExplanations={['①은 가치가 낮으므로 우선순위가 낮습니다. 운영 효율화 과제로 분류합니다.', '정답입니다. 가치 高 + 난이도 低는 즉시 추진할 1순위 과제입니다.', '③은 가치는 높지만 난이도도 높아 역량 확보 후 장기 추진 과제로 분류합니다.', '④는 가치도 낮고 난이도도 높아 보류 또는 제외 대상입니다.']}
/>

---

<Quiz
  questionNumber={2}
  question="데이터 거버넌스의 3대 구성 요소로 올바르게 짝지어진 것은?"
  options={['① 원칙(Principle), 조직(Organization), 프로세스(Process)', '② 수집, 저장, 분석', '③ 보안, 프라이버시, 컴플라이언스', '④ 마스터 데이터, 트랜잭션 데이터, 메타 데이터']}
  correctAnswer={1}
  explanation="데이터 거버넌스의 3대 구성 요소는 원칙(Principle), 조직(Organization), 프로세스(Process)입니다. 원칙은 데이터를 어떻게 관리할 것인가에 대한 방침, 조직은 데이터 관리 책임 주체(데이터 스튜어드, CDO 등), 프로세스는 데이터 품질 관리·메타데이터 관리 절차입니다."
  optionExplanations={['정답입니다. 데이터 거버넌스 3요소는 원칙·조직·프로세스입니다.', '②는 데이터 파이프라인 단계이지 거버넌스 구성 요소가 아닙니다.', '③은 데이터 보안 관련 개념이지 거버넌스의 3대 구성 요소가 아닙니다.', '④는 데이터 유형의 분류이지 거버넌스 구성 요소가 아닙니다.']}
/>

---

<Quiz
  questionNumber={3}
  question="CRISP-DM과 KDD 방법론의 차이로 가장 올바른 설명은?"
  options={['① CRISP-DM은 기술적 접근을, KDD는 비즈니스 관점의 접근을 강조한다.', '② CRISP-DM은 비즈니스 이해에서 시작하지만, KDD는 데이터 선택에서 시작한다.', '③ KDD는 6단계이고, CRISP-DM은 5단계이다.', '④ 두 방법론은 단계 수와 시작점이 모두 동일하다.']}
  correctAnswer={2}
  explanation="CRISP-DM은 비즈니스 이해 → 데이터 이해 → 데이터 준비 → 모델링 → 평가 → 배포(6단계, 비즈니스 중심)로 진행됩니다. KDD는 데이터 선택 → 전처리 → 변환 → 데이터 마이닝 → 해석/평가(5단계, 기술 중심)로 진행됩니다. KDD는 데이터 자체에서 출발하고, CRISP-DM은 비즈니스 문제 정의에서 출발합니다."
  optionExplanations={['①은 반대입니다. CRISP-DM이 비즈니스 관점, KDD가 기술 중심입니다.', '정답입니다. CRISP-DM은 비즈니스 이해, KDD는 데이터 선택에서 시작합니다.', '③은 반대입니다. KDD가 5단계, CRISP-DM이 6단계입니다.', '④는 두 방법론은 단계 수(5vs6)와 시작점이 다릅니다.']}
/>

---

<Quiz
  questionNumber={4}
  question="주민등록번호 앞 6자리(생년월일)를 연령대(예: 30대, 40대)로 변환하여 제공하는 비식별화 기법은?"
  options={['① 가명처리(Pseudonymization)', '② 마스킹(Masking)', '③ 범주화(Generalization)', '④ 삭제(Suppression)']}
  correctAnswer={3}
  explanation="구체적인 값(특정 생년월일)을 더 넓은 범주(30대, 40대)로 변환하는 것은 범주화(Generalization) 또는 집계(Aggregation)입니다. 마스킹은 값을 감추는 것(예: 19XX년), 가명처리는 다른 식별자로 대체하는 것입니다."
  optionExplanations={['①은 가명처리가 식별 정보를 다른 식별자(예: ID_A1234)로 대체하는 기법입니다.', '②는 마스킹이 일부 문자를 * 등으로 감추는 기법입니다.', '정답입니다. 구체적 값을 넓은 범주로 변환하는 것이 범주화(Generalization)입니다.', '④는 삭제가 식별 가능 데이터 자체를 완전히 제거하는 기법입니다.']}
/>

---

<Quiz
  questionNumber={5}
  question="KPI(Key Performance Indicator) 설정 시 SMART 원칙에서 'M'이 의미하는 것은?"
  options={['① Meaningful (의미 있는)', '② Measurable (측정 가능한)', '③ Manageable (관리 가능한)', '④ Modern (현대적인)']}
  correctAnswer={2}
  explanation="SMART 원칙: S=Specific(구체적), M=Measurable(측정 가능), A=Achievable(달성 가능), R=Relevant(관련성), T=Time-bound(기한 명확). M은 목표 달성 여부를 수치로 확인할 수 있어야 한다는 의미입니다."
  optionExplanations={['①은 Meaningful은 SMART 원칙에 포함되지 않습니다.', '정답입니다. M은 Measurable(측정 가능)로, 수치로 달성 여부를 확인할 수 있어야 합니다.', '③은 Manageable은 SMART 원칙에 포함되지 않습니다.', '④는 Modern은 SMART 원칙에 포함되지 않습니다.']}
/>

---

## 2과목: 빅데이터 탐색 (문제 6~10)

---

<Quiz
  questionNumber={6}
  question="박스플롯(Box Plot)에서 IQR(사분위 범위)을 이용한 이상치 판단 기준으로 Q1=25, Q3=55일 때, 다음 중 이상치로 판단되는 값은?"
  options={['① 80', '② 95', '③ 10', '④ 45']}
  correctAnswer={2}
  explanation="IQR = Q3 - Q1 = 55 - 25 = 30. 이상치 상한 = Q3 + 1.5 × IQR = 55 + 45 = 100. 이상치 하한 = Q1 - 1.5 × IQR = 25 - 45 = -20. 정상 범위는 -20에서 100이며, 실전에서는 이 범위를 벗어나는 값이 이상치입니다. 선택지 중 상한(100)에 가장 근접한 값은 95입니다."
  optionExplanations={['①은 80이 이상치 상한(100) 이내의 값으로 정상 범위에 포함됩니다.', '정답입니다. IQR 기준 정상 범위는 -20~100이며, 95는 선택지 중 상한(100)에 가장 근접한 값입니다.', '③은 10이 이상치 하한(-20)보다 크므로 정상 범위에 포함됩니다.', '④는 45가 Q1과 Q3 사이의 값으로 정상 범위 내에 있습니다.']}
/>

---

<Quiz
  questionNumber={7}
  question="데이터 [4, 6, 8, 10, 12]의 표본 분산(Sample Variance, n-1 기준)은?"
  options={['① 8', '② 10', '③ 16', '④ 6.4']}
  correctAnswer={2}
  explanation="평균 = (4+6+8+10+12)/5 = 40/5 = 8. 편차 제곱합 = (4-8)²+(6-8)²+(8-8)²+(10-8)²+(12-8)² = 16+4+0+4+16 = 40. 표본 분산(n-1 기준) = 40 / (5-1) = 40/4 = 10. 모분산 기준이면 40/5=8이 됩니다."
  optionExplanations={['①은 모분산(n 기준) = 40/5 = 8입니다. 표본 분산은 n-1을 사용합니다.', '정답입니다. 표본 분산 = 편차 제곱합 / (n-1) = 40/4 = 10입니다.', '③은 편차 제곱합의 최댓값(16)이지 분산이 아닙니다.', '④는 편차 제곱합(40)을 n²(25)이 아닌 다른 수로 나눈 잘못된 계산입니다.']}
/>

---

<Quiz
  questionNumber={8}
  question="이항분포 B(n, p)에서 n과 p가 클수록 분포의 모양이 어떻게 변하는가?"
  options={['① n이 클수록 분포는 정규분포에 가까워진다.', '② p가 0.5에 가까울수록 분포는 더 치우친 형태가 된다.', '③ n이 클수록 분포는 더 좁아진다.', '④ p=1이면 항상 성공하므로 분산은 최대가 된다.']}
  correctAnswer={1}
  explanation="중심극한정리에 의해 n(시행 횟수)이 충분히 크면 이항분포는 정규분포에 가까워집니다. 일반적으로 np≥5이고 n(1-p)≥5를 만족하면 정규분포로 근사 가능합니다."
  optionExplanations={['정답입니다. 중심극한정리에 의해 n이 충분히 크면 이항분포는 정규분포에 수렴합니다.', '②는 p=0.5에 가까울수록 분포는 대칭적(정규분포와 유사) 형태가 됩니다. 치우침이 증가하는 것이 아닙니다.', '③은 n이 클수록 분산은 np(1-p)로 커지나, 표준화된 형태에서는 정규분포로 수렴합니다.', '④는 p=1이면 항상 성공하므로 분산=n×1×0=0이 됩니다.']}
/>

---

<Quiz
  questionNumber={9}
  question="다음 중 독립 표본 t-검정(Independent Samples t-test)을 사용하기에 가장 적합한 상황은?"
  options={['① 동일 그룹의 처치 전·후 혈압 변화를 비교할 때', '② 서로 다른 두 학교 학생들의 평균 성적을 비교할 때', '③ 세 지역의 월 평균 기온을 동시에 비교할 때', '④ 하나의 표본 평균이 특정 기준값과 다른지 검정할 때']}
  correctAnswer={2}
  explanation="독립 표본 t-검정은 서로 다른 두 집단의 평균을 비교할 때 사용합니다. 대응 표본 t-검정(Paired t-test)은 동일 집단의 전·후 비교에, 일원분산분석(ANOVA)은 세 집단 이상의 평균 비교에, 단일 표본 t-검정은 표본 평균과 기준값 비교에 사용합니다."
  optionExplanations={['①은 동일 그룹 전·후 비교이므로 대응 표본 t-검정(Paired t-test)이 적합합니다.', '정답입니다. 서로 다른 두 학교(두 독립 집단)의 평균 비교에는 독립 표본 t-검정이 적합합니다.', '③은 세 집단 이상의 비교이므로 일원분산분석(One-way ANOVA)이 적합합니다.', '④는 하나의 표본 평균과 기준값 비교이므로 단일 표본 t-검정이 적합합니다.']}
/>

---

<Quiz
  questionNumber={10}
  question="산점도(Scatter Plot)를 통해 파악하기 어려운 정보는?"
  options={['① 두 연속형 변수 사이의 선형 관계', '② 이상치 데이터 포인트의 존재', '③ 특정 카테고리의 빈도(Frequency)', '④ 두 변수 간 음의 상관관계']}
  correctAnswer={3}
  explanation="산점도는 두 연속형 변수 사이의 관계, 이상치, 상관관계 방향을 파악하는 데 유용합니다. 그러나 범주형 변수의 빈도를 파악하기에는 적합하지 않습니다. 빈도 파악에는 막대 차트, 도수분포표, 파이 차트가 적합합니다."
  optionExplanations={['①은 산점도의 주요 용도입니다. 점들의 패턴으로 선형 관계를 확인할 수 있습니다.', '②는 산점도에서 군집에서 벗어난 점으로 이상치를 식별할 수 있습니다.', '정답입니다. 범주형 변수의 빈도 파악은 막대 차트나 도수분포표가 적합합니다.', '④는 산점도에서 우하향 패턴으로 음의 상관관계를 확인할 수 있습니다.']}
/>

---

## 3과목: 빅데이터 모델링 (문제 11~15)

---

<Quiz
  questionNumber={11}
  question="의사결정나무에서 클래스 A 60개, 클래스 B 40개로 구성된 노드의 지니 지수(Gini Index)는?"
  options={['① 0.40', '② 0.48', '③ 0.50', '④ 0.60']}
  correctAnswer={2}
  explanation="P(A) = 60/100 = 0.6, P(B) = 40/100 = 0.4. 지니 지수 = 1 - (P(A)² + P(B)²) = 1 - (0.36 + 0.16) = 1 - 0.52 = 0.48. 지니 지수가 0에 가까울수록 순수한 노드(한 클래스만 존재), 0.5에 가까울수록 불순도가 높습니다."
  optionExplanations={['①은 P(B) = 0.4를 그대로 정답으로 혼동한 경우입니다.', '정답입니다. 지니 지수 = 1 - (0.6² + 0.4²) = 1 - 0.52 = 0.48입니다.', '③은 두 클래스가 50:50일 때의 최대 지니 지수(0.5)입니다.', '④는 P(A) = 0.6을 그대로 정답으로 혼동한 경우입니다.']}
/>

---

<Quiz
  questionNumber={12}
  question="랜덤 포레스트(Random Forest)가 일반 의사결정나무보다 과적합에 강한 핵심 이유는?"
  options={['① 트리의 깊이를 더 깊게 만들기 때문이다.', '② 각 트리를 만들 때 무작위로 선택한 특성의 부분집합만 사용하기 때문이다.', '③ 결측치를 자동으로 채워주기 때문이다.', '④ 데이터를 전처리 없이 사용할 수 있기 때문이다.']}
  correctAnswer={2}
  explanation="랜덤 포레스트는 배깅(복원 추출로 서브셋 생성)과 랜덤 특성 선택(각 분기 시 전체 특성 중 무작위로 일부만 사용)의 두 가지 무작위성이 트리 간 상관관계를 낮춰 과적합을 억제합니다. 여러 트리의 결과를 다수결(분류) 또는 평균(회귀)으로 통합합니다."
  optionExplanations={['①은 트리를 깊게 만들수록 오히려 과적합이 심해집니다.', '정답입니다. 무작위 특성 선택과 배깅으로 트리 간 다양성을 확보하여 과적합을 억제합니다.', '③은 랜덤 포레스트가 결측치를 자동으로 채우지는 않습니다.', '④는 전처리 필요 여부는 과적합 방지와 직접 관련이 없습니다.']}
/>

---

<Quiz
  questionNumber={13}
  question="K-means에서 최적의 K(클러스터 수)를 선택하는 방법으로 가장 일반적으로 사용되는 것은?"
  options={['① 실루엣 점수가 가장 낮은 K를 선택한다.', '② 엘보우 방법(Elbow Method)에서 WSS 감소가 급격히 둔화되는 지점의 K를 선택한다.', '③ 항상 K=√(n/2)로 고정한다.', '④ K를 최대한 크게 설정한다.']}
  correctAnswer={2}
  explanation="엘보우 방법(Elbow Method)은 K를 1부터 늘려가며 클러스터 내 오차제곱합(WSS)을 계산하고, WSS 감소가 급격히 둔화되는 꺾이는 지점(팔꿈치 모양)을 최적 K로 선택합니다. 실루엣 점수가 가장 높은 K를 선택하는 방법도 함께 사용합니다."
  optionExplanations={['①은 실루엣 점수가 가장 높은 K를 선택해야 합니다. 낮은 것을 선택하면 잘못된 군집입니다.', '정답입니다. 엘보우 방법에서 WSS 감소가 둔화되는 꺾이는 지점의 K가 최적입니다.', '③은 K를 공식으로 고정하면 데이터의 실제 군집 구조를 무시하게 됩니다.', '④는 K가 최대이면 각 포인트가 자기 군집이 되어 의미가 없습니다.']}
/>

---

<Quiz
  questionNumber={14}
  question="Ridge 회귀와 Lasso 회귀의 차이로 옳은 것은?"
  options={['① Ridge는 일부 계수를 정확히 0으로 만들어 변수 선택 효과가 있다.', '② Lasso는 L2 정규화를 사용하여 모든 계수를 골고루 줄인다.', '③ Lasso는 L1 정규화를 사용하여 일부 계수를 정확히 0으로 만드는 변수 선택 효과가 있다.', '④ Ridge와 Lasso 모두 과적합을 더 심하게 만들 수 있다.']}
  correctAnswer={3}
  explanation="Ridge(L2 정규화)는 계수 제곱합을 패널티로 사용하여 모든 계수를 작게 만들지만 0이 되지는 않습니다. 다중공선성에 강합니다. Lasso(L1 정규화)는 계수 절댓값합을 패널티로 사용하여 일부 계수를 정확히 0으로 만드는 변수 선택 효과가 있으며, 희소 모델 구축에 유리합니다."
  optionExplanations={['①은 계수를 0으로 만드는 것은 Ridge가 아니라 Lasso의 특성입니다.', '②는 L2 정규화는 Ridge이고, Lasso는 L1 정규화를 사용합니다.', '정답입니다. Lasso는 L1 정규화로 일부 계수를 정확히 0으로 만들어 변수 선택 효과를 냅니다.', '④는 Ridge와 Lasso는 모두 정규화를 통해 과적합을 방지하는 기법입니다.']}
/>

---

<Quiz
  questionNumber={15}
  question="다음 중 재현율(Recall, 민감도)을 정밀도(Precision)보다 더 중요하게 고려해야 하는 상황은?"
  options={['① 스팸 메일 필터 (정상 메일이 스팸으로 분류되는 불편함 최소화)', '② 암 진단 시스템 (암 환자를 놓치지 않는 것이 중요)', '③ 명품 진위 감정 시스템 (가짜를 진짜로 통과시키지 않는 것이 중요)', '④ 신제품 추천 시스템 (관심 없는 상품 추천 최소화)']}
  correctAnswer={2}
  explanation="재현율 중시: 실제 양성을 놓치는 비용(FN)이 매우 클 때 사용합니다. 암 진단, 보안 위협 탐지, 불량품 검사 등이 해당합니다. 정밀도 중시: 거짓 경보(FP)의 비용이 클 때 사용합니다. 스팸 필터, 명품 진위, 추천 시스템 등이 해당합니다."
  optionExplanations={['①은 정상 메일이 스팸으로 오분류되는 FP 비용이 크므로 정밀도를 중시합니다.', '정답입니다. 암 환자를 정상으로 잘못 분류하면(FN) 치료 기회를 잃으므로 재현율이 더 중요합니다.', '③은 가짜를 진짜로 통과시키지 않는 것이 중요하므로 정밀도를 중시합니다.', '④는 관심 없는 상품 추천(FP)을 줄여야 하므로 정밀도를 중시합니다.']}
/>

---

## 4과목: 빅데이터 결과 해석 (문제 16~20)

---

<Quiz
  questionNumber={16}
  question="경영진을 위한 분석 보고서(Executive Summary)의 특징으로 가장 적절한 것은?"
  options={['① 모델 파라미터와 알고리즘 상세 설명을 포함한다.', '② 1~2페이지 내외로, 핵심 결과와 권고사항 중심으로 작성한다.', '③ 코드와 데이터 파이프라인을 모두 포함한다.', '④ 모든 실험 결과와 실패 사례를 상세히 기술한다.']}
  correctAnswer={2}
  explanation="경영진 보고서(Executive Summary)의 특징: 분량 1~2페이지, 전문 용어 최소화, 핵심 결과·비즈니스 영향·권고사항 중심, 숫자는 최소화하고 비유와 현실적 의미 위주로 작성합니다. ①③④는 기술 문서나 상세 보고서에 해당합니다."
  optionExplanations={['①은 알고리즘 상세 설명은 기술 문서에 포함하는 내용입니다.', '정답입니다. 경영진 보고서는 1~2페이지로 핵심 결과와 권고사항을 간결하게 전달합니다.', '③은 코드와 파이프라인은 기술 문서에 포함하는 내용입니다.', '④는 상세 실험 결과는 상세 보고서나 부록에 넣습니다.']}
/>

---

<Quiz
  questionNumber={17}
  question="배포된 모델을 교체하기 전, 새 모델이 실제로 더 나은 성능을 보이는지 확인하기 위해 사용하는 방법은?"
  options={['① 교차검증(Cross-validation)', '② A/B 테스트(A/B Testing)', '③ 데이터 증강(Data Augmentation)', '④ 하이퍼파라미터 튜닝(Hyperparameter Tuning)']}
  correctAnswer={2}
  explanation="A/B 테스트는 실제 서비스 환경에서 사용자를 두 그룹(A: 기존 모델, B: 새 모델)으로 무작위 분리하고, 동시에 두 모델의 성과를 비교하는 방법입니다. 통계적 유의성 검정을 통해 새 모델이 우연이 아닌 실질적 개선을 가져오는지 확인합니다."
  optionExplanations={['①은 교차검증은 모델 개발 단계에서 성능을 추정하는 방법이지, 실서비스 배포 전 비교 방법이 아닙니다.', '정답입니다. A/B 테스트는 실제 서비스 환경에서 기존 모델과 새 모델을 동시에 비교합니다.', '③은 데이터 증강은 학습 데이터를 늘리는 기법입니다.', '④는 하이퍼파라미터 튜닝은 모델 개발 단계의 최적화 작업입니다.']}
/>

---

<Quiz
  questionNumber={18}
  question="배포된 이탈 예측 모델에서 최근 예측 정확도가 급격히 낮아졌다. 점검 결과 신규 가입자의 인구통계 특성과 구매 패턴이 학습 데이터와 크게 달라져 있음을 발견했다. 이 현상으로 가장 적절한 용어는?"
  options={['① 과적합(Overfitting)', '② 모델 붕괴(Model Collapse)', '③ 데이터 드리프트(Data Drift)', '④ 다중공선성(Multicollinearity)']}
  correctAnswer={3}
  explanation="입력 데이터의 분포(인구통계, 구매 패턴)가 학습 당시와 달라지는 현상을 데이터 드리프트(Data Drift)라고 합니다. 이 경우 새로운 데이터로 모델을 재학습(Retraining)해야 합니다."
  optionExplanations={['①은 과적합은 훈련-검증 성능 격차 문제이지, 배포 후 입력 분포 변화와 다릅니다.', '②는 모델 붕괴는 주로 생성 모델에서 발생하는 현상입니다.', '정답입니다. 입력 데이터 분포가 시간이 지나 변화하는 것이 데이터 드리프트입니다.', '④는 다중공선성은 변수 간 높은 상관관계 문제로, 배포 후 성능 저하와 다릅니다.']}
/>

---

<Quiz
  questionNumber={19}
  question="다음 중 시각화에서 3D 파이 차트가 문제가 되는 이유로 가장 적절한 것은?"
  options={['① 색상이 너무 적어 구분이 어렵다.', '② 원근법으로 인해 앞쪽 조각이 실제보다 더 크게 보여 비율을 왜곡한다.', '③ 데이터 포인트가 너무 많아 읽기 어렵다.', '④ 추세를 파악하기 어렵다.']}
  correctAnswer={2}
  explanation="3D 파이 차트는 원근법(Perspective)으로 인해 앞쪽에 있는 조각이 실제 비율보다 크게 보이는 시각적 왜곡이 발생합니다. 이는 데이터를 정확하게 전달하지 못하는 대표적인 잘못된 시각화 방법입니다. 2D 파이 차트 또는 막대 차트로 대체하는 것이 권장됩니다."
  optionExplanations={['①은 색상 문제가 아니라 원근법에 의한 왜곡이 3D 파이 차트의 핵심 문제입니다.', '정답입니다. 원근법으로 앞쪽 조각이 더 크게 보여 비율을 왜곡합니다.', '③은 데이터 포인트 수는 3D 여부와 무관한 문제입니다.', '④는 추세 파악은 라인 차트의 용도이며, 파이 차트는 비율 비교에 사용합니다.']}
/>

---

<Quiz
  questionNumber={20}
  question="분석 모델을 온라인 쇼핑몰에 적용할 때, 각 사용자가 상품 상세 페이지를 방문하는 순간 해당 사용자에게 맞춤형 상품을 즉시 추천하는 방식으로 가장 적합한 배포 방법은?"
  options={['① 배치 예측 (Batch Prediction)', '② 오프라인 분석 (Offline Analysis)', '③ REST API 기반 실시간 예측', '④ 임베디드 모델 (Embedded Model)']}
  correctAnswer={3}
  explanation="사용자가 특정 페이지를 방문하는 즉시 해당 시점의 정보(현재 보고 있는 상품, 세션 히스토리 등)를 반영하여 추천하려면 REST API 기반 실시간 예측 방식이 가장 적합합니다. API 호출 → 모델 예측 → 결과 반환이 수십 ms 이내에 이루어집니다."
  optionExplanations={['①은 배치 예측은 대량 데이터를 일정 주기로 처리하는 방식으로, 즉각 응답이 필요한 경우에는 적합하지 않습니다.', '②는 오프라인 분석은 실시간 서비스 환경에 직접 적용할 수 없습니다.', '정답입니다. REST API 기반 실시간 예측은 사용자 요청 즉시 응답하는 방식입니다.', '④는 임베디드 모델은 앱 내장 방식으로, 서버 요청 없이 기기에서 처리할 때 사용합니다.']}
/>

---

## 오답 유형 분석

| 유형 | 빠지기 쉬운 함정 |
|------|-----------------|
| 계산 문제 (Q7, Q11) | 모분산(n)과 표본분산(n-1) 혼동, 지니 지수 공식 실수 |
| 용어 문제 (Q2, Q5) | 거버넌스 구성요소와 SMART 약어 암기 |
| 선택 문제 (Q9, Q15) | t-검정 종류(독립/대응/단일)를 상황에 맞게 구분 |
| 드리프트 구분 (Q18) | 데이터 드리프트(입력 분포) vs 컨셉 드리프트(관계) 혼동 |
| 배포 방식 (Q20) | 실시간=API, 대량 사전계산=배치 구분 |

## 마무리 복습

<Quiz questionNumber={1} question="이항분포에서 n이 충분히 클 때 어떤 분포로 근사할 수 있는가?" options={["포아송 분포", "균등 분포", "정규 분포", "지수 분포"]} correctAnswer={3} explanation="중심극한정리에 의해 n이 충분히 크면(일반적으로 np≥5, n(1-p)≥5) 이항분포는 정규분포로 근사됩니다. 이를 통해 이항분포 계산을 정규분포 표를 이용해 간편하게 수행할 수 있습니다." optionExplanations={["포아송 분포는 n이 매우 크고 p가 매우 작을 때(np=λ 일정) 이항분포의 근사로 사용됩니다.", "균등 분포는 모든 값이 동일한 확률을 가지는 분포로, 이항분포와 무관합니다.", "정답입니다. 중심극한정리에 의해 n이 충분히 크면 이항분포는 정규분포에 수렴합니다.", "지수 분포는 대기 시간 등에 사용되는 연속 확률 분포입니다."]} />

<Quiz questionNumber={2} question="랜덤 포레스트에서 최종 예측값(분류 문제)을 결정하는 방법은?" options={["가장 처음 만들어진 트리의 예측값을 사용한다.", "모든 트리의 예측값 중 다수결로 결정한다.", "가장 정확도가 높은 트리 하나의 예측값을 사용한다.", "모든 트리 예측값의 평균을 반올림한다."]} correctAnswer={2} explanation="랜덤 포레스트는 앙상블 학습의 일종으로, 여러 개의 의사결정나무를 만들고 분류 문제에서는 다수결(Majority Voting), 회귀 문제에서는 평균(Averaging)으로 최종 예측값을 결정합니다." optionExplanations={["특정 트리 하나만 사용하는 것은 단일 의사결정나무와 다를 바가 없습니다.", "정답입니다. 분류에서는 다수결(Voting)로 최종 클래스를 결정합니다.", "성능이 좋은 트리 하나를 선택하는 것은 모델 선택이지 앙상블이 아닙니다.", "분류 문제에서는 평균을 반올림하지 않고 다수결을 사용합니다. 회귀에서는 평균을 사용합니다."]} />

<Quiz questionNumber={3} question="A/B 테스트를 수행할 때 반드시 지켜야 할 원칙으로 틀린 것은?" options={["두 그룹에 사용자를 무작위로 배정한다.", "초기에 새 모델이 좋아 보이면 즉시 실험을 중단하고 적용한다.", "두 그룹은 동시에 같은 기간에 실험한다.", "통계적 유의성을 검정하여 우연에 의한 차이인지 확인한다."]} correctAnswer={2} explanation="A/B 테스트의 조기 종료(Early Stopping)는 통계적으로 신뢰할 수 없는 결과를 낳습니다. 초기에 우연히 좋아 보이는 결과가 나왔더라도, 충분한 표본 크기와 기간을 확보한 후에 결론을 내려야 합니다. 미리 설정한 기간과 표본 크기를 지키는 것이 원칙입니다." optionExplanations={["무작위 배정은 선택 편향을 방지하는 A/B 테스트의 핵심 원칙입니다.", "정답(틀린 것)입니다. 초기 결과만 보고 조기 종료하면 통계적 오류(1종 오류 증가)가 발생합니다.", "동시에 같은 기간에 실험하는 것은 시간적 혼입 변수를 방지합니다.", "통계적 유의성 검정은 결과가 우연인지 확인하는 필수 단계입니다."]} />

## 참고 자료

- [KDATA 데이터자격시험 - 빅데이터분석기사](https://www.dataq.or.kr/www/sub/a_07.do)

---
title: '28. 모델링·결과 해석 기출 유형'
description: '3과목(빅데이터 모델링)과 4과목(빅데이터 결과 해석) 핵심 유형 문제와 해설을 정리합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 혼동행렬 계산, K-means 반복, 연관규칙 수치, ROC-AUC 해석 등 계산형 문제를 직접 손으로 풀어보고, 각 알고리즘의 개념 문제도 함께 익힙니다.
</Callout>

## 유형별 문제 풀이

### 유형 1: 알고리즘 선택

상황을 보고 지도학습 / 비지도학습 / 강화학습을 구분하고, 적절한 알고리즘을 선택합니다.

---

<Quiz
  questionNumber={1}
  question="전자상거래 기업에서 고객 구매 이력 데이터가 있으나 '우수 고객', '일반 고객' 등의 레이블(정답)이 없다. 고객을 구매 패턴이 비슷한 그룹으로 나누고 싶을 때 가장 적절한 분석 방법은?"
  options={['① 로지스틱 회귀 (Logistic Regression)', '② 의사결정나무 (Decision Tree)', '③ K-평균 군집분석 (K-means Clustering)', '④ 랜덤 포레스트 (Random Forest)']}
  correctAnswer={3}
  explanation="정답 레이블이 없는 상태에서 유사한 개체끼리 묶는 작업은 비지도학습의 군집분석입니다. K-means는 대표적인 군집분석 알고리즘입니다. ①②④는 모두 정답 레이블이 필요한 지도학습 알고리즘입니다."
  optionExplanations={['①은 분류 레이블이 있어야 하는 지도학습 알고리즘입니다.', '②는 분류·회귀에 사용하는 지도학습 알고리즘입니다.', '정답입니다. K-means는 레이블 없이 유사한 개체를 군집으로 묶는 비지도학습 알고리즘입니다.', '④도 분류·회귀에 사용하는 지도학습 앙상블 알고리즘입니다.']}
/>

---

### 유형 2: 혼동행렬 계산

혼동행렬(Confusion Matrix)이 주어지면 정확도·정밀도·재현율·F1-Score를 직접 계산합니다.

**혼동행렬 공식 정리:**

| 지표 | 공식 |
|------|------|
| 정확도(Accuracy) | (TP + TN) / (TP + FN + FP + TN) |
| 정밀도(Precision) | TP / (TP + FP) |
| 재현율(Recall) | TP / (TP + FN) |
| F1-Score | 2 × (Precision × Recall) / (Precision + Recall) |

---

<Quiz
  questionNumber={2}
  question="이탈 고객 예측 모델의 혼동행렬이 다음과 같을 때(TP=70, FN=30, FP=15, TN=85), 정밀도(Precision)와 재현율(Recall)을 올바르게 계산한 것은?"
  options={['① 정밀도 = 0.70, 재현율 = 0.70', '② 정밀도 = 0.82, 재현율 = 0.70', '③ 정밀도 = 0.70, 재현율 = 0.82', '④ 정밀도 = 0.88, 재현율 = 0.85']}
  correctAnswer={2}
  explanation="정밀도 = TP / (TP + FP) = 70 / (70 + 15) = 70 / 85 ≈ 0.82. 재현율 = TP / (TP + FN) = 70 / (70 + 30) = 70 / 100 = 0.70. 참고: 정확도 = (70 + 85) / 200 = 0.775."
  optionExplanations={['①은 정밀도를 TP/(TP+FN)으로 잘못 계산한 경우입니다.', '정답입니다. 정밀도 = 70/85 ≈ 0.82, 재현율 = 70/100 = 0.70입니다.', '③은 정밀도와 재현율을 서로 바꿔 계산한 경우입니다.', '④는 전혀 다른 수치입니다. 정밀도는 TP/(TP+FP), 재현율은 TP/(TP+FN)으로 계산해야 합니다.']}
/>

---

### 유형 3: K-means 1회 반복 계산

K-means는 거리를 계산하여 클러스터를 재배정하고, 새로운 중심을 계산합니다.

---

<Quiz
  questionNumber={3}
  question="다음 조건으로 K-means 1회 반복 후 새로운 클러스터 중심을 구하시오. 데이터 포인트: A(1, 2), B(3, 4), C(5, 1), D(7, 3). K=2, 초기 중심: C1=(1, 2), C2=(7, 3). 1회 반복 후 새로운 중심으로 올바른 것은?"
  options={['① C1=(2, 3), C2=(6, 2)', '② C1=(3, 3), C2=(6, 2)', '③ C1=(3, 7/3), C2=(6, 2)', '④ C1=(2, 7/3), C2=(6, 2)']}
  correctAnswer={1}
  explanation="A(1,2)는 C1까지 거리 0, C2까지 √37 → C1 배정. B(3,4)는 C1까지 √8, C2까지 √17 → C1 배정. C(5,1)는 C1까지 √17, C2까지 √8 → C2 배정. D(7,3)는 C2까지 거리 0 → C2 배정. 새 C1 = A,B 평균 = ((1+3)/2, (2+4)/2) = (2, 3). 새 C2 = C,D 평균 = ((5+7)/2, (1+3)/2) = (6, 2)."
  optionExplanations={['정답입니다. C1에 A,B가 배정되어 새 중심 (2,3), C2에 C,D가 배정되어 새 중심 (6,2)입니다.', '②는 C1의 x좌표를 잘못 계산한 경우입니다.', '③은 C1의 y좌표를 잘못 계산한 경우입니다.', '④는 C1의 x,y 좌표 모두 잘못 계산한 경우입니다.']}
/>

---

### 유형 4: 연관규칙 계산

지지도, 신뢰도, 향상도를 주어진 수치로 직접 계산합니다.

| 지표 | 공식 | 의미 |
|------|------|------|
| 지지도(Support) | P(A ∩ B) = &#123;A,B&#125; 동시 구매 / 전체 거래 수 | 얼마나 자주 같이 나오는가 |
| 신뢰도(Confidence) | P(B\|A) = &#123;A,B&#125; 동시 구매 / A 구매 | A를 사면 B도 살 확률 |
| 향상도(Lift) | P(B\|A) / P(B) = 신뢰도 / P(B) | 우연 대비 얼마나 강한 연관인가 |

---

<Quiz
  questionNumber={4}
  question="10건의 거래 중 상품 A와 B를 함께 구매한 거래는 4건, 상품 A만 구매한 거래(B 포함)는 6건, 상품 B만 구매한 거래(A 포함)는 5건이다. 규칙 'A → B'의 신뢰도와 향상도를 올바르게 계산한 것은?"
  options={['① 신뢰도 = 0.4, 향상도 = 0.8', '② 신뢰도 = 0.67, 향상도 = 1.33', '③ 신뢰도 = 0.8, 향상도 = 1.6', '④ 신뢰도 = 0.5, 향상도 = 1.0']}
  correctAnswer={2}
  explanation="지지도 = 4/10 = 0.4. 신뢰도 = P(B|A) = {A,B}/A = 4/6 ≈ 0.67. P(B) = 5/10 = 0.5. 향상도 = 신뢰도 / P(B) = 0.67 / 0.5 = 1.33. 향상도가 1보다 크면 A와 B 사이에 양의 연관관계가 있음을 의미합니다."
  optionExplanations={['①은 신뢰도를 지지도(4/10=0.4)로 계산한 경우입니다.', '정답입니다. 신뢰도 = 4/6 ≈ 0.67, 향상도 = 0.67/0.5 = 1.33입니다.', '③은 신뢰도를 B 거래 수 기준(4/5=0.8)으로 잘못 계산한 경우입니다.', '④는 신뢰도를 전체 거래 기준(5/10=0.5)으로 잘못 계산한 경우입니다.']}
/>

---

### 유형 5: 과적합 판단

---

<Quiz
  questionNumber={5}
  question="모델 훈련 결과가 다음과 같다. 이 상황에 대한 판단과 해결책으로 올바른 것은? (훈련 데이터 정확도: 0.96, 검증 데이터 정확도: 0.61)"
  options={['① 과소적합(Underfitting)이며, 모델의 복잡도를 높여야 한다.', '② 과적합(Overfitting)이며, 훈련 데이터를 더 추가하거나 정규화를 적용해야 한다.', '③ 정상 범위이며, 그대로 배포해도 무방하다.', '④ 검증 데이터가 너무 적어 신뢰할 수 없는 결과이다.']}
  correctAnswer={2}
  explanation="훈련 정확도(0.96)와 검증 정확도(0.61)의 차이가 0.35로 매우 크면 과적합(Overfitting) 상태입니다. 해결책으로 훈련 데이터 추가, 정규화(L1/L2) 적용, 모델 복잡도 감소(트리 깊이 제한), 드롭아웃, 교차검증 활용 등을 사용합니다."
  optionExplanations={['①은 과소적합은 훈련·검증 정확도가 모두 낮을 때 해당합니다. 이 경우 훈련 정확도는 0.96으로 높습니다.', '정답입니다. 훈련↑ 검증↓ 격차가 크면 과적합이며, 정규화·데이터 추가·복잡도 감소로 해결합니다.', '③은 훈련과 검증 정확도 차이가 0.35나 되므로 정상이 아닙니다.', '④는 검증 데이터 크기와 무관하게, 훈련-검증 격차가 과적합의 징후입니다.']}
/>

---

### 유형 6: ROC-AUC 해석

---

<Quiz
  questionNumber={6}
  question="ROC 곡선의 AUC(Area Under the Curve) 값에 대한 설명으로 가장 올바른 것은?"
  options={['① AUC = 0.5이면 완벽한 분류기이다.', '② AUC = 1.0이면 무작위 추측 수준이다.', '③ AUC = 0.9이면 학습 데이터에서만 좋은 성능이다.', '④ AUC = 0.5이면 무작위 추측과 동일한 수준이다.']}
  correctAnswer={4}
  explanation="AUC 값의 해석: AUC = 0.5이면 무작위 추측과 동일(분류 능력 없음), AUC = 0.7~0.8이면 허용 가능한 수준, AUC = 0.8~0.9이면 우수한 수준, AUC = 0.9~1.0이면 매우 우수, AUC = 1.0이면 완벽한 분류(실제로는 과적합 의심). AUC는 임계값에 무관하게 모델의 전반적 분류 성능을 하나의 수치로 나타냅니다."
  optionExplanations={['①은 AUC = 0.5는 완벽한 분류기가 아니라 무작위 추측 수준입니다.', '②는 AUC = 1.0은 완벽한 분류기(실제로는 과적합 의심)입니다. 무작위 추측은 0.5입니다.', '③은 AUC = 0.9는 우수한 수준으로, 학습 데이터에서만 좋다는 의미가 아닙니다(AUC는 검증 세트 기준).', '정답입니다. AUC = 0.5는 모든 임계값에서 무작위 추측과 동일한 성능입니다.']}
/>

---

### 유형 7: PCA(주성분 분석) 목적

---

<Quiz
  questionNumber={7}
  question="500개의 특성(변수)을 가진 유전자 데이터로 질병을 예측하는 모델을 만들려 한다. 학습이 너무 느리고 성능도 낮다. 이를 해결하기 위해 PCA를 적용하려 한다. PCA의 주목적으로 가장 적절한 것은?"
  options={['① 데이터의 결측치를 채운다.', '② 범주형 변수를 수치형으로 변환한다.', '③ 분산을 최대한 보존하면서 변수 수를 줄인다.', '④ 데이터를 정규 분포로 변환한다.']}
  correctAnswer={3}
  explanation="PCA(Principal Component Analysis)는 차원 축소 기법으로, 원래 변수들의 선형 조합인 주성분을 만들어 데이터의 분산(정보)을 최대한 보존하면서 변수 수(차원)를 줄입니다. 학습 속도 향상, 다중공선성 제거, 시각화(2~3차원 표현) 등에 활용됩니다. 주성분 수 선택 기준: 누적 설명 분산이 80~95% 이상이 되는 지점까지의 주성분 수를 선택합니다."
  optionExplanations={['①은 결측치 처리는 PCA와 별개의 전처리 단계입니다.', '②는 범주형→수치형 변환은 원-핫 인코딩 등의 인코딩 기법입니다.', '정답입니다. PCA는 분산을 최대 보존하면서 고차원 데이터를 저차원으로 축소하는 차원 축소 기법입니다.', '④는 정규화(Normalization)나 표준화(Standardization)가 해당 목적에 사용됩니다.']}
/>

---

### 유형 8: 모델 모니터링 용어

---

<Quiz
  questionNumber={8}
  question="온라인 쇼핑몰의 고객 이탈 예측 모델이 배포된 지 6개월이 지난 후, 입력으로 사용되는 고객 연령대 분포와 구매 카테고리 비중이 학습 당시와 크게 달라진 것이 탐지되었다. 이 현상을 정확하게 설명하는 용어는?"
  options={['① 컨셉 드리프트(Concept Drift)', '② 데이터 드리프트(Data Drift)', '③ 과적합(Overfitting)', '④ 샘플링 편향(Sampling Bias)']}
  correctAnswer={2}
  explanation="데이터 드리프트(Data Drift)는 모델의 입력 데이터 분포가 시간이 지남에 따라 변화하는 현상입니다. 반면 컨셉 드리프트(Concept Drift)는 입력과 출력 사이의 관계(패턴) 자체가 변하는 현상입니다. 이 문제에서는 입력 데이터의 분포(연령대, 카테고리 비중)가 변했으므로 데이터 드리프트입니다."
  optionExplanations={['①은 컨셉 드리프트는 입력-출력 관계 자체가 변하는 현상입니다. 여기서는 입력 분포가 변했습니다.', '정답입니다. 입력 데이터(연령대, 카테고리 비중)의 분포 변화는 데이터 드리프트입니다.', '③은 과적합은 훈련-검증 성능 격차 문제이며, 배포 후 데이터 분포 변화와 다릅니다.', '④는 샘플링 편향은 데이터 수집 단계의 문제로, 배포 후 분포 변화와 다릅니다.']}
/>

---

## 핵심 정리

- 레이블 없는 군집 문제 → K-means (비지도학습)
- 혼동행렬: 정밀도=TP/(TP+FP), 재현율=TP/(TP+FN)
- K-means: 거리 계산 → 클러스터 배정 → 중심 재계산
- 연관규칙: 지지도=공동/전체, 신뢰도=공동/A, 향상도=신뢰도/P(B)
- 훈련↑ 검증↓ 격차 크면 → 과적합
- AUC 0.5=무작위, 1.0=완벽 (실제론 과적합 의심)
- PCA: 분산 보존 + 차원 축소
- 데이터 드리프트(입력 분포 변화) vs 컨셉 드리프트(관계 변화)

## 마무리 복습

<Quiz questionNumber={1} question="혼동행렬에서 TP=80, FN=20, FP=40, TN=60일 때 재현율(Recall)은?" options={["0.67", "0.75", "0.80", "0.60"]} correctAnswer={3} explanation="재현율(Recall) = TP / (TP + FN) = 80 / (80 + 20) = 80 / 100 = 0.80입니다. 재현율은 실제 양성(Positive) 중에서 모델이 올바르게 양성으로 예측한 비율을 의미합니다." optionExplanations={["0.67은 정밀도 TP/(TP+FP) = 80/120의 값입니다.", "0.75는 정확도 (TP+TN)/(전체) = 140/200의 값입니다.", "정답입니다. TP/(TP+FN) = 80/100 = 0.80이 재현율입니다.", "0.60은 TN/(TN+FP) = 60/100으로, 특이도(Specificity)에 해당합니다."]} />

<Quiz questionNumber={2} question="연관규칙 분석에서 향상도(Lift)가 1보다 크다는 것이 의미하는 바는?" options={["A를 구매한 사람이 B를 구매할 확률이 전체 거래에서 B를 구매할 확률보다 높다.", "지지도가 매우 높아 자주 함께 구매된다는 의미이다.", "신뢰도가 100%라는 의미이다.", "A와 B는 음의 상관관계를 가진다."]} correctAnswer={1} explanation="향상도 = 신뢰도 / P(B) = P(B|A) / P(B)입니다. 향상도가 1보다 크다는 것은 A를 구매했을 때 B를 구매할 확률이, A와 무관하게 B를 구매할 확률보다 높다는 의미입니다. 즉, A와 B 사이에 양의 연관관계가 존재합니다." optionExplanations={["정답입니다. 향상도 > 1은 A 구매가 B 구매 가능성을 높이는 양의 연관을 의미합니다.", "지지도는 공동 출현 빈도를 나타내며, 향상도와 독립적입니다.", "향상도는 신뢰도와 P(B)의 비율이지, 신뢰도 자체가 100%임을 의미하지 않습니다.", "향상도 < 1이 음의 연관관계입니다."]} />

<Quiz questionNumber={3} question="PCA(주성분 분석)의 주성분 개수를 결정하는 방법으로 가장 일반적으로 사용되는 기준은?" options={["주성분 개수를 원래 변수 수의 절반으로 고정한다.", "누적 설명 분산이 80~95% 이상이 되는 지점까지의 주성분 수를 선택한다.", "고유값(Eigenvalue)이 5 이상인 주성분만 선택한다.", "항상 2개의 주성분만 사용한다."]} correctAnswer={2} explanation="PCA에서 주성분 수 선택의 대표적 기준은 '누적 설명 분산 비율'입니다. 보통 80~95%의 분산을 설명하는 주성분 수까지 선택합니다. 스크리 플롯(Scree Plot)에서 분산 기여도가 급격히 감소하는 엘보우 지점을 기준으로 삼기도 합니다." optionExplanations={["고정 비율로 선택하면 데이터의 실제 분산 구조를 무시하게 됩니다.", "정답입니다. 누적 설명 분산 80~95% 기준 또는 스크리 플롯 엘보우 지점이 일반적입니다.", "고유값 1 이상(Kaiser 기준)이 일반적이며, 5 이상은 지나치게 엄격합니다.", "2개 고정은 2D 시각화 목적에만 한정되며, 일반적 기준이 아닙니다."]} />

## 참고 자료

- [KDATA 데이터자격시험 - 빅데이터분석기사](https://www.dataq.or.kr/www/sub/a_07.do)

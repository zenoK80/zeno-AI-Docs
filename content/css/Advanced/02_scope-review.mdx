---
title: '02-복습. @scope로 스타일 범위 제한'
description: '@scope의 범위 지정 방식을 4지선다 퀴즈로 점검하고, to로 하한선을 지정하는 연습을 한다.'
---

## 02편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"`@scope (.card) to (.card-footer)`에서, `.card-footer` 요소 자신은 스타일 범위에 포함되나?"}
  options={[
    "포함된다",
    "포함되지 않는다 — \"여기서부터는 제외\"라는 경계선일 뿐이다",
    "브라우저마다 다르게 처리한다",
    "`.card-footer`의 자식 요소만 포함되고 자신은 제외된다",
  ]}
  correctAnswer={2}
  explanation={"`to (.card-footer)`는 \"이 요소를 만나는 순간부터는 범위 밖\"이라는 하한선을 정하는 것이다. `.card-footer` 자기 자신과 그 하위 요소 모두 범위에서 제외된다."}
  optionExplanations={[
    "`to`로 지정한 요소는 범위의 경계선이라 자기 자신도 스타일 범위에서 제외된다.",
    "정답이다. `.card-footer` 자신과 그 하위 요소 모두 스타일 범위 밖이다.",
    "`@scope`의 하한선 동작은 명세로 정해진 표준 동작이라 브라우저마다 다르게 처리하지 않는다.",
    "자식 요소만이 아니라 `.card-footer` 자기 자신도 함께 범위에서 제외된다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"`@scope (.card) { ... }` 블록 안에서 `:scope`는 무엇을 가리키나?"}
  options={[
    "문서 전체(`<body>`)",
    "범위를 연 그 요소 자신, 즉 `.card`",
    "`.card`의 모든 자식 요소",
    "브라우저 창 전체",
  ]}
  correctAnswer={2}
  explanation={"`:scope`는 `@scope`로 지정한 범위의 시작점이 되는 그 요소 자신을 가리키는 특수 선택자다."}
  optionExplanations={[
    "`:scope`는 문서 전체가 아니라 `@scope`로 지정한 범위의 시작 요소 하나만 가리킨다.",
    "정답이다. `:scope`는 `@scope (.card)`에서 범위를 연 `.card` 요소 자신을 가리킨다.",
    "자식 요소 전체가 아니라 범위를 연 그 요소 하나만을 정확히 가리키는 선택자다.",
    "CSS 선택자는 브라우저 창 자체를 가리킬 수 없다. `:scope`는 DOM 요소를 가리킨다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"같은 명시도를 가진 두 `@scope` 규칙이 충돌하면 무엇이 이기나?"}
  options={[
    "나중에 작성된 규칙이 항상 이긴다",
    "범위가 더 좁게(더 가깝게) 선언된 규칙이 이긴다",
    "알파벳 순서가 빠른 선택자가 이긴다",
    "무작위로 결정된다",
  ]}
  correctAnswer={2}
  explanation={"`@scope`는 명시도와는 별개로 범위의 근접도라는 새로운 우선순위 기준을 추가한다. 같은 명시도라면, 스타일을 적용하려는 요소와 더 가까운(더 좁은) 범위에서 선언된 규칙이 이긴다."}
  optionExplanations={[
    "작성 순서가 아니라 범위의 근접도가 `@scope` 규칙끼리의 충돌을 결정하는 새로운 기준이다.",
    "정답이다. `@scope`는 범위의 근접도를 기준으로 삼아, 더 좁고 가까운 범위가 이긴다.",
    "CSS 명시도·충돌 규칙에 알파벳 순서라는 기준은 존재하지 않는다.",
    "`@scope`의 우선순위는 명세로 정해진 규칙에 따라 결정되며 무작위가 아니다.",
  ]}
/>

## 실습 — 범위에 하한선 추가하기

아래는 `.widget` 안의 모든 `p`에 회색을 주는 규칙이다. 그런데 `.widget` 안에 중첩된 `.nested-card`의 `p`에는 영향을 주면 안 된다고 하자. `to`를 추가해서 고쳐보자.

```css
@scope (.widget) {
  p { color: gray; }
}
```

```html
<div class="widget">
  <p>회색이어야 함</p>
  <div class="nested-card">
    <p>회색이면 안 됨</p>
  </div>
</div>
```

<Collapse title="정답 보기">

```css
@scope (.widget) to (.nested-card) {
  p { color: gray; }
}
```

</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] `@scope (시작) to (끝)` 문법을 쓸 수 있다
- [ ] `:scope`의 의미를 안다
- [ ] 언제 `@scope`를 쓰는 게 유리한지 설명할 수 있다
</Callout>

다음 편에서는 CSS 안에서 직접 계산하는 수학 함수들을 본다.

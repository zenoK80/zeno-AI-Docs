---
title: '16. 데이터베이스 예상기출문제'
description: '데이터베이스 전 범위 예상기출문제 20개 이상으로 최종 정리한다.'
---

<Callout type="info">
  **이번 문서의 목표:** 데이터베이스 전 범위(개요, 모델링, 관계 대수, SQL, 정규화, 트랜잭션, 회복, 인덱스, 쿼리 최적화, 분산 DB, NoSQL, DW, 보안)의 핵심 개념을 예상기출문제로 최종 점검한다.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="데이터베이스 시스템에서 스키마(Schema)의 의미는?"
  options={['① 데이터베이스에 저장된 실제 데이터', '② 데이터베이스의 구조와 제약 조건을 정의한 명세', '③ 사용자가 실행하는 SQL 쿼리', '④ 데이터베이스 관리 소프트웨어']}
  correctAnswer={2}
  explanation="스키마는 데이터베이스의 논리적 구조를 정의하는 명세로, 테이블 이름, 열 이름, 데이터 타입, 제약 조건 등을 포함합니다. 실제 저장된 데이터(인스턴스)와 구분됩니다."
  optionExplanations={[
    '실제 저장된 데이터는 인스턴스(Instance)입니다.',
    '맞습니다. 스키마는 데이터베이스 구조와 제약 조건의 명세입니다.',
    'SQL 쿼리는 스키마가 아닌 데이터 조작 언어입니다.',
    'DBMS 소프트웨어와 스키마는 다른 개념입니다.',
  ]}
/>

<Quiz
  questionNumber={2}
  question="ER 다이어그램에서 약한 엔티티 집합(Weak Entity Set)의 특징은?"
  options={['① 기본 키를 가지지 않고 강한 엔티티에 의존한다', '② 다른 엔티티와 관계가 없다', '③ 항상 여러 개의 기본 키를 가진다', '④ NULL 값을 가질 수 없다']}
  correctAnswer={1}
  explanation="약한 엔티티 집합은 자체적으로 기본 키가 없어 강한 엔티티(식별 엔티티)에 의존합니다. 강한 엔티티의 기본 키와 자신의 부분 키(Partial Key)를 결합하여 유일하게 식별됩니다."
  optionExplanations={[
    '맞습니다. 약한 엔티티는 기본 키가 없고 강한 엔티티에 의존합니다.',
    '약한 엔티티는 식별 관계(Identifying Relationship)를 통해 강한 엔티티와 연결됩니다.',
    '약한 엔티티는 부분 키(Partial Key)만 가집니다.',
    'NULL 값과는 무관한 개념입니다.',
  ]}
/>

<Quiz
  questionNumber={3}
  question="관계형 데이터베이스에서 참조 무결성(Referential Integrity)이란?"
  options={['① 모든 열이 NULL이 아니어야 한다', '② 외래 키의 값은 반드시 참조하는 테이블의 기본 키 값으로 존재하거나 NULL이어야 한다', '③ 기본 키는 유일해야 한다', '④ 모든 속성이 원자값이어야 한다']}
  correctAnswer={2}
  explanation="참조 무결성은 외래 키 제약으로, 외래 키의 값이 참조하는 테이블의 기본 키 값으로 존재해야 합니다(또는 NULL). 이를 통해 존재하지 않는 레코드를 참조하는 오류를 방지합니다."
  optionExplanations={[
    'NULL 불허는 NOT NULL 제약입니다.',
    '맞습니다. 참조 무결성은 외래 키가 참조 테이블의 기본 키에 존재해야 하는 제약입니다.',
    '기본 키 유일성은 개체 무결성(Entity Integrity)입니다.',
    '원자값 요건은 제1 정규형(1NF)의 조건입니다.',
  ]}
/>

<Quiz
  questionNumber={4}
  question="관계 대수에서 자연 조인(Natural Join)의 특징은?"
  options={['① 두 테이블의 모든 열 조합으로 카르테시안 곱을 수행한다', '② 공통 속성(이름이 같은 열)을 기준으로 자동으로 동등 조인하고 중복 열을 제거한다', '③ 조인 조건을 명시적으로 지정해야 한다', '④ 항상 외부 조인을 수행한다']}
  correctAnswer={2}
  explanation="자연 조인은 두 릴레이션에서 이름이 같은 속성을 자동으로 조인 조건으로 사용하고, 결과에서 중복 열을 하나만 남깁니다. 조인 조건을 명시하지 않아도 되는 편리함이 있지만 의도치 않은 조인이 발생할 수 있습니다."
  optionExplanations={[
    '카르테시안 곱은 조건 없이 모든 조합을 만드는 것으로 자연 조인이 아닙니다.',
    '맞습니다. 자연 조인은 공통 속성 기준 자동 동등 조인 + 중복 열 제거입니다.',
    '자연 조인은 조건을 명시하지 않습니다.',
    '자연 조인은 내부 조인(Inner Join) 방식입니다.',
  ]}
/>

<Quiz
  questionNumber={5}
  question="SQL에서 HAVING 절과 WHERE 절의 차이는?"
  options={['① HAVING은 개별 행을 필터링하고 WHERE는 그룹을 필터링한다', '② WHERE는 개별 행을 필터링하고 HAVING은 GROUP BY 이후 그룹을 필터링한다', '③ HAVING과 WHERE는 동일하게 사용할 수 있다', '④ HAVING은 서브쿼리에서만 사용한다']}
  correctAnswer={2}
  explanation="WHERE는 GROUP BY 수행 전에 개별 행에 조건을 적용합니다. HAVING은 GROUP BY 수행 후 각 그룹에 조건을 적용하며, 집계 함수(COUNT, SUM 등)를 사용할 수 있습니다."
  optionExplanations={[
    'HAVING과 WHERE의 역할이 반대로 설명되어 있습니다.',
    '맞습니다. WHERE는 행 필터링, HAVING은 그룹 필터링입니다.',
    'HAVING과 WHERE는 사용 목적이 다릅니다.',
    'HAVING은 GROUP BY와 함께 메인 쿼리에서도 사용합니다.',
  ]}
/>

<Quiz
  questionNumber={6}
  question="SQL의 뷰(View)에 대한 설명으로 옳지 않은 것은?"
  options={['① 복잡한 쿼리를 단순하게 재사용할 수 있다', '② 특정 열이나 행에 대한 접근을 제한할 수 있다', '③ 뷰는 실제 데이터를 별도로 저장한다', '④ 논리적 독립성을 제공한다']}
  correctAnswer={3}
  explanation="일반 뷰(View)는 실제 데이터를 저장하지 않고 정의된 SELECT 쿼리를 저장합니다. 뷰를 조회할 때마다 기반 테이블에서 실시간으로 데이터를 가져옵니다. 실체화 뷰(Materialized View)는 데이터를 저장하지만 이는 특수한 경우입니다."
  optionExplanations={[
    '복잡한 쿼리 재사용은 뷰의 장점입니다.',
    '접근 제어를 통한 보안 강화는 뷰의 장점입니다.',
    '맞습니다. 일반 뷰는 실제 데이터를 저장하지 않습니다.',
    '논리적 독립성 제공은 뷰의 특징입니다.',
  ]}
/>

<Quiz
  questionNumber={7}
  question="제3 정규형(3NF)이 되기 위해 만족해야 하는 조건은?"
  options={['① 모든 속성이 원자값을 가진다', '② 부분 함수 종속이 없다', '③ 이행적 함수 종속이 없다 (2NF를 만족하면서)', '④ 모든 결정자가 후보 키이다']}
  correctAnswer={3}
  explanation="3NF는 2NF를 만족하면서 기본 키가 아닌 속성이 기본 키에 이행적으로(transitively) 종속되지 않아야 합니다. 즉, A→B→C처럼 비키 속성 B를 통한 간접 종속이 없어야 합니다."
  optionExplanations={[
    '원자값은 제1 정규형(1NF)의 조건입니다.',
    '부분 함수 종속 제거는 제2 정규형(2NF)의 조건입니다.',
    '맞습니다. 3NF는 이행적 함수 종속 제거입니다.',
    '모든 결정자가 후보 키인 것은 BCNF의 조건입니다.',
  ]}
/>

<Quiz
  questionNumber={8}
  question="트랜잭션의 ACID 특성 중 '격리성(Isolation)'에 대한 설명은?"
  options={['① 트랜잭션의 모든 연산이 모두 실행되거나 모두 취소된다', '② 동시에 실행되는 트랜잭션들이 서로의 중간 결과에 영향을 주지 않는다', '③ 완료된 트랜잭션의 결과는 영구적으로 보존된다', '④ 트랜잭션 전후로 데이터 무결성이 유지된다']}
  correctAnswer={2}
  explanation="격리성(Isolation)은 동시에 실행되는 여러 트랜잭션이 서로 간섭하지 않아야 한다는 특성입니다. 각 트랜잭션은 마치 혼자 실행되는 것처럼 동작하여 다른 트랜잭션의 중간 결과를 볼 수 없어야 합니다."
  optionExplanations={[
    '모두 실행 또는 모두 취소는 원자성(Atomicity)입니다.',
    '맞습니다. 격리성은 동시 실행 트랜잭션 간 간섭 방지입니다.',
    '영구적 보존은 지속성(Durability)입니다.',
    '무결성 유지는 일관성(Consistency)입니다.',
  ]}
/>

<Quiz
  questionNumber={9}
  question="로그 기반 회복에서 UNDO 연산에 사용하는 정보는?"
  options={['① 로그의 이후 값(After Image)', '② 로그의 이전 값(Before Image)', '③ 트랜잭션 시작 시각', '④ 체크포인트 위치']}
  correctAnswer={2}
  explanation="UNDO는 미완료 트랜잭션의 변경을 취소하기 위해 로그에 기록된 변경 이전의 값(Before Image)을 사용하여 데이터를 원래 상태로 복원합니다."
  optionExplanations={[
    '이후 값(After Image)은 REDO에서 사용합니다.',
    '맞습니다. UNDO는 이전 값(Before Image)으로 변경을 취소합니다.',
    '트랜잭션 시작 시각은 UNDO 값으로 직접 사용하지 않습니다.',
    '체크포인트는 회복 범위를 결정하는 데 사용합니다.',
  ]}
/>

<Quiz
  questionNumber={10}
  question="B+ Tree 인덱스가 범위 검색(BETWEEN, >, <)에 효율적인 이유는?"
  options={['① 해시 함수를 사용하여 직접 접근하기 때문에', '② 리프 노드들이 정렬된 연결 리스트로 연결되어 순차 접근이 가능하기 때문에', '③ 모든 데이터가 루트 노드에 저장되기 때문에', '④ 트리의 깊이가 1이기 때문에']}
  correctAnswer={2}
  explanation="B+ Tree의 리프 노드는 키 순서대로 연결 리스트로 연결되어 있습니다. 범위 검색 시 시작점을 찾은 후 연결 리스트를 따라 순차적으로 스캔하면 되므로 효율적입니다."
  optionExplanations={[
    '해시 함수로 직접 접근하는 것은 Hash 인덱스입니다.',
    '맞습니다. 리프 노드의 정렬된 연결 리스트가 범위 검색을 효율적으로 만듭니다.',
    '모든 데이터가 리프 노드에 저장됩니다.',
    'B+ Tree는 균형 트리이지만 깊이가 1이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={11}
  question="쿼리 최적화에서 Sort-Merge Join이 Hash Join보다 유리한 경우는?"
  options={['① 두 테이블 모두 작은 경우', '② 등치 조인이면서 메모리가 부족한 경우', '③ 범위 조인(BETWEEN, >, <)이 필요한 경우', '④ 내부 테이블에 인덱스가 있는 경우']}
  correctAnswer={3}
  explanation="Hash Join은 등치(=) 조인에만 사용할 수 있습니다. 범위 조인은 Sort-Merge Join이 지원하며, 두 테이블을 정렬 후 순차적으로 병합하여 범위 조건을 처리할 수 있습니다."
  optionExplanations={[
    '두 테이블이 작은 경우 Nested Loop Join이 적합합니다.',
    '메모리 부족 시 Sort-Merge도 임시 정렬 공간이 필요합니다.',
    '맞습니다. 범위 조인은 Sort-Merge Join이 Hash Join보다 유리합니다.',
    '인덱스가 있는 경우 Nested Loop Join이 적합합니다.',
  ]}
/>

<Quiz
  questionNumber={12}
  question="분산 데이터베이스의 복제 투명성(Replication Transparency)이란?"
  options={['① 데이터가 어느 사이트에 있는지 알 필요 없다', '② 데이터가 여러 사이트에 복제되어 있는지 사용자가 알 필요 없다', '③ 데이터가 어떻게 분할되어 있는지 알 필요 없다', '④ 여러 트랜잭션이 동시에 실행됨을 인식할 필요 없다']}
  correctAnswer={2}
  explanation="복제 투명성은 데이터가 여러 사이트에 복사되어 저장되어 있더라도 사용자는 이를 인식하지 않고 하나의 데이터처럼 접근할 수 있는 특성입니다."
  optionExplanations={[
    '데이터 위치를 알 필요 없는 것은 위치 투명성입니다.',
    '맞습니다. 복제 투명성은 복제 여부를 인식하지 않아도 되는 특성입니다.',
    '분할 방식을 알 필요 없는 것은 분할 투명성입니다.',
    '동시 실행을 인식하지 않아도 되는 것은 병행 투명성입니다.',
  ]}
/>

<Quiz
  questionNumber={13}
  question="CAP 정리에서 AP 시스템(예: Cassandra)의 특징은?"
  options={['① 네트워크 분단 시 가용성보다 일관성을 우선한다', '② 네트워크 분단 시에도 가용성을 유지하되 일관성을 희생한다', '③ 항상 강한 ACID 트랜잭션을 보장한다', '④ 단일 서버에서만 동작한다']}
  correctAnswer={2}
  explanation="AP 시스템은 네트워크 분단(Partition)이 발생해도 서비스를 계속 제공(Availability)하는 것을 우선합니다. 대신 일시적으로 일관성이 없는 데이터를 반환할 수 있으며, 나중에 결과적 일관성으로 해결합니다."
  optionExplanations={[
    '일관성 우선은 CP 시스템의 특징입니다.',
    '맞습니다. AP 시스템은 가용성을 유지하되 일관성을 희생합니다.',
    '강한 ACID는 CP 시스템에 더 가깝습니다.',
    'AP 시스템은 분산 환경에서 작동합니다.',
  ]}
/>

<Quiz
  questionNumber={14}
  question="데이터 웨어하우스에서 사실 테이블(Fact Table)에 주로 저장하는 내용은?"
  options={['① 고객의 이름, 주소, 전화번호 등의 속성', '② 측정값(매출액, 수량 등)과 차원 테이블을 참조하는 외래 키', '③ 시간, 상품, 지역 등의 분석 관점 속성', '④ SQL 쿼리 이력']}
  correctAnswer={2}
  explanation="사실 테이블(Fact Table)은 비즈니스 사건이나 거래의 수치적 측정값(예: 매출액, 판매 수량, 비용)과 각 차원 테이블을 연결하는 외래 키를 포함합니다."
  optionExplanations={[
    '고객 속성은 고객 차원 테이블에 저장됩니다.',
    '맞습니다. 사실 테이블은 측정값과 차원 외래 키를 포함합니다.',
    '분석 관점 속성은 차원 테이블에 저장됩니다.',
    'SQL 이력은 데이터 웨어하우스 구성 요소가 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={15}
  question="ETL에서 'ELT' 방식이 다른 점은?"
  options={['① E, L, T 순서로 수행하며 적재 전 변환한다', '② E, L, T 순서로 수행하며 데이터를 먼저 적재한 후 변환한다', '③ ELT는 추출 단계가 없다', '④ ELT는 클라우드에서 사용할 수 없다']}
  correctAnswer={2}
  explanation="ELT(Extract, Load, Transform)는 데이터를 먼저 목적지(데이터 웨어하우스 또는 데이터 레이크)에 적재한 후, 목적지의 강력한 처리 능력을 활용하여 변환합니다. Snowflake, BigQuery 같은 클라우드 DW에서 주로 사용됩니다."
  optionExplanations={[
    'E, L, T 순서이지만 적재 후 변환합니다. 적재 전 변환은 ETL입니다.',
    '맞습니다. ELT는 적재 후 변환하는 방식입니다.',
    'ELT도 추출(E) 단계가 포함됩니다.',
    'ELT는 주로 클라우드 환경에서 사용합니다.',
  ]}
/>

<Quiz
  questionNumber={16}
  question="SQL 인젝션 공격을 방어하는 데 Prepared Statement가 효과적인 이유는?"
  options={['① 비밀번호를 자동으로 암호화하기 때문에', '② SQL 구조와 사용자 입력 데이터를 분리하여 입력이 SQL 코드로 해석되지 않기 때문에', '③ 모든 SQL 쿼리를 자동으로 최적화하기 때문에', '④ 데이터베이스 연결을 암호화하기 때문에']}
  correctAnswer={2}
  explanation="Prepared Statement는 SQL 쿼리 구조를 먼저 컴파일하고, 사용자 입력은 이후에 파라미터로만 전달합니다. 사용자 입력이 SQL 구조의 일부로 해석되지 않으므로 악의적인 SQL 코드를 삽입해도 무력화됩니다."
  optionExplanations={[
    'Prepared Statement는 암호화와 무관합니다.',
    '맞습니다. SQL 구조와 데이터 분리가 SQL 인젝션 방어의 핵심입니다.',
    '쿼리 최적화는 Prepared Statement의 부가 기능이지 주목적이 아닙니다.',
    '연결 암호화는 SSL/TLS의 역할입니다.',
  ]}
/>

<Quiz
  questionNumber={17}
  question="데이터베이스에서 트리거(Trigger)의 역할은?"
  options={['① 주기적으로 저장 공간을 청소한다', '② 특정 이벤트(INSERT/UPDATE/DELETE) 발생 시 자동으로 실행되는 프로시저', '③ 사용자 로그인을 관리한다', '④ 인덱스를 자동으로 최적화한다']}
  correctAnswer={2}
  explanation="트리거(Trigger)는 테이블에 INSERT, UPDATE, DELETE 등의 이벤트가 발생할 때 자동으로 실행되는 저장 프로시저입니다. 데이터 무결성 강화, 감사 로그 자동 기록, 복잡한 비즈니스 규칙 적용에 사용됩니다."
  optionExplanations={[
    '저장 공간 청소는 트리거의 기능이 아닙니다.',
    '맞습니다. 트리거는 이벤트 발생 시 자동 실행되는 프로시저입니다.',
    '사용자 로그인 관리는 인증/접근 제어의 역할입니다.',
    '인덱스 최적화는 별도의 DBA 작업입니다.',
  ]}
/>

<Quiz
  questionNumber={18}
  question="NoSQL 데이터베이스 유형 중 소셜 네트워크의 친구 관계와 추천 시스템에 가장 적합한 것은?"
  options={['① 키-값 저장소 (Key-Value Store)', '② 문서 저장소 (Document Store)', '③ 와이드 컬럼 저장소 (Wide Column Store)', '④ 그래프 데이터베이스 (Graph Database)']}
  correctAnswer={4}
  explanation="그래프 데이터베이스는 노드(개체)와 엣지(관계)로 복잡한 관계를 표현하고 탐색하는 데 최적화되어 있습니다. 소셜 네트워크의 친구 관계, 추천 시스템, 지식 그래프, 사기 탐지 등에 가장 적합합니다."
  optionExplanations={[
    '키-값 저장소는 캐시, 세션 등 단순 접근에 적합합니다.',
    '문서 저장소는 JSON 형식의 유연한 데이터에 적합합니다.',
    '와이드 컬럼 저장소는 대용량 시계열 데이터에 적합합니다.',
    '맞습니다. 그래프 DB는 관계 탐색이 많은 소셜/추천 시스템에 최적입니다.',
  ]}
/>

<Quiz
  questionNumber={19}
  question="RBAC에서 최소 권한 원칙(Principle of Least Privilege)이란?"
  options={['① 모든 사용자에게 동일한 권한을 부여한다', '② 사용자에게 업무 수행에 필요한 최소한의 권한만 부여한다', '③ 관리자만 권한을 가질 수 있다', '④ 사용자가 스스로 권한을 선택한다']}
  correctAnswer={2}
  explanation="최소 권한 원칙은 사용자나 프로세스에게 작업 수행에 꼭 필요한 최소한의 권한만 부여하는 보안 원칙입니다. 이를 통해 계정이 침해되더라도 피해 범위를 최소화할 수 있습니다."
  optionExplanations={[
    '모든 사용자에게 동일한 권한은 최소 권한 원칙이 아닙니다.',
    '맞습니다. 최소 권한은 필요한 최소한의 권한만 부여하는 원칙입니다.',
    '관리자만 권한을 갖는 것은 다른 개념입니다.',
    '사용자가 스스로 권한을 선택하면 보안이 취약해집니다.',
  ]}
/>

<Quiz
  questionNumber={20}
  question="데이터 마이닝의 연관 규칙에서 향상도(Lift)가 1보다 크다는 것은?"
  options={['① A와 B가 서로 무관하다', '② A와 B 사이에 양의 상관관계가 있다(A를 구매하면 B를 더 구매하는 경향)', '③ A와 B 사이에 음의 상관관계가 있다', '④ 규칙의 신뢰도가 낮다']}
  correctAnswer={2}
  explanation="향상도(Lift) = 신뢰도(A→B) / 지지도(B)입니다. Lift > 1이면 A와 B가 독립적인 경우보다 함께 발생하는 비율이 더 높다는 의미로, 양의 상관관계가 있습니다. Lift = 1이면 독립, Lift < 1이면 음의 상관관계입니다."
  optionExplanations={[
    'Lift = 1이면 A와 B가 무관(독립)합니다.',
    '맞습니다. Lift > 1은 A와 B 간 양의 상관관계를 의미합니다.',
    'Lift < 1이면 음의 상관관계입니다.',
    '향상도는 신뢰도의 상대적 수치이며 신뢰도의 낮고 높음을 직접 나타내지 않습니다.',
  ]}
/>

<Quiz
  questionNumber={21}
  question="옵티미스틱 동시성 제어(Optimistic Concurrency Control)의 특징은?"
  options={['① 모든 데이터 접근 시 잠금을 먼저 획득한다', '② 충돌이 드물다고 가정하고 작업 후 커밋 시 충돌 여부를 검사한다', '③ 읽기 전용 트랜잭션만 허용한다', '④ 단일 사용자 환경에서만 동작한다']}
  correctAnswer={2}
  explanation="옵티미스틱 동시성 제어는 충돌이 드물 것이라고 낙관적으로 가정합니다. 잠금 없이 작업을 수행하고, 커밋 시점에 다른 트랜잭션과 충돌이 있는지 검사합니다. 충돌 시 트랜잭션을 롤백하고 재시도합니다."
  optionExplanations={[
    '모든 접근 시 잠금을 획득하는 것은 비관적(Pessimistic) 동시성 제어입니다.',
    '맞습니다. 옵티미스틱은 낙관적 가정으로 잠금 없이 작업 후 커밋 시 검사합니다.',
    '읽기 전용 제한은 옵티미스틱 제어와 무관합니다.',
    '옵티미스틱 제어는 다중 사용자 환경을 위한 기법입니다.',
  ]}
/>

<Quiz
  questionNumber={22}
  question="데이터 무결성 제약 중 도메인 무결성(Domain Integrity)이란?"
  options={['① 기본 키는 NULL이 아니고 유일해야 한다', '② 외래 키는 참조 테이블의 기본 키로 존재해야 한다', '③ 각 열의 값은 정의된 도메인(데이터 타입, 범위, 형식)을 만족해야 한다', '④ 모든 행은 유일해야 한다']}
  correctAnswer={3}
  explanation="도메인 무결성은 각 열의 값이 해당 열에 정의된 도메인(허용 가능한 값의 집합: 데이터 타입, 허용 범위, CHECK 제약 등)을 만족해야 한다는 제약입니다. 예를 들어 나이 열에 음수가 들어올 수 없습니다."
  optionExplanations={[
    '기본 키의 NOT NULL + 유일 제약은 개체 무결성입니다.',
    '외래 키 제약은 참조 무결성입니다.',
    '맞습니다. 도메인 무결성은 열 값이 정의된 도메인을 만족해야 하는 제약입니다.',
    '행의 유일성은 기본 키 또는 유니크 제약으로 보장합니다.',
  ]}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr/nile/study/nStudy1_1.do)
- Ramez Elmasri, "Fundamentals of Database Systems"

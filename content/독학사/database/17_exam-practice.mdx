---
title: '17. 데이터베이스 예상기출문제'
description: '데이터베이스 시험 대비 예상기출 문제 모음'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 공식 출제 범위를 바탕으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="기본키(Primary Key)가 만족해야 하는 조건은?"
  options={['①값이 자주 중복되어도 된다', '②값이 유일해야 하고 NULL이 아니어야 한다', '③반드시 문자열이어야 한다', '④여러 개를 자유롭게 지정할 수 있다']}
  correctAnswer={2}
  explanation="기본키는 각 행을 유일하게 구별해야 하므로 값이 중복되지 않아야 하고 NULL이면 안 됩니다."
  optionExplanations={['중복되면 행을 구별할 수 없습니다.', '맞습니다. 유일성과 NULL 금지입니다.', '숫자형도 기본키가 될 수 있습니다.', '한 테이블의 기본키는 보통 하나로 지정됩니다.']}
/>

<Quiz
  questionNumber={2}
  question="N:M(다대다) 관계를 관계형 데이터베이스에서 구현하는 방법은?"
  options={['①두 테이블을 하나로 합친다', '②두 엔티티의 기본키를 모두 외래키로 가지는 연결 테이블을 만든다', '③관계를 무시한다', '④N:M은 표현할 수 없다']}
  correctAnswer={2}
  explanation="N:M 관계는 두 엔티티의 기본키를 모두 외래키로 갖는 연결 테이블로 구현합니다."
  optionExplanations={['하나로 합치면 데이터 중복 문제가 생깁니다.', '맞습니다. 연결 테이블을 사용합니다.', '관계는 반드시 표현해야 합니다.', '연결 테이블을 통해 표현 가능합니다.']}
/>

<Quiz
  questionNumber={3}
  question="'SELECT AVG(학점) FROM 학생 WHERE 학년=2;'가 계산하는 것은?"
  options={['①전체 학생의 평균 학점', '②학년이 2인 학생들만의 평균 학점', '③학년의 평균값', '④학점이 2인 학생 수']}
  correctAnswer={2}
  explanation="WHERE 절이 집계 이전에 학년이 2인 학생만 걸러내므로, 그들만의 평균 학점이 계산됩니다."
  optionExplanations={['WHERE로 걸러진 학생만 대상입니다.', '맞습니다. 학년 2인 학생들만의 평균입니다.', '평균의 대상은 학점입니다.', 'COUNT가 아니라 AVG를 사용했습니다.']}
/>

<Quiz
  questionNumber={4}
  question="3단계 스키마 중 '전체 논리적 구조'를 정의하는 단계는?"
  options={['①외부 스키마', '②개념 스키마', '③내부 스키마', '④물리 스키마']}
  correctAnswer={2}
  explanation="개념 스키마는 조직 전체의 데이터 구조를 통합적으로 정의하는 단계입니다."
  optionExplanations={['외부 스키마는 개별 사용자 관점의 뷰입니다.', '맞습니다. 개념 스키마입니다.', '내부 스키마는 물리적 저장 방식을 다룹니다.', '물리 스키마는 표준 명칭이 아닙니다.']}
/>

<Quiz
  questionNumber={5}
  question="1:N 관계를 테이블로 변환하는 방법은?"
  options={['①두 테이블을 합친다', '②N쪽(다수 쪽) 테이블에 1쪽(단수 쪽) 테이블의 기본키를 외래키로 추가한다', '③연결 테이블을 만든다', '④변환하지 않는다']}
  correctAnswer={2}
  explanation="1:N 관계는 다수 쪽 테이블에 단수 쪽 테이블의 기본키를 외래키로 추가해 표현합니다."
  optionExplanations={['합치면 중복 문제가 생깁니다.', '맞습니다. N쪽에 외래키를 추가합니다.', '연결 테이블은 N:M 관계에서 필요합니다.', '적절한 변환이 필요합니다.']}
/>

<Quiz
  questionNumber={6}
  question="슈퍼키와 후보키의 핵심 차이는?"
  options={['①같은 개념이다', '②후보키는 슈퍼키 중 최소성을 만족하는 것이다', '③슈퍼키가 항상 후보키보다 적은 속성을 가진다', '④후보키는 기본키로 선택될 수 없다']}
  correctAnswer={2}
  explanation="모든 후보키는 슈퍼키이지만, 후보키는 불필요한 속성이 없는 최소한의 조합이라는 점이 다릅니다."
  optionExplanations={['최소성이라는 중요한 차이가 있습니다.', '맞습니다. 최소성 여부입니다.', '후보키가 슈퍼키의 부분집합입니다.', '후보키 중 하나가 기본키로 선택됩니다.']}
/>

<Quiz
  questionNumber={7}
  question="LEFT JOIN에서 오른쪽 테이블에 매칭되는 데이터가 없을 때 값은?"
  options={['①0', '②NULL', '③해당 행이 사라진다', '④에러 발생']}
  correctAnswer={2}
  explanation="LEFT JOIN은 왼쪽 테이블 행을 모두 유지하며, 매칭되지 않으면 NULL로 채웁니다."
  optionExplanations={['NULL로 채워집니다.', '맞습니다. NULL입니다.', '왼쪽 테이블 행은 유지됩니다.', '정상적으로 실행됩니다.']}
/>

<Quiz
  questionNumber={8}
  question="뷰(View)의 특징은?"
  options={['①실제 데이터를 별도로 저장하는 새 테이블이다', '②실제 데이터를 저장하지 않고 쿼리 결과를 테이블처럼 사용하는 가상 테이블이다', '③절대 삭제할 수 없다', '④조회에 사용할 수 없다']}
  correctAnswer={2}
  explanation="뷰는 데이터를 저장하지 않고, 정의된 쿼리의 결과를 마치 테이블처럼 조회하게 해주는 가상 테이블입니다."
  optionExplanations={['별도로 데이터를 저장하지 않습니다.', '맞습니다. 가상 테이블입니다.', 'DROP VIEW로 삭제 가능합니다.', '일반 테이블처럼 SELECT로 조회 가능합니다.']}
/>

<Quiz
  questionNumber={9}
  question="선택(σ) 연산을 조인보다 먼저 수행하는 것이 효율적인 이유는?"
  options={['①결과가 달라지기 때문에', '②처리할 중간 데이터의 양을 크게 줄일 수 있기 때문에', '③선택 연산은 항상 실패하기 때문에', '④조인이 불가능해지기 때문에']}
  correctAnswer={2}
  explanation="조건에 맞는 데이터를 먼저 걸러내면 이후 조인할 데이터 양이 줄어 처리 비용이 감소합니다."
  optionExplanations={['최종 결과는 순서와 무관하게 동일합니다.', '맞습니다. 중간 데이터 양 감소입니다.', '정상적으로 조건에 맞는 데이터를 찾습니다.', '조인은 정상적으로 실행됩니다.']}
/>

<Quiz
  questionNumber={10}
  question="갱신 이상(Update Anomaly)이 발생하는 상황은?"
  options={['①새 데이터를 추가할 수 없는 상황', '②중복 저장된 데이터 중 일부만 수정해 불일치가 생기기 쉬운 상황', '③삭제 시 관련 없는 정보까지 사라지는 상황', '④테이블을 삭제할 수 없는 상황']}
  correctAnswer={2}
  explanation="갱신 이상은 같은 정보가 여러 곳에 중복되어, 일부만 수정하면 데이터 불일치가 생기는 문제입니다."
  optionExplanations={['이는 삽입 이상입니다.', '맞습니다. 중복 데이터의 불일치 위험입니다.', '이는 삭제 이상입니다.', '테이블 삭제와는 무관합니다.']}
/>

<Quiz
  questionNumber={11}
  question="2NF에서 제거하는 문제는?"
  options={['①원자값이 아닌 값', '②부분 함수적 종속', '③이행적 함수적 종속', '④결합 종속']}
  correctAnswer={2}
  explanation="2NF는 기본키의 일부에만 종속되는 부분 함수적 종속을 제거합니다."
  optionExplanations={['이는 1NF의 문제입니다.', '맞습니다. 부분 함수적 종속입니다.', '이는 3NF의 문제입니다.', '이는 5NF의 문제입니다.']}
/>

<Quiz
  questionNumber={12}
  question="계좌 이체에서 '출금은 되었는데 입금은 안 되는' 상황을 방지하는 ACID 특성은?"
  options={['①원자성', '②일관성', '③고립성', '④지속성']}
  correctAnswer={1}
  explanation="원자성은 트랜잭션의 모든 작업이 전부 실행되거나 전부 취소되어야 함을 보장합니다."
  optionExplanations={['맞습니다. 원자성입니다.', '일관성은 규칙 준수와 관련됩니다.', '고립성은 다른 트랜잭션과의 간섭 방지입니다.', '지속성은 결과의 영구 보존입니다.']}
/>

<Quiz
  questionNumber={13}
  question="배타 잠금(Exclusive Lock)의 특징은?"
  options={['①여러 트랜잭션이 동시에 걸 수 있다', '②한 트랜잭션이 걸면 다른 트랜잭션은 어떤 잠금도 걸 수 없다', '③읽기만 가능하다', '④잠금 기능이 없다']}
  correctAnswer={2}
  explanation="배타 잠금은 데이터를 완전히 독점해, 다른 트랜잭션은 읽기·쓰기 어떤 접근도 할 수 없습니다."
  optionExplanations={['이는 공유 잠금의 특징입니다.', '맞습니다. 완전 독점입니다.', '읽기와 쓰기 모두 가능합니다.', '강력한 독점 잠금 기능입니다.']}
/>

<Quiz
  questionNumber={14}
  question="교착상태(Deadlock)의 정의는?"
  options={['①데이터가 잘못된 값으로 저장되는 문제', '②트랜잭션들이 서로가 가진 자원을 무한정 기다리며 진행되지 않는 상태', '③트랜잭션이 너무 빨리 끝나는 상태', '④데이터베이스가 삭제된 상태']}
  correctAnswer={2}
  explanation="교착상태는 트랜잭션들이 서로 상대의 잠금을 기다리며 어느 쪽도 진행하지 못하는 상태입니다."
  optionExplanations={['데이터 오류와는 다른 성격입니다.', '맞습니다. 상호 대기로 인한 정지입니다.', '아무것도 끝나지 못하는 상태입니다.', '삭제와는 무관합니다.']}
/>

<Quiz
  questionNumber={15}
  question="로그의 'Before Image'(변경 전 값)는 어떤 작업에 사용되는가?"
  options={['①Redo', '②Undo', '③백업 생성', '④사용자 인증']}
  correctAnswer={2}
  explanation="변경 전 값은 트랜잭션 작업을 되돌리는 Undo 작업에 사용됩니다."
  optionExplanations={['Redo는 변경 후 값을 사용합니다.', '맞습니다. Undo에 사용됩니다.', '백업과는 별개입니다.', '인증과는 무관합니다.']}
/>

<Quiz
  questionNumber={16}
  question="검사시점(Checkpoint)의 주된 목적은?"
  options={['①데이터베이스 삭제', '②회복 시 처음부터 모든 로그를 다시 훑지 않도록 회복 시간을 단축', '③사용자 권한 관리', '④SQL 문법 검사']}
  correctAnswer={2}
  explanation="검사시점은 그 시점까지의 안전한 반영을 표시해 회복 시간을 단축시킵니다."
  optionExplanations={['삭제와는 무관합니다.', '맞습니다. 회복 시간 단축입니다.', '권한 관리는 GRANT/REVOKE의 역할입니다.', '문법 검사는 질의 처리의 파싱 단계입니다.']}
/>

<Quiz
  questionNumber={17}
  question="객체지향 데이터베이스가 관계형 데이터베이스와 다른 점은?"
  options={['①테이블 개념을 처음 도입했다', '②데이터와 동작(메서드)을 객체 하나에 함께 저장한다', '③SQL을 전혀 사용하지 않는다는 점에서 항상 같다', '④데이터를 저장하지 않는다']}
  correctAnswer={2}
  explanation="객체지향 DB는 데이터와 그 데이터를 다루는 동작을 객체 하나로 묶어 저장합니다."
  optionExplanations={['테이블은 관계형 모델의 핵심입니다.', '맞습니다. 데이터+동작을 함께 저장합니다.', 'ORDB는 SQL을 계속 사용합니다.', '데이터는 정상적으로 저장됩니다.']}
/>

<Quiz
  questionNumber={18}
  question="3계층(3-Tier) 구조가 2계층보다 유지보수에 유리한 이유는?"
  options={['①클라이언트가 더 복잡해지기 때문에', '②로직이 중간 서버에 모여 있어 서버만 수정하면 되기 때문에', '③데이터베이스가 필요 없기 때문에', '④사용자가 없기 때문에']}
  correctAnswer={2}
  explanation="로직이 중간 서버에 집중되어 있으면, 로직 변경 시 서버만 수정하면 됩니다."
  optionExplanations={['오히려 클라이언트가 단순해지는 것이 장점입니다.', '맞습니다. 중앙 집중 관리의 이점입니다.', 'DBMS는 여전히 사용됩니다.', '사용자는 클라이언트를 통해 시스템을 사용합니다.']}
/>

<Quiz
  questionNumber={19}
  question="빅데이터의 3V에 포함되지 않는 것은?"
  options={['①Volume', '②Velocity', '③Variety', '④Vacation']}
  correctAnswer={4}
  explanation="빅데이터의 3V는 Volume(양), Velocity(속도), Variety(다양성)입니다."
  optionExplanations={['3V 중 하나입니다.', '3V 중 하나입니다.', '3V 중 하나입니다.', '맞습니다. 이런 개념은 존재하지 않습니다.']}
/>

<Quiz
  questionNumber={20}
  question="'기저귀를 사는 사람은 맥주도 함께 사는 경향이 있다'는 분석에 해당하는 데이터마이닝 기법은?"
  options={['①분류', '②군집화', '③연관 규칙', '④정규화']}
  correctAnswer={3}
  explanation="이는 함께 구매되는 상품의 패턴을 찾는 연관 규칙(장바구니 분석)의 대표적인 사례입니다."
  optionExplanations={['분류는 범주를 예측하는 기법입니다.', '군집화는 데이터를 그룹화하는 기법입니다.', '맞습니다. 연관 규칙입니다.', '정규화는 테이블 설계 기법입니다.']}
/>

---
title: '08. 벡터'
description: '벡터의 연산, 내적, 직선과 평면의 방정식을 학습합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 벡터의 덧셈·스칼라 곱·내적을 계산하고 직선과 평면의 방정식을 세울 수 있습니다.
</Callout>

## 벡터의 기본 개념

**벡터**는 크기와 방향을 함께 갖는 양입니다. 크기만 있는 양은 **스칼라**라 합니다.

평면 위의 벡터는 두 성분 $(a, b)$로, 공간 벡터는 세 성분 $(a, b, c)$로 나타냅니다.

$$\vec{v} = (3, -2), \quad |\vec{v}| = \sqrt{3^2 + (-2)^2} = \sqrt{13}$$

벡터의 **크기(노름)**는 다음과 같습니다.

$$|\vec{v}| = \sqrt{a^2 + b^2} \quad \text{(2D)}, \qquad |\vec{v}| = \sqrt{a^2 + b^2 + c^2} \quad \text{(3D)}$$

크기가 1인 벡터를 **단위벡터**라 합니다. 벡터 $\vec{v}$의 단위벡터는 $\dfrac{\vec{v}}{|\vec{v}|}$입니다.

## 벡터의 연산

**덧셈과 뺄셈:** 같은 위치의 성분끼리 계산합니다.

$$\vec{a} + \vec{b} = (a_1+b_1,\; a_2+b_2), \qquad \vec{a} - \vec{b} = (a_1-b_1,\; a_2-b_2)$$

**스칼라 곱:** 스칼라 $k$를 곱하면 모든 성분에 $k$를 곱합니다.

$$k\vec{a} = (ka_1,\; ka_2)$$

**예제:** $\vec{a} = (2, 3)$, $\vec{b} = (-1, 4)$일 때 $2\vec{a} - \vec{b}$를 구하시오.

$$2\vec{a} - \vec{b} = (4, 6) - (-1, 4) = (5, 2)$$

## 내적 (점곱)

두 벡터 $\vec{a} = (a_1, a_2)$, $\vec{b} = (b_1, b_2)$의 **내적**은 다음과 같습니다.

$$\vec{a} \cdot \vec{b} = a_1 b_1 + a_2 b_2$$

내적과 사이각 $\theta$의 관계는 다음과 같습니다.

$$\vec{a} \cdot \vec{b} = |\vec{a}||\vec{b}|\cos\theta$$

이를 이용하면 두 벡터의 **사이각**을 구할 수 있습니다.

$$\cos\theta = \frac{\vec{a} \cdot \vec{b}}{|\vec{a}||\vec{b}|}$$

**수직 조건:** $\vec{a} \perp \vec{b} \iff \vec{a} \cdot \vec{b} = 0$

**예제:** $\vec{a} = (1, 2)$, $\vec{b} = (3, -1)$일 때 내적과 사이각을 구하시오.

$$\vec{a} \cdot \vec{b} = 1\cdot3 + 2\cdot(-1) = 1$$

$$|\vec{a}| = \sqrt{5}, \quad |\vec{b}| = \sqrt{10}$$

$$\cos\theta = \frac{1}{\sqrt{5}\cdot\sqrt{10}} = \frac{1}{\sqrt{50}} = \frac{1}{5\sqrt{2}}$$

<Callout type="warning">
  내적의 결과는 벡터가 아닌 스칼라(숫자)입니다. 내적을 통해 각도를 구할 수 있습니다.
</Callout>

## 직선의 방정식 (벡터 형태)

점 $P_0 = (x_0, y_0)$을 지나고 방향벡터 $\vec{d} = (a, b)$인 직선의 **매개변수 방정식**은 다음과 같습니다.

$$(x, y) = (x_0, y_0) + t(a, b) \quad (t \in \mathbb{R})$$

즉, $x = x_0 + ta$, $y = y_0 + tb$입니다.

**두 점을 지나는 직선:** 두 점 $A$, $B$를 지나면 방향벡터를 $\overrightarrow{AB}$로 잡습니다.

## 평면의 방정식 (벡터 형태)

점 $P_0 = (x_0, y_0, z_0)$을 지나고 법선벡터 $\vec{n} = (A, B, C)$에 수직인 평면의 방정식은 다음과 같습니다.

$$A(x - x_0) + B(y - y_0) + C(z - z_0) = 0$$

또는 $Ax + By + Cz = D$ 형태로 씁니다.

**예제:** 점 $(1, 2, 3)$을 지나고 법선벡터 $(2, -1, 3)$인 평면의 방정식을 구하시오.

$$2(x-1) - (y-2) + 3(z-3) = 0 \implies 2x - y + 3z = 9$$

## 핵심 정리

- 벡터 크기: $|\vec{v}| = \sqrt{a^2+b^2+c^2}$
- 내적: $\vec{a}\cdot\vec{b} = a_1b_1+a_2b_2$, 수직이면 내적=0
- 사이각: $\cos\theta = \dfrac{\vec{a}\cdot\vec{b}}{|\vec{a}||\vec{b}|}$
- 직선: 점 + 방향벡터로 매개변수 표현
- 평면: 점 + 법선벡터로 $\vec{n}\cdot(\vec{r}-\vec{r_0})=0$

## 마무리 복습

<Quiz
  questionNumber={1}
  question="벡터 v=(3, 4)의 크기 |v|는?"
  options={['5', '7', '12', '25']}
  correctAnswer={1}
  explanation="|v| = √(3²+4²) = √(9+16) = √25 = 5입니다."
  optionExplanations={['맞습니다. 피타고라스 정리로 계산하면 5입니다.', '성분을 더한 값입니다.', '성분을 곱한 값입니다.', '크기를 제곱한 값입니다.']}
/>

<Quiz
  questionNumber={2}
  question="a=(1, 2), b=(3, -1)의 내적 a·b는?"
  options={['-1', '1', '5', '7']}
  correctAnswer={2}
  explanation="a·b = 1×3 + 2×(-1) = 3-2 = 1입니다."
  optionExplanations={['부호를 반대로 계산했습니다.', '맞습니다. 1×3 + 2×(-1) = 1입니다.', '1×3 + 2×1로 절댓값만 계산한 오류입니다.', '성분을 모두 더한 오류입니다.']}
/>

<Quiz
  questionNumber={3}
  question="두 벡터 a와 b가 수직이기 위한 조건은?"
  options={['|a|=|b|', 'a·b=0', 'a×b=0', 'a=b']}
  correctAnswer={2}
  explanation="두 벡터가 수직이면 사이각이 90도이고 cos90=0이므로 내적 a·b=0입니다."
  optionExplanations={['크기가 같아도 수직이 아닐 수 있습니다.', '맞습니다. 내적이 0이면 두 벡터는 수직입니다.', '외적이 0이면 평행을 의미합니다.', '같은 벡터는 수직이 아닙니다.']}
/>

<Quiz
  questionNumber={4}
  question="a=(2, 0), b=(0, 3)의 사이각 θ는?"
  options={['0도', '45도', '90도', '180도']}
  correctAnswer={3}
  explanation="a·b = 2×0 + 0×3 = 0이므로 cosθ=0, θ=90도입니다."
  optionExplanations={['같은 방향일 때 0도입니다.', '내적을 잘못 계산했습니다.', '맞습니다. 내적이 0이므로 두 벡터는 수직(90도)입니다.', '반대 방향일 때 180도입니다.']}
/>

<Quiz
  questionNumber={5}
  question="점 (2, 1, 0)을 지나고 법선벡터 (1, 1, 1)인 평면의 방정식은?"
  options={['x+y+z=0', 'x+y+z=3', 'x+y+z=2', '2x+y=3']}
  correctAnswer={2}
  explanation="(x-2)+(y-1)+(z-0)=0 → x+y+z=3입니다."
  optionExplanations={['상수항 계산이 누락된 오류입니다.', '맞습니다. 점을 대입하면 2+1+0=3입니다.', '좌표 중 하나만 대입한 오류입니다.', '법선벡터 성분을 무시한 오류입니다.']}
/>

<Quiz
  questionNumber={6}
  question="벡터 a=(1, 1)의 단위벡터는?"
  options={['(1, 1)', '(1/2, 1/2)', '(1/√2, 1/√2)', '(√2, √2)']}
  correctAnswer={3}
  explanation="|a|=√2이므로 단위벡터는 (1/√2, 1/√2)입니다."
  optionExplanations={['크기를 나누지 않은 원래 벡터입니다.', '크기 √2로 나누지 않고 2로 나눈 오류입니다.', '맞습니다. 각 성분을 |a|=√2로 나눕니다.', '크기를 나누는 것이 아니라 곱한 오류입니다.']}
/>

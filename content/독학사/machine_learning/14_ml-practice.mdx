---
title: '14. 머신러닝 예상기출문제'
description: '머신러닝 전 범위 예상기출문제 20개 이상으로 최종 정리합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 머신러닝 전 범위(개요, 회귀, 분류, 앙상블, 군집화, 차원 축소, 신경망, 모델 평가, 정규화, 특징 공학)의 핵심 개념을 예상기출문제로 최종 점검합니다.
</Callout>

## 마무리 복습

<Quiz
  questionNumber={1}
  question="지도 학습(Supervised Learning)과 비지도 학습(Unsupervised Learning)의 차이는?"
  options={['① 지도 학습은 레이블이 있는 데이터로 학습하고, 비지도 학습은 레이블이 없는 데이터로 학습한다', '② 지도 학습은 컴퓨터 없이 학습한다', '③ 비지도 학습이 항상 더 높은 성능을 낸다', '④ 지도 학습은 군집화에만 사용된다']}
  correctAnswer={1}
  explanation="지도 학습은 입력 데이터와 정답 레이블(label)이 함께 주어진 데이터로 학습합니다. 비지도 학습은 레이블 없이 데이터 자체의 구조나 패턴을 학습합니다."
  optionExplanations={[
    '맞습니다. 레이블 유무가 지도/비지도 학습의 핵심 차이입니다.',
    '학습 방식은 데이터 레이블과 관련된 것으로 하드웨어와 무관합니다.',
    '성능 우열은 작업과 데이터에 따라 다릅니다.',
    '군집화는 비지도 학습에 해당합니다.',
  ]}
/>

<Quiz
  questionNumber={2}
  question="선형 회귀(Linear Regression)에서 비용 함수로 사용하는 것은?"
  options={['① 교차 엔트로피(Cross Entropy)', '② 평균 제곱 오차(MSE)', '③ 힌지 손실(Hinge Loss)', '④ KL Divergence']}
  correctAnswer={2}
  explanation="선형 회귀는 연속형 값을 예측하는 회귀 문제에 사용하며, 예측값과 실제값의 차이의 제곱 평균인 MSE(Mean Squared Error)를 비용 함수로 사용합니다."
  optionExplanations={[
    '교차 엔트로피는 분류 문제의 손실 함수입니다.',
    '맞습니다. 선형 회귀는 MSE를 비용 함수로 사용합니다.',
    '힌지 손실은 SVM의 손실 함수입니다.',
    'KL Divergence는 분포 간 차이를 측정하는 데 사용합니다.',
  ]}
/>

<Quiz
  questionNumber={3}
  question="로지스틱 회귀(Logistic Regression)의 출력 범위는?"
  options={['① (-∞, ∞)', '② [0, 1]', '③ [-1, 1]', '④ 정수만 출력']}
  correctAnswer={2}
  explanation="로지스틱 회귀는 선형 결합에 시그모이드 함수를 적용하여 출력을 (0, 1) 범위의 확률로 변환합니다. 이 확률을 임계값(보통 0.5)과 비교하여 이진 분류를 수행합니다."
  optionExplanations={[
    '제한 없는 범위는 선형 회귀의 출력입니다.',
    '맞습니다. 시그모이드 함수를 통해 (0, 1) 범위의 확률을 출력합니다.',
    '(-1, 1)은 Tanh 함수의 범위입니다.',
    '로지스틱 회귀는 연속형 확률값을 출력합니다.',
  ]}
/>

<Quiz
  questionNumber={4}
  question="의사결정나무(Decision Tree)에서 불순도를 측정하는 지표가 아닌 것은?"
  options={['① 지니 불순도(Gini Impurity)', '② 정보 이득(Information Gain)', '③ 엔트로피(Entropy)', '④ MSE(Mean Squared Error)']}
  correctAnswer={4}
  explanation="의사결정나무의 분할 기준으로 분류 문제에서는 지니 불순도, 엔트로피/정보 이득을 사용합니다. MSE는 회귀 나무에서 분할 기준으로 쓰이기도 하지만, 일반적으로 불순도 지표로 언급되지는 않습니다."
  optionExplanations={[
    '지니 불순도는 CART 알고리즘의 기본 분할 기준입니다.',
    '정보 이득은 ID3 알고리즘의 분할 기준입니다.',
    '엔트로피 감소가 정보 이득입니다.',
    '맞습니다. MSE는 회귀 문제의 손실 함수이지 분류 나무의 불순도 지표가 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={5}
  question="SVM(Support Vector Machine)에서 마진(Margin)의 의미는?"
  options={['① 모델의 정확도', '② 두 클래스를 구분하는 결정 경계와 각 클래스의 가장 가까운 데이터 포인트(서포트 벡터) 사이의 거리', '③ 오분류된 샘플 수', '④ 학습 데이터의 크기']}
  correctAnswer={2}
  explanation="SVM은 두 클래스를 구분하는 초평면 중에서 마진(결정 경계와 가장 가까운 데이터 포인트까지의 거리)을 최대화하는 결정 경계를 찾습니다. 마진이 클수록 일반화 성능이 좋아집니다."
  optionExplanations={[
    '마진은 정확도를 나타내지 않습니다.',
    '맞습니다. 마진은 결정 경계와 서포트 벡터 사이의 거리입니다.',
    '오분류 수는 슬랙 변수와 관련됩니다.',
    '마진은 데이터 크기와 무관합니다.',
  ]}
/>

<Quiz
  questionNumber={6}
  question="랜덤 포레스트(Random Forest)가 단일 의사결정나무보다 성능이 좋은 이유는?"
  options={['① 더 깊은 나무를 학습하기 때문에', '② 배깅과 무작위 특징 선택을 통해 다양한 나무를 만들어 앙상블하기 때문에', '③ 오분류에 더 높은 가중치를 부여하기 때문에', '④ 순차적으로 나무를 학습하여 오류를 수정하기 때문에']}
  correctAnswer={2}
  explanation="랜덤 포레스트는 배깅(부트스트랩 샘플링)으로 훈련 데이터를 다양하게 하고, 분할 시 무작위 특징 부분집합을 사용하여 상관관계가 낮은 다양한 나무를 만든 후 다수결/평균으로 예측합니다."
  optionExplanations={[
    '더 깊은 나무는 오히려 오버피팅 위험이 있습니다.',
    '맞습니다. 배깅과 무작위 특징 선택으로 다양한 나무를 앙상블합니다.',
    '오분류에 가중치를 부여하는 것은 부스팅의 개념입니다.',
    '순차 학습으로 오류를 수정하는 것은 부스팅(Gradient Boosting 등)입니다.',
  ]}
/>

<Quiz
  questionNumber={7}
  question="부스팅(Boosting) 알고리즘의 핵심 개념은?"
  options={['① 동일한 모델을 여러 번 학습하여 평균을 취한다', '② 이전 모델이 틀린 샘플에 더 집중하여 순차적으로 모델을 학습한다', '③ 무작위로 선택한 특징으로 독립적인 모델을 학습한다', '④ 서로 다른 알고리즘을 단순 투표로 결합한다']}
  correctAnswer={2}
  explanation="부스팅은 약한 학습기들을 순차적으로 학습하면서, 이전 모델이 잘못 분류한 샘플에 더 높은 가중치를 부여하여 오류를 집중적으로 수정하는 앙상블 방법입니다."
  optionExplanations={[
    '동일 모델 반복 평균은 배깅과 유사한 개념입니다.',
    '맞습니다. 부스팅은 이전 모델의 오류에 집중하여 순차 학습합니다.',
    '무작위 특징 선택은 랜덤 포레스트의 개념입니다.',
    '서로 다른 알고리즘의 단순 투표는 보팅(Voting) 앙상블입니다.',
  ]}
/>

<Quiz
  questionNumber={8}
  question="k-NN(k-Nearest Neighbors) 알고리즘에서 k를 너무 크게 설정하면?"
  options={['① 과적합이 발생한다', '② 언더피팅(과소적합)이 발생하고 결정 경계가 부드러워진다', '③ 학습 속도가 빨라진다', '④ 이상치에 더 민감해진다']}
  correctAnswer={2}
  explanation="k-NN에서 k가 크면 더 많은 이웃을 고려하여 결정 경계가 부드러워지고 언더피팅이 발생합니다. 반대로 k가 작으면(k=1) 결정 경계가 복잡해지고 과적합이 발생합니다."
  optionExplanations={[
    'k가 작을수록 과적합 위험이 있습니다.',
    '맞습니다. k가 크면 언더피팅이 발생하고 결정 경계가 부드러워집니다.',
    'k-NN은 학습 단계 없이 모든 계산을 예측 시 수행합니다.',
    'k가 클수록 이상치 영향이 줄어듭니다.',
  ]}
/>

<Quiz
  questionNumber={9}
  question="K-means 알고리즘의 한계가 아닌 것은?"
  options={['① K를 사전에 지정해야 한다', '② 비구형 군집 탐지가 어렵다', '③ 이상치에 민감하다', '④ 계층적 구조를 파악할 수 있다']}
  correctAnswer={4}
  explanation="K-means는 K를 미리 지정해야 하고, 구형 군집에 최적화되어 있으며, 이상치에 민감한 한계가 있습니다. 계층적 구조 파악은 계층적 군집화(Hierarchical Clustering)의 특징이며 K-means의 기능이 아닙니다."
  optionExplanations={[
    'K를 사전에 지정해야 하는 것은 K-means의 한계입니다.',
    '비구형 군집 탐지 어려움은 K-means의 한계입니다.',
    '이상치에 민감한 것은 K-means의 한계입니다.',
    '맞습니다. 계층적 구조 파악은 K-means가 아닌 계층적 군집화의 특징입니다.',
  ]}
/>

<Quiz
  questionNumber={10}
  question="PCA에서 누적 설명 분산 비율을 사용하는 이유는?"
  options={['① 모델의 정확도를 높이기 위해', '② 원하는 정보 보존 비율을 유지하면서 최소한의 주성분 수를 선택하기 위해', '③ 데이터의 평균을 계산하기 위해', '④ 클러스터 수를 결정하기 위해']}
  correctAnswer={2}
  explanation="누적 설명 분산 비율은 선택한 주성분들이 전체 데이터 분산 중 얼마나 설명하는지를 나타냅니다. 이를 통해 원하는 정보 보존 수준(예: 95%)을 달성하는 최소 주성분 수를 선택합니다."
  optionExplanations={[
    '정확도 향상은 주성분 수 선택의 직접 목적이 아닙니다.',
    '맞습니다. 정보 보존 비율을 유지하면서 최소 주성분 수를 선택하기 위해 사용합니다.',
    '평균 계산은 PCA 전처리 단계로 별도 수행합니다.',
    '클러스터 수 결정은 군집화 관련 문제입니다.',
  ]}
/>

<Quiz
  questionNumber={11}
  question="신경망에서 은닉층에 가장 많이 사용되는 활성화 함수는?"
  options={['① Sigmoid', '② Softmax', '③ ReLU', '④ Linear']}
  correctAnswer={3}
  explanation="ReLU(Rectified Linear Unit)는 계산이 단순하고 양수 구간에서 기울기 소실 문제가 없어 은닉층의 기본 활성화 함수로 가장 널리 사용됩니다. Sigmoid는 출력층(이진 분류), Softmax는 출력층(다중 분류)에 주로 사용됩니다."
  optionExplanations={[
    'Sigmoid는 기울기 소실 문제가 있어 은닉층보다 출력층에 사용됩니다.',
    'Softmax는 다중 분류 출력층에 사용됩니다.',
    '맞습니다. ReLU는 은닉층의 기본 활성화 함수입니다.',
    'Linear(선형) 활성화는 비선형성을 제공하지 않아 은닉층에 적합하지 않습니다.',
  ]}
/>

<Quiz
  questionNumber={12}
  question="역전파(Backpropagation)에서 기울기가 소실되는 주된 원인은?"
  options={['① 학습 데이터가 너무 많다', '② Sigmoid/Tanh 함수 포화 영역에서 기울기가 0에 가까워짐', '③ 학습률이 너무 작다', '④ 배치 크기가 너무 크다']}
  correctAnswer={2}
  explanation="Sigmoid와 Tanh 함수는 입력이 매우 크거나 작을 때 포화되어 기울기가 0에 가까워집니다. 체인룰로 이 기울기들을 곱하면 깊은 층으로 갈수록 기울기가 거의 0이 되는 기울기 소실이 발생합니다."
  optionExplanations={[
    '데이터 수는 기울기 소실의 원인이 아닙니다.',
    '맞습니다. 포화 함수의 기울기가 0에 수렴하여 기울기 소실이 발생합니다.',
    '학습률이 작으면 수렴이 느릴 수 있지만 기울기 소실의 원인은 아닙니다.',
    '배치 크기는 기울기 소실의 직접 원인이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={13}
  question="정밀도(Precision)가 중요한 상황은?"
  options={['① 암 진단 — 실제 환자를 놓치면 안 된다', '② 스팸 필터 — 정상 메일을 스팸으로 잘못 분류하면 안 된다', '③ 지진 경보 — 지진을 놓치면 안 된다', '④ 바이러스 감염 선별 — 감염자를 놓치면 안 된다']}
  correctAnswer={2}
  explanation="정밀도(Precision)는 양성으로 예측한 것 중 실제 양성의 비율로, FP(거짓 양성)를 줄이는 것이 목적입니다. 스팸 필터에서 정상 메일(음성)을 스팸(양성)으로 잘못 분류하는 것이 치명적인 경우에 정밀도가 중요합니다."
  optionExplanations={[
    '암 진단은 실제 양성(환자)을 놓치지 않아야 하므로 재현율이 중요합니다.',
    '맞습니다. 스팸 필터에서 정상 메일을 스팸으로 잘못 분류(FP)를 줄이는 것이 중요합니다.',
    '지진 경보는 놓치면 안 되므로 재현율이 중요합니다.',
    '바이러스 선별은 감염자(양성)를 놓치면 안 되므로 재현율이 중요합니다.',
  ]}
/>

<Quiz
  questionNumber={14}
  question="L2 정규화(Ridge)와 L1 정규화(Lasso)의 차이는?"
  options={['① L2는 일부 가중치를 0으로 만들고, L1은 모든 가중치를 균등 축소한다', '② L1은 일부 가중치를 0으로 만들고, L2는 모든 가중치를 균등 축소한다', '③ L1과 L2 모두 동일한 효과를 낸다', '④ L2는 학습률을 자동 조정하고 L1은 드롭아웃을 수행한다']}
  correctAnswer={2}
  explanation="L1(Lasso)은 절댓값 페널티를 사용하여 일부 가중치를 정확히 0으로 만들어 희소 모델을 생성합니다. L2(Ridge)는 제곱 페널티를 사용하여 모든 가중치를 0에 가깝게 균등하게 축소하지만 정확히 0이 되지는 않습니다."
  optionExplanations={[
    'L1이 일부 가중치를 0으로 만들고 L2가 균등 축소입니다. 설명이 반대입니다.',
    '맞습니다. L1은 희소화(일부 0), L2는 균등 축소입니다.',
    'L1과 L2는 서로 다른 특성을 가집니다.',
    'L1/L2는 손실 함수의 정규화 항이며 학습률/드롭아웃과 관련이 없습니다.',
  ]}
/>

<Quiz
  questionNumber={15}
  question="배치 정규화(Batch Normalization)를 적용하는 위치는 일반적으로?"
  options={['① 입력 데이터 전처리 단계', '② 각 층의 선형 변환 후, 활성화 함수 적용 전(또는 후)', '③ 손실 함수 계산 직전', '④ 역전파 완료 후']}
  correctAnswer={2}
  explanation="배치 정규화는 일반적으로 각 층의 선형 변환(가중치 곱) 이후, 활성화 함수 적용 전에 위치합니다. 일부 연구에서는 활성화 함수 후에 적용하기도 합니다."
  optionExplanations={[
    '배치 정규화는 모델 내부 각 층에 적용하는 것입니다.',
    '맞습니다. 배치 정규화는 각 층의 선형 변환 후 활성화 전에 위치합니다.',
    '손실 함수 직전은 배치 정규화 적용 위치가 아닙니다.',
    '역전파 후는 가중치 업데이트 단계입니다.',
  ]}
/>

<Quiz
  questionNumber={16}
  question="Stratified k-Fold 교차 검증을 사용해야 하는 경우는?"
  options={['① 데이터가 충분히 많을 때', '② 클래스 불균형이 있는 데이터셋에서 각 폴드의 클래스 비율을 유지해야 할 때', '③ 회귀 문제에서 항상 사용해야 할 때', '④ 테스트 세트를 여러 번 사용하고 싶을 때']}
  correctAnswer={2}
  explanation="Stratified k-Fold는 각 폴드에서 클래스 비율이 전체 데이터셋과 동일하게 유지되도록 분할합니다. 클래스 불균형이 심한 데이터셋에서 특정 폴드에 한 클래스가 쏠리는 것을 방지합니다."
  optionExplanations={[
    '데이터 크기보다 클래스 불균형 여부가 사용 기준입니다.',
    '맞습니다. 클래스 불균형 데이터에서 각 폴드의 클래스 비율을 유지하기 위해 사용합니다.',
    '회귀 문제에는 일반 k-Fold가 적합하며 Stratified는 분류 문제에 주로 사용됩니다.',
    '테스트 세트 반복 사용은 데이터 누수 문제가 있습니다.',
  ]}
/>

<Quiz
  questionNumber={17}
  question="나이브 베이즈(Naive Bayes) 분류기의 '나이브(Naive)'한 가정은?"
  options={['① 모든 클래스의 사전 확률이 동일하다', '② 모든 특징이 서로 조건부 독립이다', '③ 데이터가 정규 분포를 따른다', '④ 특징 수가 샘플 수보다 작다']}
  correctAnswer={2}
  explanation="나이브 베이즈는 클래스가 주어졌을 때 모든 특징이 서로 조건부 독립이라고 가정합니다. 실제로는 이 가정이 성립하지 않는 경우가 많지만, 단순함에도 불구하고 텍스트 분류 등에서 좋은 성능을 보입니다."
  optionExplanations={[
    '사전 확률 동일 가정은 나이브 베이즈의 '나이브' 가정이 아닙니다.',
    '맞습니다. 나이브 베이즈는 특징들이 서로 조건부 독립임을 가정합니다.',
    '정규 분포 가정은 Gaussian Naive Bayes의 부가 가정입니다.',
    '특징 수와 샘플 수 비교는 나이브 베이즈의 가정이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={18}
  question="앙상블 방법 중 배깅(Bagging)의 핵심 원리는?"
  options={['① 이전 모델의 오류에 집중하여 순차 학습', '② 부트스트랩 샘플링으로 다양한 훈련 세트를 만들어 독립적 모델 학습 후 통합', '③ 서로 다른 알고리즘을 투표로 결합', '④ 하나의 강한 모델만 사용']}
  correctAnswer={2}
  explanation="배깅(Bootstrap Aggregating)은 훈련 데이터에서 복원 추출(부트스트랩 샘플링)로 여러 다양한 훈련 세트를 만들고, 각 세트로 독립적인 모델을 학습한 후 예측을 통합(평균/다수결)합니다."
  optionExplanations={[
    '이전 모델 오류에 집중하는 것은 부스팅(Boosting)입니다.',
    '맞습니다. 배깅은 부트스트랩 샘플링 + 독립 학습 + 통합 과정입니다.',
    '서로 다른 알고리즘 투표는 보팅(Voting) 앙상블입니다.',
    '단일 강한 모델은 앙상블이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={19}
  question="t-SNE의 perplexity 하이퍼파라미터는 무엇을 제어하는가?"
  options={['① 주성분의 수', '② 학습률', '③ 각 점의 지역 이웃 수(밀도)', '④ 클러스터 수']}
  correctAnswer={3}
  explanation="t-SNE의 perplexity는 각 데이터 포인트를 둘러싸는 지역 이웃의 효과적인 수를 제어합니다. 값이 작으면 지역적 구조를, 값이 크면 전역적 구조를 더 반영합니다. 일반적으로 5~50 사이를 사용합니다."
  optionExplanations={[
    '주성분 수는 PCA의 하이퍼파라미터입니다.',
    '학습률은 경사 하강법 관련 파라미터입니다.',
    '맞습니다. perplexity는 각 점 주변의 효과적인 이웃 수를 제어합니다.',
    '클러스터 수는 K-means의 K 파라미터입니다.',
  ]}
/>

<Quiz
  questionNumber={20}
  question="다음 중 특징 스케일링이 반드시 필요한 알고리즘은?"
  options={['① 의사결정나무(Decision Tree)', '② 랜덤 포레스트(Random Forest)', '③ SVM(Support Vector Machine)과 KNN', '④ 나이브 베이즈(Naive Bayes)']}
  correctAnswer={3}
  explanation="SVM은 최대 마진을 찾고, KNN은 거리를 기반으로 예측하므로 특징의 스케일에 민감합니다. 거리/커널 계산에 모든 특징이 동등하게 기여해야 합니다. 반면 나무 기반 알고리즘(의사결정나무, 랜덤 포레스트)과 나이브 베이즈는 스케일에 영향받지 않습니다."
  optionExplanations={[
    '의사결정나무는 분기 조건에 임계값을 사용하므로 스케일에 영향받지 않습니다.',
    '랜덤 포레스트도 나무 기반이므로 스케일 불변입니다.',
    '맞습니다. SVM과 KNN은 거리/커널 계산을 사용하므로 스케일링이 필수입니다.',
    '나이브 베이즈는 확률 계산 기반으로 스케일에 영향받지 않습니다.',
  ]}
/>

<Quiz
  questionNumber={21}
  question="오버피팅(Overfitting)을 해결하는 방법이 아닌 것은?"
  options={['① 정규화(L1/L2) 적용', '② 드롭아웃 적용', '③ 더 많은 훈련 데이터 확보', '④ 모델 복잡도를 높임']}
  correctAnswer={4}
  explanation="오버피팅은 모델이 너무 복잡하거나 데이터가 부족할 때 발생합니다. 모델 복잡도를 높이는 것은 오버피팅을 더 심화시킵니다. 정규화, 드롭아웃, 더 많은 데이터, 모델 단순화가 오버피팅 해결책입니다."
  optionExplanations={[
    '정규화는 가중치 크기를 제한하여 오버피팅을 줄입니다.',
    '드롭아웃은 뉴런을 무작위 비활성화하여 오버피팅을 줄입니다.',
    '더 많은 훈련 데이터는 일반화 성능을 높여 오버피팅을 줄입니다.',
    '맞습니다. 모델 복잡도를 높이면 오버피팅이 심화됩니다.',
  ]}
/>

<Quiz
  questionNumber={22}
  question="경사 하강법(Gradient Descent)에서 학습률(Learning Rate)이 너무 크면?"
  options={['① 수렴 속도가 느려진다', '② 최솟값을 지나쳐 발산할 수 있다', '③ 항상 전역 최솟값에 수렴한다', '④ 기울기 소실이 발생한다']}
  correctAnswer={2}
  explanation="학습률이 너무 크면 각 업데이트 단계에서 최솟값을 지나쳐 반대편으로 이동하는 과도한 보정이 발생합니다. 이를 반복하면 손실이 발산하거나 진동할 수 있습니다."
  optionExplanations={[
    '수렴 속도가 느려지는 것은 학습률이 너무 작을 때입니다.',
    '맞습니다. 학습률이 너무 크면 최솟값을 지나쳐 발산할 수 있습니다.',
    '학습률이 크면 전역 최솟값 수렴이 보장되지 않습니다.',
    '기울기 소실은 학습률과 직접 관련이 없습니다.',
  ]}
/>

## 참고 자료

- [국가평생교육진흥원 독학학위제](https://bdes.nile.or.kr/nile/study/nStudy1_1.do)
- Aurélien Géron, "Hands-On Machine Learning with Scikit-Learn, Keras and TensorFlow"

---
title: '15. 컴퓨터네트워크 예상기출문제'
description: '독학사 3단계 컴퓨터네트워크 과목 전 범위를 아우르는 예상 기출문제 20개로 실전 감각을 높인다.'
---

<Callout type="info">
  **이번 문서의 목표:** 전 단원 핵심 개념을 4지선다형 문제로 점검하고, 실제 시험에 대비하는 실전 감각을 익힌다.
</Callout>

## 예상 기출문제 20선

<Quiz
  questionNumber={1}
  question="모든 단말기가 하나의 선로에 연결되어 그 선로를 공용으로 사용하는 네트워크는?"
  options={['성형 네트워크', '버스형 네트워크', '환형 네트워크', '계층형 네트워크']}
  correctAnswer={2}
  explanation="버스형 토폴로지는 모든 노드가 하나의 공유 선로(버스)에 연결됩니다."
  optionExplanations={['성형은 중앙 허브에 연결된 구조입니다.', '버스형이 하나의 선로를 공용으로 사용합니다. (정답)', '환형은 노드들이 원형으로 연결된 구조입니다.', '계층형은 트리 구조입니다.']}
/>

<Quiz
  questionNumber={2}
  question="OSI 7계층을 최하위 계층에서부터 순서대로 나열한 것은?"
  options={['물리-데이터링크-네트워크-전송-세션-표현-응용', '응용-표현-세션-전송-네트워크-데이터링크-물리', '물리-네트워크-데이터링크-전송-세션-표현-응용', '데이터링크-물리-네트워크-전송-세션-표현-응용']}
  correctAnswer={1}
  explanation="OSI 7계층: 물리(1)-데이터링크(2)-네트워크(3)-전송(4)-세션(5)-표현(6)-응용(7) 순입니다."
  optionExplanations={['올바른 순서입니다. (정답)', '이것은 역순(7→1)입니다.', '데이터링크와 네트워크의 순서가 바뀌었습니다.', '데이터링크와 물리의 순서가 바뀌었습니다.']}
/>

<Quiz
  questionNumber={3}
  question="다음 중 물리 계층에서 작동하는 네트워크 장비는?"
  options={['게이트웨이', '라우터', '브리지', '리피터']}
  correctAnswer={4}
  explanation="리피터는 신호를 증폭하여 재전송하는 물리 계층(1계층) 장비입니다."
  optionExplanations={['게이트웨이는 응용 계층(모든 계층)에서 동작합니다.', '라우터는 네트워크 계층(3계층) 장비입니다.', '브리지는 데이터 링크 계층(2계층) 장비입니다.', '리피터는 물리 계층(1계층) 장비입니다. (정답)']}
/>

<Quiz
  questionNumber={4}
  question="다음 괄호 안에 알맞은 용어는? '( )는 인터넷에서 동일한 그룹에 속한 호스트들에게 메시지를 전송할 수 있는 멀티캐스트를 위해 사용되는 프로토콜이다.'"
  options={['IGMP', 'ICMP', 'ARP', 'RARP']}
  correctAnswer={1}
  explanation="IGMP(Internet Group Management Protocol)는 멀티캐스트 그룹 관리를 위한 프로토콜입니다."
  optionExplanations={['IGMP는 멀티캐스트 그룹 관리 프로토콜입니다. (정답)', 'ICMP는 오류 보고와 진단을 위한 프로토콜입니다.', 'ARP는 IP를 MAC 주소로 변환합니다.', 'RARP는 MAC을 IP 주소로 변환합니다.']}
/>

<Quiz
  questionNumber={5}
  question="이메일 전송을 위한 응용 계층의 프로토콜은?"
  options={['FTP', 'TCP', 'SMTP', 'DHCP']}
  correctAnswer={3}
  explanation="SMTP(Simple Mail Transfer Protocol)는 이메일 전송을 위한 응용 계층 프로토콜로 포트 25를 사용합니다."
  optionExplanations={['FTP는 파일 전송 프로토콜입니다.', 'TCP는 전송 계층 프로토콜입니다.', 'SMTP는 이메일 전송을 위한 프로토콜입니다. (정답)', 'DHCP는 IP 주소 자동 할당 프로토콜입니다.']}
/>

<Quiz
  questionNumber={6}
  question="다음 괄호 안에 들어갈 용어로서 옳은 것을 순서대로 고르시오. '( ① ) 암호화 방법은 암호화 키와 복호화 키가 서로 같다는 특징이 있으며 DES 암호화 방식이 대표적이다. ( ② ) 암호화 방식은 암호화 키와 복호화 키가 서로 같지 않으며 RSA 암호화 방식이 대표적이다.'"
  options={['① 대칭키, ② 공개키', '① 공개키, ② 대칭키', '① 비대칭키, ② 대칭키', '① 공개키, ② 비밀키']}
  correctAnswer={1}
  explanation="대칭키(비밀키) 암호화는 같은 키로 암호화·복호화하며 DES가 대표적입니다. 공개키(비대칭키) 암호화는 키 쌍이 다르며 RSA가 대표적입니다."
  optionExplanations={['대칭키는 같은 키(DES), 공개키는 다른 키(RSA)가 맞습니다. (정답)', '순서가 뒤바뀌었습니다.', '비대칭키와 대칭키의 순서가 잘못되었습니다.', '용어가 정확하지 않습니다.']}
/>

<Quiz
  questionNumber={7}
  question="송신 측이 프레임에 일련번호를 부여하여 연속 전송하다가 REJ와 함께 수신된 번호에 해당하는 프레임부터 다시 연속 전송하는 ARQ 방식은?"
  options={['Stop-and-Wait ARQ', 'Selective-Repeat ARQ', 'Go-Back-N ARQ', 'Adaptive ARQ']}
  correctAnswer={3}
  explanation="Go-Back-N ARQ는 오류 발생 프레임부터 이후 모든 프레임을 재전송합니다."
  optionExplanations={['Stop-and-Wait는 한 번에 한 프레임씩 전송합니다.', 'Selective-Repeat는 오류 프레임만 선택적으로 재전송합니다.', 'Go-Back-N ARQ가 REJ 번호부터 연속 재전송합니다. (정답)', 'Adaptive ARQ는 오류율에 따라 프레임 크기를 조절합니다.']}
/>

<Quiz
  questionNumber={8}
  question="ARP(Address Resolution Protocol)의 기능은?"
  options={['도메인 이름을 IP 주소로 변환', 'IP 주소를 MAC 주소로 변환', 'MAC 주소를 IP 주소로 변환', 'IP 주소를 도메인 이름으로 변환']}
  correctAnswer={2}
  explanation="ARP는 알고 있는 IP 주소로부터 해당 장치의 MAC 주소를 얻어내는 프로토콜입니다."
  optionExplanations={['도메인→IP 변환은 DNS입니다.', 'ARP는 IP 주소를 MAC 주소로 변환합니다. (정답)', 'MAC→IP 변환은 RARP입니다.', 'IP→도메인 변환은 역방향 DNS입니다.']}
/>

<Quiz
  questionNumber={9}
  question="TCP 3-way Handshake의 올바른 순서는?"
  options={['SYN → ACK → SYN+ACK', 'SYN → SYN+ACK → ACK', 'ACK → SYN → SYN+ACK', 'SYN+ACK → SYN → ACK']}
  correctAnswer={2}
  explanation="TCP 연결 수립: SYN(클라이언트) → SYN+ACK(서버) → ACK(클라이언트)의 3단계입니다."
  optionExplanations={['ACK가 SYN+ACK보다 먼저 오는 것은 틀린 순서입니다.', 'SYN → SYN+ACK → ACK가 올바른 순서입니다. (정답)', 'ACK가 첫 번째인 것은 잘못된 순서입니다.', 'SYN+ACK가 먼저 오는 것은 잘못된 순서입니다.']}
/>

<Quiz
  questionNumber={10}
  question="IP 주소 클래스 B의 첫 번째 옥텟 범위는?"
  options={['1 ~ 126', '128 ~ 191', '192 ~ 223', '224 ~ 239']}
  correctAnswer={2}
  explanation="클래스 B는 첫 번째 옥텟이 128~191인 IP 주소입니다. 기본 서브넷 마스크는 255.255.0.0(/16)입니다."
  optionExplanations={['1~126은 클래스 A의 범위입니다.', '128~191은 클래스 B의 범위입니다. (정답)', '192~223은 클래스 C의 범위입니다.', '224~239는 클래스 D(멀티캐스트)의 범위입니다.']}
/>

<Quiz
  questionNumber={11}
  question="무선 LAN(Wi-Fi)에서 사용하는 매체 접근 제어 방식은?"
  options={['CSMA/CD', 'CSMA/CA', 'TDMA', 'Token Ring']}
  correctAnswer={2}
  explanation="무선 환경에서는 충돌 감지가 어려워 충돌 회피 방식인 CSMA/CA를 사용합니다. CSMA/CD는 유선 이더넷에서 사용합니다."
  optionExplanations={['CSMA/CD는 유선 이더넷에서 사용합니다.', 'CSMA/CA는 무선 LAN에서 사용하는 충돌 회피 방식입니다. (정답)', 'TDMA는 이동통신에서 사용합니다.', 'Token Ring은 IBM의 구형 LAN 기술입니다.']}
/>

<Quiz
  questionNumber={12}
  question="IPv6 주소의 길이는?"
  options={['32비트', '64비트', '128비트', '256비트']}
  correctAnswer={3}
  explanation="IPv6는 128비트 주소 체계를 사용하여 약 3.4 × 10의 38제곱 개의 주소를 제공합니다."
  optionExplanations={['32비트는 IPv4의 주소 길이입니다.', '64비트는 IPv6 주소 절반 크기입니다.', 'IPv6는 128비트 주소입니다. (정답)', '256비트는 IPv6 주소 길이가 아닙니다.']}
/>

<Quiz
  questionNumber={13}
  question="DNS(Domain Name System)가 사용하는 기본 포트 번호는?"
  options={['21', '25', '53', '80']}
  correctAnswer={3}
  explanation="DNS는 도메인 이름을 IP 주소로 변환하는 서비스로 포트 53을 사용합니다(주로 UDP)."
  optionExplanations={['포트 21은 FTP 제어 연결에 사용됩니다.', '포트 25는 SMTP(이메일 전송)에 사용됩니다.', 'DNS는 포트 53을 사용합니다. (정답)', '포트 80은 HTTP에 사용됩니다.']}
/>

<Quiz
  questionNumber={14}
  question="TCP Slow Start에서 혼잡 윈도우(cwnd)의 초기 크기와 증가 방식은?"
  options={['ssthresh에서 시작하여 선형 증가', '최대 크기에서 시작하여 감소', '1 MSS에서 시작하여 지수적 증가', '1 MSS에서 시작하여 선형 증가']}
  correctAnswer={3}
  explanation="Slow Start는 cwnd를 1 MSS에서 시작하여 ACK를 받을 때마다 지수적으로 증가합니다. ssthresh 도달 시 선형 증가로 전환합니다."
  optionExplanations={['ssthresh에서 시작하는 선형 증가는 혼잡 회피(CA) 단계입니다.', '최대 크기에서 시작하는 방식은 없습니다.', 'Slow Start는 1 MSS에서 시작하여 지수적으로 증가합니다. (정답)', '1 MSS에서 시작하지만 증가 방식은 지수적입니다.']}
/>

<Quiz
  questionNumber={15}
  question="서브넷 마스크 255.255.255.192를 CIDR 표기법으로 나타내면?"
  options={['/24', '/25', '/26', '/27']}
  correctAnswer={3}
  explanation="255.255.255.192 = 11111111.11111111.11111111.11000000 → 앞 26비트가 1이므로 /26입니다."
  optionExplanations={['/24는 255.255.255.0입니다.', '/25는 255.255.255.128입니다.', '255.255.255.192는 /26입니다. (정답)', '/27은 255.255.255.224입니다.']}
/>

<Quiz
  questionNumber={16}
  question="방화벽의 주요 역할이 아닌 것은?"
  options={['허가되지 않은 네트워크 접근 차단', 'IP 주소 필터링', '포트 번호 기반 트래픽 제어', 'IP 주소 자동 할당']}
  correctAnswer={4}
  explanation="IP 주소 자동 할당은 DHCP의 기능입니다. 방화벽은 트래픽을 검사하고 보안 규칙에 따라 허용·차단합니다."
  optionExplanations={['허가되지 않은 접근 차단은 방화벽의 핵심 기능입니다.', 'IP 주소 기반 필터링은 방화벽의 기능입니다.', '포트 번호 기반 제어는 방화벽의 기능입니다.', 'IP 주소 자동 할당은 DHCP의 기능입니다. (정답)']}
/>

<Quiz
  questionNumber={17}
  question="SDN(Software-Defined Networking)에서 OpenFlow 프로토콜의 역할은?"
  options={['이메일 트래픽을 관리한다', '암호화 터널을 생성한다', 'SDN 컨트롤러와 스위치 간 통신 인터페이스 역할을 한다', '도메인 이름을 IP로 변환한다']}
  correctAnswer={3}
  explanation="OpenFlow는 SDN 컨트롤러가 네트워크 스위치의 플로우 테이블을 원격으로 관리하기 위한 표준 남북방향 인터페이스 프로토콜입니다."
  optionExplanations={['이메일 트래픽 관리는 SMTP 관련 기능입니다.', '암호화 터널 생성은 IPSec, SSL VPN의 기능입니다.', 'OpenFlow는 SDN 컨트롤러와 스위치 간 통신 인터페이스입니다. (정답)', '도메인→IP 변환은 DNS의 기능입니다.']}
/>

<Quiz
  questionNumber={18}
  question="FTP에서 데이터 전송에 사용하는 포트 번호는?"
  options={['20', '21', '22', '23']}
  correctAnswer={1}
  explanation="FTP는 제어 연결에 포트 21, 데이터 전송에 포트 20을 사용합니다."
  optionExplanations={['포트 20은 FTP 데이터 전송에 사용됩니다. (정답)', '포트 21은 FTP 제어 연결에 사용됩니다.', '포트 22는 SSH에 사용됩니다.', '포트 23은 Telnet에 사용됩니다.']}
/>

<Quiz
  questionNumber={19}
  question="DHCP 동작 과정 DORA의 올바른 순서는?"
  options={['Discover → Request → Offer → Acknowledge', 'Discover → Offer → Request → Acknowledge', 'Offer → Discover → Request → Acknowledge', 'Request → Discover → Offer → Acknowledge']}
  correctAnswer={2}
  explanation="DHCP는 Discover(탐색) → Offer(제안) → Request(요청) → Acknowledge(확인)의 4단계로 동작합니다."
  optionExplanations={['Offer와 Request의 순서가 바뀌었습니다.', 'Discover → Offer → Request → Acknowledge가 올바른 순서입니다. (정답)', 'Offer가 Discover보다 먼저 오는 것은 잘못된 순서입니다.', 'Request가 첫 번째인 것은 잘못된 순서입니다.']}
/>

<Quiz
  questionNumber={20}
  question="IPv6에서 브로드캐스트를 대체하는 것은?"
  options={['유니캐스트(Unicast)', '멀티캐스트(Multicast)', '애니캐스트(Anycast)', '브로드캐스트를 동일하게 지원']}
  correctAnswer={2}
  explanation="IPv6에는 브로드캐스트가 없습니다. IPv4에서 브로드캐스트를 사용하던 기능들은 멀티캐스트로 대체됩니다."
  optionExplanations={['유니캐스트는 단일 수신자에게 전달하는 방식입니다.', 'IPv6에서 브로드캐스트 기능은 멀티캐스트로 대체됩니다. (정답)', '애니캐스트는 가장 가까운 단일 수신자에게 전달하는 방식입니다.', 'IPv6에는 브로드캐스트가 없습니다.']}
/>

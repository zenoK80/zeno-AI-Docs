---
title: '16. IP·라우팅 기출 유형'
description: 'IP 주소, 서브넷팅, 라우팅 관련 시험 대비 예상기출 문제 모음'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 공식 출제 범위를 바탕으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="IP 주소 클래스 중 가장 많은 네트워크를 지원하되 각 네트워크의 호스트 수는 적은 클래스는?"
  options={['①클래스 A', '②클래스 B', '③클래스 C', '④클래스 D']}
  correctAnswer={3}
  explanation="클래스 C는 네트워크 부분이 24비트로 넓어 많은 네트워크를 지원하지만, 호스트 부분은 8비트로 좁아 각 네트워크의 호스트 수가 적습니다."
  optionExplanations={['클래스 A는 적은 네트워크에 매우 많은 호스트를 지원합니다.', '클래스 B는 중간 규모입니다.', '맞습니다. 클래스 C입니다.', '클래스 D는 멀티캐스트용으로 별도 분류됩니다.']}
/>

<Quiz
  questionNumber={2}
  question="192.168.1.0/24를 8개의 서브넷으로 나누려면 몇 비트를 빌려야 하는가?"
  options={['①1비트', '②2비트', '③3비트', '④4비트']}
  correctAnswer={3}
  explanation="2^n≥8을 만족하는 최소 n은 3이므로, 3비트를 빌려야 8개의 서브넷을 만들 수 있습니다."
  optionExplanations={['1비트로는 2개의 서브넷만 가능합니다.', '2비트로는 4개의 서브넷만 가능합니다.', '맞습니다. 2^3=8이므로 3비트입니다.', '4비트는 16개의 서브넷을 만들 때 필요합니다.']}
/>

<Quiz
  questionNumber={3}
  question="ARP가 IP 주소로부터 알아내는 것은?"
  options={['①도메인 이름', '②MAC 주소', '③포트 번호', '④서브넷 마스크']}
  correctAnswer={2}
  explanation="ARP(Address Resolution Protocol)는 같은 네트워크 안에서 IP 주소에 대응하는 MAC 주소를 알아내는 프로토콜입니다."
  optionExplanations={['도메인 이름 변환은 DNS의 역할입니다.', '맞습니다. MAC 주소입니다.', '포트 번호와는 무관한 프로토콜입니다.', '서브넷 마스크는 별도로 설정되는 값입니다.']}
/>

<Quiz
  questionNumber={4}
  question="ICMP가 활용되는 대표적인 명령어는?"
  options={['①ping', '②ls', '③cd', '④grep']}
  correctAnswer={1}
  explanation="ping 명령어는 ICMP Echo Request/Reply 메시지를 이용해 목적지의 응답 여부를 확인합니다."
  optionExplanations={['맞습니다. ping입니다.', 'ls는 파일 목록을 보는 명령어로 네트워크와 무관합니다.', 'cd는 디렉터리 이동 명령어입니다.', 'grep은 텍스트 검색 명령어입니다.']}
/>

<Quiz
  questionNumber={5}
  question="여러 호스트에게 동시에 데이터를 전송하는 멀티캐스트를 관리하는 프로토콜은?"
  options={['①ARP', '②ICMP', '③IGMP', '④RARP']}
  correctAnswer={3}
  explanation="IGMP(Internet Group Management Protocol)는 멀티캐스트 그룹을 관리하는 프로토콜입니다."
  optionExplanations={['ARP는 IP-MAC 주소 변환에 사용됩니다.', 'ICMP는 네트워크 상태 점검·오류 알림에 사용됩니다.', '맞습니다. IGMP입니다.', 'RARP는 MAC 주소로 IP 주소를 알아내는 프로토콜입니다.']}
/>

<Quiz
  questionNumber={6}
  question="NAT(Network Address Translation)의 주된 목적은?"
  options={['①MAC 주소를 변경하기 위해', '②한정된 공인 IP 주소 자원을 절약하기 위해', '③암호화를 수행하기 위해', '④도메인 이름을 등록하기 위해']}
  correctAnswer={2}
  explanation="NAT는 하나의 공인 IP로 여러 사설 IP 기기가 인터넷을 함께 사용할 수 있게 해, 공인 IPv4 주소 자원을 절약합니다."
  optionExplanations={['MAC 주소가 아니라 IP 주소를 변환합니다.', '맞습니다. 공인 IP 자원 절약입니다.', '암호화와는 무관한 기능입니다.', '도메인 등록은 DNS와 관련된 별도의 절차입니다.']}
/>

<Quiz
  questionNumber={7}
  question="정적 라우팅과 동적 라우팅의 차이는?"
  options={['①정적 라우팅은 자동 갱신, 동적 라우팅은 수동 설정이다', '②정적 라우팅은 수동 설정, 동적 라우팅은 라우터끼리 정보를 교환해 자동 갱신한다', '③둘은 완전히 같은 방식이다', '④정적 라우팅은 대규모 네트워크 전용이다']}
  correctAnswer={2}
  explanation="정적 라우팅은 관리자가 수동으로 설정하고, 동적 라우팅은 라우터들이 서로 정보를 주고받아 자동으로 경로를 갱신합니다."
  optionExplanations={['설명이 반대로 되어 있습니다.', '맞습니다. 수동 설정 vs. 자동 갱신입니다.', '서로 다른 방식입니다.', '오히려 정적 라우팅은 소규모 네트워크에 적합합니다.']}
/>

<Quiz
  questionNumber={8}
  question="라우팅 경로를 결정할 때 홉 수(라우터 개수)만을 기준으로 삼는 프로토콜은?"
  options={['①RIP', '②OSPF', '③BGP', '④ARP']}
  correctAnswer={1}
  explanation="RIP(Routing Information Protocol)는 홉 수만을 기준으로 최적 경로를 결정하는 단순한 프로토콜입니다."
  optionExplanations={['맞습니다. RIP입니다.', 'OSPF는 회선 속도 등을 고려한 비용을 기준으로 삼습니다.', 'BGP는 정책 기반으로 경로를 결정합니다.', 'ARP는 라우팅 프로토콜이 아니라 주소 변환 프로토콜입니다.']}
/>

<Quiz
  questionNumber={9}
  question="서로 다른 조직(AS) 사이에서 인터넷 경로를 결정하는 프로토콜은?"
  options={['①RIP', '②OSPF', '③BGP', '④IGMP']}
  correctAnswer={3}
  explanation="BGP(Border Gateway Protocol)는 서로 다른 AS 사이(EGP)에서 사용되는 인터넷 핵심 라우팅 프로토콜입니다."
  optionExplanations={['RIP는 조직 내부(IGP)에서 사용됩니다.', 'OSPF도 조직 내부(IGP)에서 사용됩니다.', '맞습니다. BGP입니다.', 'IGMP는 멀티캐스트 관리 프로토콜입니다.']}
/>

<Quiz
  questionNumber={10}
  question="라우팅 테이블에서 '0.0.0.0/0' 항목이 나타내는 것은?"
  options={['①특정 목적지 전용 경로', '②다른 항목과 일치하지 않는 목적지를 위한 기본 경로', '③차단해야 할 IP 목록', '④사설 IP 대역']}
  correctAnswer={2}
  explanation="0.0.0.0/0은 라우팅 테이블의 다른 규칙과 일치하지 않는 모든 목적지를 처리하는 기본 경로(Default Route)입니다."
  optionExplanations={['특정 목적지가 아니라 나머지 전체를 위한 항목입니다.', '맞습니다. 기본 경로입니다.', '차단 목록과는 다른 개념입니다.', '사설 IP 대역과는 별개의 개념입니다.']}
/>

<Quiz
  questionNumber={11}
  question="사설 IP 대역에 해당하는 것은?"
  options={['①8.8.8.8', '②192.168.1.1', '③1.1.1.1', '④203.0.113.1']}
  correctAnswer={2}
  explanation="192.168.x.x는 대표적인 사설 IP 대역입니다."
  optionExplanations={['이는 공개 DNS 서버로 사용되는 공인 IP입니다.', '맞습니다. 사설 IP 대역입니다.', '이 역시 공인 IP(공개 DNS)입니다.', '이는 예시용 공인 IP 대역입니다.']}
/>

<Quiz
  questionNumber={12}
  question="/26 서브넷의 실제 사용 가능한 호스트 개수는?"
  options={['①64개', '②63개', '③62개', '④32개']}
  correctAnswer={3}
  explanation="/26은 호스트 비트 6개로 2^6=64개 중 네트워크·브로드캐스트 주소 2개를 제외한 62개가 사용 가능합니다."
  optionExplanations={['전체 개수이며 예약 주소를 제외하지 않은 값입니다.', '하나만 제외한 값입니다.', '맞습니다. 64-2=62개입니다.', '계산이 잘못되었습니다.']}
/>

<Quiz
  questionNumber={13}
  question="OSPF가 RIP보다 정교한 경로를 계산할 수 있는 이유는?"
  options={['①OSPF는 IP 주소를 사용하지 않기 때문에', '②OSPF는 홉 수뿐 아니라 회선 속도 등을 반영한 비용을 고려하기 때문에', '③OSPF는 사설 IP만 지원하기 때문에', '④OSPF는 UDP를 전혀 사용하지 않기 때문에']}
  correctAnswer={2}
  explanation="OSPF는 단순 홉 수가 아니라 회선 속도 등을 반영한 비용을 계산해 더 정교한 경로를 찾습니다."
  optionExplanations={['OSPF도 IP 기반 라우팅 프로토콜입니다.', '맞습니다. 비용 기반 계산이 핵심입니다.', '공인·사설 IP 모두 다룰 수 있습니다.', '이 설명은 OSPF의 특징과 직접 관련이 없습니다.']}
/>

<Quiz
  questionNumber={14}
  question="TTL(Time To Live) 필드의 역할은?"
  options={['①패킷의 크기를 지정한다', '②패킷이 무한히 네트워크를 떠도는 것을 방지한다', '③암호화 여부를 나타낸다', '④포트 번호를 지정한다']}
  correctAnswer={2}
  explanation="TTL은 라우터를 지날 때마다 감소하며 0이 되면 패킷을 폐기해, 라우팅 루프로 인한 무한 순환을 방지합니다."
  optionExplanations={['크기 정보와는 다른 필드입니다.', '맞습니다. 무한 루프 방지입니다.', '암호화 여부는 다른 필드나 프로토콜(SSL/TLS)에서 다룹니다.', '포트 번호는 전송 계층 헤더에 포함됩니다.']}
/>

<Quiz
  questionNumber={15}
  question="IGP(내부 게이트웨이 프로토콜)에 해당하는 것을 모두 포함하는 조합은?"
  options={['①RIP, OSPF', '②BGP만', '③ARP, ICMP', '④RIP, BGP']}
  correctAnswer={1}
  explanation="RIP와 OSPF는 하나의 조직(AS) 내부에서 사용되는 대표적인 IGP입니다."
  optionExplanations={['맞습니다. RIP와 OSPF입니다.', 'BGP는 EGP(조직 간)에 해당합니다.', 'ARP, ICMP는 라우팅 프로토콜이 아닙니다.', 'BGP는 IGP가 아니라 EGP입니다.']}
/>

---
title: '18. 자료구조 예상기출문제'
description: '자료구조 전 범위(포인터~해싱)를 아우르는 예상기출 문제 20문항입니다.'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 공식 출제 범위를 바탕으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="포인터(Pointer)가 저장하는 것은?"
  options={['①실제 데이터 값 자체', '②다른 변수가 저장된 메모리 주소', '③변수의 이름', '④프로그램의 실행 시간']}
  correctAnswer={2}
  explanation="포인터는 값 자체가 아니라, 다른 변수가 위치한 메모리 주소를 저장하는 변수입니다."
  optionExplanations={['이는 일반 변수에 대한 설명입니다.', '맞습니다. 포인터는 주소를 저장합니다.', '변수 이름과는 다른 개념입니다.', '실행 시간과는 무관합니다.']}
/>

<Quiz
  questionNumber={2}
  question="배열의 인덱스로 특정 위치의 값을 바로 가져오는 연산의 시간복잡도는?"
  options={['①O(n)', '②O(n²)', '③O(1)', '④O(log n)']}
  correctAnswer={3}
  explanation="인덱스 접근은 데이터 개수와 무관하게 항상 일정한 시간이 걸리는 O(1)(상수 시간) 연산입니다."
  optionExplanations={['데이터를 순차적으로 확인하는 경우의 복잡도입니다.', '이중 반복이 필요한 경우의 복잡도입니다.', '맞습니다. O(1)입니다.', '매번 절반씩 줄여나가는 경우의 복잡도입니다.']}
/>

<Quiz
  questionNumber={3}
  question="재귀 함수에 기저 조건(Base Case)이 없으면 발생하는 문제는?"
  options={['①실행 속도가 빨라진다', '②함수가 무한히 자신을 호출해 스택 오버플로우가 발생한다', '③결과가 항상 0이 된다', '④컴파일 오류가 즉시 발생한다']}
  correctAnswer={2}
  explanation="기저 조건이 없으면 재귀 호출을 멈출 방법이 없어, 무한히 호출되다 스택이 가득 차 오버플로우가 발생합니다."
  optionExplanations={['오히려 무한 호출로 인해 실행이 멈추지 않습니다.', '맞습니다. 스택 오버플로우가 발생합니다.', '결과를 얻기 전에 오류가 발생합니다.', '문법상 오류가 아니라 실행 중 발생하는 런타임 오류입니다.']}
/>

<Quiz
  questionNumber={4}
  question="희소행렬(Sparse Matrix)을 (행, 열, 값) 형태로 표현하는 주된 이유는?"
  options={['①계산 속도를 항상 느리게 만들기 위해', '②대부분이 0인 값을 저장하지 않아 메모리를 절약하기 위해', '③행렬을 사용하지 않기 위해', '④2차원 배열을 사용할 수 없어서']}
  correctAnswer={2}
  explanation="희소행렬은 대부분의 값이 0이므로, 0이 아닌 값만 기록해 메모리 낭비를 줄입니다."
  optionExplanations={['오히려 메모리 절약을 통해 효율을 높이는 것이 목적입니다.', '맞습니다. 메모리 절약이 핵심 목적입니다.', '행렬을 여전히 표현하고 사용합니다.', '2차원 배열도 사용 가능하지만 비효율적이라 대안을 사용하는 것입니다.']}
/>

<Quiz
  questionNumber={5}
  question="이중 연결 리스트가 단순 연결 리스트보다 갖는 장점은?"
  options={['①메모리를 덜 사용한다', '②양방향으로 이동할 수 있고 삭제 시 이전 노드를 바로 알 수 있다', '③삽입이 불가능하다', '④항상 배열보다 빠르다']}
  correctAnswer={2}
  explanation="이중 연결 리스트는 이전 노드도 가리키고 있어 양방향 이동이 가능하고, 삭제 시 이전 노드 탐색이 필요 없습니다."
  optionExplanations={['오히려 포인터가 2개라 메모리를 더 사용합니다.', '맞습니다. 양방향 이동과 편리한 삭제가 장점입니다.', '이중 연결 리스트도 삽입이 가능합니다.', '연산에 따라 배열이 더 빠른 경우도 있습니다.']}
/>

<Quiz
  questionNumber={6}
  question="후위 표기식 '5 2 -'를 스택으로 계산한 결과는?"
  options={['①3', '②-3', '③7', '④10']}
  correctAnswer={1}
  explanation="push(5), push(2) 후 '-'를 만나 pop 2, pop 5 순서로 꺼내 5-2=3을 계산합니다."
  optionExplanations={['맞습니다. 5-2=3입니다.', '피연산자 순서를 반대로 계산한 값입니다.', '덧셈으로 잘못 계산한 값입니다.', '곱셈으로 잘못 계산한 값입니다.']}
/>

<Quiz
  questionNumber={7}
  question="원형 큐에서 크기 6인 배열의 현재 Rear가 인덱스 5일 때, 다음 위치는?"
  options={['①5', '②6', '③0', '④에러']}
  correctAnswer={3}
  explanation="(5+1) mod 6 = 6 mod 6 = 0으로, 다시 처음(인덱스 0)으로 순환합니다."
  optionExplanations={['현재 위치 그대로입니다.', '배열의 유효 인덱스 범위(0~5)를 벗어난 값입니다.', '맞습니다. 나머지 연산으로 0으로 순환합니다.', '원형 큐는 정상적으로 순환하며 에러가 발생하지 않습니다.']}
/>

<Quiz
  questionNumber={8}
  question="전위 순회(Preorder)의 방문 순서는?"
  options={['①왼쪽→루트→오른쪽', '②루트→왼쪽→오른쪽', '③왼쪽→오른쪽→루트', '④오른쪽→왼쪽→루트']}
  correctAnswer={2}
  explanation="전위 순회는 루트를 가장 먼저(Pre) 방문한 뒤 왼쪽, 오른쪽 서브트리 순으로 방문합니다."
  optionExplanations={['이는 중위 순회입니다.', '맞습니다. 루트가 가장 먼저입니다.', '이는 후위 순회입니다.', '표준 순회 방식이 아닙니다.']}
/>

<Quiz
  questionNumber={9}
  question="이진 탐색 트리를 중위 순회했을 때 얻어지는 결과는?"
  options={['①무작위 순서', '②정렬된 순서', '③역순 정렬', '④항상 같은 값만 반복']}
  correctAnswer={2}
  explanation="이진 탐색 트리를 중위 순회하면 노드들이 정렬된 순서대로 출력되는 중요한 성질이 있습니다."
  optionExplanations={['무작위가 아닌 규칙적인 순서입니다.', '맞습니다. 정렬된 순서를 얻습니다.', '오름차순으로 정렬되지 역순이 아닙니다.', '서로 다른 노드 값들이 출력됩니다.']}
/>

<Quiz
  questionNumber={10}
  question="최대 히프(Max Heap)의 규칙은?"
  options={['①왼쪽<부모<오른쪽', '②부모 값이 자식 값보다 크거나 같다', '③모든 노드가 정렬되어 있다', '④자식이 항상 2개다']}
  correctAnswer={2}
  explanation="최대 히프는 부모 값이 항상 자식 값보다 크거나 같은 규칙을 가집니다."
  optionExplanations={['이는 이진 탐색 트리(BST)의 규칙입니다.', '맞습니다. 부모≥자식이 최대 히프의 규칙입니다.', '히프는 부모-자식 관계만 규칙이 있고 전체가 정렬된 것은 아닙니다.', '리프 노드는 자식이 없을 수 있습니다.']}
/>

<Quiz
  questionNumber={11}
  question="B-트리의 핵심 균형 조건은?"
  options={['①루트가 항상 가장 큰 값이다', '②모든 리프 노드가 항상 같은 레벨(깊이)에 위치한다', '③모든 노드가 자식을 1개만 가진다', '④키를 저장하지 않는다']}
  correctAnswer={2}
  explanation="B-트리는 모든 리프 노드가 항상 같은 깊이에 있도록 유지해, 탐색 시간을 일정하게 보장합니다."
  optionExplanations={['루트의 값 크기와는 무관한 조건입니다.', '맞습니다. 모든 리프가 같은 깊이라는 것이 핵심 조건입니다.', 'B-트리는 여러 자식을 가질 수 있습니다.', 'B-트리 노드는 여러 개의 키를 저장합니다.']}
/>

<Quiz
  questionNumber={12}
  question="인접행렬로 그래프를 표현할 때의 특징은?"
  options={['①정점 수와 무관하게 항상 같은 공간을 사용한다', '②두 정점의 연결 여부를 O(1)로 확인 가능하지만 O(n²) 공간이 필요하다', '③연결 여부를 확인할 수 없다', '④메모리를 전혀 사용하지 않는다']}
  correctAnswer={2}
  explanation="인접행렬은 n×n 크기의 표를 사용해, 연결 여부는 즉시 확인 가능하지만 정점 수의 제곱에 비례하는 공간이 필요합니다."
  optionExplanations={['정점 수(n)의 제곱에 비례해 공간이 늘어납니다.', '맞습니다. O(1) 확인과 O(n²) 공간이 특징입니다.', '오히려 연결 여부를 매우 빠르게 확인할 수 있습니다.', '행렬 저장을 위한 메모리가 필요합니다.']}
/>

<Quiz
  questionNumber={13}
  question="가중치가 없는 그래프에서 최단 경로를 찾는 데 적합한 탐색 방식은?"
  options={['①DFS', '②BFS', '③임의 순서 탐색', '④정렬']}
  correctAnswer={2}
  explanation="BFS는 가까운 정점부터 방문하므로, 처음 방문한 시점이 곧 최소 간선 수(최단 경로)를 의미합니다."
  optionExplanations={['DFS는 깊이 우선이라 최단 경로 보장이 어렵습니다.', '맞습니다. BFS가 가중치 없는 그래프의 최단 경로 탐색에 적합합니다.', '체계적인 탐색 순서가 필요합니다.', '정렬은 탐색 방법이 아닙니다.']}
/>

<Quiz
  questionNumber={14}
  question="다익스트라 알고리즘이 계산하는 것은?"
  options={['①그래프의 정점 개수', '②시작 정점에서 다른 모든 정점까지의 최단 거리', '③그래프의 간선 개수', '④그래프가 트리인지 여부']}
  correctAnswer={2}
  explanation="다익스트라 알고리즘은 하나의 시작 정점에서 다른 모든 정점까지의 최단 거리를 계산합니다."
  optionExplanations={['정점 개수를 세는 것이 목적이 아닙니다.', '맞습니다. 최단 거리 계산이 핵심 목적입니다.', '간선 개수를 세는 것이 목적이 아닙니다.', '트리 여부 판별과는 다른 목적입니다.']}
/>

<Quiz
  questionNumber={15}
  question="퀵 정렬이 최악의 경우 O(n²)까지 느려질 수 있는 이유는?"
  options={['①메모리가 부족해서', '②피벗을 잘못 선택하면 데이터가 불균형하게 나뉠 수 있어서', '③합병 과정이 느려서', '④히프 구조가 깨져서']}
  correctAnswer={2}
  explanation="피벗 선택이 나쁘면 데이터가 한쪽으로 치우쳐 나뉘어 성능이 급격히 저하됩니다."
  optionExplanations={['메모리 부족이 근본 원인이 아닙니다.', '맞습니다. 나쁜 피벗 선택이 원인입니다.', '합병은 합병 정렬의 과정입니다.', '퀵 정렬은 히프를 사용하지 않습니다.']}
/>

<Quiz
  questionNumber={16}
  question="합병 정렬(Merge Sort)이 퀵 정렬과 비교했을 때 갖는 장점은?"
  options={['①추가 메모리를 전혀 사용하지 않는다', '②최악의 경우에도 항상 O(n log n)을 보장한다', '③항상 퀵 정렬보다 빠르다', '④자릿수를 비교하지 않는다']}
  correctAnswer={2}
  explanation="합병 정렬은 데이터 분포와 무관하게 항상 O(n log n)의 성능을 보장하는 것이 핵심 장점입니다."
  optionExplanations={['오히려 합병 과정에서 추가 메모리가 필요합니다.', '맞습니다. 최악의 경우에도 안정적인 성능이 장점입니다.', '평균적으로는 상황에 따라 다를 수 있습니다.', '기수 정렬에 대한 설명입니다.']}
/>

<Quiz
  questionNumber={17}
  question="이진 탐색을 사용하기 위한 필수 조건은?"
  options={['①데이터가 정렬되어 있어야 한다', '②데이터가 문자열이어야 한다', '③데이터 개수가 짝수여야 한다', '④조건 없이 아무 데이터에나 사용 가능하다']}
  correctAnswer={1}
  explanation="이진 탐색은 반드시 정렬된 데이터에서만 올바르게 동작합니다."
  optionExplanations={['맞습니다. 정렬이 필수 조건입니다.', '문자열이든 숫자든 정렬만 되어 있으면 가능합니다.', '개수의 홀짝은 조건이 아닙니다.', '정렬되지 않은 데이터에는 사용할 수 없습니다.']}
/>

<Quiz
  questionNumber={18}
  question="테이블 크기 5, 나눗셈법 해시 함수를 사용할 때 키 18의 저장 위치는?"
  options={['①0', '②3', '③4', '④5']}
  correctAnswer={2}
  explanation="h(18) = 18 mod 5 = 3입니다."
  optionExplanations={['계산이 잘못되었습니다.', '맞습니다. 18÷5=3...나머지3입니다.', '계산이 잘못되었습니다.', '테이블 크기(5)를 인덱스로 사용할 수 없습니다.']}
/>

<Quiz
  questionNumber={19}
  question="체이닝(Chaining) 방식으로 충돌을 처리할 때 각 칸에 연결하는 자료구조는?"
  options={['①배열', '②연결 리스트', '③트리', '④그래프']}
  correctAnswer={2}
  explanation="체이닝은 충돌이 발생한 같은 위치에 연결 리스트를 매달아 데이터를 이어붙이는 방식입니다."
  optionExplanations={['배열은 크기 변경이 유연하지 않아 적합하지 않습니다.', '맞습니다. 연결 리스트를 사용합니다.', '트리는 체이닝의 표준적인 구현 방식이 아닙니다.', '그래프는 이 목적에 사용되지 않습니다.']}
/>

<Quiz
  questionNumber={20}
  question="적재율(Load Factor)이 매우 높아졌을 때 해싱의 성능은?"
  options={['①항상 O(1)을 유지한다', '②충돌 확률이 높아져 성능이 저하된다', '③오히려 더 빨라진다', '④데이터를 저장할 수 없게 된다']}
  correctAnswer={2}
  explanation="적재율이 높아지면 충돌 가능성이 커져 해싱의 평균 성능이 O(1)에서 멀어지며 저하됩니다."
  optionExplanations={['적재율이 높아지면 성능이 저하됩니다.', '맞습니다. 충돌 증가로 인한 성능 저하가 발생합니다.', '오히려 성능이 나빠집니다.', '체이닝의 경우 여전히 저장은 가능하지만 느려집니다.']}
/>

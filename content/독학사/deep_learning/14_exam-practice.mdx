---
title: '14. 딥러닝 예상기출문제'
description: '딥러닝 전 범위의 핵심 개념을 점검하는 20문항 예상기출문제 모음이다.'
---

<Callout type="warning">
  **최종 점검:** 이 문서는 딥러닝 전 범위(신경망 기초부터 트랜스포머, 생성 모델, 전이학습까지)를 아우르는 예상기출문제입니다. 풀기 전에 각 챕터를 충분히 복습하세요.
</Callout>

## 예상기출문제 20선

<Quiz
  questionNumber={1}
  question="퍼셉트론(Perceptron)이 XOR 문제를 해결하지 못하는 이유는?"
  options={['연산 속도가 너무 느리기 때문에', '단일 선형 결정 경계로는 XOR의 비선형 분류 경계를 표현할 수 없기 때문에', '학습 데이터가 부족하기 때문에', 'GPU를 지원하지 않기 때문에']}
  correctAnswer={2}
  explanation="단층 퍼셉트론은 직선(선형 결정 경계)으로만 데이터를 분류합니다. XOR은 단일 직선으로 분류 불가능한 비선형 문제이므로 다층 신경망(MLP)이 필요합니다."
  optionExplanations={['연산 속도 문제가 아닙니다.', 'XOR은 선형 분리 불가능한 문제로 단층 퍼셉트론으로는 해결 불가합니다. (정답)', '데이터 부족 문제가 아닙니다.', 'GPU 지원과 무관합니다.']}
/>

<Quiz
  questionNumber={2}
  question="ReLU 활성화 함수가 Sigmoid보다 딥러닝에서 선호되는 이유는?"
  options={['ReLU의 출력 범위가 0~1로 확률 해석이 용이하기 때문에', 'ReLU는 양수 구간에서 기울기가 1로 일정하여 기울기 소실 문제를 완화하기 때문에', 'ReLU는 음수 입력에 대해 항상 음수를 출력하기 때문에', 'ReLU는 미분이 불가능하기 때문에']}
  correctAnswer={2}
  explanation="Sigmoid는 출력이 0 또는 1에 가까워질수록 기울기가 거의 0이 됩니다(기울기 소실). ReLU는 양수 구간에서 기울기가 항상 1이라 기울기 소실 문제가 없고 계산도 빠릅니다."
  optionExplanations={['0~1 출력은 Sigmoid의 특징으로, ReLU는 0에서 무한대입니다.', 'ReLU는 양수 구간 기울기 1로 기울기 소실 문제를 완화합니다. (정답)', 'ReLU는 음수 입력에 대해 0을 출력합니다.', 'ReLU는 0 이외의 점에서 미분 가능합니다.']}
/>

<Quiz
  questionNumber={3}
  question="역전파(Backpropagation) 알고리즘의 핵심 수학 원리는?"
  options={['적분 법칙', '연쇄 법칙(Chain Rule)', '테일러 급수', '행렬 분해']}
  correctAnswer={2}
  explanation="역전파는 출력층에서 입력층 방향으로 기울기를 전파합니다. 합성함수의 미분인 연쇄 법칙(Chain Rule)을 사용하여 각 층의 가중치에 대한 손실 함수의 편미분을 계산합니다."
  optionExplanations={['적분 법칙은 역전파와 관련 없습니다.', '역전파는 연쇄 법칙을 핵심 원리로 사용합니다. (정답)', '테일러 급수는 역전파의 핵심이 아닙니다.', '행렬 분해는 역전파의 핵심이 아닙니다.']}
/>

<Quiz
  questionNumber={4}
  question="배치 경사 하강법(Batch GD), 확률적 경사 하강법(SGD), 미니배치 경사 하강법의 비교로 옳은 것은?"
  options={['SGD는 전체 데이터로 기울기를 계산한다', '배치 GD는 한 샘플씩 업데이트하여 속도가 가장 빠르다', '미니배치는 소규모 배치로 기울기를 계산하여 배치 GD와 SGD의 절충안이다', 'SGD는 항상 전역 최솟값에 수렴한다']}
  correctAnswer={3}
  explanation="미니배치 경사 하강법은 소규모 배치(예: 32, 64개)로 기울기를 계산합니다. 전체 데이터(배치 GD)보다 빠르고 단일 샘플(SGD)보다 안정적인 절충안입니다."
  optionExplanations={['SGD는 한 샘플씩 기울기를 계산합니다.', '배치 GD가 전체 데이터를 사용하여 가장 느립니다.', '미니배치는 배치 GD와 SGD의 절충안입니다. (정답)', 'SGD는 노이즈로 인해 전역 최솟값 수렴이 보장되지 않습니다.']}
/>

<Quiz
  questionNumber={5}
  question="Adam 옵티마이저가 SGD보다 유리한 점은?"
  options={['계산량이 더 적다', '1차 모멘텀과 2차 모멘텀을 모두 활용하여 적응적 학습률을 사용한다', '전역 최솟값을 항상 찾는다', '하이퍼파라미터가 없다']}
  correctAnswer={2}
  explanation="Adam은 모멘텀(1차 모멘텀)과 각 매개변수의 기울기 제곱의 지수 이동 평균(2차 모멘텀)을 결합하여 각 매개변수에 적응적인 학습률을 적용합니다."
  optionExplanations={['Adam은 추가 연산으로 계산량이 더 많습니다.', 'Adam은 1차+2차 모멘텀으로 적응적 학습률을 사용합니다. (정답)', '전역 최솟값 보장은 없습니다.', 'Adam도 β1, β2, ε 등의 하이퍼파라미터가 있습니다.']}
/>

<Quiz
  questionNumber={6}
  question="드롭아웃(Dropout)을 학습 시에만 적용하고 테스트 시에는 적용하지 않는 이유는?"
  options={['테스트 시 속도를 높이기 위해', '테스트 시에는 안정적인 예측을 위해 모든 뉴런을 활성화해야 하기 때문에', '테스트 데이터에는 노이즈가 없기 때문에', '드롭아웃은 역전파에만 필요하기 때문에']}
  correctAnswer={2}
  explanation="드롭아웃은 과적합을 방지하기 위해 학습 시 무작위 뉴런을 비활성화합니다. 테스트 시에는 안정적인 예측을 위해 모든 뉴런을 활성화하고, 가중치를 (1-p)로 스케일링하여 기댓값을 맞춥니다."
  optionExplanations={['속도 향상이 주목적이 아닙니다.', '테스트 시 안정적 예측을 위해 모든 뉴런을 활성화합니다. (정답)', '테스트 데이터의 노이즈 여부와 무관합니다.', '드롭아웃은 순전파에도 적용됩니다.']}
/>

<Quiz
  questionNumber={7}
  question="합성곱 신경망(CNN)에서 가중치 공유(Weight Sharing)의 효과는?"
  options={['모든 층이 동일한 가중치를 사용하여 학습 불가', '동일 필터를 전체 입력에 적용하여 매개변수 수를 크게 줄임', '각 픽셀마다 별도 가중치로 세밀한 학습 가능', '합성곱 연산의 속도가 느려짐']}
  correctAnswer={2}
  explanation="CNN에서 동일한 필터(가중치)를 입력 전체에 슬라이딩하며 적용합니다. 이로 인해 완전 연결층보다 매개변수 수가 극적으로 줄어들고, 위치 불변적 특징 학습이 가능합니다."
  optionExplanations={['모든 층이 같은 가중치가 아닌, 같은 층 내에서 필터를 공유합니다.', '동일 필터를 전체 입력에 적용하여 매개변수를 크게 줄입니다. (정답)', '완전 연결층 방식으로 매개변수가 더 많아집니다.', '가중치 공유로 오히려 계산이 효율적입니다.']}
/>

<Quiz
  questionNumber={8}
  question="ResNet의 잔차 연결(Residual Connection)이 매우 깊은 신경망 학습에서 기여하는 것은?"
  options={['데이터 증강 효과', '기울기가 잔차 연결을 통해 직접 흐를 수 있어 기울기 소실 문제 해결', '모델 크기를 줄이는 효과', '학습률을 자동으로 조정하는 효과']}
  correctAnswer={2}
  explanation="잔차 연결은 x를 F(x)+x로 바로 더하는 Skip Connection을 제공합니다. 역전파 시 기울기가 이 경로를 통해 직접 흐를 수 있어 수백 층 이상의 깊은 네트워크도 학습 가능합니다."
  optionExplanations={['데이터 증강과 무관합니다.', '잔차 연결로 기울기가 직접 흐를 수 있어 기울기 소실 문제가 해결됩니다. (정답)', '모델 크기 감소와 직접 관련 없습니다.', '학습률 자동 조정은 옵티마이저가 담당합니다.']}
/>

<Quiz
  questionNumber={9}
  question="RNN에서 BPTT(Backpropagation Through Time)의 설명으로 옳은 것은?"
  options={['각 시간 단계를 독립적으로 역전파한다', '시간 단계를 펼쳐서(Unrolling) 순서를 거슬러 기울기를 전파한다', '순전파만 수행하는 특수한 알고리즘이다', '시간을 순방향으로만 역전파한다']}
  correctAnswer={2}
  explanation="BPTT는 RNN을 시간 단계에 따라 펼쳐서 일반 역전파처럼 적용합니다. 출력에서 입력 방향으로, 즉 시간을 거슬러 기울기를 전파합니다."
  optionExplanations={['독립적 역전파는 시퀀스 맥락을 학습하지 못합니다.', 'BPTT는 시간을 펼쳐 거슬러 기울기를 전파합니다. (정답)', 'BPTT도 역전파 알고리즘입니다.', '역전파는 항상 출력→입력 방향입니다.']}
/>

<Quiz
  questionNumber={10}
  question="LSTM에서 셀 상태(Cell State)의 역할은?"
  options={['현재 입력만 저장한다', '장기 기억을 유지하는 컨베이어 벨트 역할을 한다', '활성화 함수 출력값을 저장한다', '학습 오차를 누적한다']}
  correctAnswer={2}
  explanation="LSTM의 셀 상태는 시퀀스 전체를 통해 컨베이어 벨트처럼 장기 정보를 유지합니다. 망각/입력/출력 게이트가 선택적으로 정보를 추가하거나 제거합니다."
  optionExplanations={['현재 입력은 x_t가 처리합니다.', '셀 상태는 장기 기억을 유지하는 컨베이어 벨트 역할입니다. (정답)', '활성화 함수 출력은 은닉 상태 h_t입니다.', '학습 오차 누적은 셀 상태의 역할이 아닙니다.']}
/>

<Quiz
  questionNumber={11}
  question="트랜스포머(Transformer)에서 포지셔널 인코딩(Positional Encoding)이 필요한 이유는?"
  options={['어텐션 계산량을 줄이기 위해', 'RNN과 달리 시퀀스를 병렬 처리하므로 순서 정보가 없어 별도로 위치 정보를 추가해야 하기 때문에', '단어 임베딩의 차원을 맞추기 위해', '마스크드 어텐션을 구현하기 위해']}
  correctAnswer={2}
  explanation="트랜스포머는 시퀀스를 병렬로 처리하여 RNN과 달리 순서 정보가 없습니다. 사인/코사인 함수 기반 포지셔널 인코딩을 임베딩에 더하여 위치 정보를 제공합니다."
  optionExplanations={['포지셔널 인코딩은 계산량 감소와 관련 없습니다.', '병렬 처리로 순서 정보가 없어 포지셔널 인코딩이 필요합니다. (정답)', '임베딩 차원 정렬은 선형 변환으로 수행합니다.', '마스크드 어텐션은 별도 기법입니다.']}
/>

<Quiz
  questionNumber={12}
  question="셀프 어텐션(Self-Attention)에서 Query, Key, Value의 역할을 올바르게 설명한 것은?"
  options={['Q는 데이터를 압축, K는 재구성, V는 손실 계산', 'Q는 현재 위치의 질의, K는 각 위치의 검색 인덱스, V는 실제 값', 'Q와 K는 동일하고 V만 다르다', 'Q, K, V는 모두 학습되지 않는 고정 행렬이다']}
  correctAnswer={2}
  explanation="Q(Query)는 현재 위치가 다른 위치에 대해 질의하고, K(Key)는 각 위치가 제공하는 검색 인덱스이며, V(Value)는 각 위치가 가진 실제 정보입니다. Q와 K의 내적으로 어텐션 가중치를 계산하고, V를 가중합합니다."
  optionExplanations={['Q/K/V의 설명이 틀립니다.', 'Q는 질의, K는 검색 인덱스, V는 실제 값입니다. (정답)', 'Q, K, V는 서로 다른 선형 변환으로 생성됩니다.', 'Q, K, V는 학습 가능한 가중치로 생성됩니다.']}
/>

<Quiz
  questionNumber={13}
  question="GAN(Generative Adversarial Network)에서 생성자(G)와 판별자(D)의 학습 목표는?"
  options={['G와 D 모두 손실을 최소화한다', 'G는 판별자를 속이는 방향으로, D는 진짜와 가짜를 구분하는 방향으로 학습한다', 'G는 진짜를 분류하고, D는 가짜를 생성한다', 'G와 D는 같은 목적함수를 공유한다']}
  correctAnswer={2}
  explanation="GAN은 적대적 학습입니다. 생성자(G)는 판별자를 속이는(가짜를 진짜처럼) 방향으로 학습하고, 판별자(D)는 진짜와 가짜를 정확히 구분하는 방향으로 학습합니다."
  optionExplanations={['G는 손실 최소화, D는 손실 최대화 방향으로 학습합니다.', 'G는 판별자를 속이고, D는 진짜/가짜를 구분하도록 학습합니다. (정답)', 'G가 가짜를 생성하고, D가 분류합니다.', 'G와 D는 서로 반대되는 목적을 가집니다.']}
/>

<Quiz
  questionNumber={14}
  question="배치 정규화(Batch Normalization)가 학습을 안정화하는 원리는?"
  options={['가중치를 무작위로 초기화하여 다양성을 높인다', '각 미니배치의 층 입력을 평균 0, 분산 1로 정규화하여 내부 공변량 이동을 감소시킨다', '학습률을 자동으로 줄인다', '드롭아웃처럼 일부 뉴런을 비활성화한다']}
  correctAnswer={2}
  explanation="배치 정규화는 각 미니배치에서 층의 입력을 정규화(평균 0, 분산 1)합니다. 내부 공변량 이동(Internal Covariate Shift)을 줄여 더 높은 학습률 사용과 빠른 수렴을 가능하게 합니다."
  optionExplanations={['무작위 초기화는 배치 정규화와 다릅니다.', '미니배치 정규화로 내부 공변량 이동을 줄이는 것이 배치 정규화의 원리입니다. (정답)', '학습률 자동 감소는 스케줄러가 담당합니다.', '드롭아웃과는 다른 기법입니다.']}
/>

<Quiz
  questionNumber={15}
  question="VAE(Variational Autoencoder)와 일반 오토인코더의 가장 큰 차이는?"
  options={['VAE는 입력을 더 작은 크기로 압축한다', 'VAE는 잠재 변수를 확률 분포(평균, 분산)로 표현하여 연속적 잠재 공간을 학습한다', 'VAE는 판별자를 사용하여 학습한다', 'VAE는 항상 더 선명한 이미지를 생성한다']}
  correctAnswer={2}
  explanation="일반 오토인코더는 잠재 변수를 고정 벡터로 인코딩합니다. VAE는 잠재 변수를 평균(μ)과 분산(σ²)으로 표현되는 확률 분포로 모델링하여 연속적이고 구조화된 잠재 공간을 만듭니다."
  optionExplanations={['압축 크기는 일반 오토인코더와 큰 차이가 없을 수 있습니다.', 'VAE는 잠재 변수를 확률 분포로 표현하여 연속적 잠재 공간을 학습합니다. (정답)', 'VAE는 GAN과 달리 판별자를 사용하지 않습니다.', '이미지 선명도는 일반적으로 GAN이 더 우수합니다.']}
/>

<Quiz
  questionNumber={16}
  question="전이학습에서 파인튜닝(Fine-tuning) 시 사전학습 모델 층에 낮은 학습률을 사용하는 이유는?"
  options={['학습을 빠르게 수렴시키기 위해', '사전학습된 유용한 가중치를 파괴하지 않으면서 새 도메인에 조금씩 적응시키기 위해', '과적합을 방지하기 위해', '정규화 효과를 내기 위해']}
  correctAnswer={2}
  explanation="사전학습 모델의 가중치에는 대규모 데이터에서 학습한 유용한 정보가 담겨 있습니다. 높은 학습률은 이를 파괴할 수 있으므로, 낮은 학습률로 조금씩 새 도메인에 맞게 조정합니다."
  optionExplanations={['낮은 학습률은 수렴을 느리게 합니다.', '낮은 학습률로 사전학습 가중치를 보존하면서 새 도메인에 적응합니다. (정답)', '과적합 방지는 주목적이 아닙니다.', '정규화 효과는 주목적이 아닙니다.']}
/>

<Quiz
  questionNumber={17}
  question="GAN의 모드 붕괴(Mode Collapse) 문제의 설명으로 옳은 것은?"
  options={['판별자가 모든 입력을 진짜로 판별하는 현상', '생성자가 다양한 데이터 분포 대신 일부 패턴만 반복 생성하는 현상', '학습 중 기울기가 폭발하는 현상', '두 모델의 학습 손실이 모두 0에 수렴하는 현상']}
  correctAnswer={2}
  explanation="모드 붕괴는 생성자가 판별자를 쉽게 속일 수 있는 소수의 샘플(모드)만 반복 생성하고, 데이터의 다양한 분포를 포괄하지 못하는 현상입니다."
  optionExplanations={['판별자가 모든 입력을 진짜로 판별하면 생성자가 이미 학습 완료된 상태입니다.', '생성자가 일부 패턴만 반복 생성하는 것이 모드 붕괴입니다. (정답)', '기울기 폭발은 학습 불안정의 다른 형태입니다.', '손실이 모두 0이면 이상적 학습 완료 상태입니다.']}
/>

<Quiz
  questionNumber={18}
  question="BERT 모델의 사전학습 방식에 포함되는 태스크는?"
  options={['다음 단어 예측과 이미지 분류', '마스크 언어 모델링(MLM)과 다음 문장 예측(NSP)', '텍스트 생성과 요약', '이미지 캡셔닝과 객체 탐지']}
  correctAnswer={2}
  explanation="BERT는 문장의 일부 단어를 [MASK] 토큰으로 대체하고 예측하는 마스크 언어 모델링(MLM), 두 문장이 연속되는지 예측하는 다음 문장 예측(NSP)으로 사전학습됩니다."
  optionExplanations={['이미지 분류는 BERT의 사전학습 태스크가 아닙니다.', 'BERT는 MLM과 NSP로 사전학습됩니다. (정답)', '텍스트 생성은 GPT의 방식에 가깝습니다.', 'BERT는 텍스트 모델로 이미지 작업을 수행하지 않습니다.']}
/>

<Quiz
  questionNumber={19}
  question="멀티헤드 어텐션(Multi-Head Attention)에서 여러 헤드를 사용하는 장점은?"
  options={['계산량을 줄일 수 있다', '각 헤드가 서로 다른 종류의 의미적/구조적 관계를 동시에 학습할 수 있다', '어텐션 결과를 반드시 평균내야 하기 때문에', '단일 어텐션이 메모리 초과 오류를 발생시키기 때문에']}
  correctAnswer={2}
  explanation="멀티헤드 어텐션은 여러 헤드가 서로 다른 부분 공간에서 어텐션을 계산합니다. 어떤 헤드는 문법적 관계를, 다른 헤드는 의미적 관계를 학습하여 더 풍부한 표현이 가능합니다."
  optionExplanations={['멀티헤드 어텐션은 추가 계산을 필요로 합니다.', '여러 헤드가 서로 다른 관계를 동시에 학습하는 것이 장점입니다. (정답)', '결과는 연결(Concatenation) 후 선형 변환합니다.', '메모리 오류 방지가 목적이 아닙니다.']}
/>

<Quiz
  questionNumber={20}
  question="딥러닝에서 기울기 소실(Vanishing Gradient) 문제의 원인과 해결책을 올바르게 짝지은 것은?"
  options={['원인: 학습률이 너무 큼 / 해결: 학습률 감소', '원인: 깊은 신경망에서 역전파 시 기울기가 지수적으로 작아짐 / 해결: ReLU, 잔차 연결, LSTM', '원인: 데이터가 너무 많음 / 해결: 드롭아웃', '원인: 배치 크기가 너무 큼 / 해결: 미니배치 사용']}
  correctAnswer={2}
  explanation="기울기 소실은 역전파 시 기울기가 층을 거슬러 올라갈수록 지수적으로 작아지는 현상입니다. ReLU(양수 구간 기울기 1), 잔차 연결(기울기 직접 흐름), LSTM/GRU(게이트 구조)로 해결합니다."
  optionExplanations={['학습률과 기울기 소실은 다른 문제입니다.', '역전파 시 지수적으로 기울기가 작아지고, ReLU/잔차 연결/LSTM으로 해결합니다. (정답)', '데이터 양과 기울기 소실은 관련 없습니다.', '배치 크기와 기울기 소실은 관련 없습니다.']}
/>

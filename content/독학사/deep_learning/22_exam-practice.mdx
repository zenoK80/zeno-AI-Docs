---
title: '22. 딥러닝 예상기출문제'
description: '딥러닝 시험 대비 예상기출 문제 모음'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 공식 출제 범위를 바탕으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="신경망이 '깊다(Deep)'고 불리는 기준은?"
  options={['①입력 데이터의 크기가 클 때', '②은닉층(hidden layer)이 여러 개일 때', '③학습 시간이 오래 걸릴 때', '④파라미터가 하나도 없을 때']}
  correctAnswer={2}
  explanation="신경망이 은닉층을 여러 개 가질 때 '깊은' 신경망이라고 부릅니다."
  optionExplanations={['데이터 크기와는 다른 기준입니다.', '맞습니다. 은닉층 개수가 기준입니다.', '학습 시간과는 다른 기준입니다.', '파라미터가 없으면 신경망이 성립하지 않습니다.']}
/>

<Quiz
  questionNumber={2}
  question="XOR 문제가 단층 퍼셉트론으로 풀리지 않는 이유는?"
  options={['①계산이 불가능한 연산이기 때문에', '②참/거짓 점들이 하나의 직선으로 나뉘지 않는 형태로 배치되기 때문에', '③입력이 3개 이상 필요하기 때문에', '④신경망과 무관한 개념이기 때문에']}
  correctAnswer={2}
  explanation="XOR의 네 가지 조합을 평면에 그리면 직선 하나로 나눌 수 없는 형태로 배치됩니다."
  optionExplanations={['XOR은 정상적으로 계산 가능한 연산입니다.', '맞습니다. 선형 분리 불가능입니다.', 'XOR은 입력 2개로 정의됩니다.', 'XOR 문제는 신경망 이론의 중요한 사례입니다.']}
/>

<Quiz
  questionNumber={3}
  question="ReLU 함수의 정의는?"
  options={['①항상 0을 출력한다', '②max(0, z)', '③0~1 사이로 압축한다', '④입력을 제곱한다']}
  correctAnswer={2}
  explanation="ReLU(z)=max(0,z)로, 양수는 그대로, 음수는 0으로 만드는 함수입니다."
  optionExplanations={['양수 입력은 그대로 통과됩니다.', '맞습니다. max(0,z)입니다.', '이는 Sigmoid에 대한 설명입니다.', '제곱 연산이 아닙니다.']}
/>

<Quiz
  questionNumber={4}
  question="순전파(Forward Pass)가 하는 일은?"
  options={['①가중치를 갱신한다', '②입력이 각 층을 순서대로 통과해 최종 출력을 계산한다', '③손실 함수의 기울기를 구한다', '④데이터를 훈련·테스트로 나눈다']}
  correctAnswer={2}
  explanation="순전파는 입력이 신경망의 각 층을 순서대로 통과하며 예측값을 계산하는 과정입니다."
  optionExplanations={['가중치 갱신은 역전파 이후입니다.', '맞습니다. 예측값 계산입니다.', '기울기 계산은 역전파의 역할입니다.', '데이터 분할과는 무관합니다.']}
/>

<Quiz
  questionNumber={5}
  question="다중 분류에 주로 사용되는 손실 함수는?"
  options={['①MSE', '②Binary Cross Entropy', '③Categorical Cross Entropy', '④ReLU']}
  correctAnswer={3}
  explanation="여러 클래스 중 하나를 예측하는 다중 분류에는 Categorical Cross Entropy가 주로 사용됩니다."
  optionExplanations={['MSE는 회귀 문제에 사용됩니다.', 'Binary CE는 이진 분류에 사용됩니다.', '맞습니다. Categorical Cross Entropy입니다.', 'ReLU는 활성화 함수입니다.']}
/>

<Quiz
  questionNumber={6}
  question="역전파가 순전파와 반대 방향으로 진행되는 이유는?"
  options={['①단순한 다양성을 위해', '②체인 룰에 따라 손실에서부터 거꾸로 거슬러 올라가며 기울기를 곱해야 하기 때문에', '③역전파는 순전파와 같은 방향이다', '④역전파는 신경망과 무관하다']}
  correctAnswer={2}
  explanation="가중치의 영향력을 구하려면 체인 룰에 따라 손실에서부터 거꾸로 거슬러 올라가야 합니다."
  optionExplanations={['수학적 필연성에 의한 것입니다.', '맞습니다. 체인 룰의 방향성 때문입니다.', '역전파는 반대 방향으로 진행됩니다.', '역전파는 신경망 학습의 핵심 알고리즘입니다.']}
/>

<Quiz
  questionNumber={7}
  question="Adam 최적화 알고리즘이 결합한 두 아이디어는?"
  options={['①SGD와 기본 경사하강법', '②Momentum과 RMSProp', '③활성화 함수와 손실 함수', '④순전파와 역전파']}
  correctAnswer={2}
  explanation="Adam은 Momentum의 관성 유지와 RMSProp의 파라미터별 학습률 조정을 결합한 알고리즘입니다."
  optionExplanations={['데이터 사용 방식의 차이입니다.', '맞습니다. Momentum + RMSProp입니다.', '최적화와는 다른 구성 요소입니다.', '학습 과정의 다른 단계입니다.']}
/>

<Quiz
  questionNumber={8}
  question="Dropout이 과적합을 방지하는 원리는?"
  options={['①모든 뉴런을 영구히 제거한다', '②학습 시 무작위로 일부 뉴런을 잠시 비활성화한다', '③가중치를 항상 0으로 고정한다', '④학습 데이터를 늘린다']}
  correctAnswer={2}
  explanation="Dropout은 학습마다 무작위로 뉴런을 잠시 꺼서 특정 뉴런 의존을 방지합니다."
  optionExplanations={['영구 제거가 아니라 일시적 비활성화입니다.', '맞습니다. 무작위 일시 비활성화입니다.', '가중치 자체를 고정하는 것이 아닙니다.', '데이터 증가와는 다른 접근입니다.']}
/>

<Quiz
  questionNumber={9}
  question="합성곱(Convolution)에서 필터의 숫자들은 어떻게 정해지는가?"
  options={['①사람이 직접 입력한다', '②역전파를 통해 학습된다', '③항상 무작위로 고정되어 변하지 않는다', '④존재하지 않는다']}
  correctAnswer={2}
  explanation="필터는 역전파를 통해 학습되는 파라미터입니다."
  optionExplanations={['사람이 직접 정하지 않고 학습을 통해 결정됩니다.', '맞습니다. 학습되는 파라미터입니다.', '학습 과정에서 계속 조정됩니다.', '필터는 CNN의 핵심 구성 요소입니다.']}
/>

<Quiz
  questionNumber={10}
  question="Max Pooling이 수행하는 연산은?"
  options={['①평균값 계산', '②최댓값 선택', '③합계 계산', '④무작위 선택']}
  correctAnswer={2}
  explanation="Max Pooling은 정해진 구역에서 가장 큰 값만 선택합니다."
  optionExplanations={['평균은 Average Pooling입니다.', '맞습니다. 최댓값 선택입니다.', '합계 연산이 아닙니다.', '무작위가 아닌 결정적 선택입니다.']}
/>

<Quiz
  questionNumber={11}
  question="ResNet의 Residual Connection 공식 y=F(x)+x가 기울기 소실을 완화하는 원리는?"
  options={['①F(x)를 제거하기 때문에', '②x 항의 미분이 그대로 전달되어 최소한의 기울기가 보장되기 때문에', '③활성화 함수를 사용하지 않기 때문에', '④입력을 항상 0으로 만들기 때문에']}
  correctAnswer={2}
  explanation="덧셈의 미분은 그대로 전달되어, x 항 덕분에 최소 1에 해당하는 기울기가 보장됩니다."
  optionExplanations={['F(x)도 여전히 학습되는 부분입니다.', '맞습니다. x 항이 기울기 통로 역할을 합니다.', 'Residual Connection도 활성화 함수와 함께 사용됩니다.', '입력을 그대로 보존해 전달합니다.']}
/>

<Quiz
  questionNumber={12}
  question="RNN이 CNN과 달리 시퀀스 데이터에 적합한 이유는?"
  options={['①이미지 처리 전용이기 때문에', '②은닉 상태로 이전 시점의 정보를 기억하기 때문에', '③파라미터가 없기 때문에', '④학습이 필요 없기 때문에']}
  correctAnswer={2}
  explanation="RNN은 은닉 상태로 이전 정보를 기억해 순서 의존성을 처리할 수 있습니다."
  optionExplanations={['CNN이 이미지 처리에 특화되어 있습니다.', '맞습니다. 은닉 상태를 통한 정보 기억입니다.', 'RNN도 파라미터를 가집니다.', 'RNN도 역전파로 학습됩니다.']}
/>

<Quiz
  questionNumber={13}
  question="LSTM의 망각 게이트(Forget Gate)의 역할은?"
  options={['①새 정보를 추가한다', '②셀 상태에서 불필요한 과거 정보를 얼마나 지울지 결정한다', '③은닉 상태를 출력한다', '④활성화 함수를 선택한다']}
  correctAnswer={2}
  explanation="망각 게이트는 이전 셀 상태에서 불필요한 정보를 얼마나 지울지 결정합니다."
  optionExplanations={['새 정보 추가는 입력 게이트의 역할입니다.', '맞습니다. 불필요한 정보 삭제 결정입니다.', '은닉 상태 계산은 출력 게이트와 관련됩니다.', '게이트의 역할이 아닙니다.']}
/>

<Quiz
  questionNumber={14}
  question="원-핫 인코딩의 근본적인 한계는?"
  options={['①계산이 너무 빠르다는 것', '②차원이 매우 크고 단어 간 의미 관계를 표현하지 못한다는 것', '③숫자로 바꿀 수 없다는 것', '④문장 순서를 표현할 수 없다는 것']}
  correctAnswer={2}
  explanation="원-핫 인코딩은 차원이 매우 크고, 서로 다른 단어의 내적이 항상 0이 되어 의미 관계를 표현하지 못합니다."
  optionExplanations={['오히려 차원이 커서 비효율적입니다.', '맞습니다. 큰 차원과 의미 미반영입니다.', '숫자로 바꾸는 것 자체는 가능합니다.', '순서 표현은 별개의 문제입니다.']}
/>

<Quiz
  questionNumber={15}
  question="Attention이 RNN보다 장기 의존성 문제에서 자유로운 이유는?"
  options={['①순서를 전혀 고려하지 않기 때문에', '②모든 단어 쌍을 거리와 무관하게 한 번에 비교하기 때문에', '③짧은 문장에만 사용되기 때문에', '④학습이 필요 없기 때문에']}
  correctAnswer={2}
  explanation="Attention은 단어 사이 거리와 무관하게 모든 쌍을 직접 비교해, 정보가 희미해지는 문제가 없습니다."
  optionExplanations={['Positional Encoding으로 순서를 반영합니다.', '맞습니다. 거리 무관 직접 비교입니다.', '긴 문장에도 효과적입니다.', '정상적으로 학습되는 구조입니다.']}
/>

<Quiz
  questionNumber={16}
  question="Transformer에서 Positional Encoding이 필요한 이유는?"
  options={['①이미 순서 정보를 자동으로 알기 때문에', '②Attention 자체는 순서 정보를 담지 못해 이를 보충하기 위해', '③문장 길이를 늘리기 위해', '④단어 의미를 삭제하기 위해']}
  correctAnswer={2}
  explanation="Attention은 모든 단어를 동시에 비교하므로 순서 정보를 자동으로 알 수 없어, 별도로 추가해줘야 합니다."
  optionExplanations={['Attention 자체는 순서 정보를 모릅니다.', '맞습니다. 순서 정보 보충입니다.', '길이 조절과 무관합니다.', '의미 삭제와 무관합니다.']}
/>

<Quiz
  questionNumber={17}
  question="GAN에서 Generator의 목표는?"
  options={['①진짜와 가짜를 구별하는 것', '②판별자를 속일 만큼 진짜 같은 가짜 데이터를 만드는 것', '③데이터를 압축하는 것', '④레이블을 예측하는 것']}
  correctAnswer={2}
  explanation="Generator는 판별자가 진짜라고 착각할 만큼 정교한 가짜 데이터를 만드는 것이 목표입니다."
  optionExplanations={['구별은 Discriminator의 역할입니다.', '맞습니다. 판별자를 속이는 것입니다.', '압축은 오토인코더의 역할입니다.', 'GAN은 데이터 생성이 목표입니다.']}
/>

<Quiz
  questionNumber={18}
  question="VAE가 일반 오토인코더와 다른 점은?"
  options={['①인코더가 없다', '②잠재 공간을 확률 분포로 만들어 새로운 데이터를 생성할 수 있다', '③디코더가 없다', '④완전히 같은 모델이다']}
  correctAnswer={2}
  explanation="VAE는 잠재 공간을 확률 분포로 만들어, 샘플링을 통해 새로운 데이터를 생성할 수 있습니다."
  optionExplanations={['VAE도 인코더를 가집니다.', '맞습니다. 확률 분포 기반입니다.', 'VAE도 디코더를 가집니다.', '구조적으로 중요한 차이가 있습니다.']}
/>

<Quiz
  questionNumber={19}
  question="BERT와 GPT의 구조적 차이는?"
  options={['①BERT는 Decoder, GPT는 Encoder를 사용한다', '②BERT는 Encoder(양방향), GPT는 Decoder(단방향)를 사용한다', '③둘 다 CNN 기반이다', '④둘은 완전히 동일한 구조다']}
  correctAnswer={2}
  explanation="BERT는 Transformer의 Encoder를 활용한 양방향 모델이고, GPT는 Decoder를 활용한 단방향 모델입니다."
  optionExplanations={['설명이 반대로 되어 있습니다.', '맞습니다. Encoder(BERT) vs. Decoder(GPT)입니다.', '둘 다 Transformer 기반입니다.', '구조적으로 명확한 차이가 있습니다.']}
/>

<Quiz
  questionNumber={20}
  question="전이학습(Transfer Learning)에서 일반적으로 고정하는 부분은?"
  options={['①마지막 분류층', '②사전학습된 모델의 저수준 특징 추출 층', '③모든 층', '④아무 층도 고정하지 않는다']}
  correctAnswer={2}
  explanation="전이학습은 이미 검증된 저수준 특징 추출 층은 고정하고, 마지막 분류층만 새로 학습하는 것이 전형적입니다."
  optionExplanations={['마지막 층은 새로 학습되는 부분입니다.', '맞습니다. 저수준 특징 추출 층을 고정합니다.', '모든 층을 고정하면 새 문제에 적응할 수 없습니다.', '적어도 마지막 층은 새로 학습해야 합니다.']}
/>

---
title: '05. 프로세스 동기화'
description: '임계구역 문제와 뮤텍스, 세마포어, 모니터를 이용한 프로세스 동기화 방법을 이해한다.'
---

<Callout type="info">
  **이번 문서의 목표:** 임계구역 문제의 3가지 요구조건을 외우고, 세마포어의 P/V 연산 원리와 생산자-소비자 문제 해결 방법을 이해한다.
</Callout>

## 동기화가 왜 필요한가?

여러 프로세스(또는 스레드)가 동시에 같은 데이터에 접근하면 예상치 못한 결과가 발생합니다. 예를 들어, 두 사람이 동시에 같은 은행 계좌에서 돈을 출금한다고 가정합시다.

- 잔액: 100만원
- 사람 A: 잔액 100만원 읽음 → 80만원 출금 처리 중 (아직 저장 안 함)
- 사람 B: 잔액 100만원 읽음 → 90만원 출금 처리 중 (아직 저장 안 함)
- 사람 A: 잔액 20만원으로 저장
- 사람 B: 잔액 10만원으로 저장

결국 170만원을 출금했는데 잔액이 10만원만 남습니다. 총 80만원이 사라졌습니다!

이런 문제를 방지하는 것이 **동기화(Synchronization, 싱크로나이제이션)**입니다.

**쉽게 말하면:** 동기화는 여러 사람이 하나의 화장실을 쓸 때 안에 사람이 있으면 밖에서 기다리도록 하는 규칙입니다.

## 임계구역(Critical Section, 크리티컬 섹션)

**임계구역**은 여러 프로세스가 동시에 접근하면 안 되는 **공유 자원에 접근하는 코드 영역**입니다.

**쉽게 말하면:** 임계구역은 "한 번에 한 사람만 들어갈 수 있는 구역"입니다.

```
while (true) {
    [진입 구역]        ← 임계구역 진입 허가 요청
    [임계구역]         ← 공유 자원 접근 (한 번에 하나의 프로세스만)
    [퇴출 구역]        ← 임계구역 사용 완료 알림
    [나머지 구역]      ← 그 외 코드
}
```

### 임계구역 문제의 3가지 요구조건

| 조건 | 설명 |
|------|------|
| **상호배제(Mutual Exclusion, 뮤추얼 익스클루전)** | 한 프로세스가 임계구역에 있으면 다른 프로세스는 진입 불가 |
| **진행(Progress)** | 임계구역을 사용 중인 프로세스가 없고 진입하려는 프로세스가 있으면, 진입 결정이 유한 시간 내에 이루어져야 함 |
| **한정 대기(Bounded Waiting)** | 프로세스가 임계구역 진입 요청 후 무한정 대기하지 않도록 보장 |

## 뮤텍스(Mutex, Mutual Exclusion)

**뮤텍스**는 임계구역을 보호하기 위한 잠금(Lock) 메커니즘입니다. 화장실 문에 걸쇠를 거는 것과 같습니다.

- `acquire()`: 잠금 획득 (다른 프로세스가 사용 중이면 대기)
- `release()`: 잠금 해제

```
뮤텍스 잠금 상태: available (true/false)

acquire():
    while (!available) { /* 바쁜 대기(Busy Waiting) */ }
    available = false

release():
    available = true
```

**바쁜 대기(Busy Waiting):** CPU를 계속 사용하며 잠금을 기다리는 방식. CPU 낭비가 발생합니다. **스핀락(Spin Lock)**이라고도 합니다.

**쉽게 말하면:** 스핀락은 화장실 앞에서 계속 노크하며 "비었어요?" 를 반복하는 것입니다. 안에 사람이 있어도 계속 에너지(CPU)를 씁니다.

## 세마포어(Semaphore, 세마포)

**세마포어**는 정수형 변수를 이용한 동기화 도구입니다. Dijkstra(다익스트라)가 제안했습니다.

**쉽게 말하면:** 세마포어는 주차장 입구의 빈 자리 수 표시판 같습니다. 빈 자리(세마포어 값)가 0이면 기다리고, 1 이상이면 들어갈 수 있습니다.

두 가지 원자적(Atomic, 쪼갤 수 없는) 연산만 허용됩니다.

| 연산 | 동작 |
|------|------|
| **P 연산 (wait, 대기)** | 세마포어 값이 0보다 크면 1 감소 후 진행; 0이면 대기 |
| **V 연산 (signal, 신호)** | 세마포어 값을 1 증가; 대기 중인 프로세스가 있으면 깨움 |

**기억법:** P는 "Proberen"(네덜란드어로 시도하다), V는 "Verhogen"(네덜란드어로 증가시키다)

### 이진 세마포어 vs 범용 세마포어

| 종류 | 값 범위 | 용도 |
|------|---------|------|
| **이진 세마포어(Binary Semaphore)** | 0 또는 1 | 상호배제 (뮤텍스와 유사) |
| **범용(카운팅) 세마포어(Counting Semaphore)** | 0 이상 정수 | 자원 개수 관리 |

### 생산자-소비자 문제 (세마포어 해결)

**생산자-소비자 문제(Producer-Consumer Problem)**는 생산자가 데이터를 버퍼(Buffer, 임시 저장 공간)에 넣고 소비자가 꺼내는 상황에서 충돌을 막는 문제입니다. 예: 요리사(생산자)가 음식을 테이블에 올리고 손님(소비자)이 가져가는 상황.

```
세마포어:
- mutex = 1        (버퍼 상호배제)
- empty = N        (빈 버퍼 개수)
- full = 0         (꽉 찬 버퍼 개수)

생산자:
    P(empty)       ← 빈 공간 있을 때까지 대기
    P(mutex)       ← 버퍼 잠금
    [버퍼에 데이터 추가]
    V(mutex)       ← 버퍼 잠금 해제
    V(full)        ← 소비자에게 알림

소비자:
    P(full)        ← 데이터 있을 때까지 대기
    P(mutex)       ← 버퍼 잠금
    [버퍼에서 데이터 꺼냄]
    V(mutex)       ← 버퍼 잠금 해제
    V(empty)       ← 생산자에게 알림
```

## 모니터(Monitor)

**모니터**는 프로그래밍 언어 수준에서 동기화를 지원하는 고수준(High-level) 메커니즘입니다. 세마포어보다 사용하기 안전합니다.

**쉽게 말하면:** 세마포어는 직접 자물쇠(P/V 연산)를 관리해야 하지만, 모니터는 건물 관리인이 출입을 자동으로 통제해주는 것과 같습니다.

- 모니터 내부의 프로시저(Procedure, 절차)는 한 번에 하나의 프로세스만 실행 가능
- **조건 변수(Condition Variable)**를 통해 대기/깨움 처리
  - `wait()`: 현재 프로세스를 조건 큐에 넣고 모니터 양보
  - `signal()`: 조건 큐에서 대기 중인 프로세스 하나를 깨움

Java(자바)의 `synchronized` 블록이 모니터를 기반으로 합니다.

## 판독자-기록자 문제

**여러 판독자(Reader, 읽기만 하는 프로세스)**는 동시에 공유 데이터를 읽을 수 있지만, **기록자(Writer, 쓰는 프로세스)**가 쓸 때는 판독자와 기록자 모두 배제해야 합니다.

**비유:** 도서관에서 책을 여러 명이 동시에 읽는 건 괜찮지만, 누군가 책을 수정(기록)하는 동안은 다른 사람이 읽거나 쓸 수 없습니다.

| 정책 | 특징 | 문제 |
|------|------|------|
| **판독자 우선** | 판독자가 읽는 동안 새 판독자도 진입 가능 | 기록자 기아(Writer Starvation) |
| **기록자 우선** | 기록자가 대기 중이면 새 판독자 진입 불가 | 판독자 기아(Reader Starvation) |

## 핵심 정리

- 임계구역(Critical Section) 문제는 상호배제, 진행, 한정 대기 3가지 조건을 만족해야 한다
- 뮤텍스(Mutex)는 이진 잠금으로 상호배제를 구현하며, 바쁜 대기(Spin Lock)가 단점이다
- 세마포어의 P 연산은 자원 획득(대기), V 연산은 자원 해제(신호)이다
- 이진 세마포어는 상호배제, 범용 세마포어는 자원 개수 관리에 사용된다
- 모니터(Monitor)는 고수준 동기화 도구로 Java의 synchronized 블록이 기반이다

## 마무리 복습

<Quiz
  questionNumber={1}
  question="다음 중 세마포어에 대한 설명으로 적절하지 않은 것은?"
  options={['상호배제를 위해 사용한다', 'P, V 연산을 사용한다', '임계영역을 보호한다', '경쟁상태(Race Condition)를 유도한다']}
  correctAnswer={4}
  explanation="세마포어는 경쟁 상태를 방지하기 위한 동기화 도구입니다. 경쟁 상태를 유도하는 것이 아니라 해결합니다."
  optionExplanations={['세마포어는 상호배제 구현에 사용됩니다.', 'P 연산(wait)과 V 연산(signal)이 세마포어의 핵심입니다.', '세마포어는 임계영역 보호에 사용됩니다.', '세마포어는 경쟁 상태를 방지합니다. 유도가 아닙니다.']}
/>

<Quiz
  questionNumber={2}
  question="임계구역(Critical Section) 문제의 해결 조건으로 옳지 않은 것은?"
  options={['상호배제(Mutual Exclusion)', '진행(Progress)', '한정 대기(Bounded Waiting)', '선점(Preemption)']}
  correctAnswer={4}
  explanation="임계구역 문제의 3가지 해결 조건은 상호배제, 진행, 한정 대기입니다. 선점은 임계구역 문제의 요구조건이 아닙니다."
  optionExplanations={['상호배제는 임계구역 문제의 핵심 조건입니다.', '진행 조건은 유한 시간 내에 진입 결정이 이루어져야 합니다.', '한정 대기는 무한 대기를 방지합니다.', '선점은 임계구역 문제의 요구조건이 아닙니다.']}
/>

<Quiz
  questionNumber={3}
  question="세마포어의 P 연산(wait)이 수행하는 동작은?"
  options={['세마포어 값을 1 증가시키고 대기 중인 프로세스를 깨운다', '세마포어 값이 0보다 크면 1 감소하고 진행, 0이면 대기한다', '항상 세마포어 값을 0으로 초기화한다', '임계구역에서 즉시 빠져나온다']}
  correctAnswer={2}
  explanation="P 연산은 세마포어 값이 양수이면 1 감소하고 임계구역에 진입합니다. 값이 0이면 자원이 없으므로 대기합니다."
  optionExplanations={['값을 1 증가시키고 깨우는 것은 V 연산(signal)입니다.', 'P 연산은 자원 획득(진입) 연산입니다.', 'P 연산이 값을 0으로 초기화하지 않습니다.', '빠져나오는 것은 V 연산의 역할입니다.']}
/>

<Quiz
  questionNumber={4}
  question="이진 세마포어(Binary Semaphore)와 범용 세마포어(Counting Semaphore)의 차이로 옳은 것은?"
  options={['이진 세마포어는 0과 1만 가질 수 있고, 범용 세마포어는 0 이상의 정수를 가질 수 있다', '범용 세마포어는 0과 1만 가질 수 있고, 이진 세마포어는 무한한 값을 가진다', '이진 세마포어는 여러 자원 관리에, 범용 세마포어는 상호배제에 사용된다', '두 세마포어는 동일하며 이름만 다르다']}
  correctAnswer={1}
  explanation="이진 세마포어는 0 또는 1의 값만 가지며 상호배제에 주로 사용됩니다. 범용(카운팅) 세마포어는 0 이상의 정수로 여러 자원의 개수를 관리합니다."
  optionExplanations={['이진은 0/1, 범용은 0 이상 정수입니다.', '설명이 반대입니다.', '이진이 상호배제, 범용이 자원 개수 관리에 사용됩니다.', '두 세마포어는 값 범위와 용도가 다릅니다.']}
/>

<Quiz
  questionNumber={5}
  question="뮤텍스(Mutex)에서 스핀락(Spin Lock)의 단점은?"
  options={['잠금 획득이 너무 느리다', 'CPU를 소비하며 바쁜 대기를 하므로 CPU 낭비가 발생한다', '여러 프로세스가 동시에 잠금을 획득할 수 있다', '이진 세마포어와 동작 방식이 완전히 다르다']}
  correctAnswer={2}
  explanation="스핀락은 잠금이 해제될 때까지 반복문을 돌며 CPU를 계속 사용하는 바쁜 대기(Busy Waiting) 방식입니다. 대기 시간이 길면 CPU 낭비가 심합니다."
  optionExplanations={['스핀락이 느린 것이 단점이 아닙니다.', '바쁜 대기로 CPU를 낭비하는 것이 스핀락의 단점입니다.', '스핀락은 상호배제를 보장합니다. 동시 획득 불가능합니다.', '이진 세마포어와 유사한 동작을 합니다.']}
/>

<Quiz
  questionNumber={6}
  question="생산자-소비자 문제에서 세마포어 empty와 full의 역할은?"
  options={['empty는 꽉 찬 버퍼 수, full은 빈 버퍼 수를 나타낸다', 'empty는 빈 버퍼 수를 나타내고 full은 데이터가 있는 버퍼 수를 나타낸다', 'empty와 full 모두 상호배제를 위해 사용된다', '두 세마포어는 같은 용도로 사용된다']}
  correctAnswer={2}
  explanation="empty는 생산자가 데이터를 넣을 수 있는 빈 버퍼의 수(초기값 N), full은 소비자가 꺼낼 수 있는 데이터의 수(초기값 0)입니다."
  optionExplanations={['설명이 반대입니다.', 'empty는 빈 버퍼, full은 채워진 버퍼의 개수입니다.', '상호배제는 별도의 mutex 세마포어가 담당합니다.', '두 세마포어는 서로 다른 역할을 합니다.']}
/>

<Quiz
  questionNumber={7}
  question="모니터(Monitor)가 세마포어보다 사용하기 편리한 이유는?"
  options={['더 빠르게 동작하기 때문이다', 'P/V 연산을 직접 작성하지 않아도 언어 수준에서 자동으로 상호배제를 보장하기 때문이다', '여러 프로세스가 동시에 모니터 내부를 실행할 수 있기 때문이다', '운영체제가 모니터를 직접 관리하기 때문이다']}
  correctAnswer={2}
  explanation="모니터는 프로그래밍 언어 수준에서 상호배제를 자동으로 보장합니다. 프로그래머가 P/V 연산을 직접 작성하지 않아도 되므로 실수를 줄일 수 있습니다."
  optionExplanations={['모니터가 세마포어보다 반드시 빠른 것은 아닙니다.', '언어 수준의 자동 상호배제 보장이 모니터의 핵심 장점입니다.', '모니터는 한 번에 하나의 프로세스만 내부를 실행할 수 있습니다.', '모니터는 언어 런타임이 관리합니다.']}
/>

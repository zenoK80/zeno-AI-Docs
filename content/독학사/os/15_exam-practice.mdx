---
title: '15. 운영체제 예상기출문제'
description: '운영체제 전 범위(하드웨어 기초~UNIX/Linux)를 아우르는 예상기출 문제 20문항입니다.'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 공식 출제 범위를 바탕으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="CPU 내부에서 실제 산술·논리 연산을 수행하는 부분은?"
  options={['①제어장치', '②연산장치(ALU)', '③레지스터', '④버스']}
  correctAnswer={2}
  explanation="연산장치(ALU)는 덧셈·뺄셈 등 산술 연산과 비교·판단 등 논리 연산을 수행합니다."
  optionExplanations={['제어장치는 명령어를 해석하고 신호를 보냅니다.', '맞습니다. ALU가 실제 계산을 수행합니다.', '레지스터는 빠른 임시 저장 공간입니다.', '버스는 데이터가 오가는 통로입니다.']}
/>

<Quiz
  questionNumber={2}
  question="32비트 시스템이 표현할 수 있는 최대 메모리 용량은 약 얼마인가?"
  options={['①4MB', '②4GB', '③4TB', '④16GB']}
  correctAnswer={2}
  explanation="2^32 ≈ 4,294,967,296 바이트로 약 4GB입니다."
  optionExplanations={['단위가 너무 작습니다.', '맞습니다. 2^32≈4GB입니다.', '32비트로는 이 정도 용량을 표현할 수 없습니다.', '이는 32비트가 아닌 다른 비트 수의 근사값입니다.']}
/>

<Quiz
  questionNumber={3}
  question="완성된 실행 파일을 디스크에서 RAM으로 옮기는 과정은?"
  options={['①컴파일', '②링킹', '③로딩', '④인터럽트']}
  correctAnswer={3}
  explanation="로딩은 실행 파일을 디스크에서 RAM으로 적재하는 과정입니다."
  optionExplanations={['컴파일은 소스코드를 목적 코드로 번역하는 단계입니다.', '링킹은 목적 코드들을 결합하는 단계입니다.', '맞습니다. 로딩의 정의입니다.', '인터럽트는 별개의 개념입니다.']}
/>

<Quiz
  questionNumber={4}
  question="사용자 영역의 프로그램이 하드웨어 관련 작업을 요청할 때 사용하는 정해진 창구는?"
  options={['①직접 접근', '②시스템 호출(System Call)', '③이메일', '④레지스터 직접 수정']}
  correctAnswer={2}
  explanation="사용자 프로그램은 시스템 호출을 통해서만 커널에 작업을 요청할 수 있습니다."
  optionExplanations={['사용자 영역은 하드웨어에 직접 접근할 수 없습니다.', '맞습니다. 시스템 호출이 정해진 창구입니다.', '이메일은 무관한 개념입니다.', '레지스터 직접 수정은 허용되지 않습니다.']}
/>

<Quiz
  questionNumber={5}
  question="프로세스가 입출력을 기다리는 Waiting 상태에서 다시 CPU를 받으려면 어떤 상태를 거쳐야 하는가?"
  options={['①바로 Running', '②Ready', '③New', '④Terminated']}
  correctAnswer={2}
  explanation="Waiting 상태에서는 반드시 Ready 상태를 거친 뒤 스케줄러의 선택을 받아야 합니다."
  optionExplanations={['Waiting에서 바로 Running으로 갈 수 없습니다.', '맞습니다. Ready를 거쳐야 합니다.', 'New는 프로세스가 처음 생성될 때의 상태입니다.', 'Terminated는 종료 상태입니다.']}
/>

<Quiz
  questionNumber={6}
  question="같은 프로세스 내의 여러 스레드가 공유하는 것은?"
  options={['①각자의 스택만', '②코드, 데이터, 힙 영역', '③아무것도 공유하지 않는다', '④프로세스 ID만']}
  correctAnswer={2}
  explanation="같은 프로세스의 스레드들은 코드·데이터·힙을 공유하고, 스택만 각자 독립적으로 가집니다."
  optionExplanations={['스택은 오히려 각 스레드가 독립적으로 가집니다.', '맞습니다. 코드·데이터·힙을 공유합니다.', '많은 자원을 공유하는 것이 스레드의 핵심 특징입니다.', 'PID는 프로세스 단위로 부여됩니다.']}
/>

<Quiz
  questionNumber={7}
  question="A(실행시간4), B(실행시간2), C(실행시간6)가 동시에 도착했을 때 SJF의 실행 순서는?"
  options={['①A→B→C', '②B→A→C', '③C→A→B', '④B→C→A']}
  correctAnswer={2}
  explanation="SJF는 실행시간이 짧은 순서(B=2, A=4, C=6)로 처리합니다."
  optionExplanations={['도착 순서(FCFS)와 혼동한 답입니다.', '맞습니다. 실행시간이 짧은 순서입니다.', '긴 것부터 처리하는 잘못된 순서입니다.', '순서가 잘못되었습니다.']}
/>

<Quiz
  questionNumber={8}
  question="Round Robin 스케줄링에서 타임 퀀텀이 지나치게 작을 때 발생하는 문제는?"
  options={['①호위 효과', '②문맥 교환이 너무 자주 일어나 오버헤드 증가', '③기아 상태', '④교착상태']}
  correctAnswer={2}
  explanation="타임 퀀텀이 너무 작으면 프로세스 전환(문맥 교환)이 지나치게 자주 발생해 오버헤드가 커집니다."
  optionExplanations={['호위 효과는 FCFS의 문제입니다.', '맞습니다. 잦은 문맥 교환이 문제입니다.', 'RR은 순환 방식이라 기아 상태 위험이 낮습니다.', '교착상태는 스케줄링과 별개의 문제입니다.']}
/>

<Quiz
  questionNumber={9}
  question="임계구역 문제 해결을 위한 세 가지 조건에 포함되지 않는 것은?"
  options={['①상호 배제', '②진행', '③한정 대기', '④최대 속도 보장']}
  correctAnswer={4}
  explanation="임계구역 해결 조건은 상호 배제, 진행, 한정 대기이며, 속도 보장은 포함되지 않습니다."
  optionExplanations={['임계구역 해결의 필수 조건입니다.', '임계구역 해결의 필수 조건입니다.', '임계구역 해결의 필수 조건입니다.', '맞습니다. 속도 보장은 조건에 포함되지 않습니다.']}
/>

<Quiz
  questionNumber={10}
  question="세마포어 S값이 -3이라는 것이 의미하는 것은?"
  options={['①오류가 발생했다', '②3개의 프로세스가 signal을 기다리며 대기 중이다', '③자원이 3개 남아있다', '④3개의 프로세스가 실행 중이다']}
  correctAnswer={2}
  explanation="세마포어 값이 음수면, 그 절댓값만큼의 프로세스가 대기 중임을 나타냅니다."
  optionExplanations={['음수는 정상적인 대기 상태를 나타냅니다.', '맞습니다. 3개가 대기 중이라는 뜻입니다.', '양수일 때 자원이 남아있다는 뜻입니다.', '실행 중이 아니라 대기 중을 의미합니다.']}
/>

<Quiz
  questionNumber={11}
  question="교착상태가 발생하기 위한 네 가지 필요조건에 포함되지 않는 것은?"
  options={['①상호 배제', '②점유와 대기', '③빠른 처리 속도', '④순환 대기']}
  correctAnswer={3}
  explanation="교착상태의 네 가지 필요조건은 상호 배제, 점유와 대기, 비선점, 순환 대기입니다."
  optionExplanations={['필요조건에 포함됩니다.', '필요조건에 포함됩니다.', '맞습니다. 속도는 필요조건이 아닙니다.', '필요조건에 포함됩니다.']}
/>

<Quiz
  questionNumber={12}
  question="은행원 알고리즘(Banker's Algorithm)이 작동하기 위해 반드시 필요한 정보는?"
  options={['①각 프로세스의 실행 코드', '②각 프로세스의 최대 자원 요구량', '③사용자의 비밀번호', '④디스크의 파일 목록']}
  correctAnswer={2}
  explanation="은행원 알고리즘은 각 프로세스가 최대로 요구할 자원량을 미리 알아야 안전한 배정 여부를 판단할 수 있습니다."
  optionExplanations={['실행 코드 자체는 필요하지 않습니다.', '맞습니다. 최대 요구량을 미리 알아야 합니다.', '비밀번호와는 무관합니다.', '파일 목록과는 무관합니다.']}
/>

<Quiz
  questionNumber={13}
  question="할당된 공간 안에서 프로세스가 다 채우지 못해 남는 낭비를 무엇이라 하는가?"
  options={['①외부 단편화', '②내부 단편화', '③페이지 폴트', '④교착상태']}
  correctAnswer={2}
  explanation="할당된 블록 내부에서 남는 공간의 낭비를 내부 단편화라고 합니다."
  optionExplanations={['외부 단편화는 블록들 바깥에 흩어진 빈틈의 낭비입니다.', '맞습니다. 내부 단편화입니다.', '페이지 폴트는 가상 메모리 관련 개념입니다.', '교착상태는 별개의 문제입니다.']}
/>

<Quiz
  questionNumber={14}
  question="페이징이 동적분할과 달리 외부 단편화를 발생시키지 않는 이유는?"
  options={['①프로세스를 아예 메모리에 넣지 않아서', '②고정 크기의 페이지/프레임 단위로 나눠, 작은 빈 프레임도 모두 사용 가능하기 때문', '③메모리를 사용하지 않아서', '④디스크만 사용하기 때문']}
  correctAnswer={2}
  explanation="페이징은 모든 조각이 같은 크기라서, 어떤 빈 프레임이든 다음 페이지를 넣을 수 있어 외부 단편화가 생기지 않습니다."
  optionExplanations={['프로세스는 정상적으로 메모리에 로딩됩니다.', '맞습니다. 균일한 크기가 핵심 이유입니다.', '메모리를 정상적으로 사용합니다.', '페이징은 RAM(메모리)을 사용하는 기법입니다.']}
/>

<Quiz
  questionNumber={15}
  question="LRU(Least Recently Used) 알고리즘이 기반하는 원리는?"
  options={['①무작위성', '②지역성 원리(최근 사용된 것이 앞으로도 쓰일 가능성이 높음)', '③디스크 속도', '④프로세스 우선순위']}
  correctAnswer={2}
  explanation="LRU는 최근에 사용된 페이지일수록 앞으로도 다시 쓰일 가능성이 높다는 지역성 원리에 기반합니다."
  optionExplanations={['무작위가 아닌 특정 원리에 기반합니다.', '맞습니다. 지역성 원리입니다.', '디스크 속도와는 무관합니다.', '우선순위와는 무관한 페이지 교체 알고리즘입니다.']}
/>

<Quiz
  questionNumber={16}
  question="스래싱(Thrashing)이 발생했을 때 올바른 해결 방향은?"
  options={['①더 많은 프로세스를 동시에 실행한다', '②동시 실행 프로세스 수를 줄이거나 물리 메모리를 늘린다', '③CPU 속도를 낮춘다', '④디스크를 포맷한다']}
  correctAnswer={2}
  explanation="스래싱은 프레임 부족이 원인이므로, 동시 실행 프로세스를 줄이거나 메모리를 늘려야 합니다."
  optionExplanations={['오히려 스래싱을 악화시킵니다.', '맞습니다. 프로세스 수 조절 또는 메모리 증설이 해결책입니다.', 'CPU 속도와는 직접 관련이 없습니다.', '디스크 포맷은 해결책이 아닙니다.']}
/>

<Quiz
  questionNumber={17}
  question="UNIX 권한 표기 'rwxr-x---'에서 그룹(Group)의 권한은?"
  options={['①읽기·쓰기·실행 모두 가능', '②읽기·실행만 가능', '③아무 권한 없음', '④쓰기만 가능']}
  correctAnswer={2}
  explanation="세 글자씩 소유자(rwx), 그룹(r-x), 기타(---)를 나타내므로, 그룹은 읽기·실행만 가능합니다."
  optionExplanations={['이는 소유자의 권한입니다.', '맞습니다. 그룹은 r-x(읽기·실행)입니다.', '이는 기타 사용자의 권한입니다.', '쓰기 권한이 그룹에는 없습니다.']}
/>

<Quiz
  questionNumber={18}
  question="RAID 0에 대한 올바른 설명은?"
  options={['①데이터를 안전하게 보호하는 것이 주된 목적이다', '②속도 향상이 목적이며, 안정성은 오히려 낮다', '③디스크가 하나만 있어도 구성할 수 있다', '④가장 안전한 RAID 레벨이다']}
  correctAnswer={2}
  explanation="RAID 0은 스트라이핑으로 속도를 높이지만, 중복 저장이 없어 디스크 하나만 고장 나도 데이터가 손실됩니다."
  optionExplanations={['보호가 아닌 속도 향상이 목적입니다.', '맞습니다. 속도는 빠르지만 안정성이 낮습니다.', 'RAID는 여러 디스크를 묶는 기술입니다.', '가장 위험한 구성 중 하나입니다.']}
/>

<Quiz
  questionNumber={19}
  question="UNIX에서 새 프로세스를 만들 때 기존 프로세스를 복제하는 시스템 호출은?"
  options={['①exec()', '②fork()', '③kill()', '④wait()']}
  correctAnswer={2}
  explanation="fork()는 현재 프로세스를 복제해 새로운 자식 프로세스를 만드는 시스템 호출입니다."
  optionExplanations={['exec()는 프로세스를 새 프로그램으로 교체하는 함수입니다.', '맞습니다. fork()가 복제 역할을 합니다.', 'kill()은 프로세스를 종료시키는 함수입니다.', 'wait()는 자식 프로세스의 종료를 기다리는 함수입니다.']}
/>

<Quiz
  questionNumber={20}
  question="Linux의 /proc 디렉터리가 특별한 이유는?"
  options={['①디스크 용량을 가장 많이 차지해서', '②실제 디스크 파일이 아니라 실행 중인 프로세스 정보를 실시간으로 파일처럼 제공해서', '③사용자가 접근할 수 없어서', '④가장 오래된 디렉터리라서']}
  correctAnswer={2}
  explanation="/proc는 가상 파일 시스템으로, 커널이 실시간 프로세스 정보를 파일 형태로 제공하는 디렉터리입니다."
  optionExplanations={['가상 파일이라 실제 디스크 용량을 거의 차지하지 않습니다.', '맞습니다. 실시간 정보를 파일처럼 제공하는 것이 특징입니다.', '일반적으로 사용자가 정보를 확인할 수 있습니다.', '생성 시기와는 무관한 특징입니다.']}
/>

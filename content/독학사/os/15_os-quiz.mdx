---
title: '15. 운영체제 예상기출문제'
description: '운영체제 전 범위에 걸친 예상기출문제 20개로 실전 감각을 기른다.'
---

<Callout type="info">
  **이번 문서의 목표:** 운영체제 전 범위의 핵심 개념을 기출 유형 문제로 점검하고, 틀린 문제의 개념을 복습하여 실전 시험에 대비한다.
</Callout>

## 예상기출문제 (20문항)

<Quiz
  questionNumber={1}
  question="다음 중 세마포어에 대한 설명으로 적절하지 않은 것은?"
  options={['상호배제를 위해 사용한다', 'P, V 연산(operation)을 사용한다', '임계영역을 보호한다', '경쟁상태(race condition)를 유도한다']}
  correctAnswer={4}
  explanation="세마포어는 경쟁 상태를 유도하는 것이 아니라 임계구역을 보호하여 경쟁 상태를 방지하는 동기화 도구입니다."
  optionExplanations={['세마포어는 상호배제를 위해 사용됩니다.', 'P(wait)와 V(signal) 연산이 세마포어의 핵심입니다.', '세마포어는 임계영역을 보호합니다.', '경쟁 상태를 유도하는 것이 아니라 방지합니다. (맞습니다.)']}
/>

<Quiz
  questionNumber={2}
  question="교착상태(deadlock)의 발생 조건에 해당하는 것들을 나열하면?"
  options={['상호배제, 임계구역, 점유와 대기, 환형 대기', '상호배제, 점유와 대기, 비선점, 환형 대기', '상호배제, 임계구역, 비선점, 바쁜 대기', '점유와 대기, 임계구역, 바쁜 대기, 환형 대기']}
  correctAnswer={2}
  explanation="교착상태의 4가지 발생 조건은 상호배제(Mutual Exclusion), 점유와 대기(Hold and Wait), 비선점(No Preemption), 환형 대기(Circular Wait)입니다."
  optionExplanations={['임계구역은 교착상태 조건이 아닙니다.', '교착상태의 4가지 조건이 모두 포함됩니다. (맞습니다.)', '임계구역, 바쁜 대기는 교착상태 조건이 아닙니다.', '임계구역, 바쁜 대기는 교착상태 조건이 아닙니다.']}
/>

<Quiz
  questionNumber={3}
  question="실행 시간이 두 시간씩 소요되는 세 개의 작업이 동시에 주어질 때, FCFS 정책에 따라 스케줄링을 하면 평균 반환 시간은?"
  options={['2시간', '3시간', '4시간', '6시간']}
  correctAnswer={3}
  explanation="FCFS로 처리 시 각 작업의 반환 시간: 2시간 + 4시간 + 6시간. 평균 반환 시간 = (2+4+6)/3 = 4시간"
  optionExplanations={['2시간은 단일 작업의 실행 시간입니다.', '3시간은 틀린 계산입니다.', '(2+4+6)/3 = 4시간이 정답입니다. (맞습니다.)', '6시간은 마지막 작업의 반환 시간입니다.']}
/>

<Quiz
  questionNumber={4}
  question="다중 프로그래밍 환경에서 메모리 보호를 위해 사용되는 레지스터는?"
  options={['경계 레지스터', '프로그램 카운터', '범용 레지스터', '재배치 레지스터']}
  correctAnswer={1}
  explanation="경계(Limit) 레지스터는 프로세스의 메모리 영역 크기를 저장하여 다른 프로세스의 영역을 침범하지 못하도록 보호합니다."
  optionExplanations={['경계 레지스터가 메모리 보호에 사용됩니다. (맞습니다.)', '프로그램 카운터는 다음 명령어 주소를 저장합니다.', '범용 레지스터는 일반 계산에 사용됩니다.', '재배치 레지스터는 기준 레지스터의 다른 이름입니다.']}
/>

<Quiz
  questionNumber={5}
  question="페이지 교체 기법에서 교체될 페이지로 가장 이상적인 것은?"
  options={['참조된 횟수가 가장 적은 페이지', '주기억장치에 가장 오래 있었던 페이지', '이전에 가장 오랫동안 사용되지 않은 페이지', '이후에 가장 오랫동안 사용되지 않을 페이지']}
  correctAnswer={4}
  explanation="OPT(최적 알고리즘)는 앞으로 가장 오랫동안 사용되지 않을 페이지를 교체합니다. 이론적으로 최소의 페이지 폴트를 보장하는 이상적인 방법입니다."
  optionExplanations={['참조 횟수 기준은 LFU 알고리즘입니다.', '가장 오래 있었던 페이지 교체는 FIFO입니다.', '이전에 오래 사용 안 한 것은 LRU입니다.', '이후에 가장 오래 사용 안 할 것을 교체하는 OPT가 가장 이상적입니다. (맞습니다.)']}
/>

<Quiz
  questionNumber={6}
  question="운영체제에서 커널(Kernel)에 대한 설명으로 옳은 것은?"
  options={['응용 프로그램과 사용자 간의 인터페이스만 담당한다', '운영체제의 핵심 부분으로 항상 메모리에 상주하며 하드웨어를 직접 제어한다', '커널은 사용자 모드에서만 실행된다', '커널은 디스크에만 저장되며 필요할 때 메모리에 올라온다']}
  correctAnswer={2}
  explanation="커널은 운영체제의 핵심 부분으로 항상 주기억장치에 상주합니다. 커널 모드에서 실행되며 CPU, 메모리, 파일, 입출력 등 모든 하드웨어 자원을 직접 제어합니다."
  optionExplanations={['커널은 인터페이스를 넘어 하드웨어 자원 전체를 관리합니다.', '커널은 메모리에 항상 상주하며 하드웨어를 제어합니다. (맞습니다.)', '커널은 커널 모드에서 실행됩니다.', '커널은 항상 메모리에 있습니다.']}
/>

<Quiz
  questionNumber={7}
  question="프로세스 상태에서 '대기(Waiting)' 상태에서 '준비(Ready)' 상태로 전환되는 시점은?"
  options={['타임 퀀텀이 만료되었을 때', '기다리던 I/O 작업이 완료되었을 때', '새로운 프로세스가 생성되었을 때', 'CPU를 할당받았을 때']}
  correctAnswer={2}
  explanation="프로세스가 대기 상태에 있는 것은 I/O 완료나 이벤트 발생을 기다리기 때문입니다. 해당 이벤트가 발생하면 준비 상태로 이동합니다."
  optionExplanations={['타임 퀀텀 만료는 실행→준비 전환입니다.', 'I/O 완료 시 대기→준비로 전환됩니다. (맞습니다.)', '새 프로세스 생성은 생성→준비 전환입니다.', 'CPU 할당은 준비→실행 전환입니다.']}
/>

<Quiz
  questionNumber={8}
  question="가상 메모리에서 스래싱(Thrashing)의 원인은?"
  options={['CPU 클럭 속도가 너무 높을 때', '다중 프로그래밍 정도가 너무 높아 각 프로세스에 할당된 프레임이 부족할 때', '페이지 크기가 너무 클 때', '디스크 용량이 부족할 때']}
  correctAnswer={2}
  explanation="스래싱은 너무 많은 프로세스를 동시에 실행하여 각 프로세스에 충분한 프레임이 없을 때 발생합니다. 페이지 폴트가 빈번해져 대부분의 시간을 페이지 교체에 소비합니다."
  optionExplanations={['CPU 클럭 속도는 스래싱과 무관합니다.', '프레임 부족이 스래싱의 원인입니다. (맞습니다.)', '페이지 크기는 스래싱의 직접적 원인이 아닙니다.', '디스크 용량 부족은 별개 문제입니다.']}
/>

<Quiz
  questionNumber={9}
  question="SCAN 디스크 스케줄링에서 헤드의 이동 방식은?"
  options={['요청 도착 순서대로 처리', '현재 위치에서 가장 가까운 요청부터 처리', '한쪽 끝에서 반대쪽 끝으로 이동하며 경로의 요청 처리, 끝에서 방향 전환', '항상 같은 방향으로만 이동 후 처음으로 복귀']}
  correctAnswer={3}
  explanation="SCAN(엘리베이터 알고리즘)은 헤드가 한쪽 끝에서 반대쪽 끝으로 이동하며 경로상의 모든 요청을 처리하고 끝에 도달하면 방향을 바꿉니다."
  optionExplanations={['도착 순서 처리는 FCFS입니다.', '가장 가까운 요청 처리는 SSTF입니다.', 'SCAN은 왕복 스윕 방식입니다. (맞습니다.)', '단방향 후 복귀는 C-SCAN입니다.']}
/>

<Quiz
  questionNumber={10}
  question="페이지 테이블에서 TLB(Translation Lookaside Buffer)를 사용하는 이유는?"
  options={['페이지 테이블의 크기를 줄이기 위해', '주소 변환에 필요한 메모리 접근 횟수를 줄이기 위해', '외부 단편화를 제거하기 위해', '페이지 폴트를 방지하기 위해']}
  correctAnswer={2}
  explanation="페이지 테이블은 메모리에 있어 주소 변환마다 메모리 접근이 필요합니다. TLB는 최근 변환 결과를 캐시에 저장하여 TLB 히트 시 메모리 접근 없이 빠르게 변환할 수 있습니다."
  optionExplanations={['TLB는 페이지 테이블 크기를 줄이지 않습니다.', '메모리 접근 횟수 감소가 TLB의 목적입니다. (맞습니다.)', '외부 단편화 제거는 페이징의 특성입니다.', '페이지 폴트는 TLB와 직접적으로 관련이 없습니다.']}
/>

<Quiz
  questionNumber={11}
  question="프로세스 동기화에서 임계구역(Critical Section)의 요구조건이 아닌 것은?"
  options={['상호배제(Mutual Exclusion)', '진행(Progress)', '한정 대기(Bounded Waiting)', '선점(Preemption)']}
  correctAnswer={4}
  explanation="임계구역 문제의 3가지 요구조건은 상호배제, 진행, 한정 대기입니다. 선점은 임계구역 문제의 요구조건이 아닙니다."
  optionExplanations={['상호배제는 임계구역의 핵심 조건입니다.', '진행 조건은 유한 시간 내 진입 결정을 보장합니다.', '한정 대기는 무한 대기를 방지합니다.', '선점은 임계구역 요구조건이 아닙니다. (맞습니다.)']}
/>

<Quiz
  questionNumber={12}
  question="Unix/Linux에서 새로운 프로세스를 생성하는 시스템 호출은?"
  options={['exec()', 'fork()', 'wait()', 'exit()']}
  correctAnswer={2}
  explanation="fork() 시스템 호출은 현재 프로세스(부모)를 복제하여 새로운 자식 프로세스를 생성합니다. exec()는 현재 프로세스에 새 프로그램을 적재합니다."
  optionExplanations={['exec()는 현재 프로세스에 새 프로그램을 올립니다.', 'fork()가 새 프로세스를 생성합니다. (맞습니다.)', 'wait()는 자식 프로세스가 종료될 때까지 대기합니다.', 'exit()는 프로세스를 종료합니다.']}
/>

<Quiz
  questionNumber={13}
  question="내부 단편화(Internal Fragmentation)가 발생하는 메모리 할당 방식은?"
  options={['동적 분할 (Dynamic Partition)', '고정 분할 (Fixed Partition)', '세그멘테이션 (Segmentation)', '연결 할당 (Linked Allocation)']}
  correctAnswer={2}
  explanation="고정 분할에서는 메모리를 고정 크기 파티션으로 나누어, 프로세스가 파티션보다 작으면 남은 공간이 낭비됩니다. 이것이 내부 단편화입니다."
  optionExplanations={['동적 분할은 외부 단편화가 발생합니다.', '고정 분할에서 내부 단편화가 발생합니다. (맞습니다.)', '세그멘테이션은 가변 크기로 내부 단편화가 없습니다.', '연결 할당은 파일 시스템에서 외부 단편화와 관련됩니다.']}
/>

<Quiz
  questionNumber={14}
  question="교착상태 회피에서 은행원 알고리즘(Banker's Algorithm)의 목적은?"
  options={['교착상태 발생 후 프로세스를 종료한다', '시스템이 항상 안전 상태(Safe State)를 유지하도록 자원을 할당한다', '교착상태 발생 조건 4가지를 모두 제거한다', '프로세스가 필요한 자원을 한꺼번에 요청하게 한다']}
  correctAnswer={2}
  explanation="은행원 알고리즘은 자원 요청 시 가상으로 할당해보고 안전 상태가 유지되면 실제 할당합니다. 항상 안전 상태를 보장하여 교착상태를 회피합니다."
  optionExplanations={['프로세스 종료는 복구(Recovery) 방법입니다.', '안전 상태 유지가 은행원 알고리즘의 목적입니다. (맞습니다.)', '4가지 조건 제거는 예방(Prevention) 방법입니다.', '한꺼번에 요청은 예방 방법 중 하나입니다.']}
/>

<Quiz
  questionNumber={15}
  question="라운드 로빈(Round Robin) 스케줄링에서 타임 퀀텀이 매우 클 때 어떤 알고리즘과 유사해지는가?"
  options={['SSTF', 'SJF', 'FCFS', 'Priority']}
  correctAnswer={3}
  explanation="타임 퀀텀이 매우 크면 각 프로세스가 한 번에 완료될 때까지 CPU를 점유하므로 도착 순서대로 처리하는 FCFS와 동일하게 동작합니다."
  optionExplanations={['SSTF는 디스크 스케줄링입니다.', 'SJF는 실행 시간 기준입니다.', '타임 퀀텀이 크면 FCFS와 같아집니다. (맞습니다.)', 'Priority는 우선순위 기반입니다.']}
/>

<Quiz
  questionNumber={16}
  question="페이징에서 논리 주소를 물리 주소로 변환하는 과정에서 필요한 정보는?"
  options={['페이지 크기와 세그먼트 테이블', '페이지 번호와 페이지 테이블', '프레임 번호와 세그먼트 번호', '오프셋과 세그먼트 테이블']}
  correctAnswer={2}
  explanation="페이징의 주소 변환: 논리 주소에서 페이지 번호(p)를 추출하고 페이지 테이블에서 해당 프레임 번호(f)를 찾아 물리 주소 = f * 페이지크기 + 오프셋으로 계산합니다."
  optionExplanations={['세그먼트 테이블은 세그멘테이션에서 사용합니다.', '페이지 번호와 페이지 테이블로 물리 주소를 계산합니다. (맞습니다.)', '세그먼트 번호는 세그멘테이션에서 사용합니다.', '세그먼트 테이블은 페이징에서 사용하지 않습니다.']}
/>

<Quiz
  questionNumber={17}
  question="스레드(Thread)에 대한 설명으로 옳지 않은 것은?"
  options={['하나의 프로세스 내에서 여러 스레드가 실행될 수 있다', '스레드들은 프로세스의 코드, 데이터, 힙을 공유한다', '각 스레드는 독립적인 스택을 가진다', '스레드가 생성될 때 별도의 독립된 주소 공간이 생성된다']}
  correctAnswer={4}
  explanation="스레드는 프로세스 내부에서 동작하므로 별도의 독립된 주소 공간을 생성하지 않습니다. 같은 프로세스의 주소 공간(코드, 데이터, 힙)을 공유합니다."
  optionExplanations={['하나의 프로세스에서 여러 스레드가 실행 가능합니다.', '스레드는 코드, 데이터, 힙을 공유합니다.', '스택은 각 스레드가 개별적으로 보유합니다.', '스레드는 프로세스의 주소 공간을 공유하므로 별도 주소 공간이 없습니다. (맞습니다.)']}
/>

<Quiz
  questionNumber={18}
  question="파일 시스템에서 inode에 저장되지 않는 정보는?"
  options={['파일 크기', '파일 소유자', '파일 이름', '데이터 블록 포인터']}
  correctAnswer={3}
  explanation="inode는 파일 메타데이터(크기, 소유자, 권한, 타임스탬프)와 데이터 블록 포인터를 저장합니다. 파일 이름은 inode가 아닌 디렉터리 항목에 저장됩니다."
  optionExplanations={['파일 크기는 inode에 있습니다.', '파일 소유자는 inode에 있습니다.', '파일 이름은 디렉터리에 저장됩니다. (맞습니다.)', '데이터 블록 포인터는 inode의 핵심 정보입니다.']}
/>

<Quiz
  questionNumber={19}
  question="DMA를 사용하는 주된 이유는?"
  options={['소량 데이터 전송을 빠르게 처리하기 위해', 'CPU 개입 없이 대량 데이터를 메모리와 장치 간에 직접 전송하여 CPU 부하를 줄이기 위해', '인터럽트 발생 횟수를 늘리기 위해', '디스크 스케줄링을 개선하기 위해']}
  correctAnswer={2}
  explanation="DMA는 CPU의 개입 없이 DMA 제어기가 메모리와 I/O 장치 사이에서 대량 데이터를 직접 전송합니다. CPU는 전송이 완료될 때 인터럽트로 통보받으며 그 동안 다른 작업을 수행합니다."
  optionExplanations={['소량 데이터는 인터럽트 방식이 효율적입니다.', 'CPU 부하 감소와 대량 전송 지원이 DMA의 목적입니다. (맞습니다.)', 'DMA는 인터럽트를 줄입니다.', '디스크 스케줄링과는 직접 관련이 없습니다.']}
/>

<Quiz
  questionNumber={20}
  question="요구 페이징(Demand Paging)에서 페이지 폴트(Page Fault)가 발생하는 조건은?"
  options={['페이지 테이블에서 유효-무효 비트가 1인 경우', '페이지 테이블에서 유효-무효 비트가 0인 경우', 'TLB 히트가 발생한 경우', '프레임 수가 페이지 수보다 많은 경우']}
  correctAnswer={2}
  explanation="페이지 테이블의 유효-무효 비트가 0이면 해당 페이지가 현재 물리 메모리에 없다는 의미입니다. CPU가 이 페이지에 접근하면 페이지 폴트 트랩이 발생합니다."
  optionExplanations={['유효 비트가 1이면 메모리에 있으므로 페이지 폴트가 없습니다.', '유효 비트가 0이면 메모리에 없어 페이지 폴트가 발생합니다. (맞습니다.)', 'TLB 히트는 빠른 주소 변환이 가능한 경우입니다.', '프레임이 많으면 오히려 페이지 폴트가 줄어듭니다.']}
/>

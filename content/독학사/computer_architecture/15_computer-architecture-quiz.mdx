---
title: '15. 컴퓨터 구조 종합 모의고사'
description: '컴퓨터 구조 전 범위(01~14)를 아우르는 종합 모의고사 20문항으로 실력을 최종 점검한다.'
---

<Callout type="info">
  **이번 문서의 목표:** 컴퓨터 구조 전 범위의 핵심 개념을 종합 모의고사 형태로 점검한다. 각 문제에 대한 해설을 통해 틀린 부분을 즉시 확인하고 보완한다.
</Callout>

## 종합 모의고사 안내

- 총 **20문항** (각 5점 = 100점 만점)
- 문항 순서: 컴퓨터 구조 개요 → 데이터 표현 → 논리 게이트 → 조합/순서 논리 회로 → CPU → 명령어 → 파이프라인 → 메모리 → 입출력 → 병렬/고성능
- 정답과 해설을 확인하며 부족한 단원을 복습하세요.

---

<Quiz
  questionNumber={1}
  question="폰 노이만 구조(Von Neumann Architecture)의 핵심 특징으로 옳은 것은?"
  options={['명령어와 데이터가 별도의 메모리에 저장된다', '프로그램이 데이터와 함께 메모리에 저장되며 순차적으로 실행된다', 'CPU가 병렬로 여러 명령어를 동시에 처리한다', '입출력 장치가 CPU 내부에 통합되어 있다']}
  correctAnswer={2}
  explanation="폰 노이만 구조(내장 프로그램 방식)의 핵심은 프로그램(명령어)과 데이터를 동일한 주기억장치에 저장하고, CPU가 순차적으로 명령어를 인출하여 실행한다는 점입니다. 명령어와 데이터가 같은 버스를 공유하여 병목현상이 발생하는 단점(폰 노이만 병목)도 있습니다."
  optionExplanations={['명령어와 데이터를 분리하는 것은 하버드 구조입니다.', '프로그램이 메모리에 저장되어 순차 실행되는 것이 폰 노이만 구조의 핵심입니다. 정답입니다.', '폰 노이만 구조는 기본적으로 순차 실행입니다.', '입출력 장치는 CPU 외부에 있습니다.']}
/>

<Quiz
  questionNumber={2}
  question="10진수 -27을 8비트 2의 보수(Two's Complement)로 표현한 것은?"
  options={['11100101', '10011011', '00011011', '11011011']}
  correctAnswer={1}
  explanation="+27 = 00011011(2). 1의 보수: 11100100. 2의 보수: 11100100 + 1 = 11100101. 따라서 -27의 8비트 2의 보수 표현은 11100101입니다. 검증: 11100101에서 MSB가 1이므로 음수, 다시 2의 보수를 취하면 00011011 = 27."
  optionExplanations={['-27의 2의 보수 표현은 11100101입니다. 정답입니다.', '나머지는 잘못된 값입니다.', '나머지는 잘못된 값입니다.', '나머지는 잘못된 값입니다.']}
/>

<Quiz
  questionNumber={3}
  question="NAND 게이트의 진리표에서 입력 A=1, B=1일 때 출력은?"
  options={['0', '1', 'A', 'B']}
  correctAnswer={1}
  explanation="NAND 게이트는 AND 게이트의 출력을 반전(NOT)한 것입니다. AND의 경우 A=1, B=1이면 출력 = 1입니다. NAND는 이를 반전하므로 출력 = NOT(1 AND 1) = NOT(1) = 0입니다."
  optionExplanations={['A=1, B=1일 때 NAND 출력은 0입니다. 정답입니다.', 'A=1, B=1에서 AND는 1이고 이를 반전한 NAND는 0입니다.', 'A는 출력값이 아닙니다.', 'B는 출력값이 아닙니다.']}
/>

<Quiz
  questionNumber={4}
  question="전가산기(Full Adder)가 반가산기(Half Adder)와 다른 점은?"
  options={['전가산기는 XOR 게이트를 사용하지 않는다', '전가산기는 아래 자리에서 올라오는 올림수(Carry In)를 입력으로 받는다', '전가산기는 출력이 1비트만 있다', '전가산기는 순서 논리 회로에 속한다']}
  correctAnswer={2}
  explanation="반가산기(Half Adder)는 2개의 1비트 입력(A, B)만 받아 합(Sum)과 올림수(Carry Out)를 출력합니다. 전가산기(Full Adder)는 여기에 아래 자리에서 올라오는 올림수(Carry In, Cin)를 추가 입력으로 받습니다. 이를 통해 여러 비트 가산기를 연결하여 다중 비트 덧셈을 수행할 수 있습니다."
  optionExplanations={['전가산기도 XOR 게이트를 사용합니다.', '캐리 입력(Carry In)을 추가로 받는 것이 전가산기의 특징입니다. 정답입니다.', '전가산기도 합(Sum)과 올림수(Carry Out) 2비트를 출력합니다.', '조합 논리 회로에 속합니다.']}
/>

<Quiz
  questionNumber={5}
  question="플립플롭(Flip-Flop)의 역할로 가장 적절한 것은?"
  options={['여러 비트를 동시에 연산하는 ALU의 구성 요소이다', '클럭(Clock) 신호에 맞춰 1비트 데이터를 저장하는 기본 기억 소자이다', '주소를 디코딩하는 조합 논리 회로이다', '데이터를 직렬로 전송하는 버스 인터페이스이다']}
  correctAnswer={2}
  explanation="플립플롭(Flip-Flop)은 클럭 신호의 엣지(상승 또는 하강)에 맞춰 1비트 데이터를 저장하는 기본 기억 소자입니다. 순서 논리 회로의 핵심이며, 여러 개를 조합하면 레지스터(다중 비트 저장), 카운터, 시프트 레지스터 등을 만들 수 있습니다."
  optionExplanations={['ALU는 산술/논리 연산을 수행하는 회로입니다.', '클럭 신호에 맞춰 1비트를 저장하는 것이 플립플롭의 역할입니다. 정답입니다.', '주소 디코딩은 디코더(Decoder)의 역할입니다.', '버스 인터페이스는 데이터 전송 회로입니다.']}
/>

<Quiz
  questionNumber={6}
  question="CPU의 PC(Program Counter, 프로그램 카운터) 레지스터의 역할은?"
  options={['연산 결과를 임시로 저장한다', '다음에 실행할 명령어의 메모리 주소를 저장한다', '현재 실행 중인 명령어를 저장한다', '메모리 접근 시 데이터를 임시 저장한다']}
  correctAnswer={2}
  explanation="PC(Program Counter)는 다음에 실행할 명령어가 저장된 메모리 주소를 저장합니다. 명령어 인출(Fetch) 후 PC는 자동으로 다음 명령어 주소로 증가합니다. 분기(Branch) 명령어 실행 시 PC가 분기 목적지 주소로 변경됩니다."
  optionExplanations={['연산 결과 임시 저장은 누산기(AC) 또는 범용 레지스터입니다.', '다음 실행할 명령어 주소를 저장하는 것이 PC의 역할입니다. 정답입니다.', '현재 실행 중인 명령어를 저장하는 것은 IR(Instruction Register)입니다.', '메모리 접근 데이터를 임시 저장하는 것은 MBR(Memory Buffer Register)입니다.']}
/>

<Quiz
  questionNumber={7}
  question="즉시 주소 지정 방식(Immediate Addressing Mode)의 특징은?"
  options={['명령어 내에 실제 데이터가 직접 포함되어 있다', '명령어 내에 메모리 주소가 들어 있고 해당 주소에서 데이터를 읽는다', '레지스터 번호가 명령어에 있고 해당 레지스터에서 데이터를 읽는다', '명령어에 있는 주소가 실제 데이터의 주소를 가리키는 주소이다']}
  correctAnswer={1}
  explanation="즉시 주소 지정(Immediate Addressing)은 명령어의 피연산자 필드에 실제 데이터(상수) 값이 직접 포함됩니다. 메모리 접근이 필요 없어 가장 빠르지만, 데이터 크기가 명령어 피연산자 필드 크기로 제한됩니다. 예: MOV R1, #5 (R1에 상수 5를 저장)"
  optionExplanations={['명령어 내에 실제 데이터가 직접 들어있는 것이 즉시 주소 지정입니다. 정답입니다.', '메모리 주소로 데이터를 읽는 것은 직접 주소 지정(Direct Addressing)입니다.', '레지스터에서 데이터를 읽는 것은 레지스터 주소 지정(Register Addressing)입니다.', '주소의 주소를 사용하는 것은 간접 주소 지정(Indirect Addressing)입니다.']}
/>

<Quiz
  questionNumber={8}
  question="파이프라인에서 데이터 해저드(Data Hazard)를 해결하는 방법으로 가장 보편적인 것은?"
  options={['분기 예측(Branch Prediction)', '포워딩(Forwarding/Bypassing)', '하버드 구조(Harvard Architecture)', '투기적 실행(Speculative Execution)']}
  correctAnswer={2}
  explanation="데이터 해저드는 이전 명령어의 결과가 아직 레지스터에 저장되지 않았는데 다음 명령어가 그 값을 필요로 할 때 발생합니다. 포워딩(Forwarding)은 ALU 결과를 레지스터 파일에 쓰기 전에 직접 다음 명령어의 입력으로 전달하여 스톨(Stall) 없이 처리하는 가장 보편적인 해결책입니다."
  optionExplanations={['분기 예측은 제어 해저드(Control Hazard) 해결 방법입니다.', '포워딩은 데이터 해저드의 가장 대표적인 해결 방법입니다. 정답입니다.', '하버드 구조는 구조적 해저드(Structural Hazard) 해결 방법입니다.', '투기적 실행은 제어 해저드 해결 방법입니다.']}
/>

<Quiz
  questionNumber={9}
  question="SRAM이 DRAM보다 캐시 메모리에 적합한 이유는?"
  options={['용량이 크고 가격이 저렴하기 때문이다', '리프레시(Refresh)가 필요 없고 속도가 빠르기 때문이다', '비휘발성이라 전원 차단 시에도 데이터가 유지되기 때문이다', '커패시터를 사용하여 집적도가 높기 때문이다']}
  correctAnswer={2}
  explanation="SRAM(Static RAM)은 플립플롭으로 데이터를 저장하여 리프레시가 필요 없고 속도가 매우 빠릅니다(수 ns). 반면 DRAM은 커패시터를 사용하여 주기적 리프레시가 필요하고 상대적으로 느립니다. 캐시 메모리는 CPU와 매우 가까이서 빠르게 동작해야 하므로 SRAM이 적합합니다."
  optionExplanations={['SRAM은 DRAM보다 용량이 작고 가격이 비쌉니다.', '리프레시 불필요와 빠른 속도가 SRAM이 캐시에 적합한 이유입니다. 정답입니다.', 'SRAM도 DRAM도 모두 휘발성(전원 차단 시 데이터 소멸)입니다.', '커패시터를 사용하여 집적도가 높은 것은 DRAM의 특성입니다.']}
/>

<Quiz
  questionNumber={10}
  question="캐시 유효 접근 시간(EAT) 공식 EAT = h × Tc + (1-h) × Tm에서 히트율(h)이 높아질수록?"
  options={['EAT가 메인 메모리 접근 시간(Tm)에 가까워진다', 'EAT가 캐시 접근 시간(Tc)에 가까워진다', 'EAT와 히트율은 무관하다', 'EAT가 Tc와 Tm의 산술 평균이 된다']}
  correctAnswer={2}
  explanation="EAT = h × Tc + (1-h) × Tm에서 h가 1에 가까워지면 (1-h)가 0에 가까워집니다. 따라서 EAT = 1 × Tc + 0 × Tm = Tc가 됩니다. 즉 히트율이 높을수록 EAT는 캐시 접근 시간(Tc)에 가까워집니다. 이것이 캐시의 핵심 가치입니다."
  optionExplanations={['h가 낮아질수록 EAT가 Tm에 가까워집니다.', 'h가 높아질수록 EAT가 Tc(캐시 접근 시간)에 가까워집니다. 정답입니다.', 'EAT는 히트율에 직접 영향을 받습니다.', '히트율이 0.5일 때만 산술 평균이 됩니다.']}
/>

<Quiz
  questionNumber={11}
  question="LRU(Least Recently Used) 캐시 교체 알고리즘의 기본 원리는?"
  options={['캐시에 가장 먼저 들어온 블록을 교체한다', '가장 최근에 사용된 블록을 교체한다', '가장 오래 사용되지 않은 블록을 교체한다', '무작위로 블록을 선택하여 교체한다']}
  correctAnswer={3}
  explanation="LRU(Least Recently Used)는 시간적 지역성을 활용한 알고리즘입니다. 최근에 사용된 블록은 곧 다시 사용될 가능성이 높으므로 유지하고, 가장 오래 전에 사용된(최근 사용이 없는) 블록을 교체합니다. 실제 구현에서는 사용 시간 스탬프나 카운터를 사용합니다."
  optionExplanations={['가장 먼저 들어온 블록 교체는 FIFO 알고리즘입니다.', '가장 최근 사용된 블록을 교체하면 지역성을 해치므로 반대 방향입니다.', '가장 오래 사용되지 않은 블록을 교체하는 것이 LRU입니다. 정답입니다.', '무작위 교체는 Random 알고리즘입니다.']}
/>

<Quiz
  questionNumber={12}
  question="RAID 5의 특징으로 옳은 것은?"
  options={['디스크 2개 고장 시에도 데이터 복구가 가능하다', '데이터 중복 없이 스트라이핑만 사용하여 최고 성능을 낸다', '패리티를 모든 디스크에 분산 저장하며 디스크 1개 고장 시 복구 가능하다', '미러링만 사용하여 용량 효율이 50%이다']}
  correctAnswer={3}
  explanation="RAID 5는 데이터와 패리티 정보를 모든 참여 디스크에 고르게 분산하는 방식입니다. 최소 3개의 디스크가 필요하며, 디스크 하나가 고장 나도 나머지 디스크의 데이터와 패리티로 복구할 수 있습니다. 용량 효율 (n-1)/n으로 성능과 안정성의 균형이 좋아 가장 많이 사용됩니다."
  optionExplanations={['2개 고장 복구는 RAID 6의 특성입니다.', '스트라이핑만 사용하는 것은 RAID 0입니다.', 'RAID 5는 분산 패리티로 디스크 1개 고장 복구가 가능합니다. 정답입니다.', '미러링으로 용량 50%는 RAID 1의 특성입니다.']}
/>

<Quiz
  questionNumber={13}
  question="디스크 스케줄링에서 SCAN 알고리즘이 SSTF보다 유리한 점은?"
  options={['헤드 이동 거리가 항상 SSTF보다 짧다', '처음 요청부터 순서대로 처리하여 공정성을 보장한다', '특정 트랙 요청이 무기한 대기하는 기아(Starvation) 현상을 방지한다', '어떤 방향으로도 이동하지 않고 현재 위치에서 처리한다']}
  correctAnswer={3}
  explanation="SSTF는 가장 가까운 요청을 먼저 처리하므로 새로운 요청이 헤드 근처에서 계속 들어오면 멀리 있는 요청은 무기한 대기하는 기아(Starvation) 현상이 발생할 수 있습니다. SCAN은 한 방향으로 끝까지 이동하며 경로의 모든 요청을 처리하므로 어떤 요청도 최대 2번의 스윕 안에 처리됩니다."
  optionExplanations={['SCAN의 헤드 이동 거리가 항상 SSTF보다 짧지는 않습니다.', '순서대로 처리하는 것은 FCFS입니다.', 'SCAN은 기아 현상을 방지하여 SSTF보다 공정합니다. 정답입니다.', 'SCAN은 헤드가 이동하며 처리합니다.']}
/>

<Quiz
  questionNumber={14}
  question="DMA(Direct Memory Access) 방식에서 CPU의 역할은?"
  options={['CPU가 바이트 단위로 직접 데이터를 전송한다', 'CPU가 주기적으로 장치 상태를 확인하며 데이터를 전송한다', 'CPU가 DMA 컨트롤러에 전송 명령을 내리고 완료 인터럽트를 기다리며 다른 작업을 수행한다', 'CPU가 DMA 컨트롤러를 대신하여 메모리와 장치 사이의 데이터를 직접 이동한다']}
  correctAnswer={3}
  explanation="DMA 방식에서 CPU는 DMA 컨트롤러에 전송 소스, 목적지, 크기 등을 설정하고 전송을 시작합니다. 이후 CPU는 다른 작업을 수행하고, DMA 컨트롤러가 독립적으로 메모리와 I/O 장치 사이의 데이터를 전송합니다. 전송 완료 후 DMA 컨트롤러가 CPU에 인터럽트를 보내 완료를 알립니다."
  optionExplanations={['바이트 단위 직접 전송은 폴링 방식의 특성입니다.', '주기적 상태 확인은 폴링 방식입니다.', 'CPU가 명령 후 다른 작업을 하고 완료 인터럽트를 받는 것이 DMA입니다. 정답입니다.', 'DMA 컨트롤러가 CPU 대신 데이터를 전송하는 것이 DMA의 핵심입니다.']}
/>

<Quiz
  questionNumber={15}
  question="메모리 매핑 I/O(Memory-Mapped I/O)의 특징은?"
  options={['IN/OUT 전용 명령어를 사용하여 입출력 장치에 접근한다', 'I/O 장치의 레지스터를 메모리 주소 공간에 포함시켜 일반 메모리 명령어로 접근한다', 'DMA 방식에서만 사용 가능하다', 'I/O 장치와 메모리가 완전히 분리된 주소 공간을 사용한다']}
  correctAnswer={2}
  explanation="메모리 매핑 I/O는 입출력 장치의 제어 레지스터와 데이터 레지스터를 메모리 주소 공간의 일부로 할당합니다. CPU는 일반 LOAD/STORE 명령어로 I/O 장치에 접근할 수 있어 프로그래밍이 단순합니다. IN/OUT 전용 명령어를 사용하는 것은 독립 I/O(Isolated I/O) 방식입니다."
  optionExplanations={['IN/OUT 전용 명령어는 독립 I/O(Isolated I/O)의 특성입니다.', 'I/O 레지스터를 메모리 주소에 배치하는 것이 메모리 매핑 I/O입니다. 정답입니다.', '메모리 매핑 I/O는 DMA에만 국한되지 않습니다.', '주소 공간이 분리된 것은 독립 I/O 방식입니다.']}
/>

<Quiz
  questionNumber={16}
  question="플린 분류법(Flynn's Taxonomy)에서 SIMD에 해당하는 것은?"
  options={['단일 코어 CPU의 전통적인 순차 실행 방식', '여러 코어가 각자 다른 명령어와 데이터를 처리하는 멀티코어 CPU', '동일한 명령어로 여러 데이터를 동시에 처리하는 GPU나 벡터 프로세서', '여러 프로세서가 하나의 데이터 스트림에 서로 다른 명령어를 적용하는 방식']}
  correctAnswer={3}
  explanation="SIMD(Single Instruction, Multiple Data)는 하나의 명령어로 여러 데이터를 동시에 처리합니다. GPU가 대표적으로, 수천 개의 코어가 동일한 연산(예: 픽셀 색상 계산)을 각기 다른 데이터(각 픽셀)에 동시에 적용합니다. CPU의 SSE/AVX 명령어도 SIMD의 일종입니다."
  optionExplanations={['단일 코어 순차 실행은 SISD입니다.', '여러 코어가 다른 명령어와 데이터를 처리하는 것은 MIMD입니다.', 'GPU와 벡터 프로세서가 SIMD의 대표적 예입니다. 정답입니다.', '여러 명령어로 단일 데이터를 처리하는 것은 MISD입니다.']}
/>

<Quiz
  questionNumber={17}
  question="암달의 법칙(Amdahl's Law)에서 프로그램의 50%만 병렬화 가능할 때 프로세서를 무한히 늘리면 최대 스피드업은?"
  options={['2배', '4배', '무한대', '1.5배']}
  correctAnswer={1}
  explanation="S = 1 / ((1-P) + P/N)에서 P=0.5, N을 무한대로 보내면 S = 1 / ((1-0.5) + 0) = 1 / 0.5 = 2배입니다. 병렬화할 수 없는 순차 부분(50%)이 성능 향상의 상한을 1/(1-0.5) = 2배로 결정합니다. 아무리 프로세서를 추가해도 2배를 넘을 수 없습니다."
  optionExplanations={['S = 1/0.5 = 2배가 최대 스피드업입니다. 정답입니다.', '4배는 P=0.75일 때의 최대값입니다.', '순차 부분이 50%이므로 무한대가 아닙니다.', '1.5배는 프로세서가 제한적일 때의 값입니다.']}
/>

<Quiz
  questionNumber={18}
  question="RISC 프로세서의 특징으로 옳지 않은 것은?"
  options={['명령어 수가 적고 고정 길이이다', '대부분의 명령어가 1 클럭에 실행된다', '메모리 접근은 LOAD/STORE 명령어로만 수행한다', '복잡한 명령어를 하드웨어가 직접 실행하여 컴파일러 부담이 적다']}
  correctAnswer={4}
  explanation="RISC에서는 명령어가 단순하므로 복잡한 연산을 수행하려면 여러 단순 명령어로 분해해야 합니다. 이 작업을 컴파일러가 담당하므로 컴파일러의 최적화 부담이 CISC보다 큽니다. CISC는 복잡한 연산을 하나의 명령어로 처리하는 하드웨어를 가지므로 컴파일러 부담이 상대적으로 작습니다."
  optionExplanations={['RISC는 명령어 수가 적고 고정 길이입니다. 옳은 설명입니다.', '1 클럭 실행이 RISC의 특징입니다. 옳은 설명입니다.', 'RISC는 LOAD/STORE만 메모리 접근합니다. 옳은 설명입니다.', 'RISC에서는 컴파일러 최적화 부담이 크므로 이 설명은 틀렸습니다. 정답입니다.']}
/>

<Quiz
  questionNumber={19}
  question="멀티코어 프로세서에서 캐시 일관성(Cache Coherency) 문제가 발생하는 이유는?"
  options={['각 코어의 클럭 속도가 달라 타이밍 불일치가 발생하기 때문이다', '여러 코어가 같은 메모리 데이터를 각자의 캐시에 복사하고 한 코어가 수정하면 다른 코어의 캐시 내용이 달라지기 때문이다', '캐시 용량이 코어 수보다 적기 때문이다', '코어마다 다른 운영체제를 사용하기 때문이다']}
  correctAnswer={2}
  explanation="멀티코어에서 각 코어는 자신의 L1/L2 캐시를 가집니다. 여러 코어가 같은 메모리 데이터를 각자 캐시에 복사한 후, 코어 A가 자신의 캐시에서 그 데이터를 수정하면 코어 B의 캐시에는 아직 이전 값이 남아 불일치가 발생합니다. 이를 MESI 프로토콜로 해결합니다."
  optionExplanations={['클럭 속도 차이와 캐시 일관성은 직접적 관련이 없습니다.', '각 코어가 같은 데이터를 복사 후 한 코어가 수정하면 불일치가 발생합니다. 정답입니다.', '캐시 용량과 캐시 일관성은 다른 문제입니다.', '일반적으로 멀티코어는 하나의 OS를 공유합니다.']}
/>

<Quiz
  questionNumber={20}
  question="클라우드 컴퓨팅의 서비스 모델 중 IaaS(Infrastructure as a Service)의 설명으로 옳은 것은?"
  options={['완성된 소프트웨어를 인터넷으로 제공하며 사용자는 소프트웨어만 사용한다', '개발 플랫폼과 미들웨어를 제공하여 개발자가 코드 작성에만 집중할 수 있게 한다', '서버, 스토리지, 네트워크 등 인프라를 제공하고 사용자가 OS 이상을 직접 관리한다', '물리 서버를 구매하지 않고 데이터센터를 임대하는 방식이다']}
  correctAnswer={3}
  explanation="IaaS(Infrastructure as a Service)는 가상 서버, 스토리지, 네트워크 등 기본 인프라를 클라우드로 제공합니다. 사용자는 운영체제, 미들웨어, 애플리케이션을 직접 설치하고 관리합니다. 가장 큰 유연성을 제공하지만 관리 부담도 가장 큽니다. 예: AWS EC2, Google Compute Engine."
  optionExplanations={['완성된 소프트웨어 제공은 SaaS(Software as a Service)입니다.', '개발 플랫폼 제공은 PaaS(Platform as a Service)입니다.', '인프라 제공 후 사용자가 OS 이상을 관리하는 것이 IaaS입니다. 정답입니다.', '데이터센터 임대는 IaaS와 유사하나 가상화를 통한 온디맨드 제공이 IaaS의 핵심입니다.']}
/>

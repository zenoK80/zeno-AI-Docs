---
title: '15. 컴퓨터구조 종합 예상 기출문제'
description: '독학사 2단계 컴퓨터구조 전 범위 예상 기출문제 20문항 풀이'
---

<Callout type="info">
  **종합 복습:** 컴퓨터구조 전 범위의 핵심 개념을 기출 유형 문제로 점검합니다. 각 문제에 정확한 이유를 파악하여 유사 변형 문제에도 대응할 수 있도록 학습하세요.
</Callout>

## 컴퓨터구조 예상 기출문제 (20문항)

<Quiz
  questionNumber={1}
  question="폰 노이만 구조의 핵심 특징인 '저장 프로그램(Stored Program)' 개념이란?"
  options={['데이터만 메모리에 저장하고 프로그램은 별도 ROM에 저장', '프로그램과 데이터를 같은 메모리 공간에 함께 저장하여 CPU가 순차적으로 실행', '프로그램은 항상 보조기억장치에서 읽어 직접 실행', 'CPU가 프로그램 없이 하드와이어드로 동작']}
  correctAnswer={2}
  explanation="폰 노이만 구조의 핵심은 저장 프로그램(Stored Program) 방식으로, 프로그램(명령어)과 데이터를 같은 메모리에 저장하고 CPU가 메모리에서 명령어를 순서대로 꺼내 실행하는 방식입니다."
  optionExplanations={['폰 노이만 구조는 프로그램과 데이터를 같은 메모리에 저장합니다.', '저장 프로그램 방식으로 프로그램과 데이터를 같은 메모리에 저장합니다. (맞습니다.)', '보조기억장치에서 직접 실행하지 않고 메인 메모리에 로드하여 실행합니다.', '하드와이어드만으로는 유연한 프로그래밍이 불가합니다.']}
/>

<Quiz
  questionNumber={2}
  question="10진수 -7을 4비트 2의 보수로 표현하면?"
  options={['1000', '1001', '1110', '1111']}
  correctAnswer={2}
  explanation="+7 = 0111. 2의 보수: 1단계 1의 보수 = 1000, 2단계 +1 = 1001. 따라서 -7 = 1001(2의 보수)"
  optionExplanations={['1000은 -8의 2의 보수 표현입니다.', '-7의 2의 보수는 0111 → 1000 → 1001입니다. (맞습니다.)', '1110은 -2의 2의 보수 표현입니다.', '1111은 -1의 2의 보수 표현입니다.']}
/>

<Quiz
  questionNumber={3}
  question="IEEE 754 단정도(32비트) 부동소수점의 형식으로 올바른 것은?"
  options={['부호 1비트 + 지수 7비트 + 가수 24비트', '부호 1비트 + 지수 8비트 + 가수 23비트', '부호 2비트 + 지수 8비트 + 가수 22비트', '부호 1비트 + 지수 11비트 + 가수 20비트']}
  correctAnswer={2}
  explanation="IEEE 754 단정도(Single Precision, 32비트): 부호 1비트 + 지수 8비트(바이어스 127) + 가수 23비트. 총 32비트."
  optionExplanations={['지수는 8비트이며 가수는 23비트입니다.', '부호 1 + 지수 8(바이어스 127) + 가수 23 = 32비트가 단정도입니다. (맞습니다.)', '부호는 1비트입니다.', '지수 11비트는 배정도(64비트)의 형식입니다.']}
/>

<Quiz
  questionNumber={4}
  question="XOR 게이트에서 두 입력이 A=1, B=0일 때 출력은?"
  options={['0', '1', 'A의 값', 'B의 값']}
  correctAnswer={2}
  explanation="XOR(배타적 OR)은 두 입력이 서로 다를 때 1을 출력합니다. A=1, B=0은 서로 다르므로 출력은 1입니다."
  optionExplanations={['두 입력이 같을 때(00 또는 11) 출력이 0입니다.', 'A=1, B=0은 서로 다르므로 XOR 출력은 1입니다. (맞습니다.)', 'XOR 출력은 A 값과 항상 같지 않습니다.', 'XOR 출력은 B 값과 항상 같지 않습니다.']}
/>

<Quiz
  questionNumber={5}
  question="전가산기(Full Adder)의 입력 A=1, B=1, Cin=1일 때 Sum과 Cout은?"
  options={['Sum=0, Cout=0', 'Sum=1, Cout=1', 'Sum=0, Cout=1', 'Sum=1, Cout=0']}
  correctAnswer={2}
  explanation="Sum = A⊕B⊕Cin = 1⊕1⊕1 = 0⊕1 = 1. Cout = A·B + Cin·(A⊕B) = 1·1 + 1·(1⊕1) = 1 + 1·0 = 1. 따라서 Sum=1, Cout=1. 즉 1+1+1 = 11(2진수) = 3."
  optionExplanations={['Sum과 Cout이 모두 0인 경우가 아닙니다.', 'Sum=1, Cout=1입니다. (맞습니다.)', 'Sum=0은 틀립니다. Sum = 1⊕1⊕1 = 1입니다.', 'Cout=0은 틀립니다. Cout = 1·1 + 1·0 = 1입니다.']}
/>

<Quiz
  questionNumber={6}
  question="JK 플립플롭에서 J=0, K=1일 때 다음 상태는?"
  options={['0 (Reset)', '1 (Set)', '현재 상태 유지', '현재 상태 반전(Toggle)']}
  correctAnswer={1}
  explanation="JK 플립플롭 진리표: J=0, K=0이면 유지, J=0, K=1이면 0(Reset), J=1, K=0이면 1(Set), J=1, K=1이면 Toggle. J=0, K=1이면 Q(t+1) = 0입니다."
  optionExplanations={['J=0, K=1이면 Reset 상태로 Q=0이 됩니다. (맞습니다.)', '1은 J=1, K=0(Set)일 때의 결과입니다.', '유지는 J=0, K=0일 때의 결과입니다.', 'Toggle은 J=1, K=1일 때의 결과입니다.']}
/>

<Quiz
  questionNumber={7}
  question="PC(Program Counter)와 IR(Instruction Register)의 역할에 대한 설명으로 옳은 것은?"
  options={['PC는 현재 실행 중인 명령어를 저장하고, IR은 다음 명령어 주소를 저장한다', 'PC는 다음 실행할 명령어의 주소를 저장하고, IR은 현재 실행 중인 명령어를 저장한다', 'PC와 IR 모두 메모리 주소를 저장한다', 'IR은 ALU 연산 결과를 저장한다']}
  correctAnswer={2}
  explanation="PC(Program Counter)는 다음에 실행할 명령어가 있는 메모리 주소를 저장합니다. IR(Instruction Register)은 메모리에서 인출한 현재 실행 중인 명령어를 저장합니다."
  optionExplanations={['설명이 반대입니다.', 'PC=다음 명령어 주소, IR=현재 명령어 내용입니다. (맞습니다.)', 'IR은 주소가 아닌 명령어 내용(비트)을 저장합니다.', 'ALU 결과는 AC(누산기)에 저장됩니다.']}
/>

<Quiz
  questionNumber={8}
  question="직접 주소 지정과 간접 주소 지정의 메모리 접근 횟수를 비교한 것으로 옳은 것은?"
  options={['직접: 0회, 간접: 1회', '직접: 1회, 간접: 1회', '직접: 1회, 간접: 2회', '직접: 2회, 간접: 2회']}
  correctAnswer={3}
  explanation="직접 주소 지정은 오퍼랜드가 실제 주소이므로 그 주소에서 데이터를 읽는 1회 접근이 필요합니다. 간접 주소 지정은 오퍼랜드 주소에서 실제 주소를 읽은 후(1회), 실제 주소에서 데이터를 읽어야(2회) 합니다."
  optionExplanations={['직접이 0회는 아닙니다. 직접은 1회 접근합니다.', '간접은 2회 접근이 필요합니다.', '직접 1회, 간접 2회가 맞습니다. (맞습니다.)', '직접은 2회가 아닌 1회입니다.']}
/>

<Quiz
  questionNumber={9}
  question="파이프라인에서 분기 예측(Branch Prediction)이 필요한 이유는?"
  options={['데이터 해저드를 해결하기 위해', '구조적 해저드를 해결하기 위해', '분기 명령어로 인한 제어 해저드에서 파이프라인 낭비를 줄이기 위해', '파이프라인 단계 수를 늘리기 위해']}
  correctAnswer={3}
  explanation="분기 명령어의 실제 실행 경로가 결정되기 전에 파이프라인에 잘못된 명령어가 들어올 수 있는 제어 해저드를 줄이기 위해 분기 예측을 사용합니다. 예측이 맞으면 지연 없이 처리되고, 틀리면 취소 후 재시작합니다."
  optionExplanations={['데이터 해저드는 포워딩, 지연(stall)으로 해결합니다.', '구조적 해저드는 자원 분리(예: 하버드 구조)로 해결합니다.', '분기 명령어에 의한 제어 해저드를 줄이기 위해 분기 예측을 사용합니다. (맞습니다.)', '파이프라인 단계 수 증가는 분기 예측과 무관합니다.']}
/>

<Quiz
  questionNumber={10}
  question="SRAM이 캐시 메모리에 사용되는 주요 이유는?"
  options={['용량이 크기 때문이다', '가격이 저렴하기 때문이다', '리프레시가 필요 없으며 DRAM보다 빠르기 때문이다', '비휘발성이기 때문이다']}
  correctAnswer={3}
  explanation="SRAM은 플립플롭을 기반으로 하여 리프레시 없이도 데이터를 유지하며, DRAM보다 훨씬 빠릅니다. 단, 집적도가 낮고 비싸서 소용량의 캐시 메모리로 사용됩니다."
  optionExplanations={['SRAM은 집적도가 낮아 용량이 작습니다.', 'SRAM은 DRAM보다 비쌉니다.', 'SRAM은 리프레시 불필요하고 빨라서 캐시에 사용됩니다. (맞습니다.)', 'SRAM도 전원이 꺼지면 데이터가 지워지는 휘발성입니다.']}
/>

<Quiz
  questionNumber={11}
  question="캐시 히트율이 0.9이고 캐시 접근 시간 20ns, 메인 메모리 접근 시간 200ns일 때 EAT는?"
  options={['20ns', '38ns', '200ns', '180ns']}
  correctAnswer={2}
  explanation="EAT = h×Tc + (1-h)×Tm = 0.9×20 + 0.1×200 = 18 + 20 = 38ns"
  optionExplanations={['20ns는 캐시만 접근할 때의 시간입니다.', 'EAT = 0.9×20 + 0.1×200 = 18+20 = 38ns입니다. (맞습니다.)', '200ns는 메모리만 접근할 때의 시간입니다.', '180ns는 다른 계산 결과입니다.']}
/>

<Quiz
  questionNumber={12}
  question="직접 매핑(Direct Mapping) 캐시에서 캐시 슬롯이 128개일 때, 메모리 블록 번호 300번의 캐시 슬롯 번호는?"
  options={['44번', '56번', '64번', '172번']}
  correctAnswer={1}
  explanation="캐시 슬롯 번호 = 메모리 블록 번호 mod 캐시 슬롯 수 = 300 mod 128 = 44"
  optionExplanations={['300 mod 128 = 44입니다. (맞습니다.)', '56은 300 mod 128 = 44와 다릅니다.', '64는 128의 절반이지만 300 mod 128 = 44입니다.', '172는 300 mod 128의 결과가 아닙니다.']}
/>

<Quiz
  questionNumber={13}
  question="RAID 5에서 디스크 3개를 사용할 때 실제 사용 가능한 데이터 용량은 전체 용량의 몇 %인가?"
  options={['33%', '50%', '67%', '100%']}
  correctAnswer={3}
  explanation="RAID 5에서 n개 디스크의 실제 용량 효율은 (n-1)/n입니다. 3개 디스크이면 (3-1)/3 = 2/3 ≈ 67%입니다."
  optionExplanations={['33%는 1/3로 3개 중 1개만 사용하는 경우입니다.', '50%는 RAID 1의 효율입니다.', 'RAID 5 3디스크 용량 효율 = (3-1)/3 = 2/3 ≈ 67%입니다. (맞습니다.)', '100%는 패리티 없는 RAID 0의 효율입니다.']}
/>

<Quiz
  questionNumber={14}
  question="DMA 방식과 인터럽트 방식의 차이에 대한 설명으로 옳은 것은?"
  options={['인터럽트 방식은 데이터 전송마다 CPU가 개입하지 않는다', 'DMA는 소량의 데이터 전송에 적합하다', 'DMA는 전송 시작과 완료 시에만 CPU가 개입하여 대용량 전송에 효율적이다', '인터럽트 방식은 DMA보다 CPU 효율이 높다']}
  correctAnswer={3}
  explanation="DMA는 DMA 컨트롤러가 전송을 담당하므로 CPU는 전송 시작 명령과 완료 인터럽트 처리 시에만 개입합니다. 따라서 대용량 데이터 블록 전송 시 CPU 부하가 매우 낮습니다."
  optionExplanations={['인터럽트 방식은 각 데이터 전송마다 CPU가 개입합니다.', 'DMA는 대용량 블록 전송에 적합합니다.', 'DMA는 시작/완료만 CPU가 개입하여 대용량 전송에 효율적입니다. (맞습니다.)', 'DMA가 CPU 효율이 더 높습니다.']}
/>

<Quiz
  questionNumber={15}
  question="Flynn 분류에서 SIMD의 대표적인 예로 가장 적합한 것은?"
  options={['단일 코어 순차 처리 CPU', '멀티코어 CPU에서의 독립적 스레드 실행', 'GPU의 동일한 연산을 수천 개의 픽셀 데이터에 동시 적용', '운영체제의 프로세스 스케줄링']}
  correctAnswer={3}
  explanation="SIMD(Single Instruction, Multiple Data)는 하나의 명령이 여러 데이터에 동시 적용됩니다. GPU가 이미지 처리나 딥러닝에서 수천 개의 데이터에 같은 연산을 동시에 수행하는 것이 대표적 예입니다."
  optionExplanations={['단일 코어 순차 처리는 SISD입니다.', '독립적 스레드 실행은 MIMD입니다.', 'GPU가 수천 데이터에 같은 연산을 동시 적용하는 것이 SIMD입니다. (맞습니다.)', '프로세스 스케줄링은 Flynn 분류와 무관합니다.']}
/>

<Quiz
  questionNumber={16}
  question="RISC 아키텍처의 Load-Store 방식이란?"
  options={['메모리 LOAD 명령어로 부팅하는 방식', 'LOAD와 STORE 명령어만 메모리에 접근하고, 나머지 연산은 레지스터에서 수행하는 방식', '메모리를 가득 채울 때까지 데이터를 적재하는 방식', '스택에서 LOAD와 STORE로 모든 연산을 수행하는 방식']}
  correctAnswer={2}
  explanation="RISC의 Load-Store 아키텍처: 메모리 접근은 LOAD(메모리→레지스터)와 STORE(레지스터→메모리) 명령어만 가능하고, ADD, SUB 등의 연산은 레지스터 간에만 이루어집니다. 이를 통해 명령어 구조를 단순화합니다."
  optionExplanations={['부팅 방식과 무관합니다.', 'LOAD/STORE만 메모리 접근하고 나머지는 레지스터 연산이 Load-Store 방식입니다. (맞습니다.)', '메모리 적재 전략과 다릅니다.', '스택 기반은 0-주소 명령어 방식입니다.']}
/>

<Quiz
  questionNumber={17}
  question="캐시 매핑 방식 중 세트 연관 매핑(Set-Associative Mapping)의 장점은?"
  options={['직접 매핑보다 구현이 더 단순하다', '완전 연관 매핑보다 항상 히트율이 높다', '직접 매핑보다 히트율이 높고 완전 연관 매핑보다 구현이 단순하다', '어떤 메모리 블록도 캐시의 어느 슬롯에나 저장 가능하다']}
  correctAnswer={3}
  explanation="집합 연관 매핑은 캐시를 여러 집합으로 나누고 집합 내에서는 완전 연관을 적용합니다. 직접 매핑보다 충돌 미스가 줄어 히트율이 높고, 완전 연관보다 검색 범위가 제한되어 구현이 단순합니다."
  optionExplanations={['세트 연관 매핑은 직접 매핑보다 복잡합니다.', '완전 연관 매핑보다 히트율이 낮을 수 있습니다.', '직접보다 히트율 높고, 완전보다 구현 단순한 절충안입니다. (맞습니다.)', '어느 슬롯에나 저장 가능한 것은 완전 연관 매핑입니다.']}
/>

<Quiz
  questionNumber={18}
  question="SSTF(Shortest Seek Time First)와 SCAN 디스크 스케줄링의 비교로 옳은 것은?"
  options={['SCAN이 항상 헤드 이동 거리가 더 짧다', 'SSTF는 기아 현상이 없고 SCAN은 기아 현상이 있다', 'SSTF는 가까운 요청을 우선하여 기아 현상이 발생할 수 있고, SCAN은 방향 이동으로 기아를 방지한다', 'SCAN과 SSTF는 동일한 결과를 생성한다']}
  correctAnswer={3}
  explanation="SSTF는 현재 헤드 위치에서 가장 가까운 요청만 처리하므로 헤드에서 멀리 있는 요청이 기아 상태에 빠질 수 있습니다. SCAN은 헤드가 한쪽 끝에서 다른 끝으로 이동하면서 모든 요청을 처리하므로 기아가 발생하지 않습니다."
  optionExplanations={['항상 SCAN이 더 짧지 않습니다.', 'SSTF가 기아 가능, SCAN은 기아 방지입니다. 반대로 설명한 것이 틀립니다.', 'SSTF=기아 가능, SCAN=기아 방지가 맞습니다. (맞습니다.)', '두 알고리즘은 다른 결과를 생성합니다.']}
/>

<Quiz
  questionNumber={19}
  question="암달의 법칙(Amdahl's Law)에 의하면 프로그램의 50%가 병렬화 가능하고 프로세서 수가 무한히 늘어날 때 최대 스피드업은?"
  options={['1배', '2배', '4배', '무한대']}
  correctAnswer={2}
  explanation="N→∞일 때 S = 1/((1-P) + P/N) → 1/(1-P). P=0.5이면 최대 스피드업 = 1/(1-0.5) = 1/0.5 = 2배. 순차 부분(50%)이 상한을 결정합니다."
  optionExplanations={['1배는 병렬화 효과가 없는 경우입니다.', '최대 스피드업 = 1/(1-0.5) = 2배입니다. (맞습니다.)', '4배는 P=0.75일 때의 상한입니다.', '무한대는 100% 병렬화 가능할 때만 이론적으로 가능합니다.']}
/>

<Quiz
  questionNumber={20}
  question="다음 중 컴퓨터구조에서 '하버드 구조(Harvard Architecture)'의 특징은?"
  options={['프로그램과 데이터를 같은 메모리에 저장한다', '명령어 메모리와 데이터 메모리를 물리적으로 분리하여 구조적 해저드를 방지한다', '모든 명령어를 순차적으로 처리한다', '마이크로프로그램 방식으로 명령어를 처리한다']}
  correctAnswer={2}
  explanation="하버드 구조는 명령어용 메모리와 데이터용 메모리를 물리적으로 분리합니다. 이를 통해 파이프라인에서 명령어 인출(Instruction Fetch)과 데이터 접근(Data Memory Access)이 동시에 다른 메모리를 사용하여 구조적 해저드를 방지합니다. 폰 노이만 구조의 반대 개념입니다."
  optionExplanations={['프로그램과 데이터를 같은 메모리에 저장하는 것은 폰 노이만 구조입니다.', '명령어와 데이터 메모리 분리가 하버드 구조의 핵심입니다. (맞습니다.)', '순차 처리는 폰 노이만 구조의 특성입니다.', '마이크로프로그램 방식은 CISC 제어장치의 특성입니다.']}
/>

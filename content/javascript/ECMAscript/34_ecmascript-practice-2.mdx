---
title: '34. ECMAScript 핵심 퀴즈 2'
description: '프로토타입, 클래스부터 비동기, 이벤트 루프, 모듈까지 문서 15~32의 핵심 개념을 종합 점검합니다.'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 앞서 배운 개념을 종합적으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="모든 배열이 .map()을 사용할 수 있는 이유는?"
  options={['①모든 배열마다 map이 개별 복사되어 있어서', '②Array.prototype에 map이 정의되어 있고 프로토타입 체인을 통해 찾아 쓰기 때문', '③map은 예약어라서', '④엔진이 매번 즉석에서 만들어줘서']}
  correctAnswer={2}
  explanation="map은 Array.prototype에 한 번만 정의되어 있고 모든 배열이 프로토타입 체인을 통해 공유합니다."
  optionExplanations={['개별 복사가 아닌 공유 방식입니다.', '맞습니다. 프로토타입 체인을 통한 공유입니다.', '예약어가 아닙니다.', '즉석 생성이 아닌 공유 함수입니다.']}
/>

<Quiz
  questionNumber={2}
  question="자식 클래스의 constructor에서 super()를 먼저 호출하지 않고 this를 사용하면?"
  options={['①정상 동작한다.', '②ReferenceError가 발생한다.', '③this가 undefined가 된다.', '④부모 constructor가 자동으로 건너뛰어진다.']}
  correctAnswer={2}
  explanation="부모의 초기화가 끝나기 전에는 this를 사용할 수 없도록 강제됩니다."
  optionExplanations={['에러가 발생합니다.', '맞습니다. super() 호출 전 this 접근은 에러입니다.', '접근 자체가 에러로 처리됩니다.', '자동으로 건너뛰어지지 않습니다.']}
/>

<Quiz
  questionNumber={3}
  question="`#balance` 같은 private 필드에 클래스 외부에서 접근하려 하면?"
  options={['①undefined가 반환된다.', '②SyntaxError가 발생한다.', '③정상적으로 값을 읽을 수 있다.', '④자동으로 public이 된다.']}
  correctAnswer={2}
  explanation="# 필드는 클래스 몸체 밖에서는 문법적으로 언급조차 할 수 없어 문법 오류가 발생합니다."
  optionExplanations={['undefined가 아닌 문법 오류입니다.', '맞습니다. 문법 자체가 외부 접근을 차단합니다.', '읽을 수 없습니다.', '자동 변환되지 않습니다.']}
/>

<Quiz
  questionNumber={4}
  question="`[1,2,3].reduce((acc,n)=>acc+n, 10)`의 결과는?"
  options={['①6', '②10', '③16', '④NaN']}
  correctAnswer={3}
  explanation="초기값 10에서 시작해 1,2,3을 순서대로 더해 16이 됩니다."
  optionExplanations={['초기값 10을 빠뜨린 계산입니다.', '배열 합산이 빠졌습니다.', '맞습니다. 10+1+2+3=16입니다.', '정상적으로 숫자가 계산됩니다.']}
/>

<Quiz
  questionNumber={5}
  question="`stock || '없음'`에서 stock이 0일 때 문제가 되는 이유는?"
  options={['①0은 문자열이라서', '②||가 0을 거짓으로 취급해 의도치 않게 \'없음\'이 출력되기 때문', '③0은 undefined와 같아서', '④문법 오류가 나서']}
  correctAnswer={2}
  explanation="||는 0, '', false를 모두 거짓으로 취급해 유효한 0이 잘못 처리됩니다."
  optionExplanations={['0은 숫자입니다.', '맞습니다. 0이 거짓으로 취급되는 것이 함정입니다.', '0과 undefined는 다른 값입니다.', '정상 동작합니다.']}
/>

<Quiz
  questionNumber={6}
  question="`0 ?? '없음'`의 결과는?"
  options={["①'없음'", "②0", "③undefined", "④오류 발생"]}
  correctAnswer={2}
  explanation="??는 null/undefined일 때만 대체값을 사용합니다. 0은 유효한 값으로 유지됩니다."
  optionExplanations={["0은 대체되지 않습니다.", "맞습니다. 0은 유효한 값으로 유지됩니다.", "undefined가 아닙니다.", "정상 실행됩니다."]}
/>

<Quiz
  questionNumber={7}
  question="Promise가 한 번 fulfilled 상태가 되면?"
  options={['①다시 pending으로 돌아갈 수 있다.', '②상태가 영구적으로 고정되어 다시 바뀌지 않는다.', '③rejected로 바뀔 수 있다.', '④자동으로 삭제된다.']}
  correctAnswer={2}
  explanation="Promise는 한 번 확정되면 그 상태가 영구적으로 유지됩니다."
  optionExplanations={['확정된 상태는 되돌아가지 않습니다.', '맞습니다. 상태는 한 번 확정되면 불변입니다.', '바뀌지 않습니다.', '삭제되지 않습니다.']}
/>

<Quiz
  questionNumber={8}
  question="Promise.all([p1,p2,p3])에서 p2만 reject되면?"
  options={['①p1, p3의 결과만 반환된다.', '②전체 Promise.all이 즉시 reject된다.', '③p2만 제외하고 계속 진행된다.', '④무한 대기한다.']}
  correctAnswer={2}
  explanation="Promise.all은 하나라도 실패하면 즉시 전체가 reject됩니다."
  optionExplanations={['부분 결과만 반환하지 않습니다.', '맞습니다. 하나의 실패가 전체를 실패로 만듭니다.', '제외하고 진행되지 않습니다.', '즉시 reject됩니다.']}
/>

<Quiz
  questionNumber={9}
  question="`const user = fetchUser(id)`처럼 await를 빠뜨리면 user에 담기는 것은?"
  options={['①실제 사용자 데이터', '②완료되지 않은 Promise 객체', '③undefined', '④오류 발생']}
  correctAnswer={2}
  explanation="await 없이 호출하면 결과값이 아닌 Promise 객체 자체가 담깁니다."
  optionExplanations={['실제 데이터가 아닙니다.', '맞습니다. Promise 자체가 담깁니다.', '값이 존재합니다.', '문법 오류는 아닙니다.']}
/>

<Quiz
  questionNumber={10}
  question="`console.log('A'); setTimeout(()=>console.log('B'),0); Promise.resolve().then(()=>console.log('C')); console.log('D')`의 실행 순서는?"
  options={['①A, B, C, D', '②A, D, C, B', '③A, D, B, C', '④A, C, D, B']}
  correctAnswer={2}
  explanation="동기 코드(A,D) → 마이크로태스크(C) → 태스크(B) 순서로 실행됩니다."
  optionExplanations={['setTimeout이 동기 코드보다 먼저 실행되지 않습니다.', '맞습니다. 동기 → 마이크로태스크 → 태스크 순서입니다.', 'C가 B보다 먼저입니다.', 'D보다 C가 먼저 출력되지 않습니다.']}
/>

<Quiz
  questionNumber={11}
  question="마이크로태스크 큐와 태스크 큐 중 먼저 처리되는 것은?"
  options={['①태스크 큐가 항상 먼저', '②마이크로태스크 큐가 항상 먼저, 완전히 비워질 때까지', '③둘은 동시에 처리된다.', '④무작위 순서']}
  correctAnswer={2}
  explanation="콜 스택이 빌 때마다 마이크로태스크 큐를 완전히 비운 뒤 태스크 큐를 처리합니다."
  optionExplanations={['마이크로태스크가 먼저입니다.', '맞습니다. 마이크로태스크 우선 원칙입니다.', '순서가 정해져 있습니다.', '정해진 순서를 따릅니다.']}
/>

<Quiz
  questionNumber={12}
  question="named export를 가져올 때 필요한 문법은?"
  options={['①중괄호 없이 자유로운 이름', '②중괄호 { } 안에 export한 것과 정확히 같은 이름', '③대괄호 [ ]', '④따옴표로 감싼 이름']}
  correctAnswer={2}
  explanation="named export는 export한 이름 그대로 중괄호 안에 명시해서 가져와야 합니다."
  optionExplanations={['자유로운 이름은 default export의 특징입니다.', '맞습니다. 정확한 이름과 중괄호가 필요합니다.', '중괄호를 사용합니다.', '따옴표로 감싸지 않습니다.']}
/>

<Quiz
  questionNumber={13}
  question="같은 모듈을 여러 파일에서 import하면?"
  options={['①매번 새로 실행되어 독립적인 상태를 갖는다.', '②처음 한 번만 실행되고 캐시된 결과를 공유한다.', '③import할 때마다 오류가 발생한다.', '④마지막 import만 유효하다.']}
  correctAnswer={2}
  explanation="모듈은 처음 로딩될 때 한 번만 실행되고, 이후에는 캐시된 동일한 인스턴스를 공유합니다."
  optionExplanations={['매번 새로 실행되지 않습니다.', '맞습니다. 캐시되어 공유됩니다.', '정상적으로 재사용됩니다.', '모두 같은 인스턴스를 참조합니다.']}
/>

<Quiz
  questionNumber={14}
  question="`Symbol('id') === Symbol('id')`의 결과는?"
  options={['①true', '②false', "③문자열 'id'", '④오류 발생']}
  correctAnswer={2}
  explanation="같은 설명 문자열이어도 Symbol()을 호출할 때마다 완전히 새로운 고유값이 만들어집니다."
  optionExplanations={['같은 설명이어도 다른 값입니다.', '맞습니다. 매번 고유한 값이 생성됩니다.', '비교 결과는 boolean입니다.', '정상적으로 비교되어 false를 반환합니다.']}
/>

<Quiz
  questionNumber={15}
  question="Proxy의 handler에 정의하는 get, set 같은 메서드를 무엇이라고 부르나요?"
  options={['①이벤트', '②트랩(trap)', '③콜백', '④제너레이터']}
  correctAnswer={2}
  explanation="Proxy handler의 메서드들은 해당 동작을 가로채 실행되기 때문에 트랩이라고 부릅니다."
  optionExplanations={['이벤트 시스템과는 다른 개념입니다.', '맞습니다. 트랩이라는 용어를 사용합니다.', '일반적인 콜백과는 역할이 다릅니다.', '제너레이터와는 무관합니다.']}
/>

## 참고 자료

- [ECMAScript Language Specification](https://tc39.es/ecma262/)

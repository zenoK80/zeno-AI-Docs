---
title: '04-복습. 표현식과 문'
description: '표현식과 문을 구별하는 감각을 4지선다 퀴즈로 다지고, ASI 함정을 직접 재현해 본다.'
---

## 04편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"표현식인지 아닌지 가장 쉽게 확인하는 방법은?"}
  options={[
    "세미콜론(`;`)이 붙어 있는지 본다",
    "`console.log(그것)`으로 감쌌을 때 에러 없이 값이 찍히는지 본다",
    "글자 수가 짧은지 본다",
    "중괄호(`{ }`)가 있는지 본다",
  ]}
  correctAnswer={2}
  explanation={"표현식은 \"평가되어 하나의 값이 되는 코드 조각\"이다. `console.log()` 괄호 안에 넣었을 때 문제없이 값이 출력되면 표현식이고, 문법 오류가 나면 표현식이 아니라 문이다."}
  optionExplanations={[
    "세미콜론은 문에도, 표현식이 포함된 줄에도 똑같이 붙을 수 있어서 구분 기준이 되지 못한다.",
    "정답이다. `console.log()`로 감싸서 값이 정상 출력되는지 확인하는 게 가장 실용적인 판별법이다.",
    "글자 수와 표현식 여부는 아무 관계가 없다.",
    "중괄호는 블록문이나 객체 리터럴 모두에 쓰이므로, 있고 없음만으로는 표현식 여부를 가릴 수 없다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"다음 중 문(Statement)에 해당하는 것은?"}
  options={[
    "`1 + 1`",
    "`age > 18`",
    "`let x = 1`",
    "`'hello'`",
  ]}
  correctAnswer={3}
  explanation={"`let x = 1`은 \"변수를 선언하고 값을 넣어라\"는 실행 지시이지, 그 자체가 어떤 값으로 평가되지 않는다. 반면 나머지는 각각 값으로 평가되는 표현식이다."}
  optionExplanations={[
    "`1 + 1`은 `2`라는 값으로 평가되는 표현식이다.",
    "`age > 18`은 `true`나 `false`로 평가되는 표현식이다.",
    "정답이다. `let x = 1`은 값으로 평가되지 않는 실행 지시, 즉 문이다.",
    "`'hello'`는 문자열 그 자체로 평가되는 표현식이다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"`if`문 대신 삼항 연산자(`조건 ? 참일때 : 거짓일때`)를 값이 필요한 자리에 쓸 수 있는 이유는?"}
  options={[
    "삼항 연산자가 타이핑하기 더 짧아서",
    "삼항 연산자는 표현식이라 값으로 평가되지만, `if`문은 문이라 값이 되지 않기 때문에",
    "`if`문은 옛날 문법이라 곧 사라질 예정이라서",
    "둘은 사실 완전히 똑같이 동작해서 아무 차이가 없다",
  ]}
  correctAnswer={2}
  explanation={"값이 필요한 자리에는 표현식만 올 수 있다. `if`문은 그 자체가 값이 되지 않아서 그런 자리에 쓸 수 없다. 반면 삼항 연산자는 조건을 평가해서 최종적으로 값 하나를 만들어내는 표현식이다."}
  optionExplanations={[
    "타이핑 길이는 이 문법 선택의 근본 이유가 아니다. 핵심은 값으로 평가되는지 여부다.",
    "정답이다. 삼항 연산자는 표현식이라 값이 되지만, `if`문은 문이라 값이 되지 않는다는 차이가 핵심이다.",
    "`if`문은 지금도 널리 쓰이는 표준 제어문이며 사라질 계획이 없다.",
    "둘은 값으로 평가되는지 여부가 다르기 때문에, 값이 필요한 자리에서는 완전히 다르게 동작한다.",
  ]}
/>

## 실습 — ASI 함정 직접 재현하기

**ASI**<sup>Automatic Semicolon Insertion, 자동 세미콜론 삽입</sup>는 세미콜론을 생략해도 자바스크립트가 알아서 채워 넣어 주는 기능이다. 편리하지만 가끔 의도와 다르게 채워 넣는다. 아래 함수를 콘솔이나 파일에 그대로 쳐보고, 결과를 예상해 본 뒤 실행해서 확인하자.

```js
function getValue() {
  return
  {
    value: 1
  }
}

console.log(getValue())
```

<Collapse title="결과와 이유">
`undefined`가 나온다. `return` 다음에 줄바꿈이 있으면, ASI가 그 줄바꿈 자리에 세미콜론을 끼워 넣어 `return;`으로 만들어버린다. 그러면 그 아래 `{ value: 1 }`은 실행되지 않는 죽은 코드가 된다.
</Collapse>

이제 같은 함수를 아래처럼 고쳐서 다시 실행해 보자.

```js
function getValueFixed() {
  return {
    value: 1
  }
}

console.log(getValueFixed())
```

무엇이 달라졌는지, 왜 이번엔 제대로 나오는지 스스로 설명해 보자.

## 정리 체크리스트

<Callout type="default">
- [ ] 표현식과 문을 예시 코드로 구별할 수 있다
- [ ] "값이 필요한 자리에는 표현식만 온다"는 원칙을 안다
- [ ] `return` 뒤 줄바꿈이 왜 위험한지 설명할 수 있다
</Callout>

다음 편에서는 이 표현식들이 만들어내는 값, 즉 데이터 타입을 본다.

---
title: '03-복습. 변수와 상수'
description: 'let과 const의 차이, var를 쓰지 않는 이유를 4지선다 퀴즈와 코드 실습으로 점검한다.'
---

## 03편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"새 변수를 선언할 때, 특별한 이유가 없다면 기본적으로 무엇을 먼저 고려해야 하나?"}
  options={[
    "`var`",
    "`let`",
    "`const`",
    "셋 다 아무거나 똑같다",
  ]}
  correctAnswer={3}
  explanation={"기본은 `const`, 값이 바뀌어야 할 필요가 생겼을 때만 `let`으로 바꾼다. `const`로 시작하면 코드를 읽는 사람이 \"이 값은 절대 안 바뀐다\"고 안심할 수 있고, 실수로 값을 덮어써서 생기는 버그도 막을 수 있다."}
  optionExplanations={[
    "`var`는 블록 스코프를 무시하는 등 여러 문제가 있어 지금은 아예 쓰지 않는 게 원칙이다.",
    "`let`은 값이 바뀌어야 할 때만 선택하는 차선책이다. 기본으로 먼저 고려할 대상은 아니다.",
    "정답이다. 재할당이 필요 없다면 기본적으로 `const`를 먼저 고려하는 게 원칙이다.",
    "셋의 동작 방식(재할당 가능 여부, 스코프)이 서로 다르므로 아무거나 똑같이 써도 되는 게 아니다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"`const user = { name: '지민' }`로 선언한 뒤 `user.name = '민준'`을 실행하면 어떻게 되나?"}
  options={[
    "에러가 난다. `const`로 선언한 건 절대 못 바꾸기 때문이다",
    "에러 없이 정상 동작한다. `user.name`이 `'민준'`으로 바뀐다",
    "에러가 나지만 코드는 계속 실행된다",
    "`user`가 자동으로 `let`으로 바뀐다",
  ]}
  correctAnswer={2}
  explanation={"`const`가 막는 건 \"상자 자체를 다른 상자로 바꾸는 것\"(재할당)이지, \"상자 안 내용물을 바꾸는 것\"(객체 내부 값 수정)이 아니다. 그래서 `const`는 \"절대 못 바꾼다\"가 아니라 \"재할당만 못 한다\"가 정확한 설명이다."}
  optionExplanations={[
    "`const`가 막는 건 재할당(`user = {...}`)이지, 객체 내부 프로퍼티를 바꾸는 것(`user.name = ...`)이 아니다.",
    "정답이다. 객체 내부 프로퍼티 수정은 재할당이 아니라서 `const`로도 문제없이 동작한다.",
    "이 코드는 애초에 에러가 나지 않는다. \"에러가 나지만 계속 실행된다\"는 전제부터 틀렸다.",
    "`const`로 선언한 변수의 종류가 실행 중에 `let`으로 자동 변경되는 동작은 존재하지 않는다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"`var`가 가진 문제로 옳은 것을 모두 포함한 것은?"}
  options={[
    "같은 이름으로 여러 번 선언해도 에러가 안 난다",
    "블록(`{ }`) 안에서 선언해도 밖에서 접근된다",
    "A와 B 둘 다",
    "둘 다 아니다 — `var`는 사실 아무 문제가 없다",
  ]}
  correctAnswer={3}
  explanation={"`var`는 두 가지 문제가 있다. ① 같은 이름으로 다시 선언해도 아무 경고 없이 조용히 덮어써진다. ② 블록이라는 개념을 무시하고, 블록 안에서 선언해도 블록 밖에서 그 값이 그대로 보인다."}
  optionExplanations={[
    "이 설명 자체는 맞지만, `var`의 문제는 이것 하나가 아니라 블록 스코프 무시 문제까지 함께 있다.",
    "이 설명 자체는 맞지만, `var`의 문제는 이것 하나가 아니라 중복 선언 허용 문제까지 함께 있다.",
    "정답이다. `var`는 중복 선언을 조용히 허용하는 문제와 블록 스코프를 무시하는 문제를 둘 다 갖고 있다.",
    "`var`는 실제로 두 가지 문제를 갖고 있어서, 그 때문에 `let`/`const`가 도입됐다.",
  ]}
/>

## 실습 — 직접 에러를 내보기

콘솔에서 아래를 순서대로 실행하며 결과를 먼저 예상해 보고, 실제 결과와 비교한다.

```js
const MAX = 10
MAX = 20   // ① 예상 결과는?
```

```js
let score
console.log(score)   // ② 예상 결과는?
score = 100
```

```js
if (true) {
  var leaked = 'var는 샌다'
}
console.log(leaked)   // ③ 예상 결과는?

if (true) {
  let safe = 'let은 안 샌다'
}
console.log(safe)   // ④ 예상 결과는?
```

<Collapse title="정답 확인">
① `TypeError: Assignment to constant variable.` (상수에 재할당하려 했다는 에러)
② `undefined` (선언만 하고 값을 안 넣은 상태라 "아직 값 없음"이 자동으로 채워진다)
③ `'var는 샌다'` — 블록 밖인데도 접근된다
④ `ReferenceError: safe is not defined` — 블록 밖에서는 그런 이름이 없다는 에러
</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] "기본은 const, 필요할 때만 let" 원칙을 설명할 수 있다
- [ ] `var`를 쓰지 않는 이유 두 가지를 안다
- [ ] `const`가 막는 것과 안 막는 것을 구별할 수 있다
</Callout>

다음은 표현식과 문의 차이다.

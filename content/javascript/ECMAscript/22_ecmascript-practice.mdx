---
title: '22. ECMAScript 핵심 퀴즈'
description: 'ECMAScript 전체 내용을 20문제로 복습합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** ECMAScript 핵심 개념을 20문제로 정리하고 실력을 점검합니다.
</Callout>

<Quiz
  questionNumber={1}
  question="JavaScript와 ECMAScript의 관계로 가장 정확한 설명은?"
  options={[
    '서로 다른 두 언어이다.',
    'ECMAScript는 JavaScript의 문법과 동작을 정의한 표준이다.',
    'JavaScript는 Node.js에서만 실행된다.',
    'ECMAScript는 HTML의 또 다른 이름이다.',
  ]}
  correctAnswer={2}
  explanation="JavaScript는 언어 이름이고, ECMAScript는 그 언어의 표준 명세입니다. 브라우저와 Node.js는 모두 ECMAScript를 구현합니다."
  optionExplanations={[
    '별개의 언어가 아닙니다.',
    '맞습니다. ECMA International이 ECMA-262로 정의합니다.',
    'JavaScript는 브라우저와 Node.js 등 다양한 환경에서 실행됩니다.',
    'ECMAScript와 HTML은 관계가 없습니다.',
  ]}
/>

<Quiz
  questionNumber={2}
  question="다음 중 표현식(expression)이 아닌 것은?"
  options={['2 + 3', 'fn()', "if (x > 0) {}", 'x > 0 ? 1 : 0']}
  correctAnswer={3}
  explanation="if 문은 값을 만들지 않는 문(statement)입니다. 2 + 3, fn(), 삼항 연산자는 모두 값을 만드는 표현식입니다."
  optionExplanations={[
    '수식은 표현식입니다.',
    '함수 호출도 표현식입니다.',
    '맞습니다. if는 문이며 값을 반환하지 않습니다.',
    '삼항 연산자는 표현식입니다.',
  ]}
/>

<Quiz
  questionNumber={3}
  question="`typeof null`이 반환하는 값은?"
  options={["'null'", "'undefined'", "'object'", "'boolean'"]}
  correctAnswer={3}
  explanation="typeof null이 'object'를 반환하는 것은 JavaScript의 역사적 버그입니다. null 체크에는 === null을 사용합니다."
  optionExplanations={[
    "'null'이 아닌 'object'를 반환합니다.",
    "'undefined'는 undefined 타입에 해당합니다.",
    "맞습니다. 역사적 버그로 'object'가 반환됩니다.",
    "boolean은 true/false의 타입입니다.",
  ]}
/>

<Quiz
  questionNumber={4}
  question="`0 ?? 'default'`의 결과는?"
  options={["'default'", '0', 'null', 'undefined']}
  correctAnswer={2}
  explanation="?? 연산자는 좌측이 null이나 undefined일 때만 우측을 반환합니다. 0은 null/undefined가 아니므로 0이 반환됩니다."
  optionExplanations={[
    "|| 연산자라면 'default'가 되지만 ??는 다릅니다.",
    "맞습니다. 0은 null/undefined가 아니므로 그대로 반환됩니다.",
    "null은 ??가 우측으로 넘기는 값입니다.",
    "undefined도 ??가 우측으로 넘기는 값입니다.",
  ]}
/>

<Quiz
  questionNumber={5}
  question="다음 코드의 출력은? `for (let i = 0; i < 5; i++) { if (i === 2) continue; if (i === 4) break; console.log(i); }`"
  options={['0 1 3', '0 1 2 3 4', '0 1', '0 1 3 4']}
  correctAnswer={1}
  explanation="i가 2일 때 continue로 건너뛰고, i가 4일 때 break로 종료됩니다. 출력되는 값은 0, 1, 3입니다."
  optionExplanations={[
    '맞습니다. 2는 건너뛰고 4에서 종료됩니다.',
    'continue와 break로 일부가 제외됩니다.',
    '3도 출력됩니다.',
    '4에서 break되므로 4는 출력되지 않습니다.',
  ]}
/>

<Quiz
  questionNumber={6}
  question="원시값(primitive)과 객체(object)의 가장 큰 차이는?"
  options={[
    '원시값은 메서드를 가질 수 있다.',
    '원시값은 불변이며 값으로 복사되고, 객체는 가변이며 참조로 전달된다.',
    '객체는 변경이 불가능하다.',
    '원시값과 객체는 메모리 관리 방식이 동일하다.',
  ]}
  correctAnswer={2}
  explanation="원시값은 immutable이고 복사 시 값 자체가 복사됩니다. 객체는 mutable이고 복사 시 참조(메모리 주소)가 복사됩니다."
  optionExplanations={[
    '원시값 자체는 메서드가 없습니다(래퍼 객체 제외).',
    '맞습니다. 복사 방식과 가변성이 핵심 차이입니다.',
    '객체는 가변입니다.',
    '메모리 관리 방식이 다릅니다.',
  ]}
/>

<Quiz
  questionNumber={7}
  question="함수 선언문과 함수 표현식에서 호이스팅의 차이는?"
  options={[
    '둘 다 호이스팅되어 선언 전에 호출 가능하다.',
    '둘 다 호이스팅되지 않는다.',
    '함수 선언문은 완전히 호이스팅되어 선언 전 호출 가능하지만, 함수 표현식은 선언 전 호출 시 오류가 발생한다.',
    '함수 표현식이 더 먼저 호이스팅된다.',
  ]}
  correctAnswer={3}
  explanation="함수 선언문은 전체가 호이스팅되어 어디서나 호출 가능합니다. 함수 표현식은 변수 선언만 호이스팅되고 값(함수)은 할당 전이라 호출 시 오류입니다."
  optionExplanations={[
    '함수 표현식은 선언 전 호출이 불가합니다.',
    '함수 선언문은 호이스팅됩니다.',
    '맞습니다. 호이스팅 범위가 다릅니다.',
    '함수 선언문이 완전히 호이스팅됩니다.',
  ]}
/>

<Quiz
  questionNumber={8}
  question="스코프 체인(scope chain)이란?"
  options={[
    '전역 변수끼리 연결되는 체인',
    '함수가 정의된 순서의 목록',
    '변수를 찾을 때 현재 스코프에서 시작해 바깥쪽 스코프로 올라가는 탐색 순서',
    '클로저가 만들어지는 순서',
  ]}
  correctAnswer={3}
  explanation="스코프 체인은 변수를 현재 스코프에서 먼저 찾고, 없으면 바깥 스코프로 올라가며 전역까지 탐색하는 메커니즘입니다."
  optionExplanations={[
    '전역 변수만의 개념이 아닙니다.',
    '함수 정의 순서가 아닙니다.',
    '맞습니다. 안에서 바깥으로 탐색하는 것이 스코프 체인입니다.',
    '클로저 생성 순서와는 다릅니다.',
  ]}
/>

<Quiz
  questionNumber={9}
  question="클로저를 이용한 데이터 은닉의 핵심은?"
  options={[
    'Object.freeze()를 사용한다.',
    '외부 함수의 변수를 내부 함수가 기억하고, 외부에서 직접 접근이 불가한 상태를 유지한다.',
    'class의 private 필드(#)를 사용한다.',
    'const 키워드만으로 충분하다.',
  ]}
  correctAnswer={2}
  explanation="클로저를 이용한 데이터 은닉은 내부 함수가 외부 변수를 기억하면서, 외부에서 그 변수에 직접 접근하지 못하게 합니다."
  optionExplanations={[
    'Object.freeze()는 객체 속성 변경을 막습니다.',
    '맞습니다. 클로저 변수는 반환된 함수를 통해서만 접근 가능합니다.',
    'private 필드는 클래스 문법이며 클로저와 별개입니다.',
    'const는 재할당만 막습니다.',
  ]}
/>

<Quiz
  questionNumber={10}
  question="화살표 함수에서 this는 어떻게 결정되나요?"
  options={[
    '호출 방식에 따라 동적으로 결정된다.',
    '항상 window를 가리킨다.',
    '함수가 정의된 위치의 외부 스코프 this를 캡처한다(렉시컬 this).',
    'new 키워드로 호출해야만 this가 결정된다.',
  ]}
  correctAnswer={3}
  explanation="화살표 함수는 자체 this 바인딩이 없고, 정의된 시점의 외부 컨텍스트 this를 사용합니다. 이를 렉시컬 this라고 합니다."
  optionExplanations={[
    '동적 결정은 일반 함수의 특징입니다.',
    '항상 window가 아닌 외부 스코프의 this입니다.',
    '맞습니다. 정의 시점의 외부 this를 캡처합니다.',
    'new와 화살표 함수는 함께 사용할 수 없습니다.',
  ]}
/>

<Quiz
  questionNumber={11}
  question="프로토타입 체인에서 프로퍼티를 찾지 못하면 최종적으로 무엇이 반환되나요?"
  options={['null', '0', 'undefined', 'TypeError']}
  correctAnswer={3}
  explanation="프로토타입 체인의 끝인 null까지 탐색해도 없으면 undefined가 반환됩니다. 오류가 발생하지는 않습니다."
  optionExplanations={[
    'null 자체가 반환되지는 않습니다.',
    '0이 아닌 undefined입니다.',
    '맞습니다. 없는 프로퍼티 접근 시 undefined가 반환됩니다.',
    '일반적인 프로퍼티 접근에서는 오류가 없습니다.',
  ]}
/>

<Quiz
  questionNumber={12}
  question="ES6 class의 static 메서드를 호출하는 방법은?"
  options={[
    '인스턴스에서 호출한다: instance.method()',
    '클래스 이름으로 호출한다: ClassName.method()',
    'super.method()로 호출한다.',
    'prototype에 접근해 호출한다.',
  ]}
  correctAnswer={2}
  explanation="static 메서드는 클래스의 인스턴스가 아닌 클래스 자체에 속합니다. ClassName.method()로 호출합니다."
  optionExplanations={[
    '인스턴스에는 static 메서드가 없습니다.',
    '맞습니다. 클래스 이름으로 직접 호출합니다.',
    'super는 부모 클래스 참조이며 static 호출 방식이 아닙니다.',
    'prototype을 통한 접근이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={13}
  question="구조 분해 할당에서 기본값이 적용되는 경우는?"
  options={[
    '값이 null일 때',
    '값이 0일 때',
    '값이 undefined이거나 프로퍼티가 없을 때',
    '값이 false일 때',
  ]}
  correctAnswer={3}
  explanation="구조 분해 기본값은 값이 undefined인 경우에만 적용됩니다. null, 0, false, ''는 명시적 값이므로 기본값이 사용되지 않습니다."
  optionExplanations={[
    'null은 명시적 값이므로 기본값이 사용되지 않습니다.',
    '0도 명시적 값입니다.',
    '맞습니다. undefined이거나 없는 경우에만 기본값이 적용됩니다.',
    'false도 명시적 값입니다.',
  ]}
/>

<Quiz
  questionNumber={14}
  question="배열 메서드 중 원본 배열을 수정하는 것은?"
  options={['map()', 'filter()', 'sort()', 'reduce()' ]}
  correctAnswer={3}
  explanation="sort()는 원본 배열을 직접 정렬합니다. map(), filter(), reduce()는 원본을 수정하지 않고 새 값을 반환합니다."
  optionExplanations={[
    'map()은 새 배열을 반환하며 원본 불변입니다.',
    'filter()도 새 배열을 반환합니다.',
    '맞습니다. sort()는 원본 배열을 정렬합니다.',
    'reduce()는 누적값을 반환하며 원본 불변입니다.',
  ]}
/>

<Quiz
  questionNumber={15}
  question="Promise.all()과 Promise.allSettled()의 차이는?"
  options={[
    '둘 다 동일하다.',
    'Promise.all()은 하나라도 실패하면 전체가 reject되고, Promise.allSettled()는 모두 완료된 뒤 성공/실패 여부를 각각 반환한다.',
    'Promise.allSettled()는 성공한 것만 반환한다.',
    'Promise.all()이 더 느리다.',
  ]}
  correctAnswer={2}
  explanation="Promise.all은 하나라도 실패하면 즉시 전체 reject입니다. Promise.allSettled는 모든 Promise가 완료될 때까지 기다리고 각각의 결과를 반환합니다."
  optionExplanations={[
    '둘은 다르게 동작합니다.',
    '맞습니다. 실패 처리 방식이 다릅니다.',
    'allSettled는 실패한 것도 포함해 모두 반환합니다.',
    '실행 속도 비교가 핵심이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={16}
  question="ES 모듈에서 default export를 import하는 방법은?"
  options={[
    "import { default } from './module'",
    "import default from './module'",
    "import anyName from './module'",
    "import * from './module'",
  ]}
  correctAnswer={3}
  explanation="default export는 중괄호 없이 원하는 이름으로 import합니다. 이름은 자유롭게 정할 수 있습니다."
  optionExplanations={[
    "default는 예약어이므로 { default }처럼 쓸 수 없습니다.",
    "'import default'는 구문 오류입니다.",
    "맞습니다. 이름을 자유롭게 정해 import합니다.",
    "'import *'는 네임스페이스 import이며 반드시 'as'가 필요합니다.",
  ]}
/>

<Quiz
  questionNumber={17}
  question="이터러블 객체가 되기 위해 반드시 구현해야 하는 것은?"
  options={[
    'length 프로퍼티',
    '[Symbol.iterator]() 메서드',
    'next() 메서드',
    'forEach() 메서드',
  ]}
  correctAnswer={2}
  explanation="이터러블 프로토콜은 [Symbol.iterator]() 메서드를 구현해야 합니다. 이 메서드가 이터레이터(next() 메서드를 가진 객체)를 반환해야 합니다."
  optionExplanations={[
    'length는 유사 배열의 조건입니다.',
    '맞습니다. Symbol.iterator 구현이 이터러블의 조건입니다.',
    'next()는 이터레이터의 조건입니다(이터러블과 다릅니다).',
    'forEach()는 배열 등의 메서드이며 이터러블의 조건이 아닙니다.',
  ]}
/>

<Quiz
  questionNumber={18}
  question="Set과 배열의 가장 큰 차이는?"
  options={[
    'Set은 인덱스로 요소에 접근할 수 있다.',
    'Set은 중복 값을 허용하지 않는다.',
    '배열은 순서가 없다.',
    'Set은 숫자만 저장할 수 있다.',
  ]}
  correctAnswer={2}
  explanation="Set은 고유한 값만 저장하는 컬렉션입니다. 배열은 중복 값을 허용합니다."
  optionExplanations={[
    'Set은 인덱스 접근을 지원하지 않습니다.',
    '맞습니다. 중복 허용 여부가 핵심 차이입니다.',
    '배열도 삽입 순서가 유지됩니다.',
    'Set은 모든 타입의 값을 저장할 수 있습니다.',
  ]}
/>

<Quiz
  questionNumber={19}
  question="다음 코드의 출력은? `const [a, ...b] = [1, 2, 3, 4]; console.log(b);`"
  options={['[2, 3, 4]', '2', '[1, 2, 3]', 'undefined']}
  correctAnswer={1}
  explanation="나머지 패턴 ...b는 첫 번째 요소 a(=1)를 제외한 나머지 [2, 3, 4]를 배열로 담습니다."
  optionExplanations={[
    '맞습니다. 나머지 요소들이 배열로 b에 담깁니다.',
    'b는 배열이며 숫자 2가 아닙니다.',
    'b는 a 이후의 나머지이므로 [2, 3, 4]입니다.',
    'b는 정상적으로 정의됩니다.',
  ]}
/>

<Quiz
  questionNumber={20}
  question="WeakMap과 일반 Map을 구분하는 핵심 특징은?"
  options={[
    'WeakMap이 더 많은 메서드를 지원한다.',
    'WeakMap은 키가 반드시 객체여야 하며 키에 대한 참조가 사라지면 항목이 자동으로 GC 대상이 된다.',
    'WeakMap은 순회(iteration)가 가능하다.',
    'WeakMap은 문자열 키를 허용하지 않는다는 점만 다르다.',
  ]}
  correctAnswer={2}
  explanation="WeakMap의 핵심은 약한 참조입니다. 키 객체에 대한 다른 참조가 없어지면 WeakMap의 해당 항목도 자동으로 가비지 컬렉션됩니다."
  optionExplanations={[
    'WeakMap은 Map보다 적은 메서드를 지원합니다.',
    '맞습니다. 약한 참조와 자동 GC가 핵심 특징입니다.',
    'WeakMap은 이터러블이 아니어서 순회가 불가합니다.',
    '문자열 키뿐 아니라 원시값 키를 허용하지 않는다는 점이 다릅니다.',
  ]}
/>

## 참고 자료

- [MDN - JavaScript](https://developer.mozilla.org/ko/docs/Web/JavaScript)
- [ECMAScript Language Specification](https://tc39.es/ecma262/)

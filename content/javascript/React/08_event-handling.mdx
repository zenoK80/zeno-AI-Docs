---
title: '08. 이벤트 처리'
description: 'React의 이벤트 핸들러, 합성 이벤트, 폼 이벤트, 이벤트 버블링을 이해합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** React에서 이벤트를 처리하는 방법, HTML 이벤트와의 차이점, 입력 값 다루기, 이벤트 전파 제어를 익힙니다.
</Callout>

## React 이벤트 핸들러

HTML에서는 `onclick="함수()"` 처럼 문자열로 이벤트를 등록합니다. React에서는 JSX prop으로 **함수를 직접 전달**합니다.

```jsx
// HTML 방식
<button onclick="handleClick()">클릭</button>

// React 방식
function Button() {
  function handleClick() {
    alert('클릭!')
  }

  return <button onClick={handleClick}>클릭</button>
}
```

주요 차이점:
- 이벤트 이름은 **camelCase**: `onclick` → `onClick`, `onchange` → `onChange`
- **함수를 전달**하지 문자열을 전달하지 않는다
- `onClick={handleClick}` — 괄호 없이 함수 자체를 전달. `onClick={handleClick()}` 은 실행 결과(보통 undefined)를 전달.

---

## 합성 이벤트 (SyntheticEvent)

React는 브라우저마다 다른 이벤트를 **합성 이벤트**(SyntheticEvent) 로 통일합니다. 덕분에 크로스 브라우저 호환성 걱정 없이 `event.target.value` 같은 표준 속성을 씁니다.

```jsx
function InputExample() {
  function handleChange(event) {
    console.log(event.target.value)  // 입력된 값
    console.log(event.type)          // 'change'
  }

  return <input onChange={handleChange} />
}
```

---

## 자주 쓰는 이벤트 핸들러

```jsx
function EventExamples() {
  return (
    <div>
      {/* 마우스 이벤트 */}
      <button onClick={() => console.log('클릭')}>클릭</button>
      <div onMouseEnter={() => console.log('마우스 올림')}>호버</div>

      {/* 키보드 이벤트 */}
      <input
        onKeyDown={e => {
          if (e.key === 'Enter') console.log('엔터 키 누름')
        }}
      />

      {/* 폼 이벤트 */}
      <input onChange={e => console.log(e.target.value)} />

      {/* 포커스 이벤트 */}
      <input
        onFocus={() => console.log('포커스 들어옴')}
        onBlur={() => console.log('포커스 나감')}
      />
    </div>
  )
}
```

---

## 이벤트 핸들러에 인수 전달하기

이벤트 핸들러에 추가 데이터를 전달하려면 화살표 함수로 감쌉니다.

```jsx
function ItemList() {
  const items = ['사과', '바나나', '포도']

  function handleDelete(itemName) {
    console.log(`${itemName} 삭제`)
  }

  return (
    <ul>
      {items.map(item => (
        <li key={item}>
          {item}
          {/* 화살표 함수로 감싸서 인수 전달 */}
          <button onClick={() => handleDelete(item)}>삭제</button>
        </li>
      ))}
    </ul>
  )
}
```

---

## 이벤트 버블링과 전파 제어

**이벤트 버블링**(Event Bubbling) 은 자식 요소의 이벤트가 부모 요소로 전파되는 현상입니다.

```jsx
function BubblingExample() {
  return (
    <div onClick={() => console.log('div 클릭됨')}>
      <button onClick={() => console.log('button 클릭됨')}>
        클릭
      </button>
    </div>
  )
}
// 버튼 클릭 시: "button 클릭됨" → "div 클릭됨" 순서로 출력
```

전파를 막으려면 `event.stopPropagation()`을 호출합니다.

```jsx
<button onClick={e => {
  e.stopPropagation()  // 부모로 전파 차단
  console.log('button만 실행됨')
}}>
  클릭
</button>
```

### 캡처링(Capturing) — 버블링의 반대 방향

이벤트는 사실 두 단계로 전파됩니다. 부모 → 자식으로 먼저 내려가는 **캡처링 단계**, 그다음 자식 → 부모로 올라가는 **버블링 단계**입니다. React에서 `onClick` 대신 `onClickCapture`를 쓰면 캡처링 단계에서 먼저 실행됩니다.

```
캡처링(내려감) →  div → button
                              ↓ 클릭 발생
버블링(올라감)  ←  div ← button
```

<CodePlayground
  template="react"
  activeFile="/App.js"
  showConsole
  label="캡처링·버블링 순서 실습 — 버튼을 클릭해보세요"
  files={{
    '/App.js': `export default function App() {
  return (
    <div
      onClickCapture={() => console.log('1. div 캡처')}
      onClick={() => console.log('4. div 버블')}
      style={{ padding: 20, border: '1px solid #999' }}
    >
      <button
        onClickCapture={() => console.log('2. button 캡처')}
        onClick={() => console.log('3. button 버블')}
      >
        클릭
      </button>
    </div>
  )
}
// 클릭 시 순서: 1 → 2 → 3 → 4`,
  }}
/>

### 이벤트 위임(Event Delegation) — React가 내부적으로 하는 최적화

React는 각 버튼마다 개별적으로 이벤트 리스너를 등록하지 않습니다. 대신 **루트(root) DOM 노드 하나에만** 실제 이벤트 리스너를 등록해두고, 이벤트가 버블링되어 올라오면 "이게 어느 컴포넌트의 어떤 핸들러였는지" 내부적으로 찾아서 실행합니다.

> **왜 이렇게 하나?** 버튼 1,000개에 리스너를 1,000개 등록하면 메모리를 많이 씁니다. 리스너 1개만 루트에 등록하고 이벤트 버블링을 활용하면 메모리와 성능 면에서 훨씬 효율적입니다. 이 최적화 기법을 **이벤트 위임**이라고 부르며, React가 자동으로 처리해 주므로 개발자가 직접 신경 쓸 필요는 없습니다.

폼의 기본 동작(페이지 새로고침)을 막으려면 `event.preventDefault()`를 사용합니다.

```jsx
function LoginForm() {
  function handleSubmit(e) {
    e.preventDefault()  // 폼 기본 제출 동작 차단
    console.log('폼 제출')
  }

  return (
    <form onSubmit={handleSubmit}>
      <input type="email" />
      <button type="submit">로그인</button>
    </form>
  )
}
```

---

## 이벤트 핸들러와 상태 함께 사용

이벤트 핸들러에서 상태를 바꾸면 리렌더링이 일어납니다.

```jsx
function Counter() {
  const [count, setCount] = useState(0)

  return (
    <div>
      <p>카운트: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
      <button onClick={() => setCount(count - 1)}>-1</button>
    </div>
  )
}

function ToggleButton() {
  const [isOn, setIsOn] = useState(false)

  return (
    <button
      onClick={() => setIsOn(!isOn)}
      style={{ backgroundColor: isOn ? 'green' : 'gray' }}
    >
      {isOn ? '켜짐' : '꺼짐'}
    </button>
  )
}
```

---

## 핵심 정리

- React 이벤트 이름은 **camelCase**: `onClick`, `onChange`, `onKeyDown` 등.
- 이벤트 핸들러에 **함수를 전달**. `onClick={fn}` (O), `onClick={fn()}` (X).
- React가 합성 이벤트로 브라우저 차이를 통일. `e.target.value`로 입력 값 접근.
- 추가 인수 전달: `onClick={() => fn(인수)}` 화살표 함수로 감싸기.
- `e.stopPropagation()`: 이벤트 버블링 차단. `e.preventDefault()`: 기본 동작 차단.

---

## 마무리 복습

<Quiz
  questionNumber={1}
  question="React에서 버튼 클릭 이벤트를 등록하는 올바른 방법은?"
  options={['①onclick={handleClick}', '②onClick=handleClick', '③onClick={handleClick}', '④onClick={handleClick()}']}
  correctAnswer={3}
  explanation="React에서 이벤트는 camelCase로 쓰고, 함수를 중괄호 안에 전달합니다. handleClick()은 함수를 즉시 실행한 결과(보통 undefined)를 전달합니다."
  optionExplanations={['onclick은 HTML 방식입니다. JSX에서는 camelCase인 onClick을 씁니다.', '중괄호가 없으면 문자열로 처리됩니다.', '맞습니다. camelCase + 함수 참조를 중괄호로 전달합니다.', '()를 붙이면 렌더링 시 즉시 실행됩니다.']}
/>

<Quiz
  questionNumber={2}
  question="이벤트 핸들러에서 event.target.value를 사용하는 이유는?"
  options={['①이벤트 타입을 확인하기 위해', '②이벤트를 발생시킨 DOM 요소의 현재 값을 가져오기 위해', '③이벤트를 취소하기 위해', '④부모 요소로 이벤트를 전달하기 위해']}
  correctAnswer={2}
  explanation="event.target은 이벤트를 발생시킨 DOM 요소입니다. input 요소의 경우 event.target.value로 현재 입력된 값을 가져옵니다."
  optionExplanations={['이벤트 타입은 event.type으로 확인합니다.', '맞습니다. 이벤트 발생 요소의 현재 값을 읽습니다.', '이벤트 취소는 preventDefault()로 합니다.', '이벤트 전달은 버블링이 자동으로 처리합니다.']}
/>

<Quiz
  questionNumber={3}
  question="폼 제출 시 페이지 새로고침을 막으려면?"
  options={['①event.stopPropagation()', '②event.preventDefault()', '③return false', '④event.cancel()']}
  correctAnswer={2}
  explanation="event.preventDefault()는 이벤트의 기본 동작을 막습니다. 폼의 경우 페이지를 새로고침하는 기본 제출 동작을 막아줍니다."
  optionExplanations={['stopPropagation은 이벤트 버블링(부모로 전파)을 막습니다.', '맞습니다. preventDefault가 기본 동작을 차단합니다.', 'return false는 일반 JS 이벤트 핸들러에서만 작동합니다. React에서는 효과 없습니다.', 'event.cancel()은 존재하지 않습니다.']}
/>

<Quiz
  questionNumber={4}
  question="이벤트 핸들러에 추가 데이터를 전달하는 올바른 방법은?"
  options={['①onClick={handleClick(id)}', '②onClick={() => handleClick(id)}', '③onClick={handleClick, id}', '④onClick={handleClick.bind(id)}']}
  correctAnswer={2}
  explanation="화살표 함수로 감싸면 클릭 시 인수와 함께 핸들러를 호출할 수 있습니다. onClick={handleClick(id)}는 렌더링 시 즉시 실행됩니다."
  optionExplanations={['렌더링 시 즉시 실행되는 형태입니다.', '맞습니다. 화살표 함수로 감싸서 클릭 시 호출합니다.', '이 문법은 JSX에서 지원되지 않습니다.', 'bind의 첫 번째 인수는 this 컨텍스트입니다. bind(null, id) 형태가 더 적합하나 화살표 함수가 더 명확합니다.']}
/>

<Quiz
  questionNumber={5}
  question="자식 버튼을 클릭했을 때 부모 div의 onClick도 실행되는 현상은?"
  options={['①이벤트 캡처링', '②이벤트 버블링', '③이벤트 위임', '④이벤트 전달']}
  correctAnswer={2}
  explanation="이벤트 버블링은 자식 요소에서 발생한 이벤트가 부모 방향으로 전파되는 현상입니다. stopPropagation()으로 막을 수 있습니다."
  optionExplanations={['이벤트 캡처링은 부모에서 자식 방향으로의 전파 단계입니다.', '맞습니다. 자식 → 부모 방향으로 전파되는 버블링입니다.', '이벤트 위임은 부모에 이벤트를 등록해 자식 이벤트를 처리하는 패턴입니다.', '정확한 용어가 아닙니다.']}
/>

<Quiz
  questionNumber={6}
  question="React가 각 버튼마다 개별 리스너를 등록하지 않고, 루트 DOM 노드 하나에만 리스너를 등록해 이벤트를 처리하는 최적화 기법은?"
  options={['①이벤트 버블링', '②이벤트 위임(Event Delegation)', '③이벤트 캡처링', '④합성 이벤트']}
  correctAnswer={2}
  explanation="이벤트 위임은 여러 요소 각각에 리스너를 다는 대신 상위 노드 하나에만 리스너를 등록하고, 버블링을 활용해 어떤 요소에서 이벤트가 발생했는지 판단하는 기법입니다. 메모리와 성능 면에서 효율적입니다."
  optionExplanations={['버블링은 이벤트 전파 방향이고, 위임은 그것을 활용한 최적화 기법입니다.', '맞습니다. React 내부적으로 이벤트 위임을 사용합니다.', '캡처링은 반대 방향의 전파 단계입니다.', '합성 이벤트는 브라우저 차이를 통일한 이벤트 객체입니다.']}
/>

<Quiz
  questionNumber={7}
  question="onClickCapture와 onClick이 같은 요소에 모두 등록되어 있을 때, 실행 순서는?"
  options={['①onClick이 먼저, onClickCapture가 나중', '②onClickCapture가 먼저, onClick이 나중', '③동시에 실행', '④무작위 순서']}
  correctAnswer={2}
  explanation="캡처링 단계(부모→자식으로 내려감)가 먼저 발생하고, 그 다음 버블링 단계(자식→부모로 올라감)가 발생합니다. 따라서 onClickCapture가 먼저, onClick이 나중에 실행됩니다."
  optionExplanations={['캡처링이 버블링보다 먼저 발생합니다.', '맞습니다. 캡처링 단계가 먼저, 버블링 단계가 나중입니다.', '두 단계는 순차적으로 발생하며 동시가 아닙니다.', '순서는 항상 정해져 있습니다.']}
/>

## 참고 자료

- [React 공식 문서 - 이벤트에 응답하기](https://react.dev/learn/responding-to-events)

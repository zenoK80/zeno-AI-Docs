---
title: '04. JSX와 렌더링'
description: 'JSX 문법, JavaScript 표현식, 조건부 렌더링, 리스트 렌더링을 익힙니다.'
---

<Callout type="info">
  **이번 문서의 목표:** JSX가 무엇인지 이해하고, 중괄호로 JavaScript 표현식을 사용하는 방법, 조건부·리스트 렌더링 패턴을 익힙니다.
</Callout>

## JSX란?

**JSX**(JavaScript XML) 는 JavaScript 안에서 HTML처럼 UI를 작성하는 문법 확장입니다. 브라우저는 JSX를 직접 이해하지 못하므로 Babel이 JavaScript 코드로 변환합니다.

```jsx
// JSX
const element = <h1>안녕하세요!</h1>

// Babel이 변환한 결과 (내부적으로)
const element = React.createElement('h1', null, '안녕하세요!')
```

> **쉽게 말하면:** JSX는 HTML처럼 생긴 JavaScript입니다. 실제로는 `React.createElement()` 호출로 바뀌는 '편의 문법'입니다.

---

## JSX 기본 규칙

### 1. 반드시 하나의 루트 요소

JSX는 반드시 하나의 부모 요소로 감싸야 합니다.

```jsx
// ❌ 오류 — 루트 요소가 두 개
return (
  <h1>제목</h1>
  <p>내용</p>
)

// ✅ div로 감싸기
return (
  <div>
    <h1>제목</h1>
    <p>내용</p>
  </div>
)

// ✅ Fragment 사용 (불필요한 div 없이)
return (
  <>
    <h1>제목</h1>
    <p>내용</p>
  </>
)
```

`<>...</>` 는 **Fragment**라고 하며 실제 DOM에 요소를 추가하지 않습니다.

### 2. 모든 태그는 닫아야 한다

```jsx
// ❌ HTML에서는 되지만 JSX에선 오류
<input type="text">
<br>
<img src="logo.png">

// ✅ 자기 닫기 필수
<input type="text" />
<br />
<img src="logo.png" alt="로고" />
```

### 3. className, htmlFor

HTML 속성 중 JavaScript 예약어인 것들은 다른 이름을 씁니다.

```jsx
// ❌
<div class="box">...</div>
<label for="email">...</label>

// ✅
<div className="box">...</div>
<label htmlFor="email">...</label>
```

### 4. JSX 주석은 중괄호 안에

JSX 안에서는 HTML 주석(`<!-- -->`)이나 JS 한 줄 주석(`//`)을 그냥 쓸 수 없습니다. `{/* ... */}` 형태로 써야 합니다.

```jsx
function Card() {
  return (
    <div>
      {/* 이것이 JSX 안에서 유효한 주석입니다 */}
      <h1>제목</h1>
      {/* <p>임시로 숨긴 내용</p> */}
    </div>
  )
}
```

주석도 결국 `{}` 안의 JavaScript 표현식(정확히는 아무 값도 반환하지 않는 주석)으로 처리되기 때문입니다.

---

## 중괄호 `{}`로 JavaScript 표현식 사용

JSX 안에서 `{ }` 로 JavaScript 표현식을 쓸 수 있습니다. **표현식(값이 나오는 코드)만** 가능합니다.

```jsx
const name = '김철수'
const age = 25

function Profile() {
  return (
    <div>
      <h1>{name}님의 프로필</h1>        {/* 변수 */}
      <p>나이: {age}세</p>             {/* 변수 */}
      <p>내년: {age + 1}세</p>         {/* 계산식 */}
      <p>{name.toUpperCase()}</p>      {/* 메서드 호출 */}
    </div>
  )
}
```

**표현식 vs 문**(statement):
- ✅ 표현식: `1 + 1`, `name.toUpperCase()`, `isOn ? 'ON' : 'OFF'`
- ❌ 문: `if (...)`, `for (...)`, `const x = 1` (직접 사용 불가)

---

## 인라인 스타일

스타일은 문자열이 아닌 **JavaScript 객체**로 전달합니다.

```jsx
// ❌ 문자열 방식 (HTML 방식) — JSX에서 오류
<p style="color: red; font-size: 16px">텍스트</p>

// ✅ 객체 방식 — 속성 이름은 camelCase
<p style={{ color: 'red', fontSize: '16px' }}>텍스트</p>

// 변수로 분리하기
const style = { color: 'red', fontSize: '16px' }
<p style={style}>텍스트</p>
```

---

## 조건부 렌더링

JSX 안에서 `if/else` 는 직접 쓸 수 없고, 표현식을 활용합니다.

```jsx
function Status({ isOnline }) {
  // 방법 1: 삼항 연산자
  return (
    <span>{isOnline ? '온라인' : '오프라인'}</span>
  )
}

function Notification({ count }) {
  // 방법 2: && (단축 평가) — 값이 있을 때만 렌더링
  return (
    <div>
      {count > 0 && <span>알림 {count}개</span>}
    </div>
  )
}
```

<Callout type="warning">
  `&&`로 숫자를 조건으로 쓸 때 `0`은 화면에 0이 출력됩니다. `count > 0 && ...` 처럼 명시적으로 boolean 변환을 하세요.
</Callout>

---

## 리스트 렌더링

배열의 `.map()`으로 여러 요소를 렌더링합니다. 각 요소에는 고유한 `key` prop이 필요합니다.

<CodePlayground
  template="react"
  activeFile="/App.js"
  label="조건부·리스트 렌더링 실습 — 버튼을 눌러보세요"
  files={{
    '/App.js': `import { useState } from 'react'

const fruits = [
  { id: 1, name: '사과', emoji: '🍎' },
  { id: 2, name: '바나나', emoji: '🍌' },
  { id: 3, name: '포도', emoji: '🍇' },
]

export default function App() {
  const [isOnline, setIsOnline] = useState(false)

  return (
    <div>
      <button onClick={() => setIsOnline(!isOnline)}>상태 토글</button>
      <p>{isOnline ? '온라인' : '오프라인'}</p>
      <ul>
        {fruits.map(fruit => (
          <li key={fruit.id}>
            {fruit.emoji} {fruit.name}
          </li>
        ))}
      </ul>
    </div>
  )
}`,
  }}
/>

**key는 왜 필요한가?** React가 리스트가 바뀔 때 어느 항목이 추가·삭제·이동됐는지 효율적으로 파악하기 위해서입니다. key는 형제 요소 사이에서 고유하면 됩니다.

```jsx
// ❌ index를 key로 쓰면 리스트 순서가 바뀔 때 문제 발생
fruits.map((fruit, index) => <li key={index}>...</li>)

// ✅ 고유 id 사용 권장
fruits.map(fruit => <li key={fruit.id}>...</li>)
```

### key=index가 실제로 버그를 만드는 상황

"그냥 인덱스 써도 화면엔 똑같이 보이는데 왜 문제냐"고 생각하기 쉽지만, **리스트 순서가 바뀌거나 중간 항목이 삭제될 때** 실제로 눈에 보이는 버그가 생깁니다.

```jsx
// 각 항목에 입력창(input)이 있는 할 일 목록
{todos.map((todo, index) => (
  <li key={index}>
    <input defaultValue={todo.text} />
  </li>
))}
```

**시나리오:** 목록이 `[할일A, 할일B, 할일C]`이고, 사용자가 `할일B`의 입력창에 "긴급"이라고 타이핑했습니다. 이후 맨 앞의 `할일A`를 삭제하면 배열은 `[할일B, 할일C]`가 됩니다.

- `할일B`는 이제 인덱스 0, `할일C`는 인덱스 1이 됩니다.
- React는 "index=0의 key는 그대로 0이니 같은 컴포넌트"라고 착각해 **입력창의 내용을 재사용**합니다.
- 결과: 화면에는 `할일B`가 나오는데, 입력창에는 원래 `할일A`가 갖고 있던 텍스트(또는 엉뚱한 값)가 남아있는 버그가 발생합니다.

`key={todo.id}`처럼 항목 고유의 id를 쓰면, 항목이 삭제되어도 각 컴포넌트가 "자기 자신"을 정확히 유지하므로 이런 문제가 생기지 않습니다.

<Callout type="info">
  **예외:** 리스트가 절대 추가·삭제·재정렬되지 않고, 입력 상태도 없는 단순 정적 목록이라면 인덱스를 key로 써도 실질적인 문제는 없습니다. 하지만 습관적으로 고유 id를 쓰는 것이 안전합니다.
</Callout>

---

## 핵심 정리

- JSX는 JavaScript 안에서 HTML처럼 UI를 쓰는 문법. Babel이 `React.createElement()`로 변환.
- 반드시 하나의 루트 요소로 감싸야 한다. 불필요한 div가 싫으면 `<>...</>`(Fragment) 사용.
- `class` → `className`, `for` → `htmlFor`, 모든 태그는 닫아야 한다.
- `{ }` 안에 JavaScript 표현식을 쓸 수 있다. `if/for` 같은 문(statement)은 직접 불가.
- 조건부 렌더링: 삼항 연산자(`? :`) 또는 단축 평가(`&&`).
- 리스트 렌더링: `.map()` + `key` prop 필수.

---

## 마무리 복습

<Quiz
  questionNumber={1}
  question="JSX에서 반드시 지켜야 하는 규칙이 아닌 것은?"
  options={['①하나의 루트 요소로 감싸기', '②모든 태그 닫기', '③class 대신 className 사용', '④인라인 스타일은 반드시 객체 변수로 분리하기']}
  correctAnswer={4}
  explanation="인라인 스타일을 변수로 분리하는 것은 선택사항입니다. style={{ color: red }} 처럼 직접 객체 리터럴을 전달해도 됩니다."
  optionExplanations={['루트 요소는 JSX의 필수 규칙입니다.', '모든 태그를 닫는 것은 JSX의 필수 규칙입니다.', 'class는 JS 예약어라 className을 써야 합니다.', '인라인 스타일 객체를 변수로 분리하는 것은 선택사항입니다.']}
/>

<Quiz
  questionNumber={2}
  question="Fragment(프래그먼트)를 사용하는 이유는?"
  options={['①컴포넌트의 성능을 향상시키기 위해', '②불필요한 DOM 요소(div 등) 추가 없이 여러 요소를 하나로 묶기 위해', '③이벤트 전파를 막기 위해', '④CSS 스타일을 적용하기 위해']}
  correctAnswer={2}
  explanation="Fragment(<>...</>)는 실제 DOM 요소를 생성하지 않고 여러 JSX 요소를 하나의 그룹으로 묶어 줍니다. 불필요한 div 중첩을 피할 수 있습니다."
  optionExplanations={['Fragment는 성능 최적화가 목적이 아닙니다.', '맞습니다. 불필요한 DOM 요소 없이 묶는 것이 목적입니다.', '이벤트 전파와는 관련이 없습니다.', 'Fragment에는 스타일을 적용할 수 없습니다.']}
/>

<Quiz
  questionNumber={3}
  question="JSX 안의 {} 에 들어갈 수 없는 것은?"
  options={['①변수', '②함수 호출 결과', '③삼항 연산자', '④if 문']}
  correctAnswer={4}
  explanation="JSX의 {} 안에는 값을 반환하는 표현식만 올 수 있습니다. if 문은 값을 반환하지 않는 '문(statement)'이므로 직접 사용할 수 없습니다."
  optionExplanations={['변수는 값을 나타내므로 가능합니다.', '함수 호출 결과는 값이므로 가능합니다.', '삼항 연산자는 값을 반환하므로 가능합니다.', 'if 문은 표현식이 아닌 문(statement)이라 직접 사용 불가합니다.']}
/>

<Quiz
  questionNumber={4}
  question="리스트 렌더링에서 key prop이 필요한 이유는?"
  options={['①스타일을 구별하기 위해', '②React가 어떤 항목이 변경·추가·삭제됐는지 효율적으로 파악하기 위해', '③이벤트 핸들러를 연결하기 위해', '④서버에서 데이터를 구별하기 위해']}
  correctAnswer={2}
  explanation="key는 React 내부의 Diffing 알고리즘이 리스트 변경 사항을 추적할 때 사용합니다. 고유한 key가 있어야 어떤 항목이 바뀌었는지 정확히 파악할 수 있습니다."
  optionExplanations={['key는 스타일과 관련 없습니다.', '맞습니다. Reconciliation 과정에서 변경 추적에 사용됩니다.', '이벤트 핸들러 연결에는 key가 필요 없습니다.', 'key는 클라이언트 사이드 React의 최적화 목적입니다.']}
/>

<Quiz
  questionNumber={5}
  question="다음 중 count가 0일 때 화면에 0이 출력되는 코드는?"
  options={['①{count > 0 && count개}', '②{count && count개}', '③{count ? count개 : null}', '④{count !== 0 && count개}']}
  correctAnswer={2}
  explanation="&& 연산자에서 count가 0이면 거짓값이지만 React가 0을 그대로 렌더링합니다. count > 0, count !== 0, !! count처럼 명시적으로 boolean 변환해야 합니다."
  optionExplanations={['count > 0은 false가 되어 아무것도 렌더링하지 않습니다.', '맞습니다. count가 0이면 && 앞의 0이 그대로 출력됩니다.', 'count가 0이면 falsy라서 null이 렌더링됩니다.', 'count !== 0은 false가 되어 아무것도 렌더링하지 않습니다.']}
/>

<Quiz
  questionNumber={6}
  question="리스트 렌더링 시 key로 배열의 인덱스를 사용하면 안 되는 주된 이유는?"
  options={['①인덱스는 숫자가 아니기 때문에', '②리스트 항목이 추가·삭제·이동될 때 잘못된 컴포넌트가 업데이트될 수 있기 때문에', '③인덱스를 찾는 비용이 크기 때문에', '④React가 인덱스를 key로 인식하지 못하기 때문에']}
  correctAnswer={2}
  explanation="인덱스는 위치 기반입니다. 항목이 추가·삭제·재정렬되면 인덱스가 바뀌어 React가 잘못된 항목을 재사용하거나 불필요한 리렌더링이 발생합니다."
  optionExplanations={['인덱스는 숫자입니다.', '맞습니다. 인덱스는 위치 기반이라 순서 변경 시 문제가 생깁니다.', '성능 비용이 문제가 아닙니다.', 'React는 숫자를 key로 사용할 수 있습니다.']}
/>

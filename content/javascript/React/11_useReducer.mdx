---
title: '11. useReducer'
description: 'useReducer로 복잡한 상태 로직을 분리하고 관리합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** useReducer의 패턴을 이해하고 복잡한 상태를 체계적으로 관리합니다.
</Callout>

## useReducer 기본

```jsx
import { useReducer } from 'react'

// reducer 함수: 순수 함수여야 함
function counterReducer(state, action) {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 }
    case 'decrement':
      return { count: state.count - 1 }
    case 'reset':
      return { count: 0 }
    default:
      throw new Error(`Unknown action: ${action.type}`)
  }
}

function Counter() {
  const [state, dispatch] = useReducer(counterReducer, { count: 0 })

  return (
    <div>
      <p>카운트: {state.count}</p>
      <button onClick={() => dispatch({ type: 'increment' })}>+1</button>
      <button onClick={() => dispatch({ type: 'decrement' })}>-1</button>
      <button onClick={() => dispatch({ type: 'reset' })}>초기화</button>
    </div>
  )
}
```

## 페이로드(payload) 전달

```jsx
function todoReducer(state, action) {
  switch (action.type) {
    case 'add':
      return [...state, { id: Date.now(), text: action.payload, done: false }]
    case 'toggle':
      return state.map(todo =>
        todo.id === action.payload ? { ...todo, done: !todo.done } : todo
      )
    case 'delete':
      return state.filter(todo => todo.id !== action.payload)
    default:
      return state
  }
}

function TodoApp() {
  const [todos, dispatch] = useReducer(todoReducer, [])
  const [input, setInput] = useState('')

  const addTodo = () => {
    if (!input.trim()) return
    dispatch({ type: 'add', payload: input })
    setInput('')
  }

  return (
    <div>
      <input value={input} onChange={e => setInput(e.target.value)} />
      <button onClick={addTodo}>추가</button>
      <ul>
        {todos.map(todo => (
          <li key={todo.id}>
            <span
              style={{ textDecoration: todo.done ? 'line-through' : 'none' }}
              onClick={() => dispatch({ type: 'toggle', payload: todo.id })}
            >
              {todo.text}
            </span>
            <button onClick={() => dispatch({ type: 'delete', payload: todo.id })}>삭제</button>
          </li>
        ))}
      </ul>
    </div>
  )
}
```

## useState vs useReducer

```
useState가 적합한 경우:
- 단순한 값 (boolean, string, number)
- 서로 독립적인 상태들

useReducer가 적합한 경우:
- 여러 개의 관련된 상태
- 복잡한 상태 전환 로직
- 다음 상태가 이전 상태에 의존
- 테스트하기 쉬운 상태 로직이 필요할 때
```

## Context + useReducer 패턴

```jsx
const StoreContext = createContext(null)
const DispatchContext = createContext(null)

function StoreProvider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState)

  return (
    <StoreContext.Provider value={state}>
      <DispatchContext.Provider value={dispatch}>
        {children}
      </DispatchContext.Provider>
    </StoreContext.Provider>
  )
}

// 소비 훅
export const useStore = () => useContext(StoreContext)
export const useDispatch = () => useContext(DispatchContext)
```

## 핵심 정리

- `useReducer(reducer, initialState)`로 복잡한 상태를 관리합니다.
- reducer는 `(state, action) => newState` 형태의 순수 함수입니다.
- `dispatch({ type: '...', payload: ... })`로 액션을 발생시킵니다.
- Context와 결합해 Redux 패턴을 구현할 수 있습니다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="useReducer의 reducer 함수 시그니처로 올바른 것은?"
  options={[
    '(action) => newState',
    '(state, action) => newState',
    '(dispatch, state) => action',
    '(newState, oldState) => action',
  ]}
  correctAnswer={2}
  explanation="reducer는 현재 상태(state)와 발생한 액션(action)을 받아 새 상태를 반환하는 순수 함수입니다."
  optionExplanations={[
    'state도 인수로 필요합니다.',
    '맞습니다. (state, action) => newState가 표준 형태입니다.',
    'dispatch는 reducer의 인수가 아닙니다.',
    '인수 순서가 다릅니다.',
  ]}
/>

<Quiz
  questionNumber={2}
  question="reducer 함수가 반드시 순수 함수여야 하는 이유는?"
  options={[
    'JavaScript 문법 요구 사항이다.',
    'React가 상태 변화를 예측 가능하게 하고, 동일 입력에 동일 출력을 보장하기 위해',
    '성능을 위해',
    '비동기 처리를 막기 위해',
  ]}
  correctAnswer={2}
  explanation="순수 함수는 동일한 입력에 항상 동일한 출력을 냅니다. 이는 상태 변화를 예측 가능하게 하고 테스트를 쉽게 하며 React의 Strict Mode 같은 기능이 올바르게 동작하게 합니다."
  optionExplanations={[
    'JavaScript 요구사항이 아닙니다.',
    '맞습니다. 예측 가능성과 테스트 용이성이 이유입니다.',
    '성능도 이유이지만 예측 가능성이 더 중요합니다.',
    '순수함수는 동기/비동기와 무관합니다.',
  ]}
/>

<Quiz
  questionNumber={3}
  question="`dispatch({ type: 'add', payload: '새 항목' })`에서 payload의 역할은?"
  options={[
    '액션의 타입을 지정한다.',
    '액션과 함께 전달되는 추가 데이터다.',
    'reducer 함수를 지정한다.',
    '상태를 직접 변경한다.',
  ]}
  correctAnswer={2}
  explanation="payload는 액션에 필요한 추가 데이터를 전달하는 컨벤션입니다. reducer에서 action.payload로 접근하여 상태 업데이트에 사용합니다."
  optionExplanations={[
    '타입은 type 필드가 지정합니다.',
    '맞습니다. payload는 액션의 추가 데이터입니다.',
    'reducer는 useReducer의 첫 번째 인수입니다.',
    '상태는 reducer 반환값으로 변경됩니다.',
  ]}
/>

<Quiz
  questionNumber={4}
  question="useState 대신 useReducer가 적합한 경우는?"
  options={[
    '단순한 boolean 토글',
    '여러 상태가 서로 연관되어 있고 상태 전환 로직이 복잡한 경우',
    '텍스트 입력 값 관리',
    '항상 useReducer가 더 낫다.',
  ]}
  correctAnswer={2}
  explanation="여러 상태가 함께 변경되거나 복잡한 로직이 있을 때 useReducer로 로직을 reducer 함수에 집중시키면 코드가 더 명확해집니다."
  optionExplanations={[
    '단순한 토글은 useState가 더 간단합니다.',
    '맞습니다. 복잡한 연관 상태 관리에 useReducer가 적합합니다.',
    '텍스트 입력은 useState가 더 간단합니다.',
    '상황에 따라 useState가 더 나을 수도 있습니다.',
  ]}
/>

<Quiz
  questionNumber={5}
  question="reducer에서 알 수 없는 action.type을 처리하는 권장 방법은?"
  options={[
    '아무것도 하지 않는다.',
    '현재 상태를 그대로 반환하거나 오류를 throw한다.',
    'undefined를 반환한다.',
    '빈 객체를 반환한다.',
  ]}
  correctAnswer={2}
  explanation="unknown action 처리 방법은 두 가지입니다. 현재 상태를 반환해 안전하게 처리하거나, 개발 중 버그를 빠르게 발견하기 위해 오류를 throw합니다."
  optionExplanations={[
    'undefined를 반환하면 상태가 망가집니다.',
    '맞습니다. 현재 상태 반환 또는 오류 throw가 권장됩니다.',
    'undefined 반환은 상태를 초기화할 수 있어 위험합니다.',
    '빈 객체 반환은 상태를 잃게 됩니다.',
  ]}
/>

<Quiz
  questionNumber={6}
  question="Context + useReducer 패턴에서 상태와 dispatch를 왜 별도 Context로 분리하나요?"
  options={[
    '코드를 더 복잡하게 만들기 위해',
    'dispatch만 사용하는 컴포넌트가 상태 변경 시 불필요하게 리렌더링되는 것을 방지하기 위해',
    '보안을 위해',
    'TypeScript 타입 안전성을 위해',
  ]}
  correctAnswer={2}
  explanation="dispatch 함수는 안정적이지만 상태는 자주 변합니다. 분리하면 dispatch만 사용하는 컴포넌트는 상태 변화 시 리렌더링되지 않아 성능이 개선됩니다."
  optionExplanations={[
    '복잡성이 아닌 성능이 이유입니다.',
    '맞습니다. 불필요한 리렌더링을 방지합니다.',
    '보안과 관련 없습니다.',
    'TypeScript와 무관합니다.',
  ]}
/>

## 참고 자료

- [React - useReducer](https://react.dev/reference/react/useReducer)

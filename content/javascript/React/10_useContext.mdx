---
title: '10. Context API와 useContext'
description: 'Context로 props drilling 없이 전역 상태를 공유하는 방법을 이해합니다.'
---

<Callout type="info">
  **이번 문서의 목표:** Context API로 컴포넌트 트리 전체에 데이터를 공유하고 props drilling을 해결합니다.
</Callout>

## Props Drilling 문제

```jsx
// 문제: 중간 컴포넌트가 사용하지 않는 props를 전달해야 함
function App() {
  const [user, setUser] = useState({ name: '김철수', theme: 'dark' })
  return <Layout user={user} />
}

function Layout({ user }) {
  return <Header user={user} />
}

function Header({ user }) {
  return <UserMenu user={user} />
}

function UserMenu({ user }) {
  return <p>{user.name}</p>  // 실제 사용
}
```

## Context 생성과 사용

```jsx
import { createContext, useContext, useState } from 'react'

// 1. Context 생성
const UserContext = createContext(null)

// 2. Provider로 값 제공
function App() {
  const [user, setUser] = useState({ name: '김철수', theme: 'dark' })

  return (
    <UserContext.Provider value={{ user, setUser }}>
      <Layout />
    </UserContext.Provider>
  )
}

// 3. 어느 깊이에서든 useContext로 소비
function UserMenu() {
  const { user } = useContext(UserContext)
  return <p>{user.name}</p>
}
```

## 커스텀 훅으로 Context 래핑

```jsx
const ThemeContext = createContext('light')

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light')

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  )
}

// 안전하게 사용하는 커스텀 훅
export function useTheme() {
  const context = useContext(ThemeContext)
  if (!context) {
    throw new Error('useTheme은 ThemeProvider 내부에서 사용해야 합니다.')
  }
  return context
}
```

## 여러 Context 조합

```jsx
function App() {
  return (
    <AuthProvider>
      <ThemeProvider>
        <LocaleProvider>
          <AppContent />
        </LocaleProvider>
      </ThemeProvider>
    </AuthProvider>
  )
}
```

## Context 성능 고려사항

```jsx
// Context 값이 바뀌면 useContext를 사용하는 모든 컴포넌트가 리렌더링됨
// 값을 분리하여 불필요한 리렌더링 방지

// 상태와 dispatch를 분리
const StateContext = createContext(null)
const DispatchContext = createContext(null)

function Provider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState)

  return (
    <StateContext.Provider value={state}>
      <DispatchContext.Provider value={dispatch}>
        {children}
      </DispatchContext.Provider>
    </StateContext.Provider>
  )
}
```

## 핵심 정리

- Context는 props drilling 없이 컴포넌트 트리에 데이터를 공유합니다.
- `createContext`, `Provider`, `useContext`를 함께 사용합니다.
- 커스텀 훅으로 Context를 래핑해 안전하고 편리하게 사용합니다.
- Context 값이 변하면 소비하는 모든 컴포넌트가 리렌더링됩니다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="Context API를 사용하는 주된 이유는?"
  options={[
    '컴포넌트를 빠르게 만들기 위해',
    'Props drilling 없이 컴포넌트 트리 어디서든 데이터를 공유하기 위해',
    '서버 사이드 렌더링을 위해',
    '전역 CSS를 관리하기 위해',
  ]}
  correctAnswer={2}
  explanation="Context는 컴포넌트 트리를 통해 데이터를 전달하지 않고 어느 깊이에서든 데이터에 접근할 수 있게 합니다. props drilling 문제를 해결합니다."
  optionExplanations={[
    '속도 개선이 목적이 아닙니다.',
    '맞습니다. props drilling 해결이 핵심 목적입니다.',
    'SSR과 직접 관련이 없습니다.',
    '전역 CSS 관리와 관련 없습니다.',
  ]}
/>

<Quiz
  questionNumber={2}
  question="Context를 사용하려면 어떤 세 가지 요소가 필요한가요?"
  options={[
    'useState, useEffect, useContext',
    'createContext(생성), Provider(공급), useContext(소비)',
    'createContext, Redux, connect',
    'useRef, forwardRef, createContext',
  ]}
  correctAnswer={2}
  explanation="Context 사용의 세 단계: createContext로 Context 객체 생성, Provider로 값 제공, useContext로 값 소비합니다."
  optionExplanations={[
    'useState와 useEffect는 필수가 아닙니다.',
    '맞습니다. 생성, 제공, 소비의 세 단계입니다.',
    'Redux는 별도 라이브러리입니다.',
    'useRef와 forwardRef는 Context와 무관합니다.',
  ]}
/>

<Quiz
  questionNumber={3}
  question="Context의 값이 변경될 때 어떤 컴포넌트가 리렌더링되나요?"
  options={[
    'Provider 컴포넌트만',
    'Provider의 모든 자식 컴포넌트',
    '해당 Context를 useContext로 구독하는 컴포넌트만',
    '앱 전체',
  ]}
  correctAnswer={3}
  explanation="Context 값이 바뀌면 해당 Context를 useContext로 사용하는 컴포넌트들만 리렌더링됩니다. 사용하지 않는 컴포넌트는 영향을 받지 않습니다."
  optionExplanations={[
    'Provider만 리렌더링되지 않습니다.',
    '모든 자식이 리렌더링되지는 않습니다.',
    '맞습니다. useContext를 사용하는 컴포넌트만 리렌더링됩니다.',
    '앱 전체가 리렌더링되지는 않습니다.',
  ]}
/>

<Quiz
  questionNumber={4}
  question="useContext를 커스텀 훅으로 래핑하는 이점은?"
  options={[
    '성능이 향상된다.',
    'Provider 외부에서 사용 시 오류를 발생시켜 잘못된 사용을 방지하고 사용 편의성이 높아진다.',
    '코드가 짧아진다.',
    'Context를 전역에서 사용할 수 있다.',
  ]}
  correctAnswer={2}
  explanation="커스텀 훅에서 context가 null이면 오류를 throw하면 Provider 외부에서 잘못 사용하는 경우를 빠르게 발견할 수 있습니다."
  optionExplanations={[
    '성능은 Context 구조에 달려 있습니다.',
    '맞습니다. 안전한 사용과 오류 감지가 장점입니다.',
    '코드 간결화보다 안전성이 목적입니다.',
    '커스텀 훅과 무관하게 Context는 Provider 범위 내에서 동작합니다.',
  ]}
/>

<Quiz
  questionNumber={5}
  question="Context의 단점은?"
  options={[
    '전역 상태를 공유할 수 없다.',
    'Context 값이 자주 변경되면 해당 Context를 사용하는 컴포넌트들이 빈번하게 리렌더링될 수 있다.',
    '함수를 전달할 수 없다.',
    'React 18 이상에서만 작동한다.',
  ]}
  correctAnswer={2}
  explanation="Context 값이 변할 때마다 useContext를 쓰는 컴포넌트가 리렌더링됩니다. 자주 변하는 데이터에는 상태 분리나 외부 상태관리 라이브러리가 더 적합할 수 있습니다."
  optionExplanations={[
    '전역 상태 공유가 Context의 목적입니다.',
    '맞습니다. 빈번한 업데이트 시 성능 문제가 생길 수 있습니다.',
    '함수도 전달 가능합니다.',
    'React 16.3 이상에서 지원됩니다.',
  ]}
/>

<Quiz
  questionNumber={6}
  question="모든 전역 상태를 Context로 관리하는 것이 항상 옳은가요?"
  options={[
    '예, Context는 모든 상태 관리에 적합합니다.',
    '아니오. 자주 변하는 상태에는 Zustand, Jotai 등 외부 라이브러리가 더 효율적일 수 있습니다.',
    '아니오. Context는 사용이 어려워 redux만 써야 합니다.',
    '예, Context만으로 모든 것이 가능합니다.',
  ]}
  correctAnswer={2}
  explanation="Context는 테마, 인증 정보처럼 자주 변하지 않는 데이터에 적합합니다. 자주 업데이트되는 전역 상태는 Zustand, Jotai, Redux Toolkit 같은 라이브러리가 더 효율적입니다."
  optionExplanations={[
    '모든 상태에 Context가 최선은 아닙니다.',
    '맞습니다. 자주 변하는 상태에는 다른 도구가 적합합니다.',
    'Redux가 유일한 해결책이 아닙니다.',
    '상황에 따라 다른 도구를 선택해야 합니다.',
  ]}
/>

## 참고 자료

- [React - Context로 데이터 깊이 전달하기](https://react.dev/learn/passing-data-deeply-with-context)

---
title: '22. Web API 핵심 퀴즈 2'
description: 'fetch, CORS, WebSocket부터 Observer, Canvas, Worker까지 문서 10~20의 핵심 개념을 종합 점검합니다.'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 앞서 배운 개념을 종합적으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="fetch()가 reject되는 경우는 언제인가요?"
  options={['①HTTP 상태 코드가 404일 때', '②HTTP 상태 코드가 500일 때', '③네트워크 오류(연결 실패, CORS 오류 등)가 발생했을 때', '④서버가 JSON을 반환하지 않을 때']}
  correctAnswer={3}
  explanation="fetch는 네트워크 수준 오류에서만 reject됩니다. HTTP 오류는 resolve로 처리됩니다."
  optionExplanations={['404는 HTTP 오류이므로 resolve됩니다.', '500도 HTTP 오류이므로 resolve됩니다.', '맞습니다. 네트워크 오류에서만 reject됩니다.', 'JSON 여부와 fetch의 reject는 무관합니다.']}
/>

<Quiz
  questionNumber={2}
  question="같은 출처 정책(Same-Origin Policy)이 존재하는 이유는?"
  options={['①웹사이트 로딩 속도를 높이기 위해', '②다른 사이트의 악성 코드가 사용자의 민감한 데이터를 몰래 읽지 못하게 막기 위해', '③서버 비용을 절감하기 위해', '④검색 엔진 최적화를 위해']}
  correctAnswer={2}
  explanation="같은 출처 정책은 악성 사이트가 다른 사이트의 데이터에 몰래 접근하는 것을 막는 보안 장치입니다."
  optionExplanations={['속도 향상이 목적이 아닙니다.', '맞습니다. 보안이 핵심 목적입니다.', '비용 절감과는 무관합니다.', 'SEO와는 무관합니다.']}
/>

<Quiz
  questionNumber={3}
  question="CORS 오류가 발생했을 때 올바른 해결 방향은?"
  options={['①fetch() 호출 코드를 이리저리 수정해본다.', '②서버(API 제공자) 쪽에서 Access-Control-Allow-Origin 헤더를 올바르게 설정해야 한다.', '③브라우저를 재설치한다.', '④CORS 오류는 해결할 수 없다.']}
  correctAnswer={2}
  explanation="CORS는 서버 응답 헤더로 허용 여부를 결정하므로 서버 쪽 설정으로 해결해야 합니다."
  optionExplanations={['클라이언트 코드 수정으로는 해결되지 않습니다.', '맞습니다. 서버 설정이 근본 해결책입니다.', '브라우저 문제가 아닙니다.', '서버 설정으로 해결 가능합니다.']}
/>

<Quiz
  questionNumber={4}
  question="fetch와 WebSocket의 핵심 차이는?"
  options={['①완전히 동일한 기술이다.', '②fetch는 클라이언트가 요청해야만 응답받는 단방향 방식, WebSocket은 서버도 먼저 메시지를 보낼 수 있는 양방향 방식이다.', '③WebSocket은 텍스트만 다루고 fetch는 모든 데이터를 다룬다.', '④fetch가 항상 더 빠르다.']}
  correctAnswer={2}
  explanation="fetch는 요청-응답 한 번으로 끝나지만, WebSocket은 연결이 유지되어 양방향 통신이 가능합니다."
  optionExplanations={['서로 다른 통신 방식입니다.', '맞습니다. 단방향 vs 양방향의 차이입니다.', 'fetch도 다양한 데이터 타입을 다룹니다.', '용도가 달라 단순 비교는 부적절합니다.']}
/>

<Quiz
  questionNumber={5}
  question="history.pushState()가 하는 일은?"
  options={['①페이지를 새로고침하며 서버에 새 요청을 보낸다.', '②서버 요청 없이 주소창의 URL을 바꾸고 방문 기록에 새 항목을 추가한다.', '③현재 페이지를 완전히 삭제한다.', '④쿠키를 초기화한다.']}
  correctAnswer={2}
  explanation="pushState는 서버 요청 없이 URL을 바꾸고 방문 기록을 추가합니다."
  optionExplanations={['서버 요청 없이 동작합니다.', '맞습니다. 새로고침 없는 URL 변경입니다.', '페이지를 삭제하지 않습니다.', '쿠키와는 무관합니다.']}
/>

<Quiz
  questionNumber={6}
  question="localStorage와 sessionStorage의 가장 큰 차이는?"
  options={['①저장 용량이 다르다.', '②localStorage는 탭이 닫혀도 유지되고, sessionStorage는 탭이 닫히면 삭제된다.', '③sessionStorage가 더 보안이 높다.', '④localStorage는 JSON만 저장 가능하다.']}
  correctAnswer={2}
  explanation="localStorage는 영구 보존되지만 sessionStorage는 탭이 닫히면 삭제됩니다."
  optionExplanations={['용량은 둘 다 약 5MB로 비슷합니다.', '맞습니다. 지속 시간이 핵심 차이입니다.', '보안 차이는 주된 구분이 아닙니다.', '둘 다 문자열만 저장하므로 JSON.stringify가 필요합니다.']}
/>

<Quiz
  questionNumber={7}
  question="requestAnimationFrame을 setInterval 대신 애니메이션에 사용하는 이유는?"
  options={['①setInterval은 사용이 어렵기 때문에', '②브라우저의 렌더링 주기에 맞춰 실행되어 부드럽고 효율적인 애니메이션이 가능하다.', '③requestAnimationFrame이 항상 더 빠르게 실행되기 때문에', '④setInterval은 여러 번 호출이 불가하기 때문에']}
  correctAnswer={2}
  explanation="requestAnimationFrame은 화면 갱신 주기에 동기화되어 부드러운 애니메이션과 탭 비활성화 시 자동 절전이 가능합니다."
  optionExplanations={['setInterval도 간단하지만 애니메이션에 부적합합니다.', '맞습니다. 렌더링 주기 동기화가 핵심입니다.', '반드시 더 빠르지는 않습니다.', 'setInterval도 여러 번 호출 가능합니다.']}
/>

<Quiz
  questionNumber={8}
  question="IntersectionObserver를 scroll 이벤트 대신 사용하는 이유는?"
  options={['①scroll 이벤트는 모바일에서 동작하지 않아서', '②브라우저가 최적화해 처리하므로 성능이 좋고 코드도 간단하기 때문', '③더 많은 기능을 제공해서', '④scroll 이벤트가 더 이상 지원되지 않아서']}
  correctAnswer={2}
  explanation="IntersectionObserver는 브라우저가 내부적으로 최적화하여 처리합니다."
  optionExplanations={['scroll 이벤트는 모바일에서도 동작합니다.', '맞습니다. 성능과 코드 단순성이 장점입니다.', '기능보다 성능이 선택 이유입니다.', 'scroll 이벤트는 현재도 지원됩니다.']}
/>

<Quiz
  questionNumber={9}
  question="window.addEventListener('resize', ...)와 ResizeObserver의 차이는?"
  options={['①완전히 동일하다.', '②resize 이벤트는 브라우저 창 전체 크기 변화만 감지하고, ResizeObserver는 특정 요소 자체의 크기 변화를 감지한다.', '③ResizeObserver가 더 오래된 API다.', '④resize 이벤트가 더 정밀하다.']}
  correctAnswer={2}
  explanation="resize는 창 전체를, ResizeObserver는 개별 요소의 크기 변화를 감지합니다."
  optionExplanations={['다른 대상을 감지합니다.', '맞습니다. 감지 대상의 범위가 다릅니다.', 'ResizeObserver가 더 최신 API입니다.', 'ResizeObserver가 요소 단위로 더 정밀합니다.']}
/>

<Quiz
  questionNumber={10}
  question="dragover 이벤트에서 e.preventDefault()를 호출하지 않으면?"
  options={['①drop 이벤트가 정상적으로 발생한다.', '②drop 이벤트 자체가 발생하지 않는다.', '③dragover 이벤트가 무한 반복된다.', '④파일이 자동으로 업로드된다.']}
  correctAnswer={2}
  explanation="브라우저 기본 동작(드롭 거부)이 유지되어 drop 이벤트가 발생하지 않습니다."
  optionExplanations={['drop 이벤트가 발생하지 않습니다.', '맞습니다. 흔한 실수의 원인입니다.', '무한 반복되지 않습니다.', '자동 업로드가 아닙니다.']}
/>

<Quiz
  questionNumber={11}
  question="Geolocation, 카메라, 마이크 API가 공통적으로 요구하는 것은?"
  options={['①로그인', '②사용자의 명시적 권한 허가', '③결제 정보', '④특정 브라우저 확장 프로그램']}
  correctAnswer={2}
  explanation="민감한 개인 정보이므로 브라우저가 사용자 허가를 요구합니다."
  optionExplanations={['로그인 여부와는 무관합니다.', '맞습니다. 사용자 허가가 공통 요구사항입니다.', '결제 정보와는 무관합니다.', '확장 프로그램이 필요하지 않습니다.']}
/>

<Quiz
  questionNumber={12}
  question="FileReader.readAsDataURL()이 반환하는 결과는?"
  options={['①파일의 텍스트 내용', '②base64로 인코딩된 Data URL 문자열', '③ArrayBuffer 객체', '④File 객체']}
  correctAnswer={2}
  explanation="readAsDataURL()은 파일을 base64 인코딩된 data: URL 문자열로 반환합니다."
  optionExplanations={['텍스트는 readAsText()의 결과입니다.', '맞습니다. base64 문자열입니다.', 'ArrayBuffer는 readAsArrayBuffer()의 결과입니다.', 'File 객체가 아닙니다.']}
/>

<Quiz
  questionNumber={13}
  question="Canvas 좌표계에서 y값이 커질수록 어느 방향으로 이동하나요?"
  options={['①위쪽', '②아래쪽', '③왼쪽', '④오른쪽']}
  correctAnswer={2}
  explanation="Canvas는 왼쪽 위가 원점이며 y값이 커질수록 아래로 내려갑니다."
  optionExplanations={['위쪽이 아닙니다.', '맞습니다. y값 증가는 아래쪽입니다.', '왼쪽은 x값 감소 방향입니다.', 'x값 증가가 오른쪽입니다.']}
/>

<Quiz
  questionNumber={14}
  question="Web Worker 안에서 DOM을 직접 조작하려고 하면?"
  options={['①정상적으로 조작된다.', '②DOM에 접근할 수 없어 조작이 불가능하다.', '③메인 스레드의 DOM이 즉시 바뀐다.', '④새로운 DOM이 워커 안에 자동 생성된다.']}
  correctAnswer={2}
  explanation="워커는 DOM에 직접 접근할 수 없어 계산 결과를 메인 스레드로 보내야 합니다."
  optionExplanations={['DOM 접근이 불가능합니다.', '맞습니다. 접근이 제한됩니다.', '직접 접근이 안 되므로 즉시 변경도 불가능합니다.', '워커에는 DOM 자체가 없습니다.']}
/>

<Quiz
  questionNumber={15}
  question="Service Worker를 등록하기 위한 필수 조건은?"
  options={['①모든 환경에서 사용 가능하다.', '②HTTPS 환경(또는 localhost)이어야 한다.', '③Node.js가 필요하다.', '④특정 브라우저에서만 사용 가능하다.']}
  correctAnswer={2}
  explanation="Service Worker는 보안 컨텍스트(HTTPS 또는 localhost)에서만 등록됩니다."
  optionExplanations={['HTTPS 조건이 필요합니다.', '맞습니다. HTTPS나 localhost가 필요합니다.', 'Node.js가 필요하지 않습니다.', '대부분의 최신 브라우저가 지원합니다.']}
/>

## 참고 자료

- [MDN - Web APIs](https://developer.mozilla.org/ko/docs/Web/API)

---
title: '01-복습. Web APIs란?'
description: 'ECMAScript와 Web API의 경계를 4지선다 퀴즈로 점검하고, 콘솔에서 직접 판별 기준을 적용해 본다.'
---

## 01편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"Web API의 명세(공식 규격 문서)는 누가 관리하나?"}
  options={[
    "TC39",
    "WHATWG · W3C",
    "각 브라우저 회사가 마음대로 정한다",
    "정해진 관리 주체가 없다",
  ]}
  correctAnswer={2}
  explanation={"WHATWG와 W3C는 웹 표준을 정하는 단체 이름이다. 이들이 DOM, Fetch, HTML 같은 Web API의 규격을 관리한다. 반면 TC39는 자바스크립트 언어(ECMAScript) 규격을 관리하는 별도의 단체다."}
  optionExplanations={[
    "TC39는 Web API가 아니라 자바스크립트 언어(ECMAScript) 자체를 관리하는 별도의 단체다.",
    "정답이다. WHATWG와 W3C가 DOM, Fetch 같은 Web API의 규격을 관리한다.",
    "브라우저 회사가 마음대로 정하는 게 아니라, 여러 회사가 참여하는 표준 단체가 합의된 규격을 관리한다.",
    "Web API도 ECMAScript처럼 명확한 관리 주체(WHATWG·W3C)가 있다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"다음 중 Web API에 속하는 것을 모두 고른 조합은? (`fetch` / `class` / `localStorage` / `Map` / `addEventListener`)"}
  options={[
    "`fetch`, `class`, `Map`",
    "`fetch`, `localStorage`, `addEventListener`",
    "`class`, `Map`, `addEventListener`",
    "전부 다 Web API다",
  ]}
  correctAnswer={2}
  explanation={"`fetch`, `localStorage`, `addEventListener`는 전부 브라우저가 추가로 얹어주는 Web API다. `class`와 `Map`은 언어(ECMAScript) 자체에 내장된 기능이라 Web API가 아니다."}
  optionExplanations={[
    "`class`와 `Map`은 Web API가 아니라 언어(ECMAScript) 자체에 내장된 기능이다.",
    "정답이다. `fetch`, `localStorage`, `addEventListener` 세 가지 모두 브라우저가 제공하는 Web API다.",
    "`class`와 `Map`은 언어 자체 기능이라서 이 조합에 섞이면 안 된다.",
    "`class`와 `Map`은 언어에 내장된 기능이지 브라우저가 추가한 기능이 아니므로, 전부가 Web API인 건 아니다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"`setTimeout`, `fetch`처럼 Node.js에서도 쓸 수 있는 API를 이 시리즈(Web APIs)에서 다루는 이유는?"}
  options={[
    "Node.js에서도 쓸 수 있으니 사실 언어 자체 기능이기 때문에",
    "이들은 원래 브라우저(WHATWG) 명세의 API이고, Node.js가 나중에 가져다 구현한 것이기 때문에",
    "이름이 헷갈려서 그냥 편의상 여기서 다루는 것뿐, 원칙적으로는 언어 소속이 맞다",
    "Node.js가 이 API들을 처음 만들었고 브라우저가 나중에 따라 했기 때문에",
  ]}
  correctAnswer={2}
  explanation={"`fetch`나 `setTimeout`은 원래 브라우저를 위해 만들어진 WHATWG 명세의 API다. Node.js가 개발자 편의를 위해 나중에 자기 환경에도 비슷하게 구현해서 넣은 것이지, 그렇다고 언어(ECMAScript) 소속으로 바뀌는 건 아니다."}
  optionExplanations={[
    "여러 환경에서 쓸 수 있다고 해서 소속이 바뀌지는 않는다. 기준은 \"원래 어느 명세에 정의됐는가\"다.",
    "정답이다. 원래 브라우저를 위한 WHATWG 명세 API이고, Node.js가 나중에 가져다 구현한 것이다.",
    "단순한 편의상 분류가 아니라, \"원래 어느 명세 소속인가\"라는 명확한 기준에 따른 분류다.",
    "시간 순서가 반대다. 브라우저(WHATWG 명세)가 먼저이고, Node.js가 나중에 비슷하게 구현했다.",
  ]}
/>

## 실습 — 판별 기준 적용해 보기

아래 목록을 보고, 배운 판별 기준(① Node.js에는 없는가? ② WHATWG·W3C 명세인가? ③ 렌더링·상호작용·네트워크 관련인가?)으로 Web API인지 언어인지 스스로 분류해 보자.

```
1. querySelector
2. Promise
3. JSON.parse
4. MutationObserver
5. Array.map
6. CustomEvent
```

<Collapse title="정답 보기">
Web API: 1(querySelector), 4(MutationObserver), 6(CustomEvent)
언어(ECMAScript): 2(Promise), 3(JSON.parse), 5(Array.map)
</Collapse>

콘솔에서 각각을 직접 쳐서 `[native code]`가 나오는지도 확인해 보자. (힌트: 둘 다 `[native code]`로 나오므로 출력만으로는 구별 안 된다 — 그래서 판별 기준이 필요하다.)

## 정리 체크리스트

<Callout type="default">
- [ ] Web API와 ECMAScript의 관리 주체 차이를 안다
- [ ] 세 가지 판별 기준으로 새로운 API를 분류할 수 있다
- [ ] Node.js에도 있는 API가 왜 이 시리즈 소관인지 설명할 수 있다
</Callout>

다음 편에서는 이 경계를 실전 사례로 더 깊이 파본다.

---
title: '05-복습. window와 브라우저 전역 환경'
description: 'window와 globalThis의 관계, var와 let의 전역 등록 차이를 4지선다 퀴즈와 콘솔 실습으로 점검한다.'
---

## 05편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"`document`, `console`은 사실 무엇의 프로퍼티(소속된 값)인가?"}
  options={[
    "`HTML`",
    "`window`",
    "`Node.js`",
    "독립적인 존재이지 누구의 프로퍼티도 아니다",
  ]}
  correctAnswer={2}
  explanation={"`window`는 브라우저 자바스크립트의 전역 객체다. `document`, `console` 등은 사실 `window.document`, `window.console`인데, `window.`을 생략해도 되게 만들어져서 짧게 쓸 수 있는 것뿐이다."}
  optionExplanations={[
    "`HTML`이라는 이름의 자바스크립트 객체는 존재하지 않는다.",
    "정답이다. `document`, `console`은 사실 `window`의 프로퍼티이며, `window.`을 생략해서 짧게 쓸 수 있는 것이다.",
    "`Node.js`는 브라우저와 다른 별도의 실행 환경이지, `document`가 소속된 객체가 아니다.",
    "`document`, `console`은 실제로 `window`라는 상위 객체에 소속된 프로퍼티다. 독립적인 존재가 아니다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"최상위(어떤 함수 안도 아닌 곳)에서 `var siteName = 'A'`로 선언했다면, `window.siteName`의 값은?"}
  options={[
    "`undefined`",
    "`'A'`",
    "에러가 난다",
    "`null`",
  ]}
  correctAnswer={2}
  explanation={"`var`로 선언한 최상위 변수는 자동으로 `window`의 프로퍼티로 등록된다. 그래서 `window.siteName`을 확인하면 `'A'`가 그대로 나온다."}
  optionExplanations={[
    "`var`로 선언한 최상위 변수는 값이 그대로 `window`에 등록되므로 `undefined`가 아니라 실제 값이 나온다.",
    "정답이다. `var`로 선언한 최상위 변수는 자동으로 `window`의 프로퍼티가 되어 `'A'`가 그대로 나온다.",
    "이 코드는 정상적으로 동작하며 에러가 나지 않는다.",
    "`null`은 개발자가 직접 넣어야 나오는 값이라, 이 코드에서는 나오지 않는다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"`window`와 `globalThis`의 차이를 가장 정확히 설명한 것은?"}
  options={[
    "완전히 똑같아서 아무 때나 바꿔 써도 된다",
    "`window`는 브라우저 전용 이름이라 Node.js에는 없지만, `globalThis`는 환경을 가리지 않고 \"지금 환경의 전역 객체\"를 가리킨다",
    "`globalThis`가 옛날 이름이고 `window`가 새로 나온 이름이다",
    "`globalThis`는 함수에서만 쓸 수 있다",
  ]}
  correctAnswer={2}
  explanation={"`window`는 오직 브라우저에만 존재하는 이름이다. `globalThis`는 ES2020에서 추가된, 환경에 상관없이 항상 통하는 전역 객체 이름이다."}
  optionExplanations={[
    "`window`는 Node.js에 없어서 두 이름을 아무 환경에서나 똑같이 바꿔 쓸 수는 없다.",
    "정답이다. `window`는 브라우저 전용이고, `globalThis`는 환경에 상관없이 통하는 이름이다.",
    "시간 순서가 반대다. `window`가 원래부터 있던 브라우저 전용 이름이고, `globalThis`가 2020년에 나중에 추가됐다.",
    "`globalThis`는 함수 안이 아니라 어디서든(최상위 스코프 포함) 쓸 수 있는 전역 이름이다.",
  ]}
/>

## 실습 — window 뜯어보기

브라우저 콘솔에서 순서대로 실행해 보자.

```js
// 1. window 안에 프로퍼티가 몇 개나 있는지
Object.keys(window).length

// 2. var로 선언해서 window에 등록되는지 확인
var testVar = 'var는 등록됨'
console.log(window.testVar)

// 3. let으로 선언하면 어떻게 다른지 확인
let testLet = 'let은 등록 안 됨'
console.log(window.testLet)

// 4. globalThis로 확인
console.log(globalThis === window)
```

<Collapse title="예상 결과">
1. 수백 개
2. `'var는 등록됨'`
3. `undefined`
4. `true`
</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] `window`가 브라우저의 전역 객체라는 걸 안다
- [ ] `var`와 `let`의 전역 등록 차이를 직접 확인했다
- [ ] `globalThis`를 언제 쓰는지 설명할 수 있다
</Callout>

이걸로 Part 1(브라우저와 실행 환경)의 도입부를 마쳤다. 다음 파트부터 HTML과 DOM을 본격적으로 다룬다.

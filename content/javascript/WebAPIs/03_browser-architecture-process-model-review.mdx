---
title: '03-복습. 브라우저 구조와 프로세스 모델'
description: '멀티 프로세스 구조를 4지선다 퀴즈로 점검하고, 작업 관리자로 실제 프로세스 개수를 확인해 본다.'
---

## 03편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"옛날(2008년 이전) 브라우저처럼 프로세스 하나로 모든 탭을 처리하는 구조의 가장 큰 문제는?"}
  options={[
    "속도가 너무 빨라서 오히려 불안정했다",
    "탭 하나의 스크립트가 무한 루프에 빠지면 다른 탭까지 전부 멈췄다",
    "메모리를 거의 안 써서 컴퓨터가 아까웠다",
    "특별한 문제는 없었고 그냥 유행이 지나서 바뀌었다",
  ]}
  correctAnswer={2}
  explanation={"프로세스가 하나뿐이면 그 안의 모든 탭이 같은 메모리 공간을 나눠 쓴다. 탭 A의 코드에 문제가 생기면 격리되지 않고 탭 B, C까지 전부 영향을 받아 브라우저 전체가 멈췄다."}
  optionExplanations={[
    "속도가 문제가 아니라, 격리되지 않은 구조 때문에 한 탭의 오류가 전체로 번지는 게 진짜 문제였다.",
    "정답이다. 탭들이 격리되지 않아서, 한 탭의 무한 루프가 다른 모든 탭까지 멈추게 만들었다.",
    "메모리를 적게 쓰는 건 오히려 장점처럼 보이지만, 실제로는 격리 부족이라는 심각한 안정성 문제가 있었다.",
    "단순한 유행 변화가 아니라, 안정성과 보안이라는 명확한 기술적 문제 때문에 구조가 바뀌었다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"우리가 짜는 자바스크립트 코드는 어느 프로세스 안에서 실행되나?"}
  options={[
    "브라우저 프로세스(Browser Process)",
    "렌더러 프로세스(Renderer Process)",
    "GPU 프로세스",
    "네트워크 프로세스",
  ]}
  correctAnswer={2}
  explanation={"렌더러 프로세스가 실제 웹페이지(HTML·CSS·JS)를 처리하는 담당이고, 탭마다 별도로 뜬다. 브라우저 프로세스는 주소창·북마크 등 브라우저 UI 전체를 총괄하는 역할이라 우리 코드와는 별개다."}
  optionExplanations={[
    "브라우저 프로세스는 주소창·북마크 같은 브라우저 UI를 총괄할 뿐, 웹페이지의 자바스크립트를 실행하지 않는다.",
    "정답이다. 웹페이지의 HTML·CSS·JS는 탭마다 뜨는 렌더러 프로세스가 처리한다.",
    "GPU 프로세스는 그래픽 처리를 전담하는 별도 프로세스이지, 자바스크립트 실행 담당이 아니다.",
    "네트워크 프로세스는 네트워크 요청을 전담하는 별도 프로세스이지, 자바스크립트 실행 담당이 아니다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"멀티 프로세스 구조로 안정성과 보안을 얻는 대신 치러야 하는 대가는?"}
  options={[
    "속도가 항상 느려진다",
    "메모리 사용량이 늘어난다",
    "인터넷 연결이 더 자주 끊긴다",
    "대가가 전혀 없다",
  ]}
  correctAnswer={2}
  explanation={"프로세스마다 각자 별도의 메모리 공간을 차지하기 때문에, 탭을 많이 열수록 전체 메모리 사용량이 늘어난다. 이런 \"무언가를 내주고 무언가를 얻는 관계\"를 트레이드오프라고 부른다."}
  optionExplanations={[
    "속도가 항상 느려지는 게 아니라, 오히려 각 탭이 독립적으로 실행되어 반응성이 좋아지는 경우도 많다. 대가는 메모리 쪽이다.",
    "정답이다. 프로세스마다 별도 메모리 공간을 쓰므로 전체 메모리 사용량이 늘어난다.",
    "멀티 프로세스 구조와 인터넷 연결 안정성은 관계없다.",
    "메모리 사용량 증가라는 실제 대가가 있으므로, \"대가가 전혀 없다\"는 틀렸다.",
  ]}
/>

## 실습 — 작업 관리자로 직접 확인하기

<Steps>

### 크롬 작업 관리자를 연다

<kbd>Shift</kbd>+<kbd>Esc</kbd> 또는 메뉴 → 도구 더보기 → 작업 관리자

### 탭 3개를 새로 연다

각기 다른 사이트를 열어본다.

### 프로세스 목록에서 `탭:`으로 시작하는 항목 개수를 센다

새로 연 탭 수만큼 늘어났는지 확인한다.

### 탭 하나를 닫아본다

작업 관리자 목록에서도 해당 프로세스가 사라지는지 확인한다.

</Steps>

이 실습으로 "탭 = 별도 프로세스"라는 걸 눈으로 직접 확인했다.

## 정리 체크리스트

<Callout type="default">
- [ ] 프로세스가 하나뿐이던 구조의 문제를 설명할 수 있다
- [ ] 렌더러 프로세스가 무엇을 담당하는지 안다
- [ ] 멀티 프로세스 구조의 트레이드오프(메모리 vs 안정성)를 안다
</Callout>

다음 편에서는 이 렌더러 프로세스 안으로 들어가 본다.

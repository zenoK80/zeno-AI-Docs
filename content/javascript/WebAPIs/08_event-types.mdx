---
title: '08. 이벤트 종류'
description: '마우스, 키보드, 폼, 로드, 스크롤 등 주요 이벤트 유형과 각 이벤트 객체의 특성을 익힙니다.'
---

<Callout type="info">
  **이번 문서의 목표:** 상황별로 어떤 이벤트를 사용해야 하는지 판단하고, 각 이벤트 객체의 고유한 프로퍼티를 활용할 수 있습니다.
</Callout>

## 마우스 이벤트

```js
const box = document.querySelector('.box')

box.addEventListener('click', (e) => console.log('클릭', e.clientX, e.clientY))
box.addEventListener('dblclick', () => console.log('더블 클릭'))
box.addEventListener('mouseenter', () => console.log('마우스 진입'))
box.addEventListener('mouseleave', () => console.log('마우스 이탈'))
box.addEventListener('mousemove', (e) => console.log(e.offsetX, e.offsetY))
box.addEventListener('contextmenu', (e) => {
  e.preventDefault()  // 우클릭 메뉴 방지
})
```

<Table>
  <thead>
    <tr><th>이벤트</th><th>설명</th></tr>
  </thead>
  <tbody>
    <tr><td>mouseenter / mouseleave</td><td>요소 진입/이탈, 자식 요소로는 버블링되지 않음</td></tr>
    <tr><td>mouseover / mouseout</td><td>요소 진입/이탈, 자식 요소를 거칠 때도 버블링됨</td></tr>
    <tr><td>mousemove</td><td>요소 위에서 마우스가 움직일 때마다 (매우 빈번하게 발생)</td></tr>
  </tbody>
</Table>

## 키보드 이벤트

<CodePlayground
  template="vanilla"
  activeFile="/index.js"
  showConsole
  label="키보드 이벤트 실습 — 입력창에 타이핑해보세요"
  files={{
    '/index.html': `<input type="text" placeholder="여기에 입력하세요" />
<script src="/index.js"></script>`,
    '/index.js': `const input = document.querySelector('input')

input.addEventListener('keydown', (e) => {
  console.log('키 눌림:', e.key, '/ 물리 위치:', e.code)
  if (e.key === 'Enter') console.log('엔터 입력됨')
})

input.addEventListener('keyup', (e) => {
  console.log('키 뗌:', e.key)
})`,
  }}
/>

`e.key`는 실제 입력된 문자('a', 'A', 'Enter')이고, `e.code`는 키보드의 물리적 위치('KeyA', 'Enter')입니다. 한글 입력이나 Shift 조합 시 `key`는 바뀌지만 `code`는 항상 동일한 물리 키를 가리킵니다.

## 폼 이벤트

```js
const form = document.querySelector('form')
const emailInput = document.querySelector('#email')

form.addEventListener('submit', (e) => {
  e.preventDefault()   // 기본 폼 제출(페이지 새로고침) 방지
  const data = new FormData(form)
  console.log(data.get('email'))
})

emailInput.addEventListener('input', (e) => {
  console.log('실시간 값:', e.target.value)  // 매 입력마다 발생
})

emailInput.addEventListener('change', (e) => {
  console.log('변경 확정:', e.target.value)  // 포커스를 잃을 때 발생
})

emailInput.addEventListener('focus', () => console.log('포커스 얻음'))
emailInput.addEventListener('blur', () => console.log('포커스 잃음'))
```

`input`은 값이 바뀔 때마다 실시간으로, `change`는 값 변경이 "확정"될 때(포커스를 잃거나 Enter)만 발생합니다.

## 로드 이벤트 — 페이지가 준비되는 두 시점

```js
// 1. HTML 파싱이 끝난 시점 — 이미지·CSS는 아직 로딩 중일 수 있음
document.addEventListener('DOMContentLoaded', () => {
  console.log('DOM 준비됨')
})

// 2. 이미지, CSS, 폰트 등 모든 외부 리소스까지 완전히 로딩된 시점
window.addEventListener('load', () => {
  console.log('페이지 완전 로딩')
})
```

`DOMContentLoaded`가 먼저, `load`가 나중에 발생합니다. DOM 요소를 조작하는 코드는 대부분 `DOMContentLoaded`만으로 충분하며, 이미지 크기를 계산해야 하는 것처럼 리소스가 완전히 필요한 경우에만 `load`를 기다립니다.

## 스크롤 이벤트

```js
window.addEventListener('scroll', () => {
  const scrollY = window.scrollY
  const scrollHeight = document.documentElement.scrollHeight
  const clientHeight = window.innerHeight
  const progress = (scrollY / (scrollHeight - clientHeight)) * 100

  console.log(`스크롤 진행률: ${progress.toFixed(1)}%`)
})
```

<Callout type="warning">
  스크롤 이벤트는 스크롤 중 매우 빈번하게(초당 수십 번) 발생합니다. 무거운 계산을 매번 실행하면 화면이 버벅입니다. `requestAnimationFrame`으로 프레임 단위로 묶어 처리하거나, 뷰포트 진입 감지가 목적이라면 아예 `IntersectionObserver`(16번 문서)로 대체하는 것이 성능에 좋습니다.
</Callout>

## 포인터 이벤트 — 마우스와 터치를 통합

```js
const canvas = document.querySelector('canvas')

canvas.addEventListener('pointerdown', (e) => {
  console.log('포인터 타입:', e.pointerType)  // 'mouse', 'touch', 'pen'
  console.log('압력:', e.pressure)
})
```

마우스 전용 이벤트(`mousedown` 등)와 터치 전용 이벤트(`touchstart` 등)를 각각 따로 처리하던 것을, **Pointer Events**는 입력 장치 종류와 상관없이 하나의 이벤트로 통합해 처리할 수 있게 해줍니다.

## 핵심 정리

- mouseenter/leave는 버블링되지 않고, mouseover/out은 버블링된다.
- e.key는 입력된 문자, e.code는 물리적 키 위치를 나타낸다.
- input은 매 입력마다, change는 값 변경이 확정될 때 발생한다.
- DOMContentLoaded는 HTML 파싱 완료 시, load는 모든 리소스 로딩 완료 시 발생한다.
- 스크롤 이벤트는 매우 빈번하므로 requestAnimationFrame이나 IntersectionObserver로 최적화한다.

## 마무리 복습

<Quiz
  questionNumber={1}
  question="mouseenter와 mouseover의 차이는?"
  options={['①mouseenter는 버블링되지만 mouseover는 안 된다.', '②mouseenter는 버블링되지 않지만 mouseover는 자식 요소를 거칠 때도 버블링된다.', '③완전히 동일하다.', '④mouseover는 우클릭에만 발생한다.']}
  correctAnswer={2}
  explanation="mouseenter는 요소 자체에 대해서만 발생하고 버블링되지 않습니다. mouseover는 자식 요소 경계를 거칠 때도 계속 발생하며 버블링됩니다."
  optionExplanations={['mouseenter가 버블링되지 않는 쪽입니다.', '맞습니다. 버블링 여부가 핵심 차이입니다.', '동작 방식이 다릅니다.', '마우스 진입/이탈과 관련된 이벤트입니다.']}
/>

<Quiz
  questionNumber={2}
  question="e.key와 e.code의 차이는?"
  options={['①둘 다 같은 값을 반환한다.', "②e.key는 실제 입력 문자, e.code는 물리적 키 위치를 나타낸다.", '③e.code는 숫자 코드를 반환한다.', '④e.key는 항상 소문자만 반환한다.']}
  correctAnswer={2}
  explanation="e.key는 Shift나 한글 입력 등에 따라 달라지는 실제 문자이고, e.code는 항상 같은 물리적 키의 위치(예: 'KeyA')를 나타냅니다."
  optionExplanations={['서로 다른 값을 반환합니다.', '맞습니다. 논리적 문자와 물리적 위치의 차이입니다.', "e.code는 'KeyA' 같은 문자열입니다.", '대문자 입력 시 e.key도 대문자로 반환됩니다.']}
/>

<Quiz
  questionNumber={3}
  question="input 이벤트와 change 이벤트의 발생 시점 차이는?"
  options={['①둘 다 매 키 입력마다 발생한다.', '②input은 값이 바뀔 때마다, change는 포커스를 잃거나 확정될 때 발생한다.', '③change는 체크박스에서만 발생한다.', '④input은 폼 제출 시에만 발생한다.']}
  correctAnswer={2}
  explanation="input은 실시간으로, change는 값 변경이 확정되는 시점(포커스 상실 등)에 발생합니다."
  optionExplanations={['change는 매 입력마다 발생하지 않습니다.', '맞습니다. 발생 시점이 다릅니다.', 'change는 다양한 입력 요소에서 발생합니다.', 'input은 매 입력마다 실시간으로 발생합니다.']}
/>

<Quiz
  questionNumber={4}
  question="DOMContentLoaded와 load 이벤트 중 먼저 발생하는 것은?"
  options={['①load가 항상 먼저', '②DOMContentLoaded가 먼저, load가 나중', '③동시에 발생', '④순서가 무작위']}
  correctAnswer={2}
  explanation="HTML 파싱만 끝나면 DOMContentLoaded가 먼저 발생하고, 이미지 등 모든 리소스까지 로딩이 끝나야 load가 발생합니다."
  optionExplanations={['load가 항상 나중입니다.', '맞습니다. DOMContentLoaded가 먼저 발생합니다.', '발생 시점이 다릅니다.', '항상 정해진 순서를 따릅니다.']}
/>

<Quiz
  questionNumber={5}
  question="스크롤 이벤트 처리를 최적화하는 방법으로 적절하지 않은 것은?"
  options={['①requestAnimationFrame으로 프레임 단위 처리', '②뷰포트 진입 감지는 IntersectionObserver로 대체', '③스크롤 콜백 안에서 매번 무거운 DOM 조작을 직접 실행', '④필요한 계산만 최소화해서 실행']}
  correctAnswer={3}
  explanation="스크롤은 매우 빈번하게 발생하므로 콜백 안에서 무거운 작업을 그대로 실행하면 성능 저하가 심합니다. 이는 최적화 방법이 아니라 문제를 일으키는 원인입니다."
  optionExplanations={['적절한 최적화 방법입니다.', '적절한 최적화 방법입니다.', '맞습니다. 이는 오히려 성능을 악화시킵니다.', '적절한 최적화 방향입니다.']}
/>

---
title: '21. Web API 핵심 퀴즈 1'
description: 'DOM, BOM, 이벤트, 폼까지 문서 01~09의 핵심 개념을 종합 점검합니다.'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 앞서 배운 개념을 종합적으로 재구성한 문제입니다.
</Callout>

<Quiz
  questionNumber={1}
  question="Web API를 가장 알맞게 설명한 것은?"
  options={['①JavaScript 변수 이름을 짓는 규칙', '②브라우저 기능을 정해진 방법으로 사용하게 해 주는 연결 창구', '③HTML 파일을 압축하는 형식', '④CSS 색상만 계산하는 프로그램']}
  correctAnswer={2}
  explanation="Web API는 문서, 이벤트, 네트워크 같은 브라우저 기능을 JavaScript에서 사용하도록 공개한 인터페이스입니다."
  optionExplanations={['변수 이름 규칙은 JavaScript 문법입니다.', '맞습니다. 공개된 사용법으로 기능을 요청합니다.', '파일 압축 형식이 아닙니다.', '색상 계산에 한정되지 않습니다.']}
/>

<Quiz
  questionNumber={2}
  question="DOM이 '트리'라고 불리는 이유는?"
  options={['①실제 나무 그림을 사용해서', '②노드들이 부모-자식 관계로 연결되어 가지처럼 뻗어나가는 구조이기 때문', '③트리라는 이름의 개발자가 만들어서', '④HTML의 별명이 트리라서']}
  correctAnswer={2}
  explanation="DOM은 document를 뿌리로 하여 요소들이 계층적으로 연결된 구조입니다."
  optionExplanations={['그림과는 무관합니다.', '맞습니다. 계층적 구조가 나무 모양과 닮았습니다.', '이름의 유래가 아닙니다.', 'HTML의 별명이 아닙니다.']}
/>

<Quiz
  questionNumber={3}
  question="`document`와 `window.document`의 관계는?"
  options={['①서로 다른 별개의 객체다.', '②완전히 같은 객체이며, document는 window.을 생략한 표현이다.', '③document가 window를 포함한다.', '④브라우저마다 다르다.']}
  correctAnswer={2}
  explanation="document는 window.document의 축약 표현으로 완전히 동일한 객체입니다."
  optionExplanations={['같은 객체를 가리킵니다.', '맞습니다. window.document와 동일합니다.', 'window가 document를 포함하는 관계입니다.', '표준화된 동작입니다.']}
/>

<Quiz
  questionNumber={4}
  question="HTMLCollection이 '살아있는(live)' 컬렉션이라는 의미는?"
  options={['①메모리에서 자동으로 삭제된다.', '②DOM이 변경되면 컬렉션 내용도 실시간으로 자동 갱신된다.', '③이터러블이 아니다.', '④항상 비어 있다.']}
  correctAnswer={2}
  explanation="HTMLCollection은 DOM 변경 시 즉시 반영되는 live 컬렉션입니다."
  optionExplanations={['자동 삭제와는 무관합니다.', '맞습니다. DOM 변경이 실시간 반영됩니다.', '순회는 가능합니다.', '항상 비어 있지 않습니다.']}
/>

<Quiz
  questionNumber={5}
  question="children과 childNodes의 차이는?"
  options={['①둘 다 텍스트 노드를 포함한다.', '②children은 요소 노드만, childNodes는 텍스트 노드를 포함한 모든 자식 노드를 반환한다.', '③childNodes는 요소 노드만 반환한다.', '④완전히 동일하다.']}
  correctAnswer={2}
  explanation="children은 Element 노드만, childNodes는 텍스트·주석 노드까지 모두 포함합니다."
  optionExplanations={['children은 텍스트 노드를 포함하지 않습니다.', '맞습니다. 포함 범위가 다릅니다.', 'childNodes가 모든 노드를 포함합니다.', '서로 다릅니다.']}
/>

<Quiz
  questionNumber={6}
  question="innerHTML에 사용자 입력을 직접 삽입하면 안 되는 이유는?"
  options={['①렌더링 속도가 느려지기 때문이다.', '②XSS(Cross-Site Scripting) 공격에 취약해지기 때문이다.', '③HTML 태그가 이스케이프되기 때문이다.', '④텍스트만 지원하기 때문이다.']}
  correctAnswer={2}
  explanation="innerHTML은 HTML을 파싱해 렌더링하므로 악성 스크립트가 실행될 수 있습니다."
  optionExplanations={['보안 문제가 핵심입니다.', '맞습니다. 악성 script 태그 등이 실행될 수 있습니다.', 'innerHTML은 이스케이프하지 않습니다.', 'HTML 마크업을 지원합니다.']}
/>

<Quiz
  questionNumber={7}
  question="이벤트 전파의 올바른 순서는?"
  options={['①타겟 → 캡처링 → 버블링', '②캡처링 → 타겟 → 버블링', '③버블링 → 캡처링 → 타겟', '④순서가 정해져 있지 않다.']}
  correctAnswer={2}
  explanation="이벤트는 캡처링 → 타겟 → 버블링 순서로 전파됩니다."
  optionExplanations={['타겟이 먼저가 아닙니다.', '맞습니다. 캡처링 → 타겟 → 버블링입니다.', '순서가 반대입니다.', '고정된 순서가 있습니다.']}
/>

<Quiz
  questionNumber={8}
  question="stopPropagation()과 preventDefault()의 차이는?"
  options={['①동일한 기능이다.', '②stopPropagation은 이벤트 전파를 막고, preventDefault는 브라우저의 기본 동작을 막는다.', '③preventDefault가 전파를 막는다.', '④stopPropagation이 기본 동작을 막는다.']}
  correctAnswer={2}
  explanation="stopPropagation은 전파를, preventDefault는 기본 동작을 각각 독립적으로 막습니다."
  optionExplanations={['서로 다른 역할입니다.', '맞습니다. 전파 중단과 기본 동작 차단입니다.', 'preventDefault는 전파와 무관합니다.', 'stopPropagation은 기본 동작을 막지 않습니다.']}
/>

<Quiz
  questionNumber={9}
  question="이벤트 위임(event delegation)의 핵심 장점은?"
  options={['①이벤트 전파를 완전히 막는다.', '②부모 하나에 리스너를 달아 메모리를 절약하고, 동적으로 추가된 자식에도 자동 적용된다.', '③항상 동기적으로 처리한다.', '④캡처링 단계에서만 동작한다.']}
  correctAnswer={2}
  explanation="이벤트 위임은 부모 리스너 하나로 자식들의 이벤트를 효율적으로 처리합니다."
  optionExplanations={['전파 차단이 목적이 아닙니다.', '맞습니다. 효율성과 동적 대응이 핵심입니다.', '동기/비동기와 무관합니다.', '버블링을 주로 활용합니다.']}
/>

<Quiz
  questionNumber={10}
  question="e.key와 e.code의 차이는?"
  options={['①둘 다 같은 값을 반환한다.', '②e.key는 실제 입력 문자, e.code는 물리적 키 위치를 나타낸다.', '③e.code는 숫자 코드를 반환한다.', '④e.key는 항상 소문자만 반환한다.']}
  correctAnswer={2}
  explanation="e.key는 실제 입력 문자, e.code는 물리적 키 위치입니다."
  optionExplanations={['서로 다른 값입니다.', '맞습니다. 논리적 문자 vs 물리적 위치입니다.', "e.code는 'KeyA' 같은 문자열입니다.", '대문자도 그대로 반환됩니다.']}
/>

<Quiz
  questionNumber={11}
  question="input 이벤트와 change 이벤트의 발생 시점 차이는?"
  options={['①둘 다 매 키 입력마다 발생한다.', '②input은 값이 바뀔 때마다, change는 포커스를 잃거나 확정될 때 발생한다.', '③change는 체크박스에서만 발생한다.', '④input은 폼 제출 시에만 발생한다.']}
  correctAnswer={2}
  explanation="input은 실시간으로, change는 값 변경이 확정될 때 발생합니다."
  optionExplanations={['change는 매 입력마다 발생하지 않습니다.', '맞습니다. 발생 시점이 다릅니다.', 'change는 다양한 요소에서 발생합니다.', 'input은 실시간으로 발생합니다.']}
/>

<Quiz
  questionNumber={12}
  question="DOMContentLoaded와 load 이벤트 중 먼저 발생하는 것은?"
  options={['①load가 항상 먼저', '②DOMContentLoaded가 먼저, load가 나중', '③동시에 발생', '④순서가 무작위']}
  correctAnswer={2}
  explanation="HTML 파싱이 끝나면 DOMContentLoaded, 모든 리소스가 로딩되면 load가 발생합니다."
  optionExplanations={['load가 항상 나중입니다.', '맞습니다. DOMContentLoaded가 먼저입니다.', '발생 시점이 다릅니다.', '정해진 순서를 따릅니다.']}
/>

<Quiz
  questionNumber={13}
  question="FormData의 핵심 장점은?"
  options={['①폼 데이터를 JSON으로 자동 변환한다.', '②폼 요소를 자동으로 검사한다.', '③폼 전체 입력값을 name 속성 기준으로 쉽게 수집하고, 파일을 포함한 요청도 처리한다.', '④GET 요청에 쿼리스트링을 자동 추가한다.']}
  correctAnswer={3}
  explanation="FormData는 폼의 모든 값을 자동으로 모으고 파일 전송도 처리합니다."
  optionExplanations={['자동 JSON 변환은 없습니다.', '유효성 검사는 별도 API입니다.', '맞습니다. 데이터 수집과 파일 전송이 장점입니다.', 'GET 쿼리스트링과는 무관합니다.']}
/>

<Quiz
  questionNumber={14}
  question="`input.validity.valueMissing`이 true인 경우는?"
  options={['①입력값이 너무 길 때', '②required 속성이 있는데 값이 비어 있을 때', '③이메일 형식이 맞지 않을 때', '④값이 최솟값보다 작을 때']}
  correctAnswer={2}
  explanation="valueMissing은 required인데 값이 비어 있을 때 true가 됩니다."
  optionExplanations={['너무 길면 tooLong입니다.', '맞습니다. required이면서 비어있을 때 true입니다.', '형식 오류는 typeMismatch입니다.', '최솟값 미만은 rangeUnderflow입니다.']}
/>

<Quiz
  questionNumber={15}
  question="novalidate 속성이 하는 일은?"
  options={['①폼 제출 자체를 막는다.', '②브라우저의 기본 유효성 검사 UI를 비활성화해 JavaScript로 직접 제어할 수 있게 한다.', '③폼을 읽기 전용으로 만든다.', '④AJAX 제출을 자동으로 활성화한다.']}
  correctAnswer={2}
  explanation="novalidate는 브라우저 내장 검증 팝업을 끄고 커스텀 검증을 구현할 수 있게 합니다."
  optionExplanations={['제출 자체를 막지는 않습니다.', '맞습니다. 커스텀 검증을 위한 옵션입니다.', 'readonly가 읽기 전용을 담당합니다.', 'AJAX와 직접 관련 없습니다.']}
/>

## 참고 자료

- [MDN - Web APIs](https://developer.mozilla.org/ko/docs/Web/API)

---
title: '02-복습. 브라우저는 화면을 어떻게 그릴까'
description: '렌더링 파이프라인을 4지선다 퀴즈로 점검하고, 크롬 Performance 탭으로 실제 리플로우 비용을 관찰해 본다.'
---

## 02편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"브라우저가 화면을 그리는 다섯 단계를 순서대로 나열한 것은?"}
  options={[
    "페인트 → 레이아웃 → DOM 생성 → CSSOM 생성 → 렌더 트리",
    "DOM 생성 → CSSOM 생성 → 렌더 트리 생성 → 레이아웃 → 페인트",
    "CSSOM 생성 → DOM 생성 → 레이아웃 → 렌더 트리 → 페인트",
    "순서는 정해져 있지 않고 브라우저 마음대로다",
  ]}
  correctAnswer={2}
  explanation={"DOM과 CSSOM을 각각 만든 뒤, 이 둘을 합쳐 실제로 화면에 그려질 것만 남긴 렌더 트리를 만든다. 그다음 위치·크기를 계산하는 레이아웃, 마지막으로 픽셀을 칠하는 페인트 순서로 진행된다."}
  optionExplanations={[
    "순서가 완전히 거꾸로다. 그리기(페인트)는 항상 마지막 단계이지 첫 단계가 아니다.",
    "정답이다. DOM과 CSSOM을 먼저 만들고, 렌더 트리, 레이아웃, 페인트 순서로 진행된다.",
    "DOM이 CSSOM보다 항상 먼저 만들어지며, 렌더 트리 없이 레이아웃부터 계산할 수는 없다.",
    "이 다섯 단계는 브라우저가 매번 동일하게 따르는 정해진 순서다. 임의로 바뀌지 않는다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"\"DOM 조작이 느리다\"는 말을 정확하게 고쳐 말하면?"}
  options={[
    "DOM을 읽고 쓰는 것 자체가 원래 느리다",
    "DOM을 바꾼 뒤 브라우저가 다시 계산해야 하는 레이아웃·페인트 작업이 비용이 크다",
    "사실은 전혀 느리지 않고 아무 문제 없다",
    "오래된 브라우저에서만 느리다",
  ]}
  correctAnswer={2}
  explanation={"DOM 자체를 읽거나 쓰는 동작은 빠르다. 진짜 비용이 드는 건 그 변경 때문에 브라우저가 다시 실행해야 하는 레이아웃(위치·크기 재계산)과 페인트(다시 그리기) 작업이다."}
  optionExplanations={[
    "DOM을 읽거나 쓰는 동작 자체는 빠르다. 느려지는 원인은 그 뒤에 따라오는 재계산 작업이다.",
    "정답이다. DOM 변경 자체가 아니라, 그로 인해 다시 실행되는 레이아웃·페인트 작업이 비용의 진짜 원인이다.",
    "무분별하게 DOM을 자주 바꾸면 재계산 비용이 쌓여 실제로 화면이 버벅일 수 있으므로 \"문제 없다\"는 틀린 설명이다.",
    "레이아웃·페인트 재계산 비용은 최신 브라우저에서도 똑같이 발생하는 구조적인 문제다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"React의 Virtual DOM과 일괄 반영(batching)이 실제로 하는 일은?"}
  options={[
    "DOM 조작을 아예 없애버린다",
    "\"언제, 얼마나 자주 브라우저에 재계산을 요청할지\"를 개발자 대신 관리해서 반영 횟수를 줄인다",
    "브라우저의 레이아웃 계산 자체를 더 빠른 알고리즘으로 바꿔치기한다",
    "CSS를 자동으로 최적화해준다",
  ]}
  correctAnswer={2}
  explanation={"Virtual DOM과 일괄 반영(Batching)은 여러 변경을 모아 최소한의 횟수로 실제 DOM에 반영해 준다. DOM 조작 자체를 없애는 게 아니라, 얼마나 효율적으로 반영할지를 대신 관리해주는 것이다."}
  optionExplanations={[
    "React도 결국 실제 DOM을 조작해서 화면을 바꾼다. 조작 자체를 없애는 게 아니라 그 횟수를 줄여줄 뿐이다.",
    "정답이다. Virtual DOM과 batching은 변경을 모아 반영 횟수를 최소화하도록 관리해준다.",
    "브라우저 내부의 레이아웃 계산 알고리즘 자체는 React가 손댈 수 없는 영역이다.",
    "CSS 코드 자체를 자동으로 최적화하는 기능은 아니다. React가 관리하는 건 DOM 반영 시점과 횟수다.",
  ]}
/>

## 실습 — Performance 탭으로 리플로우 직접 관찰하기

<Steps>

### 아무 웹페이지에서 개발자 도구를 연다

<kbd>F12</kbd> → **Performance** 탭

### 녹화 버튼을 누른다

빨간 원 버튼을 클릭한다.

### 페이지를 스크롤하거나 버튼을 몇 번 클릭한다

3~5초 정도면 충분하다.

### 녹화를 멈추고 결과를 본다

그래프에서 **노란색(Scripting, 자바스크립트 실행)**, **보라색(Rendering, 레이아웃 계산)**, **초록색(Painting, 색칠하기)** 구간의 비율을 확인한다.

</Steps>

어느 색이 가장 큰 비중을 차지했는가? 그 이유를 이 편의 내용으로 설명해 보자.

## 정리 체크리스트

<Callout type="default">
- [ ] 렌더링 파이프라인 5단계를 순서대로 말할 수 있다
- [ ] "DOM 조작이 느리다"는 말을 정확하게 고쳐 말할 수 있다
- [ ] Performance 탭에서 Rendering/Painting 구간을 찾아봤다
</Callout>

다음 편에서는 반대쪽, JavaScript 엔진이 코드를 실행하는 원리를 본다.

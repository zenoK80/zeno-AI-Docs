---
title: '05-복습. JSX 완전 정복'
description: 'JSX 문법 규칙을 4지선다 퀴즈로 점검하고, 틀린 JSX 코드를 찾아 고치는 실습을 한다.'
---

## 05편 복습

## 문제로 확인하기

<Quiz
  questionNumber={1}
  question={"JSX에서 `class` 대신 `className`을 쓰는 이유는?"}
  options={[
    "그냥 React 팀의 개인적인 취향이라서",
    "`class`는 자바스크립트에서 클래스를 선언하는 예약어(이미 다른 용도로 정해진 단어)라서",
    "`className`이 타이핑하기 더 편해서",
    "HTML에서도 원래 `className`을 쓰기 때문에",
  ]}
  correctAnswer={2}
  explanation={"JSX는 결국 `React.createElement(...)` 같은 자바스크립트 코드로 변환된다. `class`는 자바스크립트에서 이미 예약어라서 속성 이름으로 그대로 쓰면 충돌이 난다. 그래서 DOM의 실제 프로퍼티 이름인 `className`을 대신 쓴다."}
  optionExplanations={[
    "취향 문제가 아니라, JSX가 변환되는 자바스크립트 코드에서 `class`가 예약어와 충돌하기 때문에 생긴 규칙이다.",
    "정답이다. `class`가 자바스크립트의 예약어와 충돌해서 DOM의 실제 프로퍼티 이름인 `className`을 대신 쓴다.",
    "타이핑 편의성과는 관계없다. 오히려 `className`이 `class`보다 더 길다.",
    "HTML에서는 원래 `class`를 쓴다. `className`은 JSX(자바스크립트)에서만 쓰는 이름이다.",
  ]}
/>
<Quiz
  questionNumber={2}
  question={"JSX의 중괄호(`{ }`) 안에 넣을 수 있는 것은?"}
  options={[
    "`if` 문을 포함한 모든 자바스크립트 코드",
    "표현식(값으로 평가되는 코드)만",
    "오직 문자열만",
    "아무것도 넣을 수 없다",
  ]}
  correctAnswer={2}
  explanation={"JSX의 `{ }` 안에는 표현식(평가되어 하나의 값이 되는 코드)만 들어갈 수 있다. `if`문은 값으로 평가되지 않는 문이라 그 자리에 들어갈 수 없다."}
  optionExplanations={[
    "`if`문처럼 값으로 평가되지 않는 문(statement)은 `{ }` 안에 넣을 수 없다. 조건 분기는 삼항 연산자나 `&&`로 표현해야 한다.",
    "정답이다. `{ }` 안에는 표현식(값으로 평가되는 코드)만 들어갈 수 있다.",
    "문자열만이 아니라 숫자, 변수, 함수 호출 등 값으로 평가되는 모든 표현식이 들어갈 수 있다.",
    "표현식이라면 얼마든지 넣을 수 있다. 완전히 비워두는 것과는 다른 이야기다.",
  ]}
/>
<Quiz
  questionNumber={3}
  question={"최상위 요소가 여러 개일 때, DOM에 별도 요소를 만들지 않으면서 감싸는 문법은?"}
  options={[
    "`<div>...</div>`",
    "`<span>...</span>`",
    "`<>...</>` (Fragment)",
    "`<group>...</group>`",
  ]}
  correctAnswer={3}
  explanation={"Fragment(`<>...</>`)는 여러 요소를 하나로 묶어주되, 실제 화면의 DOM에는 아무 요소도 추가하지 않는 특수 문법이다. React 컴포넌트는 최상위 요소를 하나만 반환할 수 있으므로, 여러 요소를 나란히 두려면 Fragment로 묶어야 한다."}
  optionExplanations={[
    "`<div>`로 감싸도 동작은 하지만, 불필요한 `<div>`가 실제 화면 DOM 구조에 그대로 남는다는 점이 Fragment와 다르다.",
    "`<span>`도 `<div>`와 마찬가지로 실제 DOM에 요소를 추가한다. \"별도 요소를 만들지 않는다\"는 조건에 맞지 않는다.",
    "정답이다. Fragment(`<>...</>`)는 실제 DOM에 아무 요소도 추가하지 않으면서 여러 요소를 묶어준다.",
    "`<group>`은 HTML/JSX에 존재하지 않는 태그다.",
  ]}
/>

## 실습 — 틀린 JSX 찾아 고치기

아래 코드에는 문법 오류가 4개 숨어 있다. 하나씩 찾아 고쳐보자.

```jsx
function Profile() {
  return (
    <h1>내 프로필</h1>
    <img src="me.png">
    <div class="info" onclick="handleClick()">
      <p>안녕하세요</p>
    </div>
  )
}
```

<Collapse title="정답 보기">

```jsx
function Profile() {
  return (
    <>
      <h1>내 프로필</h1>
      <img src="me.png" />
      <div className="info" onClick={handleClick}>
        <p>안녕하세요</p>
      </div>
    </>
  )
}
```

고친 것: ① 최상위 요소 두 개 → Fragment로 감쌈 ② `<img>` 셀프 클로징 안 함 → `/>` 추가
③ `class` → `className` ④ `onclick="..."` (문자열 호출) → `onClick={handleClick}` (함수 참조)
</Collapse>

## 정리 체크리스트

<Callout type="default">
- [ ] JSX와 HTML의 차이 4가지(최상위 하나, 태그 닫기, className, 중괄호)를 안다
- [ ] 틀린 JSX를 보고 스스로 고칠 수 있다
- [ ] JSX가 결국 함수 호출로 변환된다는 걸 기억한다
</Callout>

이걸로 "왜 React이고 어떻게 동작하나" 파트를 마쳤다. 다음 편부터 컴포넌트와 Props를 다룬다.

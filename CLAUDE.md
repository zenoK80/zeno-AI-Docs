# Claude Code 작업 지침

이 프로젝트는 **Next.js + Nextra** 기반 학습 문서 사이트입니다.
MDX 파일을 작성하거나 수정할 때 아래 규칙을 **반드시** 지켜야 합니다.
규칙을 어기면 빌드가 깨져 사이트가 배포되지 않습니다.

---

## MDX 파싱 규칙 (빌드 에러 방지)

### ❌ JSX prop 안에 작은따옴표 중첩 금지

JSX 컴포넌트의 `{}` 배열이나 문자열 prop 안에서 작은따옴표를 중첩하면 acorn 파싱 에러가 납니다.

```mdx
// ❌ 에러 — 작은따옴표 안에 또 작은따옴표
options={['변수를 'let'으로 선언한다']}
explanation="typeof는 'string'을 반환한다"

// ✅ 수정 — 내부 따옴표 제거
options={['변수를 let으로 선언한다']}
explanation="typeof는 string을 반환한다"
```

### ❌ JSX prop 안에 `<`, `>` 날것 사용 금지

`question=""` 같은 큰따옴표 속성에서 꺾쇠괄호를 쓰면 JSX 태그로 해석됩니다.

```mdx
// ❌ 에러
question="T<string>이란?"
options={['List<T>를 사용한다']}

// ✅ 수정 — HTML entity 사용
question="T&lt;string&gt;이란?"
options={['List&lt;T&gt;를 사용한다']}

// ✅ 또는 꺾쇠 없이 설명
question="제네릭 T(string)이란?"
```

### ❌ 산문(코드블록 밖)에 날것의 `{`, `}` 금지

MDX는 `{}`를 JSX 표현식으로 해석합니다.

```mdx
// ❌ 에러
프로세스 ID는 {pid}로 확인합니다.
환경변수 ${HOME}을 출력합니다.

// ✅ 수정 — 인라인 코드 스팬 사용
프로세스 ID는 `{pid}`로 확인합니다.
환경변수 `$HOME`을 출력합니다.
```

### ❌ 산문에 날것의 `<`, `>` 금지 (코드블록 밖)

```mdx
// ❌ 에러
IP 주소 범위는 0.0.0.0 ~ 255.255.255.255 <클래스 A 이상>

// ✅ 수정
IP 주소 범위는 0.0.0.0 ~ 255.255.255.255 (클래스 A 이상)
```

### ❌ 큰따옴표 속성 안에 백슬래시 이스케이프 금지

```mdx
// ❌ 에러
explanation="브라우저에서 \"module\" 속성이 필요합니다."

// ✅ 수정 — 이스케이프 없이 표현
explanation="브라우저에서 module 속성이 필요합니다."
```

---

## 목차(TOC) 설계 원칙

### 핵심 원칙: 덧셈을 알아야 곱셈을 한다

모든 과목의 목차는 **선행 개념을 독립 파일로 순서에 포함**한다.
`00_prerequisites.mdx` 하나에 몰아넣지 않는다.
선행 개념마다 별도 파일을 만들어 본문 파일과 순서대로 나열한다.

```
// ❌ 잘못된 방식
00_prerequisites.mdx  ← 모든 선행 개념을 한 파일에 몰아넣음
01_main-topic.mdx

// ✅ 올바른 방식 — 덧셈(선행) 먼저, 그다음 곱셈(본론)
01_prerequisite-concept-A.mdx   ← 선행 개념 1
02_prerequisite-concept-B.mdx   ← 선행 개념 2
03_first-main-topic.mdx         ← 본론 시작
04_second-main-topic.mdx
...
NN_exam-practice.mdx            ← 마지막은 기출·모의
```

### 파일 수 기준

- 일반 학습 파일: **10~15분** 읽을 분량 (핵심 개념 2~4개)
- 선행 개념 파일: 과목당 2~5개 (완전 뉴비가 본론 진입 전에 알아야 할 것만)
- 기출·모의 파일: 마지막 2~5개
- 일반 과목: 총 15~35편이 적절 (억지로 늘리거나 줄이지 않는다)

### 목차 작성 절차

1. `plan-study/reference/` 또는 `plan-skill/reference/` 폴더에 레퍼런스가 있으면 먼저 읽는다
2. 없으면 공식 출제 기준·시험 안내 페이지를 참조한다
3. 선행 개념(뉴비가 모를 만한 것)을 먼저 나열한다
4. 본론 파일을 논리적 순서로 나열한다
5. 마지막에 기출 유형·모의고사 파일을 배치한다

---

## MDX 파일 구조

### 파일명 규칙

- `NN_kebab-case.mdx` (NN은 두 자리 숫자: 01, 02, ... 10, 11, ...)
- frontmatter에는 `title`, `description`만 포함

### 일반 학습 파일 구조 (모든 파일 동일)

```mdx
---
title: 'NN. 제목'
description: '한 줄 설명'
---

<Callout type="info">
  **이번 문서의 목표:** 이 파일을 다 읽으면 무엇을 할 수 있는지 한 문장으로.
</Callout>

## 소제목

> **쉽게 말하면:** 개념을 한 줄로 요약.

[내용 — 왜 필요한가(동기) → 정의 → 예시·비유 → 실제 적용]

## 핵심 정리

[이 파일에서 배운 것 3~5줄 불릿]

## 마무리 복습

<Quiz
  questionNumber={1}
  question="문제?"
  options={['①보기1', '②보기2', '③보기3', '④보기4']}
  correctAnswer={1}
  explanation="해설"
  optionExplanations={['설명1', '설명2', '설명3', '설명4']}
/>
```

### 기출·모의 파일 구조

```mdx
---
title: 'NN. [과목명] 예상기출문제'
description: '[과목명] 시험 대비 예상기출 문제 모음'
---

<Callout type="warning">
  실제 기출 문제를 그대로 복제하지 않습니다. 공식 출제 범위를 바탕으로 재구성한 문제입니다.
</Callout>

<Quiz questionNumber={1} ... />
<Quiz questionNumber={2} ... />
...
<Quiz questionNumber={20} ... />
```

---

## Quiz 컴포넌트 규칙

- `import` 없이 전역 컴포넌트로 바로 사용
- `correctAnswer`: **1-indexed** 정수 (1, 2, 3, 4 중 하나)
- **일반 학습 파일**: 마무리 복습 Quiz **5~8개**
- **기출·모의 파일**: Quiz **15~25개**
- `question`, `explanation` 속성은 큰따옴표(`"`)로 감싸기
- `options`, `optionExplanations` 배열 원소는 작은따옴표(`'`)로 감싸기
  - **배열 원소 안에 작은따옴표 절대 사용 금지**

```mdx
<Quiz
  questionNumber={1}
  question="문제 내용"
  options={['①보기1', '②보기2', '③보기3', '④보기4']}
  correctAnswer={2}
  explanation="정답 근거와 오답 이유"
  optionExplanations={['오답 이유', '정답 근거', '오답 이유', '오답 이유']}
/>
```

---

## 수식 표기

- 인라인 수식: `$수식$`
- 블록 수식: `$$수식$$`
- KaTeX로 렌더링됨
- 수식 바로 아래에 각 변수 설명 필수

```mdx
$$
\bar{x} = \frac{1}{n} \sum_{i=1}^{n} x_i
$$

- $\bar{x}$ (엑스 바): 표본 평균
- $n$: 데이터 개수
- $x_i$: i번째 데이터 값
- $\sum$ (시그마): i=1부터 n까지 모두 더하라는 기호
```

---

## 레퍼런스·출처 규칙

- `plan-study/reference/[과목]/` 폴더에 파일이 있으면 **반드시** 해당 파일을 참조하고 본문에 인용
- **레퍼런스 폴더가 있는 과목**: MDX 맨 아래 `## 참고 자료` 섹션 불필요 (내부 파일이 출처)
- **레퍼런스 폴더가 없는 과목**: MDX 맨 아래에 아래 형식으로 출처 표기

```mdx
---

## 참고 자료

- [자료 이름](URL)
- [자료 이름](URL)
```

---

## 전역 컴포넌트 (import 없이 사용)

### Nextra 기본 컴포넌트

| 컴포넌트 | 용도 |
|----------|------|
| `Callout` | 중요 설명·주의사항 (`type="info"`, `"warning"`, `"error"`) |
| `Table` | 비교표 |
| `Tabs` / `Tab` | 탭 전환 |
| `Steps` | 단계별 과정 |
| `Collapse` | 접었다 펼치는 설명 |
| `Cards` / `Card` | 문서 링크 카드 |
| `FileTree` | 폴더 구조 |
| `Playground` | MDX·JSX 클라이언트 렌더링 (일반 JS 실행기 아님) |

### 커스텀 컴포넌트

| 컴포넌트 | 용도 | import |
|----------|------|--------|
| `Quiz` | 4지선다 퀴즈 (정답·해설 표시) | 불필요 (전역) |
| `CodePlayground` | Sandpack 기반 JS·React 코드 편집·실행 | 불필요 (전역) |
| `OneDriveVideo` | OneDrive 학습 영상 iframe 삽입 | 불필요 (전역) |

### 시각화 패키지 (app/components/ 에 래핑 후 사용)

| 패키지 | 용도 | 적합한 상황 |
|--------|------|-------------|
| `KaTeX` | 수식 (`$...$`, `$$...$$`, nextra 내장 렌더러) | 수학·통계 공식. **볼드(`**`) 텍스트 안에 괄호 `(...)`를 절대 넣지 않는다** — `**용어(English)**` 형태는 렌더링이 깨지는 버그가 있음(2026-08 확인). 반드시 `**용어**(English)`처럼 괄호를 볼드 밖으로 뺀다 |
| `Mermaid` | 흐름도·상태도·관계도 | 정적인 단계·관계 표현 |
| `Recharts` | 차트 (막대·선·산점도) | 데이터 비교·분포 |
| `JSXGraph` | 수학 그래프 | 함수·벡터·도형 직접 조작 |
| `React Flow` | 노드 이동 흐름도 | 사용자가 노드를 움직여야 의미가 있을 때만 |
| `Sandpack` | 코드 실행 | JS·React 코드 실습 (`CodePlayground` 래퍼 사용) |

**주의:** 모든 패키지를 한 문서에 억지로 넣지 않는다. 정적인 흐름은 Mermaid, 조작이 필요한 경우만 React Flow를 선택한다.

---

## 콘텐츠 작성 원칙 — 초보자 친화적 문서

이 사이트의 독자는 **완전 입문자**(뉴비) 기준으로 작성한다.
처음 보는 사람이 막히지 않도록 다음을 **반드시** 포함한다.

### 용어·기호 설명 규칙

- **영단어가 처음 등장할 때** 한국어 뜻을 괄호로 함께 표기한다.
  - 예: `population`(모집단), `variance`(분산), `matrix`(행렬)
- **약어·축약어**는 처음 등장할 때 풀어서 쓴다.
  - 예: OS(Operating System, 운영체제), SQL(Structured Query Language)
- **수학·통계 기호**는 처음 등장할 때 이름과 읽는 법을 설명한다.
  - 예: $\mu$ (뮤, 평균), $\sigma$ (시그마, 표준편차), $\Sigma$ (대문자 시그마, 합계)
- **공식·식**은 각 변수가 무엇을 뜻하는지 바로 아래에 항목별로 설명한다.

### 이론 설명 규칙

- "왜 이게 필요한가"(동기)를 **먼저** 설명하고, 그다음 정의를 제시한다.
- 개념을 설명할 때 **비유·생활 예시**를 반드시 하나 이상 포함한다.
- 전문 용어가 나왔을 때 `> **쉽게 말하면:**` 한 줄 요약을 추가한다.
- 코드 예시는 최대한 짧고 직관적으로, 변수명도 한국어 의미가 바로 보이게 짓는다.
- `따로 검색해야 이해되는 문장`을 만들지 않는다.

### 설명 순서 기본 패턴

```
1. 왜 필요한가 (문제 상황·동기)
2. 쉽게 말하면 (한 줄 요약)
3. 정의 (용어 + 기호 설명 포함)
4. 작은 예시 (5~10개 데이터나 짧은 코드)
5. 계산 또는 적용 (중간 단계 생략 없이)
6. 결과 해석 (숫자로 끝내지 않고 말로 해석)
7. 자주 틀리는 점
```

### 예시

```mdx
## 분산(Variance)이란?

> **쉽게 말하면:** 데이터가 평균에서 얼마나 흩어져 있는지를 나타내는 숫자입니다.

점수가 [90, 91, 89]인 반과 [50, 90, 130]인 반이 있다고 하면,
두 반의 평균은 똑같이 90점이지만 두 번째 반의 점수가 훨씬 들쭉날쭉합니다.
이 들쭉날쭉한 정도를 숫자 하나로 표현한 것이 분산입니다.

$$
\sigma^2 = \frac{1}{N} \sum_{i=1}^{N} (x_i - \mu)^2
$$

- $\sigma^2$ (시그마 제곱): 분산 값
- $N$: 데이터 개수
- $x_i$: i번째 데이터 값
- $\mu$ (뮤): 평균
- $\sum$ (시그마): 모두 더하라는 기호
```

---

## 심화 작성 규칙 (2026-08-24 피드백 반영)

과거 세션에서 "얕다", "이해가 안 된다"는 피드백을 반복적으로 받았다. 아래 규칙은 그 피드백을 반영한 **필수** 기준이다.

### 개념 이름(약어)은 영단어 어원을 쪼개서 설명한다

HTML, CSS, DOM, API처럼 **약어로 된 핵심 개념 이름이 처음 등장할 때**는 각 영단어를 쪼개 뜻을 풀어준다.

```mdx
❌ HTML(HyperText Markup Language)은 웹 페이지의 구조를 담당합니다.

✅ | 단어 | 뜻 |
   |------|-----|
   | Hyper | 초월한, ~을 뛰어넘는 (다른 문서로 링크로 이동) |
   | Text | 글, 문서 |
   | Markup | mark(표시하다)+up → 표시를 붙이다 |
   | Language | 언어 |
   HTML = "문서를 뛰어넘어 연결하며, 표시를 붙이는 언어"
```

**주의:** 이 어원 분해는 **약어·핵심 개념 이름**에만 쓴다. "구조", "속성", "배치"처럼 흔한 한자어 일반 단어에 한자 풀이를 억지로 붙이지 않는다. 실제로 새로운 개념을 설명하는 자리에서만 쓴다.

### 이미 설치된 시각화 컴포넌트를 실제로 사용한다

산문·표로만 설명하지 말고, 상황에 맞는 컴포넌트를 적극 사용한다.

| 상황 | 컴포넌트 |
|------|----------|
| 실행 가능한 JS/React 코드 예제 | `<CodePlayground template="vanilla" \| "react" ... />` — 정적 코드블록 대신 사용 |
| 데이터 분포·비교 | `<DataBarChart data={[...]} label="..." unit="..." />` |
| 구조·흐름·계층 관계 | ` ```mermaid ` 코드펜스 (flowchart TD 등) — ASCII 아트 대신 |
| 집합 관계(교집합 등) | `<SetDiagram />` |
| 개념 간 관계도 | `<ConceptFlow />` |

CodePlayground 사용 예 (React):

```mdx
<CodePlayground
  template="react"
  activeFile="/App.js"
  showConsole
  label="설명"
  files={{
    '/App.js': `import { useState } from 'react'

export default function Counter() {
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(count + 1)}>{count}</button>
}`,
  }}
/>
```

### 수식은 한 줄에 여러 계산을 이어붙이지 않는다 (모바일 가로 스크롤 방지)

`\quad`로 두 개 이상의 독립된 계산을 한 줄에 이어 붙이면 모바일에서 가로 스크롤이 생긴다. 반드시 `$$` 블록을 나눈다.

```mdx
❌ $$\bar{x} = \frac{10+20+30}{3} = 20, \quad \bar{y} = \frac{...}{3} = 30$$

✅ $$
\bar{x} = \frac{10+20+30}{3} = 20
$$

$$
\bar{y} = \frac{...}{3} = 30
$$
```

### 선행(prerequisite) 파일은 본론과 내용이 겹치면 안 된다

과목 앞부분에 넣는 선행 개념 파일은, 뒤에 나올 본론 파일(예: 13번 "모집단과 표본", 19번 "모델링 절차")과 **주제가 겹치면 안 된다**. 선행 파일은 "그 본론을 읽기 위해 필요한 순수 사전 지식"(수식 읽는 법, 용어 지도 등)만 다룬다. 새 선행 파일을 만들기 전에 뒤 목차에 같은 주제가 이미 있는지 반드시 확인한다.

### 깊이 기준 — "요약본"이 아니라 "강의 한 챕터" 수준

- 표·불릿만으로 개념을 나열하고 끝내지 않는다. 왜 그런지(동기), 실제 숫자로 계산하는 과정, 흔히 틀리는 지점, 관련 개념과의 관계까지 포함한다.
- 공식이 나오면 반드시 작은 예시 데이터로 손으로 계산하는 과정을 단계별로 보여준다(중간 단계 생략 금지).
- 새 파일을 작성하기 전, 비슷한 형식의 기존 파일(예: 이 프로젝트의 `14_descriptive-statistics.mdx`, `20_linear-and-logistic-regression.mdx`)을 참고해서 그 정도 깊이를 기준으로 삼는다.

---

## 커밋 규칙

- 커밋 메시지에 `Co-Authored-By: Claude` 등 AI 기여자 표시 **절대 금지**
- 커밋 메시지 형식: `docs: 내용 설명`
- 과목 1개 완성 후 즉시 커밋·push
